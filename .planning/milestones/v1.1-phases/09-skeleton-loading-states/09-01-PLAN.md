---
phase: 09-skeleton-loading-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Skeleton.tsx
  - src/components/ui/Skeleton.test.ts
  - src/components/ui/index.ts
autonomous: true
requirements:
  - UI-05
tags:
  - ui
  - primitive
  - skeleton
  - loading

planner_decisions:
  - id: PD-01
    decision: "Skeleton.tsx imports `HTMLAttributes` via `import type { HTMLAttributes } from 'react'`; the component uses `Omit<HTMLAttributes<HTMLDivElement>, 'children'>` so consumers cannot pass children (skeleton bars are conceptually void elements)."
    rationale: "RESEARCH § Skeleton Primitive Shape locks this signature. Matches the type-only-import convention from Card.tsx (`import type { HTMLAttributes } from 'react'`)."
    resolves: "RESEARCH § Skeleton Primitive Shape (prop contract)"
  - id: PD-02
    decision: "Skeleton uses `export function` named export (NO `forwardRef`)."
    rationale: "Matches EmptyState.tsx:15 (composite primitive, no forwardRef). Card.tsx uses forwardRef because it's a layout primitive that consumers may need to measure; Skeleton bars are leaf placeholders that nobody measures. Mirror EmptyState's simpler signature."
    resolves: "RESEARCH § Skeleton Primitive Shape (convention reasoning)"
  - id: PD-03
    decision: "Skeleton renders with `role=\"status\"` AND `aria-label=\"Loading\"` AND `aria-busy=\"true\"` on the root `<div>`."
    rationale: "Minimum a11y per RESEARCH recommendation. Screen readers announce loading state without spamming when composed in groups. Per VALIDATION.md Wave 0 requirement: `role=\"status\"` for a11y."
    resolves: "VALIDATION.md § Wave 0 Requirements + RESEARCH § Skeleton Primitive Shape"
  - id: PD-04
    decision: "Skeleton.test.ts uses `.test.ts` extension (NOT `.tsx`) per vitest.config.ts `include: ['src/**/*.test.ts']` glob. JSX is expressed via `React.createElement(...)` — JSX literal is forbidden in `.ts` files (esbuild does not transform JSX inside `.ts`)."
    rationale: "Matches EmptyState.test.ts:1-4 (the only other UI test in the repo). Avoids touching vitest config mid-milestone."
    resolves: "RESEARCH § Validation Architecture + RESEARCH § Patterns to Mirror (Test framework)"
  - id: PD-05
    decision: "Barrel update in `src/components/ui/index.ts` adds TWO lines: (a) replace `export { EmptyState } from './EmptyState';` with `export { EmptyState, shouldShowEmptyState } from './EmptyState';`, and (b) append `export { Skeleton } from './Skeleton';`. Do NOT re-export `SkeletonProps` type (barrel symmetry — Button/Card/EmptyState do not re-export their Props types)."
    rationale: "Activates the `shouldShowEmptyState` predicate that 08-REVIEW IN-01 flagged as dead code. Plan 09-02 imports `shouldShowEmptyState` from `./ui` alongside `Skeleton`. The optional `SkeletonProps` re-export per RESEARCH Example 3 is skipped because no consumer in Phase 9 needs the type."
    resolves: "RESEARCH § Barrel updates + RESEARCH § Don't Hand-Roll (shouldShowEmptyState activation)"
  - id: PD-06
    decision: "Task 1 is RED (test file with all 7 tests, expected to fail because Skeleton.tsx does not exist yet). Task 2 is GREEN (Skeleton.tsx implementation makes tests pass). Task 3 is barrel update. Two atomic commits per project Phase 8 pattern: `test(09): add failing Skeleton tests` then `feat(09): implement Skeleton primitive`."
    rationale: "Mirrors Phase 8 RED→GREEN test discipline. Forces the test to actually fail first (proves the test runs) before the implementation lands."
    resolves: "RESEARCH § Patterns to Mirror (Test structure) + Project TDD detection heuristic"

must_haves:
  truths:
    - "A `Skeleton` primitive exists in `src/components/ui/` that renders a div with `animate-pulse` Tailwind class, `bg-slate-700` background, and `role=\"status\"` for screen readers (per D-02 + D-04 + VALIDATION Wave 0)"
    - "Skeleton accepts three variants: `'line'` (default — `h-4 w-full rounded`), `'card'` (`h-24 w-full rounded-xl`), and `'circle'` (`h-10 w-10 rounded-full`) — per D-02"
    - "Skeleton accepts overridable `width`, `height`, `rounded`, and `className` props (Tailwind-class strings) — per D-02"
    - "`shouldShowEmptyState` is re-exported from `src/components/ui/index.ts` alongside `EmptyState`, activating the predicate that 08-REVIEW IN-01 flagged as currently unused"
    - "`Skeleton` is exported from `src/components/ui/index.ts` so Plan 09-02 consumers can import via `import { Skeleton, shouldShowEmptyState } from './ui'`"
    - "Unit tests prove all three variants render correct Tailwind classes, props are appended, `animate-pulse` is present by default, and `role=\"status\"` is set — RED→GREEN discipline (test commit precedes implementation)"
    - "D-02: single Skeleton primitive in src/components/ui/ alongside Button/Input/Select/Textarea/Card/EmptyState; variants `'line' | 'card' | 'circle'`; configurable width/height/rounded; animation built in (no opt-out for v1)"
    - "D-04: Tailwind `animate-pulse` baked into the primitive — applied to the root `<div>`; no custom CSS, no keyframes, no new dependencies"
    - "D-08: NO `skeleton-loading` feature key registered in src/features.ts — Plan 09-01 does NOT modify src/features.ts (skeletons fall on the Phase 7 invisible-refactor side of the badge line)"
    - "D-11: Skeleton primitive lives in src/components/ui/ (excluded from `lint:no-raw-html` scan via `startsWith('src/components/ui')`); the primitive's render output uses only `<div>` — no raw `<button>`, `<input>`, `<select>`, or `<textarea>`"
  artifacts:
    - path: "src/components/ui/Skeleton.tsx"
      provides: "Skeleton primitive — variants line/card/circle, width/height/rounded/className overrides, animate-pulse built in, role=\"status\" for a11y"
      exports: ["Skeleton", "SkeletonProps"]
      contains_classes: ["bg-slate-700", "animate-pulse", "h-4 w-full rounded", "h-24 w-full rounded-xl", "h-10 w-10 rounded-full"]
    - path: "src/components/ui/Skeleton.test.ts"
      provides: "Unit tests for Skeleton primitive — 7 tests covering default render, three variants, dimension props, rounded override, className append"
      contains_test_ids: ["UI-05 default render", "UI-05 line variant", "UI-05 card variant", "UI-05 circle variant", "UI-05 dimension props", "UI-05 rounded override", "UI-05 className override"]
    - path: "src/components/ui/index.ts"
      provides: "Top-level primitives barrel — adds Skeleton named export + shouldShowEmptyState re-export"
      contains: "export { Skeleton } from './Skeleton'"
  key_links:
    - from: "src/components/ui/index.ts"
      to: "src/components/ui/Skeleton.tsx"
      via: "barrel re-export"
      pattern: "export \\{ Skeleton \\} from './Skeleton'"
    - from: "src/components/ui/index.ts"
      to: "src/components/ui/EmptyState.tsx (shouldShowEmptyState predicate)"
      via: "barrel re-export"
      pattern: "export \\{ EmptyState, shouldShowEmptyState \\} from './EmptyState'"
    - from: "src/components/ui/Skeleton.test.ts"
      to: "src/components/ui/Skeleton.tsx"
      via: "import for testing"
      pattern: "import \\{ Skeleton \\} from './Skeleton'"
---

<objective>
Ship the `Skeleton` primitive, its unit tests, and the barrel update that re-exports `Skeleton` + `shouldShowEmptyState`. This plan is the foundation that Plan 02 wires into AssetLibrary, JobsManager, and PrinterSettings.

Purpose: A reviewable foundation pass. Plan 02 cannot fail in a way that the Wave 0 tests in this plan don't catch first (rendered variant classes + `animate-pulse` + `role="status"`).

Output: 3 files created/modified; one vitest suite with 7 passing Skeleton tests; lint guard + tsc -b green; build/lint pass with the primitive present but unused.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-skeleton-loading-states/09-CONTEXT.md
@.planning/phases/09-skeleton-loading-states/09-RESEARCH.md
@.planning/phases/09-skeleton-loading-states/09-VALIDATION.md

<interfaces>
<!-- Locked contracts the executor needs. The full source is in RESEARCH.md § Skeleton Primitive Shape + § Code Examples 1-2; this is a fast-lookup summary. -->

SkeletonProps (locked — RESEARCH § Skeleton Primitive Shape):
- variant?: 'line' | 'card' | 'circle' (default 'line')
- width?: string  (Tailwind class, e.g., 'w-24', 'w-1/2')
- height?: string (Tailwind class, e.g., 'h-4', 'h-12')
- rounded?: string (override default radius, e.g., 'rounded-full', 'rounded-xl')
- className?: string (final escape hatch, appended last)
- Extends Omit<HTMLAttributes<HTMLDivElement>, 'children'>

Variant default styles (locked — RESEARCH § Skeleton Primitive Shape):
- line:   'h-4 w-full rounded'
- card:   'h-24 w-full rounded-xl'
- circle: 'h-10 w-10 rounded-full'

Built-in classes on every Skeleton root:
- bg-slate-700 (bar color, matches the inner row background of real Card rows)
- animate-pulse (D-04)
- role="status" aria-label="Loading" aria-busy="true" (PD-03)

Existing barrel (src/components/ui/index.ts — currently 6 lines):
  export { Button, ButtonLink, getButtonClasses } from './Button';
  export { Input } from './Input';
  export { Select } from './Select';
  export { Textarea } from './Textarea';
  export { Card } from './Card';
  export { EmptyState } from './EmptyState';

Existing test analog (src/components/ui/EmptyState.test.ts — imports lines 1-4):
  import { describe, it, expect } from 'vitest';
  import * as React from 'react';
  import { renderToStaticMarkup } from 'react-dom/server';
  import { EmptyState, shouldShowEmptyState } from './EmptyState';

shouldShowEmptyState predicate (existing — src/components/ui/EmptyState.tsx:34-36):
- Already implemented + tested in Phase 8 (EmptyState.test.ts:8-25).
- Phase 9 only ADDS it to the barrel — does NOT modify EmptyState.tsx or EmptyState.test.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write failing Skeleton unit tests</name>
  <files>src/components/ui/Skeleton.test.ts</files>
  <read_first>
    - src/components/ui/EmptyState.test.ts (the only existing UI test — pattern analog for `.test.ts` extension + vitest import style + `React.createElement` JSX-via-createElement convention + leading `// --- UI-XX …` line-comment IDs)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Code Examples Example 2 (the canonical 7-test list for Skeleton — copy the test bodies verbatim)
    - .planning/phases/09-skeleton-loading-states/09-VALIDATION.md (Wave 0 requirements + per-task verification map — confirms 7 tests cover all variants + role + props + className)
    - vitest.config.ts (confirm include glob is `src/**/*.test.ts` — extension MUST be `.test.ts`, NOT `.test.tsx`)
  </read_first>
  <behavior>
    - Test 1 (UI-05 default render): `renderToStaticMarkup(React.createElement(Skeleton))` produces HTML containing `animate-pulse`, `bg-slate-700`, and `role="status"`
    - Test 2 (UI-05 line variant): `renderToStaticMarkup(React.createElement(Skeleton, { variant: 'line' }))` produces HTML containing `h-4` and `rounded`
    - Test 3 (UI-05 card variant): `renderToStaticMarkup(React.createElement(Skeleton, { variant: 'card' }))` produces HTML containing `h-24` and `rounded-xl`
    - Test 4 (UI-05 circle variant): `renderToStaticMarkup(React.createElement(Skeleton, { variant: 'circle' }))` produces HTML containing `rounded-full`
    - Test 5 (UI-05 dimension props): `renderToStaticMarkup(React.createElement(Skeleton, { width: 'w-32', height: 'h-6' }))` produces HTML containing `w-32` and `h-6`
    - Test 6 (UI-05 rounded override): `renderToStaticMarkup(React.createElement(Skeleton, { rounded: 'rounded-2xl' }))` produces HTML containing `rounded-2xl`
    - Test 7 (UI-05 className override): `renderToStaticMarkup(React.createElement(Skeleton, { className: 'my-custom-class' }))` produces HTML containing `my-custom-class`
  </behavior>
  <action>
    CRITICAL — JSX FORBIDDEN IN .test.ts FILE: this test file uses the `.test.ts` extension (per PD-04 and the vitest include glob `src/**/*.test.ts`). esbuild does NOT transform JSX inside `.ts` files — JSX literal will fail at parse time. Every React element in this file MUST be constructed via `React.createElement(...)` function calls. The file imports React with `import * as React from 'react'`. Do NOT write `<Skeleton ... />` syntax anywhere. This mirrors EmptyState.test.ts.

    Create `src/components/ui/Skeleton.test.ts` (extension `.test.ts` — NOT `.tsx`). Imports MUST be exactly:
      `import { describe, it, expect } from 'vitest';`
      `import * as React from 'react';`
      `import { renderToStaticMarkup } from 'react-dom/server';`
      `import { Skeleton } from './Skeleton';`

    The `from './Skeleton'` import will fail to resolve at this point because Skeleton.tsx does NOT yet exist — THIS IS EXPECTED AND DESIRED for RED phase. The test file must be created BEFORE Task 2 implements Skeleton.tsx.

    Wrap all assertions in one `describe('Skeleton', () => { … })` block. Each `it` block starts with a line comment `// --- UI-05 …` matching the EmptyState.test.ts convention. Express all React element construction via `React.createElement(Skeleton, { ... })` per PD-04 and the JSX-FORBIDDEN warning above. Implement all seven tests from the `<behavior>` block using this construction style.

    Test 1 MUST assert all three behaviors on a single render (default `animate-pulse`, `bg-slate-700`, and `role="status"`) so the test is meaningful — do NOT split into three trivial tests. Use `expect(html).toContain(...)` for class names. Use `expect(html).toMatch(/role="status"/)` for the role attribute (matches the regex form from RESEARCH Example 2 line 1052).

    Commit this file with message `test(09): add failing Skeleton tests` BEFORE proceeding to Task 2. The commit proves the RED state — `npm test` MUST fail with a clear "Cannot find module './Skeleton'" or "Skeleton is not defined" error.
  </action>
  <verify>
    <automated>test -f src/components/ui/Skeleton.test.ts</automated>
    <automated>npm test 2>&1 | grep -E "(Cannot find module|Skeleton|FAIL)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/Skeleton.test.ts` exists with extension `.test.ts` (NOT `.tsx`).
    - `grep -E "describe\\('Skeleton'" src/components/ui/Skeleton.test.ts` returns 1 match.
    - `grep -cE "\\bit\\(" src/components/ui/Skeleton.test.ts` returns at least 7 (seven test cases — format-agnostic, tolerant of any indent style).
    - `grep -cE "React.createElement" src/components/ui/Skeleton.test.ts` returns at least 7 (PD-04 — JSX via createElement, one per test minimum).
    - `grep -cE "<Skeleton[^a-zA-Z]" src/components/ui/Skeleton.test.ts | grep -v '^#'` returns 0 (PD-04 — NO JSX literal of `<Skeleton ... />`; the executor uses `React.createElement(Skeleton, ...)` exclusively).
    - `grep -E "renderToStaticMarkup" src/components/ui/Skeleton.test.ts` returns at least 7 (one per test).
    - `grep -E "from './Skeleton'" src/components/ui/Skeleton.test.ts` returns 1 match (the import that triggers RED).
    - `grep -E "animate-pulse" src/components/ui/Skeleton.test.ts` returns at least 1 match (Test 1 asserts the class).
    - `grep -E "role=\"status\"" src/components/ui/Skeleton.test.ts` returns at least 1 match (PD-03 — Test 1 asserts the role).
    - `npm test` exits NON-ZERO. The failure output must reference Skeleton — either "Cannot find module './Skeleton'" (TS/Vite resolution error) or "Skeleton is not defined" (runtime). This proves the RED state.
    - The test file is committed with message `test(09): add failing Skeleton tests` BEFORE Task 2 begins.
  </acceptance_criteria>
  <done>Seven failing Skeleton tests committed in RED state — proves the test runs and fails because Skeleton.tsx does not yet exist. Pre-existing EmptyState + threeMfParser tests still parse (their `npm test` failures are limited to the new Skeleton suite).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement Skeleton primitive to make tests pass</name>
  <files>src/components/ui/Skeleton.tsx</files>
  <read_first>
    - src/components/ui/Card.tsx (pattern analog — type-only import via `import type { HTMLAttributes } from 'react'`; named export style; className composition with template literal)
    - src/components/ui/EmptyState.tsx (pattern analog — composite primitive with `export function`, no forwardRef per PD-02)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Skeleton Primitive Shape (prop contract + convention reasoning + variant styles table)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Code Examples Example 1 (the full canonical Skeleton.tsx — copy verbatim per PD-01)
    - .planning/phases/09-skeleton-loading-states/09-CONTEXT.md (D-02 + D-04 + D-11 — primitive shape, animation, lint exclusion)
    - src/components/ui/Skeleton.test.ts (the 7 tests this implementation must make pass — read AFTER creating the test file in Task 1)
  </read_first>
  <action>
    Create `src/components/ui/Skeleton.tsx` exporting:

    1. A type-only import of `HTMLAttributes` from React: `import type { HTMLAttributes } from 'react';` (PD-01 — matches Card.tsx's import style).

    2. A local type alias (NOT exported): `type SkeletonVariant = 'line' | 'card' | 'circle';` (matches Card.tsx's local `CardVariant` pattern at Card.tsx:4).

    3. `export interface SkeletonProps extends Omit<HTMLAttributes<HTMLDivElement>, 'children'>` with these optional fields IN THIS ORDER: `variant?: SkeletonVariant; width?: string; height?: string; rounded?: string;`. The `className` field is inherited from `HTMLAttributes` — do NOT redeclare it. Per PD-01, the `Omit<…, 'children'>` is mandatory because Skeleton bars are conceptually void.

    4. A local `const variantStyles: Record<SkeletonVariant, string>` literal at module scope (NOT exported) with EXACTLY these three entries (verbatim per RESEARCH § Skeleton Primitive Shape):
       - `line:   'h-4 w-full rounded'`
       - `card:   'h-24 w-full rounded-xl'`
       - `circle: 'h-10 w-10 rounded-full'`

    5. `export function Skeleton({ variant = 'line', width, height, rounded, className = '', ...props }: SkeletonProps)` returning a single `<div>` with:
       - `role="status"` attribute (PD-03)
       - `aria-label="Loading"` attribute (PD-03)
       - `aria-busy="true"` attribute (PD-03)
       - `className={\`bg-slate-700 animate-pulse ${variantStyles[variant]} ${width ?? ''} ${height ?? ''} ${rounded ?? ''} ${className}\`.trim()}` — exact composition order from RESEARCH Example 1: base classes (`bg-slate-700 animate-pulse`) first, then variant default, then overrides, then user className, with trailing `.trim()` (matches the Card.tsx:31 / Input.tsx:27 convention)
       - `{...props}` spread LAST so consumers' explicit attributes (e.g., `aria-label="Loading materials"`) take precedence

    Do NOT use `forwardRef` (PD-02 — matches EmptyState.tsx:15). Do NOT add a `displayName` line (forwardRef-specific; named function exports get their name automatically). Do NOT export the `SkeletonVariant` type alias (it's internal). Do NOT export `variantStyles` (it's internal).

    Run `npm test` after writing. ALL 7 Skeleton tests MUST pass. If any test fails, fix the implementation — do NOT modify the test file (the test contract is locked in Task 1).

    Commit this file with message `feat(09): implement Skeleton primitive`.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>npx tsc -b</automated>
    <automated>npm run lint:no-raw-html</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/Skeleton.tsx` exists.
    - `grep -E "^import type \\{ HTMLAttributes \\} from 'react'" src/components/ui/Skeleton.tsx` returns 1 match (PD-01 — type-only import).
    - `grep -E "^type SkeletonVariant = 'line' \\| 'card' \\| 'circle'" src/components/ui/Skeleton.tsx` returns 1 match (variant union — NOT exported).
    - `grep -E "^export interface SkeletonProps" src/components/ui/Skeleton.tsx` returns 1 match.
    - `grep -E "Omit<HTMLAttributes<HTMLDivElement>, 'children'>" src/components/ui/Skeleton.tsx` returns 1 match (PD-01 — children omitted).
    - `grep -E "^export function Skeleton\\(" src/components/ui/Skeleton.tsx` returns 1 match.
    - `grep -cE "^export " src/components/ui/Skeleton.tsx` returns exactly 2 (the interface + the function — `SkeletonVariant` and `variantStyles` are NOT exported).
    - `grep -cE "forwardRef|displayName" src/components/ui/Skeleton.tsx` returns 0 (PD-02 — no forwardRef, no displayName).
    - `grep -E "role=\"status\"" src/components/ui/Skeleton.tsx` returns 1 match (PD-03).
    - `grep -E "aria-label=\"Loading\"" src/components/ui/Skeleton.tsx` returns 1 match (PD-03).
    - `grep -E "aria-busy=\"true\"" src/components/ui/Skeleton.tsx` returns 1 match (PD-03).
    - `grep -E "animate-pulse" src/components/ui/Skeleton.tsx` returns 1 match (D-04 — Tailwind animation baked in).
    - `grep -E "bg-slate-700" src/components/ui/Skeleton.tsx` returns 1 match (bar background).
    - `grep -E "h-4 w-full rounded" src/components/ui/Skeleton.tsx` returns 1 match (line variant).
    - `grep -E "h-24 w-full rounded-xl" src/components/ui/Skeleton.tsx` returns 1 match (card variant).
    - `grep -E "h-10 w-10 rounded-full" src/components/ui/Skeleton.tsx` returns 1 match (circle variant).
    - `grep -E "variantStyles\\[variant\\]" src/components/ui/Skeleton.tsx` returns 1 match (lookup pattern).
    - `grep -E "\\.trim\\(\\)" src/components/ui/Skeleton.tsx` returns 1 match (className composition convention).
    - File ends with `{...props}` spread on the `<div>` AFTER the locked attributes — line-order check via `grep -nE 'aria-busy=\"true\"|\\{\\.\\.\\.props\\}' src/components/ui/Skeleton.tsx | head` shows the aria-busy line number BEFORE the `{...props}` line number.
    - `grep -v '^#' src/components/ui/Skeleton.tsx | grep -cE "<button|<input|<select|<textarea"` returns 0 (D-11 — no raw form elements; primitive uses `<div>` only).
    - `npm test` exits 0 — ALL 7 Skeleton tests pass AND pre-existing EmptyState + threeMfParser tests still pass.
    - `npx tsc -b` exits 0 (strict TypeScript including `noUnusedLocals`, `noUnusedParameters` — proves no dead identifiers introduced).
    - `npm run lint:no-raw-html` exits 0 (Skeleton.tsx is correctly excluded by `startsWith('src/components/ui')` per D-11).
    - The implementation file is committed with message `feat(09): implement Skeleton primitive` AFTER Task 1's RED commit.
  </acceptance_criteria>
  <done>Skeleton primitive ships with the locked render contract (3 variants + dimension overrides + className escape hatch + `role="status"` a11y + `animate-pulse` baked in). All 7 unit tests pass GREEN. TypeScript strict mode green; lint guard green; pre-existing tests untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Update primitives barrel — add Skeleton + re-export shouldShowEmptyState</name>
  <files>src/components/ui/index.ts</files>
  <read_first>
    - src/components/ui/index.ts (existing barrel — full file is 6 lines; preserve exact one-export-per-line style)
    - src/components/ui/EmptyState.tsx (line 34-36 — confirms `shouldShowEmptyState` is already a named export from this file; just needs to be added to the barrel re-export)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Code Examples Example 3 (the canonical barrel update — copy verbatim per PD-05)
    - .planning/phases/09-skeleton-loading-states/09-CONTEXT.md (D-11 — barrel update preserves the lint guard exclusion contract; D-08 — barrel does NOT register any feature key)
  </read_first>
  <action>
    Modify `src/components/ui/index.ts` with exactly TWO edits per PD-05:

    1. Replace the existing line 6 — `export { EmptyState } from './EmptyState';` — with `export { EmptyState, shouldShowEmptyState } from './EmptyState';`. The `shouldShowEmptyState` predicate already exists in EmptyState.tsx:34-36 (Phase 8 exported it from that file but never added it to the barrel — 08-REVIEW IN-01 flagged this). Adding it here activates the predicate as part of the canonical `./ui` surface so Plan 09-02 consumers can import it alongside `Skeleton`.

    2. Append ONE new line after the (now-modified) EmptyState export line: `export { Skeleton } from './Skeleton';`.

    Do NOT add `export type { SkeletonProps } from './Skeleton';` (PD-05 — barrel symmetry with Button/Card/EmptyState which do not re-export their Props types; the optional re-export shown in RESEARCH Example 3 is skipped because no consumer in Phase 9 needs the type at import time).

    Do NOT modify any other line in the file. Do NOT reorder the existing Button/Input/Select/Textarea/Card exports.

    Final file MUST be exactly 7 lines (one per export). Verify visually before committing.

    Commit this file with message `feat(09): export Skeleton + shouldShowEmptyState from ui barrel`.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
    <automated>npm run lint:no-raw-html</automated>
    <automated>npm test</automated>
    <automated>grep -cE "^export \\{ " src/components/ui/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^export \\{ Skeleton \\} from './Skeleton';" src/components/ui/index.ts` returns exactly 1.
    - `grep -cE "^export \\{ EmptyState, shouldShowEmptyState \\} from './EmptyState';" src/components/ui/index.ts` returns exactly 1.
    - `grep -cE "EmptyState" src/components/ui/index.ts` returns 1 (single EmptyState export line; not duplicated).
    - `grep -cE "SkeletonProps" src/components/ui/index.ts` returns 0 (PD-05 — props type NOT re-exported through barrel).
    - `grep -cE "^export \\{ Button" src/components/ui/index.ts` returns 1 (existing Button export preserved).
    - `grep -cE "^export \\{ Input \\} from './Input';" src/components/ui/index.ts` returns 1 (existing Input export preserved).
    - `grep -cE "^export \\{ Select \\} from './Select';" src/components/ui/index.ts` returns 1 (existing Select export preserved).
    - `grep -cE "^export \\{ Textarea \\} from './Textarea';" src/components/ui/index.ts` returns 1 (existing Textarea export preserved).
    - `grep -cE "^export \\{ Card \\} from './Card';" src/components/ui/index.ts` returns 1 (existing Card export preserved).
    - `wc -l < src/components/ui/index.ts` returns 7 (one line per export — the file is exactly 7 lines).
    - `npx tsc -b` exits 0 (the barrel changes compile against Skeleton.tsx and EmptyState.tsx exports).
    - `npm run lint:no-raw-html` exits 0 (no raw form elements introduced).
    - `npm test` exits 0 (all 3 test suites pass: threeMfParser + EmptyState + Skeleton).
    - The barrel update is committed with message `feat(09): export Skeleton + shouldShowEmptyState from ui barrel`.
  </acceptance_criteria>
  <done>`Skeleton` is importable from `./ui` via `import { Skeleton } from './ui'`. `shouldShowEmptyState` is importable from `./ui` via `import { shouldShowEmptyState } from './ui'` — activating the predicate that 08-REVIEW IN-01 flagged as currently unused. Plan 09-02 can now import both alongside `EmptyState` in a single line. Build chain green.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the full Phase 9 Plan 01 sampling suite:

1. `npm test` — vitest exits 0 (existing threeMfParser test + Phase 8 EmptyState tests + new Skeleton tests; 7 Skeleton tests pass).
2. `npx tsc -b` — strict TypeScript exits 0.
3. `npm run lint:no-raw-html` — exits 0 (Skeleton.tsx + Skeleton.test.ts correctly excluded by `startsWith('src/components/ui')`).
4. `npm run build` — full build chain green (`node scripts/lint-no-raw-html.mjs && tsc -b && vite build`).
5. `grep -E "Skeleton|skeleton-loading" src/features.ts` returns 0 matches — D-08 honored, no feature key registered.

If all five pass, the foundation is solid and Plan 09-02 can wire it in.
</verification>

<success_criteria>
- Skeleton primitive renders the locked contract from RESEARCH § Skeleton Primitive Shape (verified by 7 unit tests in Skeleton.test.ts).
- All three variants (`line`, `card`, `circle`) produce the locked default Tailwind classes per D-02.
- `animate-pulse` is built into the primitive root per D-04 — verified by Test 1.
- `role="status"`, `aria-label="Loading"`, `aria-busy="true"` are present on the root `<div>` per PD-03 — verified by Test 1.
- `Skeleton` is exported from `./ui` so Plan 09-02 can import it.
- `shouldShowEmptyState` is re-exported from `./ui` — activates the predicate that 08-REVIEW IN-01 flagged as dead code.
- `npm test`, `npx tsc -b`, `npm run lint:no-raw-html`, and `npm run build` all exit 0.
- D-08 honored: NO `skeleton-loading` feature key in `src/features.ts` (file untouched in this plan).
- D-11 honored: lint guard green; Skeleton primitive composes only `<div>` (no raw form elements).
- Pre-existing tests untouched: EmptyState.test.ts (7 tests) and threeMfParser.test.ts still pass.
- Two atomic commits per RED→GREEN discipline: `test(09): add failing Skeleton tests` THEN `feat(09): implement Skeleton primitive`, plus the barrel update commit.
</success_criteria>

<output>
Create `.planning/phases/09-skeleton-loading-states/09-01-SUMMARY.md` when done. Include:
- Final list of files created/modified with line counts (Skeleton.tsx, Skeleton.test.ts, index.ts)
- vitest output (7 Skeleton tests + 7 EmptyState tests + threeMfParser tests all passing)
- Confirmation that Task 1's RED commit landed BEFORE Task 2's GREEN commit (git log shows the order)
- Any PD-XX decisions revised during execution (e.g., if PD-03 a11y attributes needed adjustment to satisfy a test)
- Note for Plan 02: confirm `import { Skeleton, shouldShowEmptyState, EmptyState } from './ui'` works as a single-line three-symbol import (no separate import lines needed)
</output>
</content>
</invoke>