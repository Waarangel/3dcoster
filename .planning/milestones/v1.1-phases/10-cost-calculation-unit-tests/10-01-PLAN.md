---
phase: 10-cost-calculation-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/costCalc.ts
autonomous: true
requirements:
  - TEST-01
tags:
  - testing
  - cost-calculation
  - pure-module-extraction

must_haves:
  truths:
    - "src/utils/costCalc.ts exists as a pure module with zero React imports"
    - "calculateCost(input: CalcInput): CostBreakdown is exported and returns the exact CostBreakdown contract from src/types.ts:116"
    - "Six named sub-helpers are exported: calculateFilamentCost, calculateElectricityCost, calculateDepreciation, calculateNozzleWear, calculateLabor, calculateAmortization"
    - "Failure-rate clamp Math.min(Math.max(failureRate, 0), 99) lives INSIDE calculateCost (per D-02)"
    - "Module compiles cleanly under tsc -b with zero new errors"
    - "Math semantics are byte-for-byte identical to the useMemo at CostCalculator.tsx:374-446"
  artifacts:
    - path: "src/utils/costCalc.ts"
      provides: "Pure cost-calculation module with CalcInput type, 6 sub-helpers, and calculateCost entrypoint"
      contains: "export function calculateCost"
      min_lines: 80
  key_links:
    - from: "src/utils/costCalc.ts"
      to: "src/types.ts"
      via: "import type { CostBreakdown, Material, PrinterConfig, PrinterInstance, ElectricityConfig, MaterialUsage }"
      pattern: "import type.*from '\\.\\./types'"
    - from: "src/utils/costCalc.ts"
      to: "src/utils/gcodeParser.ts"
      via: "getMaterialDensity import for calculateNozzleWear"
      pattern: "import.*getMaterialDensity.*from './gcodeParser'"
---

<objective>
Create the pure `src/utils/costCalc.ts` module containing the `CalcInput` interface, six named sub-helpers (one per cost factor), and the coarse `calculateCost(input: CalcInput): CostBreakdown` entrypoint. This plan extracts NOTHING from `CostCalculator.tsx` yet — it stands up the new module compiling cleanly so Plan 02 (refactor consumer) and Plan 03 (tests) can run in parallel against a stable interface.

Purpose: Isolate the cost math behind a pure-function boundary so it becomes (a) testable without React/IndexedDB and (b) protected from silent regression by Plan 04's coverage gate. Establishes the testable seam without changing runtime behavior.

Output: A new pure utility module that mirrors the existing `useMemo((): CostBreakdown => {…})` body at `src/components/CostCalculator.tsx:374-446` with byte-for-byte identical math semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cost-calculation-unit-tests/10-CONTEXT.md
@.planning/phases/10-cost-calculation-unit-tests/10-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/types.ts:116-129 (LOCKED — every consumer field must be preserved):
```typescript
export interface CostBreakdown {
  filament: number;
  electricity: number;
  printerDepreciation: number;
  nozzleWear: number;
  modelAmortization: number;
  materials: number;
  labor: number;
  subtotal: number;
  failureAdjusted: number;
  profitMargin: number;
  targetProfit: number;
  sellingPrice: number;
}
```

From src/types.ts:103-110:
```typescript
export interface MaterialUsage {
  materialId: string;
  quantity: number;
}

export interface ElectricityConfig {
  costPerKwh: number;
}
```

From src/utils/gcodeParser.ts:64 (already exported, reuse — do NOT re-implement):
```typescript
export function getMaterialDensity(filamentType: string | null): number
```

From src/components/CostCalculator.tsx:47-52 (internal FilamentRow shape that the useMemo consumes — costCalc must accept a structurally compatible row shape):
```typescript
interface FilamentRow {
  filamentId: string;
  grams: number;
  editedPrice: number;
  editedCurrency: Currency;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/utils/costCalc.ts with CalcInput interface and module skeleton</name>
  <files>src/utils/costCalc.ts</files>
  <read_first>
    - src/components/CostCalculator.tsx (lines 1-10 for imports; lines 47-52 for FilamentRow shape; lines 374-452 for the useMemo body and dependency array — this is the math contract)
    - src/types.ts (lines 80-150 for CostBreakdown, MaterialUsage, ElectricityConfig, Material, PrinterConfig, PrinterInstance shapes)
    - src/utils/gcodeParser.ts (lines 1-80 — module-shape analog and the getMaterialDensity export this module will import)
    - .planning/phases/10-cost-calculation-unit-tests/10-PATTERNS.md (section "src/utils/costCalc.ts" — the math source excerpts and import patterns)
    - .planning/phases/10-cost-calculation-unit-tests/10-CONTEXT.md (D-01, D-02, D-04 for scope; D-13 for which guards exist)
  </read_first>
  <behavior>
    - The exported CalcInput interface fields cover every variable the useMemo at CostCalculator.tsx:374 reads from closure: filamentRows, printTimeHours, selectedPrinter, selectedInstance, electricity, materials, materialsUsed, prepTimeMinutes, postProcessingMinutes, laborHourlyRate, failureRate, profitMarginPercent, targetProfit, sellingPrice, modelCost, modelCostPerUnit
    - All 12 fields of CostBreakdown (filament, electricity, printerDepreciation, nozzleWear, modelAmortization, materials, labor, subtotal, failureAdjusted, profitMargin, targetProfit, sellingPrice) are returned by calculateCost — none renamed, none added, none removed
    - Module is pure: no React imports, no Dexie/IndexedDB imports, no sessionStorage, no Date.now(), no Math.random()
    - tsc -b passes cleanly with the new file present
  </behavior>
  <action>
    Create new file `src/utils/costCalc.ts`. Top of file: import `type { CostBreakdown, Material, PrinterConfig, PrinterInstance, ElectricityConfig, MaterialUsage, Currency } from '../types'` and import `{ getMaterialDensity } from './gcodeParser'`. Define and export `interface CalcInput` mirroring every closure variable read by the useMemo at CostCalculator.tsx:374-452 — fields: `filamentRows: Array<{ filamentId: string; grams: number; editedPrice: number; editedCurrency: Currency }>`, `printTimeHours: number`, `selectedPrinter: PrinterConfig | undefined`, `selectedInstance: PrinterInstance | undefined`, `electricity: ElectricityConfig`, `materials: Material[]`, `materialsUsed: MaterialUsage[]`, `prepTimeMinutes: number`, `postProcessingMinutes: number`, `laborHourlyRate: number`, `failureRate: number`, `profitMarginPercent: number`, `targetProfit: number`, `sellingPrice: number`, `modelCost: number`, `modelCostPerUnit: boolean`. Field-level optionality must match what the useMemo treats as nullable (use `?? 0`/`?? null` callsites in CostCalculator.tsx:388-393 as the guide — selectedPrinter and selectedInstance are nullable). Add no implementation bodies yet — function stubs only with `throw new Error('not implemented')` are acceptable, but prefer wiring up the actual bodies in Task 2 within the same task scope is also fine; if you choose stubs, mark each with a TODO referencing Task 2. Do NOT introduce a separate CostBreakdown re-export — consumers continue importing from `'../types'`.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/plan10-01-task1-tsc.log; grep -c "error TS" /tmp/plan10-01-task1-tsc.log | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/utils/costCalc.ts` exists
    - File contains `export interface CalcInput` with all 16 fields listed above
    - File imports `getMaterialDensity` from `'./gcodeParser'`
    - File imports types from `'../types'`
    - File has zero React imports (`grep -c "from 'react'" src/utils/costCalc.ts` returns 0)
    - `npx tsc -b` exits 0 with no new TS errors introduced
  </acceptance_criteria>
  <done>The new module file exists, the CalcInput contract is declared, and the type-only skeleton compiles. Implementation bodies are filled in by Task 2 (or this same task if combined).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 6 sub-helpers and calculateCost entrypoint with byte-for-byte math preservation</name>
  <files>src/utils/costCalc.ts</files>
  <read_first>
    - src/utils/costCalc.ts (the skeleton from Task 1)
    - src/components/CostCalculator.tsx (lines 374-452 — the canonical math source; copy semantics, not syntax)
    - .planning/phases/10-cost-calculation-unit-tests/10-PATTERNS.md (section "Math source — the useMemo to extract" — every sub-block excerpt is reproduced there)
    - .planning/phases/10-cost-calculation-unit-tests/10-CONTEXT.md (D-02 for API shape; D-13 fourth bullet "Defensive guards" — extract only guards that exist, do not invent new ones)
  </read_first>
  <behavior>
    - `calculateFilamentCost(filamentRows)` returns `Σ row.grams * row.editedPrice` for rows where `row.filamentId` is truthy and `row.editedPrice > 0`; rows failing the guard contribute 0 (matches CostCalculator.tsx:376-379)
    - `calculateElectricityCost(printTimeHours, selectedPrinter, electricity)` returns 0 when `selectedPrinter` is undefined, otherwise `(selectedPrinter.wattage / 1000) * printTimeHours * electricity.costPerKwh` (matches CostCalculator.tsx:382-384)
    - `calculateDepreciation(printTimeHours, selectedPrinter, selectedInstance)` computes `purchasePrice = selectedInstance?.actualPurchasePrice ?? selectedPrinter?.purchasePrice ?? 0`, `recoveryMonths = selectedInstance?.recoveryMonths ?? 12`, `monthlyHours = selectedInstance?.estimatedMonthlyPrintHours ?? 40`, returns 0 if `recoveryMonths * monthlyHours <= 0`, else `(purchasePrice / (recoveryMonths * monthlyHours)) * printTimeHours` (matches CostCalculator.tsx:388-393)
    - `calculateNozzleWear(filamentRows, materials, selectedPrinter)` sums per-row `grams / getMaterialDensity(material?.filamentType ?? null)` over `grams > 0` rows, then returns 0 if `selectedPrinter` is undefined, else `(totalVolumeCm3 / selectedPrinter.nozzleLifespanCm3) * selectedPrinter.nozzleCost` (matches CostCalculator.tsx:397-405)
    - `calculateLabor(prepTimeMinutes, postProcessingMinutes, laborHourlyRate)` returns `((prepTimeMinutes + postProcessingMinutes) / 60) * laborHourlyRate` (matches CostCalculator.tsx:418-419)
    - `calculateAmortization(...)` returns 0 — this is the placeholder modelAmortization value at CostCalculator.tsx:408. Document that the field is reserved for future use and currently returns 0; keep the helper exported so the test plan can lock the contract.
    - `calculateCost(input)` orchestrates the six helpers, computes `perUnitModelCost = modelCostPerUnit ? modelCost : 0`, `perUnitSubtotal = filament + electricity + materials + labor + perUnitModelCost`, applies the failure clamp `Math.min(Math.max(failureRate, 0), 99)` and `failureMultiplier = 1 / (1 - clampedFailureRate / 100)` inline, computes `materialsCost = Σ usage.quantity * (material.costPerUnit ?? 0)` from `materialsUsed`, returns the full 12-field CostBreakdown with profitMargin/targetProfit/sellingPrice passed through from input
    - Failure-rate clamp lives INSIDE `calculateCost` (D-02) — NOT as a separate exported helper
  </behavior>
  <action>
    In `src/utils/costCalc.ts`, implement six exported named functions and the `calculateCost` entrypoint. Each helper follows the `export function name(args): number` pattern from `src/utils/gcodeParser.ts:64` (the `getMaterialDensity` shape). Above each function add a one-line comment naming the formula (matches gcodeParser commenting style). Helper signatures: `calculateFilamentCost(filamentRows: CalcInput['filamentRows']): number`, `calculateElectricityCost(printTimeHours: number, selectedPrinter: PrinterConfig | undefined, electricity: ElectricityConfig): number`, `calculateDepreciation(printTimeHours: number, selectedPrinter: PrinterConfig | undefined, selectedInstance: PrinterInstance | undefined): number`, `calculateNozzleWear(filamentRows: CalcInput['filamentRows'], materials: Material[], selectedPrinter: PrinterConfig | undefined): number`, `calculateLabor(prepTimeMinutes: number, postProcessingMinutes: number, laborHourlyRate: number): number`, `calculateAmortization(): number` (returns 0 — placeholder, document inline). Then implement `export function calculateCost(input: CalcInput): CostBreakdown` that calls each helper, computes `materialsCost` inline from `input.materialsUsed` and `input.materials`, applies the failure-rate clamp inline (`const clampedFailureRate = Math.min(Math.max(input.failureRate, 0), 99); const failureMultiplier = 1 / (1 - clampedFailureRate / 100); const failureAdjusted = perUnitSubtotal * failureMultiplier;`), and returns the full 12-field CostBreakdown matching CostCalculator.tsx:433-446 exactly. Field order in the returned object should match the source useMemo for diff cleanliness. Do NOT add new defensive branches (zero filaments, no printer, etc.) — only preserve what the useMemo already has. The math source per-block is in PATTERNS.md "Math source — the useMemo to extract" and at CostCalculator.tsx:374-446.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/plan10-01-task2-tsc.log; grep -c "error TS" /tmp/plan10-01-task2-tsc.log | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^export function (calculateFilamentCost|calculateElectricityCost|calculateDepreciation|calculateNozzleWear|calculateLabor|calculateAmortization|calculateCost)\\b" src/utils/costCalc.ts | wc -l` returns 7 (six sub-helpers + calculateCost)
    - `grep -c "Math.min(Math.max" src/utils/costCalc.ts` returns >= 1 (failure-rate clamp present inside the file)
    - `grep -c "getMaterialDensity" src/utils/costCalc.ts` returns >= 1 (nozzle wear uses the shared density lookup, not a hardcoded constant)
    - `grep -c "1.24\\|1.27\\|1.04" src/utils/costCalc.ts` returns 0 (no hardcoded density numbers leaked from gcodeParser)
    - `grep -c "from 'react'" src/utils/costCalc.ts` returns 0 (pure module — no React)
    - `npx tsc -b` exits 0 with zero new errors
    - The returned object literal in `calculateCost` contains all 12 CostBreakdown field names (verify by `grep -E "filament:|electricity:|printerDepreciation:|nozzleWear:|modelAmortization:|materials:|labor:|subtotal:|failureAdjusted:|profitMargin:|targetProfit:|sellingPrice:" src/utils/costCalc.ts | wc -l` returns >= 12)
  </acceptance_criteria>
  <done>All six sub-helpers and the `calculateCost` entrypoint are implemented in `src/utils/costCalc.ts`. Math semantics are identical to the useMemo at CostCalculator.tsx:374-446. The file compiles under `tsc -b`. No tests yet, no CostCalculator.tsx changes yet — those land in Plan 02 and Plan 03 respectively.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No new trust boundaries introduced this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-N/A | N/A | src/utils/costCalc.ts | accept | Pure-function extraction with no new inputs, no auth, no network, no user input parsing. Module reads typed inputs already validated upstream by React form state. No new attack surface. |

**Rationale:** Phase 10 extracts existing math from a React component into a pure module. No new IO, no new external dependencies (only the existing `getMaterialDensity` import), no user input parsing, no serialization boundaries crossed. Equivalent attack surface to the pre-extraction code.
</threat_model>

<verification>
- `npx tsc -b` exits 0 (no new type errors)
- `src/utils/costCalc.ts` exists and contains all 7 exports (6 sub-helpers + calculateCost)
- No raw density numbers (1.24, 1.27, 1.04) in the new file — all density lookups go through `getMaterialDensity`
- No React imports in the new file
- `grep -c "Math.min(Math.max" src/utils/costCalc.ts` >= 1 (failure-rate clamp lives inside calculateCost per D-02)
- Math semantics manually cross-checked against `src/components/CostCalculator.tsx:374-446` line by line (each `return` branch and each formula matches)
</verification>

<success_criteria>
1. `src/utils/costCalc.ts` exists as a pure utility module with `export interface CalcInput`, six exported sub-helpers, and `export function calculateCost(input: CalcInput): CostBreakdown`
2. Math semantics are byte-for-byte identical to `CostCalculator.tsx:374-446` — every formula, every guard, every nullable-fallback chain matches
3. Failure-rate clamp `Math.min(Math.max(failureRate, 0), 99)` lives inside `calculateCost`, not as a separate exported helper (D-02)
4. `npx tsc -b` exits 0 with no new TypeScript errors
5. Zero React imports in the new module (confirms pure-utility classification)
</success_criteria>

<output>
Create `.planning/phases/10-cost-calculation-unit-tests/10-01-SUMMARY.md` when done. Include the public API surface (function signatures), the `CalcInput` field list, and a one-line confirmation that math semantics match the source useMemo.
</output>
