---
phase: 10-cost-calculation-unit-tests
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/components/CostCalculator.tsx
autonomous: true
requirements:
  - TEST-01
tags:
  - testing
  - cost-calculation
  - integration

must_haves:
  truths:
    - "src/components/CostCalculator.tsx imports calculateCost from '../utils/costCalc'"
    - "The useMemo at the original location is now a thin wrapper that calls calculateCost and returns its result"
    - "The CostBreakdown returned by the useMemo has the same 12 fields with the same semantics as before extraction — no consumer field renamed or removed"
    - "All existing dependency-array entries are preserved (no re-renders introduced or removed)"
    - "npx tsc -b passes cleanly with the refactored CostCalculator.tsx"
    - "npm run build passes cleanly (existing chain — vitest not yet wired in; that's Plan 04)"
  artifacts:
    - path: "src/components/CostCalculator.tsx"
      provides: "Cost-breakdown UI integration that consumes calculateCost via a thin useMemo"
      contains: "import { calculateCost } from '../utils/costCalc'"
  key_links:
    - from: "src/components/CostCalculator.tsx"
      to: "src/utils/costCalc.ts"
      via: "calculateCost call inside the costs useMemo"
      pattern: "calculateCost\\("
---

<objective>
Refactor the `useMemo((): CostBreakdown => {…})` at `src/components/CostCalculator.tsx:374` so it imports and calls `calculateCost` from `src/utils/costCalc.ts` (created by Plan 01) instead of inlining the math. This is a pure integration change — UI semantics, runtime behavior, and the returned `CostBreakdown` field shape MUST remain byte-for-byte identical.

Purpose: Close the integration seam opened by Plan 01. After this plan, the UI runs against the same pure module that Plan 03's tests cover and Plan 04's coverage threshold gates.

Output: A diff to `CostCalculator.tsx` that adds one import, deletes the inline math block, and replaces it with a `calculateCost({...})` call. The dependency array stays unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cost-calculation-unit-tests/10-CONTEXT.md
@.planning/phases/10-cost-calculation-unit-tests/10-PATTERNS.md
@.planning/phases/10-cost-calculation-unit-tests/10-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. From Plan 01 output. -->

From src/utils/costCalc.ts (created by Plan 01):
```typescript
export interface CalcInput { /* 16 fields — see Plan 01 SUMMARY */ }
export function calculateCost(input: CalcInput): CostBreakdown;
// + 6 sub-helpers — not consumed by CostCalculator.tsx directly
```

From src/components/CostCalculator.tsx:374-452 (the useMemo to replace):
The closure reads these locals: filamentRows, printTimeHours, selectedPrinter, selectedInstance, electricity, materials, materialsUsed, prepTimeMinutes, postProcessingMinutes, laborHourlyRate, failureRate, profitMarginPercent, targetProfit, sellingPrice, modelCost, modelCostPerUnit. These become the CalcInput fields passed to calculateCost.

Dependency array (line 447-452, MUST stay unchanged):
```typescript
[
  materials, filamentRows, printTimeHours, selectedPrinter, electricity,
  selectedInstance, materialsUsed, laborHourlyRate, prepTimeMinutes,
  postProcessingMinutes, failureRate, profitMarginPercent, targetProfit, sellingPrice,
  modelCost, modelCostPerUnit
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace inline useMemo body at CostCalculator.tsx:374 with calculateCost call</name>
  <files>src/components/CostCalculator.tsx</files>
  <read_first>
    - src/utils/costCalc.ts (the module created by Plan 01 — confirm the exported CalcInput field names and calculateCost signature)
    - src/components/CostCalculator.tsx (lines 1-10 for current imports; lines 360-460 for the useMemo to refactor and surrounding context including the dependency array)
    - .planning/phases/10-cost-calculation-unit-tests/10-PATTERNS.md (section "src/components/CostCalculator.tsx (component, integration only — line 374)" — has the exact replacement shape)
    - .planning/phases/10-cost-calculation-unit-tests/10-CONTEXT.md (D-01 "CostCalculator imports and calls it — same UI, same behavior"; D-04 "Packaging/shipping/marketplace fees/profit margin/target profit/selling price stay inline in CostCalculator.tsx")
    - .planning/phases/10-cost-calculation-unit-tests/10-01-SUMMARY.md (Plan 01 output — confirms CalcInput field names match)
  </read_first>
  <behavior>
    - The `costs` useMemo at line 374 calls `calculateCost({...inputs})` and returns its result directly
    - All 16 closure variables that the previous inline body read are now passed as fields in the CalcInput object literal
    - The dependency array on lines 447-452 is preserved verbatim — same variable names, same order
    - No new state, no new effects, no new props added to the component
    - The 12-field CostBreakdown returned by the useMemo is identical to before (every consumer of `costs.filament`, `costs.electricity`, `costs.printerDepreciation`, `costs.nozzleWear`, `costs.modelAmortization`, `costs.materials`, `costs.labor`, `costs.subtotal`, `costs.failureAdjusted`, `costs.profitMargin`, `costs.targetProfit`, `costs.sellingPrice` keeps working unchanged)
    - The pricing pipeline below line 452 (trueCost, fixedCosts, breakEvenInfo, packaging/shipping/marketplace fees/profit margin/target profit/selling price) is UNCHANGED per D-04
    - `tsc -b` and `npm run build` both pass
  </behavior>
  <action>
    Edit `src/components/CostCalculator.tsx`:
    1. Replace the existing `import { getMaterialDensity } from '../utils/gcodeParser';` at line 8 with `import { calculateCost } from '../utils/costCalc';`. After this refactor, `getMaterialDensity` is no longer consumed in this file (its only call site moves to `costCalc.ts`), so unconditionally remove that import. Under Vercel's `tsc -b` with `noUnusedLocals`, leaving it would fail the build.
    2. Replace the entire body of the useMemo at line 374-446 with a single expression: `const costs = useMemo((): CostBreakdown => calculateCost({ filamentRows, printTimeHours, selectedPrinter, selectedInstance, electricity, materials, materialsUsed, prepTimeMinutes, postProcessingMinutes, laborHourlyRate, failureRate, profitMarginPercent, targetProfit, sellingPrice, modelCost, modelCostPerUnit }), [/* preserved dep array */]);` — the dependency array on lines 447-452 stays identical to its current contents (do not reorder, do not add, do not remove).
    3. The inline math (filamentCost reduce, electricityCost ternary, depreciation block, nozzleWear block, modelAmortization const, materialsCost reduce, laborCost calc, perUnitSubtotal, clampedFailureRate / failureMultiplier / failureAdjusted) is DELETED — that logic now lives in `costCalc.ts` from Plan 01.
    4. Do NOT touch anything below the closing `]);` of the useMemo dependency array (line 452). Specifically: `trueCost`, `fixedCosts` useMemo, `breakEvenInfo` useMemo, packaging/shipping/marketplace/profit/target/selling-price pipeline below remain unchanged per D-04.
    5. Comment above the refactored useMemo: `// Cost calculation extracted to src/utils/costCalc.ts for testability (Phase 10, D-01)` to preserve provenance.
    The expected diff shape is: 1 import added, ~75 lines deleted (the inline math), ~18 lines added (the calculateCost call + dep array + comment). Net delete.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/plan10-02-tsc.log; grep -c "error TS" /tmp/plan10-02-tsc.log | grep -E "^0$" && npm run build 2>&1 | tee /tmp/plan10-02-build.log | tail -20 && grep -c "calculateCost" src/components/CostCalculator.tsx | grep -vE "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { calculateCost } from '../utils/costCalc'" src/components/CostCalculator.tsx` returns 1
    - `grep -c "import { getMaterialDensity }" src/components/CostCalculator.tsx` returns 0 (unused after extraction; would break `tsc -b` under `noUnusedLocals`)
    - `grep -c "calculateCost(" src/components/CostCalculator.tsx` returns >= 1 (the call site inside the useMemo)
    - `grep -c "clampedFailureRate" src/components/CostCalculator.tsx` returns 0 (inline failure-clamp deleted — it lives in costCalc.ts now)
    - `grep -c "totalRecoveryHours" src/components/CostCalculator.tsx` returns 0 (inline depreciation block deleted)
    - `grep -c "depreciationPerHour" src/components/CostCalculator.tsx` returns 0 (inline depreciation block deleted)
    - The dep array at lines ~447-452 still contains all 16 entries (materials, filamentRows, printTimeHours, selectedPrinter, electricity, selectedInstance, materialsUsed, laborHourlyRate, prepTimeMinutes, postProcessingMinutes, failureRate, profitMarginPercent, targetProfit, sellingPrice, modelCost, modelCostPerUnit) — verify with `grep -A 6 "calculateCost({" src/components/CostCalculator.tsx`
    - `npx tsc -b` exits 0 with zero new errors
    - `npm run build` exits 0 (note: Plan 04 will modify the build chain; this verifies the current chain still passes)
    - Pricing-pipeline locals below line 452 are unchanged — search for `trueCost`, `fixedCosts`, `breakEvenInfo` and confirm those useMemos are untouched: `grep -c "const trueCost = " src/components/CostCalculator.tsx` returns 1; `grep -c "const fixedCosts = useMemo" src/components/CostCalculator.tsx` returns 1
  </acceptance_criteria>
  <done>The cost useMemo at CostCalculator.tsx:374 is now a thin wrapper calling `calculateCost`. The component compiles, the build passes, the rendered UI is unchanged (no test yet — that's Plan 03). The math now runs through the pure module, ready to be locked behind tests in Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No new trust boundaries — this is a pure refactor that moves existing math behind a new function call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-N/A | N/A | CostCalculator.tsx ↔ costCalc.ts | accept | Internal module boundary inside the same TypeScript build. Inputs come from existing React state already trusted by the previous inline code path. No new attack surface. |

**Rationale:** This is a code-organization refactor with no new IO, no new dependency, no new user-input parsing. Equivalent attack surface to pre-refactor.
</threat_model>

<verification>
- `npx tsc -b` exits 0 (no new TS errors)
- `npm run build` exits 0 (full build chain still green — pre-Plan-04 state)
- Component diff is a net delete (more lines removed than added)
- `costs.*` consumer fields below line 452 (trueCost, fixedCosts, breakEvenInfo, packaging/shipping pipeline) work unchanged — verified by build passing and by manual grep of consumers
- Manual UI smoke (deferred to Plan 03's automated tests + Plan 04's coverage gate; no checkpoint required because behavior change is mechanically prevented by the byte-identical calculateCost implementation in Plan 01)
</verification>

<success_criteria>
1. `src/components/CostCalculator.tsx` imports `calculateCost` from `'../utils/costCalc'`
2. The inline math block at lines 374-446 (filamentCost reduce, electricityCost, depreciation, nozzleWear, modelAmortization, materialsCost, laborCost, perUnitSubtotal, failure clamp/multiplier/adjusted, return literal) is REPLACED by a single `calculateCost({...})` call
3. The useMemo dependency array (originally lines 447-452) is preserved verbatim
4. Pricing pipeline below line 452 (trueCost, fixedCosts, breakEvenInfo, and packaging/shipping/marketplace fees/profit/target/selling price logic) is UNCHANGED per D-04
5. `npx tsc -b` and `npm run build` both exit 0
</success_criteria>

<output>
Create `.planning/phases/10-cost-calculation-unit-tests/10-02-SUMMARY.md` when done. Include: the diff line count (added/deleted), confirmation that the dependency array is preserved verbatim, and confirmation that the pricing pipeline below line 452 is untouched.
</output>
