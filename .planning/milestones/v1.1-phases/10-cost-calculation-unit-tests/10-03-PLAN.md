---
phase: 10-cost-calculation-unit-tests
plan: 03
type: tdd
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/utils/costCalc.test.ts
autonomous: true
requirements:
  - TEST-01
tags:
  - testing
  - cost-calculation
  - vitest

must_haves:
  truths:
    - "src/utils/costCalc.test.ts exists and is discovered by vitest's include glob src/**/*.test.ts"
    - "Every D-13 edge case has at least one test: 1/2/16 filament multi-filament loop, failure-rate 0/99/over-100/negative clamp, per-material density PLA/PETG/ABS/unrecognized, no-printer-selected, recovery-hours-zero degenerate"
    - "Each of the 6 sub-helpers has its own describe block with at least one test"
    - "calculateCost has its own describe block with at least one end-to-end integration test"
    - "An `it.todo('tax/VAT applies after subtotal — activates in v1.2')` placeholder exists per D-12 exact phrasing"
    - "vitest run exits 0 with the new test file present"
    - "No DOM/React imports, no jsdom dependency in this file (pure math)"
  artifacts:
    - path: "src/utils/costCalc.test.ts"
      provides: "Vitest test suite locking the 7 cost factors + failure clamp + density smoke tests"
      contains: "describe('calculateCost'"
      min_lines: 150
  key_links:
    - from: "src/utils/costCalc.test.ts"
      to: "src/utils/costCalc.ts"
      via: "imports of calculateCost and 6 sub-helpers"
      pattern: "from './costCalc'"
    - from: "src/utils/costCalc.test.ts"
      to: "src/utils/gcodeParser.ts"
      via: "getMaterialDensity smoke tests (D-11 hybrid fixtures)"
      pattern: "from './gcodeParser'"
---

<objective>
Write `src/utils/costCalc.test.ts` — the unit test suite that locks the cost-calculation math established by Plan 01. The suite covers every ROADMAP-listed factor, every D-13 edge case, every sub-helper, the coarse `calculateCost` integration, and includes the D-12 tax/VAT `it.todo` placeholder.

Purpose: Lock existing math behind automated regression detection so future refactors cannot silently change cost output. This is the verification artifact that satisfies TEST-01 (cost factors covered) and feeds Plan 04 (coverage gate has something to measure).

Output: A single co-located test file using the vitest pattern from `src/utils/threeMfParser.test.ts`. Pure-function tests, no React, no DOM, no fixtures from the filesystem.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cost-calculation-unit-tests/10-CONTEXT.md
@.planning/phases/10-cost-calculation-unit-tests/10-PATTERNS.md
@.planning/phases/10-cost-calculation-unit-tests/10-01-SUMMARY.md

<interfaces>
<!-- Plan 01 outputs that this test file consumes -->

From src/utils/costCalc.ts (Plan 01 output):
```typescript
export interface CalcInput { /* see Plan 01 */ }
export function calculateCost(input: CalcInput): CostBreakdown;
export function calculateFilamentCost(filamentRows: CalcInput['filamentRows']): number;
export function calculateElectricityCost(printTimeHours: number, selectedPrinter: PrinterConfig | undefined, electricity: ElectricityConfig): number;
export function calculateDepreciation(printTimeHours: number, selectedPrinter: PrinterConfig | undefined, selectedInstance: PrinterInstance | undefined): number;
export function calculateNozzleWear(filamentRows: CalcInput['filamentRows'], materials: Material[], selectedPrinter: PrinterConfig | undefined): number;
export function calculateLabor(prepTimeMinutes: number, postProcessingMinutes: number, laborHourlyRate: number): number;
export function calculateAmortization(): number;
```

From src/utils/gcodeParser.ts:64-78 (smoke-test target — locks the v1.0 density table):
```typescript
export function getMaterialDensity(filamentType: string | null): number
// PLA → 1.24, PETG/PET → 1.27, ABS → 1.04, ASA → 1.07, TPU → 1.21
// PA/NYLON → 1.14, PC → 1.20, PVA → 1.23, PPS → 1.35, PPA → 1.18
// unknown/null → 1.24 (PLA default)
```

From src/types.ts CostBreakdown (consumer contract — tests verify all 12 fields are returned with the expected types):
```typescript
{ filament, electricity, printerDepreciation, nozzleWear, modelAmortization, materials,
  labor, subtotal, failureAdjusted, profitMargin, targetProfit, sellingPrice }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create costCalc.test.ts with synthetic-fixture helpers, 6 sub-helper describes, and getMaterialDensity smoke tests</name>
  <files>src/utils/costCalc.test.ts</files>
  <read_first>
    - src/utils/costCalc.ts (the module under test — Plan 01 output; needs to be read to know exact signatures and which guards exist)
    - src/utils/threeMfParser.test.ts (PRIMARY analog — read all 155 lines for the describe/it/expect pattern, the synthetic-fixture style, and the floating-point assertion conventions)
    - src/utils/gcodeParser.ts (lines 64-78 — getMaterialDensity is the smoke-test target; the table values are the expected results)
    - .planning/phases/10-cost-calculation-unit-tests/10-PATTERNS.md (section "src/utils/costCalc.test.ts" — has the full edge-case mapping table and import shape)
    - .planning/phases/10-cost-calculation-unit-tests/10-CONTEXT.md (D-11 hybrid fixtures; D-13 edge cases list; D-12 tax/VAT exact phrasing)
  </read_first>
  <behavior>
    - `calculateFilamentCost` tests cover: empty rows → 0; one row with `filamentId='x', grams=500, editedPrice=0.02` → 10.00; row with empty filamentId → contributes 0; row with editedPrice=0 → contributes 0; row with editedPrice<0 → contributes 0 (matches the `if (!row.filamentId || row.editedPrice <= 0) return sum` guard)
    - `calculateElectricityCost` tests cover: no printer → 0; 100W printer × 2h × $0.12/kWh → 0.024
    - `calculateDepreciation` tests cover: no printer + no instance → 0; instance with `actualPurchasePrice=1200, recoveryMonths=12, estimatedMonthlyPrintHours=40` × 4h printTime → (1200/(12*40))*4 = 10.00; printer fallback when instance is undefined (uses `selectedPrinter.purchasePrice`); zero-recovery-hours guard (`recoveryMonths=0` or `estimatedMonthlyPrintHours=0` → 0 not NaN/Infinity)
    - `calculateNozzleWear` tests cover: no printer → 0; PLA 124g (= 100cm³ at density 1.24) × printer with `nozzleLifespanCm3=1000, nozzleCost=20` → (100/1000)*20 = 2.00; mixed-material (PLA + PETG) sums correctly using per-material density; rows with grams<=0 contribute 0
    - `calculateLabor` tests cover: 0+0+rate → 0; 30 prep + 30 post @ $20/h → 20.00; both zero → 0
    - `calculateAmortization` test confirms the placeholder returns 0 (locks the contract so v1.2 changes are intentional)
    - `getMaterialDensity` smoke tests assert: PLA→1.24, PETG→1.27, ABS→1.04, unrecognized string ("UnknownXYZ")→1.24, null→1.24 (D-11 hybrid: real-data smoke against the table that fixed the v1.0 nozzle-wear bug)
    - `calculateCost` integration tests cover: multi-filament loops (1/2/16 rows); failure-rate clamp at 0 / 99 / 150 (clamps to 99) / -10 (clamps to 0); full 12-field CostBreakdown is returned with profitMargin/targetProfit/sellingPrice passed through unchanged from input
    - `it.todo('tax/VAT applies after subtotal — activates in v1.2')` placeholder at end of `describe('calculateCost', ...)` block — exact phrasing per D-12
    - `vitest run` exits 0
  </behavior>
  <action>
    Create `src/utils/costCalc.test.ts`. Header: `import { describe, it, expect } from 'vitest';` then `import { calculateCost, calculateFilamentCost, calculateElectricityCost, calculateDepreciation, calculateNozzleWear, calculateLabor, calculateAmortization } from './costCalc';` then `import { getMaterialDensity } from './gcodeParser';` then `import type { Material, PrinterConfig, PrinterInstance, ElectricityConfig, MaterialUsage } from '../types';`. Add small fixture-builder helpers at the top of the file (similar to `makeSlicedZip` in threeMfParser.test.ts) — for cost-calc the helpers are pure: `makePrinter(overrides)` returning a `PrinterConfig` with sane defaults (wattage 100, nozzleLifespanCm3 1000, nozzleCost 20, purchasePrice 1200), `makeInstance(overrides)` returning `PrinterInstance` (recoveryMonths 12, estimatedMonthlyPrintHours 40, actualPurchasePrice 1200), `makeMaterial(id, filamentType)` returning `Material` with the given filamentType. Then write describes in this order: (1) `describe('calculateFilamentCost', ...)` — tests for empty/single/multi/guarded rows; (2) `describe('calculateElectricityCost', ...)` — tests for no-printer + happy path; (3) `describe('calculateDepreciation', ...)` — tests for no-printer/no-instance, instance happy path, printer fallback, zero-recovery-hours guard; (4) `describe('calculateNozzleWear', ...)` — tests for no-printer, single-material happy path, multi-material density mixing, zero-gram rows skipped; (5) `describe('calculateLabor', ...)` — tests for zeros and happy path; (6) `describe('calculateAmortization', ...)` — one test asserting it returns 0 (placeholder); (7) `describe('getMaterialDensity (smoke tests)', ...)` — five tests for PLA, PETG, ABS, unrecognized string, null input (D-11 hybrid fixture coverage); (8) `describe('calculateCost (integration)', ...)` — tests for: 1 filament row, 2 filament rows (multi-color), 16 filament rows (Bambu AMS Hub max per PROJECT.md v1.0 decision), failure-rate clamp at 0/99/150/-10, full CostBreakdown returns all 12 fields with correct types, profitMargin/targetProfit/sellingPrice pass through from input unchanged. Test names follow `it('Test N (TEST-01): description', ...)` format from threeMfParser.test.ts:59. Use `toBeCloseTo(value, 2)` for monetary assertions and `toBe(value)` for table lookups and exact-zero degenerate cases (PATTERNS.md "Floating-point monetary assertions"). Final line of `describe('calculateCost', ...)`: `it.todo('tax/VAT applies after subtotal — activates in v1.2');` — exact phrasing per D-12 (no second argument, vitest accepts a single string).
    
    Edge-case coverage requirements (D-13, every entry MUST be a test):
    - 1 filament row (v0 migration path)
    - 2 filament rows (generic multi-color)
    - 16 filament rows (Bambu AMS Hub max — PROJECT.md v1.0 decision)
    - Failure rate 0 → multiplier == 1
    - Failure rate 99 → multiplier == 100 (1 / (1 - 0.99))
    - Failure rate 150 → clamps to 99 → multiplier == 100
    - Failure rate -10 → clamps to 0 → multiplier == 1
    - getMaterialDensity('PLA') === 1.24
    - getMaterialDensity('PETG') === 1.27
    - getMaterialDensity('ABS') === 1.04
    - getMaterialDensity('UnknownXYZ') === 1.24 (fallback)
    - getMaterialDensity(null) === 1.24 (default branch)
    - No-printer-selected → electricityCost == 0, nozzleWear == 0
    - Recovery hours zero → depreciation == 0 (guard at original CostCalculator.tsx:392)
    
    Do NOT invent tests for branches that do NOT exist in `costCalc.ts`. If after writing the above the coverage report (run by Plan 04) flags an unreachable defensive line, mark that line in `costCalc.ts` with `/* c8 ignore next */` per PATTERNS.md "Defensive untested-branch pattern" — but only the executor of Plan 04 sees the coverage report; this plan's job is to write the tests for branches that EXIST.
    
    Do NOT import React, do NOT import `React.createElement`, do NOT import `renderToStaticMarkup`. This file is pure math. (Skeleton.test.ts and EmptyState.test.ts use React.createElement — that pattern is NOT what we want here per CONTEXT scope.)
  </action>
  <verify>
    <automated>npx vitest run src/utils/costCalc.test.ts 2>&1 | tee /tmp/plan10-03-vitest.log | tail -30; grep -cE "Tests +[0-9]+ passed" /tmp/plan10-03-vitest.log | grep -vE "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/utils/costCalc.test.ts` exists
    - `grep -c "import { describe, it, expect } from 'vitest'" src/utils/costCalc.test.ts` returns 1
    - `grep -c "from 'react'" src/utils/costCalc.test.ts` returns 0 (no React in pure-math tests)
    - `grep -c "describe('calculateFilamentCost'" src/utils/costCalc.test.ts` returns 1
    - `grep -c "describe('calculateElectricityCost'" src/utils/costCalc.test.ts` returns 1
    - `grep -c "describe('calculateDepreciation'" src/utils/costCalc.test.ts` returns 1
    - `grep -c "describe('calculateNozzleWear'" src/utils/costCalc.test.ts` returns 1
    - `grep -c "describe('calculateLabor'" src/utils/costCalc.test.ts` returns 1
    - `grep -c "describe('calculateAmortization'" src/utils/costCalc.test.ts` returns 1
    - `grep -c "describe('getMaterialDensity" src/utils/costCalc.test.ts` returns 1
    - `grep -c "describe('calculateCost" src/utils/costCalc.test.ts` returns 1 (the integration describe)
    - `grep -c "it.todo('tax/VAT applies after subtotal — activates in v1.2'" src/utils/costCalc.test.ts` returns 1 (D-12 exact phrasing, em-dash matters)
    - `grep -cE "filamentRows\\.length === 16|new Array\\(16\\)|Array\\.from\\(\\{ ?length: ?16" src/utils/costCalc.test.ts | grep -vE "^0$"` — confirms 16-filament edge case is present (one of these patterns must appear)
    - `grep -cE "failureRate: ?150|failureRate: ?99|failureRate: ?-10|failureRate: ?0" src/utils/costCalc.test.ts` returns >= 4 (all four failure-rate clamp boundaries tested)
    - `npx vitest run src/utils/costCalc.test.ts` exits 0 and the summary line shows "passed" with no failures
    - The full project `npx vitest run` also exits 0 (the new file does not break the existing threeMfParser.test.ts / Skeleton.test.ts / EmptyState.test.ts suite)
  </acceptance_criteria>
  <done>The test file exists with the structure described, exits 0 under `vitest run`, and locks every D-13 edge case + every sub-helper + the calculateCost integration + the tax/VAT todo placeholder. Plan 04 will then wire `vitest run --coverage` into the build and add the threshold gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure unit tests — no network, no filesystem fixtures, no user input parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-N/A | N/A | src/utils/costCalc.test.ts | accept | Pure synthetic-fixture unit tests with no IO, no network, no DOM. Test fixtures are hardcoded numeric inputs. No new attack surface. |

**Rationale:** Tests strengthen the codebase's resistance to regression. No new external dependency installed (vitest, @vitest/coverage-v8, jsdom are already in devDependencies). No production runtime impact.
</threat_model>

<verification>
- `npx vitest run src/utils/costCalc.test.ts` exits 0
- `npx vitest run` (full project, all test files) exits 0 — confirms the new file does not break the existing threeMfParser/Skeleton/EmptyState suites
- Every D-13 edge case has a matching `it(...)` (verified by grep gates in acceptance_criteria)
- `it.todo('tax/VAT applies after subtotal — activates in v1.2')` is present with the exact em-dash phrasing
- Zero React imports in the test file (pure-math classification)
</verification>

<success_criteria>
1. `src/utils/costCalc.test.ts` exists, is picked up by `vitest run`, and exits 0
2. Every ROADMAP-listed factor (filament, electricity, depreciation, nozzle wear, labor, failure-rate adjustment, model amortization) has at least one explicit test
3. Every D-13 edge case has a test: 1/2/16 filament rows, failure-rate boundaries (0/99/150/-10), per-material density (PLA/PETG/ABS/unrecognized/null), no-printer-selected, zero-recovery-hours guard
4. Tax/VAT pending test exists with D-12 exact phrasing: `it.todo('tax/VAT applies after subtotal — activates in v1.2')`
5. Test file is deterministic — no IndexedDB, no browser API, no network, no React rendering
6. The full project `vitest run` exits 0 (new tests don't break existing tests)
</success_criteria>

<output>
Create `.planning/phases/10-cost-calculation-unit-tests/10-03-SUMMARY.md` when done. Include: test count by describe block, pass/fail summary, confirmation that the tax/VAT todo placeholder is present with exact D-12 phrasing, and the vitest run wall-clock time.
</output>
