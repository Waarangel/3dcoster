---
phase: 11-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements:
  - PERF-01
  - PERF-02
tags:
  - vite
  - react-window
  - dependencies
  - supply-chain

must_haves:
  truths:
    - "`react-window` is installed as a runtime dependency at the v2 line so JobsManager and AssetLibrary can import the `List` component"
    - "`rollup-plugin-visualizer` is installed as a devDependency for opt-in bundle analysis only"
    - "`npm run analyze` is wired but NOT part of `npm run build` — the visualizer never runs on Vercel deploys"
    - "Both packages are confirmed canonical (verified maintainers and repos) before install, satisfying the supply-chain threat (T-11-SC)"
  artifacts:
    - path: "package.json"
      provides: "Updated `dependencies` (react-window), `devDependencies` (rollup-plugin-visualizer), and `scripts.analyze`"
      contains: "react-window"
    - path: "package-lock.json"
      provides: "Locked transitive deps for both new packages"
      contains: "react-window"
  key_links:
    - from: "package.json scripts.analyze"
      to: "vite.config.ts visualizer plugin (Plan 02)"
      via: "`vite build --mode analyze` triggers the conditional visualizer plugin"
      pattern: "analyze.*vite build"
    - from: "react-window dep"
      to: "src/components/JobsManager.tsx import (Plan 04) and src/components/AssetLibrary.tsx import (Plan 05)"
      via: "named import `{ List }` from 'react-window'"
      pattern: "from 'react-window'"
---

<objective>
Install the two new third-party dependencies that Phase 11 needs (`react-window` for virtualization per D-05, `rollup-plugin-visualizer` for opt-in bundle analysis per D-10) and wire the opt-in `analyze` script. Gate the install behind an explicit package-legitimacy human checkpoint (T-11-SC mitigation) so a typosquat or compromised maintainer is caught before code lands.

Purpose: Unblocks Plans 02–05 (vite.config.ts, bundle assertion, virtualization). Honors the user-locked D-05 (`react-window ^2`) and D-10 (`rollup-plugin-visualizer ^5` devDep) decisions while satisfying the threat model's package legitimacy gate (every new install is verified-by-human, per the GSD planner contract on `[ASSUMED]` packages).

Output: `package.json` with two new deps and the `analyze` script; `package-lock.json` regenerated; `node_modules/react-window` and `node_modules/rollup-plugin-visualizer` installed and importable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-performance-optimization/11-CONTEXT.md
@.planning/phases/11-performance-optimization/11-PATTERNS.md
@package.json
@.claude/CLAUDE.md

<interfaces>
<!-- Current package.json dependency + script shape the executor edits in place. -->

From package.json (current, lines 6–18 scripts, 19–29 deps, 30–54 devDeps):
```
"scripts": {
  "dev": "vite",
  "build": "node scripts/lint-no-raw-html.mjs && vitest run --coverage && tsc -b && vite build",
  "lint": "eslint .",
  "lint:no-raw-html": "node scripts/lint-no-raw-html.mjs",
  "preview": "vite preview",
  "prepare": "chmod +x .git/hooks/pre-commit 2>/dev/null || true",
  "tauri": "tauri",
  "tauri:dev": "tauri dev",
  "tauri:build": "tauri build",
  "test": "vitest run",
  "test:watch": "vitest"
}
```
- `dependencies` are alphabetized: @tauri-apps/plugin-shell, @vercel/analytics, dexie, dexie-react-hooks, jszip, papaparse, react, react-dom, react-router-dom
- `devDependencies` are alphabetized; `rollup-plugin-visualizer` slots between `react-dom` and `sharp`

Verified facts (researched 2026-05-20):
- `react-window` latest is `2.2.7`; maintainer `brianvaughn` (canonical); repo `github.com/bvaughn/react-window`
- `react-window` v2 exposes `List`, `Grid`, `useDynamicRowHeight`, `useListRef` — NOT `FixedSizeList`/`VariableSizeList` (those are v1 names). PATTERNS.md references v1 names; Plans 04 and 05 use the v2 `List` API.
- `rollup-plugin-visualizer` latest is `7.0.1`; D-10 locks `^5`. The 5.x line is two majors behind. Plan honors D-10 (`^5`) per user-decision fidelity; if the install resolves to a deprecated 5.x release, the package-legitimacy checkpoint is where the user decides whether to bump the decision or proceed.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate (T-11-SC)</name>
  <read_first>
    - .planning/phases/11-performance-optimization/11-CONTEXT.md (D-05 react-window ^2, D-10 rollup-plugin-visualizer ^5)
    - This plan's `<threat_model>` block (T-11-SC entry)
    - .planning/phases/11-performance-optimization/11-PATTERNS.md (no install gate analog exists yet — this is the first install in v1.1)
  </read_first>
  <what-built>
    No code yet — this is a verification step BEFORE any `npm install`. The two packages to install carry supply-chain risk; both are `[ASSUMED]` (not previously audited in this project) and require human verification of legitimacy before they land in `package-lock.json`.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/react-window in a browser
       - Confirm the package page shows "Maintainers: brianvaughn" (bvaughn — author of react-window v1 and v2)
       - Confirm "Repository" field links to `github.com/bvaughn/react-window`
       - Confirm "Weekly Downloads" is in the millions (high-trust signal)
       - Confirm latest is in the 2.x line (planner verified 2.2.7 on 2026-05-20; minor versions may have advanced since)
    2. Open https://www.npmjs.com/package/rollup-plugin-visualizer
       - Confirm "Maintainers: btd"
       - Confirm "Repository" links to `github.com/btd/rollup-plugin-visualizer`
       - Confirm "Weekly Downloads" is healthy (six figures or more)
       - Note: latest is 7.x as of 2026-05-20; D-10 locks `^5`. If the 5.x line is deprecated on npm OR the user prefers latest, override D-10 here.
    3. Reply with one of:
       - `approved` — both packages verified, install with the decision-locked specs (`react-window@^2`, `rollup-plugin-visualizer@^5`)
       - `approved with bump: visualizer ^7` — override D-10 to current major
       - `blocked: <reason>` — anything looks off (wrong maintainer, low downloads, suspicious repo); halt and surface
  </how-to-verify>
  <acceptance_criteria>
    User has typed one of the approval phrases above. Until then the Task 2 install MUST NOT run.
  </acceptance_criteria>
  <resume-signal>Type one of the approval phrases above ("approved", "approved with bump: visualizer ^7", or "blocked: ...")</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install dependencies and add analyze script</name>
  <read_first>
    - package.json (current state — lines 6–18 scripts, 19–29 deps, 30–54 devDeps; alphabetization rule)
    - .planning/phases/11-performance-optimization/11-PATTERNS.md (`package.json` section — "Pattern: dependency additions" — alphabetical order is enforced)
    - .planning/phases/11-performance-optimization/11-CONTEXT.md (D-05, D-10 for spec strings)
  </read_first>
  <action>
    Run `npm install react-window@^2` (runtime dep per D-05) followed by `npm install --save-dev rollup-plugin-visualizer@^5` (devDep per D-10). If Task 1 returned "approved with bump: visualizer ^7", substitute `rollup-plugin-visualizer@^7` for the second install. After both installs succeed, edit `package.json` to add a new `analyze` script: insert `"analyze": "vite build --mode analyze",` into the `scripts` block immediately after the `"lint:no-raw-html"` line (places it next to the existing lint script, before `preview` — keeps build-tooling scripts grouped). Do NOT touch `scripts.build` in this plan — that append is reserved for Plan 03 to avoid a merge conflict. Verify that `dependencies` lists `react-window` between `react-router-dom` and… actually `react-router-dom` is currently the last entry; `react-window` goes alphabetically AFTER it (still alphabetically sorted: `react-router-dom`, `react-window`). Verify `devDependencies` lists `rollup-plugin-visualizer` between `eslint-plugin-react-refresh` and `sharp` (alphabetical). Verify `package-lock.json` was regenerated by `npm install` (both file mtimes will be newer than pre-install).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies['react-window']) throw new Error('react-window missing'); if(!p.devDependencies['rollup-plugin-visualizer']) throw new Error('visualizer missing'); if(!p.scripts.analyze || !p.scripts.analyze.includes('vite build --mode analyze')) throw new Error('analyze script missing or wrong'); if(p.scripts.build.includes('assert-bundle-size')) throw new Error('build chain modified — that is Plan 03 work, abort'); console.log('package.json OK:', p.dependencies['react-window'], p.devDependencies['rollup-plugin-visualizer'], p.scripts.analyze)"</automated>
    <automated>node -e "require.resolve('react-window'); require.resolve('rollup-plugin-visualizer'); console.log('both packages resolvable')"</automated>
  </verify>
  <done>
    `package.json` shows `react-window` in `dependencies`, `rollup-plugin-visualizer` in `devDependencies`, and `scripts.analyze` set to `"vite build --mode analyze"`. `scripts.build` is unchanged. `package-lock.json` is regenerated and both packages resolve via `require.resolve`. Lockfile is staged for commit (orchestrator commits at end of phase per project convention).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Third-party code enters the project; supply-chain risk applies to every new install |
| dev-build → production bundle | `rollup-plugin-visualizer` is devDep-only and MUST never ship to users (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SC | Tampering | npm install (react-window, rollup-plugin-visualizer) | mitigate | Blocking human checkpoint (Task 1) verifies maintainer + repo + download volume on npmjs.com before install. Both packages marked `[ASSUMED]` until checkpoint passes. |
| T-11-LK | Information Disclosure | rollup-plugin-visualizer in production bundle | mitigate | D-10 makes the plugin opt-in via `--mode analyze`; Plan 02's vite.config.ts uses a functional defineConfig with `mode === 'analyze'` guard. This plan adds the script but NOT the plugin wiring, so accidental leak is impossible until Plan 02 runs and is reviewed. |
| T-11-LK2 | Tampering | package.json scripts.build chain (Plan 01 vs Plan 03 collision risk) | mitigate | Task 2 action explicitly forbids editing `scripts.build`; Plan 03 owns that append. The Task 2 `<verify>` block fails if `build` already contains `assert-bundle-size`. |
</threat_model>

<verification>
After this plan runs:
1. `npm ls react-window` shows the installed version
2. `npm ls rollup-plugin-visualizer` shows the installed version
3. `grep '"analyze":' package.json` returns one match
4. `grep 'assert-bundle-size' package.json` returns ZERO matches (Plan 03's territory)
5. `npm run build` still succeeds end-to-end (existing chain untouched)
</verification>

<success_criteria>
- `react-window` in `dependencies` at a `^2` range
- `rollup-plugin-visualizer` in `devDependencies` at the approved range (^5 per D-10 or ^7 if user bumped)
- `scripts.analyze` reads `"vite build --mode analyze"`
- `scripts.build` is UNCHANGED from the pre-plan state
- Lockfile regenerated, both packages resolvable
- Package legitimacy checkpoint (Task 1) was approved by a human before Task 2 ran
</success_criteria>

<output>
Create `.planning/phases/11-performance-optimization/11-01-SUMMARY.md` when done. Record:
- Exact installed versions (`npm ls react-window rollup-plugin-visualizer`)
- Whether user bumped D-10 to ^7 (and the rationale)
- Confirmation that `scripts.build` was not touched
</output>
