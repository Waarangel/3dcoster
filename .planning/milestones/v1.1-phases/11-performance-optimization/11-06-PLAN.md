---
phase: 11-performance-optimization
plan: 06
type: execute
wave: 4
depends_on:
  - 11-02
  - 11-03
  - 11-04
  - 11-05
files_modified:
  - .planning/phases/11-performance-optimization/11-SUMMARY.md
autonomous: false
requirements:
  - PERF-01
  - PERF-02
tags:
  - uat
  - documentation
  - summary

must_haves:
  truths:
    - "A live manual UAT run confirms scrolling a 500-job list under 4× CPU throttle in DevTools produces no dropped frames (>16ms) — satisfies PERF-02 success criterion #3"
    - "The expanded/collapsed selection toggle in JobsManager works correctly for at least 3 different jobs in the 500-item fixture without overlap or clipping"
    - "AssetLibrary at 100 items/page (triggers virtualization) scrolls smoothly and the printer/materials div-grid layouts maintain column alignment, sort headers, and Edit/Delete button behavior"
    - "20% of seeded printer assets include notes+tags to surface any clipping caused by Plan 05's numeric `rowHeight={56}` (W3 from iteration-1 review)"
    - "The phase SUMMARY documents (a) the seed-jobs DevTools console snippet, (b) the cleanup procedure, (c) the measured headroom under the 300 KB gzipped budget, (d) the design Q1/Q2/Q3 resolutions that actually landed"
  artifacts:
    - path: ".planning/phases/11-performance-optimization/11-SUMMARY.md"
      provides: "Phase-level wrap-up: what shipped, how to repeat the UAT, link to design-decision resolutions"
      contains: "DevTools"
      contains_also: "PERF-01"
      contains_also2: "PERF-02"
  key_links:
    - from: ".planning/phases/11-performance-optimization/11-SUMMARY.md"
      to: ".planning/STATE.md"
      via: "the orchestrator updates STATE.md after Plan 06 completes"
      pattern: "Phase 11.*Complete"
---

<objective>
Run the manual UAT for PERF-02 (500-job scroll smoothness under 4× CPU throttle), capture the result, and write the phase SUMMARY. Resolve design Q3 (the seed-jobs script's runtime ambiguity) by documenting paste-into-DevTools-console snippets IN the SUMMARY — no standalone script file is created or shipped.

**Design Q3 resolution recorded in this plan:** PATTERNS.md noted that `scripts/seed-test-jobs.mjs` is misleading because Dexie is browser-only. CONTEXT.md "Specifics" says the seed should be ephemeral and NOT checked in. The cleanest expression of both constraints is: **no script file is created.** Instead, the SUMMARY embeds verbatim DevTools console snippets that the developer (or any future maintainer) can paste into the running app to seed Dexie with 500 fake jobs + 200 fake printer assets (20% with notes+tags per W3), plus a one-line cleanup snippet to delete them. This keeps the repo clean and matches "ephemeral" exactly.

The seed snippets target the correct Dexie tables verified directly from `src/db/database.ts` on 2026-05-20: `db.jobs` (for PrintJob) and `db.materials` (for Asset/PrinterAsset — the `materials` table holds ALL assets including those with `category: 'printer'`). The `db` object is imported from `'/src/db/database.ts'` (NOT `/src/db.ts` — that path does not exist). The PrinterAsset shape follows `src/types.ts:39-75` (Asset interface + PrinterAsset extension).

Purpose: Closes Phase 11. After this plan, Phase 11 is shippable as part of the v1.2 bundled release per CONTEXT.md's "ship together as part of the v1.2 release" note.

Output: `.planning/phases/11-performance-optimization/11-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-performance-optimization/11-CONTEXT.md
@.planning/phases/11-performance-optimization/11-PATTERNS.md
@.planning/phases/11-performance-optimization/11-01-SUMMARY.md
@.planning/phases/11-performance-optimization/11-02-SUMMARY.md
@.planning/phases/11-performance-optimization/11-03-SUMMARY.md
@.planning/phases/11-performance-optimization/11-04-SUMMARY.md
@.planning/phases/11-performance-optimization/11-05-SUMMARY.md
@src/db/database.ts
@src/types.ts
@vite.config.ts
@src/components/JobsManager.tsx
@src/components/AssetLibrary.tsx
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual UAT — 500-job scroll smoothness under 4× CPU throttle (PERF-02)</name>
  <read_first>
    - .planning/phases/11-performance-optimization/11-CONTEXT.md (D-13 procedure: 500-item fixture, DevTools Performance, 4× CPU slowdown, no dropped frames >16ms per frame)
    - .planning/phases/11-performance-optimization/11-04-SUMMARY.md (the post-edit JobsManager state; defaultRowHeight + useDynamicRowHeight + key arg notes)
    - .planning/phases/11-performance-optimization/11-05-SUMMARY.md (which design Q1 path landed for AssetLibrary tables — div-grid replacement)
    - src/db/database.ts (verifies tables: db.jobs, db.materials)
    - src/types.ts (verifies PrintJob + Asset + PrinterAsset shapes for the seed snippets)
    - .claude/CLAUDE.md (port 4173 is pinned for `npm run dev`)
  </read_first>
  <what-built>
    Plan 04 virtualized the JobsManager jobs list above 100 items. Plan 05 virtualized AssetLibrary's three call sites above 50 items per page (with div-grid replacement for the two desktop tables). Plan 02 split the bundle; Plan 03 gates the 300 KB budget. The build chain is green. This checkpoint exercises the runtime behavior — automated tests cannot observe scroll smoothness or visual clipping, so a human runs the procedure below.
  </what-built>
  <how-to-verify>
    Open this checkpoint's instructions in the SUMMARY's "How to repeat the UAT" section (Task 2 writes that section). The procedure is also reproduced here verbatim — both copies must match.

    **Setup (1 minute):**
    1. `npm run dev` (port 4173 — kill any other dev servers first per .claude/CLAUDE.md)
    2. Open http://localhost:4173 in a Chromium-based browser (Chrome / Edge / Brave)
    3. Open DevTools (F12), Console tab
    4. Paste the **seed-500-jobs** snippet (full text below) and press Enter — it adds 500 fake jobs to Dexie's `jobs` table
    5. Paste the **seed-200-printers** snippet — it adds 200 fake printer assets to Dexie's `materials` table (with 20% carrying notes+tags per W3)
    6. Refresh the page; the Jobs tab should now show 500 entries with the virtualized list active, and the Asset Library should show 200+ printer entries

    **Seed snippet (jobs) — paste verbatim into DevTools console.**

    The PrintJob field set is derived from `src/types.ts:139-175` (verified). All required fields are included. The executor MUST cross-check this snippet against the live interface in `src/types.ts` BEFORE the UAT runs — if `PrintJob` has acquired new required fields since 2026-05-20, the snippet is updated to include them.
    ```
    (async () => {
      const { db } = await import('/src/db/database.ts');
      const now = Date.now();
      const jobs = Array.from({ length: 500 }, (_, i) => ({
        id: `uat-${now}-${i}`,
        name: `UAT Job ${i + 1}`,
        createdAt: new Date(),
        updatedAt: new Date(),
        filaments: [{ filamentId: '', grams: 50 + (i % 100) }],
        printTimeHours: 1 + (i % 8),
        printerInstanceId: '',
        modelCost: 0,
        prepTimeMinutes: 5,
        postProcessingMinutes: 10,
        materialsUsed: [],
        failureRate: 0.05,
        costPerUnit: 5 + (i % 20),
        sellingPrice: 15 + (i % 30),
        copiesSold: i % 5,
      }));
      await db.jobs.bulkAdd(jobs);
      console.log(`Seeded 500 UAT jobs. Prefix: uat-${now}`);
    })();
    ```

    **Seed snippet (printer assets, 20% with notes+tags per W3) — paste verbatim into DevTools console.**

    The PrinterAsset field set is derived from `src/types.ts:39-75` (verified). Required Asset fields: `id`, `name`, `category`. Optional but included for realism: `brand`, `currency`, `purchasePrice`, `wattage`, `nozzleCost`. 20% of seeded entries carry `notes` (multi-line) AND `tags` (3-tag array) to surface any clipping caused by Plan 05's numeric `rowHeight={56}`.
    ```
    (async () => {
      const { db } = await import('/src/db/database.ts');
      const now = Date.now();
      const assets = Array.from({ length: 200 }, (_, i) => {
        const carriesNotes = i % 5 === 0; // 20% subset
        return {
          id: `uat-asset-${now}-${i}`,
          name: `UAT Printer ${i + 1}`,
          category: 'printer',
          brand: `Brand ${i % 10}`,
          currency: 'USD',
          purchasePrice: 500 + i * 5,
          expectedLifespanHours: 5000,
          wattage: 100 + (i % 50),
          nozzleCost: 5 + (i % 10),
          nozzleLifespanCm3: 1000,
          ...(carriesNotes ? {
            notes: `Long note line 1 for UAT printer ${i + 1}.\nLine 2 — extra info that may wrap vertically.\nLine 3 — clipping test content.`,
            tags: ['fdm', 'enclosed', 'multi-color'],
          } : {}),
        };
      });
      await db.materials.bulkAdd(assets);
      console.log(`Seeded 200 UAT printer assets (40 with notes+tags). Prefix: uat-asset-${now}`);
    })();
    ```

    **UAT procedure — JobsManager (5 minutes):**
    7. DevTools → Performance tab → click the gear icon → set "CPU" to "4× slowdown"
    8. Click the red "Record" circle to start profiling
    9. Scroll the Jobs list from top to bottom and back to top (use the scrollbar or mousewheel — about 5 seconds of scrolling)
    10. Stop the recording
    11. In the Performance flame chart, look at the "Frames" track at the top — each frame is colored green (≤16.7ms), yellow (longer), or red (jank)
    12. **Pass criterion:** no red frames; ideally ≥95% green. Yellow frames are tolerated if rare and the perception is "smooth".
    13. Click on 3 different jobs (one near the top, one middle, one bottom). For each, confirm the card expands without overlapping its neighbours, and re-clicking collapses it cleanly.
    14. Reset the CPU throttle to "No throttling".

    **UAT procedure — AssetLibrary (5 minutes):**
    15. Refresh, go to Asset Library, switch items-per-page to "100". Confirm:
        - 100 rows render
        - Scrolling is smooth (no need to re-engage CPU throttle for this — visual judgement is sufficient at 100 rows)
        - For the printer table div-grid: column alignment matches across header and rows (header "Wattage" column lines up with row wattage values, etc.)
        - For the 20% of rows with notes+tags: check whether the 56px row height clips the content. Record the observation (clipped / not clipped) in the report below — this informs the follow-up decision per Plan 05's T-11-CLIP2 mitigation.
        - Sort header clicks (e.g., on "Printer name", "Brand") re-order the rows correctly
        - Clicking Edit on any row opens the edit form for THAT row's asset (closure correctness)
        - Clicking Delete on any row removes only that row
    16. Switch back to "10/page" — confirm the layout is the same div-grid shape but without virtualization wrappers (no scroll container)

    **Cleanup:**
    17. Paste into DevTools console:
    ```
    (async () => {
      const { db } = await import('/src/db/database.ts');
      const jobsDeleted = await db.jobs.where('id').startsWith('uat-').delete();
      const materialsDeleted = await db.materials.where('id').startsWith('uat-').delete();
      console.log(`Cleaned up: ${jobsDeleted} jobs, ${materialsDeleted} materials`);
    })();
    ```
    Refresh the page; verify the Jobs and Asset Library tabs return to their pre-UAT state.

    **Report:**
    18. Reply with:
        - `approved` + a one-line frame summary (e.g., "100% green frames during scroll; expand/collapse smooth on 3 sampled jobs; AssetLibrary 100/page smooth; notes+tags rows NOT clipped at 56px")
        - `approved with clipping` — virtualization passed but the 20% notes+tags rows clipped at 56px → triggers follow-up to swap to `useDynamicRowHeight` per Plan 05's T-11-CLIP2 mitigation
        - `regression: <description>` + which step failed
        - `blocked: <reason>` (e.g., snippet errored, dev server crashed)
  </how-to-verify>
  <acceptance_criteria>
    User has replied with `approved` or `approved with clipping`. Task 2 (writing the SUMMARY) only runs after one of those replies. A `regression:` reply triggers gap-closure planning per the planner's gap_closure mode — NOT a SUMMARY write. An `approved with clipping` reply DOES proceed to SUMMARY but the SUMMARY records the clipping observation and flags the `useDynamicRowHeight` follow-up.
  </acceptance_criteria>
  <resume-signal>Reply with one of the approval phrases above</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write the phase SUMMARY</name>
  <files>.planning/phases/11-performance-optimization/11-SUMMARY.md</files>
  <read_first>
    - .planning/phases/11-performance-optimization/11-01-SUMMARY.md
    - .planning/phases/11-performance-optimization/11-02-SUMMARY.md
    - .planning/phases/11-performance-optimization/11-03-SUMMARY.md
    - .planning/phases/11-performance-optimization/11-04-SUMMARY.md
    - .planning/phases/11-performance-optimization/11-05-SUMMARY.md
    - .planning/phases/11-performance-optimization/11-CONTEXT.md (for the original decision IDs)
    - $HOME/.claude/get-shit-done/templates/summary.md (template — if available; otherwise mirror the 10-SUMMARY.md structure)
  </read_first>
  <action>
    Create `.planning/phases/11-performance-optimization/11-SUMMARY.md`. Sections (in order):

    **1. Phase Goal & Outcome** — Quote the ROADMAP Phase 11 goal verbatim. State the four success criteria + which ones passed (all four expected: chunk-split visible in dist/, main chunk <300KB gzipped, virtualization above thresholds, identical behavior below).

    **2. What Shipped** (one short paragraph per plan):
    - Plan 01 — installed react-window@<exact version from 11-01-SUMMARY> + rollup-plugin-visualizer@<version>, added scripts.analyze
    - Plan 02 — vite.config.ts manualChunks: 3 chunks (react-vendor, dexie-vendor, vendor); conditional visualizer gated on mode==='analyze'
    - Plan 03 — scripts/assert-bundle-size.mjs; final main-chunk gzipped size = <NNN KB> (headroom = <300-NNN KB>)
    - Plan 04 — JobsManager virtualized above 100 jobs using react-window v2 `<List>` + `useDynamicRowHeight({ defaultRowHeight: 88, key: selectedJobId ?? '' })`; selection toggles invalidate the cache via the `key` arg (design Q2 resolution — no `.reset()` method exists in v2)
    - Plan 05 — AssetLibrary three call sites virtualized above 50 items/page; design Q1 resolved by replacing the two desktop `<table>` blocks with `<div>`-based grids (ARIA `role="row"`/`role="columnheader"`/`role="cell"` preserves semantics). Iteration-1's `tagName="tbody"` + `<colgroup>` approach was rejected as structurally broken.
    - Plan 06 — manual UAT passed (with or without clipping observation); this SUMMARY (design Q3 resolution = DevTools snippet, no script file)

    **3. Decisions Honored** — Table mapping D-01 through D-13 to "✓ honored" / "↻ overridden (reason)". The visualizer version bump (if Plan 01 used ^7 instead of ^5) goes here as a documented override of D-10.

    **4. How to Repeat the UAT** — Embed the seed-500-jobs DevTools snippet, the seed-200-printers snippet (with the 20% notes+tags subset), the cleanup snippet (covering BOTH `db.jobs` AND `db.materials`), and the 4× CPU throttle procedure VERBATIM from Task 1's `<how-to-verify>` block. Future devs paste these into the console without having to find this Task. This is the design Q3 resolution — the snippets live HERE and ONLY HERE; no `scripts/seed-*.mjs` file is checked in. **All snippets must import from `'/src/db/database.ts'` (verified path) and use `db.materials` (NOT `db.assets` — there is no `assets` table).**

    **5. Bundle Composition (post-split)** — From 11-02-SUMMARY: filename + size for each of the three vendor chunks plus the main entry chunk. One line on the largest contributor to `vendor-*.js` per the visualizer treemap.

    **6. Threat-model status** — T-11-SC (package legitimacy) — checkpoint passed in Plan 01. T-11-LK (visualizer leak) — verified by Plan 02 (`dist/stats.html` absent from default builds). T-11-GATE (threshold drift), T-11-COLALIGN, T-11-CLIP, T-11-CLIP2 — all mitigated / observed as documented.

    **7. Pointers for v1.2 / future phases** —
    - If the main chunk grows past 250 KB gzipped, consider per-library granular chunking (CONTEXT.md "Deferred Ideas" — D-02 reconsideration trigger)
    - If the UAT reported clipping on the 20% notes+tags rows (T-11-CLIP2), swap AssetLibrary's numeric `rowHeight={56}` for `useDynamicRowHeight({ defaultRowHeight: 56 })` (the JobsManager pattern from Plan 04). The seed snippet already includes the clipping fixture — re-running the UAT validates the fix.
    - The div-grid replacement (Plan 05) lost native `<table>` markup. If a future accessibility audit requires `<table>` semantics back, the design Q1 needs revisiting (likely via a CSS-only solution that keeps `<table>` layout intact alongside virtualization, e.g., not virtualizing tables at all and accepting the perf cost above 50/page).
    - Automated perf testing belongs in v2 alongside E2E (TEST-F1 per REQUIREMENTS.md)

    **8. Files modified** — list each file touched across all 6 plans with a one-phrase reason.

    **9. NEW Badge** — NOT added (CONTEXT.md "Out of scope" — infrastructure improvement is invisible to users; user perceives speed but no new feature). Confirm in this section.

    Use Markdown. Keep total length under ~250 lines.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-performance-optimization/11-SUMMARY.md</automated>
    <automated>grep -c "PERF-01" .planning/phases/11-performance-optimization/11-SUMMARY.md | awk '{ if ($1 >= 2) print "OK"; else exit 1 }'</automated>
    <automated>grep -c "PERF-02" .planning/phases/11-performance-optimization/11-SUMMARY.md | awk '{ if ($1 >= 2) print "OK"; else exit 1 }'</automated>
    <automated>grep -c "DevTools" .planning/phases/11-performance-optimization/11-SUMMARY.md | awk '{ if ($1 >= 1) print "OK"; else exit 1 }'</automated>
    <automated>grep -c "bulkAdd" .planning/phases/11-performance-optimization/11-SUMMARY.md | awk '{ if ($1 >= 2) print "OK: seed snippet embedded for jobs + assets"; else exit 1 }'</automated>
    <automated>grep -c "db.materials" .planning/phases/11-performance-optimization/11-SUMMARY.md | awk '{ if ($1 >= 2) print "OK: uses db.materials (not db.assets)"; else { print "FAIL: snippets must reference db.materials"; exit 1 } }'</automated>
    <automated>grep -c "/src/db/database.ts" .planning/phases/11-performance-optimization/11-SUMMARY.md | awk '{ if ($1 >= 3) print "OK: correct db import path"; else { print "FAIL: snippets must import from /src/db/database.ts"; exit 1 } }'</automated>
    <automated>grep -c "db.assets" .planning/phases/11-performance-optimization/11-SUMMARY.md | grep -q "^0$" || (echo "FAIL: db.assets must not appear (correct table is db.materials)"; exit 1)</automated>
    <automated>grep -cE "D-0[1-9]|D-1[0-3]" .planning/phases/11-performance-optimization/11-SUMMARY.md | awk '{ if ($1 >= 13) print "OK: all 13 decisions referenced"; else { print "FAIL: not all decisions covered: " $1; exit 1 } }'</automated>
    <automated>! ls scripts/seed-test-jobs.mjs 2>/dev/null && echo "OK: no seed script file (design Q3 resolution)"</automated>
  </verify>
  <done>
    `.planning/phases/11-performance-optimization/11-SUMMARY.md` exists with all 9 sections. The DevTools console snippets are embedded so the UAT is repeatable by any future maintainer — they import from `/src/db/database.ts` and use `db.jobs` + `db.materials` (NOT `db.assets`). No `scripts/seed-test-jobs.mjs` file is created (design Q3). All 13 decisions from CONTEXT.md are referenced in section 3. ROADMAP.md is unchanged here — the orchestrator marks Phase 11 complete and updates ROADMAP/STATE in a separate step.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live dev server → UAT browser session | The UAT runs against actual production-mode artifacts (mostly — `npm run dev` is the Vite dev server, NOT the built bundle; some perf differences exist but the virtualization behavior is identical) |
| UAT seed data → user's real Dexie | The UAT-seeded jobs/assets all use the `uat-` ID prefix so the cleanup snippet can target them precisely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-UATLEAK | Information Disclosure (test data persists in real DB) | UAT jobs/materials left in Dexie after test | mitigate | Cleanup snippet (step 17 in UAT) deletes by `uat-` prefix from BOTH `db.jobs` AND `db.materials`. SUMMARY emphasizes running it before closing the session. |
| T-11-DEVVSBUILD | Tampering (UAT doesn't observe prod build) | `npm run dev` uses Vite dev server with HMR, NOT the production bundle | accept | Documented limitation; the bundle-size gate in Plan 03 already verifies prod build is healthy. Virtualization behavior is identical between dev and prod (react-window has no env-conditional paths). For true prod UAT, `npm run preview` is available — call out in SUMMARY as an optional follow-up. |
| T-11-SEEDDRIFT | Tampering (snippets diverge from real types) | If `PrintJob` or `Asset` acquires new required fields after this plan ships, the snippets will fail Dexie's type assertions | mitigate | Action `read_first` instructs the executor to re-verify `src/types.ts` against the snippet before running. SUMMARY section 4 directs future maintainers to do the same. |
</threat_model>

<verification>
After Task 2, the SUMMARY contains:
- The four ROADMAP success criteria, each marked passed
- The full DevTools UAT procedure (so future devs repeat without re-deriving), using `/src/db/database.ts` and `db.jobs`/`db.materials`
- A decision honor table covering D-01 through D-13
- Measured bundle size + headroom
- Design Q1, Q2, Q3 resolutions explicitly recorded (Q1 = div-grid, Q2 = `useDynamicRowHeight` with `key` arg, Q3 = DevTools snippets in SUMMARY)
- File list
- The clipping observation from the 20% notes+tags subset

The orchestrator then commits the SUMMARY and updates STATE.md/ROADMAP.md to mark Phase 11 complete.
</verification>

<success_criteria>
- The UAT checkpoint received an `approved` or `approved with clipping` reply from a human
- `.planning/phases/11-performance-optimization/11-SUMMARY.md` exists and contains all 9 sections
- No `scripts/seed-test-jobs.mjs` file exists in the repo (design Q3)
- All 13 CONTEXT.md decisions (D-01 through D-13) are referenced in section 3
- The DevTools seed/cleanup snippets are embedded verbatim and use the correct db path + table names (`/src/db/database.ts`, `db.jobs`, `db.materials`)
- Plan 04 and Plan 05 design resolutions are recorded (incl. the iteration-2 corrections)
- The 20% notes+tags seed subset is documented; clipping observation captured
</success_criteria>

<output>
This plan's output IS `.planning/phases/11-performance-optimization/11-SUMMARY.md`. There is no separate per-plan SUMMARY for Plan 06 (it's the phase-level one).
</output>
