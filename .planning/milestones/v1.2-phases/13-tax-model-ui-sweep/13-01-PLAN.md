---
phase: 13-tax-model-ui-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/taxRates.ts
  - src/utils/taxResolution.ts
  - src/utils/taxResolution.test.ts
autonomous: true
requirements:
  - TAX-03
tags:
  - tax
  - data
  - util
  - wave-0

must_haves:
  truths:
    - "D-01: src/data/taxRates.ts ships 32 rows — 5 currency-keyed lookup rows (USD/GBP/AUD/CAD/SEK) + 27 EU country reference rows + supporting EUR/CZK/DKK/PLN currency tags"
    - "D-02: TAX_RATES is keyed/looked-up by Currency; resolveTaxRate uses input.currency for the .find()"
    - "D-03: Every TaxRateEntry carries rateAsOf (ISO date); isRateStale returns true when more than 18 months past rateAsOf"
    - "D-04: resolveTaxRate returns kind: 'manual' (rate 0) for unknown currencies — the chain never silently defaults to 0% via fallthrough"
    - "D-05: When input.currency === 'EUR', resolveTaxRate returns kind: 'eu-average' with rate 21 BEFORE any array .find() — Pitfall 4 (EU collapse) is prevented by branch ordering"
    - "D-11: US row carries the locked marketplace-facilitator note string verbatim (UI-SPEC line 117); note travels into TaxRateSource.note for tooltip consumption"
    - "TAX-03 chain order (override → settings → eu-average → region → manual) is implemented in resolveTaxRate per RESEARCH Example 2"
    - "tooltipForSource(source, userCurrency) returns the locked strings from UI-SPEC table at 13-UI-SPEC.md:113-126 — one branch per TaxRateSource.kind. When userCurrency === 'USD', the locked D-11 marketplace-facilitator string is appended to the tooltip for ALL source.kind values (override / settings / region / eu-average / manual), implementing D-12 (note persists on US user-entered rates) and D-13 (note appears in both Settings and Cost Breakdown surfaces)"
    - "All Vitest cases listed in 13-VALIDATION.md:51-55 pass (US region default, EU average, manual unknown region, is stale, is fresh) AND the new tooltipForSource USD-append cases pass (D-12 + D-13 coverage)"
    - "tsc -b passes — readonly TaxRateEntry[] and discriminated union narrow correctly"
  artifacts:
    - path: "src/data/taxRates.ts"
      provides: "Static region tax-rate table + EU_AVERAGE_RATE constant + TaxRateEntry interface"
      contains: "export const TAX_RATES"
    - path: "src/utils/taxResolution.ts"
      provides: "resolveTaxRate fallback chain + isRateStale + tooltipForSource + TaxRateSource discriminated union"
      contains: "export function resolveTaxRate"
    - path: "src/utils/taxResolution.test.ts"
      provides: "Vitest suite covering resolveTaxRate branches and isRateStale boundary cases"
      contains: "describe('resolveTaxRate'"
  key_links:
    - from: "src/utils/taxResolution.ts"
      to: "src/data/taxRates.ts"
      via: "named import"
      pattern: "import.*\\{.*TAX_RATES.*\\}.*from.*data/taxRates"
    - from: "src/utils/taxResolution.ts"
      to: "src/types.ts"
      via: "type-only import of Currency"
      pattern: "import type.*Currency.*from.*types"
---

<objective>
Create the static tax-rate data file, the pure-function resolveTaxRate fallback-chain helper, and the Vitest suite that locks the resolution behavior. This is the foundation layer for all downstream tax UI (Plans 03 and 05) — both Default Tax Rate (Settings) and the per-job Tax row (Cost Breakdown) consume resolveTaxRate + tooltipForSource.

Purpose: Centralize the fallback chain (per-job override → Settings default → EU-average branch → region table → manual) in one helper so the Settings UI and CostCalculator never drift. The discriminated TaxRateSource type carries provenance (override / settings / region / eu-average / manual) into the tooltip layer for D-08 / D-13.

Output: Three new files — one static data file, one pure-util module, one Vitest spec. No React, no Dexie, no IO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-tax-model-ui-sweep/13-CONTEXT.md
@.planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md
@.planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md
@.planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md
@.planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md
@src/data/bambuFilaments.ts
@src/types.ts

<interfaces>
<!-- Existing surface this plan binds to. Executor uses these directly — no codebase exploration needed. -->

From src/types.ts (Currency type, lines 12-30):
```typescript
export type Currency = 'USD' | 'EUR' | 'GBP' | 'AUD' | 'NZD' | 'CAD' | 'JPY' | 'CHF'
  | 'SEK' | 'NOK' | 'DKK' | 'PLN' | 'CZK' | 'HUF' | 'RON' | 'BGN' | 'HRK' | 'ZAR';
```

From src/data/bambuFilaments.ts (header-comment + typed-array pattern for src/data/ files):
- 3-line provenance header
- `import type { ... } from '../types';`
- One `export const` typed-array, one entry per line for diff-clean editing

Sibling pure-util pattern at src/utils/costCalc.ts:1-23 (header comment + exported interfaces above functions + named exports + defensive returns over throws).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/data/taxRates.ts static region table</name>
  <files>src/data/taxRates.ts</files>
  <read_first>
    - src/data/bambuFilaments.ts (canonical analog for src/data/ file shape: header comment, type import, typed array of objects, comma-trailing rows)
    - src/types.ts:12-30 (Currency union — exactly 18 currencies including USD/EUR/GBP/AUD/CAD/CZK/DKK/PLN/SEK/ZAR — verify the table only references currencies in this union)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 269-334 (RESEARCH Example 3: complete TAX_RATES array, 32 rows, with rateAsOf dates and US/CA notes)
    - .planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md lines 113-122 (locked strings: the US marketplace-facilitator note and CA federal-GST note copy verbatim into the row `note` fields)
  </read_first>
  <behavior>
    - Test 1 (covered by Task 3): TAX_RATES.find(r => r.currency === 'USD') returns the US row with rate=0 and note matching D-11 wording
    - Test 2 (covered by Task 3): TAX_RATES.find(r => r.currency === 'GBP') returns rate=20, region='GB'
    - Test 3 (covered by Task 3): EU_AVERAGE_RATE === 21 (D-05 lock)
    - Test 4 (covered by Task 3): TAX_RATES has 32 entries total (5 currency-keyed + 27 EU country reference rows)
  </behavior>
  <action>
    Create new file src/data/taxRates.ts. Header comment block (3-4 lines) explaining: "Static tax-rate table keyed off Currency for the resolveTaxRate fallback chain. 27 EU member states + UK/AU/CA/US. EU is consumed via EU_AVERAGE_RATE branch in resolveTaxRate (D-05); per-country rows are reference data. Source: Tax Foundation 2026 VAT Rates + EU Commission TEDB."

    Add `import type { Currency } from '../types';` per the bambuFilaments.ts convention.

    Export interface `TaxRateEntry` with these exact fields: `currency: Currency`, `region: string`, `label: string`, `rate: number`, `rateAsOf: string`, `note?: string`.

    Export constant `EU_AVERAGE_RATE = 21` (per D-05 lock; documented inline that the value is the median of 27 EU standard rates, not the mean — see RESEARCH Discrepancy 2).

    Export constant `TAX_RATES: readonly TaxRateEntry[] = [...]` with the EXACT 32 rows from RESEARCH Example 3 (13-RESEARCH.md:294-334). The `readonly` modifier is required — TAX_RATES is pure lookup data (PATTERNS.md S7 divergence from bambuFilaments).

    The 5 currency-keyed lookup rows (the ones resolveTaxRate's array.find() will actually return):
    - USD/US: rate=0, rateAsOf='2025-01-01', note=locked US marketplace-facilitator string from UI-SPEC line 117 (verbatim: "Most US states require marketplaces (Etsy, eBay, Amazon) to collect sales tax for you. Override only if you sell direct or in a non-facilitator state.")
    - GBP/GB: rate=20, rateAsOf='2011-01-04', no note
    - AUD/AU: rate=10, rateAsOf='2000-07-01', no note
    - CAD/CA: rate=5, rateAsOf='2008-01-01', note=locked CA federal-GST string from UI-SPEC line 118 (verbatim: "Federal GST only. Provincial sales tax (PST/HST) varies — override for your province.")
    - SEK/SE: rate=25, rateAsOf='1996-01-01', no note (Sweden — also one of the 27 EU member state rows)

    The 27 EU member state rows (currency tags: EUR/CZK/DKK/PLN/SEK as appropriate per RESEARCH Example 3). EU-currency members (e.g. Austria, Belgium, Germany) carry `currency: 'EUR'` — these are reference rows; resolveTaxRate skips them via the EUR branch (Pitfall 4 prevention).

    Each row on its own line, trailing comma, follow the exact spacing in RESEARCH Example 3 for diff-clean editing.

    No functions in this file — pure data.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/13-01-task1.tsc.log; ! grep -q "error TS" /tmp/13-01-task1.tsc.log</automated>
  </verify>
  <acceptance_criteria>
    - File exists: src/data/taxRates.ts contains `export interface TaxRateEntry`, `export const EU_AVERAGE_RATE`, and `export const TAX_RATES`
    - `grep -c "^  {" src/data/taxRates.ts` returns 32 (each row starts with two-space `  {`)
    - `grep -c "rateAsOf:" src/data/taxRates.ts` returns 32 (every row has rateAsOf)
    - `grep "EU_AVERAGE_RATE = 21" src/data/taxRates.ts` matches
    - `grep "readonly TaxRateEntry\[\]" src/data/taxRates.ts` matches
    - US row note matches D-11 exact wording (substring): `grep -F "Most US states require marketplaces (Etsy, eBay, Amazon) to collect sales tax for you. Override only if you sell direct or in a non-facilitator state." src/data/taxRates.ts` finds the line
    - CA row note matches D-locked wording (substring): `grep -F "Federal GST only. Provincial sales tax (PST/HST) varies — override for your province." src/data/taxRates.ts` finds the line
    - `npx tsc -b` exits 0 with no errors
  </acceptance_criteria>
  <done>
    src/data/taxRates.ts compiles, exports TaxRateEntry / EU_AVERAGE_RATE / TAX_RATES, and contains 32 rows with the exact US and CA note strings from UI-SPEC.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/utils/taxResolution.ts with resolveTaxRate, isRateStale, tooltipForSource</name>
  <files>src/utils/taxResolution.ts</files>
  <read_first>
    - src/utils/costCalc.ts:1-23 (pure-util module header convention — header comment, interface exports above functions, named exports only, defensive returns)
    - src/utils/costCalc.ts:62-80 (defensive-return idiom — calculateElectricityCost ternary and calculateDepreciation guard)
    - src/data/taxRates.ts (the file Task 1 just created — TaxRateEntry, EU_AVERAGE_RATE, TAX_RATES export names)
    - src/types.ts:12-30 (Currency union — the resolveTaxRate input type)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 213-266 (RESEARCH Example 2: full resolveTaxRate + isRateStale implementations)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 694-715 (RESEARCH Example 3: tooltipForSource switch implementation)
    - .planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md lines 111-126 (locked tooltip body-text strings, branch by source.kind AND userCurrency — the override-on-USD, settings-on-USD, and eu-average-on-USD rows enforce D-12 / D-13)
  </read_first>
  <behavior>
    - Test 1 (covered by Task 3): resolveTaxRate({jobOverride: 8, settingsDefault: 20, currency: 'USD'}) returns {kind: 'override', rate: 8} — override wins over settings
    - Test 2 (covered by Task 3): resolveTaxRate({jobOverride: undefined, settingsDefault: 15, currency: 'GBP'}) returns {kind: 'settings', rate: 15} — settings wins over region
    - Test 3 (covered by Task 3): resolveTaxRate({jobOverride: undefined, settingsDefault: undefined, currency: 'USD'}) returns {kind: 'region', rate: 0, region: 'US', rateAsOf: '2025-01-01', note: <marketplace-facilitator>}
    - Test 4 (covered by Task 3): resolveTaxRate with currency='EUR' returns {kind: 'eu-average', rate: 21, note: <EU verify wording>}
    - Test 5 (covered by Task 3): resolveTaxRate with currency='ZAR' returns {kind: 'manual', rate: 0}
    - Test 6 (covered by Task 3): isRateStale('2024-01-01', new Date('2026-05-21')) returns true (~28 months)
    - Test 7 (covered by Task 3): isRateStale('2025-08-01', new Date('2026-05-21')) returns false (~9.7 months)
    - Test 8 (covered by Task 3): isRateStale('not-a-date') returns false (NaN guard)
    - Test 9 (covered by Task 3): tooltipForSource returns the exact UI-SPEC strings for each source.kind
  </behavior>
  <action>
    Create new file src/utils/taxResolution.ts. Top-of-file: 3-line header comment mirroring src/utils/costCalc.ts:11-13 ("Pure tax-resolution module — owns the fallback chain (override → settings → eu-average → region → manual). No React, no Dexie, no IO. Consumed by SettingsModal Default Tax Rate field and CostCalculator Tax row.").

    Imports (relative paths only, per CONVENTIONS.md):
    - `import type { Currency } from '../types';` (consumed by both `resolveTaxRate` input AND `tooltipForSource` second argument)
    - `import { TAX_RATES, EU_AVERAGE_RATE, type TaxRateEntry } from '../data/taxRates';`

    Export type alias `TaxRateSource` as a discriminated union with these EXACT 5 variants (RESEARCH Example 2 lines 217-222):
    - `{ kind: 'override'; rate: number }`
    - `{ kind: 'settings'; rate: number }`
    - `{ kind: 'region'; rate: number; region: string; rateAsOf: string; note?: string }`
    - `{ kind: 'eu-average'; rate: number; note: string }`
    - `{ kind: 'manual'; rate: 0 }`

    Export interface `ResolveTaxRateInput` with these fields (RESEARCH Example 2 lines 224-228): `jobOverride: number | undefined`, `settingsDefault: number | undefined`, `currency: Currency`. The `| undefined` is explicit (not `?:`) — the input is a fully-formed call site.

    Export function `resolveTaxRate(input: ResolveTaxRateInput): TaxRateSource`. Chain order (RESEARCH Example 2 lines 230-257, lock):
    1. `typeof input.jobOverride === 'number'` → return `{ kind: 'override', rate: input.jobOverride }`
    2. `typeof input.settingsDefault === 'number'` → return `{ kind: 'settings', rate: input.settingsDefault }`
    3. `input.currency === 'EUR'` → return `{ kind: 'eu-average', rate: EU_AVERAGE_RATE, note: 'EU midpoint rate — verify for your country' }` (NOTE: use the word "midpoint" per UI-SPEC line 122 note; RESEARCH text says "average" but UI-SPEC locked "midpoint" — UI-SPEC wins). The EUR branch comes BEFORE the .find() to prevent Pitfall 4 (currency-keyed EU collapse).
    4. `const row = TAX_RATES.find(r => r.currency === input.currency)` → if row found, return `{ kind: 'region', rate: row.rate, region: row.region, rateAsOf: row.rateAsOf, note: row.note }`
    5. Default (no match): return `{ kind: 'manual', rate: 0 }` (per TAX-03: never silently default to 0% via fallthrough; this is the explicit unknown-region sentinel)

    Export function `isRateStale(rateAsOf: string, now: Date = new Date()): boolean` per RESEARCH Example 2 lines 260-265:
    - Parse: `const asOf = new Date(rateAsOf)`
    - Guard: `if (Number.isNaN(asOf.getTime())) return false;`
    - Compute months: `const monthsApprox = (now.getTime() - asOf.getTime()) / (1000 * 60 * 60 * 24 * 30.44)` (30.44 = average days/month — Pitfall 5)
    - Return `monthsApprox > 18` (D-03 threshold)

    Export function `tooltipForSource(source: TaxRateSource, userCurrency: Currency): string`. The second `userCurrency` argument is REQUIRED — it powers D-12 / D-13 (US marketplace-facilitator note persists across override / settings / eu-average / manual branches, not just region). Switch on `source.kind`, build the base string from the UI-SPEC table at 13-UI-SPEC.md:113-126, then append the locked D-11 US note when `userCurrency === 'USD'`. Each branch:
    - `'override'`: base = `\`Per-job override — ${source.rate}%\``
    - `'settings'`: base = `\`From Settings default — ${source.rate}%\``
    - `'region'`: compose with `isRateStale(source.rateAsOf)` flag — append " — rate may be stale, verify locally" when stale; append "\n" + source.note when present. Locked template: `\`From your region (${source.region}, as of ${source.rateAsOf}${stale}).${note ? '\\n' + note : ''}\``. (The US region row already carries the D-11 note inline via `source.note`; the USD-append step below avoids double-appending — see the de-dup guard.)
    - `'eu-average'`: base = `\`${source.note} (${source.rate}%)\`` (composes the locked note text + percent)
    - `'manual'`: base = `'Unknown region — enter manually. We don\\'t have a default rate for your currency yet.'`

    AFTER computing the base string, apply the USD-append step (D-12 + D-13):
    - Const `US_NOTE = 'Most US states require marketplaces (Etsy, eBay, Amazon) to collect sales tax for you. Override only if you sell direct or in a non-facilitator state.'` — defined once at module top.
    - If `userCurrency === 'USD'` AND the base string does NOT already include `US_NOTE` (de-dup guard for the `region` US branch whose `source.note` already contains it), return `\`${base}\\n${US_NOTE}\`` — newline-separated append. Otherwise return `base` unchanged.

    Examples to validate (covered by Task 3 tests):
    - `tooltipForSource({kind:'override', rate:8}, 'USD')` → `Per-job override — 8%\nMost US states require marketplaces...`
    - `tooltipForSource({kind:'override', rate:8}, 'GBP')` → `Per-job override — 8%`
    - `tooltipForSource({kind:'settings', rate:15}, 'USD')` → `From Settings default — 15%\nMost US states require marketplaces...`
    - `tooltipForSource({kind:'eu-average', rate:21, note:'EU midpoint rate — verify for your country'}, 'EUR')` → `EU midpoint rate — verify for your country (21%)`
    - `tooltipForSource({kind:'region', rate:0, region:'US', rateAsOf:'2025-01-01', note:'<US note>'}, 'USD')` → returns the region string with the note INLINE (from source.note) and DOES NOT double-append (de-dup guard).

    No throws anywhere. The `kind: 'manual'` branch is the explicit unknown-region sentinel.

    Per CONVENTIONS.md: 2-space indent, single quotes, trailing commas, named exports only (no default export).
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/13-01-task2.tsc.log; ! grep -q "error TS" /tmp/13-01-task2.tsc.log</automated>
  </verify>
  <acceptance_criteria>
    - File exists: src/utils/taxResolution.ts contains `export function resolveTaxRate`, `export function isRateStale`, `export function tooltipForSource`
    - `grep -c "^export " src/utils/taxResolution.ts` returns 5 or 6 (type TaxRateSource, interface ResolveTaxRateInput, 3 functions; possibly re-export type)
    - `grep "kind: 'override'\|kind: 'settings'\|kind: 'region'\|kind: 'eu-average'\|kind: 'manual'" src/utils/taxResolution.ts` shows all 5 variants
    - Import line present: `grep "from '../data/taxRates'" src/utils/taxResolution.ts` matches
    - Import line present: `grep "import type.*Currency" src/utils/taxResolution.ts` matches
    - EUR branch is BEFORE the .find() — verify by reading the function body in order
    - tooltipForSource signature accepts userCurrency: `grep -E "function tooltipForSource\(source: TaxRateSource, *userCurrency: Currency\)" src/utils/taxResolution.ts` matches
    - USD-append constant defined: `grep -F "Most US states require marketplaces (Etsy, eBay, Amazon) to collect sales tax for you. Override only if you sell direct or in a non-facilitator state." src/utils/taxResolution.ts` matches at least once (the US_NOTE constant)
    - USD-append guard wired: `grep -F "userCurrency === 'USD'" src/utils/taxResolution.ts` matches inside tooltipForSource body
    - No `throw` statements anywhere in the file: `! grep -E "^\s*throw " src/utils/taxResolution.ts`
    - `npx tsc -b` exits 0 with no errors
  </acceptance_criteria>
  <done>
    src/utils/taxResolution.ts compiles. resolveTaxRate exists with the 5-step fallback chain (override → settings → eu-average → region → manual). isRateStale and tooltipForSource exist with the locked UI-SPEC string outputs. No throws.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create src/utils/taxResolution.test.ts Vitest suite</name>
  <files>src/utils/taxResolution.test.ts</files>
  <read_first>
    - src/utils/costCalc.test.ts:1-21 (Vitest imports + named-import pattern from sibling)
    - src/utils/costCalc.test.ts:108-156 (describe-block shape: one describe per exported function, descriptive `it` names)
    - src/utils/taxResolution.ts (the file Task 2 just created — function signatures and TaxRateSource shape)
    - .planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md lines 51-55 (locked test substrings for the harness: "US region default", "EU average", "manual (unknown region)", "is stale", "is fresh")
    - .planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md lines 157-168 (test-coverage map for resolveTaxRate + isRateStale)
  </read_first>
  <behavior>
    - Five resolveTaxRate branches each get an it() (override-wins, settings-wins, US region default, EU average, manual unknown region)
    - Two isRateStale boundary cases plus invalid-date guard
    - Test names contain the exact substrings listed in VALIDATION.md so `vitest run -t "..."` matches: "US region default", "EU average", "manual (unknown region)", "is stale", "is fresh"
  </behavior>
  <action>
    Create new file src/utils/taxResolution.test.ts mirroring src/utils/costCalc.test.ts:1-21.

    Imports:
    - `import { describe, it, expect } from 'vitest';`
    - `import { resolveTaxRate, isRateStale, tooltipForSource } from './taxResolution';`
    - `import type { TaxRateSource } from './taxResolution';` (type-only)

    First describe block: `describe('resolveTaxRate', () => { ... })` with these test cases (each name MUST contain the bolded substring so VALIDATION harness greps match):
    1. `it('returns kind override when jobOverride is set (override wins over settings and region)', ...)` — input `{jobOverride: 8, settingsDefault: 20, currency: 'USD'}`; expect `{kind: 'override', rate: 8}`
    2. `it('returns kind settings when only settingsDefault is set (settings wins over region)', ...)` — input `{jobOverride: undefined, settingsDefault: 15, currency: 'GBP'}`; expect `{kind: 'settings', rate: 15}`
    3. `it('US region default returns kind region with rate 0 and marketplace-facilitator note', ...)` — input `{jobOverride: undefined, settingsDefault: undefined, currency: 'USD'}`; narrow with `if (result.kind === 'region')`; expect `result.rate === 0`, `result.region === 'US'`, `result.rateAsOf === '2025-01-01'`, `result.note` contains "marketplaces"
    4. `it('EU average returns kind eu-average with rate 21 for EUR currency', ...)` — input `{jobOverride: undefined, settingsDefault: undefined, currency: 'EUR'}`; expect `{kind: 'eu-average', rate: 21}` (note string presence not asserted strictly — just shape)
    5. `it('manual (unknown region) returns kind manual rate 0 for currency not in TAX_RATES', ...)` — input `{jobOverride: undefined, settingsDefault: undefined, currency: 'ZAR'}`; expect `{kind: 'manual', rate: 0}`
    6. `it('GBP region default returns kind region with rate 20', ...)` — input `{jobOverride: undefined, settingsDefault: undefined, currency: 'GBP'}`; expect `kind: 'region'`, `rate: 20`, `region: 'GB'`

    Second describe block: `describe('isRateStale', () => { ... })` with these test cases:
    1. `it('is stale when rateAsOf is more than 18 months before now', ...)` — call `isRateStale('2024-01-01', new Date('2026-05-21'))`; expect `true`
    2. `it('is fresh when rateAsOf is within 18 months of now', ...)` — call `isRateStale('2025-08-01', new Date('2026-05-21'))`; expect `false`
    3. `it('returns false for invalid date string (NaN guard)', ...)` — call `isRateStale('not-a-date', new Date('2026-05-21'))`; expect `false`

    Third describe block: `describe('tooltipForSource', () => { ... })` with assertions per discriminator kind PLUS USD-append coverage (≥ 8 tests). Each one constructs a TaxRateSource literal and asserts the returned string contains the locked text (use `.toContain()` for portions):
    - override + non-USD: `tooltipForSource({kind:'override', rate:8}, 'GBP')` → contains `"Per-job override —"` AND does NOT contain `"marketplaces"`
    - override + USD: `tooltipForSource({kind:'override', rate:8}, 'USD')` → contains `"Per-job override —"` AND contains `"marketplaces (Etsy, eBay, Amazon)"` (D-12 lock)
    - settings + non-USD: `tooltipForSource({kind:'settings', rate:15}, 'GBP')` → contains `"From Settings default —"` AND does NOT contain `"marketplaces"`
    - settings + USD: `tooltipForSource({kind:'settings', rate:15}, 'USD')` → contains `"From Settings default —"` AND contains `"marketplaces"` (D-13 lock)
    - region + non-USD (GBP): contains `"From your region (GB"` AND does NOT contain `"marketplaces"`
    - region + USD (de-dup guard): `tooltipForSource({kind:'region', rate:0, region:'US', rateAsOf:'2025-01-01', note:'<full D-11 note string>'}, 'USD')` → contains `"From your region (US"` AND contains `"marketplaces"` exactly ONCE (use `.match(/marketplaces/g).length === 1` or `str.split('marketplaces').length === 2` to assert no double-append)
    - eu-average + EUR: contains `"EU midpoint rate"` AND does NOT contain `"marketplaces"`
    - manual + non-USD: contains `"Unknown region"` AND does NOT contain `"marketplaces"`
    - manual + USD: contains `"Unknown region"` AND contains `"marketplaces"` (D-13 lock: even the manual sentinel surfaces the US note when the user is in USD)

    Use object literals for input — no fixture builders needed (3 scalar fields per call).

    No new test runner config. Existing vitest.config.ts at repo root handles src/**/*.test.ts.
  </action>
  <verify>
    <automated>npx vitest run src/utils/taxResolution.test.ts --reporter=default 2>&1 | tee /tmp/13-01-task3.test.log; grep -q "PASS\|✓.*passed" /tmp/13-01-task3.test.log && ! grep -q "FAIL\|✗" /tmp/13-01-task3.test.log</automated>
  </verify>
  <acceptance_criteria>
    - File exists: src/utils/taxResolution.test.ts
    - `grep -c "describe(" src/utils/taxResolution.test.ts` returns 3 (resolveTaxRate, isRateStale, tooltipForSource)
    - Test substrings present (VALIDATION harness matches): `grep -F "US region default" src/utils/taxResolution.test.ts` matches; same for "EU average", "manual (unknown region)", "is stale", "is fresh"
    - `npx vitest run src/utils/taxResolution.test.ts -t "US region default"` exits 0
    - `npx vitest run src/utils/taxResolution.test.ts -t "EU average"` exits 0
    - `npx vitest run src/utils/taxResolution.test.ts -t "manual (unknown region)"` exits 0
    - `npx vitest run src/utils/taxResolution.test.ts -t "is stale"` exits 0
    - `npx vitest run src/utils/taxResolution.test.ts -t "is fresh"` exits 0
    - Full file run `npx vitest run src/utils/taxResolution.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>
    src/utils/taxResolution.test.ts contains 11 passing tests across 3 describe blocks. All 5 VALIDATION-locked substrings are present in test names. Full file run exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → tax rate state | Currency string and numeric rates enter via UI props; resolveTaxRate consumes them locally. No network, no other process. |
| Static data file → consumers | TAX_RATES is bundled at build time; tampering requires source modification, not a runtime attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | resolveTaxRate input | mitigate | Type-narrow Currency at call sites (TypeScript strict mode); explicit `typeof input.jobOverride === 'number'` guards reject non-numeric input — string `'5'` cannot accidentally become the override |
| T-13-02 | Information Disclosure | TAX_RATES static file | accept | Tax rates are public information (Tax Foundation + EU Commission TEDB). No PII, no secrets. Bundled at build time. |
| T-13-03 | Denial of Service | isRateStale date parsing | mitigate | `Number.isNaN(asOf.getTime())` guard returns false on invalid input; no recursion, no unbounded loops. Math is O(1). |
| T-13-04 | Tampering | Discriminated union narrowing | mitigate | TypeScript exhaustiveness check on `switch (source.kind)` in tooltipForSource — strict-mode build fails if a kind is missed when the union changes |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan. RESEARCH.md "Package Legitimacy Audit" confirms zero new packages. Auto-accept. |
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx vitest run src/utils/taxResolution.test.ts` exits 0 with all 11 tests passing
- All 5 VALIDATION-locked test name substrings match: US region default, EU average, manual (unknown region), is stale, is fresh
- TAX_RATES contains exactly 32 rows
- EU_AVERAGE_RATE === 21
- US row note contains the literal D-11 marketplace-facilitator string
- CA row note contains the literal D-locked federal-GST string
</verification>

<success_criteria>
- 3 new files committed: src/data/taxRates.ts, src/utils/taxResolution.ts, src/utils/taxResolution.test.ts
- resolveTaxRate implements the 5-step fallback chain in correct order (override → settings → eu-average → region → manual) — EUR branch BEFORE .find() (Pitfall 4 prevention)
- tooltipForSource returns the exact locked UI-SPEC strings for each source.kind
- isRateStale uses 30.44 days/month and the 18-month threshold
- Wave 0 dependency for downstream plans satisfied — Plans 03 and 05 can import resolveTaxRate / tooltipForSource / TaxRateSource directly
</success_criteria>

<output>
Create `.planning/phases/13-tax-model-ui-sweep/13-01-SUMMARY.md` when done — note any test count differences and confirm all 11 tests pass.
</output>
