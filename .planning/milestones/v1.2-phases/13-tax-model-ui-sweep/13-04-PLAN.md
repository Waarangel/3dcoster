---
phase: 13-tax-model-ui-sweep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AssetLibrary.tsx
  - src/components/JobsManager.tsx
  - src/components/PrinterSettings.tsx
autonomous: true
requirements:
  - UI-08
  - UI-09
  - UI-10
tags:
  - ui-sweep
  - infotooltip
  - newbadge-cleanup
  - compact

must_haves:
  truths:
    - "UI-08 applied: 7 numeric inputs in AssetLibrary, 3 in JobsManager, 6 in PrinterSettings get the `compact` prop added (16 inputs across 3 files)"
    - "D-15: AssetLibrary 'Lifespan units' placeholder `'For reusable items'` is migrated to an InfoTooltip on the label; the placeholder becomes empty"
    - "D-15 + D-16: 3 descriptive <p className='text-xs text-slate-500 mt-1'> blocks in PrinterSettings (Purchase Price description :156, Recovery Period description :171, Monthly Print Hours description :181) are migrated into InfoTooltip on the respective labels; the <p> blocks are deleted"
    - "UI-10 partial: 2 NewBadge JSX sites removed from AssetLibrary — `<NewBadge feature=\"csv-import\" ... />` at line 763 and `{cat === 'packaging' && <NewBadge feature=\"packaging-materials\" />}` at line 845 (the entire `{cat === 'packaging' && ...}` conditional is deleted, not just the inner JSX)"
    - "After NewBadge JSX removals in AssetLibrary, the `import { NewBadge }` line is also removed (grep returns zero remaining usages)"
    - "InfoTooltip is imported in all 3 files (currently imported in none of them)"
    - "D-14: text inputs (Asset Name, Custom Category, Brand, Unit, Notes, Tags, Search, Nickname, Customer Name) are NOT given `compact` — only numeric inputs"
    - "tsc -b passes; all 3 files still compile"
  artifacts:
    - path: "src/components/AssetLibrary.tsx"
      provides: "7 compact numeric inputs; Lifespan Units InfoTooltip; 2 NewBadge sites removed; NewBadge import removed"
      contains: "InfoTooltip"
    - path: "src/components/JobsManager.tsx"
      provides: "3 compact numeric inputs in the Record Sale form"
      contains: "compact"
    - path: "src/components/PrinterSettings.tsx"
      provides: "6 compact numeric inputs; 3 InfoTooltip migrations"
      contains: "InfoTooltip"
  key_links:
    - from: "src/components/AssetLibrary.tsx (Lifespan Units InfoTooltip)"
      to: "the migrated description text 'For items that get reused (e.g., a brush)'"
      via: "InfoTooltip text prop"
      pattern: "For items that get reused"
---

<objective>
Apply UI-08 (compact prop on numeric inputs) and UI-09 (placeholder/description migrations to InfoTooltip on label) to three components — AssetLibrary, JobsManager, and PrinterSettings — and remove the two stale NewBadge JSX sites in AssetLibrary (UI-10 partial).

Purpose: These three components are owned exclusively by this plan (no overlap with the tax UI plans), so the sweep work happens in parallel with the SettingsModal sweep (Plan 03). 16 numeric inputs gain `compact`; 4 descriptive sites migrate to InfoTooltip-on-label; 2 stale NewBadge sites in AssetLibrary are removed and the NewBadge import is dropped (file-level cleanup per RESEARCH Pitfall 6).
Output: 3 modified files. No new files. Zero functional behavior change beyond styling/decoration — sweep is purely visual + cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-tax-model-ui-sweep/13-CONTEXT.md
@.planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md
@.planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md
@.planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md
@src/components/AssetLibrary.tsx
@src/components/JobsManager.tsx
@src/components/PrinterSettings.tsx
@src/components/ui/Input.tsx
@src/components/ui/InfoTooltip.tsx
@src/components/NewBadge.tsx

<interfaces>
<!-- Existing shared primitives. All three files consume them. -->

From src/components/ui/Input.tsx:11, 23: `compact?: boolean` prop caps width at `max-w-28` (112px). Drop-in additive prop — no other markup changes required to swap.

From src/components/ui/InfoTooltip.tsx: `<InfoTooltip text={string} className={string?} />`. Import path: `./ui/InfoTooltip` from src/components/*.tsx.

Canonical label pattern (CostCalculator.tsx:861-863):
`<label className="flex items-center gap-1.5 text-xs text-slate-400 mb-1"><span>Label</span><InfoTooltip text="..." /></label>`

Replaces the legacy: `<label className="block text-xs text-slate-400 mb-1">Label</label>` + `<p className="text-xs text-slate-500 mt-1 or mt-2">Description</p>` pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sweep AssetLibrary — 7 compact inputs, 1 InfoTooltip migration, 2 NewBadge removals + import cleanup</name>
  <files>src/components/AssetLibrary.tsx</files>
  <read_first>
    - src/components/AssetLibrary.tsx (full file — see line 1-10 for imports, lines 763 + 845 for NewBadge JSX, lines 952-1042 for the 7 numeric inputs, lines 1035-1042 for the Lifespan Units placeholder)
    - src/components/ui/InfoTooltip.tsx (verify export + path)
    - src/components/NewBadge.tsx (verify the import path being removed — `from './NewBadge'`)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 459-479 (UI Sweep Inventory for AssetLibrary — 7 numeric inputs to add compact + 1 descriptive placeholder migration)
    - .planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md lines 200, 204 (UI-10 cleanup — remove the entire `{cat === 'packaging' && <NewBadge .../>}` conditional, then drop the NewBadge import once zero usages remain)
    - .planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md lines 413-425 (AssetLibrary analog assignment — Lifespan Units InfoTooltip migration text and the "remove entire conditional expression" instruction for line 845)
  </read_first>
  <action>
    Step 1 — Imports. Add `import { InfoTooltip } from './ui/InfoTooltip';` to the import block at top of AssetLibrary.tsx. After Step 4 removes the NewBadge import, the import block ends up with InfoTooltip added and NewBadge removed.

    Step 2 — Add `compact` to the 7 numeric Inputs (each currently lacks `compact`):
    1. Purchase Price ($) (printer) — line ~952-959
    2. Wattage (printer) — line ~963-969
    3. Expected Lifespan hours (printer) — line ~973-978
    4. Nozzle Cost ($) (printer) — line ~982-988
    5. Nozzle Lifespan cm³ (printer) — line ~992-997
    6. Package Cost ($) (material) — line ~1015-1022
    7. Units per Package (material) — line ~1026-1032
    Plus the 8th numeric input — Lifespan Units (optional) at line ~1036-1042 — also gets `compact` as part of Step 3.

    Step 3 — Lifespan Units placeholder migration. Locate the Lifespan Units Input around line 1036-1042 which currently has `placeholder="For reusable items"`. Two changes:
    - On the Input: replace `placeholder="For reusable items"` with `placeholder=""` (empty), and add `compact`.
    - On the preceding `<label className="block text-xs text-slate-400 mb-1">Lifespan units (optional)</label>` (or similar — read the file for exact label text): rewrite to `<label className="flex items-center gap-1.5 text-xs text-slate-400 mb-1"><span>[exact existing label text]</span><InfoTooltip text="For items that get reused (e.g., a brush)" /></label>`.

    Step 4 — NewBadge JSX removals (UI-10 partial):
    - Line ~763: delete the entire `<NewBadge feature="csv-import" ... />` element (whatever props it has, the whole self-closing tag).
    - Line ~845: delete the ENTIRE `{cat === 'packaging' && <NewBadge feature="packaging-materials" />}` JSX expression — the curly-brace expression block itself, NOT just the inner `<NewBadge>`. Otherwise a dangling `{cat === 'packaging' && false}` (or similar) remains as inert syntax. Per UI-SPEC line 201 and PATTERNS.md line 423.

    Step 5 — NewBadge import cleanup. After Step 4, run a sanity check (manual or grep) to confirm zero `<NewBadge` JSX references remain in AssetLibrary.tsx. Then remove the `import { NewBadge } from './NewBadge';` line entirely (RESEARCH Pitfall 6 + UI-SPEC line 204).

    DO NOT modify text inputs (Asset Name, Custom Category, Brand, Unit, Notes, Tag input, Search box) — D-14 restricts the sweep to numeric.
    DO NOT touch existing descriptive `<p>` blocks unrelated to the Lifespan Units migration (AssetLibrary has no other descriptive-placeholder migrations per RESEARCH inventory).
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/13-04-task1.tsc.log; ! grep -q "error TS" /tmp/13-04-task1.tsc.log</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -n "NewBadge" src/components/AssetLibrary.tsx` returns no matches (zero JSX usages AND zero imports)
    - `! grep -F "feature=\"csv-import\"" src/components/AssetLibrary.tsx` (csv-import badge removed)
    - `! grep -F "feature=\"packaging-materials\"" src/components/AssetLibrary.tsx` (packaging-materials badge removed)
    - `! grep -F "cat === 'packaging' && <NewBadge" src/components/AssetLibrary.tsx` (the entire conditional is gone)
    - `grep "import.*InfoTooltip.*from" src/components/AssetLibrary.tsx` matches (InfoTooltip imported)
    - `grep -F "For items that get reused" src/components/AssetLibrary.tsx` matches (Lifespan Units InfoTooltip text)
    - `! grep -F "placeholder=\"For reusable items\"" src/components/AssetLibrary.tsx` (placeholder migrated away)
    - `grep -c "compact" src/components/AssetLibrary.tsx` returns at least 8 — 7 newly-swept inputs plus the Lifespan Units input (NOTE: count may be lower if `compact` is conditionally absent in code paths; the executor verifies that EACH of the 8 named inputs has the prop)
    - Spot-check each numeric input: every Input at the 8 specified line ranges (952, 963, 973, 982, 992, 1015, 1026, 1036) contains the literal string `compact` as one of its props
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>
    AssetLibrary.tsx has zero NewBadge JSX or import references, an InfoTooltip import, 8 `compact` numeric inputs, and the Lifespan Units description migrated to an InfoTooltip with "For items that get reused (e.g., a brush)". File compiles.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sweep JobsManager — add `compact` to 3 Record Sale form numeric inputs</name>
  <files>src/components/JobsManager.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx (lines 540-595 — the 3 numeric inputs in the Record Sale form: Sale Quantity, Price per Unit, Shipping Cost)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 481-490 (UI Sweep Inventory for JobsManager — exactly 3 numeric inputs to add `compact`; zero descriptive placeholders to migrate)
    - .planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md lines 428-455 (JobsManager analog assignment — Sale Quantity / Price per Unit / Shipping Cost get `compact`; no other changes; placeholders may remain empty or be "0"/"0.00")
  </read_first>
  <action>
    Add the `compact` prop to exactly 3 numeric Inputs in the Record Sale form:
    1. Sale Quantity Input — line ~541-546
    2. Sale Price per Unit ($) Input — line ~550-555
    3. Sale Shipping Cost ($) Input — line ~589-594

    Keep all other Input props unchanged. Keep the existing parseInt/parseFloat coercion in onChange handlers intact.

    Customer Name input at line ~561-567 is a TEXT input — DO NOT add `compact` (D-14).

    DO NOT modify the JobsManager filter UI, the job list, the row delete buttons, or any other section. DO NOT add InfoTooltip imports — there are zero descriptive `<p>` blocks or sentence-length placeholders in this file (RESEARCH UI Sweep Inventory line 490: "Zero descriptive placeholders requiring InfoTooltip migration").
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/13-04-task2.tsc.log; ! grep -q "error TS" /tmp/13-04-task2.tsc.log</automated>
  </verify>
  <acceptance_criteria>
    - The 3 named numeric inputs each contain `compact`: verify via inspection that lines around 541-546 (Sale Quantity), 550-555 (Price per Unit), 589-594 (Shipping Cost) each include the literal `compact` prop on their `<Input>`
    - `grep -c "compact" src/components/JobsManager.tsx` returns exactly 3 (file had 0 compact inputs pre-change; Task 2 adds 3)
    - `! grep -F "placeholder=\"Facebook Marketplace buyer\"" src/components/JobsManager.tsx | grep -F "compact"` — the Customer Name text input does NOT have `compact` (D-14 — text inputs stay wide). If the planner cannot scope this single-line check, executor verifies by reading the Customer Name `<Input>` and confirming `compact` is absent.
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>
    3 numeric Inputs in the JobsManager Record Sale form have `compact`. Zero other changes. File compiles.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sweep PrinterSettings — 6 compact inputs + 3 InfoTooltip migrations</name>
  <files>src/components/PrinterSettings.tsx</files>
  <read_first>
    - src/components/PrinterSettings.tsx (full file — see lines 135-155 for Starting Hours / Purchase Price, line 156 for "What you paid" description, line 171 for "Target to break even" description, lines 175-181 for Monthly Print Hours / "Expected usage" description, lines 268-312 for the Edit form mirrors)
    - src/components/CostCalculator.tsx:861-863 (canonical InfoTooltip-on-label pattern)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 492-508 (UI Sweep Inventory for PrinterSettings — 6 numeric inputs to add `compact`, 3 descriptive `<p>` blocks to migrate)
    - .planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md lines 459-479 (PrinterSettings analog assignment — exact migrated tooltip strings: "What you paid (may differ from MSRP)", "Target months to recover your investment via printed-job sales", "Expected hours/month — used to estimate cost recovery")
  </read_first>
  <action>
    Step 1 — Imports. Add `import { InfoTooltip } from './ui/InfoTooltip';` to the import block at top of PrinterSettings.tsx (file currently does not import it).

    Step 2 — Add `compact` to 6 numeric Inputs:
    1. Starting Hours (Add form) — line ~136
    2. Purchase Price ($) (Add form) — line ~150
    3. Monthly Print Hours (Add form) — line ~175
    4. Print Hours (Edit form) — line ~276
    5. Purchase Price (Edit form) — line ~284
    6. Monthly Print Hours (Edit form) — line ~308

    Step 3 — Migrate 3 descriptive `<p>` blocks to InfoTooltip on the corresponding label. For each, rewrite the existing `<label className="block text-xs text-slate-400 mb-1">[Label]</label>` to `<label className="flex items-center gap-1.5 text-xs text-slate-400 mb-1"><span>[Label]</span><InfoTooltip text="[locked tooltip text]" /></label>`, then DELETE the corresponding `<p className="text-xs text-slate-500 mt-1">...</p>` underneath the Input.
    - Purchase Price (Add form): description at line ~156 reads "What you paid". Locked tooltip text per PATTERNS.md: `"What you paid (may differ from MSRP)"`.
    - Recovery Period (Add form): description at line ~171 reads "Target to break even". Locked tooltip text: `"Target months to recover your investment via printed-job sales"`.
    - Monthly Print Hours (Add form): description at line ~181 reads "Expected usage". Locked tooltip text: `"Expected hours/month — used to estimate cost recovery"`.

    The Edit form mirrors may or may not have descriptive `<p>` blocks — RESEARCH UI Sweep Inventory does NOT list any in the Edit form. If they exist (executor verifies by reading the file), migrate them with the same locked tooltip text; if absent, leave the Edit form labels untouched (only `compact` is added there).

    DO NOT modify text inputs (Nickname in Add and Edit forms — D-14).
    DO NOT remove or change any onChange / value bindings.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/13-04-task3.tsc.log; ! grep -q "error TS" /tmp/13-04-task3.tsc.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import.*InfoTooltip.*from" src/components/PrinterSettings.tsx` matches
    - `grep -F "What you paid (may differ from MSRP)" src/components/PrinterSettings.tsx` matches (Purchase Price migrated tooltip)
    - `grep -F "Target months to recover your investment via printed-job sales" src/components/PrinterSettings.tsx` matches (Recovery Period migrated tooltip)
    - `grep -F "Expected hours/month — used to estimate cost recovery" src/components/PrinterSettings.tsx` matches (Monthly Print Hours migrated tooltip)
    - `! grep -F ">What you paid</p>" src/components/PrinterSettings.tsx` — the original `<p>What you paid</p>` is gone
    - `! grep -F ">Target to break even</p>" src/components/PrinterSettings.tsx` — gone
    - `! grep -F ">Expected usage</p>" src/components/PrinterSettings.tsx` — gone
    - `grep -c "compact" src/components/PrinterSettings.tsx` returns at least 6 (file had 0 compact inputs pre-change; this task adds 6 minimum)
    - All 6 numeric Inputs at the named line ranges (136, 150, 175, 276, 284, 308) contain `compact` as a prop
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>
    PrinterSettings.tsx has 6 `compact` numeric inputs, 3 InfoTooltip migrations on the Add form labels with the exact locked tooltip strings, and the 3 descriptive `<p>` blocks deleted. File compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Numeric inputs across 3 components | All inputs preserve existing onChange handlers; only the `compact` prop is added (visual width cap). No input validation behavior changes. |
| InfoTooltip body text | Hard-coded literal strings copied from existing `<p>` descriptions. No user-controllable input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Tampering | Existing numeric input clamps/parsers | mitigate | The sweep is additive: only `compact` is added; existing parseInt/parseFloat/onChange handlers are preserved verbatim. No clamps removed. |
| T-13-12 | Information Disclosure | Stale NewBadge JSX leaking dead feature keys | mitigate | UI-10 removes 2 stale JSX sites in AssetLibrary; the NewBadge component returns null for unregistered keys so prior leak surface is also closed. |
| T-13-13 | Tampering | InfoTooltip text injection | mitigate | All migrated tooltip texts are string literals copied from existing `<p>` blocks. React JSX text escaping prevents HTML injection. No dangerouslySetInnerHTML usage. |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan. RESEARCH.md "Package Legitimacy Audit" confirms zero new packages. Auto-accept. |
</threat_model>

<verification>
- `npx tsc -b` exits 0
- 8 newly-compact Inputs in AssetLibrary (7 sweep + 1 Lifespan Units which was already in this plan's scope)
- 3 newly-compact Inputs in JobsManager
- 6 newly-compact Inputs in PrinterSettings
- AssetLibrary has zero NewBadge references (JSX + import)
- 3 InfoTooltip migrations in PrinterSettings carry the exact locked strings
- 1 InfoTooltip migration in AssetLibrary carries "For items that get reused (e.g., a brush)"
- All 3 files compile under `tsc -b`
</verification>

<success_criteria>
- 3 modified files committed
- UI-08 coverage advances by 17 inputs (8 AssetLibrary + 3 JobsManager + 6 PrinterSettings; Plan 03 added 3 more in SettingsModal; Plan 05 will sweep CostCalculator)
- UI-09 coverage advances by 4 sites (3 PrinterSettings + 1 AssetLibrary; Plan 03 added 3 more in SettingsModal; Plan 05 may add 0-1 more in CostCalculator)
- UI-10 advances by 2 of the 11 JSX removals; AssetLibrary's NewBadge import is removed (file is fully cleaned)
</success_criteria>

<output>
Create `.planning/phases/13-tax-model-ui-sweep/13-04-SUMMARY.md` when done. Note any line numbers that shifted from the spec and confirm the InfoTooltip migrations carry the exact locked strings.
</output>
