---
phase: 14-customer-details-etsy-helper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/CollapsibleSection.tsx
  - src/components/ui/CollapsibleSection.test.ts
  - src/components/ui/index.ts
  - src/types.ts
  - src/features.ts
autonomous: true
requirements: [CUST-01, ETSY-01]
requirements_addressed: [CUST-01, ETSY-01]
tags: [ui-primitive, types, features-registry, react, tailwind]
must_haves:
  truths:
    - "A new <CollapsibleSection> primitive exists in src/components/ui/ that renders a card with a clickable header, internal useState open/closed, and a body that mounts only when open (D-01)"
    - "Each <CollapsibleSection> instance owns its own state — there is no shared accordion controller, so opening one does NOT close another. The two Phase 14 sections (Customer + Etsy) are independent collapsibles (D-02)"
    - "The primitive is collapsed by default unless caller passes defaultOpen={true} (D-03)"
    - "The primitive accepts a `badge` slot prop and renders it inside a `relative` host outside the <button>'s flex flow so callers can pass <NewBadge> with absolute positioning (D-04, MEMORY.md)"
    - "src/types.ts PrintJob interface gains one optional non-indexed field `etsyChecks?: Record<string, boolean>` with a schema-extension comment that mirrors Phase 12 D-07's quoteNumber precedent (D-18)"
    - "src/features.ts gains exactly two new entries `customer-details` and `etsy-helper`, both dated to the actual ship date (current date at execute-phase run time), not the discussion date 2026-05-21 (D-20, Pitfall 5)"
  artifacts:
    - path: "src/components/ui/CollapsibleSection.tsx"
      provides: "CollapsibleSection primitive with title/defaultOpen/badge/right/subtitle/children props"
      exports: ["CollapsibleSection"]
      min_lines: 40
    - path: "src/components/ui/CollapsibleSection.test.ts"
      provides: "Three render-static tests covering collapsed-default, defaultOpen=true body visibility, and badge slot rendering"
      contains: "renderToStaticMarkup"
    - path: "src/components/ui/index.ts"
      provides: "Barrel export adds CollapsibleSection"
      contains: "export { CollapsibleSection } from './CollapsibleSection'"
    - path: "src/types.ts"
      provides: "PrintJob.etsyChecks?: Record<string, boolean> with schema-extension note"
      contains: "etsyChecks?: Record<string, boolean>"
    - path: "src/features.ts"
      provides: "Two new featureReleases entries: customer-details, etsy-helper, dated to ship date"
      contains: "'customer-details': new Date"
  key_links:
    - from: "src/components/ui/index.ts"
      to: "src/components/ui/CollapsibleSection.tsx"
      via: "barrel re-export"
      pattern: "export \\{ CollapsibleSection \\} from './CollapsibleSection'"
    - from: "src/components/ui/CollapsibleSection.test.ts"
      to: "src/components/ui/CollapsibleSection.tsx"
      via: "vitest import (sibling)"
      pattern: "from './CollapsibleSection'"
    - from: "src/features.ts"
      to: "src/components/NewBadge.tsx"
      via: "featureReleases registry consumed by NewBadge gate logic"
      pattern: "featureReleases\\["
---

<objective>
Lay the foundation for Phase 14 in one wave: ship a reusable `<CollapsibleSection>` UI primitive + its vitest unit test, expose it via the `src/components/ui/index.ts` barrel, extend `PrintJob` with the new optional `etsyChecks?: Record<string, boolean>` field (with the mandatory D-18 schema-extension note), and append the two new `featureReleases` entries (`customer-details`, `etsy-helper`) that Plans 14-02 and 14-03 will consume.

Purpose: Plans 14-02 and 14-03 cannot start until the primitive, the type extension, and the badge registry entries all exist. This is a pure additive plan — no consumer JSX, no existing-file behavior changes — so it can land independently and be reviewed in isolation. Without this plan, the Customer card, the Etsy card, and both NewBadge consumers in 14-02 / 14-03 have nothing to import.

Output:
- `src/components/ui/CollapsibleSection.tsx` (new) — primitive with internal useState, chevron rotation, absolute-positioned badge slot
- `src/components/ui/CollapsibleSection.test.ts` (new) — three vitest tests via `renderToStaticMarkup`
- `src/components/ui/index.ts` (modified) — one new export line
- `src/types.ts` (modified) — one new optional field on `PrintJob` with schema-extension comment
- `src/features.ts` (modified) — two new lines in the `featureReleases` map

## D-18 Schema-Extension Note (REQUIRED in PLAN.md per CONTEXT.md)

`PrintJob.etsyChecks?: Record<string, boolean>` is added in this plan as an optional, non-indexed field on the existing v6 schema. **This extends `PrintJob` beyond the explicit field list enumerated in Phase 12 SCHEMA-01.** The pattern mirrors Phase 12 D-07's `quoteNumber?: number` addition: a per-job feature flag persisted by object-spread into the existing Dexie put-path, with no migration, no schema-string change, and no Dexie version bump (v6 stays v6). Existing v6 records have `etsyChecks === undefined` which renders as "no boxes ticked" — same behavior as Phase 12 D-02 locked for every new optional field except `tags` (the only one that gets a backfill).

**Hard rule:** the Dexie schema string in `src/db/database.ts:76` (`db.version(6).stores({ ... })`) must remain literally identical after this plan. Any version bump or schema-string edit is a scope violation per the additional_context Out-of-scope guardrail #9.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-customer-details-etsy-helper/14-CONTEXT.md
@.planning/phases/14-customer-details-etsy-helper/14-RESEARCH.md
@.planning/phases/14-customer-details-etsy-helper/14-PATTERNS.md

# Pattern analogs that the executor MUST mirror
@src/components/ui/EmptyState.tsx
@src/components/ui/EmptyState.test.ts
@src/components/ui/index.ts
@src/components/SettingsModal.tsx
@src/components/NewBadge.tsx
@src/features.ts
@src/types.ts

<interfaces>
<!-- Contracts downstream plans depend on. Plans 14-02 and 14-03 will import from these. -->

From src/components/ui/CollapsibleSection.tsx (NEW — this plan creates):
```typescript
export interface CollapsibleSectionProps {
  title: string;
  defaultOpen?: boolean;        // default false (D-03 locks collapsed-by-default)
  badge?: ReactNode;            // slot for <NewBadge feature="..." className="absolute -top-1 -right-1" />
  right?: ReactNode;            // optional right-of-chevron slot (future PDF preview button, etc.)
  subtitle?: ReactNode;
  children: ReactNode;
}

export function CollapsibleSection(props: CollapsibleSectionProps): JSX.Element;
```

From src/types.ts (MODIFIED — this plan adds the one field):
```typescript
// EXISTING — already landed Phase 12:
export interface JobCustomer { name?: string; email?: string; address?: string; company?: string; }
export interface PrintJob {
  // ... 30+ existing fields ...
  customer?: JobCustomer;       // Phase 12
  tags?: string[];              // Phase 12
  taxRate?: number;             // Phase 13
  taxAmount?: number;           // Phase 13
  quoteNumber?: number;         // Phase 12 (D-07 schema-extension precedent)
  // NEW — this plan adds:
  etsyChecks?: Record<string, boolean>;   // Phase 14 D-18
}
```

From src/features.ts (MODIFIED — this plan appends 2 lines):
```typescript
export const featureReleases: Record<string, Date> = {
  // Existing 4 entries (Phase 13 audit complete — D-19):
  'settings-reorg': new Date('2026-05-20'),
  'default-profit-margin': new Date('2026-05-18'),
  'model-url': new Date('2026-05-20'),
  'default-tax-rate': new Date('2026-05-21'),
  // NEW — this plan appends (DATES MUST BE THE ACTUAL SHIP DATE AT EXECUTE TIME):
  'customer-details': new Date('YYYY-MM-DD'),
  'etsy-helper': new Date('YYYY-MM-DD'),
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CollapsibleSection primitive + vitest unit test + barrel export</name>
  <files>
    src/components/ui/CollapsibleSection.tsx
    src/components/ui/CollapsibleSection.test.ts
    src/components/ui/index.ts
  </files>
  <read_first>
    - src/components/ui/EmptyState.tsx (analog for the primitive shape, props interface idiom, default-prop destructuring)
    - src/components/ui/EmptyState.test.ts (analog for the .test.ts file extension, renderToStaticMarkup pattern, React.createElement usage — NO JSX in test files)
    - src/components/ui/index.ts (current barrel; append one line, do not reorder)
    - src/components/SettingsModal.tsx (lines 260-295 — established `relative inline-block` host + absolute NewBadge slot pattern, plus the corner-overlay pattern used at src/App.tsx:187)
    - .planning/phases/14-customer-details-etsy-helper/14-PATTERNS.md (CollapsibleSection section, lines ~25-95 — has exact JSX skeleton and gotchas)
    - .planning/phases/14-customer-details-etsy-helper/14-RESEARCH.md (Architecture Patterns → Pattern 1 — full reference JSX with aria-expanded/aria-controls + useId wiring; Common Pitfalls Pitfall 3 for badge placement)
    - vitest.config.ts (confirm `include` matches `src/**/*.test.ts` only — `.test.tsx` is silently skipped; landmine #1 in PATTERNS.md)
  </read_first>
  <behavior>
    - Test 1: `renderToStaticMarkup(React.createElement(CollapsibleSection, { title: 'Customer' }, React.createElement('div', null, 'body content')))` produces HTML that CONTAINS 'Customer', does NOT contain 'body content', and matches `/aria-expanded="false"/`. (Collapsed by default per D-03.)
    - Test 2: Same with `defaultOpen: true` — HTML CONTAINS 'visible body' (the children) and matches `/aria-expanded="true"/`. (defaultOpen prop honored.)
    - Test 3: Pass `badge: React.createElement('span', { 'data-testid': 'b' }, 'NEW')` and confirm HTML CONTAINS `data-testid="b"`. (Badge slot renders inside the relative host so callers can position absolutely.)
    - Tests use ONLY `react-dom/server` `renderToStaticMarkup` — no `@testing-library/react`, no simulated clicks (renderToStaticMarkup ignores onClick). Toggle behavior is exercised via the `defaultOpen` prop, not via click simulation (RESEARCH.md Code Examples note + PATTERNS.md Gotchas).
  </behavior>
  <action>
    Step A — Create `src/components/ui/CollapsibleSection.tsx` (new file, ~50 lines):
      1. Import block (top of file): `import { useState, useId, type ReactNode } from 'react';`. No other imports — the primitive owns its open/closed state and renders a raw `<button>` (allowed because file lives in `src/components/ui/` which is excluded from `scripts/lint-no-raw-html.mjs` per PATTERNS.md Gotchas).
      2. Export the `CollapsibleSectionProps` interface verbatim from the `<interfaces>` block above (`title: string`, `defaultOpen?: boolean`, `badge?: ReactNode`, `right?: ReactNode`, `subtitle?: ReactNode`, `children: ReactNode`). Per CONTEXT D-01 + Claude's Discretion notes — `right?` slot is included for future Phase 16 reuse; `subtitle?` is for future flexibility.
      3. Export the `CollapsibleSection` function (named export, NOT default — matches EmptyState convention). Destructure props with `defaultOpen = false` default. Inside: `const [open, setOpen] = useState(defaultOpen); const bodyId = useId();`.
      4. Root element: `<div className="bg-slate-800 rounded-xl p-6 border border-slate-700">` — this is the verbatim card chrome locked by D-05 (matches existing Pricing / Cost Breakdown cards at CostCalculator.tsx:1199).
      5. Inside the root, wrap the header in a `<div className="relative">` so the `badge` slot can render with absolute positioning OUTSIDE the `<button>`'s flex flow. The `relative` host + sibling-badge layout is the MEMORY.md anti-pattern fix (badge inside a flex-1 child pushes siblings).
      6. Header is a single `<button type="button" onClick={() => setOpen(o => !o)} aria-expanded={open} aria-controls={bodyId} className="w-full flex items-center justify-between text-left focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 rounded min-h-[44px]">`. The `min-h-[44px]` is the mobile tap-target compliance baseline (matches App.tsx:180 settings-button precedent — PATTERNS.md Gotchas).
      7. Inside the button: a left `<div className="flex items-center gap-2">` holding `<span className="text-lg font-semibold text-white">{title}</span>` and (when present) `<span className="text-sm text-slate-400">{subtitle}</span>`. A right `<div className="flex items-center gap-3">` holding `{right}` (optional slot) and the chevron SVG.
      8. Chevron SVG: 24x24 viewBox, w-5/h-5/text-slate-400, with `className={\`w-5 h-5 text-slate-400 transition-transform ${open ? 'rotate-180' : ''}\`}` — Tailwind-idiomatic rotation transition (per RESEARCH.md Pattern 1 + Claude's Discretion). Single `<path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />`. Include `aria-hidden="true"` so screen readers announce only the button's aria-expanded state.
      9. AFTER the `<button>` (inside the same `relative` `<div>` but as a sibling, not a child of the button): render `{badge}` directly. The caller passes `<NewBadge ... className="absolute -top-1 -right-1" />` so the badge floats above the card's top-right corner without affecting the flex flow.
      10. After the `relative` header div, render the body: `{open && (<div id={bodyId} className="mt-4">{children}</div>)}`. Mount-on-open (not `hidden` toggling) — matches EmptyState.tsx idiom of conditionally rendering subtrees rather than CSS-toggling them.

    Step B — Create `src/components/ui/CollapsibleSection.test.ts` (new file — extension is `.test.ts`, NOT `.test.tsx`; landmine #1 in PATTERNS.md). Mirror EmptyState.test.ts structure exactly:
      1. Imports: `import { describe, it, expect } from 'vitest'; import * as React from 'react'; import { renderToStaticMarkup } from 'react-dom/server'; import { CollapsibleSection } from './CollapsibleSection';`
      2. `describe('CollapsibleSection', () => { ... })` block with three `it(...)` cases as specified in <behavior>.
      3. Use `React.createElement(CollapsibleSection, { title: 'Customer' }, React.createElement('div', null, 'body content'))` to construct trees — no JSX (file is `.ts`, not `.tsx`).
      4. Use `renderToStaticMarkup(tree)` to produce the HTML string. Assert via `expect(html).toContain(...)`, `expect(html).not.toContain(...)`, or `expect(html).toMatch(/.../)`. No `@testing-library/react`.

    Step C — Modify `src/components/ui/index.ts` (append one line):
      After the existing `export { InfoTooltip } from './InfoTooltip';` line (line 8), add: `export { CollapsibleSection } from './CollapsibleSection';`. Do not reorder existing exports; the established convention here is append-on-top-of-existing-list (PATTERNS.md Gotchas note that the barrel is intentionally non-alphabetical).
  </action>
  <verify>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && npx vitest run src/components/ui/CollapsibleSection.test.ts --reporter=basic 2>&1 | tail -20</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -c "CollapsibleSection" src/components/ui/index.ts</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -c "aria-expanded" src/components/ui/CollapsibleSection.tsx</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && tsc -b 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/CollapsibleSection.tsx` exists and exports a named function `CollapsibleSection` (NOT default export)
    - `grep "export { CollapsibleSection } from './CollapsibleSection'" src/components/ui/index.ts` returns exactly 1 line
    - `grep -c "aria-expanded" src/components/ui/CollapsibleSection.tsx` returns at least 1 (the attribute is on the `<button>`)
    - The file lives in `src/components/ui/` (so `scripts/lint-no-raw-html.mjs` exempts the raw `<button>`)
    - Test file path is `src/components/ui/CollapsibleSection.test.ts` (NOT `.test.tsx` — vitest.config.ts include glob `src/**/*.test.ts` would silently skip a `.tsx`)
    - `npx vitest run src/components/ui/CollapsibleSection.test.ts` exits 0 with 3 passing tests
    - Test imports use `React.createElement` (no JSX in `.test.ts` files); `renderToStaticMarkup` from `react-dom/server`
    - `tsc -b` exits 0
    - Card chrome class on the root div is exactly `bg-slate-800 rounded-xl p-6 border border-slate-700` (D-05 verbatim — matches Pricing / Cost Breakdown card chrome at CostCalculator.tsx:1199)
    - `min-h-[44px]` is present on the header `<button>` for mobile tap-target compliance
    - The `badge` prop slot renders as a SIBLING of the `<button>` inside a `relative` parent div (so callers can pass `<NewBadge className="absolute -top-1 -right-1" />` without breaking the button's flex flow — MEMORY.md rule)
  </acceptance_criteria>
  <done>
    Three vitest tests pass; barrel export adds one line for CollapsibleSection; TypeScript compiles. Plans 14-02 and 14-03 can now import `CollapsibleSection` from `./ui`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend PrintJob with etsyChecks + append two features.ts entries (with ship-date instruction)</name>
  <files>
    src/types.ts
    src/features.ts
  </files>
  <read_first>
    - src/types.ts (full file — to locate the PrintJob interface block and the existing `quoteNumber?: number` line that serves as the D-07 schema-extension precedent)
    - src/features.ts (full file — 4 existing entries to mirror)
    - .planning/phases/12-schema-foundation/12-CONTEXT.md (D-02 "every other new field stays undefined", D-07 schema-extension precedent for quoteNumber)
    - .planning/phases/14-customer-details-etsy-helper/14-CONTEXT.md (D-18 schema-extension paragraph requirement, D-19 features.ts current state, D-20 ship-date instruction)
    - .planning/phases/14-customer-details-etsy-helper/14-RESEARCH.md (Common Pitfalls Pitfall 5 — features.ts ship-date warning)
    - .planning/phases/14-customer-details-etsy-helper/14-PATTERNS.md (src/types.ts and src/features.ts sections — exact insertion line numbers and verbatim comment text)
  </read_first>
  <action>
    Step A — Modify `src/types.ts` (one new field on `PrintJob`):
      1. Locate the `PrintJob` interface (currently spans approximately src/types.ts:148-195; the closing `}` is at :195 and `quoteNumber?: number;` is the last existing field at :194).
      2. Immediately after the `quoteNumber?: number;` line (before the closing `}` at :195), insert exactly:
         ```
         
           // Per-job Etsy compliance self-review (Phase 14 — D-18).
           // Schema-extension note: this field is NOT in SCHEMA-01's explicit field list.
           // It mirrors how Phase 12 D-07 flagged quoteNumber — optional, non-indexed,
           // so the Dexie v6 schema string in src/db/database.ts:76 stays unchanged and
           // no migration is needed. Existing v6 records have etsyChecks === undefined
           // which renders as "no boxes ticked".
           etsyChecks?: Record<string, boolean>;
         ```
         (4-space indent inside the interface body to match the surrounding `quoteNumber?: number;` line. Blank line before the comment block matches the existing field-grouping convention in this file.)
      3. Do NOT modify `JobCustomer` (already landed Phase 12 at :140-145). Do NOT add or modify any other field.
      4. Do NOT touch `src/db/database.ts` — the Dexie schema string at line :76 must remain literally identical (Anti-Pattern in RESEARCH.md; out-of-scope guardrail #9 in additional_context).

    Step B — Modify `src/features.ts` (append two entries to the `featureReleases` map):
      1. Locate `export const featureReleases: Record<string, Date> = { ... }` (currently lines 5-11; the existing 4 entries are settings-reorg, default-profit-margin, model-url, default-tax-rate per D-19).
      2. After the `'default-tax-rate': new Date('2026-05-21'),` line (currently :9) and before the trailing `// Add new features here with their release date` comment (currently :10), insert exactly two new lines:
         ```
           'customer-details': new Date('SHIP_DATE'),
           'etsy-helper': new Date('SHIP_DATE'),
         ```
         where `SHIP_DATE` MUST be substituted at execute time with the CURRENT DATE (as of `/gsd:execute-phase` run), formatted `YYYY-MM-DD`. Example: if executed on 2026-05-23, the strings become `new Date('2026-05-23')`. **Do NOT hard-code 2026-05-21** (Pitfall 5 — that is the discussion date; if execute-phase runs 14+ days later the NewBadge never appears at all due to Gate 2 in NewBadge.tsx:48-49).
      3. Concrete substitution instruction for the executor: run `date +%Y-%m-%d` in the shell and use the returned string for BOTH dates. Both badges get the same date (they ship together in this phase).
      4. Do NOT modify any existing entry. The 4 existing entries (D-19) must remain unchanged: settings-reorg (2026-05-20), default-profit-margin (2026-05-18), model-url (2026-05-20), default-tax-rate (2026-05-21).
      5. The trailing `// Add new features here with their release date` comment remains as the last line in the object literal.
  </action>
  <verify>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -c "etsyChecks?: Record<string, boolean>" src/types.ts</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -c "^  '" src/features.ts</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -c "customer-details" src/features.ts</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -c "etsy-helper" src/features.ts</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -E "version:|version\(" src/db/database.ts | head -10</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && tsc -b 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "etsyChecks?: Record<string, boolean>" src/types.ts` returns exactly 1 match (inside the `PrintJob` interface, after `quoteNumber?: number;`)
    - A comment block immediately above the new field cites "Phase 14 — D-18" and the schema-extension note mirroring the quoteNumber precedent (verbatim — no paraphrase)
    - `grep -cE "^  '[a-z-]+': new Date" src/features.ts` returns exactly 6 (4 existing Phase 13 entries + 2 new Phase 14 entries)
    - `grep "customer-details" src/features.ts` returns exactly 1 line; same for `etsy-helper`
    - Both new dates are NOT `'2026-05-21'` (the discussion date) unless execute-phase literally runs on 2026-05-21 — they MUST be set to `date +%Y-%m-%d` at execute time. If executed on 2026-05-21 the date may coincidentally equal it; in that case add a SUMMARY note documenting that coincidence
    - `grep "db.version(7)" src/db/database.ts` returns ZERO matches (Dexie stays on v6 — out-of-scope guardrail #9)
    - `grep -E "db.version\\([0-9]+\\)" src/db/database.ts` shows the highest version number unchanged at 6
    - `tsc -b` exits 0 (TypeScript accepts the new optional field)
  </acceptance_criteria>
  <done>
    `PrintJob.etsyChecks` is available for Plan 14-02 to spread into the save path. `featureReleases.customer-details` and `featureReleases.etsy-helper` are available for Plan 14-02 and 14-03 to pass to `<NewBadge feature="..." />` consumers. Dexie schema string unchanged (v6 stays v6, no migration).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-keyboard → CostCalculator inputs | (no new surface this plan — primitive accepts ReactNode children with no string interpolation; types extension and features map are pure config) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `<CollapsibleSection>` body content | accept | Primitive renders `children: ReactNode` via React's auto-escaping JSX path — no `dangerouslySetInnerHTML`, no raw HTML construction. Standard React XSS protection applies. |
| T-14-02 | Information Disclosure | `PrintJob.etsyChecks` field | accept | Field is a `Record<string, boolean>` of self-review flags persisted to local IndexedDB only. No PII, no network transmission introduced by this plan. Phase 16 PDF generator must explicitly exclude this field per ETSY-02 / ROADMAP success criterion #4 — that constraint is enforced in Phase 16, not here. |
| T-14-SC | Tampering | npm package installs | n/a | This plan installs zero new packages. The Package Legitimacy Gate in RESEARCH.md confirms: "Not applicable — phase installs no new packages." |

No new auth, network, or PII surface introduced by this plan. Customer data is local-only IndexedDB persistence following Phase 12 conventions.
</threat_model>

<verification>
After both tasks complete:
- `npx vitest run src/components/ui/CollapsibleSection.test.ts` — 3 tests pass
- `tsc -b` — no TypeScript errors
- `grep -cE "^  '[a-z-]+': new Date" src/features.ts` — returns 6
- `grep "db.version(6)" src/db/database.ts` — present (v6 unchanged)
- `grep "db.version(7)" src/db/database.ts` — ZERO matches (no version bump)
- `node scripts/lint-no-raw-html.mjs` — passes (the `<button>` and any other interactive elements in CollapsibleSection.tsx are inside `src/components/ui/` which is excluded from the lint guard)
</verification>

<success_criteria>
- [ ] `CollapsibleSection` exports from `src/components/ui/index.ts` and is importable as `import { CollapsibleSection } from './ui'`
- [ ] Three vitest tests pass: default-collapsed, defaultOpen-shows-body, badge-slot-renders
- [ ] `PrintJob.etsyChecks?: Record<string, boolean>` is on `src/types.ts` with the D-18 schema-extension comment
- [ ] `src/features.ts` has exactly 6 entries: settings-reorg, default-profit-margin, model-url, default-tax-rate (existing 4), plus customer-details and etsy-helper (new 2, dated to actual ship date)
- [ ] Dexie schema string in `src/db/database.ts` is byte-identical to pre-plan state — v6 stays v6
- [ ] `tsc -b` passes
- [ ] All changes committed to git as a single coherent plan-14-01 commit
</success_criteria>

<output>
Create `.planning/phases/14-customer-details-etsy-helper/14-01-SUMMARY.md` when done, documenting:
- Exact final dates used in `src/features.ts` (so 14-04's UAT grep can confirm)
- The schema-extension note text inserted in `src/types.ts` (so 14-04 can confirm D-18 paragraph presence)
- Confirmation that `src/db/database.ts` was not modified (Dexie v6 unchanged)
- Test pass count from `npx vitest run src/components/ui/CollapsibleSection.test.ts`
</output>
