---
phase: 14-customer-details-etsy-helper
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/data/etsyToS.ts
  - src/data/etsyToS.test.ts
  - src/components/CostCalculator.tsx
autonomous: true
requirements: [ETSY-01, ETSY-02]
requirements_addressed: [ETSY-01, ETSY-02]
tags: [etsy, static-data, cost-calculator, persistence, react, tailwind]
must_haves:
  truths:
    - "src/data/etsyToS.ts exports a 5-item `etsyChecklist` const with the 5 LOCKED ids in this order: original-design, no-third-party-templates, ip-copyright, production-partner-disclosure, ai-disclosure (D-16)"
    - "src/data/etsyToS.ts exports `policySummaryAsOf` (set to the actual ship date by execute-phase, NOT 2026-05-21) and `policyLink` ('https://www.etsy.com/legal/creativity/') (D-16, Pitfall 5)"
    - "A new <CollapsibleSection title=\"Selling on Etsy?\"> card renders in CostCalculator.tsx between the Cost Breakdown card (ends :1492) and the Save Job button (starts :1494) — the SECOND of two new cards in that slot (after the Customer card, which Plan 14-03 adds) (D-05, RESEARCH Pitfall 1 — corrected line numbers)"
    - "The Etsy card is rendered UNCONDITIONALLY on every saved job — not gated by `marketplace === 'etsy'` or any other state. The collapsed-by-default rule (D-03) is the only thing that keeps it out of the way for non-Etsy sellers (D-15, ROADMAP #3)"
    - "The Etsy card body contains, in this order: (a) a yellow-tinted disclaimer notice with the verbatim text \"Etsy's policies change — this is a reminder, not legal advice.\" (D-17, ROADMAP #4), (b) the 5-item checklist with raw <input type=\"checkbox\"> rows each preceded by an `// allow-raw-html` JSX comment (D-16, RESEARCH Pitfall 2), (c) a date+link line rendering policySummaryAsOf and policyLink together (D-17)"
    - "Per-item Etsy check state persists per-job on `PrintJob.etsyChecks` (D-18) via the 6 CostCalculator wiring sites: useState init (~:141), sessionStorage formState object (:154), sessionStorage dep array (:179), editingJob hydration (:187-227), clearForm reset (:494-519), AND both handleSaveJob branches (update :552-573 and create :582-603) (RESEARCH Pitfall 6)"
    - "A `<NewBadge feature=\"etsy-helper\" className=\"absolute -top-1 -right-1\" />` is passed to the CollapsibleSection's `badge` slot prop — NOT inline as a flex child (MEMORY.md rule, D-04)"
  artifacts:
    - path: "src/data/etsyToS.ts"
      provides: "Static Etsy ToS checklist data with 5 locked ids, policy date, policy link"
      exports: ["EtsyChecklistItem", "etsyChecklist", "policySummaryAsOf", "policyLink"]
      contains: "etsyChecklist"
    - path: "src/data/etsyToS.test.ts"
      provides: "vitest shape/id-stability test asserting the 5 locked ids and the policyLink shape"
      contains: "toHaveLength(5)"
    - path: "src/components/CostCalculator.tsx"
      provides: "Etsy section JSX between :1492 and :1494; etsyChecks state wired into 6 sites; imports from ./ui + ../data/etsyToS + ./NewBadge"
      contains: "etsyChecks"
  key_links:
    - from: "src/components/CostCalculator.tsx"
      to: "src/data/etsyToS.ts"
      via: "named import"
      pattern: "from '../data/etsyToS'"
    - from: "src/components/CostCalculator.tsx"
      to: "src/components/ui/CollapsibleSection.tsx"
      via: "barrel import from './ui'"
      pattern: "CollapsibleSection"
    - from: "src/components/CostCalculator.tsx"
      to: "src/components/NewBadge.tsx"
      via: "named JSX consumer"
      pattern: "<NewBadge feature=\"etsy-helper\""
    - from: "src/components/CostCalculator.tsx (handleSaveJob)"
      to: "src/types.ts (PrintJob.etsyChecks)"
      via: "object-spread into PrintJob literal"
      pattern: "etsyChecks:"
---

<objective>
Land the Etsy compliance helper end-to-end: (1) author the static `src/data/etsyToS.ts` data file with the 5 LOCKED checklist ids per D-16, (2) author a vitest shape test that locks the 5 ids against accidental renames, (3) add a `<CollapsibleSection title="Selling on Etsy?">` card to `src/components/CostCalculator.tsx` between line :1492 (end of Cost Breakdown card) and line :1494 (Save Job button comment), and (4) wire `etsyChecks` state into all 6 CostCalculator state-touching sites so per-item check state persists per-job.

Purpose: Satisfy requirements ETSY-01 (collapsible checklist sourced from `etsyToS.ts`, checkable per-item, persisted per-job) and ETSY-02 (`policySummaryAsOf` date + live link to `https://www.etsy.com/legal/creativity/`, disclaimer present, checklist content NOT on the customer PDF — Phase 16 enforces the PDF exclusion).

Output:
- `src/data/etsyToS.ts` (new)
- `src/data/etsyToS.test.ts` (new)
- `src/components/CostCalculator.tsx` (modified — imports, state, persistence sites, JSX)

Plan 14-02 ships in Wave 2 immediately after Plan 14-01 (which lands the `CollapsibleSection` primitive). Plan 14-03 ships in Wave 3 after 14-02, NOT in parallel — both 14-02 and 14-03 modify `src/components/CostCalculator.tsx`, so the file-ownership rule serializes them. 14-02 lands the Etsy card first; 14-03 then inserts the Customer card directly above the Etsy card using the `{/* Etsy compliance helper — Phase 14 ETSY-01, ETSY-02 */}` comment as its landmark.

## Out-of-scope guardrails (do not touch)

- PDF rendering of `etsyChecks` (Phase 16, ETSY-02 explicitly forbids checklist on PDF)
- Conditional Etsy section based on `marketplace === 'etsy'` (rejected D-15)
- Etsy API integration / live ToS sync (paid-tier, rejected)
- Dexie schema-string bump (etsyChecks is non-indexed; v6 stays v6 — out-of-scope guardrail #9)
- Email regex / inline validation (rejected D-09; this is the Etsy card, not Customer — no email here anyway)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-customer-details-etsy-helper/14-CONTEXT.md
@.planning/phases/14-customer-details-etsy-helper/14-RESEARCH.md
@.planning/phases/14-customer-details-etsy-helper/14-PATTERNS.md
@.planning/phases/14-customer-details-etsy-helper/14-01-SUMMARY.md

# Pattern analogs
@src/data/taxRates.ts
@src/components/ui/EmptyState.test.ts
@src/components/BambuImport.tsx
@src/components/CostCalculator.tsx
@scripts/lint-no-raw-html.mjs

<interfaces>
<!-- Contracts this plan creates/consumes. -->

From src/data/etsyToS.ts (NEW — this plan creates):
```typescript
export interface EtsyChecklistItem {
  id: string;
  title: string;
  body: string;
  link?: string;
}

export const policySummaryAsOf: string;   // Set to ship date at execute time, NOT 2026-05-21
export const policyLink: string;           // 'https://www.etsy.com/legal/creativity/'
export const etsyChecklist: readonly EtsyChecklistItem[];   // length 5, locked ids per D-16
```

From src/components/ui (already shipped by 14-01):
```typescript
export interface CollapsibleSectionProps { title; defaultOpen?; badge?; right?; subtitle?; children; }
export function CollapsibleSection(props): JSX.Element;
```

From src/types.ts (already extended by 14-01):
```typescript
export interface PrintJob {
  // ... existing fields ...
  etsyChecks?: Record<string, boolean>;   // Phase 14 D-18
}
```

From src/features.ts (already extended by 14-01):
```typescript
featureReleases['etsy-helper']: Date;   // dated to actual ship date
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author src/data/etsyToS.ts + vitest shape test</name>
  <files>
    src/data/etsyToS.ts
    src/data/etsyToS.test.ts
  </files>
  <read_first>
    - src/data/taxRates.ts (lines 1-23 — analog for the comment header, the interface export, and the `readonly` const-array export pattern)
    - .planning/phases/14-customer-details-etsy-helper/14-CONTEXT.md (D-16 — locked 5 ids and item order, suggested body wording bias)
    - .planning/phases/14-customer-details-etsy-helper/14-PATTERNS.md (src/data/etsyToS.ts section, ~lines 139-209 — has the exact shape to copy)
    - .planning/phases/14-customer-details-etsy-helper/14-RESEARCH.md (Architecture Patterns → Pattern 5 — the static-data file template; Common Pitfalls Pitfall 5 — policySummaryAsOf ship-date rule; Pitfall 7 — neutral-wording bias for the no-third-party-templates item)
    - src/components/ui/EmptyState.test.ts (vitest test-file shape — `.test.ts` extension, describe/it/expect imports)
  </read_first>
  <behavior>
    - Test 1: `etsyChecklist.length === 5`
    - Test 2: `etsyChecklist.map(i => i.id)` equals `['original-design', 'no-third-party-templates', 'ip-copyright', 'production-partner-disclosure', 'ai-disclosure']` IN THIS ORDER (D-16 locked)
    - Test 3: `policyLink` matches the regex `/etsy\.com\/legal\/creativity/`
    - Test 4: `policySummaryAsOf` matches the regex `/^\d{4}-\d{2}-\d{2}$/` (ISO YYYY-MM-DD string)
  </behavior>
  <action>
    Step A — Create `src/data/etsyToS.ts` (new file):
      1. Header comment block (mirror taxRates.ts:1-5 idiom): briefly state the file's purpose ("Static Etsy ToS self-review checklist used by the Phase 14 Etsy compliance helper section in CostCalculator. No I/O, no network — see ROADMAP success criterion #3 / ETSY-02. Date constant is updated each release; the disclaimer + link mitigate stale-rule risk per PITFALLS.md m-01."). No `import type` lines needed — this file has no project-type dependencies (taxRates.ts imports Currency; etsyToS.ts has no such dep).
      2. Export `EtsyChecklistItem` interface verbatim from the `<interfaces>` block: `{ id: string; title: string; body: string; link?: string; }`.
      3. Export `policySummaryAsOf` as a `const` string literal. The literal value MUST be the CURRENT DATE at execute-phase run time (run `date +%Y-%m-%d` and use the result). **Do NOT hard-code `'2026-05-21'`** — that is the discussion date; if execute runs more than 14 days later, NewBadge gate 2 (NewBadge.tsx:48-49) blocks the badge entirely (Pitfall 5). The same date used in src/features.ts must be reused here so the registry and the user-visible date stay synchronized. The line shape: `export const policySummaryAsOf = 'YYYY-MM-DD';` where YYYY-MM-DD is the substituted ship date.
      4. Export `policyLink` as a `const` string literal: `export const policyLink = 'https://www.etsy.com/legal/creativity/';` — verbatim per D-16 (this URL is the source of truth for the ROADMAP #4 + ETSY-02 contract).
      5. Export `etsyChecklist` as a `readonly EtsyChecklistItem[]` const, exactly 5 entries IN THIS ORDER (the `id` values are LOCKED — D-16; saved `etsyChecks` state on existing jobs uses these strings as keys):
         - `id: 'original-design'` — title around "Original design", body 1-2 sentences biased toward "verify at the link below" / "see Etsy's policy" rather than asserting hard procedural rules (Pitfall 7 bias — neutral wording survives future policy shifts).
         - `id: 'no-third-party-templates'` — title around "No third-party templates", body 1-2 sentences with the SAME neutral-bias wording. This is the exact rule Etsy reversed in June 2025 (Assumption A3 in RESEARCH.md) — the disclaimer + date + link defend against future drift, so the body must defer to the link rather than asserting a permanent rule.
         - `id: 'ip-copyright'` — title around "IP and copyright", body 1-2 sentences.
         - `id: 'production-partner-disclosure'` — title around "Production-partner disclosure", body 1-2 sentences.
         - `id: 'ai-disclosure'` — title around "AI disclosure", body 1-2 sentences.
         - `link?` field: omit on most items, OR set to `policyLink` on items with a directly-relevant sub-page (planner discretion per D-16; if uncertain, omit and rely on the section-level link rendered below the checklist).

    Step B — Create `src/data/etsyToS.test.ts` (new file — extension `.test.ts`, NOT `.test.tsx`; same vitest include glob landmine):
      1. Imports: `import { describe, it, expect } from 'vitest'; import { etsyChecklist, policySummaryAsOf, policyLink } from './etsyToS';`
      2. `describe('etsyToS', () => { ... })` block with four `it(...)` cases matching the <behavior> spec:
         - `it('exports exactly 5 checklist items', () => { expect(etsyChecklist).toHaveLength(5); });`
         - `it('locks the 5 stable id values (saved etsyChecks state depends on these)', () => { expect(etsyChecklist.map(i => i.id)).toEqual([...locked array...]); });`
         - `it('exports policy link pointing at etsy.com/legal/creativity', () => { expect(policyLink).toMatch(/etsy\.com\/legal\/creativity/); });`
         - `it('exports a policySummaryAsOf date string in YYYY-MM-DD form', () => { expect(policySummaryAsOf).toMatch(/^\d{4}-\d{2}-\d{2}$/); });`
  </action>
  <verify>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && npx vitest run src/data/etsyToS.test.ts --reporter=basic 2>&1 | tail -20</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -cE "^\\s*\\{\\s*id:" src/data/etsyToS.ts</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -E "policySummaryAsOf = " src/data/etsyToS.ts</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -c "etsy.com/legal/creativity/" src/data/etsyToS.ts</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && tsc -b 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/etsyToS.ts` exists with exactly 5 entries in the `etsyChecklist` const array (verifiable via `grep -cE "^\\s*\\{\\s*id:" src/data/etsyToS.ts` returning 5)
    - The 5 ids appear IN THIS EXACT ORDER: original-design, no-third-party-templates, ip-copyright, production-partner-disclosure, ai-disclosure (D-16 locked)
    - `policyLink` equals exactly `'https://www.etsy.com/legal/creativity/'`
    - `policySummaryAsOf` is NOT `'2026-05-21'` unless execute-phase literally runs on 2026-05-21 — it MUST be set to `date +%Y-%m-%d` at execute time, matching the value used in src/features.ts (Plan 14-01)
    - `src/data/etsyToS.test.ts` exists (NOT `.test.tsx`)
    - `npx vitest run src/data/etsyToS.test.ts` exits 0 with 4 passing tests
    - `tsc -b` exits 0
    - No imports from `dexie`, `axios`, `node-fetch`, or any I/O library (static data only per PATTERNS.md Pattern 5)
  </acceptance_criteria>
  <done>
    `etsyChecklist`, `policySummaryAsOf`, `policyLink` are importable from `../data/etsyToS`. Shape test locks the 5 ids against future renames. Task 2 in this plan can wire these into CostCalculator.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Etsy CollapsibleSection card into CostCalculator (JSX + 6-site state wiring)</name>
  <files>
    src/components/CostCalculator.tsx
  </files>
  <read_first>
    - src/components/CostCalculator.tsx (lines 1-50 for imports; 125-150 for the existing state hooks pattern; 150-184 for the sessionStorage persist effect; 187-227 for the editingJob hydration effect; 494-519 for clearForm; 528-609 for handleSaveJob both branches; 1485-1525 for the insertion site between Cost Breakdown end (:1492) and Save Job button (:1494))
    - src/components/BambuImport.tsx (lines 204-216 — the canonical `// allow-raw-html` JSX comment + raw `<input type="checkbox">` precedent; this is the established convention in 4 prior sites, NOT a workaround)
    - scripts/lint-no-raw-html.mjs (full file — confirms the lint guard pattern: previous line must include `allow-raw-html` for any raw `<button>/<input>/<select>/<textarea>` outside `src/components/ui/`)
    - .planning/phases/14-customer-details-etsy-helper/14-PATTERNS.md (CostCalculator section, ~lines 313-545 — has all 7 site insertions with surrounding code excerpts and exact insertion line numbers)
    - .planning/phases/14-customer-details-etsy-helper/14-RESEARCH.md (Common Pitfalls Pitfall 1 — CORRECTED line numbers between :1492 and :1494; Pitfall 2 — allow-raw-html placement; Pitfall 3 — NewBadge slot placement; Pitfall 6 — all 6 state sites must be touched)
    - src/data/etsyToS.ts (the file Task 1 just created — to import etsyChecklist, policySummaryAsOf, policyLink)
    - src/components/ui/CollapsibleSection.tsx (the primitive Plan 14-01 shipped — to confirm props shape)
  </read_first>
  <action>
    Step A — Update imports at the top of `src/components/CostCalculator.tsx`:
      1. Add `CollapsibleSection` to the existing barrel import from `./ui` at line :6. Current line: `import { Button, Input, Select, InfoTooltip } from './ui';` → add `CollapsibleSection` (and `Textarea` if not yet present, though Textarea is Plan 14-03's concern — confirm presence; if missing add it here since both plans touch imports). For this plan ONLY add `CollapsibleSection` if Plan 14-03 will add `Textarea` separately; otherwise add both in this commit (executor decides based on Plan 14-03's merge state).
      2. Add a new import line near the existing data-file imports (after the existing `from '../utils/...'` lines around :7-9): `import { etsyChecklist, policySummaryAsOf, policyLink } from '../data/etsyToS';`. The `EtsyChecklistItem` type is NOT needed at the call site (etsyChecklist's elements are inferred).
      3. `NewBadge` import at line :5 already exists — no change.

    Step B — Add `etsyChecks` useState hook (Site 1 of 6 — useState init):
      1. Insert AFTER the existing `const [marketplace, setMarketplace] = useState<MarketplaceType>(...)` line at :141. Add:
         ```
         // Etsy self-review check state (Phase 14 — ETSY-01, D-18).
         const [etsyChecks, setEtsyChecks] = useState<Record<string, boolean>>(
           () => editingJob?.etsyChecks ?? getStoredValue('etsyChecks', {} as Record<string, boolean>)
         );
         ```
         This mirrors the `taxRateOverride` initializer at :130-132 verbatim — `editingJob?.field ?? getStoredValue(...)` pattern (PATTERNS.md state-pattern section). Locating it directly after `marketplace` keeps the form-state hooks grouped at the top of the component.

    Step C — Wire `etsyChecks` into the sessionStorage persist effect (Sites 2 + 3 of 6 — formState object literal AND dep array):
      1. In the `formState = { ... }` object literal that runs from :154-177, add `etsyChecks,` AFTER the existing `marketplace,` line at :177 (so the new field is the last key in the literal).
      2. In the dependency array at :179-183, add `etsyChecks` AFTER the existing `marketplace` token on line :183.
      Pitfall 6 warning: missing either site means sessionStorage either drops the user's typed values (object missing) or persists a stale snapshot (dep array missing).

    Step D — Wire `etsyChecks` into the editingJob hydration effect (Site 4 of 6):
      1. In the `if (editingJob) { ... }` block at :187-227, after the existing `setTaxRateOverride(editingJob.taxRate);` line at :201, add:
         ```
         setEtsyChecks(editingJob.etsyChecks ?? {});
         ```
         Position immediately after `setTaxRateOverride` (the closest analog — both are per-job Phase-13+-era optional fields). This site is the one that ensures opening a saved job for edit shows the saved Etsy check state (NOT empty), so the user does not silently overwrite saved checks on re-save (Pitfall 6 — data loss).

    Step E — Wire `etsyChecks` into clearForm() (Site 5 of 6):
      1. In the `clearForm` function at :494-519, after the existing `setMarketplace('none');` line at :516 and BEFORE the `sessionStorage.removeItem(FORM_STORAGE_KEY);` line at :518, add:
         ```
         setEtsyChecks({});
         ```
         CRITICAL (Pitfall 6 — PII/data hygiene): if this site is missed, saved Etsy check state from job A bleeds into a new job B when the user clicks "Cancel Edit" or "Save as new". Even though etsyChecks is not PII per se, the same wiring discipline applies to Plan 14-03's customer field (which IS PII), and this plan's UAT step exercises the same code path.

    Step F — Wire `etsyChecks` into BOTH handleSaveJob branches (Site 6 of 6 — update branch :552-573 AND create branch :582-603):
      1. In the UPDATE branch (`if (editingJob) { ... }` at :552-573), inside the `const updatedJob: PrintJob = { ... };` literal, after the existing `taxAmount: tax.taxAmount,` line at :572 and BEFORE the closing `};` at :573, add:
         ```
         etsyChecks: Object.keys(etsyChecks).length > 0 ? etsyChecks : undefined,
         ```
         The `Object.keys(...).length > 0 ? ... : undefined` guard ensures the field stays absent from IndexedDB when the user hasn't ticked any boxes — keeps existing-record shape clean (D-18 / Phase 12 D-02 alignment: optional fields stay undefined when empty).
      2. In the CREATE branch (the `else { const job: PrintJob = { ... }; }` block at :580-603), inside the `const job: PrintJob = { ... };` literal, after `taxAmount: tax.taxAmount,` at :601 and BEFORE `copiesSold: 0,` at :602, add the SAME line:
         ```
         etsyChecks: Object.keys(etsyChecks).length > 0 ? etsyChecks : undefined,
         ```
      Both branches MUST receive the addition — missing either site means the field never persists in one of the two save paths.

    Step G — Add the Etsy `<CollapsibleSection>` JSX between :1492 and :1494 (insertion site corrected per RESEARCH Pitfall 1):
      1. The literal anchor is the `{/* Save Job Button */}` comment at line :1494. The previous line :1492 is the closing `</div>` of the Cost Breakdown card. The two new cards (Customer in Plan 14-03, Etsy in this plan) land in this gap.
      2. **Note on plan-ordering:** This plan adds the Etsy card; Plan 14-03 adds the Customer card. Per CONTEXT D-05, the rendered order must be **Customer first, Etsy second**. If Plan 14-03 has not yet shipped when this plan executes, the Etsy section lands as a single new block above the Save button. When Plan 14-03 ships, its Customer block lands ABOVE this Etsy block in the same JSX region. The executor should leave a clear visual gap (blank line + `{/* Etsy compliance helper — Phase 14 ETSY-01, ETSY-02 */}` comment) above the Etsy block to make the insertion site for the Customer card obvious.
      3. Insert this JSX directly above `{/* Save Job Button */}` at :1494 (i.e., AFTER the closing `</div>` at :1492):
         ```tsx
         {/* Etsy compliance helper — Phase 14 ETSY-01, ETSY-02 */}
         <CollapsibleSection
           title="Selling on Etsy?"
           badge={<NewBadge feature="etsy-helper" className="absolute -top-1 -right-1" />}
         >
           {/* Disclaimer at top — verbatim wording locked by D-17 / ROADMAP success criterion #4 */}
           <div className="bg-slate-700/50 border border-yellow-500/30 text-yellow-100/90 rounded p-3 text-sm mb-4">
             Etsy's policies change — this is a reminder, not legal advice.
           </div>
       
           {/* 5-item checklist; raw <input> requires `// allow-raw-html` comment per lint guard (PATTERNS Gotcha) */}
           <div className="space-y-1">
             {etsyChecklist.map(item => (
               <label key={item.id} className="flex items-start gap-3 p-2 rounded hover:bg-slate-700/30 cursor-pointer">
                 {/* allow-raw-html: per-item checklist toggle; no Checkbox primitive exists, matches BambuImport.tsx:209 precedent */}
                 <input
                   type="checkbox"
                   className="mt-1 w-4 h-4 accent-blue-500 cursor-pointer"
                   checked={!!etsyChecks?.[item.id]}
                   onChange={e => setEtsyChecks(prev => ({ ...prev, [item.id]: e.target.checked }))}
                 />
                 <div className="flex-1">
                   <div className="text-sm font-medium text-white">{item.title}</div>
                   <div className="text-xs text-slate-400 mt-0.5">{item.body}</div>
                 </div>
               </label>
             ))}
           </div>
       
           {/* Date + live link rendered inline below checklist — D-17 */}
           <div className="mt-4 text-xs text-slate-500">
             Verified against Etsy policy as of {policySummaryAsOf} —{' '}
             <a
               href={policyLink}
               target="_blank"
               rel="noopener noreferrer"
               className="text-blue-400 hover:text-blue-300 underline"
             >
               etsy.com/legal/creativity/
             </a>
           </div>
         </CollapsibleSection>
         ```
      4. CRITICAL — `// allow-raw-html` comment placement (Pitfall 2): Inside the `.map(item => (...))` callback, the JSX comment `{/* allow-raw-html: per-item checklist toggle; no Checkbox primitive exists, matches BambuImport.tsx:209 precedent */}` MUST sit on the IMMEDIATELY preceding line of the `<input>` tag. `scripts/lint-no-raw-html.mjs` greps line-by-line for `<input` patterns and inspects `lines[i - 1]` for the `allow-raw-html` token (verified by reading scripts/lint-no-raw-html.mjs lines 32-37). 5 checklist items render 5 `<input>` tags → 5 inline comments, all generated by the same `.map` body so the comment is duplicated by React's rendering output. This produces 5 lint-compliant pairs.
      5. NewBadge slot placement (Pitfall 3 + MEMORY.md): the badge is passed as the `badge` PROP on `CollapsibleSection`, NOT as inline children inside the title or as a sibling of the title `<span>`. The primitive renders `{badge}` inside its `relative` host outside the `<button>`'s flex flow — so `className="absolute -top-1 -right-1"` floats the badge over the card's top-right corner without affecting the chevron or any other header content.
  </action>
  <verify>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && npm run test -- --run 2>&1 | tail -25</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && node scripts/lint-no-raw-html.mjs 2>&1 | tail -10</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && tsc -b 2>&1 | tail -10</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -c "etsyChecks" src/components/CostCalculator.tsx</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -c "feature=\"etsy-helper\"" src/components/CostCalculator.tsx</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep "Etsy's policies change" src/components/CostCalculator.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "etsyChecks" src/components/CostCalculator.tsx` returns ≥ 8 (1 useState + 1 sessionStorage formState + 1 dep array + 1 editingJob hydration + 1 clearForm + 2 handleSaveJob branches + at least 1 JSX consumer = minimum 8 references)
    - `grep -c "feature=\"etsy-helper\"" src/components/CostCalculator.tsx` returns exactly 1 (single NewBadge consumer at the section header)
    - `grep "Etsy's policies change — this is a reminder, not legal advice" src/components/CostCalculator.tsx` returns exactly 1 line (verbatim disclaimer per D-17 / ROADMAP #4)
    - `grep "policySummaryAsOf" src/components/CostCalculator.tsx` returns ≥ 1 hit (rendered inline below the checklist per D-17)
    - `grep "policyLink" src/components/CostCalculator.tsx` returns ≥ 1 hit (used as the `href` on the inline link per D-17)
    - `grep -c "allow-raw-html" src/components/CostCalculator.tsx` returns ≥ 1 (at least one `allow-raw-html` comment inside the .map callback for the 5 checkbox renders). NOTE: the comment text appears ONCE in source code (inside the .map body) but is duplicated 5 times in the rendered DOM. The lint script reads source lines, so 1 source comment = 5 compliant DOM checkboxes.
    - `node scripts/lint-no-raw-html.mjs` exits 0 (no raw-HTML violations in CostCalculator.tsx — every raw `<input>` is preceded by `allow-raw-html`)
    - `grep "db.version(7)" src/db/database.ts` returns 0 (Dexie stays on v6 — out-of-scope guardrail #9)
    - `npm run test -- --run` exits 0 (full vitest suite green — etsyToS.test.ts passes, CollapsibleSection.test.ts passes, and all existing tests still pass)
    - `tsc -b` exits 0
  </acceptance_criteria>
  <done>
    The Etsy compliance helper renders as the (current) second-and-final card above the Save button in CostCalculator. Per-item check state persists per-job via PrintJob.etsyChecks. The badge floats over the section's top-right corner via the slot prop. All lint and type checks pass. Plan 14-03 will insert its Customer card immediately above this section (before the `{/* Etsy compliance helper ... */}` comment) when it ships.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-keyboard → CostCalculator.etsyChecks state | User toggles checkboxes; state writes to React state, then IndexedDB on Save. No injection vector. |
| IndexedDB → JobsManager render | Read of `job.etsyChecks` (this plan doesn't render it in JobsManager — it lives only in the cost calculator. Phase 16 PDF MUST exclude per ETSY-02 / ROADMAP #4.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | etsyChecklist titles/bodies rendered into the Etsy section | accept | React JSX auto-escapes the strings — `{item.title}` and `{item.body}` are interpolated as text nodes, not raw HTML. No `dangerouslySetInnerHTML`. |
| T-14-02-02 | Information Disclosure | `etsyChecks` field on PrintJob persisted in IndexedDB | accept | Non-PII boolean self-review flags only. Field is local-IndexedDB-only; Phase 14 introduces zero network paths. Phase 16 PDF generator must exclude this field by design (ETSY-02 enforcement is Phase 16's responsibility — Phase 14 simply does not wire any PDF code). |
| T-14-02-03 | Tampering | Raw `<input type="checkbox">` in CostCalculator | accept | The 5 raw `<input>` elements use the project's established `// allow-raw-html` escape hatch (precedent: BambuImport, CsvImportModal x2, CostCalculator licensing — 4 prior sites). Build pipeline includes `lint-no-raw-html.mjs` as a guard, ensuring no untagged raw HTML form elements ship. |
| T-14-SC | Tampering | npm package installs | n/a | This plan installs zero new packages. The Package Legitimacy Gate in RESEARCH.md confirms: "Not applicable — phase installs no new packages." |

No new auth/PII/network surface introduced by Phase 14 — customer data is local-only IndexedDB data, etsyChecks is local state, no outbound calls.
</threat_model>

<verification>
- `npm run test -- --run` — full vitest suite green (including the new etsyToS.test.ts and 14-01's CollapsibleSection.test.ts)
- `node scripts/lint-no-raw-html.mjs` — passes (all 5 Etsy checkboxes have `allow-raw-html` comments)
- `tsc -b` — passes (etsyChecks type integration accepted)
- `grep -c "etsyChecks" src/components/CostCalculator.tsx` — ≥ 8 (confirms all 6 wiring sites + JSX consumer references)
- `grep -c "feature=\"etsy-helper\"" src/components/CostCalculator.tsx` — exactly 1
- Visual check (manual, but recordable): `npm run dev` on port 4173 → open CostCalculator → confirm "Selling on Etsy?" card renders as a SEPARATE card above the Save button, collapsed by default, with the chevron pointing down. Open it → confirm disclaimer notice (yellow tint), 5 checkboxes (unchecked by default for a new job), and the date+link line at the bottom. (Plan 14-04 owns this UAT formally.)

**File overlap with Plan 14-03:** Both plans touch `src/components/CostCalculator.tsx`. Per file-ownership rules in the dependency_graph step, the later plan inherits the earlier plan's wave + 1, OR plans are executed serially within the same wave with the executor merging carefully. The orchestrator may sequence 14-02 then 14-03 in Wave 2 (serial) rather than parallelizing them. Either ordering works as long as both plans add their card in the correct relative position (Customer above Etsy per D-05). Plan 14-03's frontmatter declares `depends_on: ["14-01", "14-02"]` to make this explicit.
</verification>

<success_criteria>
- [ ] `src/data/etsyToS.ts` exists with 5 LOCKED ids in D-16 order and a ship-date `policySummaryAsOf`
- [ ] `src/data/etsyToS.test.ts` passes (4 tests: length, id-order, link-shape, date-shape)
- [ ] Etsy `<CollapsibleSection>` renders in CostCalculator between :1492 (end of Cost Breakdown) and :1494 (Save Job button)
- [ ] Section is collapsed by default (no `defaultOpen` prop) per D-03 / ROADMAP #3
- [ ] Disclaimer notice with verbatim text "Etsy's policies change — this is a reminder, not legal advice." renders at the top of the section body (D-17, ROADMAP #4)
- [ ] 5 checkbox rows render, each preceded in source by a single `// allow-raw-html` JSX comment inside the .map callback (Pitfall 2 — lint guard satisfied)
- [ ] `policySummaryAsOf` date and `policyLink` URL render together below the checklist per D-17
- [ ] `<NewBadge feature="etsy-helper" className="absolute -top-1 -right-1" />` is passed to the CollapsibleSection's `badge` prop, NOT inline (MEMORY.md / D-04)
- [ ] All 6 CostCalculator wiring sites updated: useState init, sessionStorage formState, sessionStorage dep array, editingJob hydration, clearForm reset, BOTH handleSaveJob branches (update + create) (Pitfall 6)
- [ ] `npm run test -- --run` passes, `node scripts/lint-no-raw-html.mjs` passes, `tsc -b` passes
- [ ] Dexie schema string in `src/db/database.ts:76` is byte-identical to pre-plan state (v6 stays v6 — out-of-scope guardrail #9)
</success_criteria>

<output>
Create `.planning/phases/14-customer-details-etsy-helper/14-02-SUMMARY.md` when done, documenting:
- Exact `policySummaryAsOf` date value substituted (so Plan 14-04 UAT can confirm it matches `src/features.ts` ship date)
- Final wording chosen for the 5 checklist item titles/bodies (so Plan 14-04 UAT can confirm titles render in the dev server)
- Confirmation that all 6 CostCalculator wiring sites received the `etsyChecks` addition (with line numbers found at write time — may differ from the 14-PATTERNS.md numbers if Plan 14-03 shipped first)
- Whether Plan 14-03 had already shipped at the time of this plan's execution (affects relative card positioning above this section)
- `grep "db.version" src/db/database.ts` output (confirming v6 unchanged)
</output>
