---
phase: 14-customer-details-etsy-helper
plan: 04
type: execute
wave: 4
depends_on: ["14-01", "14-02", "14-03"]
files_modified: []
autonomous: false
requirements: [CUST-01, CUST-02, ETSY-01, ETSY-02]
requirements_addressed: [CUST-01, CUST-02, ETSY-01, ETSY-02]
tags: [uat, audit, features-registry, ui-10-verification, integration]
must_haves:
  truths:
    - "ROADMAP Phase 14 success criterion #1: A collapsible 'Customer' section on the cost calculator accepts name/email/address/company; all optional; collapsed by default — VERIFIED in browser at port 4173"
    - "ROADMAP Phase 14 success criterion #2: Customer name + email visible on the saved-job row in JobsManager; full address appears only on the expanded panel + PDF (Phase 16) — VERIFIED in browser"
    - "ROADMAP Phase 14 success criterion #3: A collapsible 'Selling on Etsy?' section renders the 5-item checklist sourced from src/data/etsyToS.ts; each item is checkable; per-item state persists per-job — VERIFIED in browser"
    - "ROADMAP Phase 14 success criterion #4: Etsy section shows policySummaryAsOf date + live link to https://www.etsy.com/legal/creativity/ + verbatim disclaimer 'Etsy's policies change — this is a reminder, not legal advice.'; checklist items do NOT render on customer PDF (Phase 16 owns the PDF; Phase 14 simply does not wire any PDF code for etsyChecks) — VERIFIED via grep + visual"
    - "ROADMAP Phase 14 success criterion #5 (UI-10 carry-over): src/features.ts contains EXACTLY 6 entries (Phase 13's 4 + Phase 14's 2); every id has at least one live JSX consumer; no stale entries — VERIFIED via grep audit"
    - "UI-10 baseline holds (D-19): the 4 pre-Phase-14 features.ts entries are EXACTLY `settings-reorg` (consumer: src/App.tsx:187), `default-profit-margin` (consumer: src/components/SettingsModal.tsx:265), `model-url` (consumer: src/components/CostCalculator.tsx:763), `default-tax-rate` (consumer: src/components/SettingsModal.tsx:291). Any drift from this baseline (missing entry, missing consumer, new pre-Phase-14 entry) FAILS the UI-10 audit even if the Phase 14 entries are correct."
    - "HARD acceptance: Customer field does NOT leak between jobs (PII guard — Pitfall 6 clearForm site). UAT exercises: save Job A with customer set → click Cancel Edit / create new job → verify Customer fields are EMPTY in CostCalculator"
    - "HARD acceptance: Saving an Etsy-checked job → reopening it for edit → confirms the same checkboxes are still ticked (round-trip through IndexedDB via PrintJob.etsyChecks)"
  artifacts:
    - path: ".planning/phases/14-customer-details-etsy-helper/14-04-SUMMARY.md"
      provides: "UAT results record + UI-10 audit grep output + threat-model accept-rationale closure"
      contains: "UAT"
    - path: ".planning/phases/14-customer-details-etsy-helper/14-VERIFICATION.md"
      provides: "Phase 14 verification record (created in this plan) cross-referencing 14-01/02/03 SUMMARY files"
      contains: "verified"
  key_links:
    - from: "UAT in browser (port 4173)"
      to: "src/components/CostCalculator.tsx + src/components/JobsManager.tsx"
      via: "manual visual + interactive verification by user"
      pattern: "checkpoint:human-verify"
    - from: "grep audits in this plan"
      to: "src/features.ts + src/components/*.tsx"
      via: "static analysis"
      pattern: "feature=\""
---

<objective>
Run end-to-end UAT + UI-10 carry-over audit for Phase 14 and produce the verification record.

This plan touches NO source files. It runs `checkpoint:human-verify` UAT against the 7 ROADMAP success criteria + the 2 HARD acceptance criteria (PII non-leak, Etsy persistence round-trip), executes the UI-10 grep audit on `src/features.ts`, confirms the Dexie schema string did not change, and writes the phase VERIFICATION.md / SUMMARY.md records.

Purpose: Phase 14 cannot close without (a) a human eyeballing the two new collapsible cards in the dev server because UI behavior (collapsed-by-default, badge corner positioning, truncation under narrow viewports, exact disclaimer rendering) is layout-sensitive and silent grep alone is insufficient (14-VALIDATION.md Manual-Only Verifications table), (b) confirmation that the UI-10 audit Phase 13 completed still holds, and (c) explicit confirmation that no Dexie version bump or scope-violation crept in across Plans 14-01..14-03.

Output:
- `.planning/phases/14-customer-details-etsy-helper/14-04-SUMMARY.md` (new — UAT pass/fail per criterion, grep audit outputs)
- `.planning/phases/14-customer-details-etsy-helper/14-VERIFICATION.md` (new — phase verification record cross-referencing 14-01/02/03)

NO production source files are modified by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-customer-details-etsy-helper/14-CONTEXT.md
@.planning/phases/14-customer-details-etsy-helper/14-RESEARCH.md
@.planning/phases/14-customer-details-etsy-helper/14-VALIDATION.md
@.planning/phases/14-customer-details-etsy-helper/14-01-SUMMARY.md
@.planning/phases/14-customer-details-etsy-helper/14-02-SUMMARY.md
@.planning/phases/14-customer-details-etsy-helper/14-03-SUMMARY.md

# Source artifacts to spot-check
@src/features.ts
@src/types.ts
@src/components/CostCalculator.tsx
@src/components/JobsManager.tsx
@src/data/etsyToS.ts
@src/db/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static audits — UI-10 features.ts grep gate + Dexie schema integrity + scope guardrails</name>
  <files>
    .planning/phases/14-customer-details-etsy-helper/14-04-AUDIT.md
  </files>
  <read_first>
    - src/features.ts (the current state must show 6 entries after Plans 14-01/02/03)
    - src/db/database.ts (line :76 must still show `db.version(6).stores({ ... })` with the same schema string as before Phase 14)
    - src/types.ts (PrintJob interface must contain `etsyChecks?: Record<string, boolean>` with the D-18 schema-extension comment)
    - .planning/phases/14-customer-details-etsy-helper/14-CONTEXT.md (D-19 + D-20 — exact expected state of features.ts after Phase 14)
    - .planning/phases/14-customer-details-etsy-helper/14-01-SUMMARY.md (to confirm the actual ship-date that 14-01 used for the two new entries)
    - .planning/phases/14-customer-details-etsy-helper/14-02-SUMMARY.md (to confirm policySummaryAsOf matches the features.ts ship date)
  </read_first>
  <action>
    Run all of the following commands and record the literal stdout/exit code in `.planning/phases/14-customer-details-etsy-helper/14-04-AUDIT.md`. The file is the auditable record of the static-analysis gate.

    Audit 1 — features.ts entry count (UI-10 carry-over, ROADMAP success criterion #5):
      Run: `grep -cE "^\\s*'[a-z-]+': new Date\\(" src/features.ts`
      Expected: exactly 6
      Mapping: 4 Phase 13 entries (settings-reorg, default-profit-margin, model-url, default-tax-rate) + 2 Phase 14 entries (customer-details, etsy-helper) = 6.

    Audit 2 — every features.ts id has a live JSX consumer (no orphans, no stale entries):
      For each id in `['settings-reorg', 'default-profit-margin', 'model-url', 'default-tax-rate', 'customer-details', 'etsy-helper']`, run:
        `grep -rn "feature=\"<id>\"" src/`
      Expected: each id returns ≥ 1 hit. Record the line numbers found.
      Concrete expected consumers (from D-19 + Plan 14-02/03 wiring):
        - settings-reorg → src/App.tsx:187
        - default-profit-margin → src/components/SettingsModal.tsx:265
        - model-url → src/components/CostCalculator.tsx:763
        - default-tax-rate → src/components/SettingsModal.tsx:291
        - customer-details → src/components/CostCalculator.tsx (line determined at execute time, near the Customer CollapsibleSection)
        - etsy-helper → src/components/CostCalculator.tsx (line determined at execute time, near the Etsy CollapsibleSection)
      If ANY id has 0 hits → audit FAILS → write the failing id and remediation note to 14-04-AUDIT.md and BLOCK the human-verify checkpoint until fixed.

    Audit 3 — features.ts has no orphan JSX consumers (reverse-direction check):
      Run: `grep -rEoh '<NewBadge[^>]*feature="[^"]+"' src/ | grep -oE 'feature="[^"]+"' | sort -u`
      Expected: the deduplicated list of JSX-consumer ids matches EXACTLY the 6 features.ts ids (no JSX consumer references an id that is not in the registry, and vice versa). If a JSX consumer references e.g. `feature="customer-detalis"` (typo) it will appear here as a 7th id with no registry match — that is a fail.

    Audit 4 — Dexie schema integrity (out-of-scope guardrail #9):
      Run: `grep -nE "db\\.version\\([0-9]+\\)\\.stores" src/db/database.ts`
      Expected: the highest version number remains 6. No `db.version(7)...` line. Record all matches in 14-04-AUDIT.md.
      Run: `grep -A1 "db.version(6).stores" src/db/database.ts | head -5`
      Expected: the schema string inside the v6 stores call MUST be byte-identical to what shipped in Phase 12 (jobs table primary key `id` plus secondary indexes `name, createdAt, printerInstanceId` — `etsyChecks` and `customer` are NOT indexed). Quote the v6 stores block verbatim into 14-04-AUDIT.md.

    Audit 5 — D-18 schema-extension comment present in src/types.ts:
      Run: `grep -B1 "etsyChecks?: Record<string, boolean>" src/types.ts | head -10`
      Expected: at least one preceding line cites "Phase 14 — D-18" or "Schema-extension note" (mirror of the Phase 12 D-07 quoteNumber precedent — 14-01 SUMMARY records the exact text).

    Audit 6 — date synchronization between features.ts and etsyToS.ts:
      Run: `grep "customer-details" src/features.ts && grep "etsy-helper" src/features.ts && grep "policySummaryAsOf" src/data/etsyToS.ts`
      Expected: all three lines show the SAME date string (within YYYY-MM-DD). If they diverge, the executor of 14-01 vs. 14-02 may have run on different days — flag as a soft warning (date drift of 1-2 days is acceptable as long as both are within 14 days of NewBadge gate 2; >2 days warrants a fix-up commit setting all three to the same value).
      Then check the dates are NOT '2026-05-21' (the discussion date) unless execute literally ran on 2026-05-21 today. If they are 2026-05-21 AND today's date is later than 2026-05-21, that is a Pitfall 5 violation — fail the audit and require 14-01 and 14-02 to be re-run with the correct date substitution.

    Audit 7 — lint-no-raw-html guard still passes:
      Run: `node scripts/lint-no-raw-html.mjs`
      Expected: exit 0. Output: `lint:no-raw-html passed`. If non-zero, list the violations in 14-04-AUDIT.md and FAIL the audit.

    Audit 8 — full test suite green:
      Run: `npm run test -- --run`
      Expected: exit 0, all tests pass. Record the test count from the vitest reporter (`Test Files <n> passed (<n>) | Tests <n> passed (<n>)`).

    Audit 9 — TypeScript builds clean:
      Run: `tsc -b`
      Expected: exit 0, no errors. Per user's global CLAUDE.md: 3DCoster uses `tsc -b` (not `--noEmit`).

    Audit 10 — production build succeeds (includes lint + vitest + tsc + vite + bundle-size assert):
      Run: `npm run build`
      Expected: exit 0. This is the full build gate — encompasses Audits 7-9 plus the bundle-size script. If any sub-step fails, that is the failure point — record which one and remediation.

    Write the entire audit record (commands + actual stdout + pass/fail per audit + remediation notes for any failures) to `.planning/phases/14-customer-details-etsy-helper/14-04-AUDIT.md`. The file is a verbatim record so Task 2 (human checkpoint) can review without re-running everything, and Task 3 (verification doc) can cite the audit results.
  </action>
  <verify>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -cE "^\\s*'[a-z-]+': new Date\\(" src/features.ts</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && for id in settings-reorg default-profit-margin model-url default-tax-rate customer-details etsy-helper; do echo "$id: $(grep -r "feature=\"$id\"" src/ | wc -l)"; done</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -nE "db\\.version\\([0-9]+\\)\\.stores" src/db/database.ts</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep "etsyChecks?: Record<string, boolean>" src/types.ts</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && node scripts/lint-no-raw-html.mjs 2>&1 | tail -5</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && npm run test -- --run 2>&1 | tail -5</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && tsc -b 2>&1 | tail -5</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/14-customer-details-etsy-helper/14-04-AUDIT.md` exists with the recorded output of Audits 1-10
    - Audit 1: `grep -cE "^\\s*'[a-z-]+': new Date\\(" src/features.ts` returns exactly 6
    - Audit 2: each of the 6 ids has ≥ 1 JSX consumer match in src/
    - Audit 3: forward-and-reverse mapping of ids ↔ JSX consumers is bijective (no orphan in either direction)
    - Audit 4: `grep -nE "db\\.version\\([0-9]+\\)\\.stores" src/db/database.ts` shows max version 6; no `db.version(7)` exists
    - Audit 5: src/types.ts contains the `etsyChecks?: Record<string, boolean>` line with a preceding D-18 schema-extension comment
    - Audit 6: features.ts entries `customer-details` and `etsy-helper` AND `policySummaryAsOf` in etsyToS.ts all carry dates within a 2-day window AND are NOT '2026-05-21' if today's date is past 2026-05-21
    - Audit 7: `node scripts/lint-no-raw-html.mjs` exits 0
    - Audit 8: `npm run test -- --run` exits 0
    - Audit 9: `tsc -b` exits 0
    - Audit 10: `npm run build` exits 0
    - If any audit fails, the failure is recorded in 14-04-AUDIT.md with a remediation note AND Task 2 (human checkpoint) is BLOCKED until the failure is fixed
  </acceptance_criteria>
  <done>
    All 10 audits pass and are recorded in `.planning/phases/14-customer-details-etsy-helper/14-04-AUDIT.md`. The static-analysis gate is closed. Ready for the human visual UAT (Task 2).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — visual + interactive verification at port 4173</name>
  <what-built>
    Plans 14-01 / 14-02 / 14-03 have shipped:
    - New `<CollapsibleSection>` primitive in `src/components/ui/`
    - New `src/data/etsyToS.ts` with 5 LOCKED checklist ids
    - `PrintJob.etsyChecks?: Record<string, boolean>` added to types.ts with D-18 schema-extension note
    - Two new `src/features.ts` entries (`customer-details`, `etsy-helper`) dated to ship date
    - Customer `<CollapsibleSection>` card on CostCalculator (above the Etsy card, both between :1492 and :1494)
    - Etsy `<CollapsibleSection>` card with disclaimer + 5-item checklist + date+link
    - JobsManager row subline (Name · Email) + expanded-panel Customer block
    - Static audits in Task 1 have passed (record in 14-04-AUDIT.md)
  </what-built>
  <how-to-verify>
    Open the dev server and run through the 7 ROADMAP success criteria + the 2 HARD acceptance criteria, recording pass/fail for each in 14-04-SUMMARY.md.

    Setup:
    1. From the project root, run `npm run dev`. The dev server pins to port 4173 (per project .claude/CLAUDE.md).
    2. Open `http://localhost:4173` in a desktop browser (Chrome/Firefox/Safari — any one is fine for the primary pass).
    3. Open DevTools Console — verify NO React errors or warnings appear on the CostCalculator screen.

    UAT 1 — ROADMAP success criterion #1 (CUST-01, D-03):
      - Navigate to the cost calculator screen.
      - Scroll down to just above the Save Job button.
      - Confirm: a "Customer" card is visible, COLLAPSED by default (only header shown, no form fields rendered yet).
      - The card sits ABOVE a second card titled "Selling on Etsy?" — that order is fixed by D-05 (Customer first, Etsy second).
      - The card chrome is `bg-slate-800 rounded-xl p-6 border border-slate-700` — matches the Pricing card above.
      - Click the Customer card header — the chevron rotates and the body expands to reveal 4 labeled fields in this order: Name, Email, Company, Address.
      - The Name + Email fields share a 2-column row on `≥md` viewports (≥768px). Resize the window narrower — the grid should collapse to a single column.
      - Company is full-width within the section.
      - Address is a Textarea (multi-line) — 3 rows visible by default per D-11.
      - Reload the page (Cmd+R / F5) — the Customer section returns to COLLAPSED state (no localStorage persistence per D-03).
      - Result: PASS / FAIL — record in 14-04-SUMMARY.md.

    UAT 2 — ROADMAP success criterion #2 (CUST-02, D-12, D-14):
      - Fill in the Customer card: Name = "Jane Doe", Email = "jane@example.com", Company = "Acme LLC", Address = "123 Main St\nSpringfield, IL 62701" (use a literal newline in the textarea).
      - Fill in the other required fields (printer, at least one filament, print time > 0) so the job can be saved.
      - Click "Save Job".
      - Switch to the "My Jobs" tab in JobsManager.
      - Locate the just-saved job row.
      - Confirm: directly below the existing "filaments | Xh" subline, a NEW subline reads "Jane Doe · jane@example.com" (middle dot U+00B7 separator, NOT `*`, `|`, or `-`).
      - Confirm: the subline does NOT show the address or company (CUST-02 — only name + email on the collapsed row).
      - Click the row to expand it.
      - Confirm: in the expanded panel, a "Customer" block appears BELOW the 3-column Cost/Profit/Sell-price grid and ABOVE the Model URL block (if a model URL is set; if not, the Customer block sits above the break-even progress bar).
      - The expanded Customer block shows all four fields: Name, Email, Company, Address — with the address rendered on multiple lines (the literal newline you typed is preserved via `whitespace-pre-line`).
      - Save a SECOND job with NO customer fields set — confirm: the new subline is OMITTED (no empty `<div>` reserved); the expanded panel shows the existing 3-column grid with NO Customer block above the Model URL.
      - Result: PASS / FAIL — record in 14-04-SUMMARY.md.

    UAT 3 — ROADMAP success criterion #3 (ETSY-01, D-03, D-16, D-18):
      - Return to the cost calculator. Click "Selling on Etsy?" header to expand.
      - Confirm: 5 checkbox rows render, each with a title + body text. The order from top to bottom matches D-16:
        1. (Original design)
        2. (No third-party templates)
        3. (IP and copyright)
        4. (Production-partner disclosure)
        5. (AI disclosure)
      - Tick 3 of the 5 boxes (any 3). They should toggle visually (checkmark appears in each accent-blue-500 box).
      - Reload the page (without saving) — the form re-opens and the section is collapsed by default again (D-03). Re-expand it. The 3 ticked boxes should STILL be ticked (sessionStorage persistence via the dep-array wiring from 14-02 Task 2).
      - Result: PASS / FAIL — record in 14-04-SUMMARY.md.

    UAT 4 — ROADMAP success criterion #4 (ETSY-02, D-17):
      - With the Etsy section expanded, confirm at the TOP of the body: a yellow-tinted notice with the EXACT VERBATIM text "Etsy's policies change — this is a reminder, not legal advice." — slate-700/50 background, yellow-500/30 border (visible as a faint yellow outline).
      - Below the 5 checkboxes, confirm the date-and-link line reads "Verified against Etsy policy as of YYYY-MM-DD — etsy.com/legal/creativity/" where YYYY-MM-DD is the actual ship date (NOT 2026-05-21 unless execute literally ran today on that date).
      - Click the "etsy.com/legal/creativity/" link. It should open `https://www.etsy.com/legal/creativity/` in a new tab (target="_blank" + rel="noopener noreferrer").
      - Confirm: the Etsy section does NOT render any preview/template of a PDF (Phase 14 does not wire any PDF code — Phase 16 owns the PDF and is explicitly forbidden from including `etsyChecks` per ETSY-02 / ROADMAP #4).
      - Result: PASS / FAIL — record in 14-04-SUMMARY.md.

    UAT 5 — HARD acceptance: Etsy persistence round-trip (ETSY-01, Pitfall 6 site validation):
      - Fill in a job with name "Etsy Persistence Test", select a printer + filament + print time > 0, expand the Etsy section, tick boxes [1, 3, 5] (Original design, IP/copyright, AI disclosure).
      - Click "Save Job".
      - Switch to "My Jobs" tab. Click the row to expand, then click "Edit" (the existing edit affordance on JobsManager row).
      - The CostCalculator opens with the job's data restored. Expand the Etsy section.
      - Confirm: boxes [1, 3, 5] are STILL ticked; boxes [2, 4] are NOT ticked. (Round-trip through IndexedDB via `PrintJob.etsyChecks` field — D-18.)
      - Result: PASS / FAIL — record in 14-04-SUMMARY.md.

    UAT 6 — HARD acceptance: Customer PII non-leak across jobs (Pitfall 6 clearForm site):
      - In the CostCalculator (still in edit mode for the previous job), confirm the Customer card shows "Jane Doe" / "jane@example.com" etc. from UAT 2's saved job.
      - Click "Cancel Edit" (the secondary button next to Save Job).
      - The form clears. Expand the Customer card.
      - Confirm: ALL 4 customer fields are EMPTY. NOT "Jane Doe", NOT "jane@example.com" — empty. Same for Etsy: ALL 5 checkboxes are unticked.
      - This is the Pitfall 6 / data-protection guard — missing the `setCustomer({})` line in clearForm causes PII (name, email, address) to leak from Job A into Job B.
      - Result: PASS / FAIL — record in 14-04-SUMMARY.md. If FAIL: this is a HARD BLOCKER for closing Phase 14 — file an immediate fix-up to add `setCustomer({})` (and `setEtsyChecks({})`) to clearForm in CostCalculator.tsx.

    UAT 7 — ROADMAP success criterion #5 (UI-10 carry-over, D-19, D-20):
      - The static grep audit (Task 1, Audits 1-3) already verified this. Task 2 confirms it interactively: hover over the Customer section header — a green "NEW" badge should be visible in the top-right corner of the card (`absolute -top-1 -right-1`), NOT inline next to the title.
      - Hover over the Etsy section header — same: green "NEW" badge in the top-right corner.
      - Resize the browser to a narrow viewport (≤ 768px). Confirm: the badges remain in their corners; the chevron does NOT disappear off the right edge; the section titles do NOT wrap to two lines (Pitfall 3 visual check).
      - Result: PASS / FAIL — record in 14-04-SUMMARY.md.

    UAT 8 — NewBadge time-window verification (Pitfall 5):
      - In DevTools Console, run: `Object.entries(JSON.parse(localStorage.getItem('3dcoster-feature-first-seen') || '{}'))`
      - Confirm: keys for `customer-details` and `etsy-helper` exist (added the first time the badges rendered). If absent, the badge has not been seen yet — refresh the page to register first-seen.
      - The badges should be visible NOW (first-seen 0 hours ago, within both the 14-day release window and the 36-hour seen window).
      - Optional offline confirmation: in DevTools → Application → Local Storage → `3dcoster-feature-first-seen` shows the timestamp for both keys.
      - Result: PASS / FAIL — record in 14-04-SUMMARY.md.

    After running all 8 UAT scenarios, record the results in `.planning/phases/14-customer-details-etsy-helper/14-04-SUMMARY.md` with:
      - Pass/fail per UAT scenario
      - Browser + OS + viewport used
      - Any deviations from expected behavior
      - Screenshots welcome (optional — paste paths into the SUMMARY)
  </how-to-verify>
  <resume-signal>
    Type "approved" if ALL 8 UAT scenarios pass.
    Type "failed:<scenario-id>:<description>" if any scenario fails (e.g., "failed:UAT-6:customer email Jane Doe still visible after Cancel Edit" — this triggers an immediate fix-up plan for the missing clearForm site).
    Type "skipped:<reason>" only if a scenario cannot be tested in the current environment (e.g., "skipped:UAT-8:headless browser, no localStorage UI" — rare).
  </resume-signal>
  <files>(no source files modified — human-only UAT recording goes into 14-04-SUMMARY.md created by Task 3)</files>
  <action>This is a checkpoint:human-verify task. The human runs the 8 UAT scenarios listed in <how-to-verify> at port 4173 (dev server). No source files are modified by this task. Pass/fail results are recorded into 14-04-SUMMARY.md by Task 3 after the human signals completion via <resume-signal>.</action>
  <verify><human-check>All 8 UAT scenarios pass per the <how-to-verify> protocol. Human records pass/fail per scenario in 14-04-SUMMARY.md (Task 3 writes the file).</human-check></verify>
  <done>Human types one of the resume signals: "approved", "failed:<scenario-id>:<description>", or "skipped:<reason>". Approval unblocks Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Write Phase 14 VERIFICATION.md + 14-04 SUMMARY.md + commit</name>
  <files>
    .planning/phases/14-customer-details-etsy-helper/14-VERIFICATION.md
    .planning/phases/14-customer-details-etsy-helper/14-04-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/14-customer-details-etsy-helper/14-04-AUDIT.md (Task 1 output)
    - .planning/phases/14-customer-details-etsy-helper/14-01-SUMMARY.md
    - .planning/phases/14-customer-details-etsy-helper/14-02-SUMMARY.md
    - .planning/phases/14-customer-details-etsy-helper/14-03-SUMMARY.md
    - .planning/phases/12-schema-foundation/12-VERIFICATION.md (analog for phase-verification-doc shape)
    - .planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md (analog for phase-verification-doc shape)
    - .planning/ROADMAP.md Phase 14 success criteria (the 5 bulleted items the verification doc maps against)
    - .planning/REQUIREMENTS.md CUST-01, CUST-02, ETSY-01, ETSY-02 sections (requirement contracts to mark as Pending → Complete in the traceability table)
  </read_first>
  <action>
    Step A — Create `.planning/phases/14-customer-details-etsy-helper/14-VERIFICATION.md`:
      1. Front-matter block: `phase: 14-customer-details-etsy-helper`, `status: complete`, `verified_on: <today's date>`, `verified_by: human-uat`.
      2. Section "## Requirement Verification": one row per requirement (CUST-01, CUST-02, ETSY-01, ETSY-02) with: requirement text (copied verbatim from REQUIREMENTS.md), verification method (cite the UAT scenario number + the audit number), result (PASS / FAIL), and the line numbers / files where the implementation lives.
      3. Section "## ROADMAP Success Criteria": one row per criterion #1-#5 with the same shape.
      4. Section "## Static Audit Results": a summary of Audits 1-10 from 14-04-AUDIT.md (with a link to the audit file).
      5. Section "## Schema State": confirm Dexie v6 unchanged. Quote the v6 stores call verbatim from src/db/database.ts.
      6. Section "## Threat Model Closure": for each T-14-* threat from the three plan threat models, mark Disposition status as Accepted-with-mitigation-shipped or Accepted-no-mitigation-needed (with the rationale cite).
      7. Section "## Open Items / Deferred": confirm none of the Out-of-scope guardrails (#1-#10 in additional_context) were crossed. List which Deferred Ideas (CONTEXT) remain deferred. Note that Phase 16 will consume `PrintJob.customer` (CUST-02 PDF) and explicitly NOT consume `PrintJob.etsyChecks` (ETSY-02 PDF exclusion).

    Step B — Create `.planning/phases/14-customer-details-etsy-helper/14-04-SUMMARY.md`:
      1. Brief plan-04 summary header (similar to the per-plan SUMMARY files 14-01/02/03).
      2. Section "## UAT Results": one row per UAT scenario (1-8) with pass/fail + observations + browser/viewport.
      3. Section "## Static Audit Summary": link to 14-04-AUDIT.md and list the pass count (10/10 if green).
      4. Section "## Phase 14 Closure Statement": one paragraph stating Phase 14 is closed, all 4 requirements (CUST-01, CUST-02, ETSY-01, ETSY-02) are satisfied, Dexie schema unchanged, UI-10 audit holds.
      5. Section "## Next Phase Hand-off": brief note for Phase 15 / Phase 16:
         - Phase 15 (Tags, Search + Quick Duplicate) — will reuse the Customer subline truncation pattern when implementing free-text search; the DUP-02 customer-undefined-on-duplicate rule has a unit test waiting in DUP-02's requirement contract.
         - Phase 16 (PDF) — will consume `PrintJob.customer` for the customer block on the PDF (CUST-02 "full address visible on PDF only"); MUST explicitly exclude `PrintJob.etsyChecks` from the PDF template (ETSY-02 / ROADMAP success criterion #4).

    Step C — Update `.planning/REQUIREMENTS.md` traceability table:
      1. Open REQUIREMENTS.md, locate the Traceability section.
      2. For CUST-01, CUST-02, ETSY-01, ETSY-02: change Status from "Pending" to "Complete" (verbatim — match the existing "Complete" entries for Phase 12 + Phase 13 requirements).
      3. Save.

    Step D — Update `.planning/ROADMAP.md`:
      1. In the Phases list, change `[ ] **Phase 14: Customer Details + Etsy Helper**` to `[x] **Phase 14: Customer Details + Etsy Helper** — Customer fields on jobs, display in JobsManager, Etsy ToS collapsible checklist, and features.ts dead-badge cleanup (completed YYYY-MM-DD)` where YYYY-MM-DD is today's date.
      2. In the Progress Table, set Phase 14 row to "4/4" plans complete and Status "Complete" with the completion date.

    Step E — Update `.planning/STATE.md`:
      1. Front-matter: bump `completed_phases` from 2 to 3, `completed_plans` from 10 to 14, recompute `percent` (3/5 * 100 = 60).
      2. Current Position section: change to `Phase: 14 (complete)`, `Status: Complete`, `Last activity: <today's date>`.
      3. Accumulated Context: add a Phase 14 entry to the Decisions log noting requirements covered and any notable discoveries.

    Step F — Commit:
      Run: `gsd-sdk query commit "docs(14): close Phase 14 — customer details + etsy helper verified" --files .planning/phases/14-customer-details-etsy-helper/14-04-AUDIT.md .planning/phases/14-customer-details-etsy-helper/14-04-SUMMARY.md .planning/phases/14-customer-details-etsy-helper/14-VERIFICATION.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md`
      (Adjust file list if the gsd-sdk query commit handler differs — fall back to a plain `git add` + `git commit` with the same message if needed.)
  </action>
  <verify>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && ls .planning/phases/14-customer-details-etsy-helper/14-VERIFICATION.md .planning/phases/14-customer-details-etsy-helper/14-04-SUMMARY.md .planning/phases/14-customer-details-etsy-helper/14-04-AUDIT.md</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep -E "CUST-01|CUST-02|ETSY-01|ETSY-02" .planning/REQUIREMENTS.md | grep -i "complete" | wc -l</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && grep "Phase 14" .planning/ROADMAP.md | head -3</automated>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 && git log --oneline -1</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/14-customer-details-etsy-helper/14-VERIFICATION.md` exists with all 7 sections (frontmatter, requirement verification, roadmap criteria, static audit, schema state, threat closure, open items)
    - `.planning/phases/14-customer-details-etsy-helper/14-04-SUMMARY.md` exists with all 5 sections (header, UAT results, static audit summary, closure statement, next-phase hand-off)
    - `.planning/REQUIREMENTS.md` shows CUST-01, CUST-02, ETSY-01, ETSY-02 as "Complete" in the Traceability table (4 entries flipped from "Pending" to "Complete")
    - `.planning/ROADMAP.md` shows Phase 14 with `[x]` checked and a completion date in the phase entry + Progress Table
    - `.planning/STATE.md` shows updated completed_phases=3, completed_plans=14, percent=60
    - A git commit lands containing all 6 planning files modified by this task (14-04-AUDIT, 14-04-SUMMARY, 14-VERIFICATION, REQUIREMENTS, ROADMAP, STATE)
    - The commit message starts with `docs(14):` per project commit convention
    - NO source files in `src/` are modified by this plan
  </acceptance_criteria>
  <done>
    Phase 14 is closed. Verification record + summary + audit are committed. REQUIREMENTS.md / ROADMAP.md / STATE.md reflect the completion. Phase 15 and Phase 16 are unblocked to start.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 14 → Phase 16 | Phase 16 PDF generator MUST consume `PrintJob.customer` (CUST-02 — full address on PDF) and MUST EXCLUDE `PrintJob.etsyChecks` (ETSY-02 / ROADMAP #4). The hand-off is recorded in 14-VERIFICATION.md → Open Items → Next Phase Hand-off (this plan's Task 3 Step B.5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Information Disclosure | Phase 14 → Phase 16 etsyChecks PDF leak | mitigate | Phase 14's VERIFICATION.md → Open Items section explicitly records that Phase 16 MUST NOT include `etsyChecks` in the PDF template. Phase 16 plan-phase will read this verification file as part of its `<read_first>`. |
| T-14-04-02 | Tampering | Production source files in this plan | accept | This plan modifies ZERO files in `src/`. All source-level threats are handled by plans 14-01/02/03. |
| T-14-SC | Tampering | npm package installs | n/a | This plan installs zero new packages. |

No new attack surface introduced by this plan — it is documentation + audit only.
</threat_model>

<verification>
- All 10 static audits in Task 1 pass and are recorded in 14-04-AUDIT.md
- All 8 UAT scenarios in Task 2 pass and are recorded in 14-04-SUMMARY.md by the human checker
- 14-VERIFICATION.md exists and maps all 4 requirements (CUST-01, CUST-02, ETSY-01, ETSY-02) + all 5 ROADMAP criteria to PASS results
- REQUIREMENTS.md traceability table reflects the 4 completions
- ROADMAP.md phase entry has `[x]` and a completion date
- STATE.md progress counters updated
- A single git commit records the planning-doc updates
- `npm run build` exits 0 (full build pipeline including lint, vitest, tsc -b, vite, bundle-size assert)
</verification>

<success_criteria>
- [ ] Phase 14 closed with all 4 requirements (CUST-01, CUST-02, ETSY-01, ETSY-02) verified PASS
- [ ] All 5 ROADMAP success criteria for Phase 14 verified PASS
- [ ] UI-10 carry-over audit confirms 6 entries in src/features.ts, each with ≥ 1 JSX consumer (no orphans)
- [ ] Dexie schema string in src/db/database.ts byte-identical to pre-Phase-14 state (v6 unchanged)
- [ ] PII non-leak verified (UAT 6 — clearForm site)
- [ ] Etsy persistence round-trip verified (UAT 5 — IndexedDB ↔ form)
- [ ] No new source files modified by this plan
- [ ] Planning artifacts (VERIFICATION.md, SUMMARY.md, AUDIT.md) committed to git
- [ ] REQUIREMENTS.md, ROADMAP.md, STATE.md updated to reflect Phase 14 completion
</success_criteria>

<output>
Three files produced by this plan:
- `.planning/phases/14-customer-details-etsy-helper/14-04-AUDIT.md` — Task 1 audit record (commands + outputs + pass/fail)
- `.planning/phases/14-customer-details-etsy-helper/14-04-SUMMARY.md` — Task 3 plan summary with UAT results and closure
- `.planning/phases/14-customer-details-etsy-helper/14-VERIFICATION.md` — Phase 14 verification record (analog of 12-VERIFICATION.md / 13-VERIFICATION.md)

Plus updates to:
- `.planning/REQUIREMENTS.md` (4 entries: Pending → Complete)
- `.planning/ROADMAP.md` (Phase 14: `[ ]` → `[x]`)
- `.planning/STATE.md` (progress counters)
</output>
