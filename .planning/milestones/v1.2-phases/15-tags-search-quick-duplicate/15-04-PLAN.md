---
phase: 15-tags-search-quick-duplicate
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/components/JobsManager.tsx
autonomous: true
requirements: [TAGS-02, TAGS-03, TAGS-04]
requirements_addressed: [TAGS-02, TAGS-03, TAGS-04]
tags: [jobs-manager, search, chip-filter, virtualization, cache-key, debounce]
user_setup: []
must_haves:
  truths:
    - "A sticky sub-header sits above the virtualized job list with: search input on top (full-width), chip filter row below (one chip per tag-in-use with count like 'pla · 3')"
    - "Search filters via case-insensitive substring across job.name, every tag in job.tags, AND every matching Sale's customer.{name,email,company} (joined via useSales() per D-06)"
    - "Search input is debounced at 250ms (per D-06); the debounced value drives the filter AND the cache key"
    - "Chip filter combines with AND across selected chips: a job must carry every selected tag to remain visible"
    - "Search AND chip filter combine with AND: result = (matches search) AND (has all selected chips)"
    - "Chips are sorted alphabetically by tag name; a 'Clear filters' link appears when any filter is active"
    - "The useDynamicRowHeight cache key is `${selectedJobId ?? ''}|${selectedChipsSortedJoined}|${debouncedSearchQuery}` — pipe-delimited (D-05 lock)"
    - "When filters yield zero results, a non-EmptyState block renders 'No jobs match your filter' with a 'Clear filters' button BELOW the still-visible filter UI (D-10)"
    - "At < 640px, the chip filter row scrolls horizontally (overflow-x-auto); search input stays full-width (D-14)"
  artifacts:
    - path: "src/components/JobsManager.tsx"
      provides: "searchQuery + debouncedSearchQuery + selectedChips state; tagCounts memo; jobsAfterChipFilter memo; searchedJobs memo; sticky sub-header JSX with SearchIcon + Input + chip row; cache key extended to pipe-delimited tri-key; filter empty state block; Clear filters button"
      contains: "debouncedSearchQuery"
  key_links:
    - from: "JobsManager.tsx::sticky sub-header"
      to: "JobsManager.tsx::useDynamicRowHeight cache key (line 1232)"
      via: "shared state — selectedJobId + selectedChipsKey + debouncedSearchQuery"
      pattern: "key: `\\$\\{selectedJobId.*\\}\\|\\$\\{.*chips.*\\}\\|\\$\\{.*search.*\\}`"
    - from: "JobsManager.tsx::searchedJobs"
      to: "useSales() (existing hook usage)"
      via: "Map<jobId, Sale[]> built once per render for the customer-fields join"
      pattern: "salesByJobId"
    - from: "JobsManager.tsx::chip filter row"
      to: "JobsManager.tsx::tagCounts memo"
      via: "render-time iteration over [tag, count] tuples"
      pattern: "tagCounts.map"
---

<objective>
Add the sticky filter sub-header (search bar + multi-select chip filter row + clear-filters action + filter-empty-state) to JobsManager.tsx, extend the `useDynamicRowHeight` cache key to the D-05 pipe-delimited tri-key, and wire the 250ms search debounce. This closes ROADMAP success criteria #2 (chip filter, AND logic), #3 (search across title + tags + customer), and #4 (cache-key contract + scroll-to-top behavior from cache invalidation).

Purpose: Make a 500-job JobsManager scannable. The chip filter answers "show me all PLA jobs"; the search answers "show me the Alice phone-stand job"; the cache-key extension prevents the well-known react-window stale-height regression when the visible row set changes.

Output: All filter/search/cache-key changes land in JobsManager.tsx. No new files. No changes to other components. NO `[⋯]` menu, NO inline tag editor, NO JobCard tag chips — those live in Plan 15-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md
@.planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md
@.planning/phases/15-tags-search-quick-duplicate/15-01-SUMMARY.md
@src/components/JobsManager.tsx
@src/components/CustomerLibrary.tsx
@src/components/AssetLibrary.tsx

<interfaces>
<!-- Search input — canonical analog at CustomerLibrary.tsx:115 + 141-150 + 234-253 -->
From src/components/CustomerLibrary.tsx:115 (state):
  const [searchQuery, setSearchQuery] = useState('');

From src/components/CustomerLibrary.tsx:141-150 (memoized filter):
  const searchedCustomers = useMemo(() => {
    const q = searchQuery.toLowerCase().trim();
    if (!q) return sortedCustomers;
    return sortedCustomers.filter(c =>
      (c.name || '').toLowerCase().includes(q) ||
      (c.email || '').toLowerCase().includes(q) ||
      (c.company || '').toLowerCase().includes(q)
    );
  }, [sortedCustomers, searchQuery]);

From src/components/CustomerLibrary.tsx:234-253 (search input UI with icon + clear-X button):
  <div className="relative mb-4">
    <SearchIcon className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400 pointer-events-none" />
    <Input type="text" value={searchQuery} onChange={...} placeholder="..." className="pl-9 pr-8 ..." />
    {searchQuery && (
      <Button variant="ghost" btnSize="sm" onClick={() => setSearchQuery('')} className="absolute right-2 top-1/2 -translate-y-1/2 ...">×</Button>
    )}
  </div>

<!-- Chip render — canonical analog at AssetLibrary.tsx:192-198 -->
From src/components/AssetLibrary.tsx:192-198 (chip styling — D-11 LOCKED):
  <span className="text-xs px-1.5 py-0.5 rounded bg-slate-600/50 text-slate-400">{tag}</span>
  // Wrapper: <div className="flex flex-wrap gap-1 mb-2">

<!-- Existing cache-key site — D-05 target -->
From src/components/JobsManager.tsx:1227-1235:
  const rowHeightCache = useDynamicRowHeight({
    defaultRowHeight: 88,
    key: selectedJobId ?? '',
  });

<!-- D-05 LOCKED replacement: `${selectedJobId ?? ''}|${selectedChipsSortedJoined}|${debouncedSearchQuery}` -->

<!-- useSales hook is already imported and used elsewhere in JobsManager — confirm by grepping for `useSales(` before adding -->

<!-- D-06 search scope INCLUDES Sale.customer.{name,email,company} for sales joined by Sale.jobId === job.id, but EXCLUDES PrintJob.customer (deprecated Phase 14), Sale.customer.address, Sale.customer.notes -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add searchQuery + debouncedSearchQuery + selectedChips state, tagCounts memo, search/chip filter memos, sticky filter sub-header JSX, filter-empty-state, and extend useDynamicRowHeight cache key</name>
  <files>src/components/JobsManager.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx lines 1-50 (imports — confirm `useState`, `useMemo`, `useEffect`, `useSales`, `Input`, `Button` are already imported; add a SearchIcon import if not present — use the same icon family already used in CustomerLibrary)
    - src/components/JobsManager.tsx lines 1220-1250 (the existing `useDynamicRowHeight` cache key site — D-05 lock target at line 1232)
    - src/components/JobsManager.tsx lines 1100-1230 (where the main JobsManager component body sits — find the top-level `jobs` derivation and the spot ABOVE the `<List>` rendering where the sticky sub-header will mount)
    - src/components/CustomerLibrary.tsx lines 115, 141-150, 234-273 (search state + memo + input UI + filter empty state — the structural template to mirror)
    - src/components/AssetLibrary.tsx lines 192-198 (chip render — D-11 EXACT mirror)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 46-67 (D-03 layout + D-04 AND semantics + D-05 cache key contract + D-06 search scope and 250ms debounce)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 110-113 (D-10 filter-empty-state spec — must render ABOVE the empty list region, NOT use the EmptyState primitive)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 142-147 (D-14 mobile layout — chip row overflow-x-auto at < 640px)
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 428-643 (the multi-surface analog mapping for JobsManager — 7a search, 7b chips, 7d cache key, 7f filter empty state)
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 744-752 (No-Analog notes — debounce inline approach, sticky-header outside the List, scroll+highlight in lieu of toast)
  </read_first>
  <action>
    Inside the main JobsManager component, add four new pieces of state at the top of the component (alongside existing useState calls): `const [searchQuery, setSearchQuery] = useState('');`, `const [debouncedSearchQuery, setDebouncedSearchQuery] = useState('');`, `const [selectedChips, setSelectedChips] = useState<Set<string>>(() => new Set());`. Add the 250ms debounce effect immediately after: `useEffect(() => { const t = setTimeout(() => setDebouncedSearchQuery(searchQuery), 250); return () => clearTimeout(t); }, [searchQuery]);` (PATTERNS.md No-Analog rule: inline, no `useDebounce` hook extracted unless it lands in 2+ surfaces — currently only 1 surface). Add a memo to compute every tag in use with counts (D-03 sorted alphabetically): `const tagCounts = useMemo(() => { const counts = new Map<string, number>(); for (const job of jobs) { for (const tag of job.tags ?? []) { counts.set(tag, (counts.get(tag) ?? 0) + 1); } } return [...counts.entries()].sort(([a], [b]) => a.localeCompare(b)); }, [jobs]);` Add a chip-key memo for the cache key + filter dependencies: `const selectedChipsKey = useMemo(() => [...selectedChips].sort().join(','), [selectedChips]);`. Add a chip-filter memo (D-04 AND across all selected chips): `const jobsAfterChipFilter = useMemo(() => { if (selectedChips.size === 0) return jobs; return jobs.filter(job => { const jobTags = new Set(job.tags ?? []); for (const chip of selectedChips) if (!jobTags.has(chip)) return false; return true; }); }, [jobs, selectedChips]);`. Add a search-filter memo per D-06 scope (name + tags + Sales' customer fields for matching sales): `const searchedJobs = useMemo(() => { const q = debouncedSearchQuery.toLowerCase().trim(); if (!q) return jobsAfterChipFilter; const salesByJobId = new Map<string, Sale[]>(); for (const sale of sales) { const list = salesByJobId.get(sale.jobId); if (list) list.push(sale); else salesByJobId.set(sale.jobId, [sale]); } return jobsAfterChipFilter.filter(job => { if (job.name.toLowerCase().includes(q)) return true; if (job.tags?.some(t => t.toLowerCase().includes(q))) return true; const jobSales = salesByJobId.get(job.id) ?? []; return jobSales.some(s => (s.customer?.name || '').toLowerCase().includes(q) || (s.customer?.email || '').toLowerCase().includes(q) || (s.customer?.company || '').toLowerCase().includes(q)); }); }, [jobsAfterChipFilter, sales, debouncedSearchQuery]);` — if `useSales()` is not already invoked in this component, hoist its call to the component top with `const { sales } = useSales();` (verify first: grep `useSales` in the file). Wire `searchedJobs` (the final filtered list) into the existing `<List>` rendering — find the prop that currently receives `jobs` (or whatever the existing filtered list variable name is) and replace it with `searchedJobs`. Per D-04 the final visible job list is `searchedJobs` (it already encodes the chip AND search filters). EXTEND the cache key at line 1232 verbatim per D-05: change `key: selectedJobId ?? ''` to `key: \`${selectedJobId ?? ''}|${selectedChipsKey}|${debouncedSearchQuery}\``. Update the JSDoc comment above (lines 1227-1231) to mention "key encodes selectedJobId + chip filter + debounced search — D-05 lock for TAGS-04 cache invalidation". Now add the sticky filter sub-header JSX, mounted in the JSX tree IMMEDIATELY BEFORE the `<List>` (NOT inside the List's rowComponent — per PATTERNS.md No-Analog rule about sticky-header-outside-List): wrap in `<div className="sticky top-0 z-10 bg-slate-800 pb-3">`. Inside that div, render TWO blocks. Block A — search input (mirror `CustomerLibrary.tsx:234-253`): `<div className="relative mb-3">` with SearchIcon absolute-positioned at left + `<Input type="text" value={searchQuery} onChange={e => setSearchQuery(e.target.value)} placeholder="Search jobs by title, tag, or customer" className="pl-9 pr-8 placeholder-slate-500" />` + conditional clear-X Button. Block B — chip filter row: when `tagCounts.length > 0`, render `<div className="flex flex-wrap gap-1.5 items-center sm:flex-wrap overflow-x-auto sm:overflow-visible">` (the `overflow-x-auto` + responsive break is D-14 mobile rule); map `tagCounts.map(([tag, count]) => { const isSelected = selectedChips.has(tag); return <button key={tag} type="button" onClick={() => setSelectedChips(prev => { const next = new Set(prev); if (next.has(tag)) next.delete(tag); else next.add(tag); return next; })} className={isSelected ? 'text-xs px-1.5 py-0.5 rounded bg-blue-500/30 text-blue-200 border border-blue-400/50 transition-colors' : 'text-xs px-1.5 py-0.5 rounded bg-slate-600/50 text-slate-400 hover:bg-slate-600/80 transition-colors'}>{tag} · {count}</button>; })`. After the chip map, conditionally render `{(selectedChips.size > 0 || searchQuery) && <button type="button" onClick={() => { setSelectedChips(new Set()); setSearchQuery(''); }} className="text-xs text-blue-300 hover:text-blue-200 underline ml-2">Clear filters</button>}`. Below the sticky sub-header but ABOVE the `<List>`, add the D-10 filter-empty-state: `{searchedJobs.length === 0 && jobs.length > 0 && (<div className="text-center py-12"><h3 className="text-lg font-semibold text-white">No jobs match your filter</h3><p className="text-sm text-slate-400 mt-2">Try different search terms or chip selections.</p><button type="button" onClick={() => { setSelectedChips(new Set()); setSearchQuery(''); }} className="mt-4 text-sm text-blue-300 hover:text-blue-200 underline">Clear filters</button></div>)}` (NOT the `<EmptyState>` primitive — D-10 explicit). Wrap the existing `<List>` in `{searchedJobs.length > 0 && (...)}` so the empty-filter state and the list are mutually exclusive (or simply: render the List unconditionally and let it show zero rows; the new empty-state block above is still visible to communicate). Pick the mutually-exclusive form for cleaner UX. Add a `{/* allow-raw-html: chip filter toggles + clear-filters link are tiny tap targets — Button primitive would dwarf the row, mirrors the QuoteRow overflow trigger precedent */}` comment above each `<button type="button">` raw element (the chips and clear-filters link) per the project lint guard documented in PATTERNS.md line 731-737. Do NOT touch the `[⋯]` overflow menu (Plan 15-05), the JobCard tag chips display (Plan 15-05), the inline tag editor (Plan 15-05), or features.ts (Plan 15-05). Do NOT modify the `useJobs` init wiring (Plan 15-05). Do NOT modify how `selectedJobId` is set elsewhere — just consume it in the new cache key.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -30 && grep -c "debouncedSearchQuery\|selectedChips\|tagCounts" src/components/JobsManager.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const \[searchQuery, setSearchQuery\]" src/components/JobsManager.tsx` returns `1`
    - `grep -c "const \[debouncedSearchQuery" src/components/JobsManager.tsx` returns `1`
    - `grep -c "const \[selectedChips" src/components/JobsManager.tsx` returns `1`
    - `grep -c "setTimeout(() => setDebouncedSearchQuery" src/components/JobsManager.tsx` returns `1` (the 250ms debounce effect)
    - `grep -c "tagCounts" src/components/JobsManager.tsx` returns at least `3` (memo declaration + render + chip-row dependency)
    - `grep -c "jobsAfterChipFilter\|selectedChipsKey\|searchedJobs" src/components/JobsManager.tsx` returns at least `5`
    - `grep -c "salesByJobId" src/components/JobsManager.tsx` returns at least `1` (D-06 sales join)
    - `grep -E "key: \`\\\$\\{selectedJobId" src/components/JobsManager.tsx` matches the new D-05 pipe-delimited template
    - `grep -c "bg-slate-600/50 text-slate-400" src/components/JobsManager.tsx` returns at least `1` (D-11 unselected chip styling re-used in the filter row)
    - `grep -c "bg-blue-500/30" src/components/JobsManager.tsx` returns at least `1` (selected chip styling)
    - `grep -c "No jobs match your filter" src/components/JobsManager.tsx` returns `1` (D-10 empty state)
    - `grep -c "Clear filters" src/components/JobsManager.tsx` returns at least `2` (header link + empty-state button)
    - `grep -c "overflow-x-auto" src/components/JobsManager.tsx` returns at least `1` (D-14 mobile chip-scroll)
    - `grep -c "sticky top-0" src/components/JobsManager.tsx` returns at least `1` (sub-header sticky)
    - `grep -c "useSales(" src/components/JobsManager.tsx` returns at least `1` (sales joined for D-06)
    - The existing `useDynamicRowHeight` block still exists and now uses the tri-key
    - `npx tsc -b` exits 0
    - Existing JobsManager tests (if any) still pass: `npm test -- --run src/components 2>&1 | tail -10`
  </acceptance_criteria>
  <done>Search bar + chip filter sub-header render at the top of JobsManager; AND logic + 250ms debounce + D-05 cache key are wired; filter-empty-state shows when filters yield zero matches; mobile chip overflow-x scrolls; all existing JobsManager behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text → render filter | searchQuery string is user-controlled; flows into `.toLowerCase().trim().includes(q)` filter — pure string ops, no eval, no innerHTML |
| Sale.customer fields → search match | Reads existing snapshots; no new persistence; same data path used by JobsManager elsewhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Tampering | search filter — XSS via search query | mitigate | Pure string `.includes()` on lowercased input; result drives `.filter()` only; no template injection; chips render `{tag}` as React children (escaped by default) |
| T-15-09 | Denial of Service | unbounded jobs.length × tags / sales join | accept | 500-job upper bound (per virtualization rationale in Phase 11); the join builds a single Map<jobId, Sale[]> per render — O(jobs + sales); chip-filter Set lookup is O(selectedChips) per job; well within React 18 + react-window capabilities |
</threat_model>

<verification>
- `npx tsc -b` clean exit
- Visual: sticky sub-header appears above the virtualized list with search input + chip row
- Manual: typing in the search input filters the list AFTER 250ms (debounce)
- Manual: clicking a chip selects it and filters; AND logic confirms with 2+ chips
- `key:` template at the formerly-line-1232 site now reads `\`${selectedJobId ?? ''}|${selectedChipsKey}|${debouncedSearchQuery}\``
- Existing JobsManager features (Record Sale, Create Quote, Edit, Delete, Orders sub-section) all still work
</verification>

<success_criteria>
- TAGS-02 chip filter ships with AND logic + alphabetical sort + tag count display
- TAGS-03 search bar ships with case-insensitive substring across name + tags + Sales' customer.{name,email,company}; 250ms debounce
- TAGS-04 cache-key contract ships per D-05: `\`${selectedJobId ?? ''}|${selectedChipsKey}|${debouncedSearchQuery}\``; cache invalidates on any filter dimension change → react-window resets row heights and (per react-window behavior) the list scrolls to top
- D-10 filter-empty-state surfaces when filters match zero jobs without hiding the filter UI
- D-14 mobile layout: chip row overflow-x-auto at < 640px; search input full-width
- ROADMAP Success Criteria #2, #3, #4 satisfied
- NO `[⋯]` menu, NO JobCard tag chips, NO inline tag editor in this plan (those belong to Plan 15-05)
</success_criteria>

<output>
Create `.planning/phases/15-tags-search-quick-duplicate/15-04-SUMMARY.md` when done. Include: state additions list; the exact line range where the sticky sub-header was inserted; the BEFORE and AFTER of the cache-key template; the searchedJobs final filter dependency chain (`jobs → jobsAfterChipFilter → searchedJobs`); confirmation that no Plan 15-05 surfaces were touched (no `[⋯]`, no inline editor, no features.ts).
</output>
