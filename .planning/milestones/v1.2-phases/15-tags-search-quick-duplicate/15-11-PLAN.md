---
phase: 15-tags-search-quick-duplicate
plan: 11
type: execute
wave: 8
depends_on: ["15-07", "15-08", "15-09", "15-10"]
files_modified:
  - .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
gap_id: VERIFY
requirements: [TAGS-01, TAGS-02, TAGS-03, TAGS-04, DUP-01, DUP-02]
requirements_addressed: [TAGS-01, TAGS-02, TAGS-03, TAGS-04, DUP-01, DUP-02]
tags: [verification, gap-closure-uat, checkpoint, phase-close]

must_haves:
  truths:
    - "Full automated chain runs green: `npx tsc -b` exit 0, Vitest reports ≥ 263 passed / 1 todo / 0 failed, `npm run build` exit 0, main chunk gz ≤ 300 KB per Phase 11 bundle gate."
    - "src/utils/duplicateJob.ts and src/utils/duplicateJob.test.ts are byte-identical to their pre-gap-closure state — git log shows no commits modify these two files across plans 15-07..15-10."
    - "Human UAT confirms Gap A surface (CostCalculator tag input row gone), Gap B surface (title-click panel + hover Tag icon + re-targeted NewBadge), Gap C surface (chip-filter row gone, filter-empty-state CTA reads 'Clear search'), Gap D surface (`[⋯]` overflow trigger + post-duplicate ring gone)."
    - "On gap-free verdict: VERIFICATION.md is amended with the new verdict, STATE.md advances `completed_phases` from 4 to 5, `percent` from 67 to 83."
    - "On gap-found verdict (a hypothetical Gap E or later): VERIFICATION.md records the new gap and STATE.md remains at completed_phases=4, percent=67."
  artifacts:
    - path: .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md
      provides: "Amended verification record with the gap-closure round's verdict (`gap-free` expected; or new gap_id list if any UX surface fails)."
      contains:
        - "## Gap-Closure Round (2026-05-24 amendment)"
        - "Plan 15-11"
    - path: .planning/STATE.md
      provides: "Project state advanced (on gap-free) — completed_phases=5, percent=83, current focus moves to Phase 16 finalization."
      contains_on_gap_free:
        - "completed_phases: 5"
        - "percent: 83"
  key_links:
    - from: src/utils/duplicateJob.ts
      to: pre-gap-closure commit
      via: "git log --oneline -- src/utils/duplicateJob.ts — MUST show no new commits since pre-Plan-15-07 baseline"
      pattern: "duplicateJob"
    - from: .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md
      to: phase closure
      via: "On gap-free verdict, the file's frontmatter `verdict:` field is updated to `gap-free`; on gap-found, a new gap_id letter is appended to gaps_open and the verdict stays `gaps-found`."
      pattern: "verdict:\\s*gap-free|gaps-found"
---

<objective>
Gap-closure verification — run the full automated chain (TypeScript + Vitest + build + bundle gate), then human-UAT the rescoped Phase 15 surfaces against Gap A / B / C / D's "Recommended fix surface" contracts. On gap-free verdict: amend VERIFICATION.md, advance STATE.md, and signal that Phase 15 is now complete (unblocks `/gsd:complete-milestone v1.2` after Phase 16's last plan also closes).

Purpose: Provide the closure record for the gap-closure round of Phase 15. This plan has TWO automated halves and ONE checkpoint:human-verify task between them. The automated halves are bookkeeping (run-the-chain + amend-docs); the checkpoint is where the user visually confirms the four reshape gaps have actually delivered the intended product behavior.

Output:
1. A clean run of the automated verification chain (logs captured to `/tmp/15-gap-closure-{tsc,vitest,build,bundle}.log`).
2. A human-UAT checkpoint that asks the user to confirm each of the four gap-closure surfaces (A / B / C / D) — see the `<task type="checkpoint:human-verify">` block below for the exact verification steps.
3. An amended VERIFICATION.md with the new verdict (`gap-free` or a new gap entry).
4. On gap-free verdict: STATE.md advanced to `completed_phases: 5`, `percent: 83`, `last_activity` describing the Phase 15 closure, and `stopped_at` indicating Phase 16 is the next focus.

CRITICAL non-touch in this plan: src/utils/duplicateJob.ts, src/utils/duplicateJob.test.ts, src/db/backfill.ts, src/hooks/useDatabase.ts, src/features.ts. This plan is documentation-only on the .planning/ side, plus reading source files for grep audits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md
@.planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md
@.planning/phases/15-tags-search-quick-duplicate/15-07-SUMMARY.md
@.planning/phases/15-tags-search-quick-duplicate/15-08-SUMMARY.md
@.planning/phases/15-tags-search-quick-duplicate/15-09-SUMMARY.md
@.planning/phases/15-tags-search-quick-duplicate/15-10-SUMMARY.md
@.planning/phases/15-tags-search-quick-duplicate/15-06-SUMMARY.md
@.claude/CLAUDE.md

<interfaces>
<!-- Key references the executor needs. -->

Pre-gap-closure VERIFICATION.md (.planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md) frontmatter:
```
phase: 15-tags-search-quick-duplicate
type: verification
verified: 2026-05-24
verdict: gaps-found
requirements_evaluated: [TAGS-01, TAGS-02, TAGS-03, TAGS-04, DUP-01, DUP-02]
withdrawn_requirements: [TAGS-02, DUP-01]
gaps_open: 4
amended: 2026-05-24
```

After gap-closure (target state, on gap-free verdict):
```
phase: 15-tags-search-quick-duplicate
type: verification
verified: 2026-05-24
verdict: gap-free
requirements_evaluated: [TAGS-01, TAGS-02, TAGS-03, TAGS-04, DUP-01, DUP-02]
withdrawn_requirements: [TAGS-02, DUP-01]
gaps_open: 0
gaps_closed: [A, B, C, D]
amended: <today's date — 2026-05-24>
gap_closure_round: 1
```

Pre-gap-closure STATE.md frontmatter (relevant fields):
```
progress:
  total_phases: 6
  completed_phases: 4
  total_plans: 38
  completed_plans: 36
  percent: 67
```

After gap-closure (on gap-free verdict):
```
progress:
  total_phases: 6
  completed_phases: 5
  total_plans: 43          # 38 + 5 new gap-closure plans (15-07..15-11)
  completed_plans: 43      # all complete
  percent: 83              # round((5/6) * 100)
```

Test-count baseline (pre-gap-closure, from VERIFICATION.md Automated Chain): **263 passed / 1 todo / 0 failed**.

Test-count target (post-gap-closure):
- Plan 15-07 (Gap A): no test delta. Expected: 263 / 1 / 0.
- Plan 15-08 (Gap D): no test delta. Expected: 263 / 1 / 0.
- Plan 15-09 (Gap C): no test delta. Expected: 263 / 1 / 0.
- Plan 15-10 (Gap B): may add JobsManager component tests for the new title-click panel (the plan SUMMARY decides). If added, count grows to 263+. The acceptance is `≥ 263 passed / 1 todo / 0 failed`. The verification record MUST cite the actual observed count, not the baseline.

LOCKED file evidence (DUP-02 contract preserved):
```
git log --oneline -- src/utils/duplicateJob.ts src/utils/duplicateJob.test.ts
```
After the gap-closure round commits, this command MUST show no new commits past the pre-Plan-15-07 baseline. The pre-baseline commit is the one that landed Plan 15-02 (which created these files). If the output shows a commit hash older than 2026-05-24, the files are correctly untouched.

Bundle gate (Phase 11 contract):
```
ls -lh dist/assets/index-*.js | awk '{print $5}'
```
The main chunk gz size MUST be ≤ 300 KB. Pre-gap-closure baseline: 62.0 KB. Expected post-gap-closure: marginally smaller (Plan 15-08 removed the [⋯] menu DOM, click-outside handler, handleDuplicate, handleToggleOverflow, etc.; Plan 15-09 removed the chip-row DOM, selectedChips memo, tagCounts memo, etc.; Plan 15-07 removed the CostCalculator Tags row; Plan 15-10 added the new title-click panel + 2 SVG icons but the diff is roughly net-neutral). Worst case: should stay well under 80 KB gz.

Verification log capture:
```
mkdir -p /tmp
npx tsc -b 2>&1 | tee /tmp/15-gap-closure-tsc.log
npm test -- --run 2>&1 | tee /tmp/15-gap-closure-vitest.log
npm run build 2>&1 | tee /tmp/15-gap-closure-build.log
ls -lh dist/assets/index-*.js 2>&1 | tee /tmp/15-gap-closure-bundle.log
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated verification chain (tsc + Vitest + build + bundle gate)</name>

  <read_first>
    - .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md — sections "Automated Chain" and "Per Success Criterion"
    - .planning/phases/15-tags-search-quick-duplicate/15-07-SUMMARY.md, 15-08-SUMMARY.md, 15-09-SUMMARY.md, 15-10-SUMMARY.md — confirm each prior plan landed cleanly
    - scripts/assert-bundle-size.mjs (or equivalent) — bundle gate contract
  </read_first>

  <files>(read-only; outputs to /tmp logs)</files>

  <action>
    Run the four-step automated verification chain. Capture each step's stdout+stderr to `/tmp/15-gap-closure-{tsc,vitest,build,bundle}.log`. Fail this task immediately on the first non-zero exit code.

    Step 1 — TypeScript:
    ```
    npx tsc -b 2>&1 | tee /tmp/15-gap-closure-tsc.log
    ```
    Required: exit code 0; zero errors. The log MUST contain no `error TS` lines.

    Step 2 — Vitest:
    ```
    npm test -- --run 2>&1 | tee /tmp/15-gap-closure-vitest.log
    ```
    Required: exit code 0. The final line MUST show `Tests` with `≥ 263 passed`, `1 todo`, `0 failed`. The plan executor captures the exact reported count for the SUMMARY.

    Step 3 — Build:
    ```
    npm run build 2>&1 | tee /tmp/15-gap-closure-build.log
    ```
    Required: exit code 0. The log MUST contain a `✓ built in` line. All build-time assertion scripts (bundle size, modulePreload, no-pdf-static-import) MUST pass.

    Step 4 — Bundle gate:
    ```
    ls -lh dist/assets/index-*.js 2>&1 | tee /tmp/15-gap-closure-bundle.log
    ```
    Required: the main chunk file size (in bytes or KB) MUST be ≤ 300 KB. The plan executor MUST run `gzip -c dist/assets/index-*.js | wc -c` to get the gzipped size and confirm it is ≤ 300 KB.

    Step 5 — LOCKED file integrity audit:
    ```
    git log --oneline -- src/utils/duplicateJob.ts src/utils/duplicateJob.test.ts | head -20
    ```
    Required: NO commit hashes from this session (no commits made by plans 15-07, 15-08, 15-09, 15-10). The most recent commit on these two files MUST be the one that landed Plan 15-02 (pre-gap-closure baseline).

    Additionally confirm:
    ```
    git diff HEAD~5 HEAD -- src/utils/duplicateJob.ts src/utils/duplicateJob.test.ts
    ```
    MUST output nothing (zero changes across the gap-closure round). Note: `HEAD~5` is a rough indicator (5 gap-closure plans = 5 commits if each plan = 1 commit); the executor adjusts the offset to compare against the pre-Plan-15-07 baseline. The semantics: `git diff <pre-baseline-commit> HEAD -- <locked files>` MUST produce empty output.

    Step 6 — Per-gap grep audits across the codebase:
    ```
    # Gap A — CostCalculator: zero tag-input surface
    grep -cE 'tagsInput|setTagsInput|parseTagsInput|feature="tags"' src/components/CostCalculator.tsx   # MUST output 0

    # Gap B — JobsManager: pencil-button gone; title-click + hover Tag icon in place
    grep -c 'PencilIcon' src/components/JobsManager.tsx   # MUST output 0
    grep -c 'TagIcon' src/components/JobsManager.tsx   # MUST output >= 2
    grep -c 'aria-label="Edit job title and tags"' src/components/JobsManager.tsx   # MUST output 1
    grep -c 'feature="tags"' src/components/JobsManager.tsx   # MUST output 1
    grep -c 'group flex items-center gap-3' src/components/JobsManager.tsx   # MUST output 1

    # Gap C — JobsManager: chip-filter row gone; bi-key cache; "Clear search" CTA
    grep -cE 'selectedChips|tagCounts|jobsAfterChipFilter|clearFilters' src/components/JobsManager.tsx   # MUST output 0
    grep -nE 'key:\s*`\$\{selectedJobId \?\? \x27\x27\}\|\$\{debouncedSearchQuery\}`' src/components/JobsManager.tsx | wc -l   # MUST output 1
    grep -c 'Clear search' src/components/JobsManager.tsx   # MUST output 1

    # Gap D — JobsManager: [⋯] menu gone; features.ts has no quick-duplicate
    grep -cE 'overflowOpenJobId|handleDuplicate|handleToggleOverflow|highlightedJobId|feature="quick-duplicate"|ring-2 ring-blue-400' src/components/JobsManager.tsx   # MUST output 0
    grep -c "'quick-duplicate':" src/features.ts   # MUST output 0
    grep -c "'tags':" src/features.ts   # MUST output 1
    grep -c "'search-jobs':" src/features.ts   # MUST output 1

    # DUP-02 helper + tests integrity:
    grep -c 'expect(dup\.customer)\.toBeUndefined' src/utils/duplicateJob.test.ts   # MUST output 1
    ```

    If ANY of the above grep counts deviates from the expected value, fail this task and surface the deviation to the user. Do NOT proceed to the human-UAT checkpoint.
  </action>

  <verify>
    <automated>
      # Confirm all 4 logs were created and have non-zero size:
      [ -s /tmp/15-gap-closure-tsc.log ] && [ -s /tmp/15-gap-closure-vitest.log ] && [ -s /tmp/15-gap-closure-build.log ] && [ -s /tmp/15-gap-closure-bundle.log ] && echo OK   # MUST output OK

      # Final assertion combining all gates:
      grep -E '^Tests\s+' /tmp/15-gap-closure-vitest.log | grep -E '0 failed' | wc -l   # MUST output >= 1
      grep -E '✓ built in' /tmp/15-gap-closure-build.log | wc -l   # MUST output >= 1
      grep -E 'error TS' /tmp/15-gap-closure-tsc.log | wc -l   # MUST output 0
    </automated>
  </verify>

  <acceptance_criteria>
    - All 4 log files exist and have non-zero size at `/tmp/15-gap-closure-{tsc,vitest,build,bundle}.log`.
    - `/tmp/15-gap-closure-tsc.log` contains zero `error TS` lines.
    - `/tmp/15-gap-closure-vitest.log` final summary line shows `≥ 263 passed`, `1 todo`, `0 failed`.
    - `/tmp/15-gap-closure-build.log` contains a `✓ built in` line and no error stack traces.
    - `/tmp/15-gap-closure-bundle.log` shows the main chunk size; `gzip -c dist/assets/index-*.js | wc -c` returns ≤ 307200 bytes (300 KB).
    - All grep audits in Step 6 pass with the expected counts.
    - `git log --oneline -- src/utils/duplicateJob.ts src/utils/duplicateJob.test.ts` shows no new commits past the Plan 15-02 baseline.
  </acceptance_criteria>

  <done>
    Automated chain is green. All grep audits across CostCalculator (Gap A), JobsManager (Gap B + C + D), and features.ts (Gap D) confirm the four reshape surfaces shipped per VERIFICATION.md's "Recommended fix surface" specs. DUP-02 LOCKED files are byte-identical to their Plan 15-02 baseline. The human-UAT checkpoint (Task 2) can now run.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 15 gap-closure round complete. Plans 15-07 (Gap A), 15-08 (Gap D), 15-09 (Gap C), 15-10 (Gap B) all landed. Automated chain is green (tsc clean, Vitest ≥ 263 passed, build clean, bundle ≤ 300 KB gz). DUP-02 LOCKED helper + 7-case Vitest contract preserved byte-identically.

    Awaiting user UAT to confirm each of the four reshape surfaces matches the product intent recorded in VERIFICATION.md's "Recommended fix surface" sections.
  </what-built>

  <how-to-verify>
    Pre-step: `npm run dev` is already running on port 4173 (or start it: `cd <repo-root>; npm run dev`). Open `http://localhost:4173` in a fresh-context browser window (or hard-refresh an existing session — `Cmd+Shift+R`) so the rebuilt bundle loads.

    Navigate to "My Jobs" tab. Confirm the existing jobs list (at least one job exists; if no jobs exist, save a quick dummy job via the Calculator tab first — e.g., name="UAT Job", set a printer, set printTimeHours=1, set sellingPrice=10).

    ---

    **Gap A verification — CostCalculator no longer has a Tag input row:**
    1. Click the "Calculator" tab.
    2. Scroll to the Save section. Look for the row containing "Print Name", "Model Cost", "Author Min Price", "Model URL".
    3. **PASS:** there is NO "Tags" label or input field visible in the Save section, in the form, or anywhere on the Calculator tab.
    4. **PASS:** the form still allows you to save a job — fill the required fields, click "Save Job", and confirm the job appears in My Jobs.
    5. If a "Tags" field is still visible anywhere on Calculator: **FAIL Gap A**.

    ---

    **Gap B verification — Title-click panel + hover Tag icon (Tag NewBadge re-targeted):**
    1. Return to "My Jobs" tab.
    2. Hover over a job's title row (the line that contains the chevron `▶`, the job name, and the break-even pill).
    3. **PASS:** a small Tag icon fades in next to the title on hover. The icon should be a stylized rectangular tag shape (lucide-style outline).
    4. **PASS:** a small "NEW" badge sits at the top-right of the Tag icon (the badge is on the icon — NOT on a pencil button, NOT on the title text). If you previously dismissed the badge (localStorage `seen` timer expired), it may not appear — that is expected; the placement is what matters.
    5. Click the Tag icon. **PASS:** an inline edit panel appears immediately below the title row (NOT inside the expanded accordion — the panel should be visible whether or not the job is "expanded" with its cost/break-even details).
    6. Confirm the panel contains TWO input fields: one labeled "Title" (pre-filled with the current job name) and one labeled "Tags" (pre-filled with the comma-joined current tags).
    7. Confirm the panel has ONE pair of buttons at the bottom: "Cancel" (ghost) and "Save" (primary blue).
    8. Edit the title to a new value (e.g., add " (uat)" suffix). Edit tags (e.g., "uat, gap-b-test"). Click "Save".
    9. **PASS:** the job title in the row updates immediately to the new value; the tags chips below the filament meta line update to show "uat" and "gap-b-test".
    10. Hover over the title again, click the Tag icon (or click the title directly).
    11. **PASS:** the inline edit panel opens. Click the title text DIRECTLY (not the Tag icon).
    12. **PASS:** the same inline edit panel opens. Press Escape — the panel closes without saving.
    13. **PASS:** no pencil button is anywhere on the JobCard action row. The action row contains exactly: Record Sale (green) → Create Quote (blue) → Edit (ghost) → Delete (red). No `[⋯]` button.
    14. **PASS:** the chevron `▶` button to the left of the title is the only affordance that toggles row expansion. Click the chevron — the row expands (chevron rotates to `▼`). Click the chevron again — the row collapses. Click ANYWHERE else on the card body (cost numbers, filament meta, tag chips, action buttons) — the row does NOT toggle expansion (action buttons still fire their own onClick, but the card body itself is no longer a click-toggle target).
    15. If any of: pencil button visible, Tag icon missing on hover, NEW badge in wrong position, panel does not save, panel only opens when row is expanded, card-body click toggles expansion: **FAIL Gap B**.

    ---

    **Gap C verification — Chip-filter row gone, "Clear search" CTA:**
    1. Stay on "My Jobs" tab.
    2. Inspect the sticky sub-header at the top of the list (just below "My Print Jobs" heading).
    3. **PASS:** the sub-header contains ONLY the search input (with magnifying-glass icon, clear-X button on the right when text is entered, and a small "NEW" badge in the corner of the wrapper). NO chip-filter row of tag pills (e.g. `pla · 3`, `phone-stand · 1`) appears below the search input.
    4. Type a search query that returns 0 results (e.g., "xxxxxx-no-match"). **PASS:** the filter-empty-state appears with the heading "No jobs match your search" and a "Clear search" link/button. Click "Clear search" — the search input clears, all jobs reappear.
    5. **PASS:** when typing a partial tag name in the search input (e.g., type "uat" after the Gap B test), jobs with that tag substring in their tags array DO appear (the search includes tags per D-06 scope).
    6. If a chip-filter strip is visible, OR the filter-empty-state still reads "No jobs match your filter" / "Clear filters", OR clicking "Clear search" doesn't clear the input: **FAIL Gap C**.

    ---

    **Gap D verification — [⋯] overflow trigger gone, no post-duplicate ring, helper preserved:**
    1. Stay on "My Jobs" tab. Expand any job (click its chevron).
    2. Inspect the action row at the bottom of the expanded job card.
    3. **PASS:** the action row contains ONLY: Record Sale → Create Quote → Edit → Delete. NO `[⋯]` button anywhere on the action row, no Tag icon (that lives in the title row only — Gap B), no NEW badge floating off a hidden button.
    4. **PASS:** no "Duplicate" menuitem can be opened from any JobCard action row. There is no overflow trigger.
    5. **PASS:** no row ever shows a `ring-2 ring-blue-400` blue highlight ring (this was the post-duplicate UI; it is gone).
    6. **DUP-02 helper integrity check (you do NOT need to verify this manually — the automated chain in Task 1 already confirmed):** `src/utils/duplicateJob.ts` and `src/utils/duplicateJob.test.ts` are byte-identical to their Plan 15-02 state. The 7 Vitest cases (D-15 contract + by-value isolation + nextCopyName collision logic) all pass.
    7. If any `[⋯]` button is visible, OR a Duplicate menuitem is reachable, OR a blue ring appears on any row, OR the action row contains anything beyond the four Record Sale / Create Quote / Edit / Delete buttons: **FAIL Gap D**.

    ---

    **Resume signal:**
    - Type `approved` if all four gap surfaces pass.
    - Describe the issue if any check fails (e.g., "Gap B FAIL: pencil button still visible on JobCard"). On any failure, the agent treats the surface as a new gap (Gap E, Gap F, etc.) and surfaces a follow-up plan-phase recommendation rather than amending VERIFICATION.md to gap-free.
  </how-to-verify>

  <resume-signal>Type "approved" if all 4 gap surfaces pass UAT, OR describe specific surface failures (which gap, what observed).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Amend VERIFICATION.md and advance STATE.md (on approved verdict)</name>

  <read_first>
    - .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md — full file (will be amended in place)
    - .planning/STATE.md — full file (frontmatter will be updated)
    - Logs at /tmp/15-gap-closure-{tsc,vitest,build,bundle}.log (captured in Task 1)
  </read_first>

  <files>
    .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md
    .planning/STATE.md
  </files>

  <action>
    On the "approved" resume signal from Task 2, execute this task. On any other signal (describing a failure), abort this task — the user has surfaced a new gap that requires its own gap-closure round (`/gsd:plan-phase 15 --gaps` should be re-run).

    Step 1 — Amend .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md:

    1a. Update frontmatter:
    - Change `verdict: gaps-found` → `verdict: gap-free`.
    - Change `gaps_open: 4` → `gaps_open: 0`.
    - Add a new line: `gaps_closed: [A, B, C, D]`.
    - Add a new line: `gap_closure_round: 1`.
    - Update `amended:` to today's date (2026-05-24, formatted to match the existing date format in the file).

    1b. Append a new section at the END of the file (after the existing "*Verification authored: 2026-05-24*" lines), titled `## Gap-Closure Round (2026-05-24 amendment)`:

    The section MUST include:
    - **Plans landed:** 15-07 (Gap A), 15-08 (Gap D), 15-09 (Gap C), 15-10 (Gap B), 15-11 (this plan)
    - **Automated chain result table** with the captured numbers from `/tmp/15-gap-closure-*.log`:
      - TypeScript: exit 0, 0 errors
      - Vitest: <actual_count> passed / 1 todo / 0 failed
      - Build: clean
      - Bundle: main chunk gz <actual_size> KB (vs 300 KB gate)
    - **DUP-02 LOCKED file integrity:** `git log --oneline -- src/utils/duplicateJob.ts src/utils/duplicateJob.test.ts` shows zero new commits past the Plan 15-02 baseline — paste the actual git output.
    - **Per-gap closure status** table:
      - Gap A — CLOSED — CostCalculator tag input row removed; parseTagsInput export preserved (consumed by Plan 15-10 panel).
      - Gap B — CLOSED — Title-click inline panel + hover Tag icon shipped; NewBadge `tags` re-targeted to overlay the Tag icon; chevron button added for explicit selection toggle.
      - Gap C — CLOSED — Chip-filter row removed; selectedChips/tagCounts/selectedChipsKey/jobsAfterChipFilter all removed; useDynamicRowHeight cache key narrowed to pipe-delimited bi-key (`selectedJobId|debouncedSearchQuery`); filter-empty-state CTA renamed "Clear search".
      - Gap D — CLOSED — [⋯] overflow menu removed; post-duplicate scroll/highlight removed; click-outside handler removed; features.ts `quick-duplicate` entry removed; DUP-02 helper + 7-case Vitest contract preserved byte-identically.
    - **Updated Per Success Criterion table** (re-state SC#1..#5 with their post-gap-closure status: SC#1 PASS post-Gap-A+B; SC#2 WITHDRAWN; SC#3 PASS; SC#4 PASS post-Gap-C cache-key narrow; SC#5 helper-only PASS).
    - **Updated Per Decision table** rows for D-01 (single JobsManager surface, title-click + hover Tag icon — RESHAPE COMPLETE), D-03 (search-only sticky sub-header — RESHAPE COMPLETE), D-04 (n/a), D-05 (bi-key — RESHAPE COMPLETE), D-07 (overflow withdrawn — REMOVED), D-13 (badge wiring: tags re-targeted to JobsManager hover Tag icon, search-jobs unchanged, quick-duplicate removed — RESHAPE COMPLETE), D-14 (n/a).
    - **Updated Requirement Closure** table: TAGS-01 COMPLETE, TAGS-02 WITHDRAWN (unchanged), TAGS-03 COMPLETE (unchanged), TAGS-04 COMPLETE, DUP-01 WITHDRAWN-FROM-V1.2 (unchanged), DUP-02 COMPLETE (unchanged).
    - **Next Steps:** Phase 15 is now CLOSED. Move to Phase 16 finalization (`/gsd:complete-milestone v1.2` once Phase 16 ships its last plan).

    Step 2 — Update .planning/STATE.md frontmatter:

    2a. `status: executing` stays (still mid-milestone — Phase 16 not yet closed).

    2b. Update `stopped_at:` to: `"Phase 15 closed (gap-closure round complete — verdict gap-free); next focus is Phase 16 finalization for /gsd:complete-milestone v1.2"`.

    2c. Update `last_updated:` to today's ISO timestamp.

    2d. Update `last_activity:` to: `"2026-05-24 — Phase 15 closed via gap-closure round (Plans 15-07 through 15-11). 4 gaps closed: A (CostCalc tag input removed), B (title-click inline panel + hover Tag icon), C (chip-filter row removed; TAGS-02 visibly withdrawn; cache key bi-key), D ([⋯] menu removed; DUP-01 visibly withdrawn-from-v1.2; DUP-02 helper preserved)."`.

    2e. Update `progress:` block:
    - `completed_phases: 4` → `completed_phases: 5`
    - `total_plans: 38` → `total_plans: 43` (5 new gap-closure plans: 15-07, 15-08, 15-09, 15-10, 15-11)
    - `completed_plans: 36` → `completed_plans: 43`
    - `percent: 67` → `percent: 83`

    Step 3 — Append a new entry to the "Decisions" log inside STATE.md body (under `### Decisions`, after the existing 2026-05-24 entries):

    ```
    - [v1.2 Phase 15 gap-closure 2026-05-24]: Gap-closure round complete (Plans 15-07..15-11). Verdict gap-free. SC#2 + DUP-01 confirmed withdrawn at runtime; CostCalc tag input removed; JobsManager title-click panel + hover Tag icon ship; cache key bi-key; DUP-02 helper preserved. Phase 15 advances to completed_phases=5, percent=83. Phase 16 finalization unblocks /gsd:complete-milestone v1.2.
    ```

    Step 4 — Update the "Current Position" block in STATE.md body:
    Before: `Phase: 15 (tags-search-quick-duplicate) — VERIFICATION RECORDED, GAPS FOUND`
    After:  `Phase: 16 (printable-pdf-quote) — last plan in flight`

    Before: `Plan: 6 of 6 (Tasks 1+2+3 done; phase remains OPEN pending gap closure)`
    After:  `Plan: 13 of 13 (Phase 16 final UAT in flight; Phase 15 gap-closure round complete)`

    Before: `Status: Verdict gaps-found (3 gaps...)`
    After:  `Status: Phase 15 closed gap-free; awaiting Phase 16 closure to unblock /gsd:complete-milestone v1.2.`

    Before: `Last activity: 2026-05-24`
    After: (leave unchanged — same day)

    Step 5 — Run the docs commit:
    ```
    gsd-sdk query commit "docs(15): close phase 15 gap-closure round (gap-free; A/B/C/D closed)" --files .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md .planning/STATE.md
    ```

    Do NOT modify ROADMAP.md (Phase 15's SC#2 and SC#5 are already correctly annotated as withdrawn / rescoped per the earlier Gap A/B/C amendment + Gap D amendment). Do NOT modify REQUIREMENTS.md (TAGS-02 + DUP-01 are already marked withdrawn correctly).

    Do NOT touch any src/ file in this task — this task is documentation-only.
  </action>

  <verify>
    <automated>
      # VERIFICATION.md amendment:
      grep -c '^verdict: gap-free$' .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md   # MUST output 1
      grep -c '^gaps_closed: \[A, B, C, D\]$' .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md   # MUST output 1
      grep -c '^gap_closure_round: 1$' .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md   # MUST output 1
      grep -c '## Gap-Closure Round (2026-05-24 amendment)' .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md   # MUST output 1

      # STATE.md advancement:
      grep -c '^  completed_phases: 5$' .planning/STATE.md   # MUST output 1
      grep -c '^  percent: 83$' .planning/STATE.md   # MUST output 1
      grep -c '^  total_plans: 43$' .planning/STATE.md   # MUST output 1

      # No src/ file diffs from this task:
      git diff --name-only HEAD~1 HEAD -- src/   # MUST output nothing (last commit is docs-only)

      # Commit landed:
      git log -1 --oneline   # MUST contain "docs(15)" and "gap-free"
    </automated>
  </verify>

  <acceptance_criteria>
    - .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md has `verdict: gap-free` in the frontmatter.
    - .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md has new sections: "Gap-Closure Round (2026-05-24 amendment)" with automated chain results, per-gap closure status table, updated SC + decision + requirement tables.
    - .planning/STATE.md has `completed_phases: 5`, `percent: 83`, `total_plans: 43`, `completed_plans: 43`.
    - .planning/STATE.md has the new decision-log entry citing this gap-closure round.
    - .planning/STATE.md Current Position block points to Phase 16 as the next focus.
    - The docs commit landed successfully with message starting `docs(15):` and mentioning `gap-free`.
    - `git diff --name-only HEAD~1 HEAD -- src/` outputs nothing (no source code changed in this task).
  </acceptance_criteria>

  <done>
    Phase 15 is closed. VERIFICATION.md verdict is `gap-free`. STATE.md is advanced to 5/6 phases complete (83%). The DUP-02 LOCKED helper + 7-case Vitest contract are preserved byte-identically. TAGS-02 + DUP-01 remain withdrawn (REQUIREMENTS.md, ROADMAP.md, and now VERIFICATION.md all align). Phase 16 finalization is the next milestone-level focus; `/gsd:complete-milestone v1.2` becomes runnable as soon as Phase 16's last plan ships.
  </done>
</task>

</tasks>

<verification>
- Confirm all four /tmp/15-gap-closure-*.log files are non-empty and contain the expected success signals (Task 1 automated audit).
- Confirm the user's resume signal from Task 2 is the literal string `approved` (case-insensitive ok).
- Confirm the final two-file commit in Task 3 lands cleanly via `git log -1 --oneline`.
- If the user's resume signal describes a new failure, do NOT execute Task 3 — instead surface the failure as a new gap (Gap E or later) and recommend re-running `/gsd:plan-phase 15 --gaps`.
</verification>

<success_criteria>
- Automated chain green: tsc 0 errors / Vitest ≥ 263 passed / build clean / main chunk ≤ 300 KB gz.
- DUP-02 LOCKED files byte-identical (git log + git diff evidence).
- All 4 reshape surfaces (Gap A / B / C / D) confirmed by human UAT.
- VERIFICATION.md amended to `verdict: gap-free` with full closure record.
- STATE.md advanced to completed_phases=5, percent=83.
- Docs commit landed.
- Phase 15 OPEN→CLOSED status transition is observable in the project state.
</success_criteria>

<output>
Create .planning/phases/15-tags-search-quick-duplicate/15-11-SUMMARY.md when done.

Summary must record:
- Automated chain numbers (tsc, Vitest count, build, bundle size in KB gz)
- Per-gap closure confirmation (A / B / C / D — each with the deciding grep audit result)
- DUP-02 LOCKED files integrity confirmation (git log output + git diff output)
- Human UAT verdict (approved / specific failure)
- VERIFICATION.md amendment summary (frontmatter changes + new section added)
- STATE.md advancement summary (4→5 phases, 67%→83%, plan count 38→43)
- The exact commit hash that landed the docs amendment
- Next steps: Phase 16 finalization unblocks /gsd:complete-milestone v1.2.
</output>
