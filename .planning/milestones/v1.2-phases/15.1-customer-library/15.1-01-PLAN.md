---
phase: 15.1-customer-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/db/database.ts
  - src/hooks/useDatabase.ts
  - src/utils/__tests__/byValueSnapshot.test.ts
autonomous: true
requirements:
  - CL-02
  - CL-05

must_haves:
  truths:
    - "D-01: `Customer` interface exists in src/types.ts and extends `JobCustomer` (same five fields flow into per-Sale snapshot with zero field mapping)"
    - "D-02: Dexie database opens at version 7 with a new `customers` store (schema `'id, name, email, lastUsedAt'`, non-unique email index, no-op upgrade callback); existing v6 data still loads"
    - "D-03: `useCustomers()` hook returns an array, a `customersByEmail` Map, isLoading, and CRUD + `bumpLastUsed` + `bulkImportCustomers` callbacks; library edits do NOT bump `lastUsedAt`"
    - "Editing a Customer library record does NOT mutate any historical `Sale.customer` snapshot (proven by a unit test)"
    - "`npm run build` passes with no TypeScript errors"
  artifacts:
    - path: "src/types.ts"
      provides: "`Customer` interface (extends `JobCustomer` with `id: string`, `createdAt: Date`, `lastUsedAt?: Date`)"
      contains: "export interface Customer extends JobCustomer"
    - path: "src/db/database.ts"
      provides: "Dexie v7 block adding the `customers` store and `customers: EntityTable<Customer, 'id'>` type intersection"
      contains: "db.version(7).stores"
    - path: "src/hooks/useDatabase.ts"
      provides: "`useCustomers()` entity hook with memoized `customersByEmail` map and `bumpLastUsed` / `bulkImportCustomers` callbacks"
      exports: ["useCustomers"]
    - path: "src/utils/__tests__/byValueSnapshot.test.ts"
      provides: "Vitest unit test that proves editing a Customer record does not mutate a Sale.customer snapshot (locks ROADMAP success criterion #4)"
      contains: "byte-identical"
  key_links:
    - from: "src/hooks/useDatabase.ts"
      to: "src/db/database.ts (db.customers)"
      via: "useLiveQuery(() => db.customers.toArray())"
      pattern: "db\\.customers\\.toArray"
    - from: "src/db/database.ts (intersection type)"
      to: "src/types.ts (Customer)"
      via: "import type { Customer } from '../types'"
      pattern: "customers: EntityTable<Customer"
---

<objective>
Foundation work for Phase 15.1 — Customer Library. Add the `Customer` interface (D-01), bump Dexie to v7 with the new `customers` store (D-02), and create the `useCustomers()` entity hook (D-03 + UI-SPEC discretion #15). Also land the by-value snapshot audit unit test that locks ROADMAP success criterion #4 (CL-05) so the architecture is provably correct before any consumer touches it.

Purpose: Every downstream plan (CustomerLibrary tab, CSV importer, combobox picker) imports the `Customer` type, the Dexie store, and the `useCustomers()` hook. Landing them first in Wave 1 means Plans 02–04 can be authored against a stable foundation with no scavenger hunts.

Output: Extended `src/types.ts`, Dexie v7 in `src/db/database.ts`, new `useCustomers()` hook in `src/hooks/useDatabase.ts`, and a passing Vitest test asserting Sale snapshots remain byte-identical after Customer library edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15.1-customer-library/15.1-CONTEXT.md
@.planning/phases/15.1-customer-library/15.1-UI-SPEC.md
@.planning/phases/15.1-customer-library/15.1-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@./CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. These are produced by THIS plan. -->

Target shape for `src/types.ts` (insert immediately after the existing `JobCustomer` interface — see PATTERNS.md §5 for the exact placement and JSDoc):
```typescript
export interface Customer extends JobCustomer {
  id: string;
  createdAt: Date;
  lastUsedAt?: Date;
}
```

Target Dexie schema string (D-02): `'id, name, email, lastUsedAt'` — `email` is a NON-UNIQUE secondary index (fast lookup, never throws ConstraintError on duplicate).

Target hook public surface (PATTERNS.md §8):
```typescript
export function useCustomers(): {
  customers: Customer[];
  customersByEmail: Map<string, Customer>;  // key: customer.email.trim().toLowerCase()
  isLoading: boolean;
  addCustomer: (customer: Customer) => Promise<void>;
  updateCustomer: (customer: Customer) => Promise<void>;
  deleteCustomer: (id: string) => Promise<void>;
  bumpLastUsed: (id: string) => Promise<void>;
  bulkImportCustomers: (toImport: Customer[]) => Promise<void>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `Customer` interface to src/types.ts</name>
  <files>src/types.ts</files>
  <read_first>
    - src/types.ts (read the FULL file once; pay close attention to the existing `JobCustomer` interface around line 140 — Phase 14's 2026-05-22 follow-up added `notes?: string` to it, and that field is what the library record inherits for free)
    - .planning/phases/15.1-customer-library/15.1-PATTERNS.md §5 (the exact `Customer` interface JSDoc to write — copy verbatim)
    - .planning/phases/15.1-customer-library/15.1-CONTEXT.md §decisions D-01, D-03, D-05 (locks the field set and the semantics of `lastUsedAt`)
  </read_first>
  <action>
    Insert a new `Customer` interface immediately AFTER the existing `JobCustomer` interface in `src/types.ts` (per D-01). The shape is:

    - Extends `JobCustomer` (so it picks up `name`, `email`, `company`, `address`, `notes` with zero field mapping)
    - Adds required `id: string`
    - Adds required `createdAt: Date`
    - Adds optional `lastUsedAt?: Date`

    Copy the JSDoc block verbatim from PATTERNS.md §5 — it documents:
    - That the library-only fields (`id`, `createdAt`, `lastUsedAt`) are NEVER copied into the per-Sale snapshot (D-05 by-value rule)
    - That `lastUsedAt` is bumped only when picked in the Record Sale combobox or via the email-match silent-link path (D-03), NOT by library edits

    Do NOT modify the existing `JobCustomer` interface (locked by CONTEXT §domain "Out of scope"). Do NOT change the existing `Sale.customer?: JobCustomer` field at types.ts:243 — the per-Sale snapshot stays as `JobCustomer`, not `Customer`, because the library-only fields must not leak into sales.

    Use `interface Customer extends JobCustomer` (PascalCase interface name, `extends` clause), not `type Customer = JobCustomer & ...` — matches the project's `interface for objects with extensibility` convention (CONVENTIONS.md).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    <automated>grep -c "^export interface Customer extends JobCustomer" src/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/types.ts` contains the literal line `export interface Customer extends JobCustomer {`
    - Source: the three new fields are exactly `id: string;`, `createdAt: Date;`, and `lastUsedAt?: Date;` (no other fields)
    - Source: the existing `JobCustomer` interface is unchanged (byte-identical to its pre-plan content)
    - Source: `Sale.customer` at types.ts:243 still references `JobCustomer`, NOT `Customer`
    - Build: `npm run build` exits 0 (no TypeScript errors)
    - Behavior: The new interface compiles in strict mode under `tsc -b` (existing `tsconfig.app.json` `strict: true`)
  </acceptance_criteria>
  <done>
    `src/types.ts` exports a `Customer` interface that extends `JobCustomer` with `id`, `createdAt`, and optional `lastUsedAt`; `npm run build` passes; no edits made to `JobCustomer` or `Sale.customer`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bump Dexie to v7 and add the `customers` store</name>
  <files>src/db/database.ts</files>
  <read_first>
    - src/db/database.ts (read the FULL file once; pay close attention to lines 11-19 — the `Dexie` declaration block with `EntityTable` intersections — and lines 74-90 — the existing `db.version(6)` block plus the `db.on('versionchange', ...)` reload handler)
    - .planning/phases/15.1-customer-library/15.1-PATTERNS.md §6 (the exact v7 block to add) and §7 (the EntityTable intersection edit)
    - .planning/phases/15.1-customer-library/15.1-CONTEXT.md §decisions D-02 (locks the schema string, the no-op upgrade, and the multi-tab reload coverage)
  </read_first>
  <action>
    Two edits to `src/db/database.ts`:

    (a) Add `Customer` to the existing type-only import from `'../types'` at the top of the file. The current import groups everything in one line — append `, Customer` to it (do NOT add a second `import type` line for the same source path).

    (b) Add `customers: EntityTable<Customer, 'id'>;` to the `Dexie & { ... }` intersection inside the `new Dexie('3DCosterDB') as Dexie & { ... }` declaration. Place it on its own line AFTER `settings: EntityTable<Setting, 'key'>;` so the intersection ends with the new store.

    (c) Add a new `db.version(7).stores({...})` block IMMEDIATELY after the existing `db.version(6)...` block (which ends with the `.upgrade(...)` call). The v7 stores object must include EVERY store from v6 (Dexie requires all stores listed each version) PLUS the new line `customers: 'id, name, email, lastUsedAt',`. The schema string is locked: `'id, name, email, lastUsedAt'` — `email` is the secondary index (non-unique — do NOT prefix with `&`). No `.upgrade()` callback on v7 (the store starts empty per D-02; Dexie auto-creates new stores on first open).

    Do NOT modify the existing v6 block (it stays intact for forward compatibility with v6→v7 upgrades). Do NOT modify the `db.on('versionchange', ...)` handler (already covers v7 per Phase 12 SCHEMA-02).

    Use the EXACT v7 block from PATTERNS.md §6 as the template — including the inline comment noting the email index is non-unique.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    <automated>grep -c "db.version(7).stores" src/db/database.ts</automated>
    <automated>grep -c "customers: 'id, name, email, lastUsedAt'" src/db/database.ts</automated>
    <automated>grep -c "customers: EntityTable<Customer, 'id'>" src/db/database.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/db/database.ts` contains exactly one `db.version(7).stores(` occurrence (no accidental duplicate from a previous run)
    - Source: the v7 stores object includes the line `customers: 'id, name, email, lastUsedAt',` (verbatim, including the trailing comma)
    - Source: the v7 stores object also includes ALL existing v6 stores (materials, printers, printerInstances, jobs, sales, settings) — Dexie requires every version restate every store
    - Source: the `Dexie` intersection declaration contains `customers: EntityTable<Customer, 'id'>;`
    - Source: the existing `db.version(6).stores(...)` block is byte-identical to its pre-plan content
    - Source: there is NO `.upgrade(` call attached to the v7 block (no-op upgrade per D-02)
    - Build: `npm run build` exits 0 (no TypeScript errors)
    - Behavior: opening the app in a browser loads the database without throwing; `await db.customers.count()` returns 0 on first load (will be UAT-verified in Plan 05)
  </acceptance_criteria>
  <done>
    Dexie is bumped to v7 with a new `customers` store keyed by `id` and indexed on `name`, `email`, `lastUsedAt`; the `customers` EntityTable is declared on the `db` type intersection; `npm run build` passes; v6 block untouched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add `useCustomers()` hook to src/hooks/useDatabase.ts</name>
  <files>src/hooks/useDatabase.ts</files>
  <read_first>
    - src/hooks/useDatabase.ts (read the FULL file once; pay close attention to `usePrinterInstances` at lines 186-229 — that hook is the closest minimal analog because Customer, like PrinterInstance, has no seed data; do NOT use `useAssets` as the template, it carries seed-data init the customer hook does not need)
    - .planning/phases/15.1-customer-library/15.1-PATTERNS.md §8 (the exact `useCustomers()` body to write — copy the structure verbatim, then adjust callback names and add the `customersByEmail` `useMemo`)
    - .planning/phases/15.1-customer-library/15.1-CONTEXT.md §decisions D-03 (locks the `lastUsedAt` semantics — picker bumps it, library edits do NOT) and UI-SPEC discretion #15 (locks the `customersByEmail` map exposure)
  </read_first>
  <action>
    Append a new `useCustomers()` exported function at the bottom of `src/hooks/useDatabase.ts`. The function returns an object with these named members (all locked by the interface contract in this plan's `<context>`):

    - `customers: Customer[]` — from `useLiveQuery(() => db.customers.toArray(), [])` with `?? []` fallback (matches usePrinterInstances pattern)
    - `customersByEmail: Map<string, Customer>` — memoized via `useMemo` over `customers`; key is `(c.email || '').trim().toLowerCase()`; rows with empty email are excluded from the map (no empty-key collisions)
    - `isLoading: boolean` — initial `useState(true)`; one-shot `db.customers.count()` flips it to false (cancelled-effect cleanup as in usePrinterInstances)
    - `addCustomer(customer: Customer): Promise<void>` — wraps `db.customers.add(customer)` via `useCallback([], ...)`
    - `updateCustomer(customer: Customer): Promise<void>` — wraps `db.customers.put(customer)` via `useCallback([], ...)`
    - `deleteCustomer(id: string): Promise<void>` — wraps `db.customers.delete(id)` via `useCallback([], ...)`
    - `bumpLastUsed(id: string): Promise<void>` — fetches the existing record via `db.customers.get(id)`, then `db.customers.put({...existing, lastUsedAt: new Date()})`. Guard: if `get` returns undefined (record deleted concurrently) do nothing. This callback is consumed by Plan 04 (combobox pick) and Plan 04 (silent email-match auto-link). Per D-03 it MUST NOT be called by `updateCustomer` — that's a library edit, not a "used in business" event.
    - `bulkImportCustomers(toImport: Customer[]): Promise<void>` — wraps `db.customers.bulkPut(toImport)` (mirrors `useAssets.bulkImportAssets` at lines 96-98). Consumed by Plan 03 (CSV importer).

    Imports to add at the top of the file:
    - `import type { Customer } from '../types';` (or extend the existing type import group on the same source)
    - Ensure `useMemo` is in the React imports (the file already imports `useState`, `useEffect`, `useCallback` — add `useMemo` if missing)

    Use the verbatim hook body from PATTERNS.md §8 as the template. Naming convention (CONVENTIONS.md): camelCase function names with `use` prefix; named export.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    <automated>grep -c "^export function useCustomers" src/hooks/useDatabase.ts</automated>
    <automated>grep -c "customersByEmail" src/hooks/useDatabase.ts</automated>
    <automated>grep -c "bumpLastUsed" src/hooks/useDatabase.ts</automated>
    <automated>grep -c "bulkImportCustomers" src/hooks/useDatabase.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/hooks/useDatabase.ts` contains the literal export `export function useCustomers()`
    - Source: the returned object includes ALL of these keys (assert via grep): `customers`, `customersByEmail`, `isLoading`, `addCustomer`, `updateCustomer`, `deleteCustomer`, `bumpLastUsed`, `bulkImportCustomers`
    - Source: `customersByEmail` is built via `useMemo` and uses `(c.email || '').trim().toLowerCase()` as the map key
    - Source: `bumpLastUsed` calls `db.customers.get(id)` BEFORE the `put` (read-modify-write — needed because Dexie has no in-place field update for record fields; also makes the no-op-on-missing-record behavior explicit)
    - Source: `updateCustomer` does NOT call `bumpLastUsed` and does NOT set `lastUsedAt` (D-03 locks this — library edits are not "used in business" events)
    - Build: `npm run build` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>
    `useCustomers()` is exported from `src/hooks/useDatabase.ts` with the public surface specified in `<interfaces>`; the `customersByEmail` map is memoized; `bumpLastUsed` is a separate callback that updates `lastUsedAt` only when explicitly invoked; `npm run build` passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add by-value snapshot audit unit test (CL-05)</name>
  <files>src/utils/__tests__/byValueSnapshot.test.ts</files>
  <read_first>
    - src/types.ts (after Task 1 — needed to confirm `Sale.customer?: JobCustomer` snapshot shape at line 243 and the new `Customer` interface)
    - .planning/phases/15.1-customer-library/15.1-CONTEXT.md §decisions D-05 (the by-value rule) and §"Claude's Discretion" bullet "Test coverage strategy" (locks the test name + assertion shape)
    - .planning/ROADMAP.md §"Phase 15.1: Customer Library (INSERTED)" success criterion #4 (the requirement this test locks: editing a Customer record does not mutate the corresponding Sale's customer field — byte-identical)
    - Any existing `.test.ts` file under `src/` to confirm the project's Vitest test conventions — likely `src/utils/threeMfParser.test.ts` or `src/utils/costCalc.test.ts` (look for an existing test file as the format template)
  </read_first>
  <behavior>
    - Test 1 ("editing a Customer library record does not mutate a Sale.customer snapshot"):
      - Construct a fixture: `customer: Customer = { id: 'c-1', createdAt: new Date('2026-01-01'), name: 'Alice Pre-Edit', email: 'alice@example.com', company: 'AcmePre', address: '123 Old St', notes: 'pre-edit notes' }`
      - Build a `sale.customer: JobCustomer` snapshot by spreading the customer's `JobCustomer` fields ONLY (omit `id`, `createdAt`, `lastUsedAt`): `const snapshot: JobCustomer = { name: customer.name, email: customer.email, company: customer.company, address: customer.address, notes: customer.notes };`
      - Deep-clone snapshot via `structuredClone` (built-in; available in jsdom) BEFORE mutation: `const snapshotBefore = structuredClone(snapshot);`
      - Mutate the library record (simulating an edit): `customer.name = 'Alice Edited'; customer.address = '999 New Ave'; customer.notes = 'edited notes';`
      - Assert `expect(snapshot).toEqual(snapshotBefore)` — the snapshot is byte-identical to its pre-mutation deep-clone
      - Assert `expect(snapshot.name).toBe('Alice Pre-Edit')` (explicit field check for human-readable test output)
      - Assert `expect(snapshot.address).toBe('123 Old St')`
    - Test 2 ("deleting a Customer library record does not mutate a Sale.customer snapshot"):
      - Same fixture setup as Test 1
      - "Delete" the customer by reassigning the reference to undefined: `let customerRef: Customer | undefined = customer; customerRef = undefined;`
      - Assert `expect(snapshot).toEqual(snapshotBefore)` — snapshot survives the deletion
      - The point of Test 2 is to express the architectural rule for human readers (snapshots are independent of the library record), not to prove anything Test 1 didn't — JavaScript by-value primitives don't get mutated by deleting an unrelated object. Keep the test concise (no extra setup).
    - Test 3 ("library-only fields are never present on the Sale snapshot"):
      - Same fixture setup
      - Assert `expect('id' in snapshot).toBe(false)` (TypeScript-level safety — the snapshot type is `JobCustomer`, which has no `id`)
      - Assert `expect('createdAt' in snapshot).toBe(false)`
      - Assert `expect('lastUsedAt' in snapshot).toBe(false)`
  </behavior>
  <action>
    Create the Vitest test file at `src/utils/__tests__/byValueSnapshot.test.ts` (use the existing project test directory convention — if the project uses a flat layout like `src/utils/costCalc.test.ts`, place it at `src/utils/byValueSnapshot.test.ts` instead; check existing tests to confirm the convention before creating the file).

    Imports:
    - `import { describe, it, expect } from 'vitest';`
    - `import type { Customer, JobCustomer } from '../types';` (adjust the relative path based on the test file location)

    Structure:
    - `describe('Customer library by-value snapshot rule (CL-05)', () => { ... })`
    - Three `it(...)` blocks matching the behaviors above

    Notes:
    - Use `structuredClone` (native, available in jsdom — no extra import). If the project's test env reports it missing, fall back to `JSON.parse(JSON.stringify(snapshot))` (acceptable for plain data — `JobCustomer` has no Date, Function, or non-JSON-safe fields).
    - The test is PURE — no Dexie, no React Testing Library, no DOM. It's a TypeScript-level architectural lock and should run in <50ms.
    - This is the test locked by ROADMAP success criterion #4 and CONTEXT §"Claude's Discretion" bullet "Test coverage strategy."
  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/byValueSnapshot.test.ts 2>&1 | tail -30</automated>
    <automated>grep -c "Customer library by-value snapshot rule" src/utils/__tests__/byValueSnapshot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: the test file exists at the path above (or the flat `src/utils/byValueSnapshot.test.ts` if that matches project convention)
    - Source: it declares `describe('Customer library by-value snapshot rule (CL-05)', ...)` (exact string match — searchable in code review)
    - Source: it imports `type { Customer, JobCustomer }` from the appropriate relative path
    - Test: `npx vitest run <test-file-path>` exits 0 (all three tests PASS)
    - Test: the output shows exactly 3 passing tests in this file (no skipped, no todo)
    - Behavior: the test does NOT require a real Dexie database — runs purely against TypeScript fixtures
  </acceptance_criteria>
  <done>
    A passing Vitest unit test proves the by-value snapshot rule architecturally — editing or "deleting" a Customer record cannot mutate a Sale's customer field. The test is the executable artifact that locks ROADMAP success criterion #4 / CL-05.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with no TypeScript errors after all 4 tasks
- `npx vitest run src/utils/__tests__/byValueSnapshot.test.ts` (or the flat path) exits 0
- `grep -n "export interface Customer extends JobCustomer" src/types.ts` returns one line
- `grep -n "db.version(7).stores" src/db/database.ts` returns one line
- `grep -n "customers: EntityTable<Customer, 'id'>" src/db/database.ts` returns one line
- `grep -n "^export function useCustomers" src/hooks/useDatabase.ts` returns one line
- `grep -n "customersByEmail" src/hooks/useDatabase.ts` returns at least one line (in the function body + the return statement)
- No edits to `JobCustomer` (`grep -A 15 "^export interface JobCustomer" src/types.ts` matches the pre-plan content)
- No edits to `Sale.customer` (`grep "customer\?: JobCustomer" src/types.ts` returns the existing Sale.customer line at ~243)
</verification>

<success_criteria>
- Foundation types, schema, and hook are landed; downstream plans (02 — Customers tab, 03 — CSV importer, 04 — combobox picker) can be authored against the public surface defined in this plan's `<interfaces>` block.
- ROADMAP success criterion #4 (by-value snapshot audit) is locked by a passing unit test.
- Zero changes to `JobCustomer`, `Sale.customer`, the v6 schema block, or any existing store — the v7 migration is purely additive (D-02).
</success_criteria>

<output>
Create `.planning/phases/15.1-customer-library/15.1-01-SUMMARY.md` when done. Include:
- Final paths of the four files modified
- The exact Customer interface body that landed (for downstream plans to copy)
- The hook return-object shape that landed (so Plan 04 can wire the combobox correctly)
- Test name and result count
- Confirmation that `JobCustomer` and `Sale.customer` were not modified
- Note for Plan 05: `'customer-library'` entry NOT yet added to `src/features.ts` (Plan 05 owns the badge wiring and the ship-date entry)
</output>
