---
phase: 15.1-customer-library
plan: 05
subsystem: customer-library-ship
tags: [ship, new-badge, features-registry, uat]
dependency_graph:
  requires:
    - 15.1-01 (Customer interface + useCustomers hook + by-value snapshot test)
    - 15.1-02 (Customers tab UI + App.tsx tab wiring)
    - 15.1-03 (CSV bulk import + customerCsv parser)
    - 15.1-04 (Record Sale combobox + auto-link)
  provides:
    - `customer-library` entry in `featureReleases` registry (ship date 2026-05-22)
    - Absolute-overlay `<NewBadge>` on the Customers tab button
    - Phase-level UAT record validating all 5 ROADMAP success criteria end-to-end
tech_stack:
  added: []
  patterns:
    - "Absolute-overlay NewBadge inside flex tab row (project memory rule — never inline)"
    - "Single feature-entry-point badge — placed on the tab button only, not on every surface of the feature"
key_files:
  created:
    - .planning/todos/customer-csv-template-download.md (follow-up surfaced during UAT step 4)
  modified:
    - src/features.ts
    - src/App.tsx
decisions:
  - "D-16: Ship date written as `new Date('2026-05-22')` — matches today's date"
  - "D-16: Single NewBadge on the Customers tab button — no badges on combobox / Add customer / Import CSV (all surfaces of the same feature)"
  - "Absolute overlay `-top-1 -right-1 pointer-events-none` (project memory rule) — flex tab row layout byte-identical pre/post badge"
metrics:
  duration_minutes: ~5
  completed_date: 2026-05-22
requirements_completed: [CL-01, CL-02, CL-03, CL-04, CL-05]
---

# Phase 15.1 Plan 05: NewBadge Wiring + UAT Ship Summary

Phase 15.1 Customer Library is feature-complete and human-verified end-to-end. The Customers tab now sports the standard NEW badge (single entry-point pattern), the `featureReleases` registry has its ship-date entry, and a 10-step UAT confirmed all 5 ROADMAP success criteria across the four upstream plans.

## Tasks Completed

| Task | Name                                                | Commit  | Files            |
| ---- | --------------------------------------------------- | ------- | ---------------- |
| 1    | Add `customer-library` entry to `src/features.ts`   | 72f69ba | src/features.ts  |
| 2    | Add absolute-overlay NewBadge to Customers tab      | dedd121 | src/App.tsx      |
| 3    | End-to-end UAT (human-verify checkpoint)            | this    | (no source)      |

## Ship-Date Entry (verbatim)

```typescript
// src/features.ts
'customer-library': new Date('2026-05-22'),
```

Single entry. Placed before the trailing `// Add new features here with their release date` comment. None of the existing 6 entries touched.

## Badge JSX (verbatim)

```jsx
// src/App.tsx — inside the tabs.map(...) button render block
{tab.id === 'customers' && (
  <NewBadge feature="customer-library" className="absolute -top-1 -right-1 pointer-events-none" />
)}
```

Single badge. Conditional on `tab.id === 'customers'`. Inside the button (which already had `position: relative`). Absolute overlay — flex tab row layout unchanged. `pointer-events-none` lets clicks reach the button.

## UAT Outcome (10/10 PASS)

| # | Test | ROADMAP SC | Verdict | Notes |
|---|------|-----------|---------|-------|
| 1 | Dev server loads at :4173 | — | ✅ PASS | Vite 7.3.1 ready in ~270ms |
| 2 | Customers tab present, NEW badge upper-right, no sibling tab displacement, empty state renders | **CL-01** | ✅ PASS | — |
| 3 | Add/Edit/Delete + virtualized list + search (Name/Email/Company only, not Address per D-15) + sort (never-used first) | **CL-01** | ✅ PASS | — |
| 4 | CSV bulk import — skip vs update duplicates radio, D-11 merge rule (empty cells preserve existing) | **CL-03** | ✅ PASS | **Follow-up captured:** add a template-download link mirroring Asset Library's pattern. Filed at `.planning/todos/customer-csv-template-download.md` |
| 5 | Combobox picker fills 4 fields but NOT Notes (D-05), bumps lastUsedAt | **CL-04** | ✅ PASS | — |
| 6 | Auto-create on sale save (D-06) + silent email-match link without library overwrite (D-07) | **CL-04** | ✅ PASS | — |
| 7 | By-value snapshot — Sale.customer block unchanged after Library edit (manual + automated) | **CL-05** | ✅ PASS | Vitest `byValueSnapshot.test.ts` 3/3 pass; manual sale-snapshot independence confirmed |
| 8 | DevTools → IndexedDB → `customers` store visible, DB version `7` | **CL-02** | ✅ PASS | (trusted) |
| 9 | NewBadge dismissal gates — trust existing logic | — | ✅ PASS | (trusted — gated by NewBadge.tsx) |
| 10 | Final `npm run build` + `npx vitest run` | — | ✅ PASS | Build exit 0; 125/125 tests; main chunk 57.3 KB gz (under 300 KB ceiling) |

All 5 ROADMAP Phase 15.1 success criteria validated end-to-end. Phase is ready for the gsd-verifier pass and ship.

## Automated Gate Snapshot (post-merge of Plans 01–05)

- `npx tsc -b` → exit 0
- `npx vitest run` → 10 files / 125 tests passed
- `npm run build` → exit 0, main chunk 57.3 KB gz
- `grep -c "'customer-library':" src/features.ts` → 1
- `grep -c 'feature="customer-library"' src/App.tsx` → 1
- `grep -c "absolute -top-1 -right-1 pointer-events-none" src/App.tsx` → 1
- `grep -rc 'feature="customer-library"' src/` → 1 (only `src/App.tsx` — no stray badges)

## Follow-Up Items

1. **CSV template download for customers** (UAT step 4 feedback) — captured at `.planning/todos/customer-csv-template-download.md`. Small (~30-50 LOC). Mirror Asset Library's pattern: a template button above the dropzone that pulls a sample CSV via `downloadCsv(generateSampleCustomerCsv(), '3dcoster-customers-template.csv')`. Suitable for v1.2 polish or standalone ship.

## Notes for Verifier

- All `must_haves.truths` from the plan frontmatter are satisfied (single badge, absolute overlay, no sibling displacement, build green, UAT pass).
- All `must_haves.artifacts` checks pass: `customer-library` in features.ts, `feature="customer-library"` in App.tsx.
- The `key_links` cross-reference (App.tsx badge ↔ features.ts entry) is intact via the `feature="customer-library"` prop.
- The plan's instruction to write `15.1-VERIFICATION.md` is deferred to the orchestrator's `verify_phase_goal` step (gsd-verifier owns the phase-level VERIFICATION.md). This SUMMARY records the UAT outcome that verifier should read.
