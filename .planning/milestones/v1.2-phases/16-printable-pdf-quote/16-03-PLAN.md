---
phase: 16-printable-pdf-quote
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - src/pdf/generateQuotePdf.ts
  - src/pdf/generateQuotePdf.test.ts
autonomous: true
requirements: [PDF-01, PDF-02, PDF-03]
nyquist_compliant: true
must_haves:
  truths:
    - "generateQuotePdfBytes(params) returns a Uint8Array whose first 5 bytes are '%PDF-' — implements CONTEXT.md D-01 (B&W print-shop minimal aesthetic, no color)"
    - "PDF includes the 3DCoster header (top-left wordmark) and QUOTE + Q-NNNN block (top-right) — implements CONTEXT.md D-02 (wordmark-only header, no logo, no tagline, ~50pt height)"
    - "PDF sections render top→bottom in the order: Header → [meta LEFT + customer RIGHT] → line items → totals → Notes/Terms → footer — implements CONTEXT.md D-03 (standard invoice section order)"
    - "PDF includes Issue date and Valid-until date (issue + 30 days)"
    - "PDF includes customer block only when sale.customer has at least one non-empty field"
    - "PDF includes a collapsed single-row line items table (Description / Qty / Unit Price / Amount) — implements CONTEXT.md D-04 (collapsed single row, no cost breakdown exposed)"
    - "PDF totals: always Subtotal; Tax row only when taxRate > 0; Total — implements CONTEXT.md D-07 (hide tax row at 0%, keep Subtotal + Total) with inline rate label per D-06 (e.g. 'VAT (19%): €22.80')"
    - "PDF Notes section omits when PrintJob.notes is empty; Terms section omits when UserProfile.defaultTerms is empty; entire area omits when both empty — implements CONTEXT.md D-08 (layered Notes/Terms model)"
    - "PDF footer: 'Made with 3DCoster — 3dcoster.vercel.app'"
    - "Filename: Quote-{Q-NNNN}-{slug}.pdf when slug exists; Quote-{Q-NNNN}.pdf when no customer — implements CONTEXT.md D-11 (filename pattern; uses customerNameSlug from plan 02)"
    - "Generator throws if job.quoteNumber is undefined; quote-number assignment is the caller's responsibility"
    - "No static jspdf imports outside src/pdf/ — assert-no-static-jspdf.mjs continues to exit 0 (src/pdf/ is the allowed directory and is excluded from scanning)"
  artifacts:
    - path: "src/pdf/generateQuotePdf.ts"
      provides: "Main PDF generator (dynamic-import target), web + Tauri save branches"
      exports: ["generateQuotePdf", "generateQuotePdfBytes", "QuotePdfParams"]
    - path: "src/pdf/generateQuotePdf.test.ts"
      provides: "Real integration tests for PDF byte output (no longer it.todo)"
      contains: "%PDF-"
  key_links:
    - from: "src/pdf/generateQuotePdf.ts"
      to: "src/pdf/notoSansBase64.ts"
      via: "named import (regular + bold base64 strings)"
      pattern: "from './notoSansBase64'"
    - from: "src/pdf/generateQuotePdf.ts"
      to: "src/utils/format.ts"
      via: "named imports of formatQuoteNumber + customerNameSlug"
      pattern: "from '../utils/format'"
    - from: "src/pdf/generateQuotePdf.ts"
      to: "src/utils/taxResolution.ts"
      via: "named import of taxLabelFor"
      pattern: "taxLabelFor"
    - from: "src/pdf/generateQuotePdf.ts"
      to: "@tauri-apps/plugin-dialog + @tauri-apps/plugin-fs"
      via: "DYNAMIC import inside Tauri branch"
      pattern: "await import"
---

<objective>
Wave 2 ships the PDF generator itself — the single largest piece of feature code in Phase 16. After this plan, plan 04 can wire the "Generate PDF" buttons in CostCalculator + JobsManager with a trivial dynamic import + call + try/catch.

Purpose: Concentrate all jsPDF + autotable code into ONE module with a clean exported interface. The button handlers in plan 04 stay simple. The generator owns layout, font registration, totals math, customer block omission, Notes/Terms layered model, web/Tauri save branching, and filename assembly.

Output: One generator module, one passing test file (flipped from todos). Test exits in under 60s under vitest. No regressions to Wave 0 CI gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-printable-pdf-quote/16-CONTEXT.md
@.planning/phases/16-printable-pdf-quote/16-RESEARCH.md
@.planning/phases/16-printable-pdf-quote/16-PATTERNS.md
@.planning/phases/16-printable-pdf-quote/16-VALIDATION.md
@.planning/phases/16-printable-pdf-quote/16-01-SUMMARY.md
@.planning/phases/16-printable-pdf-quote/16-02-SUMMARY.md
@.planning/phases/13-tax-model-ui-sweep/13-SUMMARY.md
@.planning/phases/14-customer-details-etsy-helper/14-SUMMARY.md
@.planning/phases/15.1-customer-library/15.1-SUMMARY.md

<interfaces>
<!-- jsPDF 4.x + autotable 5.x calling convention (RESEARCH.md Pattern 2). DO NOT use doc.autoTable() — that is the v3 pattern. Use autoTable(doc, options). -->

jspdf 4.2.1 (TypeScript surface — verify post-install in node_modules/jspdf/types/index.d.ts):
- `new jsPDF({ orientation: 'portrait', unit: 'pt', format: 'a4' })`
- `doc.text(text, x, y, options?)`, `doc.setFont(fontName, fontStyle?)`, `doc.setFontSize(n)`
- `doc.setLineWidth(w)`, `doc.line(x1,y1,x2,y2)`, `doc.setTextColor(r,g,b)`
- `doc.addFileToVFS(filename, base64String)`, `doc.addFont(filename, id, style)`
- `doc.save(filename)`, `doc.output('arraybuffer')`, `doc.output('datauristring')`
- `doc.internal.pageSize.getWidth()`, `getHeight()`
- `doc.splitTextToSize(text, maxWidth)` returns string[]

jspdf-autotable 5.0.8 (named function export):
- `import { autoTable } from 'jspdf-autotable'`
- `autoTable(doc, { head, body, startY, styles, headStyles, theme })`
- After call: `(doc as any).lastAutoTable.finalY` is the Y position after the table

src/types.ts contracts the generator reads:
- PrintJob: id, name, sellingPrice, copiesSold, taxRate?, taxAmount?, notes?, quoteNumber?
- Sale: customer?: JobCustomer, customerName? (legacy fallback)
- JobCustomer: name?, email?, company?, address?, notes? (notes is library-bound; PDF ignores it)
- UserProfile: currency, name?, defaultTerms?, nextQuoteNumber?, address?.country?

src/utils/currency.ts: formatCurrency(amount, currency) returns formatted string

src/utils/format.ts (plan 02): formatQuoteNumber(n), customerNameSlug(name?)

src/utils/taxResolution.ts (plan 02): taxLabelFor(countryCode?) returns 'VAT' | 'GST' | 'IVA' | 'Sales Tax' | 'Tax'

src/globals.d.ts: declares `const __IS_TAURI__: boolean`

Tauri 2 plugin APIs (RESEARCH.md Pattern 8):
- `save({ defaultPath, filters: [{ name, extensions }] })` returns Promise of string path OR null on cancel
- `writeFile(path, Uint8Array)` returns Promise of void

CONTEXT.md D-04 line-item shape: single row, "Custom 3D print — {jobTitle} (qty: N)", qty defaults to `copiesSold || 1`, Subtotal = qty * sellingPrice

CONTEXT.md D-07 tax row: hide when taxRate === 0 or taxAmount === 0; format as "{label} ({rate}%): {amount}"

CONTEXT.md D-08 Notes/Terms: both subsections optional; whitespace-only treated as empty; whole area omitted if both empty

CONTEXT.md D-11 filename: Quote-{Q-NNNN}-{slug}.pdf if slug, else Quote-{Q-NNNN}.pdf

Customer source per Phase 14 D-21: PDF reads from Sale.customer (with sale.customerName legacy fallback), NOT PrintJob.customer. If no sale passed (CostCalculator-side generation), customer block is omitted.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author src/pdf/generateQuotePdf.ts (full PDF generator with web + Tauri save branches)</name>
  <files>
    src/pdf/generateQuotePdf.ts
  </files>
  <read_first>
    src/pdf/notoSansBase64.ts,
    src/utils/currency.ts,
    src/utils/format.ts,
    src/utils/taxResolution.ts,
    src/types.ts,
    src/App.tsx,
    .planning/phases/16-printable-pdf-quote/16-RESEARCH.md,
    .planning/phases/16-printable-pdf-quote/16-CONTEXT.md,
    .planning/phases/16-printable-pdf-quote/16-PATTERNS.md
  </read_first>
  <behavior>
    - generateQuotePdfBytes(params) returns a Uint8Array whose first 5 bytes decode to "%PDF-"
    - Generator throws Error when params.job.quoteNumber === undefined with message: "generateQuotePdf: job.quoteNumber must be assigned before calling — assign UserProfile.nextQuoteNumber ?? 1 and persist before generating"
    - When sale.customer.name === "Alice Test", PDF byte stream contains "Alice Test"
    - When sale is undefined OR sale.customer is undefined AND sale.customerName is undefined, PDF does NOT render a customer block; the right column under the header is blank
    - When job.taxRate === 0 OR job.taxAmount === 0 OR both undefined, PDF text stream does NOT contain "Tax (" or "VAT (" or "GST ("
    - When job.taxRate === 19 AND job.taxAmount === 22.8 AND userProfile.address.country === "DE", PDF text stream contains "VAT (19%)"
    - When job.notes === undefined AND userProfile.defaultTerms === undefined, PDF does NOT contain "Notes" or "Terms" section headings
    - When job.notes === "PLA gloss" and defaultTerms undefined, PDF contains "Notes" heading and "PLA gloss"
    - When defaultTerms === "Payment 14d" and notes undefined, PDF contains "Terms" heading and "Payment 14d"
    - PDF always contains "Made with 3DCoster" and "3dcoster.vercel.app"
    - PDF always contains the formatted quote number string (e.g. "Q-0042")
  </behavior>
  <action>
    Create `src/pdf/generateQuotePdf.ts` — the main PDF generator module. Utility module, no React, no Dexie, no IO except the final save call. Module-level constants for layout dimensions.

    Module header comment: "PDF quote generator — dynamic-import target. No static jspdf import in src/ outside this file. No React, no Dexie. Mirrors layout decisions in CONTEXT.md D-01..D-12."

    Static imports (these are inside the lazy chunk, so static within-chunk is correct):
    - jsPDF named import from 'jspdf'
    - autoTable named import from 'jspdf-autotable' (NOT side-effect import; v5 named-function pattern)
    - Type-only imports from '../types': PrintJob, Sale, JobCustomer, UserProfile, Currency
    - formatCurrency from '../utils/currency'
    - formatQuoteNumber, customerNameSlug from '../utils/format'
    - taxLabelFor from '../utils/taxResolution'
    - notoSansRegularBase64, notoSansBoldBase64 from './notoSansBase64'

    Tauri imports MUST stay DYNAMIC even within this module (so web builds don't bundle Tauri plugins): use `await import('@tauri-apps/plugin-dialog')` and `await import('@tauri-apps/plugin-fs')` inside the __IS_TAURI__ branch.

    Exported types:
    - `QuotePdfParams` interface: job (PrintJob), userProfile (UserProfile), sale (optional Sale)
      - sale is optional — present when generating from JobsManager (a recorded sale exists); absent when generating from CostCalculator (no customer block to render)
      - job.quoteNumber MUST be assigned before calling — generator throws if undefined

    Exported functions:
    - `generateQuotePdf(params: QuotePdfParams): Promise<void>` — full flow: build doc, save to disk (web or Tauri)
    - `generateQuotePdfBytes(params: QuotePdfParams): Promise<Uint8Array>` — build doc, return bytes (for tests AND for Tauri's writeFile path)

    Module-level state: `let fontsLoaded = false` — guard for ensureFontsLoaded

    Internal helpers (NOT exported):
    - `ensureFontsLoaded(doc)`: per RESEARCH.md Pattern 3. Calls addFileToVFS for both regular + bold, then addFont with NotoSans id for normal + bold styles. Sets fontsLoaded = true. No-op if already loaded.
    - `renderHeader(doc, params)`: returns next Y position after header. Renders 3DCoster wordmark (bold 18pt at x=40, y=50), QUOTE label + Q-NNNN stacked top-right (right-aligned at x=pageWidth-40), thin 0.5pt horizontal rule below at y=65. Total header height about 50pt — next Y = 85.
    - `renderMetaAndCustomer(doc, params, startY)`: two-column row. LEFT column at x=40: "Quote #: Q-NNNN", "Issue date: YYYY-MM-DD" (today), "Valid until: YYYY-MM-DD" (today + 30 days using new Date(Date.now() + 30*24*60*60*1000)). RIGHT column at x=pageWidth/2: customer block (Bill To: heading bold, then Name, Email, Company, Address lines) ONLY IF sale exists AND (sale.customer.name OR sale.customer.email OR sale.customer.company OR sale.customer.address OR sale.customerName) — at least one non-empty field. If empty, render LEFT column only and skip the right side. Returns max of left/right column heights.
    - `renderLineItems(doc, params, startY)`: calls autoTable(doc, {...}) with single row body per D-04. Columns: ["Description", "Qty", "Unit Price", "Amount"]. Body row: ["Custom 3D print — " + job.name, String(qty), formatCurrency(sellingPrice, currency), formatCurrency(qty * sellingPrice, currency)]. qty = job.copiesSold || 1. theme: 'plain', styles: { font: 'NotoSans', fontSize: 10 }, headStyles: { fillColor: [255,255,255], textColor: [0,0,0], fontStyle: 'bold' }. After the call, return (doc as any).lastAutoTable.finalY as next Y position.
    - `renderTotals(doc, params, startY)`: right-aligned at x = pageWidth - 40. Always render Subtotal line (qty * sellingPrice). Render Tax row ONLY when taxRate > 0 AND taxAmount > 0 (both must be truthy and > 0). Tax label = taxLabelFor(userProfile.address?.country); Tax row text = `${label} (${rate}%): ${formatCurrency(taxAmount, currency)}`. Always render Total (bold; Subtotal + (taxAmount || 0)). Returns next Y.
    - `renderNotesAndTerms(doc, params, startY)`: Trim whitespace before treating as present. const notesStr = job.notes?.trim(); const termsStr = userProfile.defaultTerms?.trim(); if both falsy → return startY unchanged (no gap, no headings). Otherwise: if notesStr → render "Notes" heading (bold 11pt) + body wrapped via doc.splitTextToSize(notesStr, contentWidth) (normal 10pt). Then if termsStr → same pattern with "Terms" heading + termsStr body. Returns next Y.
    - `renderFooter(doc)`: fixed at y = pageHeight - 30. Text "Made with 3DCoster — 3dcoster.vercel.app". Left-aligned at x=40, 9pt size, setTextColor(120, 120, 120) for subtle gray. Reset setTextColor(0,0,0) after to avoid leaking state.
    - `buildFilename(params)`: const slug = customerNameSlug(params.sale?.customer?.name ?? params.sale?.customer?.company); return slug ? `Quote-${formatQuoteNumber(quoteNumber)}-${slug}.pdf` : `Quote-${formatQuoteNumber(quoteNumber)}.pdf`.

    Private builder: `_buildDoc(params): jsPDF` — does the throw-if-no-quoteNumber check, creates new jsPDF with portrait+pt+a4, ensureFontsLoaded, setFont('NotoSans','normal'), then threads Y position through renderHeader → renderMetaAndCustomer → renderLineItems → renderTotals → renderNotesAndTerms → renderFooter. Returns doc.

    `generateQuotePdfBytes(params)`: build doc via _buildDoc, return new Uint8Array(doc.output('arraybuffer')).

    `generateQuotePdf(params)`:
    1. const doc = _buildDoc(params)
    2. const filename = buildFilename(params)
    3. if (!__IS_TAURI__) { doc.save(filename); return; }
    4. const { save } = await import('@tauri-apps/plugin-dialog')
    5. const { writeFile } = await import('@tauri-apps/plugin-fs')
    6. const savePath = await save({ defaultPath: filename, filters: [{ name: 'PDF', extensions: ['pdf'] }] })
    7. if (!savePath) return // user cancelled — silent return per Tauri docs
    8. const buffer = doc.output('arraybuffer')
    9. await writeFile(savePath, new Uint8Array(buffer))

    Per ROADMAP success criterion #5 and D-10: font ID is 'NotoSans'. After addFont returns, setFont('NotoSans','normal') and setFont('NotoSans','bold') MUST work for all subsequent text + autotable calls. Bold is used in wordmark, section headings, and Total row.

    Per D-01: no setFillColor or setDrawColor except black-on-white. No accent colors. Header rule is black 0.5pt. Section headings use bold weight (not color). Footer uses gray (120,120,120) — acceptable monochrome variant.

    Per D-04: ONE row in autotable body. Do NOT iterate job.filaments. Do NOT expose individual cost components.

    Race-condition pattern (RESEARCH.md Pattern 12 / Pitfall 6): generator throws if job.quoteNumber undefined. Caller (plan 04) handles quote-number assignment + Dexie persistence + isGenerating button-disable before invoking the generator. This keeps the generator pure and side-effect-free apart from the final save() call.

    Architectural responsibility: PDF generator does NOT call Dexie. Does NOT mutate params. Only button handlers (plan 04) own Dexie writes.

    Dynamic Tauri imports keep these plugins off the WEB build chunk entirely. Web Vite users get a chunk with only jspdf + autotable + fonts. Tauri builds load the Tauri plugin chunks only when generateQuotePdf actually runs.

    CI gate note: `src/pdf/generateQuotePdf.ts` is the ONE file that legitimately imports `from 'jspdf'` and `from 'jspdf-autotable'` statically. `scripts/assert-no-static-jspdf.mjs` (plan 01 Task 2) EXCLUDES the `src/pdf/` directory from scanning — so this file's static imports are allowed and do NOT trip the gate. After this task lands, re-run `node scripts/assert-no-static-jspdf.mjs` to confirm the gate still exits 0.
  </action>
  <verify>
    <automated>tsc -b 2&gt;&amp;1 | (grep -E "error TS" || true) | head -5 ; node scripts/assert-no-static-jspdf.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `src/pdf/generateQuotePdf.ts` exists
    - Exports three names: `generateQuotePdf`, `generateQuotePdfBytes`, `QuotePdfParams`
    - Static imports include `from 'jspdf'` (named) AND `from 'jspdf-autotable'` (named) — both within `src/pdf/` (allowed; this directory is excluded from the assert-no-static-jspdf.mjs scan per plan 01 Task 2)
    - File contains `await import('@tauri-apps/plugin-dialog')` AND `await import('@tauri-apps/plugin-fs')` — Tauri plugins stay dynamic
    - File contains `throw new Error` with the documented quote-number-missing message
    - File contains string literals: `'Made with 3DCoster'`, `'3dcoster.vercel.app'`, `'Custom 3D print —'`, `'Quote #:'`, `'Issue date:'`, `'Valid until:'`, `'Subtotal'`, `'Total'`, `'Bill To:'`, `'Notes'`, `'Terms'`
    - `tsc -b` exits 0 (no type errors)
    - `node scripts/assert-no-static-jspdf.mjs` still exits 0 — `src/pdf/generateQuotePdf.ts` lives in the EXCLUDED `src/pdf/` directory and so is intentionally skipped by the gate. The gate's directory-exclusion (added in plan 01 Task 2) is the mechanism that keeps the build green; verify by reading the script and confirming the `if (fullPath.includes('/src/pdf/') ...) continue;` guard is present.
    - No file OUTSIDE `src/pdf/` contains a `from 'jspdf'` or `from 'jspdf-autotable'` static import (plan 04 wires consumers via `await import('../pdf/generateQuotePdf')`)
  </acceptance_criteria>
  <done>The PDF generator is implemented with all section renderers, web + Tauri save branches, font registration guard, and throws-on-missing-quoteNumber contract. TypeScript compiles. The CI static-import gate still passes because (a) only this file imports jspdf statically, and (b) the gate excludes `src/pdf/` from scanning entirely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip Wave 0 generateQuotePdf.test.ts todos to real integration tests</name>
  <files>
    src/pdf/generateQuotePdf.test.ts
  </files>
  <read_first>
    src/pdf/generateQuotePdf.ts,
    src/pdf/generateQuotePdf.test.ts,
    src/utils/costCalc.test.ts,
    .planning/phases/16-printable-pdf-quote/16-RESEARCH.md,
    .planning/phases/16-printable-pdf-quote/16-VALIDATION.md
  </read_first>
  <behavior>
    All "behavior" bullets from Task 1 — flipped from `it.todo` to real `it(name, async () => expect(...))` assertions.
  </behavior>
  <action>
    Read the Wave 0 scaffold and convert each `it.todo` to a real test that runs `generateQuotePdfBytes(params)` and asserts the documented behavior. Vitest config already has `environment: 'jsdom'` so jsPDF can run in tests (RESEARCH.md confirms jsdom is the test env).

    Fixture builders (top of file):
    ```
    function makeJob(overrides: Partial<PrintJob> = {}): PrintJob { return { ...required-fields..., quoteNumber: 42, ...overrides } as PrintJob; }
    function makeProfile(overrides: Partial<UserProfile> = {}): UserProfile { return { currency: 'EUR' as Currency, laborHourlyRate: 30, ...overrides }; }
    function makeSale(customer?: JobCustomer): Sale { return { ...required-fields..., customer } as Sale; }
    ```

    Convert all `it.todo` placeholders to real `it(...)` blocks. Decode the PDF bytes via:
    ```
    function pdfText(bytes: Uint8Array): string {
      // jsPDF writes ASCII text into the stream; for simple substring matches we can
      // String.fromCharCode it. For finer matching, use doc.output('datauristring') in the test fixture.
      let s = '';
      for (let i = 0; i < bytes.length; i++) s += String.fromCharCode(bytes[i]);
      return s;
    }
    ```

    Test list (minimum):
    1. `it('returns Uint8Array starting with %PDF- magic bytes', ...)` — `expect(pdfText(bytes).slice(0,5)).toBe('%PDF-')`
    2. `it('throws when job.quoteNumber is undefined', ...)` — `await expect(generateQuotePdfBytes({ job: makeJob({ quoteNumber: undefined as any }), userProfile: makeProfile() })).rejects.toThrow(/quoteNumber/)`
    3. `it('contains the formatted quote number Q-NNNN', ...)` — assert pdfText contains 'Q-0042' (or similar; the bytes may compress strings — if so, switch to doc.output('datauristring') decoding via atob and check the decoded text)
    4. `it('includes customer name when sale.customer.name is set', ...)` — sale with customer name 'Alice Test'; assert pdfText contains 'Alice'
    5. `it('omits customer block entirely when sale is undefined', ...)` — sale omitted; assert pdfText does NOT contain 'Bill To'
    6. `it('omits customer block when sale.customer has no non-empty fields', ...)` — sale with empty customer object; assert no 'Bill To'
    7. `it('hides Tax row when taxRate is 0', ...)` — job.taxRate=0, taxAmount=0; assert no 'Tax (' or 'VAT ('
    8. `it('hides Tax row when taxRate and taxAmount are undefined', ...)` — neither set; assert no 'Tax ('
    9. `it('shows VAT (19%) when country is DE and taxRate is 19', ...)` — userProfile.address.country='DE', job.taxRate=19, taxAmount=22.8; assert contains 'VAT (19%)'
    10. `it('shows GST when country is AU', ...)` — country='AU', taxRate=10; assert contains 'GST ('
    11. `it('shows generic Tax label when country is unset and taxRate > 0', ...)` — no country, taxRate=5; assert contains 'Tax ('
    12. `it('omits Notes and Terms sections when both empty', ...)` — neither notes nor defaultTerms; assert no 'Notes' or 'Terms' headings
    13. `it('renders Notes section when PrintJob.notes is non-empty', ...)` — notes='PLA gloss'; assert contains 'Notes' and 'PLA gloss'
    14. `it('renders Terms section when UserProfile.defaultTerms is non-empty', ...)` — defaultTerms='Payment 14d'; assert contains 'Terms' and 'Payment 14d'
    15. `it('renders both Notes and Terms when both non-empty', ...)` — both set; assert both present
    16. `it('includes Made with 3DCoster footer', ...)` — assert contains 'Made with 3DCoster'
    17. `it('includes 3dcoster.vercel.app in footer', ...)` — assert contains '3dcoster.vercel.app'
    18. `it('includes Custom 3D print — {jobName} line item', ...)` — job.name='Phone Stand'; assert contains 'Phone Stand'

    Note on string matching: jsPDF text streams may be encoded with custom fonts (NotoSans is registered via VFS). In practice the strings still appear searchable in the raw byte stream — but if a test reliably fails, switch to `doc.output('datauristring')` and `atob(uri.split(',')[1])`, then string-search that. Either approach is acceptable as long as the assertions reflect real PDF content.

    Tests must run in &lt; 60 seconds total (Wave 0's 25s target). The font registration is the slowest step; module-level fontsLoaded guard ensures only the first test pays that cost.

    Use whitespace-trim semantics for the "omit Notes/Terms" tests: pass `notes: '   '` (whitespace-only) and assert it's treated as empty.
  </action>
  <verify>
    <automated>vitest run src/pdf/generateQuotePdf.test.ts 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/pdf/generateQuotePdf.test.ts` contains real `it(...)` assertions for every behavior bullet listed in Task 1
    - At least 15 passing tests
    - Zero `it.todo` remaining
    - `vitest run src/pdf/generateQuotePdf.test.ts` exits 0
    - Total run time under 60 seconds
    - `tsc -b` exits 0
  </acceptance_criteria>
  <done>The PDF generator has real integration tests covering quote number, customer block, tax row, Notes/Terms, footer, and line items. No skipped or todo tests. Test suite runs in well under the 25s feedback-latency target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-controlled text (job.name, job.notes, customer fields, defaultTerms) | All rendered via doc.text() into PDF |
| jspdf VFS (in-memory) | Font base64 strings registered via addFileToVFS |
| Tauri save dialog → fs writeFile | User-picked path receives PDF bytes |
| __IS_TAURI__ build-time constant | Selects web vs Tauri save branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Tampering | User-controlled text rendered in PDF (HTML injection class) | mitigate | jsPDF 4.2.1 (pinned in plan 01) includes the "HTML Injection in output methods" security patch. Plan 03 uses doc.text() and autoTable() with string values only — NEVER doc.html(). PDF Object Injection in annotation color is also patched in 4.2.1; this plan never sets annotation color. |
| T-16-08 | Tampering | Static jspdf import accidentally introduced outside src/pdf/ | mitigate | `scripts/assert-no-static-jspdf.mjs` CI gate (plan 01) excludes `src/pdf/` (the allowed consumer) and fails the build if any file outside that directory imports jspdf statically. |
| T-16-09 | Tampering | Tauri writeFile writing to attacker-influenced path | mitigate | save() always shows a user-controlled dialog; user picks the path. Capability `fs:scope` restricts to `$DOWNLOAD/*` for non-dialog writes (defense-in-depth). |
| T-16-10 | Information Disclosure | Cost breakdown leaking to buyer-facing PDF | mitigate | D-04 collapsed single-row pattern enforced in code: line items table has exactly one row containing only the customer-facing description + qty + price + amount. No filament rows, no labor, no depreciation, no margin. Test 18 asserts the single-row content. |
| T-16-11 | Repudiation | Customer disputes quote contents after PDF was generated | accept | PDF includes Issue date, Valid-until date, and Q-NNNN — sufficient audit trail for a free-tier app. White-label / e-signature is paid-tier (PROJECT.md locked). |
</threat_model>

<verification>
After both tasks complete:
1. `vitest run src/pdf/generateQuotePdf.test.ts` exits 0 with 15+ passing tests
2. `vitest run` (full suite) exits 0 — no regressions
3. `tsc -b` exits 0
4. `node scripts/assert-no-static-jspdf.mjs` exits 0 (the gate excludes `src/pdf/` from scanning, so `src/pdf/generateQuotePdf.ts` is intentionally ignored even though it contains static `from 'jspdf'`; verify by reading the script and confirming the directory exclusion guard)
5. `grep -c "doc.html" src/pdf/generateQuotePdf.ts` returns 0 (NO HTML rendering — security-relevant)
6. `grep -c "doc.autoTable" src/pdf/generateQuotePdf.ts` returns 0 (must use named `autoTable(doc, ...)`, NOT the v3 method form)
</verification>

<success_criteria>
- One PDF generator module exists with exported `generateQuotePdf`, `generateQuotePdfBytes`, `QuotePdfParams`
- 15+ real integration tests pass
- Web path (`doc.save`) and Tauri path (dynamic-import dialog + fs) both wired
- Font registration uses module-level `fontsLoaded` guard (RESEARCH.md Pattern 3)
- Customer block omitted when no sale or empty customer
- Tax row hidden at 0%, region-aware label
- Notes/Terms layered model honored (each subsection independently omitted; whole area omitted if both empty)
- Filename pattern matches D-11
- No `doc.html()` calls (security-relevant)
- No `doc.autoTable()` calls (v3 anti-pattern)
- CI static-jspdf gate continues to pass (because `src/pdf/` is excluded from the scan)
</success_criteria>

<output>
Create `.planning/phases/16-printable-pdf-quote/16-03-SUMMARY.md` when done. Document: actual jsPDF/autotable API quirks encountered (if any differ from RESEARCH.md), final exported function signatures, total LOC of generator + test files, test execution time.
</output>
</output>
