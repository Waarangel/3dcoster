---
phase: 16-printable-pdf-quote
plan: "07"
type: execute
wave: 0
depends_on: ["16-04"]
gap_closure: true
gap_ids: [B]
decisions: [D-14]
files_modified:
  - src/components/JobsManager.tsx
requirements: [PDF-01]
autonomous: true
nyquist_compliant: true
must_haves:
  truths:
    - "Per D-14, the JobsManager accordion action row renders a `Print Quote` button (not `Generate PDF`) — at least 1 occurrence of `Print Quote` in JobsManager.tsx (aria-label / title / comment text also counted, per NIT I-09 relaxation), and zero occurrences of `Generate PDF` anywhere in the file."
    - "Per D-14, the Print Quote button uses the project's secondary `<Button variant=\"secondary\" btnSize=\"sm\">` primitive — affording the same visual weight as Edit / Delete next to it (not ghost-styled text-link)."
    - "Per D-14, the NewBadge for `pdf-quote` remains as an absolute overlay anchored to the button (`className=\"absolute -top-1 -right-1\"`) — placement and feature key unchanged; CLAUDE.md NEW Badge rule (overlay only, never inline) is honored."
    - "Disabled-state tooltip on the Print Quote button still reads `Set a selling price first` when `sellingPrice <= 0`."
    - "The button's click handler is unchanged in this plan — it still calls `handleGeneratePdf` with the silent most-recent-sale-customer fallback. Plan 16-10 replaces that handler with the PrintQuoteModal opener (D-18). This plan is rename + styling ONLY; behavior change is deferred to 16-10."
  artifacts:
    - path: "src/components/JobsManager.tsx"
      provides: "Print Quote button (renamed, secondary variant) with NewBadge overlay preserved"
---

<objective>
Per gap B (D-14), the JobsManager accordion's existing Generate PDF button must be renamed to **Print Quote** and re-styled with the project's secondary `<Button>` variant. The user's UAT verdict was that the current ghost styling fails the affordance test — the button doesn't look like a button. The rename also aligns terminology with the Quote workflow that lands in plans 16-09..16-12.

Purpose: Make the button look like a button (same affordance as adjacent Edit / Delete) and use language that matches the quote mental model.
Output: JobsManager.tsx with the renamed Print Quote button rendered via `<Button variant="secondary" btnSize="sm">…Print Quote</Button>` with NewBadge absolute-overlay placement preserved.

**Scope guard:** This plan does NOT change the click handler. The button still wires to the existing `handleGeneratePdf` (silent most-recent-sale fallback). Plan 16-10 owns the handler swap to open `PrintQuoteModal`. Splitting rename-from-behavior keeps blast radius small per CLAUDE.md ("act like a senior developer, go step by step").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-printable-pdf-quote/16-CONTEXT.md
@.planning/phases/16-printable-pdf-quote/16-VERIFICATION.md
@.planning/phases/16-printable-pdf-quote/16-04-SUMMARY.md
@src/components/JobsManager.tsx
@src/components/ui/Button.tsx

<interfaces>
<!-- Existing Generate PDF button site in JobsManager (per 16-04 summary): -->
<!--   - Lives in the expanded accordion action row, between Record Sale and Edit -->
<!--   - Wraps the button in a `<div className="relative">` for NewBadge overlay -->
<!--   - Uses `e.stopPropagation()` consistent with Edit/Delete -->
<!--   - Currently uses a Button variant that the UAT user described as -->
<!--     "ghost styling — doesn't look like a button" — confirm by reading the JSX -->

<!-- Existing NewBadge pattern in JobsManager.tsx line ~194 (per VERIFICATION.md) -->
<!-- + CostCalculator.tsx line ~69: -->
<!--   <NewBadge feature="pdf-quote" className="absolute -top-1 -right-1" /> -->

<!-- The shared Button primitive accepts: variant="secondary" | "primary" | "success" | "danger" | "ghost" -->
<!-- and btnSize="sm" | "md" | "lg". Per CLAUDE.md the secondary variant is the canonical "looks like a button" choice for non-CTA actions next to Edit/Delete. -->

<!-- handleGeneratePdf logic (lines ~770-810 in JobsManager.tsx per 16-04 summary): -->
<!--   - Reads `userProfile` from props -->
<!--   - Reads `mostRecentSale` for this job (silent customer fallback) -->
<!--   - Calls onPersistQuoteNumber if quoteNumber unassigned -->
<!--   - Calls `await import('../pdf/generateQuotePdf')` -->
<!-- DO NOT touch this handler in this plan. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Generate PDF → Print Quote and switch to secondary Button variant</name>
  <files>src/components/JobsManager.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx (focus: the expanded-accordion action row containing Record Sale / Generate PDF / Edit / Delete buttons; the NewBadge `pdf-quote` JSX line; the handleGeneratePdf handler — do not modify it)
    - src/components/ui/Button.tsx (confirm the `variant` prop accepts `"secondary"` and the visual treatment matches what Edit/Delete use)
    - .planning/phases/16-printable-pdf-quote/16-04-SUMMARY.md (confirms button is "in the expanded accordion action row, after Record Sale, before Edit")
    - .planning/phases/16-printable-pdf-quote/16-VERIFICATION.md (gap B verbatim user report)
  </read_first>
  <action>
    In src/components/JobsManager.tsx — surgical edit, button rename + variant change only:

    1. **Locate the Generate PDF button JSX** in the expanded-accordion action row (per 16-04 SUMMARY: "after Record Sale, before Edit"). Confirm by reading the surrounding JSX — it should be next to buttons for Record Sale / Edit / Delete and wrapped in a `<div className="relative">`.

    2. **Change the visible label** from `Generate PDF` (or whatever string is currently between the `<Button>` tags) to **`Print Quote`**. Use plain text — no icon, no emoji. The visible label string + any aria-label / title prop that previously read "Generate PDF" should ALL be updated; the goal is zero remaining `Generate PDF` occurrences in this file.

    3. **Set the Button variant** to `variant="secondary"`. If the current variant is already `"secondary"`, leave it; if it is `"ghost"` or omitted (defaulting to ghost-like), change to `"secondary"`. The visual goal per gap B is to match the affordance weight of Edit / Delete buttons in the same row.

    4. **Keep `btnSize="sm"`** — Edit/Delete use sm and the row layout depends on consistent sizing.

    5. **Preserve the disabled-state tooltip:** the `title` attribute (when disabled) must remain `Set a selling price first`. Confirm the gating condition still reads `disabled={generatingJobIds.has(job.id) || job.sellingPrice <= 0}` (per 16-04 the existing condition was `generatingJobIds.has(job.id)` only — extend with `|| job.sellingPrice <= 0` if it is not already there; per D-14 the price gate stays).

    6. **Preserve the NewBadge overlay** — `<NewBadge feature="pdf-quote" className="absolute -top-1 -right-1" />` must stay inside the `<div className="relative">` wrapper, no inline placement. Per CLAUDE.md (user memory): the badge is decoration that overlays — never inline, never push siblings, never reflow the row layout. Verify the wrapper is `relative` and the badge has `absolute -top-1 -right-1`.

    7. **Preserve `e.stopPropagation()`** on the button's onClick — accordion row click handling depends on it (per 16-04 summary).

    8. **DO NOT change the click handler reference** — the button still calls the existing `handleGeneratePdf` (the silent most-recent-sale path). Plan 16-10 swaps the handler to open `PrintQuoteModal`.

    9. **DO NOT remove or rename `handleGeneratePdf`, `generatingJobIds`, or `onPersistQuoteNumber`** — they are still active. Plan 16-10 deprecates the silent fallback by replacing the handler body.

    Project-rule reminder (CLAUDE.md `src/components/`): no raw `<button>` — use the shared `<Button>` primitive. The existing JSX already does this; preserve that pattern.
  </action>
  <acceptance_criteria>
    - `grep -c "Print Quote" src/components/JobsManager.tsx` returns a count of `1` OR more (`>= 1`) — at least one occurrence required. Per NIT I-09 relaxation: aria-label, title, and comment text matches all count; we no longer require an exact-1 match because the label may legitimately appear in aria-label + visible text.
    - `! grep -q "Generate PDF" src/components/JobsManager.tsx` succeeds — ZERO occurrences of the old label in the file (no leftover label, comment, aria-label, or title using the old name).
    - `grep -nE 'variant="secondary".*Print Quote|Print Quote.*variant="secondary"' src/components/JobsManager.tsx` returns at least one match (JSX form of the button with the variant on the same or adjacent line).
    - `grep -c 'NewBadge feature="pdf-quote"' src/components/JobsManager.tsx` returns `1` — badge preserved.
    - `grep -c 'className="absolute -top-1 -right-1"' src/components/JobsManager.tsx` returns at least `1` for the badge (per CLAUDE.md NEW Badge overlay rule).
    - `grep -c "Set a selling price first" src/components/JobsManager.tsx` returns `1` — disabled tooltip preserved.
    - `npx tsc -b` exits `0`.
    - `npx vitest run` exits `0` — no test regressions.
    - `node scripts/lint-no-raw-html.mjs` exits `0` — no raw `<button>` introduced.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Print Quote" src/components/JobsManager.tsx | grep -qE '^[1-9]' && ! grep -q "Generate PDF" src/components/JobsManager.tsx && grep -c 'variant="secondary"' src/components/JobsManager.tsx | grep -qE '^[1-9][0-9]*$' && npx tsc -b && npx vitest run</automated>
  </verify>
  <done>JobsManager accordion row shows a Print Quote button with secondary styling and the NewBadge absolute overlay preserved. Click handler unchanged (Plan 16-10 swaps it). tsc -b and full vitest both green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Print Quote button | No new trust boundary — same handler, same data, just a renamed label and refreshed styling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-23 | Spoofing | Button affordance | mitigate | Per gap B user report, the prior ghost-styled "Generate PDF" looked like a text link and could be missed or mistaken for a non-action element. Switching to the secondary variant resolves the affordance ambiguity. |
| T-16-24 | NewBadge layout disruption (CLAUDE.md memory rule) | NewBadge overlay | mitigate | Preserve `absolute -top-1 -right-1` on the badge inside a `relative` wrapper — never inline. Acceptance criteria grep enforces this. |
</threat_model>

<verification>
- Static checks (see acceptance criteria) confirm rename, variant, badge overlay, disabled tooltip.
- Type check: `npx tsc -b` exits 0.
- Unit tests: `npx vitest run` exits 0.
- Lint gate: `node scripts/lint-no-raw-html.mjs` exits 0 — no raw `<button>` introduced.
- Manual smoke (executor): the button visually weighs about the same as the adjacent Edit/Delete in the action row (no longer ghost-text-link).
</verification>

<success_criteria>
- The Print Quote button is rendered via the shared `<Button variant="secondary" btnSize="sm">` primitive with `Print Quote` as its label.
- NewBadge `pdf-quote` remains as an absolute overlay anchored to the button (CLAUDE.md memory rule respected).
- Disabled-state tooltip `Set a selling price first` is preserved.
- The button still wires to the existing `handleGeneratePdf` — behavior unchanged in this plan (plan 16-10 swaps the handler to open the PrintQuoteModal).
- All build gates green: `tsc -b`, `vitest run`, `lint-no-raw-html` all exit 0.
</success_criteria>

<output>
Create `.planning/phases/16-printable-pdf-quote/16-07-SUMMARY.md` when done.
</output>
