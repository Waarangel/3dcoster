---
phase: 16-printable-pdf-quote
plan: "09"
type: execute
wave: 2
depends_on: ["16-03", "16-04", "16-07", "16-08"]
gap_closure: true
gap_ids: [C, E]
decisions: [D-15, D-17, D-22]
files_modified:
  - src/types.ts
  - src/db/database.ts
  - src/db/database.migrations.test.ts
  - src/pdf/generateQuotePdf.ts
  - src/pdf/generateQuotePdf.test.ts
  - src/hooks/useDatabase.ts
  - src/components/JobsManager.tsx
requirements: [PDF-02]
autonomous: true
nyquist_compliant: true
must_haves:
  truths:
    - "Per D-17, a new `Quote` interface is exported from `src/types.ts` with the exact shape locked in the extension: id, quoteNumber, printJobId, customerId?, customerSnapshot (JobCustomer), lineItemsSnapshot ({ jobTitle, sellingPrice, shippingCost, resolvedTaxRate, taxAmount, currency, notes, terms, countryAtSendTime? }), status (RuntimeQuoteStatus at runtime call sites; QuoteStatus including 'draft' only in the migration code), createdAt, sentAt, decisionAt?, convertedAt?, convertedToSaleId?."
    - "Per D-17 status-enum split (G6 lock — runtime MUST NEVER write status='draft'): src/types.ts exports BOTH `QuoteStatus = 'sent' | 'accepted' | 'declined' | 'converted' | 'draft'` (the full enum, used by migration code in src/db/backfill.ts) AND `RuntimeQuoteStatus = Exclude<QuoteStatus, 'draft'>` (the narrower enum used by every NEW Quote constructor in PrintQuoteModal and the Convert-to-Sale Quote patch). Quote.status is typed as `QuoteStatus` on the interface (so legacy 'draft' rows can be read), but every runtime write payload is typed `status: RuntimeQuoteStatus` so TypeScript REFUSES `'draft'` at the call site. This is compile-time enforcement of G6, NOT a JSDoc/comment-only convention."
    - "Per D-17, Dexie schema bumps to v8 with a new `quotes` store keyed by id, indexes on `quoteNumber` and `status` (per CONTEXT extension), and a v7→v8 backfill upgrade callback that creates one Quote per PrintJob with `quoteNumber` set."
    - "Per D-17 migration backfill rule: for each PrintJob with quoteNumber, IF the job has ≥1 Sale → emit Quote(status='converted', convertedToSaleId=mostRecentSale.id, customerSnapshot=mostRecentSale.customer, convertedAt=mostRecentSale.createdAt); ELSE → emit Quote(status='draft', best-effort snapshot from job's current state). The migration code in src/db/backfill.ts is the SOLE site allowed to use the wider `QuoteStatus` type (because it legitimately needs to write 'draft' in the backfill path). All runtime call sites use `RuntimeQuoteStatus` and CANNOT compile if they try to write 'draft'."
    - "Per D-15, the lineItemsSnapshot carries `shippingCost: number` (default 0). The refactored `generateQuotePdf(quote: Quote)` signature renders a 'Shipping' row between Subtotal and Tax when `shippingCost > 0`; row is omitted when shippingCost === 0. Total = subtotal + shippingCost + taxAmount."
    - "Per D-22, the tax base remains sellingPrice ONLY — shipping is NOT taxed. The PDF's tax row computes from `subtotal × resolvedTaxRate`, never from `(subtotal + shippingCost) × resolvedTaxRate`. Existing `calculateTax(sellingPrice, ratePercent)` in src/utils/costCalc.ts is unchanged."
    - "Per D-17 G4 strict by-value rule: the refactored `generateQuotePdf(quote: Quote)` signature accepts ONLY a Quote argument — NO PrintJob or UserProfile arguments. All rendered data (jobTitle, currency, terms, country for taxLabelFor, etc.) comes from `quote.lineItemsSnapshot` and `quote.customerSnapshot`. This is type-level enforcement of the snapshot rule."
    - "Per D-17 migration test (locked): a v7 fixture with exactly 3 PrintJobs — (1 has sales, 1 has quoteNumber but no sales, 1 has no quoteNumber) — yields exactly 2 Quote rows post-upgrade. The first is status='converted'; the second is status='draft'; the third PrintJob remains as-is (no Quote written)."
    - "Per CONTEXT canonical_refs and D-18 implementation guidance, `UserProfile.nextQuoteNumber` stays as the counter source. The increment site MOVES from App.tsx → the new `useQuotes().createQuote(modalInputs)` hook action introduced in Task 4 (PrintQuoteModal calls this action; the action wraps the Dexie multi-store transaction internally). PrintQuoteModal does NOT import `db` directly — it consumes `createQuote` from the hook layer to keep the component Dexie-agnostic (architectural cleanliness per WARNING I-07 / Option B)."
  artifacts:
    - path: "src/types.ts"
      provides: "Quote interface; QuoteStatus type union (full, includes 'draft'); RuntimeQuoteStatus = Exclude<QuoteStatus, 'draft'> (narrow, no 'draft'); (deprecated marker on PrintJob.quoteNumber per D-17)"
    - path: "src/db/database.ts"
      provides: "Dexie v8 schema with quotes store; v7→v8 upgrade callback with backfill"
    - path: "src/db/database.migrations.test.ts"
      provides: "v7 fixture → v8 backfill assertion test (locked test from D-17)"
    - path: "src/pdf/generateQuotePdf.ts"
      provides: "Refactored signature: generateQuotePdf(quote: Quote): Promise<void>; reads ONLY from Quote snapshot"
    - path: "src/pdf/generateQuotePdf.test.ts"
      provides: "Updated tests covering shipping row, by-value snapshot reads, refactored signature"
    - path: "src/hooks/useDatabase.ts"
      provides: "useQuotes() entity hook mirroring useCustomers; PLUS a new `createQuote(modalInputs): Promise<Quote>` action that wraps the multi-store Dexie transaction (quotes + settings + customers) so consumers (PrintQuoteModal) never import `db` directly"
    - path: "src/components/JobsManager.tsx"
      provides: "Stopgap Quote wiring for the existing generateQuotePdf call site (replaced cleanly by plan 16-10 — listed here in files_modified so the wave-2 worktree intersection check + executor tsc gate both pass)"
---

<objective>
This is the schema foundation plan for the gap-closure extension. It introduces the `Quote` first-class entity (D-17 G4 + G7 locks), the v7→v8 Dexie migration with backfill (D-17), the shipping field (D-15), the strict by-value PDF refactor (D-17 G4), and the tax-base lock that shipping is NOT taxed (D-22). It also adds the `createQuote(modalInputs)` hook action that PrintQuoteModal will use in plan 16-10 — keeping the modal component Dexie-agnostic and concentrating the multi-store transaction inside the hook layer.

Purpose: Stand up the entire schema + PDF data contract + hook-action surface that plans 16-10 (Print Quote modal), 16-11 (Recent Quotes section), and 16-12 (Convert to Sale) build against. No UI work in this plan.

Output:
- `Quote` interface with strict snapshot shape.
- `QuoteStatus` (full enum) + `RuntimeQuoteStatus = Exclude<QuoteStatus, 'draft'>` (narrow enum) exported from src/types.ts — compile-time enforcement of G6 ("runtime never writes draft").
- Dexie v8 with `quotes` store + backfill migration with locked test.
- Refactored `generateQuotePdf(quote: Quote)` reading ONLY from the snapshot.
- Shipping row in the PDF (D-15).
- Tax base remains sellingPrice (D-22 lock — no change needed but documented).
- `useQuotes()` hook ready for plan 16-11 to consume, with a new `createQuote(modalInputs): Promise<Quote>` action that owns the multi-store transaction (quotes + settings + customers).

**Critical (CLAUDE.md "Problem-Solving Discipline"):** This is meaty but coheres because every piece is type-level wiring. Plans 16-10..16-12 cannot exist until the Quote type, the Dexie store, the migration, the by-value-PDF signature, and the createQuote hook action land. Splitting any of these into a separate plan would force downstream plans to either run against placeholder types (and re-do work after they materialize) or stall behind two foundation waves instead of one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-printable-pdf-quote/16-CONTEXT.md
@.planning/phases/16-printable-pdf-quote/16-RESEARCH.md
@.planning/phases/16-printable-pdf-quote/16-PATTERNS.md
@.planning/phases/16-printable-pdf-quote/16-03-SUMMARY.md
@.planning/phases/15.1-customer-library/15.1-01-SUMMARY.md
@.planning/phases/15.1-customer-library/15.1-CONTEXT.md
@src/types.ts
@src/db/database.ts
@src/db/backfill.ts
@src/db/backfill.test.ts
@src/pdf/generateQuotePdf.ts
@src/pdf/generateQuotePdf.test.ts
@src/hooks/useDatabase.ts
@src/utils/taxResolution.ts
@src/utils/costCalc.ts

<interfaces>
<!-- Customer pattern from Phase 15.1 (mirror exactly): -->
<!--   src/types.ts:163  export interface Customer extends JobCustomer { id; createdAt; lastUsedAt? } -->
<!--   src/db/database.ts:91-99   db.version(7).stores({ ..., customers: 'id, name, email, lastUsedAt' }); -->
<!--   src/hooks/useDatabase.ts:536  export function useCustomers() — useLiveQuery + Map for O(1) dedup + CRUD callbacks -->

<!-- Dexie versionchange reload handler (already in place from Phase 12 SCHEMA-02): -->
<!--   src/db/database.ts:104  db.on('versionchange', () => window.location.reload()); -->
<!--   Covers v8 multi-tab reload automatically. -->

<!-- Existing v6 upgrade callback pattern (the migration template to mirror): -->
<!--   src/db/database.ts:77-86  db.version(6).stores({...}).upgrade(tx => tx.table('jobs').toCollection().modify(backfillTagsOnJob)); -->
<!--   src/db/backfill.ts  backfillTagsOnJob — pure helper, unit-tested in backfill.test.ts -->
<!--   The v7→v8 backfill should follow the same shape: pure helper in src/db/backfill.ts, -->
<!--   unit-tested separately, called from the upgrade callback. -->

<!-- Existing generateQuotePdf signature (Plan 16-03): -->
<!--   export interface QuotePdfParams { job: PrintJob; userProfile: UserProfile; sale?: Sale; } -->
<!--   export async function generateQuotePdf(params: QuotePdfParams): Promise<void> -->
<!--   export async function generateQuotePdfBytes(params: QuotePdfParams): Promise<Uint8Array> -->
<!-- Per D-17 G4, this MUST become: -->
<!--   export async function generateQuotePdf(quote: Quote): Promise<void> -->
<!--   export async function generateQuotePdfBytes(quote: Quote): Promise<Uint8Array> -->
<!-- The internal renderers (header, customer block, items table, totals, notes/terms) -->
<!-- read ALL data from `quote.lineItemsSnapshot` + `quote.customerSnapshot`. -->

<!-- Tax base lock (D-22): -->
<!--   src/utils/costCalc.ts:127  calculateTax(sellingPrice, ratePercent) — already correct, do NOT change -->

<!-- 16-03 SUMMARY notes the PDF currently throws if job.quoteNumber is undefined. -->
<!-- Post-refactor, Quote.quoteNumber is REQUIRED (no `?`) so the throw becomes unreachable -->
<!-- — the type system enforces presence at the call site. -->

<!-- Status enum split (per BLOCKER I-03 / G6 lock): -->
<!--   export type QuoteStatus = 'sent' | 'accepted' | 'declined' | 'converted' | 'draft'; -->
<!--   export type RuntimeQuoteStatus = Exclude<QuoteStatus, 'draft'>; -->
<!--   Quote.status is typed `QuoteStatus` (so 'draft' rows from migration can be READ). -->
<!--   All runtime write payloads (PrintQuoteModal, Convert-to-Sale Quote patch) declare -->
<!--   `status: RuntimeQuoteStatus` so the compiler REFUSES `'draft'` at the call site. -->
<!--   Only src/db/backfill.ts is allowed to use the wider QuoteStatus when constructing -->
<!--   the 'draft' backfill rows. -->

<!-- createQuote hook action (per WARNING I-07 Option B): -->
<!--   useQuotes() gains a new action: -->
<!--     createQuote(input: CreateQuoteInput): Promise<Quote> -->
<!--   where CreateQuoteInput carries the modal-collected fields + a reference to the current -->
<!--   UserProfile snapshot. The action internally opens a Dexie transaction over -->
<!--   ['quotes', 'settings', 'customers'] and does raw db.quotes.add + db.customers.add (if new) + -->
<!--   dbSetUserProfile (bump nextQuoteNumber). Returns the freshly-written Quote object. -->
<!--   PrintQuoteModal (plan 16-10) calls `await createQuote(...)` and never imports `db`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Quote interface + QuoteStatus + RuntimeQuoteStatus types to src/types.ts</name>
  <files>src/types.ts</files>
  <read_first>
    - src/types.ts (lines 150–296 — the JobCustomer, Customer, PrintJob, Sale, UserProfile shapes; the Quote interface goes after Customer to mirror Phase 15.1 layout)
    - .planning/phases/16-printable-pdf-quote/16-CONTEXT.md (the `<extension>` section's D-17 — the LOCKED Quote shape; copy the exact field set)
    - .planning/phases/15.1-customer-library/15.1-01-SUMMARY.md (confirms placement convention — Customer interface lives immediately after JobCustomer at line 163)
  </read_first>
  <action>
    In src/types.ts, after the existing `Customer` interface (around line 172), add per D-17:

    1. **`QuoteStatus` type union (FULL — includes 'draft')** (export):
       `export type QuoteStatus = 'sent' | 'accepted' | 'declined' | 'converted' | 'draft';`
       - The `'draft'` variant is a LEGACY MIGRATION-ONLY value for v7→v8 backfilled PrintJobs that had a quoteNumber but no sales (per D-17 migration rule).
       - This type is used by `src/db/backfill.ts` (the SOLE legitimate writer of 'draft' rows). Quote.status uses this wider type so legacy rows can be READ.
       - Document this distinction with a JSDoc comment above the type citing D-17 G6 lock.

    2. **`RuntimeQuoteStatus` narrow type (per BLOCKER I-03)** (export, IMMEDIATELY below QuoteStatus):
       `export type RuntimeQuoteStatus = Exclude<QuoteStatus, 'draft'>;`
       - JSDoc citing G6 + the BLOCKER I-03 fix: "Per G6 lock, runtime code MUST NEVER WRITE status='draft'. This narrow type is the compile-time guard — every NEW Quote constructor in PrintQuoteModal (plan 16-10) and the Convert-to-Sale Quote patch (plan 16-12) types its `status` field as `RuntimeQuoteStatus`, so TypeScript refuses 'draft' at the call site."
       - The migration code in src/db/backfill.ts keeps using the wider QuoteStatus because the 'draft' backfill is legitimate.

    3. **`Quote` interface** (export) with the exact shape from D-17:
       ```
       export interface Quote {
         id: string;
         quoteNumber: number;          // 4-digit; display via formatQuoteNumber()
         printJobId: string;           // FK to PrintJob.id
         customerId?: string;          // FK to Customer.id (undefined if no library link)
         customerSnapshot: JobCustomer;  // by-value copy at Generate time (per D-17 G4)
         lineItemsSnapshot: {            // by-value snapshot of priced + identity fields
           jobTitle: string;
           sellingPrice: number;
           shippingCost: number;            // D-15; 0 when omitted
           resolvedTaxRate: number;         // resolved (post-fallback), NOT raw override — D-21 lock
           taxAmount: number;               // computed from resolvedTaxRate × sellingPrice (D-22)
           currency: Currency;              // reuses the existing Currency type union
           notes: string;                   // PrintJob.notes at Generate time (may be '')
           terms: string;                   // UserProfile.defaultTerms at Generate time (may be '')
           countryAtSendTime?: string;      // UserProfile.address.country at Generate time — taxLabelFor reproducibility
         };
         status: QuoteStatus;              // wider type so 'draft' migration rows can be READ; runtime writes are typed RuntimeQuoteStatus at the call site
         createdAt: Date;
         sentAt: Date;                  // = createdAt for v1.2 (PDF gen IS sent — G6 lock)
         decisionAt?: Date;             // set when user marks Accepted/Declined
         convertedAt?: Date;             // set when Convert to Sale fires
         convertedToSaleId?: string;     // FK to Sale.id on conversion
       }
       ```
       Include a JSDoc block above the interface citing D-17 G4 strict by-value rule AND noting that the field is typed `QuoteStatus` (read-side) while runtime writes MUST type their payload `status: RuntimeQuoteStatus`.

    4. **Add `convertedFromQuoteId?: string` to the `Sale` interface** (per D-20 back-reference) immediately after `marketplaceFee?: number`. JSDoc citing D-20.

    5. **Mark `PrintJob.quoteNumber` as deprecated** (per D-17) — add or extend the JSDoc on the existing `quoteNumber?: number` field at line 226:
       `/** @deprecated Phase 16 gap closure (D-17). Read-only after v7→v8 migration — the Quote.quoteNumber field is the source of truth. Removed in a follow-up cleanup phase. */`

    6. **Confirm `Currency` type union already includes** the currencies referenced by Quote.lineItemsSnapshot.currency: USD, EUR, GBP, CAD, AUD, NZD, CHF, JPY, ZAR. Per types.ts:11-30, the existing union covers all of these plus extras. No change needed; just confirm.

    7. **DO NOT touch JobCustomer or Customer.** The Quote interface REUSES JobCustomer for customerSnapshot — zero field mapping (mirrors Phase 15.1 D-01).
  </action>
  <acceptance_criteria>
    - `grep -c "export interface Quote" src/types.ts` returns `1`.
    - `grep -c "export type QuoteStatus" src/types.ts` returns `1`.
    - `grep -c "export type RuntimeQuoteStatus" src/types.ts` returns `1` (the BLOCKER I-03 compile-time guard).
    - `grep -c "Exclude<QuoteStatus, 'draft'>" src/types.ts` returns `1`.
    - `grep -c "convertedFromQuoteId" src/types.ts` returns `1` (on the Sale interface).
    - `grep -c "@deprecated Phase 16 gap closure" src/types.ts` returns `1` (on PrintJob.quoteNumber).
    - `grep -c "customerSnapshot: JobCustomer" src/types.ts` returns `1` (the Quote field).
    - `grep -c "lineItemsSnapshot" src/types.ts` returns `1`.
    - `grep -c "resolvedTaxRate: number" src/types.ts` returns `1`.
    - `grep -c "shippingCost: number" src/types.ts` returns at least `1` (on Quote.lineItemsSnapshot; Sale already has `shippingCost?` separately — that's allowed).
    - `npx tsc -b` exits `0` — no other file references break.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "export interface Quote " src/types.ts | grep -qx 1 && grep -c "export type QuoteStatus" src/types.ts | grep -qx 1 && grep -c "export type RuntimeQuoteStatus" src/types.ts | grep -qx 1 && grep -c "Exclude<QuoteStatus, 'draft'>" src/types.ts | grep -qx 1 && grep -c "convertedFromQuoteId" src/types.ts | grep -qx 1 && npx tsc -b</automated>
  </verify>
  <done>Quote interface + QuoteStatus (full) + RuntimeQuoteStatus (narrow) type aliases exported from src/types.ts with the exact D-17 shape. RuntimeQuoteStatus is the compile-time G6 guard. Sale gains convertedFromQuoteId? back-reference. PrintJob.quoteNumber marked deprecated. tsc -b green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add v7→v8 migration backfill pure helper + locked migration test</name>
  <files>src/db/backfill.ts, src/db/backfill.test.ts, src/db/database.migrations.test.ts</files>
  <read_first>
    - src/db/backfill.ts (the existing `backfillTagsOnJob` pure helper from Phase 12 — pattern to mirror)
    - src/db/backfill.test.ts (Phase 12's test pattern — pure helper unit-tested without Dexie)
    - src/types.ts (the new Quote interface + QuoteStatus + RuntimeQuoteStatus from Task 1)
    - .planning/phases/16-printable-pdf-quote/16-CONTEXT.md (D-17 backfill rule — the LOCKED migration test fixture)
    - src/db/database.ts (the existing v6→v7 pattern at lines 77-99)
  </read_first>
  <behavior>
    Pure helper `backfillQuotesFromJobs(jobs: PrintJob[], sales: Sale[]): Quote[]`:
    - For each PrintJob with `quoteNumber` set:
      - Lookup sales for this job by `sale.jobId === job.id`, sorted descending by `soldAt`.
      - IF sales.length > 0 → emit Quote with `status='converted'`, `convertedToSaleId=sales[0].id`, `customerSnapshot=sales[0].customer ?? { name: sales[0].customerName ?? '' }`, `convertedAt=sales[0].soldAt`, `quoteNumber=job.quoteNumber!`, `printJobId=job.id`.
      - ELSE → emit Quote with `status='draft'`, `customerSnapshot={ name: '' }` (empty snapshot — best-effort for legacy), `quoteNumber=job.quoteNumber!`, `printJobId=job.id`. No `convertedToSaleId`.
    - For each Quote emitted: `id = crypto.randomUUID()`, `createdAt = sentAt = new Date(job.createdAt)` (best-effort timestamp from job).
    - `lineItemsSnapshot` for backfilled rows: `{ jobTitle: job.name, sellingPrice: job.sellingPrice ?? 0, shippingCost: 0, resolvedTaxRate: job.taxRate ?? 0, taxAmount: job.taxAmount ?? 0, currency: <fallback from userProfile or 'USD'>, notes: job.notes ?? '', terms: '', countryAtSendTime: undefined }`. NOTE: the migration cannot read userProfile, so currency defaults to a sentinel — see action below for how to handle the currency lookup.
    - PrintJobs without quoteNumber are SKIPPED — no Quote emitted, the PrintJob row is untouched.
    - **Type-level note (per BLOCKER I-03):** the backfill helper IS the legitimate writer of `status='draft'` and so uses the wider `QuoteStatus` type when constructing the 'draft' Quote. This is the ONLY module in the codebase allowed to do this. Document with a code comment.

    Locked test (D-17 G7) — a fixture of 3 PrintJobs yields exactly 2 Quotes:
    - Job A: has quoteNumber=42, has 1 Sale → Quote(status='converted', quoteNumber=42, convertedToSaleId=saleA.id).
    - Job B: has quoteNumber=43, no Sales → Quote(status='draft', quoteNumber=43, no convertedToSaleId).
    - Job C: no quoteNumber → no Quote emitted.
  </behavior>
  <action>
    **A. Extend src/db/backfill.ts** with a new pure helper:

    ```
    // backfillQuotesFromJobs — pure helper for v7→v8 migration (Phase 16 gap closure D-17 G7).
    // Reads existing PrintJobs and Sales; returns the Quote rows that should be created.
    // No Dexie, no React, no IO. Mirrors backfillTagsOnJob's pure-function pattern.
    //
    // TYPE-LEVEL NOTE (BLOCKER I-03 / G6 lock): this is the SOLE module allowed to write
    // status='draft'. Runtime callers use RuntimeQuoteStatus (the narrow enum) which excludes
    // 'draft' so the compiler refuses any accidental 'draft' write at PrintQuoteModal /
    // Convert-to-Sale call sites.
    export function backfillQuotesFromJobs(jobs: PrintJob[], sales: Sale[]): Quote[] { ... }
    ```

    Implementation per the `<behavior>` block above. Use `crypto.randomUUID()` for ids (works in Node 18+ test environment per Phase 15.1 pattern). For the `currency` field on the snapshot: since the migration runs inside the Dexie upgrade callback (no userProfile access), default to `'USD'` as a sentinel. The PDF will render correctly for these legacy rows because they all share status='converted' or 'draft' — neither status is renderable by the runtime PDF generator (only 'sent'+ statuses surface in plan 16-11's UI). Document this currency-sentinel decision in a code comment.

    **B. Add a unit test file `src/db/backfill.test.ts`** (or extend the existing file if it has the same name):

    - Fixture: 3 PrintJobs (Job A with sales, Job B without, Job C without quoteNumber) + 1 Sale for Job A.
    - Call `backfillQuotesFromJobs(jobs, sales)`.
    - Assert: result.length === 2.
    - Assert: result[0] (Job A's Quote) has `status === 'converted'`, `quoteNumber === 42`, `convertedToSaleId === saleA.id`, `customerSnapshot.name === <saleA.customer.name>` (test fixture asserts this exact value).
    - Assert: result[1] (Job B's Quote) has `status === 'draft'`, `quoteNumber === 43`, `convertedToSaleId === undefined`.
    - Assert: Job C (no quoteNumber) produced NO Quote — sanity check via filter.
    - Run vitest. Test FAILS on the initial scaffold (RED). Implement the helper to GREEN.

    **C. Add the Dexie-level migration test `src/db/database.migrations.test.ts`** (new file):

    - Skeleton: import Dexie, open a `3DCosterDB-MIGRATIONS-TEST` (different DB name to isolate from app DB) at v7, seed the same 3-job fixture into `jobs` + `sales`, close, reopen at v8.
    - After upgrade: query `db.quotes.toArray()`. Assert length === 2. Assert one has status='converted' and the other has status='draft'. Assert the converted one's convertedToSaleId is non-empty.
    - Use jsdom + the `fake-indexeddb` polyfill if it's already in the project, OR run as a node-only test that mocks Dexie (executor picks the path with smallest blast radius — prefer fake-indexeddb if already a devDep; otherwise it's acceptable to mark this test as a behavioral integration test that runs against the real `Dexie` API in jsdom which already has IndexedDB shimmed by vitest).
    - If `fake-indexeddb` is not installed and adding it is non-trivial, the executor MAY substitute a smaller test that calls `backfillQuotesFromJobs` directly on a Dexie-shaped fixture; the locked D-17 contract is that the fixture-to-output mapping is correct, which the pure helper test already covers. Document this fallback in the SUMMARY.

    **D. DO NOT wire the migration into database.ts yet** — that's Task 3. This task just adds the helper + tests.
  </action>
  <acceptance_criteria>
    - `grep -c "export function backfillQuotesFromJobs" src/db/backfill.ts` returns `1`.
    - `src/db/backfill.test.ts` contains the 3-job fixture + 4 assertions per the behavior block.
    - `src/db/database.migrations.test.ts` exists with at minimum a `describe('v7→v8 quotes backfill (D-17 G7)', ...)` block.
    - `npx vitest run src/db/backfill.test.ts` exits `0` — all assertions pass.
    - `npx vitest run src/db/database.migrations.test.ts` exits `0` (whether via real Dexie upgrade or via the documented fallback).
    - `npx tsc -b` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "export function backfillQuotesFromJobs" src/db/backfill.ts | grep -qx 1 && npx vitest run src/db/backfill.test.ts src/db/database.migrations.test.ts && npx tsc -b</automated>
  </verify>
  <done>Pure helper + unit tests + Dexie-level migration test all green. Locked 3-job → 2-quote fixture (D-17 G7) is the canary.</done>
</task>

<task type="auto">
  <name>Task 3: Bump Dexie to v8, wire backfill, add useQuotes hook (read-side only — createQuote action lands in Task 4)</name>
  <files>src/db/database.ts, src/hooks/useDatabase.ts</files>
  <read_first>
    - src/db/database.ts (the entire file — note the v7 block at lines 91-99 and the versionchange handler at 104)
    - src/hooks/useDatabase.ts (the useCustomers hook around line 536 — mirror its shape exactly for useQuotes)
    - src/db/backfill.ts (the new helper from Task 2)
    - .planning/phases/15.1-customer-library/15.1-01-SUMMARY.md (the v7 + useCustomers pattern)
  </read_first>
  <action>
    **A. src/db/database.ts:**

    1. Extend the type-only import from `'../types'` to include `Quote`.

    2. Extend the Dexie type intersection (around line 19) with:
       `quotes: EntityTable<Quote, 'id'>;`

    3. After the v7 block (after line 99), add the v8 block:
       ```
       // v8: add `quotes` library store (Phase 16 gap closure — D-17 G7).
       // Backfill: one Quote per PrintJob with quoteNumber set.
       db.version(8).stores({
         materials: 'id, category, brand, filamentType, currency',
         printers: 'id, name',
         printerInstances: 'id, printerConfigId, nickname',
         jobs: 'id, name, createdAt, printerInstanceId',
         sales: 'id, jobId, soldAt',
         settings: 'key',
         customers: 'id, name, email, lastUsedAt',
         quotes: 'id, quoteNumber, status, printJobId, customerId, sentAt',  // indexes per D-17 — quoteNumber + status indexed
       }).upgrade(async tx => {
         const jobs = await tx.table('jobs').toArray();
         const sales = await tx.table('sales').toArray();
         const quotes = backfillQuotesFromJobs(jobs, sales);
         if (quotes.length > 0) {
           await tx.table('quotes').bulkAdd(quotes);
         }
       });
       ```
       Import `backfillQuotesFromJobs` from `./backfill` at the top of the file (extend existing import).

    4. Do NOT touch the `db.on('versionchange', ...)` handler — it already covers v8 multi-tab reload (per Phase 12 SCHEMA-02).

    5. Do NOT delete `PrintJob.quoteNumber` from any historical record. The deprecation comment in types.ts is the contract; cleanup is a future phase.

    **B. src/hooks/useDatabase.ts — add useQuotes() READ-SIDE only:**

    Add `useQuotes()` hook mirroring `useCustomers` (see 15.1-01-SUMMARY for the exact return shape). Adapt to Quote:

    ```
    export function useQuotes(): {
      quotes: Quote[];                                  // useLiveQuery, falls back to []
      quotesByJobId: Map<string, Quote[]>;              // O(1) per-job lookup for Recent Quotes section
      isLoading: boolean;                                // one-shot db.quotes.count() flips false
      addQuote: (quote: Quote) => Promise<void>;        // db.quotes.add — used by Recent Quotes status flips (plan 16-11)
      updateQuote: (quote: Quote) => Promise<void>;     // db.quotes.put (used by Mark Accepted/Declined in plan 16-11)
      deleteQuote: (id: string) => Promise<void>;       // db.quotes.delete (currently no UI consumer — surface for future hygiene)
      // createQuote action is added in Task 4 (it requires the full transactional shape + customer-link logic + counter bump).
    } { ... }
    ```

    The `quotesByJobId` Map is critical for plan 16-11's Recent Quotes section — it indexes Quote rows by their `printJobId` so the section can render in O(1) per accordion expand without re-scanning the full quotes array.

    Add `Quote` to the existing type-only import from `'../types'`.

    **C. Confirm Dexie versionchange handler unchanged.**

    **Scope guard:** the `createQuote` transactional action is intentionally NOT in this task — it lands in Task 4 along with the JobsManager stopgap wiring, because both need a clear understanding of the full Quote-construction shape that comes out of the PDF refactor.
  </action>
  <acceptance_criteria>
    - `grep -c "db.version(8).stores" src/db/database.ts` returns `1`.
    - `grep -c "quotes: EntityTable<Quote, 'id'>" src/db/database.ts` returns `1`.
    - `grep -c "quotes: 'id, quoteNumber, status, printJobId, customerId, sentAt'" src/db/database.ts` returns `1`.
    - `grep -c "backfillQuotesFromJobs" src/db/database.ts` returns at least `2` (import + call site in upgrade).
    - `grep -c "^export function useQuotes" src/hooks/useDatabase.ts` returns `1`.
    - `grep -c "quotesByJobId" src/hooks/useDatabase.ts` returns at least `2`.
    - `npx tsc -b` exits `0`.
    - `npx vitest run` exits `0` — all existing tests still pass + the new migration test still passes against the wired v8 block.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "db.version(8).stores" src/db/database.ts | grep -qx 1 && grep -c "^export function useQuotes" src/hooks/useDatabase.ts | grep -qx 1 && npx tsc -b && npx vitest run</automated>
  </verify>
  <done>Dexie v8 schema + backfill upgrade wired. useQuotes hook exposes read-side + simple CRUD (addQuote, updateQuote, deleteQuote). createQuote transactional action deferred to Task 4. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Refactor generateQuotePdf to (quote: Quote); add Shipping row; add createQuote hook action; wire JobsManager stopgap</name>
  <files>src/pdf/generateQuotePdf.ts, src/pdf/generateQuotePdf.test.ts, src/hooks/useDatabase.ts, src/components/JobsManager.tsx</files>
  <read_first>
    - src/pdf/generateQuotePdf.ts (entire file — note the existing signature `generateQuotePdf(params: QuotePdfParams)` and the 5 section renderers per 16-03 SUMMARY)
    - src/pdf/generateQuotePdf.test.ts (existing 21 tests using ToUnicode CMap extraction — note the fixture builders and the `pdfExtractText` helper)
    - src/hooks/useDatabase.ts (useCustomers + the new useQuotes from Task 3 — the createQuote action extends useQuotes)
    - src/db/database.ts (the `db` instance + `setUserProfile as dbSetUserProfile` export pattern)
    - .planning/phases/16-printable-pdf-quote/16-03-SUMMARY.md (the established jsPDF 4.x per-instance VFS pattern and the autotable v5 calling convention)
    - .planning/phases/16-printable-pdf-quote/16-CONTEXT.md (`<extension>` section D-15 for shipping row + D-17 G4 for by-value lock + D-22 for tax-base lock)
    - src/types.ts (the new Quote interface + RuntimeQuoteStatus)
  </read_first>
  <behavior>
    - `generateQuotePdf(quote: Quote): Promise<void>` — new signature, single argument.
    - `generateQuotePdfBytes(quote: Quote): Promise<Uint8Array>` — also refactored.
    - All renderers read EXCLUSIVELY from `quote.lineItemsSnapshot` and `quote.customerSnapshot`. No PrintJob, no UserProfile references.
    - Customer block: reads from `quote.customerSnapshot`. Omitted when `customerSnapshot.name === '' && customerSnapshot.email === ''` (empty-customer guard for v7→v8 backfill 'draft' rows that never get rendered anyway, but defensive).
    - Quote number rendering: `formatQuoteNumber(quote.quoteNumber)` — `quote.quoteNumber` is REQUIRED (no `?`) so the throw-when-undefined branch becomes unreachable; remove that throw.
    - Tax label: `taxLabelFor(quote.lineItemsSnapshot.countryAtSendTime)` — reads the snapshotted country, NOT the current userProfile (per D-17 G4 reproducibility).
    - Subtotal: `quote.lineItemsSnapshot.sellingPrice × 1` (collapsed single-row D-04 — qty defaults to 1 at render time, hardcoded). The Quote.lineItemsSnapshot does NOT carry quantity. Document in a code comment.
    - **Shipping row (D-15):** between Subtotal and Tax. Render when `quote.lineItemsSnapshot.shippingCost > 0`. Label: `Shipping:`. Value: `formatCurrency(shippingCost, currency)`. Omitted when shippingCost === 0 (same hide-when-zero pattern as the tax row per D-07).
    - **Tax row:** unchanged in spirit — reads `resolvedTaxRate` and `taxAmount` from the snapshot. Label uses `taxLabelFor(countryAtSendTime)`. Hidden when `resolvedTaxRate === 0 || taxAmount === 0`.
    - **Total (D-15 + D-22):** `total = sellingPrice + shippingCost + taxAmount` where `taxAmount = sellingPrice × (resolvedTaxRate / 100)` — shipping NOT included in the taxable base. Display total in bold via `formatCurrency`.
    - Tauri save path: unchanged (still uses dynamic import of `@tauri-apps/plugin-dialog` + `@tauri-apps/plugin-fs`).
    - Filename: `Quote-{Q-NNNN}-{customerNameSlug(customerSnapshot.name)}.pdf` — fallback to `Quote-Q-NNNN.pdf` when slug is empty (per D-11 unchanged).

    **createQuote hook action (per WARNING I-07 Option B):**
    - `createQuote(input: CreateQuoteInput): Promise<Quote>` added to the useQuotes hook return.
    - `CreateQuoteInput` shape: `{ job: PrintJob; userProfile: UserProfile; customerSnapshot: JobCustomer; existingCustomerId?: string; shippingCost: number; resolvedTaxRate: number; taxAmount: number }`.
    - Internally:
      - Opens `db.transaction('rw', [db.quotes, db.customers, db.settings], async () => { ... })`.
      - If `existingCustomerId` is set → bump that Customer's lastUsedAt via `db.customers.update(existingCustomerId, { lastUsedAt: new Date() })`.
      - Else if Name OR Email is present → `db.customers.add({ id: crypto.randomUUID(), createdAt: new Date(), lastUsedAt: new Date(), name, email, company, address })`. Capture the new customer id for `Quote.customerId`.
      - Build the Quote object with `status: 'sent' as RuntimeQuoteStatus` — typed narrow so the compiler refuses 'draft' here.
      - `db.quotes.add(quote)`.
      - Read the current settings JSON blob, bump `nextQuoteNumber = currentNext + 1`, write back via `dbSetUserProfile({ ...userProfile, nextQuoteNumber: <bumped> })` — all inside the transaction.
    - Returns the freshly-written Quote object for the caller to feed into `generateQuotePdf(quote)`.
    - PrintQuoteModal (plan 16-10) calls `await createQuote(...)` and NEVER imports `db` directly.
  </behavior>
  <action>
    A. src/pdf/generateQuotePdf.ts:

    1. **Change the signature** of `generateQuotePdf` and `generateQuotePdfBytes` to take a single `quote: Quote` argument. Delete the `QuotePdfParams` interface (or keep it deprecated; the cleaner move is delete-and-replace).

    2. **Update all internal renderers** to read from `quote.lineItemsSnapshot` and `quote.customerSnapshot`. The renderers are (per 16-03 SUMMARY): header, meta-LEFT + customer-RIGHT block, line items table (autoTable), totals (right-aligned), Notes/Terms, footer. Each renderer's data source switches from `job`+`userProfile` → `quote`.

    3. **Remove the `throw new Error('quoteNumber undefined')` guard** — the new signature makes it impossible (`quote.quoteNumber` is required by TypeScript). If the throw existed for runtime safety, replace with a `// invariant: Quote.quoteNumber is required per Quote interface` comment.

    4. **Insert the Shipping row** between Subtotal and Tax per the behavior block.

    5. **Confirm the tax base is sellingPrice only** (per D-22). The line `taxAmount = sellingPrice × ratePercent / 100` should already be correct from 16-03; verify no accidental `(sellingPrice + shippingCost) × ratePercent` slip. If shippingCost is reachable inside any tax-amount computation, that's a bug — must be excluded.

    6. **Tauri save path:** ensure the filename builder still works post-refactor. Use `customerNameSlug(quote.customerSnapshot.name)`.

    7. Add a top-of-file comment block citing D-17 G4 strict by-value rule: `// PDF reads ONLY from Quote.lineItemsSnapshot + Quote.customerSnapshot — NEVER from PrintJob or UserProfile at render time. Snapshot-only enforcement per D-17 G4.`

    B. src/pdf/generateQuotePdf.test.ts:

    1. **Replace the existing fixture builders** to produce `Quote` objects instead of `PrintJob` + `UserProfile` + `Sale`. Build a `makeQuote(overrides?: Partial<Quote>): Quote` helper.

    2. **Update the existing 21 tests** to call `generateQuotePdfBytes(quote)` with the new signature.

    3. **Add at least 4 new tests** for the shipping row + D-22 tax-base lock + by-value enforcement:
       - Shipping = 0 → no Shipping row in PDF (extract text, assert absence).
       - Shipping = 5.00, currency = 'EUR' → PDF contains a "Shipping" row with the string `€5.00` between Subtotal and Tax rows.
       - D-22 tax-base lock: Quote with sellingPrice=100, shippingCost=10, resolvedTaxRate=20 → PDF Total === 130.00 (100 + 10 + 100×0.20 = 130) and the Tax row shows `€20.00` (NOT `€22.00` which would be the wrong-base computation).
       - By-value: a Quote with countryAtSendTime='IT' renders `VAT` label even if the userProfile's current address.country is something else — this test only needs to pass the Quote (the function no longer accepts userProfile, so this is type-enforced).

    4. **Remove the test that asserts the throw on missing quoteNumber** — the throw is gone; the type system enforces presence.

    5. Run vitest. All tests must pass. Run `tsc -b` — all callers of `generateQuotePdf` in the codebase (currently only `src/components/JobsManager.tsx:781` per gap-closure baseline) will fail because the signature changed. THAT IS EXPECTED — Plan 16-10 fixes the JobsManager call site (the old silent-fallback handler is replaced by the modal opener). DO NOT touch JobsManager.tsx in this plan to fix it permanently; the stopgap wiring below keeps the wave clean.

    C. src/hooks/useDatabase.ts — add the `createQuote` transactional action (per WARNING I-07 Option B):

    1. **Define `CreateQuoteInput`** type at the top of the useQuotes hook (or as a separate exported type — executor picks):
       ```
       export interface CreateQuoteInput {
         job: PrintJob;
         userProfile: UserProfile;
         customerSnapshot: JobCustomer;
         existingCustomerId?: string;     // if email-match dedup found a record
         shippingCost: number;
         resolvedTaxRate: number;
         taxAmount: number;
       }
       ```

    2. **Add `createQuote` to useQuotes' return object** with the following implementation (in spirit; executor adapts to the file's style):
       ```
       async function createQuote(input: CreateQuoteInput): Promise<Quote> {
         const { job, userProfile, customerSnapshot, existingCustomerId, shippingCost, resolvedTaxRate, taxAmount } = input;
         const nextNum = userProfile.nextQuoteNumber ?? 1;

         // Build the Quote outside the transaction; assign customerId inside based on dedup/auto-create.
         let customerIdForQuote: string | undefined = existingCustomerId;

         const newCustomerCandidate = !existingCustomerId && (customerSnapshot.name?.trim() || customerSnapshot.email?.trim())
           ? {
               id: crypto.randomUUID(),
               createdAt: new Date(),
               lastUsedAt: new Date(),
               name: customerSnapshot.name ?? '',
               email: customerSnapshot.email,
               company: customerSnapshot.company,
               address: customerSnapshot.address,
             } as Customer
           : undefined;

         if (newCustomerCandidate) customerIdForQuote = newCustomerCandidate.id;

         // status: RuntimeQuoteStatus — the compile-time G6 guard per BLOCKER I-03.
         // TypeScript REFUSES `status: 'draft'` here at compile time.
         const quote: Quote = {
           id: crypto.randomUUID(),
           quoteNumber: nextNum,
           printJobId: job.id,
           customerId: customerIdForQuote,
           customerSnapshot,
           lineItemsSnapshot: {
             jobTitle: job.name,
             sellingPrice: job.sellingPrice,
             shippingCost,
             resolvedTaxRate,
             taxAmount,
             currency: userProfile.currency,
             notes: job.notes ?? '',
             terms: userProfile.defaultTerms ?? '',
             countryAtSendTime: userProfile.address?.country,
           },
           status: 'sent' satisfies RuntimeQuoteStatus,
           createdAt: new Date(),
           sentAt: new Date(),
         };

         await db.transaction('rw', [db.quotes, db.customers, db.settings], async () => {
           if (existingCustomerId) {
             await db.customers.update(existingCustomerId, { lastUsedAt: new Date() });
           } else if (newCustomerCandidate) {
             await db.customers.add(newCustomerCandidate);
           }
           await db.quotes.add(quote);
           await dbSetUserProfile({ ...userProfile, nextQuoteNumber: nextNum + 1 });
         });

         return quote;
       }
       ```

    3. **Import `db` and `setUserProfile as dbSetUserProfile`** from `'../db/database'` at the top of useDatabase.ts (extend any existing imports). This is the architectural seam — the hook owns the Dexie access; consumers (PrintQuoteModal) call the hook action.

    4. **Extend the useQuotes return type** to include `createQuote: (input: CreateQuoteInput) => Promise<Quote>`.

    5. The `addQuote(quote)` callback added in Task 3 STAYS (it is used by plan 16-11 status flips' optimistic-update path if needed, and by tests). It is distinct from `createQuote`: addQuote is a bare db.quotes.add wrapper; createQuote is the full transactional Quote-creation action with counter bump + customer link/auto-create.

    D. src/components/JobsManager.tsx — stopgap wiring to keep tsc green (replaced cleanly by plan 16-10):

    1. In `handleGeneratePdf`, after the existing logic that derives `job`, `userProfile`, and `mostRecentSale`, materialize a stopgap Quote object inline (do NOT call the new createQuote action — the silent fallback is going away in plan 16-10, no need to wire it through the hook):
       ```
       const stopgapQuote: Quote = {
         id: crypto.randomUUID(),
         quoteNumber: job.quoteNumber!,
         printJobId: job.id,
         customerId: undefined,
         customerSnapshot: mostRecentSale?.customer ?? { name: '' },
         lineItemsSnapshot: {
           jobTitle: job.name,
           sellingPrice: job.sellingPrice,
           shippingCost: 0,
           resolvedTaxRate: job.taxRate ?? 0,
           taxAmount: job.taxAmount ?? 0,
           currency: userProfile.currency,
           notes: job.notes ?? '',
           terms: userProfile.defaultTerms ?? '',
           countryAtSendTime: userProfile.address?.country,
         },
         status: 'sent' satisfies RuntimeQuoteStatus,
         createdAt: new Date(),
         sentAt: new Date(),
       };
       ```
       Then call `await generateQuotePdf(stopgapQuote)`.

       Add a comment: `// TODO(plan-16-10): REPLACED by PrintQuoteModal flow — this stopgap exists only to keep the wave-2 tsc gate green while the modal lands in wave 3.`

    2. This stopgap wiring is the SOLE reason `src/components/JobsManager.tsx` is listed in this plan's `files_modified` frontmatter (per BLOCKER I-02 fix). Without it, the wave-2 worktree would fail tsc because `generateQuotePdf`'s signature changed and JobsManager still calls the old signature.

    3. `src/components/JobsManager.tsx` is also in 16-07's files_modified (rename) — 16-07 is wave 0 and this plan is wave 2, so they serialize cleanly. Also in 16-08's files_modified (Sale audit) — 16-08 is wave 1; serializes. Also touched by 16-10 (wave 3) and 16-11 (wave 4) — both later waves; serialize. No wave conflicts.
  </action>
  <acceptance_criteria>
    - `grep -nE "export (async )?function generateQuotePdf" src/pdf/generateQuotePdf.ts` shows the new signature `generateQuotePdf(quote: Quote)`.
    - `grep -c "QuotePdfParams" src/pdf/generateQuotePdf.ts` returns `0` (interface deleted).
    - `grep -c "userProfile:" src/pdf/generateQuotePdf.ts` returns `0` (no UserProfile arg anywhere in the module).
    - `grep -c "job:" src/pdf/generateQuotePdf.ts` returns `0` or only inside doc comments (no PrintJob arg).
    - `grep -c "Shipping" src/pdf/generateQuotePdf.ts` returns at least `1` (the new row label or its detection).
    - `grep -c "shippingCost" src/pdf/generateQuotePdf.ts` returns at least `2` (read + conditional render).
    - `grep -c "createQuote" src/hooks/useDatabase.ts` returns at least `2` (export return + function definition).
    - `grep -c "db.transaction" src/hooks/useDatabase.ts` returns at least `1` (the transactional createQuote body).
    - `grep -c "RuntimeQuoteStatus\|satisfies RuntimeQuoteStatus" src/hooks/useDatabase.ts` returns at least `1` (the createQuote compile-time G6 guard).
    - `grep -c "stopgapQuote\|TODO(plan-16-10)" src/components/JobsManager.tsx` returns at least `1`.
    - `grep -c "satisfies RuntimeQuoteStatus" src/components/JobsManager.tsx` returns at least `1` (the stopgap also uses the narrow type for consistency).
    - `npx vitest run src/pdf/generateQuotePdf.test.ts` exits `0` with at least 24 tests (21 existing + 3+ new — shipping zero, shipping nonzero, D-22 tax-base lock).
    - `npx tsc -b` exits `0`.
    - `node scripts/assert-no-static-jspdf.mjs` exits `0` (src/pdf/ exclusion still works).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "QuotePdfParams" src/pdf/generateQuotePdf.ts | grep -qx 0 && grep -c "userProfile:" src/pdf/generateQuotePdf.ts | grep -qx 0 && grep -c "shippingCost" src/pdf/generateQuotePdf.ts | grep -qE '^[2-9]|^[0-9]{2,}$' && grep -c "createQuote" src/hooks/useDatabase.ts | grep -qE '^[2-9]' && grep -c "db.transaction" src/hooks/useDatabase.ts | grep -qE '^[1-9]' && npx vitest run src/pdf/generateQuotePdf.test.ts && npx tsc -b && node scripts/assert-no-static-jspdf.mjs</automated>
  </verify>
  <done>generateQuotePdf signature is (quote: Quote). All renderers read from snapshot. Shipping row works. D-22 tax-base lock holds. 24+ tests green. createQuote transactional hook action lives in useDatabase.ts (per WARNING I-07 Option B) — PrintQuoteModal will consume it in plan 16-10 instead of importing `db` directly. JobsManager stopgap keeps tsc clean for the wave handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dexie schema upgrade (v7→v8) | Migration runs on first app load post-upgrade. Failure mid-upgrade leaves the DB in an inconsistent state. Mitigated by Dexie's transactional upgrade semantics + the locked migration test. |
| Quote.customerSnapshot (PII) | Customer PII (name/email/company/address) is copied by-value into every Quote. Same data already lives on Sale.customer per Phase 14; no new PII surface introduced. |
| Quote → PDF render | PDF reads ONLY from snapshot (D-17 G4). Type-level enforcement (signature accepts Quote only) prevents accidental cross-reads from mutable PrintJob/UserProfile. |
| createQuote transactional boundary | Hook action owns the multi-store Dexie transaction (quotes + settings + customers). Consumers (PrintQuoteModal) never touch `db` directly — keeping the architectural seam clean and the transaction concentrated in one place. |
| Customer-typed strings → jspdf | Customer name/email/company/address/notes/terms flow from `quote.customerSnapshot` + `quote.lineItemsSnapshot` into `doc.text()` calls inside generateQuotePdf. Pure text — no `doc.html()` anywhere in src/pdf/. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-28 | Tampering | v7→v8 migration | mitigate | Pure helper `backfillQuotesFromJobs` is unit-tested independently (D-17 G7 locked fixture). Dexie's atomic upgrade callback + the migration test exercise the full upgrade path. |
| T-16-29 | Information Disclosure | customerSnapshot PII | accept | Same PII already in Sale.customer (Phase 14); no expanded surface. Per CLAUDE.md "real PII not mock" — backfill uses the existing Sale.customer when present, never fabricates. |
| T-16-30 | Tampering | PDF reads from mutable state | mitigate | D-17 G4 strict by-value rule enforced at the type level — generateQuotePdf(quote: Quote) cannot accept PrintJob or UserProfile. Compile-time guarantee. |
| T-16-31 | Repudiation | Legacy data integrity | accept | PrintJob.quoteNumber is preserved (deprecated, not deleted) so any pre-v8 record audit can still match a quote to its source job. |
| T-16-32 | Denial of Service | Migration on large dataset | accept | The backfill is one-pass over jobs + sales (O(n+m) where n=jobs, m=sales). For typical 3DCoster users (<10k jobs, <50k sales) this completes in well under 100ms in IndexedDB. No mitigation needed. |
| T-16-PDF-INJECT | Tampering / Information Disclosure | jspdf string-injection at PDF data entry (per WARNING I-05) | mitigate | The PDF data entry point (`generateQuotePdf(quote)`) receives `customerSnapshot` and `lineItemsSnapshot` strings — user-typed Name/Email/Company/Address/Notes/Terms — and routes them ONLY through `doc.text(string)` and `jspdf-autotable` cell renderers that escape their inputs. `doc.html()` is NEVER imported or called anywhere in src/pdf/. CI gate `scripts/assert-no-static-jspdf.mjs` enforces that no other module imports from `'jspdf'` outside src/pdf/, so the attack surface stays bounded to this one file. Mirrors T-16-35 from plan 16-10 but origin-tagged here because this is where customerSnapshot/lineItemsSnapshot flow INTO doc.text(). |
</threat_model>

<verification>
- Type-level: `npx tsc -b` exits 0 with the refactored generateQuotePdf signature — the JobsManager stopgap Quote satisfies the compiler.
- Schema: `grep -c "db.version(8)" src/db/database.ts` → 1.
- Migration: D-17 G7 locked fixture test passes (3 jobs → 2 quotes).
- PDF refactor: `grep -c "userProfile:" src/pdf/generateQuotePdf.ts` → 0; shipping row asserted in tests.
- D-22 lock: PDF test asserts Total = 100 + 10 + (100×0.20) = 130 for the {sellingPrice=100, shipping=10, tax=20%} fixture.
- createQuote action: `grep -c "createQuote" src/hooks/useDatabase.ts` ≥ 2; `grep -c "db.transaction" src/hooks/useDatabase.ts` ≥ 1.
- RuntimeQuoteStatus compile-time guard: trying to write `status: 'draft'` into a runtime quote payload fails tsc (verify by attempting the edit locally and observing the error — surface in SUMMARY).
- Build chain: `npm run build` (full chain) exits 0 — all gates green including the static-import gate and the bundle-size gate.
</verification>

<success_criteria>
- `Quote` interface + `QuoteStatus` union (full, includes 'draft') + `RuntimeQuoteStatus = Exclude<QuoteStatus, 'draft'>` (narrow) all exported from src/types.ts per D-17 exact shape.
- Sale gains `convertedFromQuoteId?` back-reference (D-20 prep).
- PrintJob.quoteNumber marked `@deprecated`.
- Dexie v8 with `quotes` store wired in src/db/database.ts; v7→v8 backfill upgrade callback uses the pure helper.
- `backfillQuotesFromJobs` pure helper unit-tested with the locked 3-job → 2-quote fixture (D-17 G7).
- `database.migrations.test.ts` exercises the upgrade end-to-end (or via the documented fallback).
- `useQuotes()` hook exposes the surface plan 16-11 will consume (quotes array, quotesByJobId Map, CRUD + status updates) PLUS the new `createQuote(input): Promise<Quote>` transactional action (per WARNING I-07 Option B) that plan 16-10 will call instead of importing `db` directly.
- `generateQuotePdf(quote: Quote)` signature enforced at the type level — PDF reads only from snapshot.
- Shipping row renders when shippingCost > 0; hidden otherwise (D-15).
- Tax base is sellingPrice only — never (sellingPrice + shippingCost) — confirmed by a numeric test (D-22).
- RuntimeQuoteStatus is the compile-time G6 guard: PrintQuoteModal (16-10) and Convert-to-Sale (16-12) write payloads use `status: RuntimeQuoteStatus`, so the compiler refuses `'draft'` at runtime call sites. Migration code in src/db/backfill.ts is the SOLE place using the wider QuoteStatus.
- JobsManager stopgap Quote keeps the wave clean for plan 16-10 to replace.
- All build gates green: `tsc -b`, `vitest run`, `assert-no-static-jspdf`, `lint-no-raw-html` all exit 0.
</success_criteria>

<output>
Create `.planning/phases/16-printable-pdf-quote/16-09-SUMMARY.md` when done.
</output>
