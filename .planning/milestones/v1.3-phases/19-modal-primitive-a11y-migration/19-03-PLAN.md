---
phase: 19-modal-primitive-a11y-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/InfoTooltip.tsx
  - src/components/ui/InfoTooltip.test.tsx
  - src/components/ui/CollapsibleSection.tsx
autonomous: true
requirements: [A11Y-04, A11Y-08]
must_haves:
  truths:
    - "InfoTooltip's tooltip span uses a useId()-generated id (NOT the static 'info-tooltip-content') (D-13)"
    - "Multiple InfoTooltip instances on the same page have unique tooltip ids"
    - "InfoTooltip's aria-describedby points to the same useId()-generated id when the tooltip is open"
    - "CollapsibleSection body is ALWAYS rendered in the DOM (with hidden={!open}) — never conditionally mounted (D-14)"
    - "CollapsibleSection's aria-controls always references a real DOM id (the body div is always present)"
  artifacts:
    - path: "src/components/ui/InfoTooltip.tsx"
      provides: "InfoTooltip with unique useId() per instance"
      contains: "useId()"
    - path: "src/components/ui/InfoTooltip.test.tsx"
      provides: "Vitest coverage asserting multi-instance unique tooltip ids (A11Y-04)"
      min_lines: 30
    - path: "src/components/ui/CollapsibleSection.tsx"
      provides: "CollapsibleSection with always-rendered hidden body"
      contains: "hidden={!open}"
  key_links:
    - from: "src/components/ui/InfoTooltip.tsx"
      to: "tooltip span id"
      via: "useId() result"
      pattern: "id=\\{tooltipId\\}"
    - from: "src/components/ui/CollapsibleSection.tsx"
      to: "body div"
      via: "always-rendered with hidden attribute"
      pattern: "hidden=\\{!open\\}"
---

<objective>
Two surgical primitive fixes that together close A11Y-04 (InfoTooltip multiple-instance id collision) and A11Y-08 (CollapsibleSection `aria-controls` references missing DOM id).

Both edits are isolated to `src/components/ui/`. Parallel-safe with the modal migrations in plans 19-04 / 19-05 (no file contention).

Purpose: Close 2 a11y findings (1 HIGH + 1 MEDIUM) via the primitive layer so every consumer of these two primitives gets the fix for free.

Output: Two file edits, each touching < 20 lines, plus a new InfoTooltip.test.tsx that asserts the multi-instance unique-id contract (A11Y-04 behavioral test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-modal-primitive-a11y-migration/19-CONTEXT.md

<interfaces>
<!-- Current code locations (must read before editing) -->

src/components/ui/InfoTooltip.tsx — current state (before fix):
- Line 1: `import { useState } from 'react';`
- Line 24: `aria-describedby={open ? 'info-tooltip-content' : undefined}` ← static string
- Line 39: `id="info-tooltip-content"` ← static string, collides if 2+ instances visible at once

src/components/ui/CollapsibleSection.tsx — current state (before fix):
- Line 1: `import { useState, useId, type ReactNode } from 'react';` (useId already imported)
- Line 21: `const bodyId = useId();` (already generates a unique id)
- Line 31: `aria-controls={bodyId}` (always references bodyId)
- Lines 53-57: body is conditionally rendered — `{open && (<div id={bodyId} className="mt-4">{children}</div>)}` ← VIOLATES ARIA spec (aria-controls points to a non-existent element when collapsed)

Pattern to apply for InfoTooltip (D-13): use the same `useId()` pattern already in CollapsibleSection.tsx:21.

Pattern to apply for CollapsibleSection (D-14): switch from `{open && (...)}` to `<div hidden={!open}>...</div>` — the body is always mounted; the HTML `hidden` attribute hides it when collapsed.

Test convention (D-21): raw `createRoot` + `act` per existing tests like JobsManager.test.tsx, DeclineQuoteModal.test.tsx — NO `@testing-library/react`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: InfoTooltip switch from static 'info-tooltip-content' id to useId() + multi-instance unique-id test</name>
  <files>src/components/ui/InfoTooltip.tsx, src/components/ui/InfoTooltip.test.tsx</files>
  <read_first>
    - src/components/ui/InfoTooltip.tsx (current implementation — see lines 24 + 39 for the two `info-tooltip-content` references)
    - src/components/ui/CollapsibleSection.tsx (canonical `useId()` import + usage pattern at lines 1 + 21)
    - .planning/phases/19-modal-primitive-a11y-migration/19-CONTEXT.md (D-13 — replace the static id at line 39 AND update aria-describedby at line 26 / 24 to use the same id; D-21 — raw createRoot + act test convention)
    - src/components/SettingsModal.tsx (the surface with 6+ InfoTooltips visible at once — confirms the multiple-instance use case the fix targets)
    - src/components/DeclineQuoteModal.test.tsx (closest analog for a small, focused vitest using raw createRoot + act)
  </read_first>
  <action>
    Two parts: (a) source edit to InfoTooltip.tsx, (b) new test file InfoTooltip.test.tsx asserting the multi-instance unique-id contract.

    **Part A — Edit src/components/ui/InfoTooltip.tsx:**

    1. Update the import at line 1: change `import { useState } from 'react';` to `import { useState, useId } from 'react';`.
    2. After the existing `const [open, setOpen] = useState(false);` line (currently line 12), add a new line: `const tooltipId = useId();`.
    3. Update the `aria-describedby` reference (currently `aria-describedby={open ? 'info-tooltip-content' : undefined}` at ~line 24): change the literal `'info-tooltip-content'` to `tooltipId`. Final form: `aria-describedby={open ? tooltipId : undefined}`.
    4. Update the tooltip span's `id` attribute (currently `id="info-tooltip-content"` at ~line 39): change to `id={tooltipId}`.

    Critical constraints for Part A:
    - Do NOT change the component's public API (no new props).
    - Do NOT change any styling, click/hover behavior, or the SVG icon markup.
    - Do NOT add any other ARIA attributes.
    - Keep the existing `// allow-raw-html` comment and the `aria-label={text}` on the button.
    - The two id references must be the SAME `tooltipId` value (both come from the single `useId()` call in this component instance).

    **Part B — Create src/components/ui/InfoTooltip.test.tsx:**

    A minimal vitest file using the project's raw `createRoot` + `act` convention (D-21). Imports:
    ```typescript
    import { act } from 'react';
    import { createRoot, type Root } from 'react-dom/client';
    import { describe, it, expect, beforeEach, afterEach } from 'vitest';
    import { InfoTooltip } from './InfoTooltip';
    ```

    Setup pattern (mirror DeclineQuoteModal.test.tsx):
    - `beforeEach`: create a `<div>` container, append to `document.body`, create a `Root` from it.
    - `afterEach`: `act(() => root.unmount())`, remove container from DOM.

    Single `describe('InfoTooltip', () => { ... })` block with at least two `it(...)` blocks:

    1. `it('renders without colliding ids when two instances are mounted')` — render two InfoTooltip instances in the same container via:
       ```tsx
       act(() => {
         root.render(
           <>
             <InfoTooltip text="First tip" />
             <InfoTooltip text="Second tip" />
           </>
         );
       });
       ```
       Then activate both tooltips so their spans render (the source uses `useState(false)` initially — the tooltip span is conditionally rendered when `open` is true). Two options:
       - Option A: Click each tooltip button via `button.dispatchEvent(new MouseEvent('click', { bubbles: true }))` inside an `act` wrapper to set both `open` states to true.
       - Option B (preferred — simpler): Query the two `[aria-describedby]` button attributes directly. Even when `open=false`, the `aria-describedby` resolves to `undefined`, so this won't work without opening. Use Option A.

       After both tooltips are open, query both tooltip span ids:
       ```typescript
       const tooltipSpans = container.querySelectorAll('[role="tooltip"]');
       // Or: container.querySelectorAll('span[id]') filtered to the tooltip spans.
       // If the source uses a different selector, query via the id attribute pattern.
       expect(tooltipSpans.length).toBe(2);
       const id1 = tooltipSpans[0].getAttribute('id');
       const id2 = tooltipSpans[1].getAttribute('id');
       expect(id1).toBeTruthy();
       expect(id2).toBeTruthy();
       expect(id1).not.toBe(id2);
       ```

       Note: read InfoTooltip.tsx first to confirm whether the tooltip span has `role="tooltip"` or is selectable by some other attribute. If the source has no `role="tooltip"`, fall back to selecting by the id pattern emitted by `useId()` (React generates ids like `:r1:`, `:r2:`) — query all elements with a non-empty id under the container and filter to the two tooltip spans by matching the parent button.

    2. `it('does not reuse the static "info-tooltip-content" id')` — render one InfoTooltip, open it, assert `container.querySelector('#info-tooltip-content')` is null. This guards against regression to the pre-fix static id.

    Critical constraints for Part B:
    - Do NOT install `@testing-library/react` or `@testing-library/user-event` (D-22).
    - Use `act(() => { ... })` around every render, re-render, and event dispatch.
    - Reset/clean up DOM in `afterEach`.
    - Keep the test file minimal — only the two assertions above are required. Do NOT expand scope to cover styling, hover behavior, or SVG icon.
  </action>
  <verify>
    <automated>npx tsc -b && npx vitest run src/components/ui/InfoTooltip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/components/ui/InfoTooltip.tsx` imports `useId` from `'react'` (`grep -c "useId" src/components/ui/InfoTooltip.tsx` returns `>= 1`).
    - Source: `src/components/ui/InfoTooltip.tsx` contains `const tooltipId = useId();`.
    - Source: `src/components/ui/InfoTooltip.tsx` does NOT contain the string `'info-tooltip-content'` (`grep -c "info-tooltip-content" src/components/ui/InfoTooltip.tsx` returns `0`).
    - Source: `src/components/ui/InfoTooltip.tsx` contains `aria-describedby={open ? tooltipId : undefined}`.
    - Source: `src/components/ui/InfoTooltip.tsx` contains `id={tooltipId}` on the tooltip span.
    - Source: `src/components/ui/InfoTooltip.test.tsx` exists.
    - Source: `src/components/ui/InfoTooltip.test.tsx` does NOT import from `@testing-library/react` or `@testing-library/user-event`.
    - Source: `src/components/ui/InfoTooltip.test.tsx` contains at least 2 `it(...)` blocks, one of which renders two InfoTooltip instances and asserts their tooltip ids differ.
    - CLI: `npx tsc -b` exits 0.
    - CLI: `npx vitest run src/components/ui/InfoTooltip.test.tsx` exits 0.
    - CLI: `npx vitest run` exits 0 (no existing test regressed).
    - Behavior: rendering two InfoTooltip instances produces two distinct `id` values in the DOM (asserted by the new test).
  </acceptance_criteria>
  <done>
    InfoTooltip now generates a unique `useId()` per instance. Multiple visible tooltips (e.g., the 6+ on SettingsModal's Costs & Rates tab) have unique ids. Public API unchanged. A11Y-04 behavioral contract locked by automated test.
    Atomic commit: `gsd-sdk query commit "fix(19-03): InfoTooltip uses useId() instead of static id + multi-instance test (A11Y-04)" --files src/components/ui/InfoTooltip.tsx src/components/ui/InfoTooltip.test.tsx`
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CollapsibleSection always-rendered hidden body</name>
  <files>src/components/ui/CollapsibleSection.tsx</files>
  <read_first>
    - src/components/ui/CollapsibleSection.tsx (current state — body is conditionally rendered at lines 53-57 via `{open && (...)}`)
    - src/components/ui/CollapsibleSection.test.ts (current tests — confirm they still pass against the new always-rendered structure)
    - .planning/phases/19-modal-primitive-a11y-migration/19-CONTEXT.md (D-14 — exact replacement pattern: `<div id={bodyId} hidden={!open} className="mt-4">{children}</div>`)
  </read_first>
  <action>
    Edit src/components/ui/CollapsibleSection.tsx:

    1. Locate the conditional body block at the end of the JSX (currently lines 53-57):
       ```
       {open && (
         <div id={bodyId} className="mt-4">
           {children}
         </div>
       )}
       ```
    2. Replace with the always-rendered, `hidden`-toggled form:
       ```
       <div id={bodyId} hidden={!open} className="mt-4">
         {children}
       </div>
       ```

    Critical constraints:
    - The `aria-controls={bodyId}` reference on the button (currently line 31) must stay — it now always points to a real DOM element regardless of `open` state.
    - Keep the `id={bodyId}` and the `mt-4` className.
    - The `hidden` attribute is the standard HTML5 attribute (boolean) — React handles `hidden={false}` correctly by omitting the attribute. No additional styling needed.
    - Do NOT change the public API (`CollapsibleSectionProps` unchanged).
    - Do NOT change the button, badge slot, chevron, or any other JSX.
    - Do NOT add `aria-hidden` — the HTML `hidden` attribute already conveys this to assistive tech.
    - If `src/components/ui/CollapsibleSection.test.ts` has any test that asserts the body is absent from the DOM when collapsed (e.g., `expect(container.querySelector('#…')).toBeNull()`), update that assertion to instead assert `body.hidden === true`. Read the existing test first to confirm whether such an assertion exists; if it does, fix it as part of this task (small bounded edit, isolated to one file).
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/CollapsibleSection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/components/ui/CollapsibleSection.tsx` does NOT contain the conditional render pattern `{open && (` for the body (`grep -c "open && (" src/components/ui/CollapsibleSection.tsx` returns `0`).
    - Source: `src/components/ui/CollapsibleSection.tsx` contains `hidden={!open}` on the body div.
    - Source: `src/components/ui/CollapsibleSection.tsx` still contains `aria-controls={bodyId}` on the button.
    - Source: `src/components/ui/CollapsibleSection.tsx` still contains `id={bodyId}` on the body div.
    - CLI: `npx tsc -b` exits 0.
    - CLI: `npx vitest run src/components/ui/CollapsibleSection.test.ts` exits 0.
    - CLI: `npx vitest run` exits 0 (no other test regressed).
    - Behavior: when `defaultOpen={false}`, the body `<div id={bodyId}>` is present in the DOM with `hidden` attribute set to true; expanding (clicking the button) removes the `hidden` attribute.
  </acceptance_criteria>
  <done>
    CollapsibleSection body is always mounted; `aria-controls` always references a real DOM id. Animation foundation laid (future enhancement, not in scope). Public API unchanged.
    Atomic commit: `gsd-sdk query commit "fix(19-03): CollapsibleSection always-render hidden body for aria-controls (A11Y-08)" --files src/components/ui/CollapsibleSection.tsx src/components/ui/CollapsibleSection.test.ts`
    (Include CollapsibleSection.test.ts in the commit only if the test assertion required updating; otherwise commit Just the .tsx file.)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| primitive → consumer DOM | Each primitive renders into consumer-supplied React trees — id collisions must not happen across instances |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Information disclosure | InfoTooltip static id collision | mitigate | Replace static `'info-tooltip-content'` with `useId()` — each instance generates a unique id; locked by automated multi-instance test |
| T-19-08 | Repudiation | CollapsibleSection broken aria-controls | mitigate | Always-render the body so `aria-controls` references a present DOM id — screen reader users get reliable expand/collapse semantics |
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx vitest run` exits 0 (no regression)
- `npx vitest run src/components/ui/InfoTooltip.test.tsx` exits 0 (A11Y-04 multi-instance contract verified)
- Source grep: `grep -c "info-tooltip-content" src/components/ui/InfoTooltip.tsx` returns 0
- Source grep: `grep -c "hidden={!open}" src/components/ui/CollapsibleSection.tsx` returns `>= 1`
- Manual DOM check: rendering two InfoTooltips produces two distinct id attribute values (now also covered by automated test)
- Manual DOM check: a collapsed CollapsibleSection still has its body div in the DOM with `hidden` attribute
</verification>

<success_criteria>
- A11Y-04 closed via InfoTooltip useId() switch + automated multi-instance unique-id test
- A11Y-08 closed via CollapsibleSection always-rendered hidden body
- No public API change to either primitive
- All existing tests still pass; new InfoTooltip test passes
- TypeScript compiles under `tsc -b`
</success_criteria>

<output>
Create `.planning/phases/19-modal-primitive-a11y-migration/19-03-SUMMARY.md` when done — include:
- Both file edits + LOC delta
- The new InfoTooltip.test.tsx — test count and what it asserts
- Confirmation that no public API changed
- Whether CollapsibleSection.test.ts needed updating (and what changed if so)
- `tsc -b` + `vitest run` exit codes (both must be 0)
- A one-line note that the SettingsModal Costs & Rates tab now produces unique tooltip ids (closes the original audit symptom)
</output>
