---
phase: 20-dexie-atomicity-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useDatabase.ts
  - src/hooks/useDatabase.test.ts
autonomous: true
threat_model_required: true
requirements:
  - DATA-01
user_setup: []

must_haves:
  truths:
    - "Sale mutations (addSale/deleteSale/updateSale) are atomic across db.sales + db.jobs — if jobs.put throws mid-tx, the sale write rolls back"
    - "No user-visible behavior change — hardening-only milestone"
    - "Vitest-spy rollback pattern is empirically validated against jsdom before further DATA-01 tests are locked in"
  artifacts:
    - path: "src/hooks/useDatabase.test.ts"
      provides: "Transaction-boundary unit tests for addSale, deleteSale, updateSale + reusable test scaffolding for DATA-02"
      contains: "describe('useSales transactions')"
    - path: "src/hooks/useDatabase.ts"
      provides: "addSale (L572), deleteSale (L585), updateSale (L601) wrapped in db.transaction('rw', db.sales, db.jobs, ...)"
      contains: "db.transaction('rw', db.sales, db.jobs"
  key_links:
    - from: "src/hooks/useDatabase.ts:addSale"
      to: "src/components/JobsManager.tsx:1490-1501 (Convert-to-Sale template)"
      via: "verbatim Dexie transaction shape — drop db.quotes from the table list"
      pattern: "db\\.transaction\\('rw',\\s*db\\.sales,\\s*db\\.jobs"
    - from: "src/hooks/useDatabase.test.ts"
      to: "vi.spyOn(db.jobs, 'put').mockRejectedValueOnce"
      via: "Wave 0 spike: prove Vitest spies trigger Dexie rollback against jsdom"
      pattern: "mockRejectedValueOnce"
---

<objective>
Wrap `addSale`, `deleteSale`, and `updateSale` in `useDatabase.ts` (lines 572–615) in a single `db.transaction('rw', db.sales, db.jobs, ...)` so a thrown exception or tab close between the sale write and the `job.copiesSold` bump rolls the entire mutation back. The Convert-to-Sale path at `JobsManager.tsx:1490-1501` is the in-production template — mirror it verbatim with `db.quotes` removed.

Purpose: Close CODE-AUDIT #4 (DATA-01, HIGH severity — partial-write data corruption). The non-atomic shape today leaves Sale rows that reference jobs whose `copiesSold` counter was never updated; the `reconcileCopiesSoldFromSales` self-heal at `useDatabase.ts:470-488` masks the bug on next page load, so users never see the inconsistency surface, but the data-integrity invariant ("Sale.quantity sum equals job.copiesSold across atomic writes") is currently violated mid-flight.

Output: All three sale mutations atomic; new `src/hooks/useDatabase.test.ts` proves rollback via Vitest spy on `db.jobs.put`; scoped vitest run green; `npm run build` chain green (`tsc -b` strict).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-dexie-atomicity-audit/20-CONTEXT.md
@.planning/phases/20-dexie-atomicity-audit/20-RESEARCH.md
@.planning/phases/20-dexie-atomicity-audit/20-PATTERNS.md
@.planning/phases/20-dexie-atomicity-audit/20-VALIDATION.md
@src/hooks/useDatabase.ts
@src/components/JobsManager.tsx
@src/db/backfill.test.ts

<interfaces>
<!-- Key contracts the executor needs. No exploration required. -->

From src/types.ts (already imported in useDatabase.ts:4):
```typescript
export interface Sale {
  id: string;
  jobId: string;
  quantity: number;
  unitPrice: number;
  totalRevenue: number;
  soldAt: Date;
  // ...optional customer fields...
}

export interface PrintJob {
  id: string;
  name: string;
  copiesSold: number;
  updatedAt: Date;
  // ...
}
```

From src/hooks/useDatabase.ts:572-615 (current non-atomic shape — the diff surface):
```typescript
const addSale = useCallback(async (sale: Sale) => {
  await db.sales.add(sale);
  const job = await db.jobs.get(sale.jobId);
  if (job) {
    await db.jobs.put({
      ...job,
      copiesSold: job.copiesSold + sale.quantity,
      updatedAt: new Date(),
    });
  }
}, []);

const deleteSale = useCallback(async (sale: Sale) => {
  await db.sales.delete(sale.id);
  const job = await db.jobs.get(sale.jobId);
  if (job) {
    await db.jobs.put({
      ...job,
      copiesSold: Math.max(0, job.copiesSold - sale.quantity),
      updatedAt: new Date(),
    });
  }
}, []);

const updateSale = useCallback(async (updated: Sale) => {
  const previous = await db.sales.get(updated.id);
  await db.sales.put(updated);
  if (previous && previous.quantity !== updated.quantity) {
    const job = await db.jobs.get(updated.jobId);
    if (job) {
      const delta = updated.quantity - previous.quantity;
      await db.jobs.put({
        ...job,
        copiesSold: Math.max(0, job.copiesSold + delta),
        updatedAt: new Date(),
      });
    }
  }
}, []);
```

From src/components/JobsManager.tsx:1490-1501 (the production template to mirror, with `db.quotes` removed):
```typescript
await db.transaction('rw', db.sales, db.quotes, db.jobs, async () => {
  await db.sales.add(sale);
  await db.quotes.put(quotePatch);
  const jobRow = await db.jobs.get(sale.jobId);
  if (jobRow) {
    await db.jobs.put({
      ...jobRow,
      copiesSold: jobRow.copiesSold + sale.quantity,
      updatedAt: new Date(),
    });
  }
});
```

From src/db/backfill.test.ts:46-77 (reusable fixture helpers — DO NOT duplicate; import or copy verbatim):
```typescript
function makeMinimalJob(overrides: Partial<PrintJob>): PrintJob { /* ... */ }
function makeMinimalSale(overrides: Partial<Sale>): Sale { /* ... */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — spike `useDatabase.test.ts` and prove Vitest-spy rollback against jsdom</name>
  <files>src/hooks/useDatabase.test.ts</files>
  <read_first>
    - src/components/JobsManager.tsx:1485-1510 (Convert-to-Sale template — the verbatim Dexie transaction shape, including `async () => { ... }` scope function and the spread/put idiom for `job.copiesSold`)
    - src/hooks/useDatabase.ts:564-635 (addSale L572, deleteSale L585, updateSale L601, plus the `useSales` hook envelope so the test imports the hook correctly)
    - src/db/backfill.test.ts:1-115 (Dexie-domain fixture helpers `makeMinimalJob` / `makeMinimalSale` + `describe`/`it`/`expect` import shape from vitest)
    - src/pdf/generateQuotePdf.test.ts:1-12 (top-level `vi.mock` idiom — the closest in-codebase analog for mocking a module the test imports indirectly)
    - .planning/phases/20-dexie-atomicity-audit/20-RESEARCH.md (§ R1, A2 — Vitest-spy Wave 0 risk; § "Validation Architecture" sampling commands; § V2 for the three concrete transaction shapes the implementation task uses)
    - .planning/phases/20-dexie-atomicity-audit/20-VALIDATION.md (§ Wave 0 Requirements; § Per-Task Verification Map rows DATA-01a/b/c)
  </read_first>
  <behavior>
    Wave 0 spike: prove the Vitest-spy rollback assertion pattern works against Dexie's transaction zone in jsdom BEFORE the implementation task locks in. This is a load-bearing risk per RESEARCH.md A2 — if `vi.spyOn(db.jobs, 'put').mockRejectedValueOnce(...)` does not trigger a Dexie rollback (because the spy escapes the transaction zone), the whole DATA-01 test plan must fall back to `tx.abort()` inside the test's own `db.transaction` call.

    Test file structure (single new file, three describe blocks anticipated for Wave-2 DATA-02 work):
    - File header: `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';` + import `db` from `'../db/database'` + import `{ useSales }` from `'./useDatabase'` (the hook's `addSale` callback is exposed on the returned object; tests call the underlying `db` writes directly OR exercise the hook via `renderHook` from `@testing-library/react` if already installed — if not, drive `db` writes directly through the same code path the hook uses, which is verifiable because the hook does no extra work beyond the documented body).
    - Import the `makeMinimalJob` and `makeMinimalSale` fixture helpers from `'../db/backfill.test.ts'` IF Vitest can resolve a `.test.ts` import (it can — `vitest` treats them as plain modules). Otherwise duplicate the two factories verbatim with a comment `// duplicated from src/db/backfill.test.ts:46-77 — keep in sync` (per RESEARCH.md Architectural Responsibility Map: test-helper duplication is acceptable when the shared file is itself a test).

    Wave 0 spike test (the ONLY test in this task — the implementation task adds the other two and the success-path tests):
    - `describe('useSales transactions — Wave 0 Vitest-spy spike (DATA-01)')` block
    - `it('vi.spyOn(db.jobs, "put").mockRejectedValueOnce triggers a Dexie rollback of db.sales.add')` — seeds the db with one job via `await db.jobs.add(makeMinimalJob({ id: 'job-x', copiesSold: 0 }))`, then attempts a single `db.transaction('rw', db.sales, db.jobs, async () => { await db.sales.add(sale); ...await db.jobs.put(...) ... })` directly (mirror addSale's target shape exactly), with `db.jobs.put` spied to reject once. Assert: (a) the awaited transaction promise rejects with the simulated error, (b) `await db.sales.get(sale.id)` returns `undefined` (proves rollback), (c) `await db.jobs.get('job-x')` still shows `copiesSold === 0` (no half-write).
    - If jsdom lacks IndexedDB at module import time (RESEARCH.md R1 says it does), the test will fail at the first `db.sales.add` call. In that case the fallback is `expect.fail('jsdom lacks IDB — defer DATA-01 rollback test to Phase 23 TEST-04 once fake-indexeddb lands; document this discovery in 20-01-SUMMARY.md and proceed to implementation without the rollback assertion')`. Either outcome is acceptable for Wave 0 — the goal is to learn the truth, not to pass.

    Cleanup: `beforeEach(async () => { await db.sales.clear(); await db.jobs.clear(); })` so test fixtures do not leak across tests.
  </behavior>
  <action>Create `src/hooks/useDatabase.test.ts` per the `<behavior>` block above. Use `describe('useSales transactions — Wave 0 Vitest-spy spike (DATA-01)')` as the outer block. Import `db` from `../db/database` and reuse `makeMinimalJob` / `makeMinimalSale` factories. Run `npx vitest run src/hooks/useDatabase.test.ts` and observe the outcome. If the rollback assertion passes, proceed to Task 2 with the empirical pattern locked in (write a short note at the top of the file: `// Wave 0 spike PASSED 2026-MM-DD — vi.spyOn rollback against jsdom works`). If it fails because jsdom lacks IDB, write `// Wave 0 spike FAILED 2026-MM-DD — jsdom lacks IDB; DATA-01 rollback assertions deferred to Phase 23 TEST-04. Tests below assert read-then-write call ORDER only, not atomicity.` Whichever path runs, Task 2 builds on the result.</action>
  <verify>
    <automated>npx vitest run src/hooks/useDatabase.test.ts</automated>
  </verify>
  <done>`src/hooks/useDatabase.test.ts` exists with a single Wave 0 spike test. Test outcome (pass OR documented-fail) is recorded as a comment at the top of the file. Vitest exits with status 0 (either because the test passed, OR because the `expect.fail` documents the limitation as an intended assertion). Decision recorded for Task 2: "spy approach works" or "fallback to call-order assertions only".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap addSale/deleteSale/updateSale in `db.transaction('rw', db.sales, db.jobs, ...)` + author the three rollback/success tests</name>
  <files>src/hooks/useDatabase.ts, src/hooks/useDatabase.test.ts</files>
  <read_first>
    - src/components/JobsManager.tsx:1485-1510 (the in-production Convert-to-Sale template — copy/paste the `db.transaction('rw', ..., async () => { ... })` shape verbatim, then delete the `db.quotes.put(quotePatch)` line and drop `db.quotes` from the table list)
    - src/hooks/useDatabase.ts:564-635 (the entire `useSales` hook envelope — the three mutations to wrap; pay special attention to L598-600 which document why `updateSale` does delta math)
    - src/hooks/useDatabase.test.ts (the Wave 0 spike file from Task 1 — its top-of-file comment tells you whether to write full rollback assertions or call-order assertions)
    - .planning/phases/20-dexie-atomicity-audit/20-RESEARCH.md (§ V2 — verbatim addSale/deleteSale/updateSale transaction bodies; § V5 — no side effects in current bodies, so wrap with no extra refactor; § R4 — useLiveQuery emission semantics: tests assert FINAL state after await, NOT emission counts)
    - .planning/phases/20-dexie-atomicity-audit/20-PATTERNS.md (§ Shared Pattern A; § File 1 — exact mirror guidance)
  </read_first>
  <behavior>
    Three production-code changes (all in `src/hooks/useDatabase.ts`):

    - `addSale` (L572-583) — wrap the existing two-step body in `await db.transaction('rw', db.sales, db.jobs, async () => { /* body verbatim */ });`. No body changes, only the transaction envelope. Closes DATA-01a.
    - `deleteSale` (L585-596) — same envelope wrap; body verbatim (the `Math.max(0, job.copiesSold - sale.quantity)` stays). Closes DATA-01b.
    - `updateSale` (L601-615) — same envelope wrap around the existing read-then-conditional-write body. The `if (previous && previous.quantity !== updated.quantity)` branch stays inside the transaction so the delta read is consistent with the job read. Closes DATA-01c.

    No new imports needed (`db` is already in scope at L3). No changes to the hook return value. No changes to consumers — the public Promise signature is identical (`Promise<void>`). useLiveQuery cache (`useSales`, `useJobs`) auto-re-emits on commit; on rollback ZERO emissions fire — this is a UX win, not a regression (RESEARCH.md R4).

    Three new tests in `src/hooks/useDatabase.test.ts` (the Wave 0 spike file from Task 1 stays — append three new describe blocks below it OR convert the spike block into the first of the three). The shape adapts to Task 1's outcome:

    - **If Wave 0 spike passed (spy triggers rollback):** write proper atomicity tests.
      - `it('addSale rolls back the sale write when jobs.put throws mid-tx')` — seed one job, spy `db.jobs.put` to reject once, call the addSale code path (transaction directly OR via `useSales().addSale`), assert: tx rejects, `db.sales.get(saleId)` returns `undefined`, `db.jobs.get(jobId).copiesSold === 0`.
      - `it('deleteSale rolls back the delete when jobs.put throws mid-tx')` — same shape; seed sale + job, attempt delete, sale still present after rollback.
      - `it('updateSale rolls back the put when jobs.put throws mid-tx')` — same shape; seed previous sale, attempt update with new quantity, previous sale still present (not overwritten) after rollback.
      - Plus one success-path test per mutation that confirms the happy path still works post-wrap (sale present, copiesSold updated correctly).
    - **If Wave 0 spike failed (jsdom lacks IDB):** write call-order assertion tests only.
      - `it('addSale opens a transaction over db.sales and db.jobs before any write')` — spy on `db.transaction` and assert it was called with `('rw', db.sales, db.jobs, expect.any(Function))`. Repeat the shape for delete/update.
      - This DOES NOT prove atomicity, but it proves the transaction envelope is correctly applied. RESEARCH.md notes this as the documented fallback. The actual rollback proof gets deferred to Phase 23 TEST-04 (`fake-indexeddb`).

    Test file organization: use `describe('useSales transactions (DATA-01)', () => { describe('addSale', ...); describe('deleteSale', ...); describe('updateSale', ...); })`. Each inner describe contains the rollback test (or call-order test) + the happy-path test. Per CONTEXT.md "Claude's Discretion": choosing one file with nested describes is the lower-friction option vs three separate files.
  </behavior>
  <action>
    Modify `src/hooks/useDatabase.ts` per D-01 (CONTEXT.md decision #1) — wrap `addSale` (L572), `deleteSale` (L585), and `updateSale` (L601) bodies in `await db.transaction('rw', db.sales, db.jobs, async () => { /* existing body verbatim */ });`. Do NOT touch any other line in the file. Do NOT add new imports. Do NOT refactor body logic. The `updateSale` body's `if (previous && previous.quantity !== updated.quantity)` branch stays inside the transaction.

    Then extend `src/hooks/useDatabase.test.ts` with three nested `describe` blocks (`addSale`, `deleteSale`, `updateSale`) under a top-level `describe('useSales transactions (DATA-01)')`. The Task 1 spike block stays — the new code uses the spike's verdict (pass → atomicity assertions; fail → call-order assertions). Six tests total in this task (3 mutations × 2 cases: success + spy/order).

    Run the build chain (`npm run build`) and confirm `tsc -b` accepts the transactional shape with zero new errors. Run `npx vitest run src/hooks/useDatabase.test.ts` and confirm green.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useDatabase.test.ts && tsc -b</automated>
    <human-check>Eyeball the diff on `src/hooks/useDatabase.ts:572-615` — confirm only the three transaction envelopes were added; no body line changed; no new imports.</human-check>
  </verify>
  <done>
    - `src/hooks/useDatabase.ts:572-615` shows three `await db.transaction('rw', db.sales, db.jobs, async () => { ... });` envelopes, each wrapping the prior body verbatim.
    - `npx vitest run src/hooks/useDatabase.test.ts` exits 0 with seven tests passing (1 Wave 0 spike + 6 mutation tests).
    - `tsc -b` exits 0 with zero new errors.
    - `grep -c "db.transaction('rw', db.sales, db.jobs," src/hooks/useDatabase.ts` returns at least 3 (one per mutation; may show 4 if `createQuote` work has not yet wrapped its own scope — that is fine).
    - No new files apart from `useDatabase.test.ts`. No imports changed in production code.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React component → useSales hook → Dexie | Unverified user input (Sale rows) crosses into IndexedDB writes; rollback semantics protect on-disk invariants |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-DATA-INTEGRITY | Tampering (mid-write corruption from non-atomic multi-store mutation) | useDatabase.ts `addSale`/`deleteSale`/`updateSale` (L572-615) | mitigate | Wrap each mutation in `db.transaction('rw', db.sales, db.jobs, async () => { ... })` — mirror the in-production Convert-to-Sale template at JobsManager.tsx:1490. Closes DATA-01 (CODE-AUDIT #4, HIGH). |
| T-20-01-RACE-CONDITION | Tampering (concurrent sale writes racing the `db.jobs.get` → `db.jobs.put` read-modify-write) | useDatabase.ts `addSale`/`updateSale` | mitigate | Wrapping in `db.transaction('rw')` serializes IDB writes within the transaction zone — concurrent calls queue rather than interleave the read-modify-write. |
| T-20-01-NO-NEW-SURFACE | (none) | — | accept | No new attack surface added; this is an internal hardening pass. No new dependencies, no new env vars, no new endpoints, no new HTML rendering. The change is invisible to UI consumers. |

No package installs in this plan — npm/pip/cargo package legitimacy gate not applicable (no `Package Legitimacy Audit` section required in RESEARCH.md for this plan).
</threat_model>

<verification>
- `npx vitest run src/hooks/useDatabase.test.ts` exits 0 with 7 tests passing.
- `tsc -b` exits 0 with zero new errors.
- `npm run build` exits 0 — the full chain (lint-no-raw-html, assert-no-static-jspdf, vitest --coverage, tsc -b, vite build, assert-bundle-size, assert-no-pdf-preload, assert-no-static-pdf-import) all green.
- `grep -c "db.transaction('rw', db.sales, db.jobs," src/hooks/useDatabase.ts` returns ≥ 3.
- File `src/hooks/useDatabase.test.ts` contains a comment at the top of file recording the Wave 0 spike verdict (pass or documented-fail) so downstream plan 20-02 inherits the test infrastructure with provenance.
</verification>

<success_criteria>
- DATA-01 closes: addSale, deleteSale, and updateSale each wrapped in a single `db.transaction('rw', db.sales, db.jobs, ...)` mirroring the Convert-to-Sale pattern at JobsManager.tsx:1490.
- `src/hooks/useDatabase.test.ts` is the new test scaffold consumed by plan 20-02 for the DATA-02 createQuote tx-scoped read test.
- Vitest-spy rollback Wave 0 risk (RESEARCH.md A2) is empirically resolved — outcome recorded for downstream plans.
- Zero user-visible behavior change. Existing `reconcileCopiesSoldFromSales` self-heal at useDatabase.ts:470 is no longer load-bearing for correctness (it stays as a defense-in-depth).
- Build chain green; phase 20 Wave 1 task complete.
</success_criteria>

<output>
Create `.planning/phases/20-dexie-atomicity-audit/20-01-SUMMARY.md` when done. Summary MUST record:
- Wave 0 spike verdict (spy works / fallback to call-order) — Phase 23 TEST-04 author needs this breadcrumb.
- Total tests added (target: 7).
- Vitest scoped run time + full-suite time.
- Any deviations from the verbatim template at JobsManager.tsx:1490 (expected: none).
</output>
