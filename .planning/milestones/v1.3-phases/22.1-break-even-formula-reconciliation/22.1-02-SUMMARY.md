---
phase: 22.1-break-even-formula-reconciliation
plan: 02
subsystem: ui
tags: [break-even, formula, snapshot, calculator, jobs-manager, round-trip, perf-08, tdd]
requires:
  - phase: 22.1-01
    provides: PrintJob.fixedCostsAtSave optional schema field + reconcileFixedCostsAtSave Path B helper + fixedCostsReconcileRan module flag wired into useJobs()
provides:
  - JobsManager.computeBreakEvenInfo widened numerator (modelCost + depreciation + nozzleWear), exported for tests
  - JobsManager Infinity guard widened (`fixedNumerator > 0` not `job.modelCost > 0`)
  - CostCalculator writes fixedCostsAtSave on Create + Update paths from existing fixedCosts memo
  - PERF-08 round-trip describe (4 it cases) in JobsManager.test.tsx locking Calculator formula === pill breakEvenCopies
affects:
  - Plan 22.1-03 (RecordSaleModal carry-overs) — the pill formula is now the canonical break-even reference
  - Plan 22.1-04 (final verification & doc closure)
tech-stack:
  added: []  # no new packages; purely formula/UI changes
  patterns:
    - "Schema-extension snapshot at Save/Update time (Phase 22.1 D-03)"
    - "Pure module-scope helper exported for unit test (mirrors OrdersQuoteRows / SaleFromQuoteSubtext / computeBreakEvenInfo)"
    - "Round-trip test locking two implementations of the same formula (Calculator memo vs pill helper)"
key-files:
  created:
    - .planning/phases/22.1-break-even-formula-reconciliation/22.1-02-SUMMARY.md
  modified:
    - src/components/JobsManager.tsx (export computeBreakEvenInfo + widened numerator + widened Infinity guard, +13/-3 lines)
    - src/components/CostCalculator.tsx (fixedCostsAtSave on Update path line 605 + Create path line 647, +14/0 lines)
    - src/components/JobsManager.test.tsx (PERF-08 describe block w/ 4 it cases + imports, +138/-2 lines)
key-decisions:
  - "D-01 widened numerator implemented as `job.modelCost + (job.fixedCostsAtSave?.depreciation ?? 0) + (job.fixedCostsAtSave?.nozzleWear ?? 0)` — legacy jobs without snapshot fall back to modelCost alone via `?? 0` defaults"
  - "Infinity guard widened from `job.modelCost > 0` → `fixedNumerator > 0` (RESEARCH Open Question #2) — zero-modelCost + non-zero depreciation jobs now surface a finite break-even"
  - "D-03 snapshot written from the existing CostCalculator `fixedCosts` memo (line 449-457) — NOT recomputed; mirrors what the Calculator widget displayed at Save time"
  - "D-04 Edit-form recompute behavior preserved — the form's live depreciation+nozzleWear are unchanged; only the job's fixedCostsAtSave is touched on Save click"
  - "D-17 export — `computeBreakEvenInfo` now exported (was module-scope-only) so JobsManager.test.tsx can import and assert against it without rendering JobsManager (mirrors OrdersQuoteRows / SaleFromQuoteSubtext / JobCard / ADD_TAG_PLACEHOLDER exports)"
patterns-established:
  - "Schema-extension snapshot pattern (Plan 22.1-01 D-02 → Plan 22.1-02 D-03 producer + Plan 22.1-02 D-01 consumer): producer writes optional snapshot at save time, consumer reads with optional-chain + nullish-coalesce, reconcile helper backfills legacy records"
  - "Consumer-side cross-validation of pure-helper no-op contracts in component tests (TDD acceptance contract — `must_haves.truths[5]` 4th case)"
requirements-completed: [PERF-08]
duration: ~25min
completed: 2026-05-28
---

# Phase 22.1 Plan 02: Formula switch + round-trip test Summary

**JobsManager break-even pill now matches the Calculator widget's formula on the same job; PERF-08 round-trip describe (4 it cases) locks the agreement.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-05-28T08:42:37Z
- **Completed:** 2026-05-28T08:48:18Z
- **Tasks:** 2 (TDD pair — RED then GREEN)
- **Files modified:** 3 (JobsManager.test.tsx, JobsManager.tsx, CostCalculator.tsx)

## Accomplishments

- **PERF-08 closed.** Calculator displayed Break-even Units === JobsManager pill `breakEvenCopies` for the same job, locked by 4 round-trip it cases.
- **CostCalculator writes the snapshot at both Save (Create) AND Update paths.** Reads the existing `fixedCosts` memo (depreciation + nozzleWear from `costs.printerDepreciation` + `costs.nozzleWear`) so the snapshot is byte-identical to what the Calculator's "Break-even Units" widget displayed at Save time.
- **JobsManager `computeBreakEvenInfo` widened.** Numerator now `job.modelCost + (job.fixedCostsAtSave?.depreciation ?? 0) + (job.fixedCostsAtSave?.nozzleWear ?? 0)`. Infinity guard widened from `job.modelCost > 0` to `fixedNumerator > 0` (zero-modelCost jobs with non-zero depreciation surface a finite break-even — RESEARCH Open Question #2 resolved).
- **`computeBreakEvenInfo` is now exported.** Mirrors the existing OrdersQuoteRows / SaleFromQuoteSubtext / JobCard / ADD_TAG_PLACEHOLDER export pattern so tests can import without rendering.
- **Consumer-side cross-validation of `reconcileFixedCostsAtSave` no-op contract.** The 4th it case calls the helper from Wave 1 against a job that already has `fixedCostsAtSave` set and asserts `[]` — proves the helper is a no-op for newly-saved jobs from the consumer side, complementing the producer-side test in `src/db/backfill.test.ts`.

## Task Commits

Each task committed atomically (TDD RED → GREEN):

1. **Task 1 (RED): Add PERF-08 round-trip describe to JobsManager.test.tsx** — `5fef938` (test)
2. **Task 2 (GREEN): Widen break-even numerator + write fixedCostsAtSave on Save/Update** — `9793c91` (feat)

**Plan metadata** (this summary + its commit) will follow.

## Files Created/Modified

- `src/components/JobsManager.tsx` — Added `export` to `computeBreakEvenInfo` (line 49). Replaced lines 65-67 (old narrow numerator) with `fixedNumerator` block + widened Infinity guard (now at lines 64-77). Surrounding `effectiveProfitPerUnit`, `Number.isFinite` guard, `remainingToBreakEven`, return object — all untouched (byte-identical).
- `src/components/CostCalculator.tsx` — Added `fixedCostsAtSave: { depreciation: fixedCosts.depreciation, nozzleWear: fixedCosts.nozzleWear }` line at:
  - Line 605 (Update path, inside `updatedJob` literal, after `tags: editingJob.tags`)
  - Line 647 (Create path, inside `job` literal, after `copiesSold: 0`)
  - Both lines preceded by a multi-line header comment naming D-03 and noting the reads come from the existing `fixedCosts` memo at line 449-457.
- `src/components/JobsManager.test.tsx` — Added imports for `Sale`, `PrinterInstance`, `PrinterConfig`, `Material`, and `reconcileFixedCostsAtSave`. Extended the dynamic import line to include `computeBreakEvenInfo`. Appended new `describe('PERF-08 — break-even formula round-trip (Phase 22.1)')` block at end of file with 4 it cases (3 formula identity + 1 reconcile no-op cross-check) + a `makeJob` fixture helper.

## Decisions Made

All from PLAN — no decisions of my own.

- **D-01 implemented verbatim** — `fixedNumerator` block at JobsManager.tsx lines 64-69; Infinity guard widened to `fixedNumerator > 0 ? Infinity : 0` at line 77.
- **D-03 producer-side implemented at both paths** — snapshot reads `fixedCosts.depreciation` + `fixedCosts.nozzleWear` from the existing memo (D-04 is honored — the memo computes from current printer state, but the JOB's snapshot is only touched on Save click).
- **D-04 honored** — the Calculator's `fixedCosts` useMemo (line 449-457) is unchanged. Edit-form's live depreciation+nozzleWear continues to track current printer settings; only the job's `fixedCostsAtSave` is written at Save time.
- **D-11 round-trip test (case #2 of 4)** + **must_haves.truths[5] consumer-side cross-validation** — 4 it cases:
  1. snapshotted job — expected 7 (`Math.ceil((100+20+5)/20)`)
  2. legacy job (no snapshot) — expected 5 (`Math.ceil(100/20)`, fallback)
  3. zero-modelCost + non-zero depreciation — expected 25 (`Math.ceil(25/1)`, validates widened Infinity guard)
  4. `reconcileFixedCostsAtSave` no-op on newly-saved jobs — expected `[]`

## Deviations from Plan

**None — plan executed exactly as written.**

The plan was unusually precise — every line number, every grep string, every test case expected value was pre-computed in the must_haves block. Implementation was mechanical translation:

1. Task 1 imports + describe block placement matched line numbers given (test file 758→896 lines).
2. Task 2 `export function computeBreakEvenInfo` (single edit at line 49), numerator replacement (line 61-63 → fixedNumerator block), and the two CostCalculator inserts at lines 599 and 635 all landed exactly where the plan specified.
3. tsc -b exits 0, full vitest suite 28/28 files / 436/437 tests pass (1 todo — the pre-existing tax test).

### One environmental note (not a code deviation)

This worktree spawned with no `node_modules/` directory. Wave 1's `deferred-items.md` documented the same gap. To run `npx tsc -b` and `npx vitest run` I symlinked the parent project's `node_modules` into the worktree (`ln -s /Users/marcusdickinson/Projects/3DCoster/node_modules ./node_modules`) and then `npm install --no-save` ran on the parent project to add the four packages Wave 1 flagged as missing (`react-window`, `@tauri-apps/plugin-dialog`, `@tauri-apps/plugin-fs`, `jspdf`, `jspdf-autotable`, `rollup-plugin-visualizer`). The symlink is not tracked by git (gitignored), and the parent project's `package.json` was NOT modified (`--no-save` flag).

After this setup: `npx tsc -b` exits 0; `npm test` reports 28/28 files, 436/437 tests pass (1 todo).

## Issues Encountered

None. The plan was atomic, locally-verifiable, and all expected test values matched the actual implementation results on first GREEN run.

## User Setup Required

None — no external service configuration required. This is a pure formula/UI change touching only the user's own local IndexedDB jobs.

## Threat Flags

None. The plan's `<threat_model>` correctly identified zero new attack surfaces:
- T-22.1-03 (info disclosure — pill shows slightly different number for legacy jobs) — accepted; this is the intended behavior. Legacy jobs (no snapshot) continue to display the old `modelCost`-only break-even via the `?? 0` fallback until `reconcileFixedCostsAtSave` (Wave 1) backfills them on next page load.
- T-22.1-04 (tampering — local-only write to additive field) — accepted; existing save path already trusted.

No new packages, no new permissions, no schema-version bump. (Wave 1 confirmed `db.version` and the schema string are unchanged; my Wave 2 changes touch zero Dexie schema code.)

## Next Phase Readiness

- **Plan 22.1-03 (RecordSaleModal carry-overs)** — unblocked. The pill formula is now canonical and snapshot is written on Save.
- **Plan 22.1-04 (final verification & doc closure)** — unblocked. The 4 PERF-08 round-trip cases lock the contract; Plan 22.1-04 just needs to record completion.

No blockers. No concerns.

## Verification (final)

- `npx tsc -b` exits 0 (after symlinking parent `node_modules` + installing the same 6 pre-existing-missing packages Wave 1 deferred).
- `npx vitest run` — 28/28 test files green, 436/437 tests pass (1 todo is the pre-existing tax test; unrelated to this plan).
- `npx vitest run src/components/JobsManager.test.tsx --reporter=verbose` confirms 4/4 PERF-08 cases green:
  - "snapshotted job — pill breakEvenCopies === Math.ceil((modelCost + depreciation + nozzleWear) / profitPerUnit)" ✓
  - "legacy job (no snapshot) — pill falls back to Math.ceil(modelCost / profitPerUnit)" ✓
  - "zero-modelCost + non-zero depreciation — pill returns finite breakEven (Infinity guard widened to fixedNumerator > 0)" ✓
  - "reconcileFixedCostsAtSave is a no-op on newly-saved jobs (already-snapshotted job returns empty patch array)" ✓
- Grep gates (per acceptance criteria):
  - `grep -c "^export function computeBreakEvenInfo" src/components/JobsManager.tsx` → 1
  - `grep -c "fixedNumerator" src/components/JobsManager.tsx` → 4 (declaration + Math.ceil call + Infinity-guard comparison + one usage in comment)
  - `grep -nE "fixedNumerator > 0 \? Infinity : 0" src/components/JobsManager.tsx` → 1 (line 77)
  - `grep -nE "job\.modelCost > 0 \? Infinity : 0" src/components/JobsManager.tsx` → 0 (old guard fully gone)
  - `grep -c "fixedCostsAtSave: { depreciation: fixedCosts.depreciation, nozzleWear: fixedCosts.nozzleWear }" src/components/CostCalculator.tsx` → 2 (Update at 605, Create at 647)
  - `grep -c "describe.*PERF-08.*break-even formula round-trip.*Phase 22.1" src/components/JobsManager.test.tsx` → 1
  - `grep -c "reconcileFixedCostsAtSave" src/components/JobsManager.test.tsx` → 5 (import + 4 references inside the 4th case + JSDoc)
  - `grep -c "expect(result).toEqual(\[\])" src/components/JobsManager.test.tsx` → 1 (the 4th case assertion)
  - `grep -c "computeBreakEvenInfo" src/components/JobsManager.test.tsx` → 5 (import + 3 inline calls + 1 reference)

## Commits

- `5fef938` — `test(22.1-02): add PERF-08 round-trip describe to JobsManager.test.tsx` (RED: 4 new it cases referencing `computeBreakEvenInfo` + `reconcileFixedCostsAtSave`; until Task 2 lands the export, the test file fails at import time — intentional TDD RED)
- `9793c91` — `feat(22.1-02): widen break-even numerator + write fixedCostsAtSave on Save/Update` (GREEN: `export` keyword + `fixedNumerator` block in JobsManager.tsx + 2 `fixedCostsAtSave` writes in CostCalculator.tsx; all 4 PERF-08 cases turn green)

## Self-Check: PASSED

**1. Created/modified files exist:**
- `src/components/JobsManager.tsx` — FOUND
- `src/components/JobsManager.test.tsx` — FOUND
- `src/components/CostCalculator.tsx` — FOUND
- `.planning/phases/22.1-break-even-formula-reconciliation/22.1-02-SUMMARY.md` — being committed in the metadata commit immediately following this file

**2. Commits exist:**
- `5fef938` — FOUND in `git log --all` (test commit)
- `9793c91` — FOUND in `git log --all` (feat commit)

**3. Test outcomes match claims:**
- `npx vitest run src/components/JobsManager.test.tsx` reports 35 passed / 0 failed — matches "all 4 PERF-08 cases green" claim (the 4 PERF-08 cases sit alongside the 31 pre-existing JobsManager.test.tsx cases; total 35 — observed and verified).
- `npx vitest run` reports 28 files passed / 436 tests passed + 1 todo — matches "full suite green" claim.
- `npx tsc -b` exits 0 — matches "tsc clean" claim.

---
*Phase: 22.1-break-even-formula-reconciliation*
*Plan: 02*
*Completed: 2026-05-28*
