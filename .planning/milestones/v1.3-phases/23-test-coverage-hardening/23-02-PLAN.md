---
phase: 23-test-coverage-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CustomerCsvImportModal.test.tsx
  - src/components/CustomerLibrary.test.tsx
autonomous: true
requirements: [TEST-02, TEST-03]
tags: [test, customer, csv, library]

must_haves:
  truths:
    - "CustomerCsvImportModal.test.tsx exists at the path locked by D-12 (sibling to source) and covers: non-CSV file → WR-06 error, valid CSV → preview step transition, dedup-mode toggle (skip vs overwrite), row select/deselect, confirm-import call shape (per TEST-02 + ROADMAP §Phase 23 SC#2)"
    - "CustomerLibrary.test.tsx exists at the path locked by D-13 (sibling to source) and covers: search-filter behavior, delete-confirmation flow, edit modal open/close, empty state, sort order (lastUsedAt desc with undefined-first per CL-01) (per TEST-03 + ROADMAP §Phase 23 SC#3)"
    - "Both test files use raw createRoot + act — NO @testing-library/react (D-08)"
    - "Both test files render the real <Modal> primitive — NO Modal stub (D-07)"
    - "Both test files receive data via PROPS and mock useDatabase only if the component imports a hook beyond props — fake-indexeddb is NOT used (per D-06; matches Phase 22's RecordSaleModal.test.tsx pattern)"
  artifacts:
    - path: "src/components/CustomerCsvImportModal.test.tsx"
      provides: "First test coverage for CustomerCsvImportModal — TEST-02"
      min_lines: 200
    - path: "src/components/CustomerLibrary.test.tsx"
      provides: "First test coverage for CustomerLibrary — TEST-03 including CL-01 sort lock"
      min_lines: 200
  key_links:
    - from: "src/components/CustomerCsvImportModal.test.tsx"
      to: "src/components/CustomerCsvImportModal"
      via: "dynamic import after vi.mock"
      pattern: "await import\\('./CustomerCsvImportModal'\\)"
    - from: "src/components/CustomerLibrary.test.tsx"
      to: "src/components/CustomerLibrary"
      via: "dynamic import after vi.mock"
      pattern: "await import\\('./CustomerLibrary'\\)"
    - from: "src/components/CustomerLibrary.test.tsx"
      to: "CL-01 sort lock"
      via: "expect rendered row order matches sorted-by-lastUsedAt-desc-undefined-first fixture"
      pattern: "lastUsedAt"
---

<objective>
Author the two remaining Customer-UI test files in parallel — both share the same scaffold (raw createRoot + act, real `<Modal>` primitive, props-only data flow) and neither depends on the other.

1. **TEST-02 — `src/components/CustomerCsvImportModal.test.tsx`**: Covers the CSV import modal's two-step flow per ROADMAP §Phase 23 SC#2: upload non-CSV → WR-06 error path, upload valid CSV → Upload step transitions to Preview step, dedup-mode toggle (skip vs overwrite) changes which rows are flagged, individual row selection/deselection updates the import-button label/count, confirming the import calls `onImportCustomers(toImport: Customer[])` with the correct shape (selected rows only, dedup mode honored).

2. **TEST-03 — `src/components/CustomerLibrary.test.tsx`**: Covers the library list per ROADMAP §Phase 23 SC#3: search-filter behavior (typing in the search input narrows the visible rows), delete-confirmation flow (Delete button → confirm dialog → `onDeleteCustomer(id)` called), edit modal open/close (Edit button opens `<CustomerEditModal>`; closing reverts state), empty state rendered when `customers: []`, and the **CL-01 sort lock** — given a fixture with mixed `lastUsedAt` values (some undefined), the rendered row order matches `lastUsedAt desc, undefined-first` (per `.planning/phases/15.1-customer-library/15.1-CONTEXT.md` CL-01).

Purpose: Closes TEST-02 + TEST-03. The two test files are independent file-creates with no overlap to Plan 23-01, 23-03, or 23-04 → safe to run in Wave 1 in parallel with the other plans.

Output: Two new test files; full `npm test` green; `tsc -b` clean; coverage report shows non-zero coverage for both files.

Per D-06: Both files mock `useDatabase` only if and where the component internally consumes a hook beyond what props provide. CustomerLibrary receives everything via props (no `useDatabase` import in CustomerLibrary.tsx — though it does `import { db } from '../db/database'` for one direct call in CL row click — confirm during read_first). CustomerCsvImportModal also receives everything via props.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-test-coverage-hardening/23-CONTEXT.md
@.planning/phases/23-test-coverage-hardening/23-PATTERNS.md
@.planning/phases/15.1-customer-library/15.1-CONTEXT.md
@.planning/phases/22-jobsmanager-decomposition-perf/22-CONTEXT.md
@.planning/phases/19-modal-primitive-a11y-migration/19-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/CustomerCsvImportModal.tsx (prop signature — TEST-02 render target):
```typescript
interface CustomerCsvImportModalProps {
  isOpen: boolean;
  existingCustomers: Customer[];
  onImportCustomers: (toImport: Customer[]) => Promise<void>;
  onClose: () => void;
}
type Step = 'upload' | 'preview';
```

From src/components/CustomerLibrary.tsx (prop signature — TEST-03 render target):
```typescript
interface CustomerLibraryProps {
  customers: Customer[];
  isLoading: boolean;
  onAddCustomer: (c: Customer) => Promise<void>;
  onUpdateCustomer: (c: Customer) => Promise<void>;
  onDeleteCustomer: (id: string) => Promise<void>;
  onBulkImportCustomers: (toImport: Customer[]) => Promise<void>;
}
```

CL-01 sort contract (from 15.1-CONTEXT.md):
```
Library renders customers sorted by lastUsedAt DESCENDING, with undefined lastUsedAt FIRST
(i.e., customers never used are displayed before customers that were last used in the past).
```

From src/components/RecordSaleModal.test.tsx lines 20-30 (spy declaration pattern to mirror):
```typescript
const onImportCustomersSpy = vi.fn<(toImport: Customer[]) => Promise<void>>().mockResolvedValue();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author CustomerCsvImportModal.test.tsx — TEST-02</name>
  <files>src/components/CustomerCsvImportModal.test.tsx</files>
  <read_first>
    - src/components/CustomerCsvImportModal.tsx (full file — executor must see the JSX/labels/error copy strings/dedup-mode UI to write selectors)
    - src/utils/customerCsv.ts (the parser the modal delegates to — for understanding what counts as a valid vs invalid CSV; especially the `parseCustomerCsv` return shape and WR-06 error message)
    - src/components/RecordSaleModal.test.tsx (lines 1-100 for imports + spy + dynamic-import scaffold; lines 162-240 for DOM harness + typeIntoInput + findButton — copy verbatim)
    - .planning/phases/23-test-coverage-hardening/23-PATTERNS.md §CustomerCsvImportModal.test.tsx pattern assignments
    - .planning/phases/23-test-coverage-hardening/23-CONTEXT.md §D-07 (real Modal) + §D-08 (raw createRoot + act)
    - .planning/phases/15.1-customer-library/15.1-CONTEXT.md §CL-03 (CSV import flow contract — references WR-06 error path)
  </read_first>
  <behavior>
    The test file MUST cover the five scenarios per TEST-02 / ROADMAP §Phase 23 SC#2 (one `it()` block each minimum):

    1. **Upload non-CSV file → WR-06 error**: Simulate a `File` upload with `type !== 'text/csv'` (e.g. `type: 'image/png'`) via the file input — assert an error message appears in DOM (read the modal's error copy from CustomerCsvImportModal.tsx and match the visible string). `onImportCustomers` is NOT called. The modal stays on the `upload` step.
    2. **Upload valid CSV → preview step transition**: Construct a minimal valid customers CSV string (header row + 2 data rows), wrap as a `File` with `type: 'text/csv'`, fire `change` on the file input → assert the modal advances from the `upload` view to the `preview` view (look for a marker DOM element that exists in preview but not upload — e.g. a table of rows OR the import button with a count).
    3. **Dedup-mode toggle**: After preview is reached, locate the dedup-mode control (radio or select for `skip` vs `overwrite`) → flip it → assert the rows flagged as duplicates change their visible state (the modal re-renders duplicate-row indicators). Use the `existingCustomers` prop to plant a known-duplicate row in the CSV.
    4. **Row select/deselect**: Click a row's checkbox (or de/select control) → assert the import-button's enabled state / count label updates accordingly. Specifically: deselect ALL rows → import button reflects 0 / disabled.
    5. **Confirm-import call shape**: With 2 rows selected and dedup-mode = `skip`, click the Import button → assert `onImportCustomersSpy.mock.calls.length === 1` AND `onImportCustomersSpy.mock.calls[0][0]` is an array of length matching the selected count, AND each entry has a `Customer`-shaped object (has `id`, `name` and/or `email`). Per D-06, the modal calls `onImportCustomers(toImport: Customer[])` from its props — no useDatabase hook is involved.
  </behavior>
  <action>
    Create `src/components/CustomerCsvImportModal.test.tsx` mirroring the `RecordSaleModal.test.tsx` scaffold at the file-shape level (imports, spy block, dynamic import, beforeEach/afterEach DOM harness, helpers). Adaptations:

    - **Imports**: `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';` + `import { createRoot, type Root } from 'react-dom/client';` + `import { act } from 'react';` + `import type { Customer } from '../types';`. NO `@testing-library/react`.
    - **Spies (module scope, BEFORE any vi.mock)**: `const onImportCustomersSpy = vi.fn<(toImport: Customer[]) => Promise<void>>().mockResolvedValue();` and `const onCloseSpy = vi.fn<() => void>();`.
    - **vi.mock decision**: Read CustomerCsvImportModal.tsx imports — if it imports anything from `../hooks/useDatabase`, mock that hook (per D-06 pattern, returning spy fns). If it only imports from `./ui`, `../utils/customerCsv`, `../utils/csvHelpers`, and `../types`, OMIT the vi.mock for useDatabase entirely. Based on the file's existing import block (line 1-10 in 23-PATTERNS.md), no useDatabase import is present → no vi.mock needed.
    - **Dynamic import AFTER mocks (if any)**: `const { CustomerCsvImportModal } = await import('./CustomerCsvImportModal');` at top level.
    - **DOM harness**: copy the `let container / let root` + `beforeEach (mockClear + createRoot)` + `afterEach (unmount + remove)` block from RecordSaleModal.test.tsx lines 162-196 with spy-clear adapted to `onImportCustomersSpy.mockClear(); onCloseSpy.mockClear();` plus reset `onImportCustomersSpy.mockResolvedValue();`.
    - **renderModal helper**: `props = { isOpen: true, existingCustomers: opts.existing ?? [], onImportCustomers: onImportCustomersSpy, onClose: onCloseSpy };` → `await act(async () => root!.render(<CustomerCsvImportModal {...props} />));`. Real `<Modal>` mounts (D-07).
    - **File-input helper**: Add a `simulateFileUpload(file: File)` helper that locates the `<input type="file">` via `document.body.querySelector('input[type="file"]')`, uses `Object.defineProperty(input, 'files', { value: [file], configurable: true })` to inject the file, and dispatches `new Event('change', { bubbles: true })` inside `act(...)`. This is the standard jsdom file-upload simulation pattern; reference any similar test in the repo if needed (e.g., grep for `input[type="file"]` across `src/**/*.test.tsx`).
    - **Valid-CSV fixture**: build a minimal customers CSV string like `name,email\nAlice,alice@x.com\nBob,bob@x.com\n` and wrap with `new File([csv], 'customers.csv', { type: 'text/csv' })`.
    - **Selectors**: read the modal's JSX to find the dedup-mode control (likely `<input type="radio" name="duplicateMode" value="skip" />` or a `<select>`) and the Import / Close buttons. Use `findButton(text)` (mirror RecordSaleModal.test.tsx) for buttons; `querySelector` for radios.
    - **WR-06 error message text**: search CustomerCsvImportModal.tsx for the error string that appears when a non-CSV file is uploaded — copy the literal substring into the test assertion (e.g. `expect(document.body.textContent).toContain('Please choose a .csv file')` — exact text comes from the source).

    Test count target: 5–8 `it()` blocks. File length target: 200–400 lines.

    Implements TEST-02.
  </action>
  <verify>
    <automated>npm test -- src/components/CustomerCsvImportModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/CustomerCsvImportModal.test.tsx` exists
    - `grep -c "@testing-library/react" src/components/CustomerCsvImportModal.test.tsx` returns 0 (D-08 lock)
    - `grep -c "createRoot" src/components/CustomerCsvImportModal.test.tsx` returns at least 1
    - `grep -c "from 'react'" src/components/CustomerCsvImportModal.test.tsx` returns at least 1 (for `act`)
    - `grep -c "vi.fn<(toImport: Customer\\[\\])" src/components/CustomerCsvImportModal.test.tsx` returns at least 1
    - `grep -c "await import('./CustomerCsvImportModal')" src/components/CustomerCsvImportModal.test.tsx` returns 1
    - `grep -cE "^  it\\(" src/components/CustomerCsvImportModal.test.tsx` returns at least 5
    - `npm test -- src/components/CustomerCsvImportModal.test.tsx` exits 0
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>TEST-02 closed. CSV import modal has its first 5+ test cases; full vitest suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author CustomerLibrary.test.tsx — TEST-03 including CL-01 sort lock</name>
  <files>src/components/CustomerLibrary.test.tsx</files>
  <read_first>
    - src/components/CustomerLibrary.tsx (full file — executor must see the JSX/labels/empty-state copy/sort logic to write selectors; especially the `useMemo` or `.sort()` call that implements CL-01)
    - src/components/RecordSaleModal.test.tsx (lines 1-100 + 162-240 — same scaffold pattern)
    - .planning/phases/23-test-coverage-hardening/23-PATTERNS.md §CustomerLibrary.test.tsx pattern assignments
    - .planning/phases/23-test-coverage-hardening/23-CONTEXT.md §D-07 + §D-08
    - .planning/phases/15.1-customer-library/15.1-CONTEXT.md §CL-01 (the sort contract being locked)
  </read_first>
  <behavior>
    Five scenarios per TEST-03 / ROADMAP §Phase 23 SC#3 + CL-01:

    1. **Search-filter behavior**: Render with 4 customers including names "Alice", "Bob", "Carol", "Dave" → type "ali" into the search input → assert only the Alice row is rendered (or at least: Bob/Carol/Dave rows are gone, Alice row is present).
    2. **Empty state**: Render with `customers: []` and `isLoading: false` → assert the empty-state copy appears in DOM (read the exact string from CustomerLibrary.tsx — look for an `EmptyState` component usage). When `isLoading: true`, the empty state should NOT appear (a skeleton renders instead).
    3. **CL-01 sort lock (THE PRIMARY ASSERTION FOR THIS FILE)**: Render with this fixture (provide in fixed declaration order so the test can assert SORTED order is different from input order):
        - C1: `{ id: 'c1', name: 'Used Recently', lastUsedAt: new Date('2026-05-01') }`
        - C2: `{ id: 'c2', name: 'Never Used A', lastUsedAt: undefined }`
        - C3: `{ id: 'c3', name: 'Used Long Ago', lastUsedAt: new Date('2025-01-01') }`
        - C4: `{ id: 'c4', name: 'Never Used B', lastUsedAt: undefined }`
       Expected rendered order (per CL-01: undefined FIRST, then desc by lastUsedAt): **C2, C4** (either order between these two — they're tied at undefined), then **C1** (more recent date), then **C3** (older date). Assert the text-content order of rendered customer-name DOM nodes matches this contract. Document in a code comment that the test references CL-01 in 15.1-CONTEXT.md.
    4. **Delete confirmation flow**: Render with 1 customer, click the Delete button on that row → assert a confirmation UI appears (read CustomerLibrary.tsx for the exact mechanism — could be inline confirm, native `window.confirm`, or a sub-modal). If `window.confirm` is used, stub it via `vi.spyOn(window, 'confirm').mockReturnValue(true)`. After confirming, assert `onDeleteCustomerSpy.mock.calls[0][0] === 'c1'` (the customer's id).
    5. **Edit modal open/close**: Render with 1 customer, click the Edit button → assert the `<CustomerEditModal>` is now in DOM (look for a marker — e.g. the modal's title text `Edit customer`). Dispatch Escape or click Cancel → assert the modal is removed from DOM (the Edit-customer title no longer appears). The `onUpdateCustomer` spy is NOT called in this scenario (we only test open/close, not save).
  </behavior>
  <action>
    Create `src/components/CustomerLibrary.test.tsx` from scratch using the same scaffold as Task 1.

    - **Imports**: `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';` + `import { createRoot, type Root } from 'react-dom/client';` + `import { act } from 'react';` + `import type { Customer } from '../types';`. NO `@testing-library/react`.
    - **Spies (module scope)**: typed spies for all 4 callbacks per the props signature:
      `const onAddCustomerSpy = vi.fn<(c: Customer) => Promise<void>>().mockResolvedValue();`
      `const onUpdateCustomerSpy = vi.fn<(c: Customer) => Promise<void>>().mockResolvedValue();`
      `const onDeleteCustomerSpy = vi.fn<(id: string) => Promise<void>>().mockResolvedValue();`
      `const onBulkImportCustomersSpy = vi.fn<(toImport: Customer[]) => Promise<void>>().mockResolvedValue();`
    - **vi.mock decision**: CustomerLibrary.tsx imports `db` from `../db/database` (line 9 of the source) — for the Delete-confirmation test path it might call `db.something`. If it does, `vi.mock('../db/database', () => ({ db: { ... } }))` with a minimal stub. Read the file to confirm; otherwise omit the vi.mock. CustomerLibrary.tsx does NOT import from `../hooks/useDatabase` so no useDatabase mock is needed.
    - **Dynamic import AFTER mocks**: `const { CustomerLibrary } = await import('./CustomerLibrary');`.
    - **DOM harness**: copy from RecordSaleModal.test.tsx lines 162-196; adapt spy-clear list.
    - **renderLibrary helper**: `props = { customers: opts.customers ?? [], isLoading: opts.isLoading ?? false, onAddCustomer: onAddCustomerSpy, onUpdateCustomer: onUpdateCustomerSpy, onDeleteCustomer: onDeleteCustomerSpy, onBulkImportCustomers: onBulkImportCustomersSpy };` → `await act(async () => root!.render(<CustomerLibrary {...props} />));`.
    - **Sort-order assertion (test #3 — CL-01)**: After render, collect the rendered customer-name elements via a stable selector (read the source to find the name's CSS class or test-friendly attribute — e.g. `div.text-sm.font-medium.text-white.truncate` from line 51-53 of CustomerLibrary.tsx, OR add `data-testid="customer-row-name"` only if no other stable selector exists — prefer the existing class to avoid touching production code). Assert the array of `textContent` values matches the expected sorted order. Add an inline code comment linking the assertion to `.planning/phases/15.1-customer-library/15.1-CONTEXT.md §CL-01`.
    - **React-window note**: CustomerLibrary uses `react-window` `List` for virtualization (per line 3 of source). In jsdom with `act()`, react-window renders some rows but may not render all rows immediately if container height is 0. The test MUST either: (a) explicitly set the container height via the rendered `style` attribute before render, OR (b) flush via `await act(async () => {});` after render, OR (c) assert only the visible/rendered rows match the expected sort prefix. The pragmatic path is to set `container.style.height = '800px'` BEFORE the render call so react-window measures a non-zero height. Confirm by reading CustomerLibrary.tsx for the `List` usage.
    - **Search input**: locate via `document.body.querySelector('input[type="search"]')` OR by placeholder text — read source for the exact placeholder/type.
    - **typeIntoInput, findButton helpers**: copy verbatim from RecordSaleModal.test.tsx.

    Test count target: 5–8 `it()` blocks. File length target: 200–400 lines.

    Implements TEST-03 + locks CL-01.
  </action>
  <verify>
    <automated>npm test -- src/components/CustomerLibrary.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/CustomerLibrary.test.tsx` exists
    - `grep -c "@testing-library/react" src/components/CustomerLibrary.test.tsx` returns 0 (D-08 lock)
    - `grep -c "createRoot" src/components/CustomerLibrary.test.tsx` returns at least 1
    - `grep -c "from 'react'" src/components/CustomerLibrary.test.tsx` returns at least 1
    - `grep -c "vi.fn<(id: string)" src/components/CustomerLibrary.test.tsx` returns at least 1 (typed onDeleteCustomerSpy)
    - `grep -c "await import('./CustomerLibrary')" src/components/CustomerLibrary.test.tsx` returns 1
    - `grep -c "lastUsedAt" src/components/CustomerLibrary.test.tsx` returns at least 2 (CL-01 fixture references)
    - `grep -c "CL-01" src/components/CustomerLibrary.test.tsx` returns at least 1 (comment-level reference to the lock)
    - `grep -cE "^  it\\(" src/components/CustomerLibrary.test.tsx` returns at least 5
    - `npm test -- src/components/CustomerLibrary.test.tsx` exits 0
    - `npm test` (full suite) exits 0
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>TEST-03 closed. CustomerLibrary has its first 5+ test cases including the CL-01 sort lock; full vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → CSV parser (CustomerCsvImportModal) | User-uploaded file content crosses into parsed `Customer[]` objects |
| In-memory Customer[] → DOM render (CustomerLibrary) | Library sort/filter functions consume potentially-untrusted Customer data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Tampering | CustomerCsvImportModal.test.tsx, CustomerLibrary.test.tsx | accept | No new attack surface — this plan only adds test files that mock callbacks and read DOM. Production behavior is unchanged. |
| T-23-06 | Information Disclosure | CL-01 sort fixture | accept | Test fixture uses synthetic data (`Used Recently`, `Never Used A`, etc.) — no real PII. |
| T-23-07 | Tampering | CSV upload path (existing) | mitigate | Existing `customerCsv.ts` parser + Phase 21 SEC-01 `sanitizeCsvCell` already cover export-side formula injection. Phase 23 only locks the modal's existing happy/error paths by test — does not change the parser. |

No new mitigations introduced. T-15-03 DUP-02 lock and CL-05 by-value lock are preserved (this plan does not touch duplicateJob or Sale.customer).
</threat_model>

<verification>
After both tasks:
- `npm run build` → exit 0
- `npm test` → all tests pass, no skipped; the 5+5 = 10+ new it() blocks count
- `npm test -- --coverage` → coverage report contains non-zero coverage lines for BOTH `src/components/CustomerCsvImportModal.tsx` and `src/components/CustomerLibrary.tsx`
- `npx tsc -b` → exit 0
- `git diff --stat src/components/CustomerCsvImportModal.tsx src/components/CustomerLibrary.tsx` returns empty diff (production code unchanged — this plan only adds test files)
</verification>

<success_criteria>
- src/components/CustomerCsvImportModal.test.tsx exists with 5+ it() blocks covering the TEST-02 scenarios (TEST-02)
- src/components/CustomerLibrary.test.tsx exists with 5+ it() blocks including the CL-01 sort lock (TEST-03)
- Both files use raw createRoot + act + real Modal primitive (D-07, D-08)
- All gates green: `npm test`, `npx tsc -b`, `npm run build`
- Production code (CustomerCsvImportModal.tsx, CustomerLibrary.tsx) is byte-identical pre/post this plan
</success_criteria>

<output>
Create `.planning/phases/23-test-coverage-hardening/23-02-SUMMARY.md` when done.
</output>
