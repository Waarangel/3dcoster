---
phase: 23-test-coverage-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/db/database.migrations.test.ts
autonomous: false
requirements: [TEST-04]
tags: [test, dexie, migration, fake-indexeddb]

must_haves:
  truths:
    - "fake-indexeddb is in devDependencies at a version compatible with Dexie v4 + IDB v3 (per D-04)"
    - "fake-indexeddb is imported ONLY from src/db/database.migrations.test.ts — NO vitest.setup.ts global injection (per D-04)"
    - "src/db/database.migrations.test.ts is FULLY REPLACED — old pure-helper fallback test removed (per D-05); pure-helper coverage in src/db/backfill.test.ts is unchanged"
    - "New test opens a real v7 Dexie fixture, bulkAdds the D-17 G7 3-job-2-quote fixture, closes, reopens at v8 with the v8 upgrade callback, asserts db.quotes.toArray() returns 2 rows matching the locked D-17 G7 assertions (statuses, printJobId, convertedToSaleId, quoteNumber)"
    - "v9 currency reconcile test path remains covered (kept inline OR ported to the real-Dexie pattern — both acceptable; researcher's discretion)"
    - "The existing 28+ other test files keep running jsdom-only with no fake-indexeddb leakage (blast radius scoped to one file per D-04)"
  artifacts:
    - path: "package.json"
      provides: "fake-indexeddb devDependency entry"
      contains: "fake-indexeddb"
    - path: "src/db/database.migrations.test.ts"
      provides: "Real-Dexie v7→v8 migration test (REPLACES the fallback-mode file at the same path)"
      contains: "import 'fake-indexeddb/auto'"
  key_links:
    - from: "src/db/database.migrations.test.ts"
      to: "fake-indexeddb/auto"
      via: "side-effect import at file top"
      pattern: "import 'fake-indexeddb/auto'"
    - from: "src/db/database.migrations.test.ts"
      to: "src/db/backfill.ts"
      via: "backfillQuotesFromJobs invocation inside the v8 upgrade callback"
      pattern: "backfillQuotesFromJobs"
    - from: "src/db/database.migrations.test.ts"
      to: "dexie"
      via: "new Dexie(...) at v7 + .version(8).upgrade(...) wiring"
      pattern: "from 'dexie'"
---

<objective>
Promote `src/db/database.migrations.test.ts` from "fallback mode" (pure-helper test) to a real-Dexie integration test via `fake-indexeddb`. The current file's own docstring (lines 6-31 of the existing file) spells out the exact replacement plan; this plan executes it.

Two deliverables:

1. **Add `fake-indexeddb` to `devDependencies`** in `package.json`. Researcher pins the exact version compatible with the existing `dexie ^4.2.1` (per D-04 + 23-CONTEXT.md "researcher pins the version"). At time of writing, fake-indexeddb v6.x is the documented Dexie-v4-compatible series. Run `npm install --save-dev fake-indexeddb` and let npm resolve, then verify the resolved version is ≥6.0.0. Commit both `package.json` and `package-lock.json`.

2. **Fully replace `src/db/database.migrations.test.ts`** at the same path (per D-05 + D-14). The new file:
   - Imports `'fake-indexeddb/auto'` as the very first line (per D-04 — scoped to this file only, no vitest.setup.ts injection).
   - Opens a v7 Dexie database with the v7 schema, bulk-adds the locked D-17 G7 fixture (3 jobs + 1 sale — matches `src/db/backfill.test.ts:46-110` and the existing fallback file lines 67-79), closes.
   - Reopens at v8 with the v8 upgrade callback that calls `backfillQuotesFromJobs(jobs, sales, currency)` and writes via `tx.table('quotes').bulkAdd(quotes)` (mirrors the actual upgrade logic in `src/db/database.ts`).
   - Asserts `await db.quotes.toArray()` returns 2 rows matching the locked D-17 G7 contract from `src/db/backfill.test.ts` (and from the existing fallback-mode file lines 84-96).
   - Uses unique DB names per test (e.g. `test-3DCosterDB-${randomUUID()}`) + `afterEach` cleanup via `Dexie.delete(dbName)` to prevent cross-test contamination in fake-indexeddb's in-memory store.
   - Per CONTEXT §"Claude's Discretion": the v9 currency reconcile test inside the current file (lines 144-182) MAY stay as a pure-helper test (it already passes), OR be re-implemented as a real-Dexie test. Recommend KEEP-AS-PURE-HELPER inside this file because (a) the v9 reconcile is the SECOND customer for fake-indexeddb and the planner only mandated v7→v8 promotion in TEST-04, (b) the pure helper `reconcileQuoteCurrency` is already exhaustively covered in `src/db/backfill.test.ts`. Decision left to executor — either path satisfies TEST-04.

Purpose: Closes TEST-04. The integration test now exercises the actual Dexie upgrade transaction boundary — any future change to `src/db/database.ts`'s `.version(8).upgrade(...)` callback that breaks the D-17 G7 contract will fail this test, where the pure-helper test would have missed the failure (the pipeline could read jobs+sales correctly but write to the wrong table, etc.).

Output: One devDep added; one test file replaced; `npm test` includes a real-Dexie migration test that passes; blast radius limited to one file (no other tests gain IndexedDB shim).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-test-coverage-hardening/23-CONTEXT.md
@.planning/phases/23-test-coverage-hardening/23-PATTERNS.md

<interfaces>
<!-- Dexie upgrade contract — extracted from src/db/database.ts -->
<!-- Executor should re-read src/db/database.ts to confirm the EXACT shape of -->
<!-- the v7→v8 upgrade callback (which schema fields are on jobs/sales/quotes -->
<!-- in v7 and v8) before writing the test fixture. -->

D-17 G7 locked assertions (from src/db/backfill.test.ts lines 84-96, reproduce in new test):
```typescript
expect(v8Quotes.length).toBe(2);
expect(v8Quotes.filter(q => q.status === 'converted').length).toBe(1);
expect(v8Quotes.filter(q => q.status === 'draft').length).toBe(1);

const converted = v8Quotes.find(q => q.status === 'converted')!;
expect(converted.printJobId).toBe('job-a');
expect(converted.convertedToSaleId).toBe('sale-a');
expect(converted.quoteNumber).toBe(42);

const draft = v8Quotes.find(q => q.status === 'draft')!;
expect(draft.printJobId).toBe('job-b');
expect(draft.convertedToSaleId).toBeUndefined();
expect(draft.quoteNumber).toBe(43);
```

fake-indexeddb side-effect import (D-04 — scoped to this file only):
```typescript
import 'fake-indexeddb/auto';
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1 (Wave 0 — package legitimacy gate): Verify fake-indexeddb npm package legitimacy before install</name>
  <files>(no files modified — human verification only)</files>
  <action>Human-only verification step. The executor presents the package authority signals listed in <what-built> and waits for the resume signal. Do NOT run `npm install` or modify any files until the verifier returns `approved`.</action>
  <verify>Verification is performed by the human verifier following the steps in <how-to-verify>. There is no automated command for this checkpoint — the resume signal IS the verification.</verify>
  <done>Verifier types `approved` after confirming all 5 package legitimacy signals (maintainer identity, recency, downloads, GitHub link, README compatibility statement). Only after `approved` does Task 2 execute the `npm install`. If verifier types `reject`, the plan halts and the developer renegotiates whether to use a different DB shim or defer TEST-04 entirely.</done>
  <what-built>
    Pre-install package legitimacy verification for `fake-indexeddb` per the planner's npm package legitimacy gate. The package is a well-established npm package (years of history, used by Dexie's own test suite) but the gate is mandatory because RESEARCH.md does not yet contain a Package Legitimacy Audit table for Phase 23 (this is a test-only phase with no formal research artifact — `has_research: false` in init).

    The package authority signals to verify:
    - Repository: https://github.com/dumbmatter/fakeIndexedDB
    - npm package: https://www.npmjs.com/package/fake-indexeddb
    - Maintained by: dumbmatter (Jeremy Scheff)
    - Used in production by: Dexie's own test suite + many other Dexie-based projects
    - Last publish recency: should be within the last 12 months
    - Weekly downloads: should be > 100k

    Disposition: `[ASSUMED]` until verified — this checkpoint flips it to `[VERIFIED]`.
  </what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/fake-indexeddb in browser
    2. Confirm:
       - Maintainer is `dumbmatter` (or similar — check the GitHub link goes to dumbmatter/fakeIndexedDB)
       - Last published: within last 12 months
       - Weekly downloads: > 100,000 (currently ~1M+ as of 2026)
       - "Repository" link points to https://github.com/dumbmatter/fakeIndexedDB
    3. Click through to the GitHub repo — confirm:
       - Stars: > 1,000
       - Open source, active project (commits within last 6 months)
       - README explicitly mentions Dexie compatibility OR documents the IndexedDB API version
    4. Type `approved` to proceed, OR type `reject` with a description if any signal is wrong (e.g. typosquat, abandoned, suspicious maintainer change).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add fake-indexeddb to devDependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (full devDependencies block to confirm structure + existing versions; check for existing fake-indexeddb entries to avoid duplicate adds)
    - .planning/phases/23-test-coverage-hardening/23-CONTEXT.md §D-04 (scope: per-test import only, no global setup)
  </read_first>
  <action>
    Run `npm install --save-dev fake-indexeddb` from the project root. Verify the installed version is in the v6.x range (compatible with Dexie ^4.2.1 + IDB v3 per Dexie's README). If npm resolves to an older major version that lacks `fake-indexeddb/auto`, force the install with `npm install --save-dev fake-indexeddb@^6.0.0` instead. After install:
    - Confirm `package.json` `devDependencies` now has an entry like `"fake-indexeddb": "^6.x.x"` (the exact patch is whatever npm pinned).
    - Confirm `package-lock.json` has the resolved version + integrity hash.
    - Run `npx tsc -b` to confirm the new types resolve (fake-indexeddb ships its own TS types).
    - Do NOT add any import in `vitest.setup.ts` (D-04 explicitly forbids global injection — confirm `vitest.setup.ts` does NOT mention fake-indexeddb anywhere in the diff).

    Implements TEST-04 prerequisite.
  </action>
  <verify>
    <automated>npm ls fake-indexeddb &amp;&amp; npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls fake-indexeddb` returns a resolved version (not "empty" / not "missing")
    - `grep -c '"fake-indexeddb"' package.json` returns 1 (single devDep entry)
    - `grep 'fake-indexeddb' vitest.setup.ts 2>/dev/null` returns 0 lines (D-04 lock — NOT globally injected). If vitest.setup.ts does not exist, that is also fine.
    - `grep 'fake-indexeddb' package-lock.json` returns at least 1 line (lockfile updated)
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>fake-indexeddb installed as devDep at v6.x; no global injection; tsc still clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Full-file replace src/db/database.migrations.test.ts with real-Dexie v7→v8 integration test</name>
  <files>src/db/database.migrations.test.ts</files>
  <read_first>
    - src/db/database.migrations.test.ts (entire current file — especially the docstring lines 6-31 which spells out the replacement plan, and lines 33-125 for the existing fixture shape + D-17 G7 assertions)
    - src/db/backfill.test.ts (lines 46-110 for the canonical D-17 G7 fixture factories + assertions — the new test MUST produce byte-identical assertions to keep the contract intact)
    - src/db/database.ts (the ACTUAL v7→v8 upgrade callback in production code — read whatever lines contain `.version(8).upgrade(...)` to mirror the upgrade logic exactly; also confirm the v7 schema string for tables `jobs`, `sales`, `quotes` if quotes exists at v7)
    - .planning/phases/23-test-coverage-hardening/23-CONTEXT.md §D-04 + §D-05 + §D-14 (per-test import / full replacement / same path)
    - .planning/phases/23-test-coverage-hardening/23-PATTERNS.md §database.migrations.test.ts pattern (lines 199-285 of PATTERNS.md — fixture, upgrade pipeline, assertions)
  </read_first>
  <action>
    Delete the existing file content of `src/db/database.migrations.test.ts` and write a new file from scratch. Structure:

    1. **Top-of-file imports (D-04 order is critical)**:
       - Line 1: `import 'fake-indexeddb/auto';` — MUST be the very first import. This sets up `globalThis.indexedDB` before any `new Dexie(...)` call evaluates.
       - Line 2: `import { describe, it, expect, beforeEach, afterEach } from 'vitest';`
       - Line 3: `import { Dexie } from 'dexie';`
       - Line 4: `import { backfillQuotesFromJobs } from './backfill';` (the actual upgrade-callback helper)
       - Line 5: `import type { PrintJob, Sale } from '../types';`

    2. **Fixture factories** (port from current file lines 33-64 verbatim — `makeJob` and `makeSale`). These match `src/db/backfill.test.ts:46-77` exactly per D-17 G7.

    3. **D-17 G7 fixture** at module scope:
       - `jobA = makeJob({ id: 'job-a', quoteNumber: 42, sellingPrice: 100, taxRate: 13, taxAmount: 13 })`
       - `jobB = makeJob({ id: 'job-b', quoteNumber: 43, sellingPrice: 50 })`
       - `jobC = makeJob({ id: 'job-c' })` (no quoteNumber → skipped by upgrade callback)
       - `saleA = makeSale({ id: 'sale-a', jobId: 'job-a', customer: { name: 'Alice', email: 'alice@example.com' } })`

    4. **describe block — `describe('v7→v8 quotes migration (D-17 G7) — real Dexie via fake-indexeddb', () => { ... })`**:
       - **Use unique DB names per test** to isolate state: `let dbName: string;` declared at describe scope; `beforeEach: dbName = \`test-3DCosterDB-${crypto.randomUUID()}\`;`. This avoids cross-test contamination in fake-indexeddb's in-memory store.
       - **afterEach**: `if (db?.isOpen()) db.close(); await Dexie.delete(dbName);` to clean up.

    5. **Main test — `it('emits exactly 2 quotes for 3-job-2-quote fixture (status mix: converted + draft) via the real v7→v8 upgrade transaction', async () => { ... })`**:
       - Step A: Open at v7 schema. Use the v7 store strings from the production schema (read `src/db/database.ts` to find the actual v7 `.version(7).stores({ ... })` block — likely `{ jobs: 'id', sales: 'id' }` plus other stores). Bulk-add `[jobA, jobB, jobC]` to `jobs` and `[saleA]` to `sales`. Close.
       - Step B: Reopen at v8 with the upgrade callback that mirrors `database.ts`'s actual v8 upgrade. The upgrade should: read jobs + sales via `tx.table('jobs').toArray()` + `tx.table('sales').toArray()`, call `backfillQuotesFromJobs(jobs, sales, 'USD')`, then `await tx.table('quotes').bulkAdd(quotes)`. The v8 stores object must include `quotes: 'id'` (and whatever indexes the real schema has — copy from `database.ts`).
       - Step C: Assert `const v8Quotes = await db.table('quotes').toArray();` matches the D-17 G7 contract — copy the 10 assertion lines from `src/db/backfill.test.ts:84-96` (or from the existing fallback file lines 84-96) BYTE-IDENTICALLY but operating on the real-Dexie result, not the pure-helper return value.

    6. **(Discretion call per CONTEXT)**: Either keep the existing v9 currency-reconcile describe block at the bottom of the file as pure-helper (the simpler choice — already passes), OR rewrite it to use the real-Dexie pattern (more work, no signal gain because `reconcileQuoteCurrency` is already exhaustively covered in `src/db/backfill.test.ts`). RECOMMENDED: keep as pure-helper for now — TEST-04 only mandates v7→v8 promotion. Document the choice in a code comment at the top of the v9 describe block (e.g. `// v9 reconcile stays at pure-helper layer — fake-indexeddb integration deferred (TEST-04 scope is v7→v8 only)`).

    7. **Top-of-file docstring**: Rewrite the existing 25-line fallback-mode docstring (lines 5-31) into a new ~10-line docstring that:
       - Documents this is the Phase 23 TEST-04 real-Dexie promotion of the prior fallback-mode test
       - Notes that `fake-indexeddb/auto` is the FIRST import (sets up globalThis.indexedDB)
       - References the D-17 G7 lock + its sibling pure-helper coverage in `src/db/backfill.test.ts`
       - States that per D-04 the fake-indexeddb shim is scoped to THIS file only

    Per D-05, the new file REPLACES the old at the same path — do not create a sibling file or rename.

    Implements TEST-04.
  </action>
  <verify>
    <automated>npm test -- src/db/database.migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - First line of `src/db/database.migrations.test.ts` is `import 'fake-indexeddb/auto';` (D-04 lock — must be first)
    - `grep -c "import 'fake-indexeddb/auto'" src/db/database.migrations.test.ts` returns 1
    - `grep "import { Dexie }" src/db/database.migrations.test.ts` returns at least 1 line
    - `grep "Dexie.delete" src/db/database.migrations.test.ts` returns at least 1 line (cleanup pattern)
    - `grep -c "backfillQuotesFromJobs" src/db/database.migrations.test.ts` returns at least 1 (real upgrade callback uses it)
    - `grep -c ".version(8)" src/db/database.migrations.test.ts` returns at least 1 (v8 upgrade is exercised)
    - The test invokes `db.table('quotes').toArray()` OR `db.quotes.toArray()` — confirms real-Dexie path, not pure-helper-only assertion
    - `grep -cE "expect\\(v8Quotes" src/db/database.migrations.test.ts` returns at least 7 (the 10 byte-identical D-17 G7 assertions; some may be combined)
    - `npm test -- src/db/database.migrations.test.ts` exits 0
    - `npm test` (full suite) exits 0 — confirms no cross-test contamination (other 28+ test files still pass with their jsdom-only setup)
    - `npx tsc -b` exits 0
    - `git diff src/db/backfill.test.ts | wc -l` returns 0 (backfill.test.ts is UNTOUCHED — pure-helper coverage preserved per D-05)
  </acceptance_criteria>
  <done>TEST-04 closed. database.migrations.test.ts runs against real fake-indexeddb-backed Dexie; D-17 G7 contract enforced at the integration boundary; pure-helper coverage in backfill.test.ts untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | New devDep `fake-indexeddb` introduces third-party code into the test environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-SC | Tampering | npm install fake-indexeddb | mitigate | **BLOCKING HUMAN CHECKPOINT (Task 1)** verifies package legitimacy via npmjs.com + GitHub before install. Package is `[ASSUMED]` until checkpoint confirms; never auto-approvable (workflow.auto_advance ignored). |
| T-23-08 | Tampering | fake-indexeddb shim leaking into other tests | mitigate | D-04 lock: per-test import only, NO vitest.setup.ts injection. Acceptance criterion `grep 'fake-indexeddb' vitest.setup.ts returns 0`. |
| T-23-09 | Information Disclosure | Test fixture data | accept | Synthetic fixture (alice@example.com, etc.) — no real PII. |

No production code is changed by this plan (only devDeps + a single test file). Existing mitigations preserved (T-15-03, CL-05).
</threat_model>

<verification>
After all 3 tasks:
- `npm run build` → exit 0 (production code unchanged; build still passes)
- `npm test` → all tests pass, no skipped; fake-indexeddb test runs at the database.migrations.test.ts file
- `npm ls fake-indexeddb` → returns a v6.x resolved version
- `npx tsc -b` → exit 0
- `grep -c "import 'fake-indexeddb/auto'" src/**/*.test.* 2>/dev/null` returns exactly 1 (only in database.migrations.test.ts — D-04 lock)
- `grep 'fake-indexeddb' vitest.setup.ts 2>/dev/null` returns 0 lines (D-04 lock)
- `git diff src/db/backfill.test.ts` returns empty (pure-helper coverage preserved per D-05)
</verification>

<success_criteria>
- fake-indexeddb in devDependencies at v6.x (TEST-04 prereq)
- src/db/database.migrations.test.ts replaced with real-Dexie integration test using fake-indexeddb (TEST-04)
- D-17 G7 contract assertions are byte-identical to src/db/backfill.test.ts equivalents (no contract drift)
- Blast radius limited to one file — vitest.setup.ts has zero fake-indexeddb references
- pure-helper layer (backfill.test.ts) is unchanged — defense in depth preserved
- Package legitimacy verified via blocking human checkpoint before install
- All gates green: `npm test`, `npx tsc -b`, `npm run build`
</success_criteria>

<output>
Create `.planning/phases/23-test-coverage-hardening/23-03-SUMMARY.md` when done.
</output>
