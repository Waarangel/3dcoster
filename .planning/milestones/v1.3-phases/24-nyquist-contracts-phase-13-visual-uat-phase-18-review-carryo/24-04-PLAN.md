---
phase: 24
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-VALIDATION.md
autonomous: true
requirements: [NYQ-04]

must_haves:
  truths:
    - "17-VALIDATION.md exists and is nyquist_compliant: true (ROADMAP success criterion 4)"
    - "Per-Task Map has at least one row per declared Phase 17 requirement (PDF-04, D-01, D-02, D-03) each pointing at `npm run build` as the automated_command with file_exists: ✅ existing"
    - "Wave 0 Requirements section explicitly states `None — existing 8-gate build chain (package.json:8) + scripts/assert-no-static-pdf-import.mjs cover all Phase 17 requirements` (per RESEARCH §1 minimum-viable shape)"
  artifacts:
    - path: ".planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-VALIDATION.md"
      provides: "Nyquist validation contract for Phase 17 — State B trivial reconstruction inheriting global guards"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "17-VALIDATION.md Per-Task Map rows"
      to: "scripts/assert-no-static-pdf-import.mjs + package.json:8 (the 8-gate build chain)"
      via: "automated_command: npm run build"
      pattern: "npm run build"
---

<objective>
NYQ-04: Author the Nyquist validation contract for Phase 17 (close-gap PDF-04) by running `/gsd:validate-phase 17`. Phase 17 is in **State B trivial** — no `17-VALIDATION.md` exists and the phase's 4 declared requirements (PDF-04 + D-01/02/03) inherit from pre-existing global guards: the 8-gate build chain in `package.json:8` and the Phase 17–added gate `scripts/assert-no-static-pdf-import.mjs`. Per CONTEXT.md D-03 + RESEARCH §1 minimum-viable shape, this plan is symmetric with NYQ-01..03 — does NOT fold in even though validation is trivial.

Purpose: Close TECH-DEBT D4 (Phase 17 missing Nyquist contract). Documents inheritance of global guards as a permanent artifact.

Output: A new `17-VALIDATION.md` with `nyquist_compliant: true`, a minimum-viable Per-Task Map (1 row per requirement), and Wave 0 Requirements explicitly stating "None — global guards cover all phase requirements."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/workflows/validate-phase.md
@$HOME/.claude/get-shit-done/templates/VALIDATION.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-CONTEXT.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-PATTERNS.md
@.planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md
@package.json
@scripts/assert-no-static-pdf-import.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run `/gsd:validate-phase 17` and verify produced VALIDATION.md (trivial-case)</name>
  <files>.planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-VALIDATION.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/workflows/validate-phase.md (the 8-step workflow this task invokes)
    - $HOME/.claude/get-shit-done/templates/VALIDATION.md (the canonical State B output template)
    - .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/ (the full phase directory — read all `17-NN-PLAN.md` + `17-NN-SUMMARY.md`)
    - package.json (specifically `package.json:8` — the 8-gate build script chain; required to document the global guards the Per-Task Map points at)
    - scripts/assert-no-static-pdf-import.mjs (the gate Phase 17 added — proves PDF-04 closure mechanism exists)
    - .planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md (reference shape for the Per-Task Map row format)
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md §1 ("Minimum-Viable VALIDATION.md (the Phase 17 case)" — explicit minimum-viable shape)
  </read_first>
  <action>
    Invoke `/gsd:validate-phase 17`. The workflow detects State B (no `17-VALIDATION.md`), spawns the `gsd-nyquist-auditor` at step 5 to scan Phase 17's plan SUMMARYs and discover the 4 declared requirements (PDF-04 + D-01, D-02, D-03). The auditor's gap analysis returns zero MISSING because each requirement is satisfied by an existing global guard. The workflow produces a minimum-viable `17-VALIDATION.md` per RESEARCH §1: (a) frontmatter `status: passed, nyquist_compliant: true, wave_0_complete: true`, (b) Test Infrastructure table populated with project Vitest 4.1.4 + the 8-gate build chain, (c) Per-Task Verification Map with one row per declared requirement, each row `automated_command: "npm run build"` and `file_exists: ✅ existing`, (d) Wave 0 Requirements section reading literally `None — existing 8-gate build chain (package.json:8) + scripts/assert-no-static-pdf-import.mjs cover all Phase 17 requirements.`, (e) Manual-Only Verifications section reading `None — all phase behaviors have automated verification.` Do NOT bypass the workflow. Commit with `gsd-sdk query commit "docs(phase-24): 17-VALIDATION.md — Nyquist compliance (NYQ-04)" --files ".planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-VALIDATION.md"` (no `--no-verify`).
  </action>
  <verify>
    <automated>test -f .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-VALIDATION.md && grep -cE "^nyquist_compliant: true$" .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-VALIDATION.md | grep -q '^1$' && grep -q "PDF-04" .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-VALIDATION.md && grep -q "npm run build" .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-VALIDATION.md && grep -q "assert-no-static-pdf-import.mjs\|8-gate" .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-VALIDATION.md && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-VALIDATION.md` exists (file created)
    - Frontmatter has `nyquist_compliant: true`, `status: passed`, `wave_0_complete: true`
    - Per-Task Map has ≥ 1 row referencing PDF-04 with `npm run build` as the automated command
    - Body explicitly references `scripts/assert-no-static-pdf-import.mjs` OR the phrase `8-gate build chain` (documenting the inherited global guard)
    - Wave 0 Requirements section exists and reads to the effect of "None — global guards cover all phase requirements"
    - `npm run build` exits 0 (the 8-gate chain stays green — proves the inherited guards still work)
    - Commit recorded with NYQ-04 message; pre-commit hooks ran
  </acceptance_criteria>
  <done>
    17-VALIDATION.md is the canonical Nyquist contract for Phase 17, documenting inheritance of global guards as a permanent artifact. NYQ-04 closes TECH-DEBT D4.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Plan creates a single `.planning/**` markdown file via an in-repo workflow. No untrusted input, no package installs, no runtime code changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | Tampering | `17-VALIDATION.md` minimum-viable contract | accept | Workflow-generated file from canonical template; the inherited global guards (`scripts/assert-no-static-pdf-import.mjs` + `package.json:8` chain) are themselves verified to exist before commit (read_first reads them). |
| T-24-04-02 | Information Disclosure | Global-guard inheritance documentation | accept | Public knowledge — the build chain is in `package.json` and the PDF-04 gate script is committed to the repo. No secrets exposed. |
</threat_model>

<verification>
- `npm run build` exits 0 (the 8-gate chain still works — proves the inherited guards documented in 17-VALIDATION.md are functional)
- `git log -1 --format=%s .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-VALIDATION.md` returns the NYQ-04 commit message
</verification>

<success_criteria>
ROADMAP success criterion #4 satisfied: `17-VALIDATION.md` exists and is `nyquist_compliant: true`.
</success_criteria>

<output>
Create `.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-04-SUMMARY.md` when done — summarizing the trivial-case outcome (0 declared requirements added, global guards inherited, commit hash).
</output>
