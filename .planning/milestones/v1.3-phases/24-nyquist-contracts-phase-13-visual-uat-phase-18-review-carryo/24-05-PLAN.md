---
phase: 24
plan: 05
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - .planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md
autonomous: false
requirements: [NYQ-05]

must_haves:
  truths:
    - "13-VERIFICATION.md frontmatter has status: passed (was human_needed) plus 3 new fields uat_closed/uat_closure_phase/uat_closure_req (ROADMAP success criterion 5)"
    - "D-04 implemented: smoke-test 2 items (Default Tax Rate field + Per-job Tax Rate round-trip) and rubber-stamp the other 6 with an explicit in-prod evidence note — not a full re-test of all 8 items"
    - "Smoke Test 1 (Default Tax Rate field) executed in `npm run dev` on port 4173 with all 9 sub-checks PASS — verifies real IndexedDB persistence (D-04b rationale)"
    - "Smoke Test 2 (Per-job Tax Rate round-trip) executed in `npm run dev` with all 8 sub-checks PASS — verifies editingJob?.taxRate hydration"
    - "13-VERIFICATION.md has a new `## UAT Closure (Phase 24 NYQ-05) — 2026-05-25` section with the Smoke-Tested table (items 1+2 PASS evidence) AND the Rubber-Stamped paragraph enumerating phases 14, 15, 15.1, 16, 17 + date range 2026-05-21 → 2026-05-25 + explicit `Phase 24 NYQ-05` reference (D-04a locked phrasing)"
  artifacts:
    - path: ".planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md"
      provides: "Phase 13 visual UAT formally closed — frontmatter flag flip + UAT Closure section appended"
      contains: "status: passed"
  key_links:
    - from: "13-VERIFICATION.md UAT Closure section"
      to: "STATE.md Roadmap Evolution (phases 14, 15, 15.1, 16, 17 merged 2026-05-21 → 2026-05-25)"
      via: "Rubber-Stamped paragraph citing in-prod evidence"
      pattern: "In-prod validated since 2026-05-21"
---

<objective>
NYQ-05: Close Phase 13's 8 deferred visual-contract UAT items by smoke-testing 2 items in `npm run dev` on port 4173 and rubber-stamping the other 6 with an explicit in-prod evidence note per CONTEXT.md D-04/D-04a. The 2 smoke-tested items (Default Tax Rate field + Per-job Tax Rate round-trip) are chosen per D-04b because they exercise real IndexedDB persistence — the only surfaces where a silent regression could hide from in-prod use. The other 6 items have been live across phases 14, 15, 15.1, 16, 17 with zero bug reports and are formally accepted as passed.

Purpose: Close TECH-DEBT D5 (Phase 13 visual UAT deferred items) and flip `13-VERIFICATION.md` from `status: human_needed` to `status: passed`.

Output: Updated `13-VERIFICATION.md` with frontmatter flag flip + new `## UAT Closure (Phase 24 NYQ-05) — 2026-05-25` section.

Depends on: 24-01 (NYQ-01 flips `13-VALIDATION.md` flags in Wave 1; this plan flips `13-VERIFICATION.md` status in Wave 2 — wave boundary prevents file-edit serialization conflicts between the validation contract and the verification report).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-CONTEXT.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-PATTERNS.md
@.planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md

<interfaces>
<!-- Smoke-test surface signatures the executor needs. Extracted from current tree on 2026-05-25. -->

From src/components/SettingsModal.tsx (lines 264-301 — Default Tax Rate UI):
  - Costs & Rates tab renders an `<Input value={userProfile.defaultTaxRate ?? ''} onChange={onUserProfileChange} />` at lines 264-301
  - `<NewBadge feature="default-tax-rate" />` at line 267 (adjacent to h3, anti-pattern-safe per project memory)
  - Empty-string handling at line 291 yields `undefined` (not `0`) — critical for the "clear field" sub-check

From src/components/CostCalculator.tsx (Per-job Tax Rate UI + persistence):
  - Line 129: state declaration `useState(() => editingJob?.taxRate)` — hydration on reopen
  - Lines 1232-1256: Input JSX in the 4th column of the `md:grid-cols-4` "Set Financial Targets" grid (≥768px width)
  - Lines 549-550: persistence in handleSaveJob update branch
  - Lines 578-579: persistence in handleSaveJob create branch

From src/features.ts:
  - `default-tax-rate` entry shipped 2026-05-21 — gates NewBadge rendering on the 14-day release-age + 36-hour seen window

13-VERIFICATION.md current frontmatter (line 1-7) — target of the flag flip:
  ```yaml
  ---
  phase: 13-tax-model-ui-sweep
  verified: 2026-05-21T13:35:00Z
  status: human_needed       # NYQ-05 flips to: passed
  score: 6/6 must-haves verified
  overrides_applied: 0
  ---
  ```

13-VERIFICATION.md target frontmatter (post NYQ-05 — adds 3 fields, flips 1):
  ```yaml
  ---
  phase: 13-tax-model-ui-sweep
  verified: 2026-05-21T13:35:00Z
  status: passed
  score: 6/6 must-haves verified
  overrides_applied: 0
  uat_closed: 2026-05-25
  uat_closure_phase: 24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryover
  uat_closure_req: NYQ-05
  ---
  ```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Smoke Test 1 — Default Tax Rate field IndexedDB persistence</name>
  <files>(no file modifications — verification-only)</files>
  <read_first>
    - .planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md (the source of truth for the 8 UAT items — item #1 is this smoke test's spec)
    - src/components/SettingsModal.tsx (read lines 1-310 — full Costs & Rates tab context for the Default Tax Rate input + NewBadge)
    - src/features.ts (Default Tax Rate badge entry — confirms 2026-05-21 release date so badge gating semantics are understood)
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md §4 "Smoke Test 1: Default Tax Rate field in Settings" (the 9 sub-checks the developer performs)
  </read_first>
  <what-built>
    The Default Tax Rate setting at `SettingsModal.tsx:264-301` writes `userProfile.defaultTaxRate` through `onUserProfileChange` into IndexedDB-backed Dexie state. The test verifies the field renders, persists across modal close/reopen, persists across a full hard-reload, and clears correctly to `undefined` (not `0`). This is one of two IndexedDB-backed surfaces NYQ-05 smoke-tests because a silent regression here would not surface during normal in-prod use (low-traffic surface — users rarely test the clear-and-reload cycle).
  </what-built>
  <action>
    The developer (user) performs the following — Claude does NOT execute these steps.

    Pre-condition: Kill any dev server running on port 4173 (per global CLAUDE.md "Kill all dev servers before starting a new one" + project CLAUDE.md port-pin).

    1. From repo root: `npm run dev` — starts Vite on port 4173.
    2. Wait for the Vite ready message. Open `http://localhost:4173` in a clean browser session (or incognito to avoid cached localStorage).
    3. Click the Settings gear icon (top-right) to open `SettingsModal`.
    4. Switch to the "Costs & Rates" tab.
    5. Confirm the following render:
       - A "Default Tax Rate" `<h3>` is present below "Default Profit Margin".
       - A NEW badge appears adjacent to (not pushing) the h3 text. If the 14-day release-age window has lapsed naturally OR the 36-hour seen window has elapsed, the badge may be absent — that is acceptable.
       - The input field is empty (placeholder "e.g., 20") on a fresh profile.
    6. Type `20` into the field. Close Settings (Escape or click outside).
    7. Reopen Settings → Costs & Rates → confirm the field shows `20`.
    8. Hard-reload the browser (Cmd-Shift-R) → reopen Settings → Costs & Rates → confirm the field still shows `20` (persisted via IndexedDB).
    9. Clear the field. Close + reopen Settings. Confirm the field is empty (NOT `0`) — verifies the `undefined`-on-empty semantics at `SettingsModal.tsx:291`.

    Stop the dev server when done (`Ctrl-C` in the terminal).
  </action>
  <verify>
    <human-check>All 9 sub-checks above pass. The developer attests to each pass before signing.</human-check>
  </verify>
  <acceptance_criteria>
    - Sub-check 5a passed: Default Tax Rate h3 renders below Default Profit Margin
    - Sub-check 5b passed: NewBadge renders adjacent (not pushing siblings) OR is absent due to expired gating window (acceptable)
    - Sub-check 5c passed: Empty input on fresh profile
    - Sub-check 7 passed: `20` typed; modal close/reopen shows `20`
    - Sub-check 8 passed: Hard-reload preserves `20` (IndexedDB round-trip confirmed)
    - Sub-check 9 passed: Clear → close/reopen shows empty (not `0`) — `undefined` semantics confirmed
  </acceptance_criteria>
  <done>
    Smoke Test 1 attested PASS. The Default Tax Rate field's IndexedDB-backed persistence + rendering contract is verified working on 2026-05-25.
  </done>
  <resume-signal>
    Type `pass-1` if all 9 sub-checks passed. If any sub-check failed, describe which one and STOP — do NOT proceed to Task 2 or Task 3. NYQ-05 cannot close until both smoke tests pass.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke Test 2 — Per-job Tax Rate input round-trip</name>
  <files>(no file modifications — verification-only)</files>
  <read_first>
    - .planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md (item #2 in the deferred-UAT section is this smoke test's spec)
    - src/components/CostCalculator.tsx (read lines 100-160 for state declaration context + lines 1200-1280 for the Set Financial Targets JSX + lines 540-590 for the handleSaveJob persistence branches)
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md §4 "Smoke Test 2: Per-job Tax Rate input round-trip" (the 8 sub-checks the developer performs)
  </read_first>
  <what-built>
    The Per-job Tax Rate input at `CostCalculator.tsx:1232-1256` (4th column of the `md:grid-cols-4` Set Financial Targets grid) reads from state initialized at line 129 via `useState(() => editingJob?.taxRate)`. The handleSaveJob function persists `taxRate` in both update (lines 549-550) and create (lines 578-579) branches. The test verifies set-on-save → reads-back-on-reopen hydration via IndexedDB. This is the second of two IndexedDB-backed smoke tests NYQ-05 requires (D-04b — silent regression hidden from in-prod use because set-once-and-forget surface).
  </what-built>
  <action>
    The developer (user) performs the following — Claude does NOT execute these steps.

    Pre-condition: Dev server fresh on port 4173 (kill any existing server first if needed). Browser session ready (existing test print job acceptable, or create a quick one).

    1. From repo root: `npm run dev` if not already running.
    2. With the cost calculator open (the default route at `http://localhost:4173`), select an existing PrintJob OR create a quick one with sellingPrice = `10`.
    3. In the "Set Financial Targets" section (4-column grid visible at ≥768px viewport width), enter `20` into the per-job Tax Rate input (the 4th column).
    4. Confirm the Cost Breakdown immediately shows `Tax (20.0%) $2.00` and `Total (with Tax) $12.00`.
    5. Save the job (click "Save Job" at the bottom of the calculator).
    6. Navigate to the Jobs list (JobsManager).
    7. Reopen the just-saved job by clicking it (returns to the cost calculator with `editingJob` populated).
    8. Confirm the per-job Tax Rate input shows `20` on reopen (verifies `CostCalculator.tsx:129` `useState(() => editingJob?.taxRate)` hydration).
    9. Confirm the Cost Breakdown still shows `Tax (20.0%) $2.00` and `Total (with Tax) $12.00`.

    Stop the dev server when done.
  </action>
  <verify>
    <human-check>All 8 sub-checks above pass. The developer attests to each pass before signing.</human-check>
  </verify>
  <acceptance_criteria>
    - Sub-check 3 passed: `20` entered into the per-job Tax Rate input (4th column)
    - Sub-check 4 passed: Cost Breakdown shows `Tax (20.0%) $2.00` and `Total (with Tax) $12.00` immediately
    - Sub-check 5 passed: Job saved successfully
    - Sub-check 8 passed: Reopened job shows per-job Tax Rate `20` (IndexedDB → state hydration confirmed)
    - Sub-check 9 passed: Cost Breakdown still shows `Tax (20.0%) $2.00` + `Total (with Tax) $12.00` post-reopen
  </acceptance_criteria>
  <done>
    Smoke Test 2 attested PASS. The Per-job Tax Rate set-on-save/read-on-reopen IndexedDB round-trip is verified working on 2026-05-25.
  </done>
  <resume-signal>
    Type `pass-2` if all 8 sub-checks passed. If any sub-check failed, describe which one and STOP — do NOT proceed to Task 3. NYQ-05 cannot close until both smoke tests pass.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply UAT closure to 13-VERIFICATION.md — frontmatter flag flip + new UAT Closure section</name>
  <files>.planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md (current state — read in full so the edit preserves all 8 items + existing verification content)
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md §4 "YAML/Markdown Edits to 13-VERIFICATION.md" (the locked target frontmatter shape + the locked appended section template with the rubber-stamp phrasing)
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-CONTEXT.md `<specifics>` (the D-04a locked-phrasing requirements — phases 14/15/15.1/16/17 enumerated, date range 2026-05-21 → 2026-05-25, "Phase 24 NYQ-05" cited)
    - .planning/STATE.md (confirms phases 14, 15, 15.1, 16, 17 merged in the 2026-05-21 → 2026-05-25 window — evidence backing the rubber-stamp note)
  </read_first>
  <action>
    Edit `.planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md` in two parts:

    Part 1 — Frontmatter flag flip + 3 new fields:
    - Flip `status: human_needed` → `status: passed`
    - Add three new frontmatter fields (after `overrides_applied: 0`):
      - `uat_closed: 2026-05-25`
      - `uat_closure_phase: 24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryover`
      - `uat_closure_req: NYQ-05`
    - Preserve `verified: 2026-05-21T13:35:00Z` unchanged (the original 6/6 truth verification still holds; new `uat_closed` records the formal UAT close date).

    Part 2 — Append a new `## UAT Closure (Phase 24 NYQ-05) — 2026-05-25` section before the terminal `_Verified..._` line (if present) and after the existing "Gaps Summary" / "Human Verification Required" content. The section MUST contain:

    - Heading: `## UAT Closure (Phase 24 NYQ-05) — 2026-05-25`
    - One-line lead-in: "The 8 deferred visual UAT items above are formally closed per Phase 24 NYQ-05 with the following disposition:"
    - Sub-section `### Smoke-Tested (2 items)` with a Markdown table of two rows:
      | # | Item | Result | Evidence |
      | 1 | Default Tax Rate field in Settings — render + IndexedDB persistence across modal close/reopen + full app reload | PASS | Verified manually in `npm run dev` on 2026-05-25: value `20` typed, persisted across modal close/reopen, persisted across hard-reload, cleared correctly to empty (undefined semantics confirmed at SettingsModal.tsx:291). |
      | 2 | Per-job Tax Rate input round-trip — set on save, reads back on reopen from `editingJob?.taxRate` | PASS | Verified manually in `npm run dev` on 2026-05-25: rate=20 saved on a test job; reopen showed `20` rehydrated from IndexedDB; Cost Breakdown showed `Tax (20.0%) $2.00` + `Total (with Tax) $12.00`. |
    - Sub-section `### Rubber-Stamped — In-Prod Evidence (6 items)` enumerating items 3 (Tax row hides at 0%), 4 (Fallback chain tooltip provenance — 4 scenarios), 5 (4-column Set Financial Targets grid layout), 6 (NewBadge `default-tax-rate` renders without pushing siblings), 7 (stale NewBadge sites no longer render across 7 surfaces), and 8 (UI-08 compact width visual consistency across 5 components / 60 compact inputs).
    - Locked rubber-stamp paragraph (D-04a phrasing — MUST appear verbatim): "In-prod validated since 2026-05-21 across phases 14, 15, 15.1, 16, and 17 with zero bug reports — formally accepted as passed per Phase 24 NYQ-05."
    - Explanatory follow-up paragraph: "The phases stacked on top of Phase 13 (Phase 14: Customer block on Sale + Etsy helper; Phase 15: Tags + search + duplicate; Phase 15.1: Customer Library; Phase 16: PDF generation; Phase 17: PDF-04 / Rollup circular chunk + tax rounding parity) all exercised these visual surfaces during their own UAT cycles, and no regressions were reported by the user across the 2026-05-21 → 2026-05-25 in-prod window."
    - Closing line: "Phase 13 visual contract is now formally closed."

    Do NOT execute this task until both Task 1 (Smoke Test 1) and Task 2 (Smoke Test 2) have signaled `pass-1` and `pass-2` respectively. If either smoke test failed, this task does not run.

    Commit with `gsd-sdk query commit "docs(phase-24): close Phase 13 UAT — 2 smoke + 6 rubber-stamp (NYQ-05)" --files ".planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md"` (no `--no-verify`).
  </action>
  <verify>
    <automated>grep -cE "^status: passed$" .planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md | grep -q '^1$' && grep -cE "^uat_closed: 2026-05-25$" .planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md | grep -q '^1$' && grep -cE "^uat_closure_req: NYQ-05$" .planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md | grep -q '^1$' && grep -q "## UAT Closure (Phase 24 NYQ-05) — 2026-05-25" .planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md && grep -q "In-prod validated since 2026-05-21 across phases 14, 15, 15.1, 16, and 17 with zero bug reports — formally accepted as passed per Phase 24 NYQ-05" .planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `13-VERIFICATION.md` frontmatter has `status: passed` (was `human_needed`)
    - Frontmatter has `uat_closed: 2026-05-25`
    - Frontmatter has `uat_closure_phase: 24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryover`
    - Frontmatter has `uat_closure_req: NYQ-05`
    - Body contains a new section header `## UAT Closure (Phase 24 NYQ-05) — 2026-05-25`
    - The exact phrase `In-prod validated since 2026-05-21 across phases 14, 15, 15.1, 16, and 17 with zero bug reports — formally accepted as passed per Phase 24 NYQ-05.` appears in the new section (D-04a locked verbatim)
    - The Smoke-Tested table has two rows both marked PASS with 2026-05-25 evidence
    - The Rubber-Stamped paragraph enumerates phases 14, 15, 15.1, 16, 17 explicitly
    - Existing "Human Verification Required" section + 6/6 truth verification content preserved (no destructive edits — only frontmatter mutation + appended section)
    - Commit recorded with NYQ-05 message; pre-commit hooks ran
  </acceptance_criteria>
  <done>
    13-VERIFICATION.md is formally closed: status passed, UAT Closure section appended with locked phrasing, in-prod evidence enumerated. NYQ-05 closes TECH-DEBT D5.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Plan modifies a single `.planning/**` markdown file. Two smoke tests run locally in `npm run dev` against the developer's own IndexedDB — no network, no untrusted input. No package installs. No runtime code changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-01 | Tampering | `13-VERIFICATION.md` status flip | accept | Doc-only edit committed via `gsd-sdk query commit` with pre-commit hooks active; git history records the change. Existing content preserved (no destructive edits — only frontmatter mutation + appended section). |
| T-24-05-02 | Repudiation | Smoke-test attestation | mitigate | Two blocking `checkpoint:human-verify` tasks require explicit `pass-1` / `pass-2` resume signals; failed sub-checks block Task 3 from running. Locked rubber-stamp phrasing per D-04a prevents soft "looks fine" rubber-stamping — the executor MUST cite the 4-day window + 5 specific phase numbers. |
| T-24-05-03 | Information Disclosure | UAT evidence content | accept | Evidence references internal Phase 13 surfaces (SettingsModal, CostCalculator); no PII, no secrets, no third-party data. |
</threat_model>

<verification>
- Both smoke tests attested PASS via resume signals `pass-1` and `pass-2`
- 13-VERIFICATION.md frontmatter has all 4 mutations (status flip + 3 new fields)
- The locked D-04a phrase appears verbatim in the body
- `git log -1 --format=%s .planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md` returns the NYQ-05 commit message
</verification>

<success_criteria>
ROADMAP success criterion #5 satisfied: Phase 13's 8 deferred visual-contract UAT items completed; `13-VERIFICATION.md` `status` flipped from `human_needed` to `passed`.
</success_criteria>

<output>
Create `.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-05-SUMMARY.md` when done — recording both smoke-test results, the markdown edit, and the commit hash.
</output>
