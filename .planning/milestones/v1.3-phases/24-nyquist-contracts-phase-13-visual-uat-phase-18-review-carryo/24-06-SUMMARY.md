---
phase: 24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo
plan: 06
subsystem: hygiene
tags: [tauri, dep-bump, pdf, error-handling, vitest, cargo, npm-dedup]

# Dependency graph
requires:
  - phase: 18-tauri-fs-scope-fix
    provides: "WR-01/02/03 review findings recorded in 18-REVIEW.md; @tauri-apps/api 2.10.1 pin (set as Rule-1 deviation during Phase 18 execution); generateQuotePdf.ts:337 forbidden-path branch + corresponding error-mapping test scaffold"
  - phase: 24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo
    provides: "Wave boundary serialization clarity — 24-05 owned .planning/** UAT closure files; 24-06 (this plan) owns the code/dep files; no file conflict"
provides:
  - "Tightened forbidden-path matcher (startsWith('forbidden path:')) anchored to plugins-workspace/v2/plugins/fs/src/error.rs#PathForbidden literal format"
  - "Collapsed single-invocation forbidden-path test using combined regex /Cannot save to \"...\" — this location is restricted/"
  - "Single @tauri-apps/api copy in built bundle (top-level 2.11.0 only; nested copies under plugin-fs + plugin-dialog eliminated)"
  - "Tauri Rust crate aligned with JS api crate at the 2.11.x floor (compiles to tauri-v2.11.2)"
affects: [v1.3-Phase 25 polish/doc batch, future Tauri dep bump plans (D-06 conditional-UAT pattern), PDF error-handling consumers]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "WR-bundling: causally-related Phase 18 review findings shipped as one plan with atomic per-task commits (D-02)"
    - "Conditional-UAT pattern (D-06): gate manual UAT on RESEARCH §2 changelog-derived risk evidence, not on every routine bump"
    - "Anchored-prefix matcher: prefer startsWith() with literal Rust error format over loose includes() substring match (locks the matcher to the upstream PathForbidden #[error(\"forbidden path: {0}\")] format)"

key-files:
  created:
    - ".planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-06-SUMMARY.md"
  modified:
    - "src/pdf/generateQuotePdf.ts (WR-03: tightened forbidden-path matcher + 2 explanatory comment lines)"
    - "src/pdf/generateQuotePdf.test.ts (WR-01: collapsed double-invocation forbidden-path test to single invocation with combined regex)"
    - "src-tauri/Cargo.toml (WR-02: tauri crate 2 → 2.11)"
    - "src-tauri/Cargo.lock (WR-02: regenerated; 950-line diff — Tauri-bump churn matching Phase 18 pattern)"
    - "package.json (WR-02: @tauri-apps/api ^2.10.1 → ^2.11.0)"
    - "package-lock.json (WR-02: regenerated; 60-line diff; 2 nested @tauri-apps/api copies removed)"
    - "src-tauri/gen/schemas/acl-manifests.json (Rule 3 auto-fix: regenerated by tauri-build from new crate permissions)"
    - "src-tauri/gen/schemas/desktop-schema.json (Rule 3 auto-fix: regenerated by tauri-build)"
    - "src-tauri/gen/schemas/macOS-schema.json (Rule 3 auto-fix: regenerated by tauri-build)"

key-decisions:
  - "WR-02 D-05 strategy (b) confirmed on the happy path — Rust crate bump + JS api re-pin succeeded; D-05a strategy (a) plugin-downgrade rejected and not attempted; D-05b strategy (c) dual-copy acceptance reserved as escape hatch only and not needed"
  - "D-06 conditional-UAT call validated empirically — npm test passed 272/272, npm run tauri build produced 3DCoster.app + 3DCoster_1.3.1_aarch64.dmg with no PDF/save regressions; routine post-bump UAT-A re-run skipped per RESEARCH §2 evidence (zero fs:*/dialog/capability-ACL local-origin semantic changes in 2.11.x)"
  - "Task 3 conditional checkpoint SKIPPED — Task 2 verification chain exited 0 across all 8 gates; the checkpoint:human-verify rollback / accept-dual-copy fallback never fired"
  - "Schema files (gen/schemas/*.json) committed alongside the bump per the Phase 18 f6b790b pattern (auto-regenerated by tauri-build from crate permissions metadata; tracked artifacts) — Rule 3 deviation to keep the worktree clean"

patterns-established:
  - "Pattern 1: Atomic per-WR commit inside a bundled WR plan — Task 1 ships WR-01 + WR-03 together as one commit (both are tiny TS edits in the same subsystem and share verify chain); Task 2 ships WR-02 alone (large dep churn deserves its own commit)"
  - "Pattern 2: Pre-flight dedup count + post-bump dedup count for Tauri version bumps — capture find counts BEFORE and AFTER npm install so the dedup outcome is auditable in the commit message (was-8 / now-6; was-2-nested / now-0-nested)"
  - "Pattern 3: Worktree-aware deviation handling — when running in a worktree with empty node_modules, run npm install BEFORE any vitest verification (saves a confusing 'Failed to resolve import' error masquerading as a code bug)"

requirements-completed: []

# Metrics
duration: 12min
completed: 2026-05-25
---

# Phase 24 Plan 06: Phase 18 Review Carryover (WR-01 + WR-02 + WR-03) Summary

**Tightened the PDF forbidden-path matcher to anchor on the Rust crate's literal error format, collapsed the double-invocation forbidden-path test to one call with a combined regex, and bumped the Tauri Rust crate to 2.11.x with @tauri-apps/api re-pinned to ^2.11.0 — eliminating the two nested @tauri-apps/api copies that Phase 18 carried as known tech debt.**

## Performance

- **Duration:** ~12 min (excluding cargo cold-compile time)
- **Started:** 2026-05-25T20:33:00Z
- **Completed:** 2026-05-25T20:45:08Z
- **Tasks:** 2 of 3 (Task 3 SKIPPED — conditional checkpoint did not fire on the happy path)
- **Files modified:** 9 (2 TS + 4 dep files + 3 auto-regenerated tauri schemas)

## Accomplishments

- WR-01 (ROADMAP success criterion 6): sticky-mock fragility eliminated — `generateQuotePdf.test.ts` forbidden-path test collapsed from 2 invocations to 1 with a combined regex anchoring the savePath echo at a specific position in the rewritten message
- WR-02 (ROADMAP success criterion 7): `@tauri-apps/api` dual-copy collapsed — `find node_modules/@tauri-apps -path '*/node_modules/*/api/package.json' | wc -l` returns **0** (was 2); total `find node_modules/@tauri-apps -name package.json | wc -l` returns **6** (was 8). Tauri Rust crate at 2.11.2; JS api crate at 2.11.0
- WR-03 (ROADMAP success criterion 8): `generateQuotePdf.ts:337` forbidden-path matcher tightened from `includes('forbidden path')` to `startsWith('forbidden path:')` — anchored to the Rust crate's literal `#[error("forbidden path: {0}")]` format; false-positive matches on unrelated errors that incidentally contain "forbidden path" eliminated
- All 8 build-chain gates green post-bump: `npx tsc -b` exit 0, `npm test` 272 passed + 1 todo, `npm run build` exit 0 (main chunk 54.6 KB gz unchanged), `npm run tauri build` exit 0 with `3DCoster.app` + `3DCoster_1.3.1_aarch64.dmg` bundles produced

## Task Commits

Each task was committed atomically:

1. **Task 1: WR-01 + WR-03 combined TS edits** — `31d1e35` (fix)
   - `src/pdf/generateQuotePdf.ts` + `src/pdf/generateQuotePdf.test.ts`
   - 4 insertions, 5 deletions; 28/28 PDF tests pass; em-dash (U+2014) preserved verbatim from `generateQuotePdf.ts:339`
2. **Task 2: WR-02 Tauri 2.11.x dep bump** — `524b168` (chore)
   - `src-tauri/Cargo.toml` + `src-tauri/Cargo.lock` + `package.json` + `package-lock.json` + 3 auto-regenerated `src-tauri/gen/schemas/*.json` files (Rule 3 deviation; same pattern Phase 18 followed in f6b790b)
   - 464 insertions, 153 deletions; `cargo check` exit 0; all 8 verification gates green

**Task 3: SKIPPED** — Task 3 was a `checkpoint:human-verify` with `gate="conditional"`, gated on Task 2's verification chain exiting non-zero. Task 2 passed cleanly across all 8 gates, so the checkpoint never fired. Per CONTEXT.md `<specifics>` lock: the executor did not silently switch strategies or attempt the rejected strategy (a) plugin-downgrade — strategy (b) was applied as planned and succeeded.

## Files Created/Modified

- `src/pdf/generateQuotePdf.ts` — WR-03: matcher tightened to `startsWith('forbidden path:')`; 2 new comment lines documenting the anchor rationale
- `src/pdf/generateQuotePdf.test.ts` — WR-01: forbidden-path test collapsed from `[toThrow(/this location is restricted/), toThrow(/savePath/)]` to a single `toThrow(/Cannot save to "<savePath>" — this location is restricted/)`. Sibling tests at lines 527 (disk full) and 537 (no-Tauri pass-through) unchanged.
- `src-tauri/Cargo.toml` — `tauri = { version = "2", ... }` → `tauri = { version = "2.11", ... }`
- `src-tauri/Cargo.lock` — regenerated by `cargo check`; resolves to `tauri v2.11.2`, `tauri-runtime v2.11.2`, `tauri-runtime-wry v2.11.2`, `tauri-plugin-fs v2.5.0`, `tauri-plugin-dialog v2.7.0` (950-line diff)
- `package.json` — `"@tauri-apps/api": "^2.10.1"` → `"@tauri-apps/api": "^2.11.0"`
- `package-lock.json` — regenerated by `npm install`; `removed 2 packages` (the two nested `@tauri-apps/api` copies under plugin-fs + plugin-dialog); 60-line diff
- `src-tauri/gen/schemas/{acl-manifests,desktop-schema,macOS-schema}.json` — regenerated by `tauri-build` from the new crate's permission metadata (Rule 3 auto-fix; see Deviations below)

## Decisions Made

- **D-05 strategy (b) confirmed empirically.** RESEARCH §2's prediction that bumping the Rust crate + re-pinning `@tauri-apps/api` to `^2.11.0` would dedup the two nested api copies held true — `removed 2 packages` from `npm install` matched the predicted 8→6 package.json reduction exactly. No fallback was required.
- **D-06 conditional-UAT call held.** The Tauri 2.11.x changelog evidence in RESEARCH §2 (zero `fs:*`/dialog/capability-ACL local-origin semantic changes) matched runtime behavior — the post-bump test suite + `npm run tauri build` exited 0 with no PDF/save regressions. The "don't run a 5-minute manual UAT on every routine dep bump" pattern is validated for future Tauri bumps in the v1.3 milestone.
- **Schema file commit inclusion (Rule 3 deviation).** The `src-tauri/gen/schemas/*.json` files are tracked artifacts auto-regenerated by `tauri-build`. The plan's `files_modified` listed only the 4 dep files, but committing those without the schemas would leave the worktree dirty. Phase 18 (commit f6b790b) committed these alongside the same kind of crate change — followed that precedent.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Empty `node_modules` in spawn-time worktree blocked vitest verification**
- **Found during:** Task 1 verification (`npx vitest run src/pdf/generateQuotePdf.test.ts`)
- **Issue:** The worktree's `node_modules/` directory was empty (created by the worktree-spawn machinery but unpopulated). vitest reported `Failed to resolve import "@tauri-apps/plugin-dialog" from "src/pdf/generateQuotePdf.test.ts"` — masquerading as a code bug.
- **Fix:** Ran `npm install` in the worktree root. Installed 650 packages cleanly.
- **Files modified:** none (npm install populated `node_modules/` but the lockfile was already committed — `package-lock.json` was not touched at this step)
- **Verification:** Subsequent `npx vitest run src/pdf/generateQuotePdf.test.ts` ran 28/28 tests pass
- **Committed in:** Not committed (`node_modules/` is gitignored). Documented here for executor-pattern visibility on future worktree-spawned plans.

**2. [Rule 3 - Blocking] Auto-regenerated tauri schemas would leave worktree dirty**
- **Found during:** Task 2 post-`npm run tauri build`
- **Issue:** `tauri-build` regenerated `src-tauri/gen/schemas/{acl-manifests,desktop-schema,macOS-schema}.json` to reflect the new crate's permission metadata. These are tracked files (committed in Phase 18 f6b790b alongside the same kind of crate change). Leaving them uncommitted would leave the worktree dirty after the WR-02 commit, blocking SUMMARY.md commit and violating Rule 3 (post-commit cleanliness invariant).
- **Fix:** Staged the 3 schema files alongside the 4 dep files in Task 2's commit; documented in commit message + this deviation entry.
- **Files modified:** `src-tauri/gen/schemas/acl-manifests.json`, `src-tauri/gen/schemas/desktop-schema.json`, `src-tauri/gen/schemas/macOS-schema.json`
- **Verification:** `git status --short` post-commit returned empty (clean tree); diff was deterministic tauri-build output, no manual edits
- **Committed in:** `524b168` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 3 blocking — worktree environment + commit-completeness invariants).
**Impact on plan:** Zero scope creep. Both deviations are infrastructure-level (env setup + auto-generated artifact handling) and explicitly excluded from the plan's expected `files_modified` set only because PLAN.md predates the worktree-spawn workflow that this executor runs under. They do not change the WR-01/02/03 substantive deliverables.

## Threat-Model Mitigations Applied

- **T-24-06-03 (Information Disclosure via false-positive forbidden-path rewrite):** mitigated as planned by WR-03 — `startsWith('forbidden path:')` excludes unrelated errors that incidentally contain "forbidden path" from triggering the savePath-disclosure rewrite branch.
- **T-24-06-04 (Denial of Service from broken build state):** N/A — Task 2 verification passed; conditional Task 3 checkpoint never fired. The rollback/accept-dual-copy escape hatch was reserved but not consumed.
- **T-24-06-05 (Elevation of Privilege — Tauri 2.11.1 remote-origin ACL hardening):** mitigation came for free with the bump (3DCoster is local-origin-only so practical risk is low, but the fix is now in the bundle).
- **T-24-06-01/02/SC (supply-chain tampering):** accepted as planned — same publishers (`tauri-apps` on npm + crates.io); Phase 18 audit cleared the package set; no `[ASSUMED]`/`[SUS]` flags raised.

## Final Dedup Verification

Per ROADMAP success criterion 7:

| Metric                                                                           | Pre-bump | Post-bump | Target |
|----------------------------------------------------------------------------------|----------|-----------|--------|
| `find node_modules/@tauri-apps -name package.json \| wc -l`                      | 8        | 6         | 6      |
| `find node_modules/@tauri-apps -path '*/node_modules/*/api/package.json' \| wc -l` | 2        | 0         | 0      |
| Top-level `@tauri-apps/api` version                                              | 2.10.1   | 2.11.0    | ≥2.11.0 |
| Rust `tauri` crate compiled version                                              | 2.10.x   | 2.11.2    | ≥2.11.0 |

Both ROADMAP success criterion 7 metrics achieved exactly as predicted by RESEARCH §2.

## Issues Encountered

None requiring problem-solving beyond the two Rule 3 deviations above. The Tauri 2.11.x bump applied cleanly on the first attempt.

## User Setup Required

None — no external service configuration required; all changes are in-repo (TS code + Rust crate version + npm dep pin + auto-regenerated tracked artifacts).

## Next Phase Readiness

- **Phase 24 wave 3 closed.** With this plan, Phase 18 review carryover items WR-01, WR-02, WR-03 are all shipped. ROADMAP success criteria 6, 7, 8 satisfied.
- **Phase 25 polish batch ready.** POL-03 (jspdf module augmentation in `generateQuotePdf.ts`) is now adjacent to WR-03's tightened matcher in the same file — Phase 25 can land that polish on top of this clean foundation.
- **D-06 conditional-UAT pattern validated.** Future Tauri minor-bump plans in v1.3+ can follow the same `RESEARCH §2 changelog-evidence-gates-UAT-trigger` shape established here.

## Self-Check: PASSED

Verified before writing this section:

- **Task 1 commit exists:** `git log --oneline --all | grep -q "31d1e35"` → FOUND
- **Task 2 commit exists:** `git log --oneline --all | grep -q "524b168"` → FOUND
- **WR-03 grep target:** `grep -c "startsWith('forbidden path:')" src/pdf/generateQuotePdf.ts` → 1 (FOUND)
- **WR-03 negative grep:** `grep -c "includes('forbidden path')" src/pdf/generateQuotePdf.ts` → 0 (correctly absent)
- **WR-01 combined regex:** `grep -c 'Cannot save to "' src/pdf/generateQuotePdf.test.ts` → 1 (FOUND)
- **WR-02 Cargo.toml:** `grep -q 'tauri = { version = "2.11"' src-tauri/Cargo.toml` → matches (FOUND)
- **WR-02 package.json:** `grep -q '"@tauri-apps/api": "\^2.11.0"' package.json` → matches (FOUND)
- **WR-02 dedup (nested copies = 0):** `find node_modules/@tauri-apps -path '*/node_modules/*/api/package.json' | wc -l` → 0 (FOUND target)
- **WR-02 dedup (total package.json = 6):** `find node_modules/@tauri-apps -name package.json | wc -l` → 6 (FOUND target)
- **8-gate build chain post-bump:** `npm run build` exit 0 + `npm run tauri build` exit 0 + `npm test` 272 passed + `npx tsc -b` exit 0 — all green

---
*Phase: 24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo*
*Plan: 06 (Phase 18 review carryover — WR-01 + WR-02 + WR-03 bundled)*
*Completed: 2026-05-25*
