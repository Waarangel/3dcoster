---
phase: 26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18-tauri-fs-scope-fix/18-VALIDATION.md
  - .planning/phases/20-dexie-atomicity-audit/20-VALIDATION.md
  - .planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md
  - .planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md
autonomous: true
requirements:
  - DOC-03
tags:
  - validation
  - nyquist
  - documentation

must_haves:
  truths:
    - "All 4 existing VALIDATION.md files have status: passed in frontmatter"
    - "All 4 existing VALIDATION.md files have nyquist_compliant: true in frontmatter"
    - "All 4 existing VALIDATION.md files have wave_0_complete: true in frontmatter"
    - "Per-task validation maps in all 4 files are unchanged (D-04: mechanical frontmatter only)"
  artifacts:
    - path: ".planning/phases/18-tauri-fs-scope-fix/18-VALIDATION.md"
      provides: "Nyquist-compliant Phase 18 contract"
      contains: "status: passed"
    - path: ".planning/phases/20-dexie-atomicity-audit/20-VALIDATION.md"
      provides: "Nyquist-compliant Phase 20 contract"
      contains: "status: passed"
    - path: ".planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md"
      provides: "Nyquist-compliant Phase 22 contract"
      contains: "status: passed"
    - path: ".planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md"
      provides: "Nyquist-compliant Phase 22.1 contract"
      contains: "status: passed"
  key_links:
    - from: ".planning/v1.3-MILESTONE-AUDIT.md tech_debt"
      to: "Phase 18/20/22/22.1 VALIDATION.md frontmatter"
      via: "audit re-run reads these fields to determine Nyquist compliance"
      pattern: "status:\\s*passed"
---

<objective>
Flip stale frontmatter on 4 existing VALIDATION.md files so the v1.3 milestone audit no longer flags them as Nyquist-incomplete. Each phase's code-evidence is verified after the fact (VERIFICATION.md status: passed for all 4) — this plan brings the Nyquist contract documents into alignment with reality per CONTEXT.md D-01, D-02, D-03, D-04.

Purpose: Close the "VALIDATION.md draft state" tech_debt item from `.planning/v1.3-MILESTONE-AUDIT.md` so `/gsd:complete-milestone v1.3` can tag cleanly.

Output: 4 VALIDATION.md files with frontmatter flipped per locked decisions; no body changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m/26-CONTEXT.md
@.planning/v1.3-MILESTONE-AUDIT.md
@./CLAUDE.md
@./.claude/CLAUDE.md

<!--
Per CONTEXT.md D-01..D-04 (the only source of truth for this plan):
- D-01: status field → `passed` for all 4
- D-02: nyquist_compliant → `true` for Phase 20/22/22.1; Phase 18 already true (leave as-is)
- D-03: wave_0_complete → `true` for all 4 (semantic flip — historical drift acknowledged)
- D-04: Per-task validation map content is NOT rewritten; frontmatter only

Current frontmatter snapshots (read into context — Task actions reference exact line replacements):

Phase 18 (`.planning/phases/18-tauri-fs-scope-fix/18-VALIDATION.md` lines 1-8):
  phase: 18
  slug: tauri-fs-scope-fix
  status: pending           ← flip to: passed
  nyquist_compliant: true   ← leave as-is
  wave_0_complete: false    ← flip to: true
  created: 2026-05-25

Phase 20 (`.planning/phases/20-dexie-atomicity-audit/20-VALIDATION.md` lines 1-8):
  phase: 20
  slug: dexie-atomicity-audit
  status: draft             ← flip to: passed
  nyquist_compliant: false  ← flip to: true
  wave_0_complete: false    ← flip to: true
  created: 2026-05-26

Phase 22 (`.planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md` lines 1-8):
  phase: 22
  slug: jobsmanager-decomposition-perf
  status: draft             ← flip to: passed
  nyquist_compliant: false  ← flip to: true
  wave_0_complete: false    ← flip to: true
  created: 2026-05-27

Phase 22.1 (`.planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md` lines 1-8):
  phase: 22.1
  slug: break-even-formula-reconciliation
  status: draft             ← flip to: passed
  nyquist_compliant: false  ← flip to: true
  wave_0_complete: false    ← flip to: true
  created: 2026-05-27
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip Phase 18 + Phase 20 VALIDATION.md frontmatter</name>
  <files>.planning/phases/18-tauri-fs-scope-fix/18-VALIDATION.md, .planning/phases/20-dexie-atomicity-audit/20-VALIDATION.md</files>
  <read_first>
    - .planning/phases/26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m/26-CONTEXT.md (D-01..D-04 lock semantics)
    - .planning/phases/18-tauri-fs-scope-fix/18-VALIDATION.md (read full file to confirm current frontmatter lines 1-8 match snapshot; body must remain byte-identical after edit)
    - .planning/phases/20-dexie-atomicity-audit/20-VALIDATION.md (read full file — same reason)
  </read_first>
  <action>
    Use Edit tool (not Write — body must NOT change) on both files. Make ONLY frontmatter edits, exact replacements:

    File 1: `.planning/phases/18-tauri-fs-scope-fix/18-VALIDATION.md`
    - Replace `status: pending` → `status: passed` (per D-01)
    - Replace `wave_0_complete: false` → `wave_0_complete: true` (per D-03)
    - DO NOT modify `nyquist_compliant: true` (per D-02 — Phase 18 already correct)
    - DO NOT modify any line from line 9 onward (body — Per-Task Verification Map, Wave 0 Requirements, etc. — preserved verbatim per D-04)

    File 2: `.planning/phases/20-dexie-atomicity-audit/20-VALIDATION.md`
    - Replace `status: draft` → `status: passed` (per D-01)
    - Replace `nyquist_compliant: false` → `nyquist_compliant: true` (per D-02)
    - Replace `wave_0_complete: false` → `wave_0_complete: true` (per D-03)
    - DO NOT modify any line from line 9 onward (body preserved verbatim per D-04)

    Do NOT add `audited:` or any new frontmatter fields — D-04 limits this plan to flipping the 3 existing fields only. Do NOT touch Phase 21's VALIDATION.md (already `status: verified`, not in scope).
  </action>
  <verify>
    <automated>
      grep -E "^status: passed$" .planning/phases/18-tauri-fs-scope-fix/18-VALIDATION.md \
        && grep -E "^nyquist_compliant: true$" .planning/phases/18-tauri-fs-scope-fix/18-VALIDATION.md \
        && grep -E "^wave_0_complete: true$" .planning/phases/18-tauri-fs-scope-fix/18-VALIDATION.md \
        && grep -E "^status: passed$" .planning/phases/20-dexie-atomicity-audit/20-VALIDATION.md \
        && grep -E "^nyquist_compliant: true$" .planning/phases/20-dexie-atomicity-audit/20-VALIDATION.md \
        && grep -E "^wave_0_complete: true$" .planning/phases/20-dexie-atomicity-audit/20-VALIDATION.md \
        && test $(grep -cE "^status: (pending|draft)$|^nyquist_compliant: false$|^wave_0_complete: false$" .planning/phases/18-tauri-fs-scope-fix/18-VALIDATION.md .planning/phases/20-dexie-atomicity-audit/20-VALIDATION.md) -eq 0
    </automated>
  </verify>
  <acceptance_criteria>
    - File 1 (`18-VALIDATION.md`): `grep -c "^status: passed$"` returns 1; `grep -c "^nyquist_compliant: true$"` returns 1; `grep -c "^wave_0_complete: true$"` returns 1
    - File 2 (`20-VALIDATION.md`): same 3 grep counts each return 1
    - Neither file contains any of: `status: pending`, `status: draft`, `nyquist_compliant: false`, `wave_0_complete: false`
    - `git diff --stat` shows ≤ 5 lines changed per file (frontmatter only; D-04 prevents body edits)
    - Phase 18 body (lines 9+) byte-identical to pre-edit (`git diff -- .planning/phases/18-tauri-fs-scope-fix/18-VALIDATION.md` shows only frontmatter lines)
    - Phase 20 body (lines 9+) byte-identical to pre-edit (same diff check)
  </acceptance_criteria>
  <done>
    Phase 18 and Phase 20 VALIDATION.md frontmatter reflects `status: passed`, `nyquist_compliant: true`, `wave_0_complete: true`. No body changes. Single atomic commit ready (recommended message: `docs(26-01): flip Phase 18+20 VALIDATION.md frontmatter to passed/compliant per D-01/02/03`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip Phase 22 + Phase 22.1 VALIDATION.md frontmatter</name>
  <files>.planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md, .planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m/26-CONTEXT.md (D-01..D-04)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md (confirm current frontmatter matches snapshot before editing)
    - .planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md (same)
  </read_first>
  <action>
    Use Edit tool on both files. Frontmatter-only edits, exact replacements:

    File 1: `.planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md`
    - Replace `status: draft` → `status: passed` (per D-01)
    - Replace `nyquist_compliant: false` → `nyquist_compliant: true` (per D-02)
    - Replace `wave_0_complete: false` → `wave_0_complete: true` (per D-03)
    - DO NOT modify any line from line 9 onward (body — Per-Task Map with 6 plan rows, Wave 0 Requirements, Manual-Only Verifications — preserved verbatim per D-04)

    File 2: `.planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md`
    - Replace `status: draft` → `status: passed` (per D-01)
    - Replace `nyquist_compliant: false` → `nyquist_compliant: true` (per D-02)
    - Replace `wave_0_complete: false` → `wave_0_complete: true` (per D-03)
    - DO NOT modify any line from line 9 onward (body preserved verbatim per D-04)

    Do NOT add new frontmatter fields. Do NOT touch Approval line at file bottom (D-04 — body untouched).
  </action>
  <verify>
    <automated>
      grep -E "^status: passed$" .planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md \
        && grep -E "^nyquist_compliant: true$" .planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md \
        && grep -E "^wave_0_complete: true$" .planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md \
        && grep -E "^status: passed$" .planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md \
        && grep -E "^nyquist_compliant: true$" .planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md \
        && grep -E "^wave_0_complete: true$" .planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md \
        && test $(grep -cE "^status: draft$|^nyquist_compliant: false$|^wave_0_complete: false$" .planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md .planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md) -eq 0
    </automated>
  </verify>
  <acceptance_criteria>
    - File 1 (`22-VALIDATION.md`): `grep -c "^status: passed$"` returns 1; `grep -c "^nyquist_compliant: true$"` returns 1; `grep -c "^wave_0_complete: true$"` returns 1
    - File 2 (`22.1-VALIDATION.md`): same 3 grep counts each return 1
    - Neither file contains any of: `status: draft`, `nyquist_compliant: false`, `wave_0_complete: false`
    - `git diff --stat` shows exactly 3 lines changed per file (3 frontmatter flips, no body edits)
    - Body verbatim preserved: `git diff -U0 -- .planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md` and `git diff -U0 -- .planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md` show only line numbers ≤ 7 changed
    - `npm test` exit code 0 (sanity — doc-only changes must not break any test; expected: 466 passed, 1 todo unchanged from baseline)
  </acceptance_criteria>
  <done>
    Phase 22 and Phase 22.1 VALIDATION.md frontmatter reflects `status: passed`, `nyquist_compliant: true`, `wave_0_complete: true`. No body changes. `npm test` still green. Single atomic commit ready (recommended message: `docs(26-01): flip Phase 22+22.1 VALIDATION.md frontmatter to passed/compliant per D-01/02/03`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies planning artifacts only — no runtime code, no inputs, no external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-ID | Information Disclosure | VALIDATION.md frontmatter | mitigate | This plan itself is the mitigation — stale `status: draft` misrepresents v1.3 Nyquist state in audit output. Flipping to `passed` per D-01 corrects the disclosure |
| T-26-01-RP | Repudiation | Frontmatter flip without audit trail | accept | D-04 prohibits adding `audited:` field — historical drift acknowledged in CONTEXT.md D-03. Commit message + this plan's existence in git is the audit trail |
</threat_model>

<verification>
After both tasks complete:
1. `grep -lE "^status: (draft|pending)$|^nyquist_compliant: false$|^wave_0_complete: false$" .planning/phases/{18,20,22,22.1}*/[12]*-VALIDATION.md` returns no matches (all 4 files clean)
2. `git diff --stat .planning/phases/{18,20,22,22.1}*/[12]*-VALIDATION.md` shows exactly 4 files modified, ≤ 5 lines changed each
3. v1.3-MILESTONE-AUDIT.md re-run (manual or scripted) would no longer list Phase 18/20/22/22.1 under `tech_debt.partial_phases`
4. `npm test` exit 0; no production code touched
</verification>

<success_criteria>
- 4 files modified, frontmatter only
- All 4 files have `status: passed`, `nyquist_compliant: true`, `wave_0_complete: true`
- Bodies (lines 9+) byte-identical to pre-edit state
- No new fields added (D-04 compliance)
- `npm test` baseline (466 passed, 1 todo) unchanged
</success_criteria>

<output>
Create `.planning/phases/26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m/26-01-SUMMARY.md` when done. Summary must enumerate the 4 files modified, the exact frontmatter fields flipped per file, confirmation that bodies were not touched, and `npm test` result.
</output>
