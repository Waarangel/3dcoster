---
phase: 26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CustomerCsvImportModal.tsx
  - src/components/CustomerCsvImportModal.test.tsx
autonomous: true
requirements:
  - POL-05
tags:
  - ui
  - polish
  - parity
  - customer-import

must_haves:
  truths:
    - "CustomerCsvImportModal renders the template-download block ABOVE the upload zone (matching CsvImportModal layout)"
    - "The template-download button label reads exactly `👥 Customer template` (with 👥 emoji prefix)"
    - "All 6 existing CustomerCsvImportModal.test.tsx tests still pass after the reorder"
    - "`npx tsc -b` exits 0 (no TypeScript regressions from the JSX reorder)"
    - "No other CustomerCsvImportModal behavior changes (D-11: layout reorder + button label only)"
  artifacts:
    - path: "src/components/CustomerCsvImportModal.tsx"
      provides: "Customer CSV import modal with template-block above upload zone + 👥 emoji-prefixed label"
      contains: "👥 Customer template"
    - path: "src/components/CustomerCsvImportModal.test.tsx"
      provides: "Existing test suite (282 LOC, 6 it() blocks) still green"
      contains: "describe"
  key_links:
    - from: "src/components/CustomerCsvImportModal.tsx UploadStep"
      to: "src/utils/csvHelpers.ts generateSampleCustomerCsv + downloadCsv"
      via: "onClick handler on `👥 Customer template` button"
      pattern: "downloadCsv\\(generateSampleCustomerCsv\\(\\), 'customer-template\\.csv'\\)"
    - from: "src/components/CustomerCsvImportModal.tsx UploadStep layout"
      to: "src/components/CsvImportModal.tsx UploadStep layout (reference pattern)"
      via: "structural parity: template block ABOVE drop zone, same `bg-slate-700/50 rounded-lg p-3` wrapper"
      pattern: "Template download.*\\n.*bg-slate-700/50"
---

<objective>
Bring `CustomerCsvImportModal.tsx` into layout parity with `CsvImportModal.tsx` per CONTEXT.md D-09, D-10, D-11. Two surgical UI changes:

1. **D-09 — Layout reorder:** Move the `{/* Template download */}` block (currently rendered AFTER the upload drop zone at lines ~298-310) to render BEFORE the drop zone. Mirror the order in `src/components/CsvImportModal.tsx` (template block at top, drop zone below).
2. **D-10 — Emoji prefix:** Change the button label from `Customer template` to `👥 Customer template`. Matches the icon weight of `📦 Materials template` / `🖨️ Printers template` in the asset modal.

Per D-11: NO other CustomerCsvImportModal changes (no dedup-mode parity, no upload-zone restyling, no other emoji additions). Per D-12: `CustomerCsvImportModal.test.tsx` may need a selector update IF it matches the button label literally — verification step confirms.

Purpose: Close the customer-import-modal-parity todo (`.planning/todos/pending/customer-import-modal-parity.md`) which surfaced during Phase 21 UAT on 2026-05-28. After this plan ships, the todo moves to `.planning/todos/completed/`.

Output: 1 component file modified (JSX reorder + label change); 1 test file potentially modified (only if a selector matches `Customer template`); `npm test` 466/466 green; `npx tsc -b` exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m/26-CONTEXT.md
@.planning/todos/pending/customer-import-modal-parity.md
@src/components/CsvImportModal.tsx
@./CLAUDE.md
@./.claude/CLAUDE.md

<!--
Per CONTEXT.md D-09, D-10, D-11, D-12:

D-09 — Current state of CustomerCsvImportModal.tsx UploadStep function:
  Lines 234-270 (approx): outer <div className="space-y-4"> wrapping (in this order):
    1. Error block ({error && ...}) — lines ~260-271 in the read snapshot
    2. Drop zone ({/* Drop zone */} div with className="cursor-pointer border-2 border-dashed...") — lines 272-296
    3. Template download ({/* Template download */} bg-slate-700/50 rounded-lg p-3 wrapper) — lines 298-310
    4. Accepted columns reference text — lines 312-315

  Target state (per CsvImportModal.tsx:235-256 reference pattern):
    1. Template download block (MOVED here, FIRST after the wrapper opening)
    2. Error block
    3. Drop zone
    4. Accepted columns reference text

  Note: The asset modal (`CsvImportModal.tsx`) puts Template FIRST, then Error, then Drop zone. The customer modal currently has Error → Drop zone → Template. The move is: relocate the Template block to be the FIRST child of <div className="space-y-4">.

D-10 — Button label change in the moved Template block:
  Current (line ~308 in the snapshot): `>Customer template</Button>` (no emoji)
  Target: `>👥 Customer template</Button>` (👥 = U+1F465 BUSTS IN SILHOUETTE)
  The 👥 character is a static JSX text literal — no XSS surface, no injection risk (per planning_guidance threat_model note).

D-11 — Out of scope (DO NOT change):
  - Dedup-mode toggle — no change
  - Upload-zone styling (border, padding, drag-over colors) — preserve verbatim
  - Other modal text — no additional emojis
  - The generateSampleCustomerCsv() onClick handler — preserve verbatim
  - The `customer-template.csv` filename literal — preserve verbatim
  - The bg-slate-700/50 rounded-lg p-3 wrapper — preserve verbatim (only its position changes)

D-12 — Test impact:
  The test file `src/components/CustomerCsvImportModal.test.tsx` exists at 282 LOC with 6 it() blocks.
  Grep against the current test file shows ZERO matches for "Customer template" or "template" — so the button label is NOT currently used as a test selector. The label change should not break any test. BUT verify this empirically (Task 2) — if any selector ever matches `Customer template`, update to `👥 Customer template`.
-->

<interfaces>
<!-- Reference: CsvImportModal.tsx UploadStep target layout pattern (lines 234-296 — read-only — DO NOT edit) -->

```tsx
function UploadStep({ ... }: UploadStepProps) {
  return (
    <div className="space-y-4">
      {/* Template download links */}                              ← Template FIRST
      <div className="bg-slate-700/50 rounded-lg p-3">
        <p className="text-sm text-slate-300 mb-2">Download a sample template to get started:</p>
        <div className="flex gap-3 flex-wrap">
          <Button ... >📦 Materials template</Button>
          <Button ... >🖨️ Printers template</Button>
        </div>
      </div>

      {/* Error */}                                                 ← Error SECOND
      {error && ( <div className="bg-red-500/10 ...">{error}...</div> )}

      {/* Drop zone */}                                             ← Drop zone THIRD
      <div onDragOver={...} className="cursor-pointer border-2 border-dashed ...">
        <svg .../>
        <p>Drop .csv file here or click to browse</p>
        <input ref={fileInputRef} type="file" .../>
      </div>
    </div>
  );
}
```

<!-- Target shape for CustomerCsvImportModal.tsx UploadStep (apply the same Template-first reorder) -->

```tsx
function UploadStep({ ... }: UploadStepProps) {
  return (
    <div className="space-y-4">
      {/* Template download */}                                     ← MOVED HERE (was below drop zone)
      <div className="bg-slate-700/50 rounded-lg p-3">
        <p className="text-sm text-slate-300 mb-2">Download a sample template to get started:</p>
        <Button
          type="button"
          variant="ghost"
          btnSize="sm"
          onClick={() => downloadCsv(generateSampleCustomerCsv(), 'customer-template.csv')}
          className="text-blue-400 hover:text-blue-300 underline underline-offset-2"
        >
          👥 Customer template                                       ← D-10 emoji prefix
        </Button>
      </div>

      {/* Error */}                                                 ← unchanged in place
      {error && ( ... )}

      {/* Drop zone */}                                             ← unchanged in place (was 2nd, now 3rd)
      <div ... > ... </div>

      {/* Accepted columns reference */}                            ← unchanged in place
      <p className="text-xs text-slate-400 mt-4"> ... </p>
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reorder UploadStep JSX + add 👥 emoji prefix in CustomerCsvImportModal.tsx</name>
  <files>src/components/CustomerCsvImportModal.tsx</files>
  <read_first>
    - .planning/phases/26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m/26-CONTEXT.md (D-09, D-10, D-11 — locked scope)
    - src/components/CustomerCsvImportModal.tsx (read the FULL UploadStep function — the snapshot in planning context shows lines 270-318, but read 220-318 to capture UploadStepProps interface + full function body)
    - src/components/CsvImportModal.tsx (read lines 230-298 as the reference layout pattern per D-09 — this is the target structural order)
  </read_first>
  <behavior>
    - Before edit: `grep -A 100 "function UploadStep" src/components/CustomerCsvImportModal.tsx | head -100` shows order Error → Drop zone → Template → Accepted-columns
    - After edit: same grep shows order Template → Error → Drop zone → Accepted-columns
    - After edit: button label reads exactly `👥 Customer template` (3 tokens: 👥 emoji + space + `Customer template`)
    - After edit: `downloadCsv(generateSampleCustomerCsv(), 'customer-template.csv')` onClick handler preserved verbatim (no signature change)
    - After edit: bg-slate-700/50 rounded-lg p-3 wrapper preserved verbatim (only position changes)
    - After edit: upload-zone JSX (className="cursor-pointer border-2 border-dashed..." with the SVG + input ref) byte-identical to pre-edit except for its position in the sibling order
    - `npx tsc -b` exits 0
    - `npm test` shows 466 passed, 1 todo, 0 failures (baseline preserved; D-11 forbids behavior changes)
  </behavior>
  <action>
    Use Edit tool on `src/components/CustomerCsvImportModal.tsx`. The change is a JSX-sibling reorder inside `function UploadStep(...)` + a single string literal change.

    Step 1 — Locate the UploadStep `return ( <div className="space-y-4"> ... </div> )` block (function definition starts around line 230). Inside that wrapper there are currently 4 sibling blocks in this order:
      a) `{/* Error */}` + `{error && (...)}`
      b) `{/* Drop zone */}` + `<div ... className="cursor-pointer border-2 border-dashed...">...</div>`
      c) `{/* Template download */}` + `<div className="bg-slate-700/50 rounded-lg p-3">...<Button>Customer template</Button>...</div>`
      d) `{/* Accepted columns reference */}` + `<p className="text-xs text-slate-400 mt-4">...</p>`

    Step 2 — Reorder per D-09: move block (c) to be the FIRST child of the wrapper. New sibling order:
      1) `{/* Template download */}` block (was c, now 1st)
      2) `{/* Error */}` block (was a, now 2nd)
      3) `{/* Drop zone */}` block (was b, now 3rd)
      4) `{/* Accepted columns reference */}` block (d, unchanged position-4)

    Step 3 — Apply D-10 emoji prefix to the button label inside the (now-first) Template block:
      - Find: `>Customer template</Button>`
      - Replace with: `>👥 Customer template</Button>`
      - (Single character/space addition before `Customer`. The 👥 character is U+1F465 BUSTS IN SILHOUETTE.)

    Hard constraints per D-11:
    - DO NOT change the `onClick={() => downloadCsv(generateSampleCustomerCsv(), 'customer-template.csv')}` handler
    - DO NOT change `bg-slate-700/50 rounded-lg p-3` wrapper className
    - DO NOT change the `Download a sample template to get started:` <p> text
    - DO NOT change variant="ghost", btnSize="sm", className="text-blue-400..." on the Button
    - DO NOT touch any sibling block's internal content — only their order in the parent flow
    - DO NOT touch UploadStepProps interface (lines ~220-228)
    - DO NOT touch PreviewStep, PreviewStats, or any code outside the UploadStep return JSX
    - DO NOT add new state, useEffect, useMemo, imports, or props

    If implementing via Edit tool's old_string/new_string pattern, use a multi-line old_string spanning from the opening `<div className="space-y-4">` to the closing `</div>` of the wrapper so the entire sibling-order rewrite is one atomic edit. Alternatively, use 2 sequential edits: one to delete the {/* Template download */} block from its current location, one to insert it as the first child after the wrapper opening.
  </action>
  <verify>
    <automated>
      grep -F "👥 Customer template" src/components/CustomerCsvImportModal.tsx \
        && test $(grep -cF "Customer template" src/components/CustomerCsvImportModal.tsx) -ge 1 \
        && test $(grep -cE "^\s*>Customer template</Button>" src/components/CustomerCsvImportModal.tsx) -eq 0 \
        && awk '/function UploadStep/,/^}/' src/components/CustomerCsvImportModal.tsx | grep -n "Template download\|/\* Error \*/\|Drop zone\|Accepted columns" | head -4 | awk -F: '{print $1}' | paste -sd, - | grep -qE "^[0-9]+,[0-9]+,[0-9]+,[0-9]+$" \
        && python3 -c "
import re
with open('src/components/CustomerCsvImportModal.tsx') as f: src = f.read()
m = re.search(r'function UploadStep.*?^}', src, re.DOTALL | re.MULTILINE)
assert m, 'UploadStep not found'
body = m.group(0)
positions = {}
for marker in ['Template download', 'Error \*/', 'Drop zone', 'Accepted columns']:
    pos = body.find(marker)
    assert pos >= 0, f'marker not found: {marker}'
    positions[marker] = pos
order = sorted(positions, key=positions.get)
expected = ['Template download', 'Error */', 'Drop zone', 'Accepted columns']
assert order == expected, f'order mismatch: {order} != {expected}'
print('JSX sibling order verified:', order)
" \
        && npx tsc -b 2>&1 | tee /tmp/26-04-tsc.log && test $(wc -c < /tmp/26-04-tsc.log) -le 10
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "👥 Customer template" src/components/CustomerCsvImportModal.tsx` returns ≥ 1 (D-10 emoji prefix present)
    - `grep -cE "^\s*>Customer template</Button>" src/components/CustomerCsvImportModal.tsx` returns 0 (old unprefixed label gone)
    - Python sibling-order check (in `<verify>`) confirms order: Template download → Error → Drop zone → Accepted columns (D-09 reorder applied)
    - `npx tsc -b` exits 0 (no output beyond exit code; D-11 — no breaking changes)
    - `grep -c "downloadCsv(generateSampleCustomerCsv(), 'customer-template.csv')" src/components/CustomerCsvImportModal.tsx` returns 1 (onClick handler preserved)
    - `grep -c "bg-slate-700/50 rounded-lg p-3" src/components/CustomerCsvImportModal.tsx` returns 1 (wrapper className preserved, count unchanged)
    - `grep -c 'variant="ghost"' src/components/CustomerCsvImportModal.tsx` count unchanged from pre-edit baseline
    - `grep -c "Download a sample template to get started:" src/components/CustomerCsvImportModal.tsx` returns 1 (header text preserved)
    - Drop-zone styling preserved: `grep -c "cursor-pointer border-2 border-dashed" src/components/CustomerCsvImportModal.tsx` returns 1
    - File line count delta ≤ 2 lines (reorder is structural, not additive; only the 👥 prefix adds a character)
    - No new imports added (grep for `^import` count unchanged from pre-edit)
  </acceptance_criteria>
  <done>
    CustomerCsvImportModal UploadStep renders Template-block first, then Error, then Drop zone, then Accepted-columns reference. Button label reads `👥 Customer template`. `npx tsc -b` exits 0. No behavioral or styling changes beyond D-09 reorder + D-10 emoji. Commit ready (recommended message: `feat(26-04): CustomerCsvImportModal layout parity with CsvImportModal per D-09/D-10/D-11`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify test suite (and patch selector if needed per D-12)</name>
  <files>src/components/CustomerCsvImportModal.test.tsx</files>
  <read_first>
    - .planning/phases/26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m/26-CONTEXT.md (D-12 test-impact note)
    - src/components/CustomerCsvImportModal.test.tsx (read FULL file — 282 LOC — to confirm whether any selector matches the literal `Customer template`)
  </read_first>
  <action>
    Step 1 — Run the test suite scoped to CustomerCsvImportModal first:
      Command: `npm test -- src/components/CustomerCsvImportModal.test.tsx`

    Step 2 — Interpret result:

      Case A: All 6 it() blocks pass on first run → NO test file edit required. The current test file (per the planning-context grep) contains zero matches for "Customer template" or "template", so the label change should not affect it. Verify by running the full suite (Step 3) and skip to Done.

      Case B: One or more tests fail with a selector-not-found error referencing "Customer template" → patch the test file:
        - Locate every literal string `Customer template` in selectors (textContent assertions, getByText-style lookups, query selectors)
        - Replace with `👥 Customer template` (matching the new button label exactly)
        - DO NOT modify any test that doesn't reference the button label
        - DO NOT add new tests (D-11 — out of scope)
        - DO NOT modify the test plan, only the selector text

      Case C: Tests fail for any OTHER reason (TypeScript error, unrelated assertion failure) → STOP. Tests should not have been affected by the JSX reorder + label change. Surface the failure to the orchestrator; do not invent fixes.

    Step 3 — Once `npm test -- src/components/CustomerCsvImportModal.test.tsx` is green, run the full test suite as the final gate:
      Command: `npm test`
      Expected: 466 passed, 1 todo, 0 failures (matches pre-Phase-26 baseline per .planning/v1.3-MILESTONE-AUDIT.md line 76)

    Step 4 — Run `npx tsc -b` one final time to confirm no TypeScript regressions from any selector update.
  </action>
  <verify>
    <automated>
      npm test -- src/components/CustomerCsvImportModal.test.tsx 2>&1 | tee /tmp/26-04-scoped.log \
        && grep -E "Tests\s+6 passed|6 passed" /tmp/26-04-scoped.log \
        && npm test 2>&1 | tee /tmp/26-04-full.log \
        && grep -E "Tests\s+466 passed.*1 todo|466 passed.*1 todo" /tmp/26-04-full.log \
        && grep -cE "(FAIL|failed)" /tmp/26-04-full.log | awk '{exit ($1 > 0)}' \
        && npx tsc -b 2>&1 | tee /tmp/26-04-tsc-final.log && test $(wc -c < /tmp/26-04-tsc-final.log) -le 10
    </automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/components/CustomerCsvImportModal.test.tsx` shows `6 passed` (all 6 existing it() blocks green)
    - `npm test` (full suite) shows `466 passed, 1 todo, 0 failed` (baseline preserved)
    - `npx tsc -b` exits 0 with no output (TypeScript clean)
    - If test file modified: `git diff src/components/CustomerCsvImportModal.test.tsx` shows ONLY string-literal `Customer template` → `👥 Customer template` substitutions (no test logic changes; D-11 scope-lock)
    - If test file NOT modified (expected for Case A per D-12 note): `git status` shows test file unmodified
    - No new test files created (D-11)
    - No new it() blocks added (D-11)
  </acceptance_criteria>
  <done>
    Test suite confirms Task 1 changes did not regress any behavior. CustomerCsvImportModal.test.tsx either remains unmodified (Case A — expected) OR has only selector-literal updates (Case B — minimal patch). Full suite green at baseline 466/1/0. tsc -b clean. The `.planning/todos/pending/customer-import-modal-parity.md` todo is ready for archival to `.planning/todos/completed/` by the next milestone-completion pass. Commit ready if test file was patched (recommended message: `test(26-04): update CustomerCsvImportModal selector to 👥 Customer template per D-12`); otherwise no test commit needed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static JSX literal | The 👥 emoji is a hardcoded text literal in the component source; no user input crosses it, no innerHTML used, no template-string interpolation of untrusted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-XSS | Tampering / XSS | 👥 emoji prefix in JSX button label | accept | Static text literal — React JSX text nodes auto-escape; no untrusted data path. Planning_guidance threat_model note confirms no injection risk |
| T-26-04-RG | Regression | JSX sibling reorder could break test selectors | mitigate | Task 2 runs full test suite as gate; D-12 documents the selector-patch protocol if any test references the literal `Customer template` |
| T-26-04-SC | Tampering | Scope creep beyond D-09/D-10 | mitigate | D-11 lock + acceptance_criteria forbids changes to onClick handler, wrapper className, drop-zone styling, dedup-mode toggle, imports |
</threat_model>

<verification>
After both tasks complete:
1. `grep -F "👥 Customer template" src/components/CustomerCsvImportModal.tsx` returns 1 match (D-10 applied)
2. JSX sibling order in UploadStep: Template → Error → Drop zone → Accepted columns (D-09 applied; verified by Python check in Task 1 `<verify>`)
3. `npm test` → 466 passed, 1 todo, 0 failures (baseline preserved per D-11)
4. `npx tsc -b` → exit 0 (no TS regressions)
5. `git diff --stat` shows ≤ 2 files modified (`CustomerCsvImportModal.tsx` always; `CustomerCsvImportModal.test.tsx` only if Case B applied)
6. No new imports, no new state, no new props, no new tests (D-11 scope-lock honored)
7. Visual parity with `CsvImportModal.tsx` confirmed by structural read (same template-first sibling order, same wrapper styling)
</verification>

<success_criteria>
- 1 component file modified (`CustomerCsvImportModal.tsx`) with JSX reorder + 👥 emoji prefix
- 0 or 1 test files modified (`CustomerCsvImportModal.test.tsx` only if Case B per D-12)
- `npm test` baseline (466 passed, 1 todo, 0 failed) preserved
- `npx tsc -b` exit 0
- No scope creep beyond D-09 + D-10 (D-11 lock honored)
- `.planning/todos/pending/customer-import-modal-parity.md` work content complete (file move to `completed/` happens at milestone close, not in this plan)
</success_criteria>

<output>
Create `.planning/phases/26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m/26-04-SUMMARY.md` when done. Summary must include:
- Confirmation of D-09 reorder (cite the Python sibling-order check output)
- Confirmation of D-10 emoji prefix (cite grep result for `👥 Customer template`)
- Test-suite result: `6 passed` scoped + `466 passed, 1 todo, 0 failed` full suite
- `npx tsc -b` exit 0 confirmation
- Whether Case A (no test edit) or Case B (selector patched) applied per D-12
- Brief visual-parity note referencing `CsvImportModal.tsx:235-256` as the matched reference pattern
</output>
