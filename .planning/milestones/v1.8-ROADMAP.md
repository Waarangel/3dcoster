# Milestone: v1.8 — Inventory & Sales Reporting

**Goal:** Ship two free-tier "floor" features that deepen the business side of 3DCoster —
material inventory tracking and a designed PDF sales report — each architected so the future
Pro "ceiling" (alerts/auto-reorder; scheduled/branded reports) bolts on additively.

**Source of truth:** `docs/V1.8_PLAN.md` (architecture from two code-architect passes) +
`docs/ROADMAP.md` #20, #33.

**Branch:** `feat/v1.8-inventory-reports` (worktree `../3DCoster-v1.8`). Merge → main when verified.
**Release:** tag `v1.8.0` on/after ~2026-06-24 (cadence guard, 5 days after v1.7.0), explicit OK.

> **GSD note:** GSD management lapsed after **v1.3** (Phase 26, shipped 2026-05-28). v1.4–v1.7
> shipped ad-hoc (no GSD milestone). This milestone **revives GSD** and continues the phase
> sequence at **27**. v1.4–v1.7 are recorded as shipped-outside-GSD in the top-level ROADMAP.

---

## Phases

- [x] **Phase 27 — Inventory ledger foundation** ✅ DONE (commit `184c7a0`)
  - `StockEvent` type + `Asset.lowStockThreshold`; Dexie **v11** `stockEvents` store; backupSchema
    lockstep (`BACKUP_STORES`/`BackupData`/`DATE_FIELDS`) + round-trip test; pure helpers
    `buildJobStockEvents` (idempotent, deterministic `jobId__assetId` events) + `deriveStockByAsset`.
  - **Verified:** 626 tests pass, tsc clean. _Req: ledger model, idempotent deduction math, backup-safe._

- [x] **Phase 28 — Inventory deduction wiring** ✅ DONE
  - `src/db/stockEventsWriter.ts`: `upsertJobStockEvents`/`removeJobStockEvents`/`applyJobStockEvents`
    (delete-by-`refId`-then-insert). Wired into `useJobs` add/update/delete **atomically** (one tx with
    the job write) — so the calculator needed no change. `useStockEvents` hook (live `stockByAssetId` +
    `logManualAdjustment`). 6 fake-indexeddb integration tests (create/edit/delete idempotency).
  - **Verified:** 632 tests pass, tsc + eslint clean, build green. _Decision: deduction lives in the data layer (useJobs), not CostCalculator — cleaner + atomic._

- [ ] **Phase 29 — Inventory UI**
  - `StockBadge` / `StockEditor` / `LowStockBar` in AssetLibrary; `lowStockThreshold` field on the
    asset form; first-run "set your current stock" prompt (no historical backfill); NewBadge.
  - _Success: a user can set stock, see it deduct after a job, and get an on-screen low-stock flag._

- [ ] **Phase 30 — PDF sales report**
  - Pure `computeSalesReport()` (`src/utils/salesReportAggregates.ts`) + tests first; lazy
    `generateSalesReportPdf()` (`src/pdf/`, mirrors generateQuotePdf + `__IS_TAURI__` save);
    `ReportsSection` in Settings → Data tab (Month/Year/Custom range + live preview + PDF/CSV).
  - _Success: generate a tax-ready PDF for any range; aggregation unit-tested; PDF stays in the lazy chunk (bundle gate)._

- [ ] **Phase 31 — v1.8 ship prep**
  - `src/features.ts` + NewBadge for both; remove both from `src/roadmap.ts` "In Development";
    CHANGELOG `[1.8.0]`; **mandatory release-diff code review**; bump 4 version files; tag on/after 06-24.

---

## Open product decisions (from docs/V1.8_PLAN.md — resolve in Phase 28–30)

1. Inventory first-run prompt (no backfill) — _recommended_. 2. Deduct packaging — _defaulted ON in Phase 27 helper_.
3. Filament stock in grams (+"≈ spools") — _recommended_. 4. Job-save/stock two-write tolerance — _recommended accept_.
5. PDF aggregate-only (per-sale → CSV) — _recommended_. 6. Reports above Backup in Data tab — _recommended_.
7. One v1.8.0 for both vs. split — _open_.

## Success criteria (milestone)
- Both features shipped free, floor-first (Pro ceiling bolts on additively).
- Backups carry stock data (round-trip tested). Bundle gate green (PDF lazy). Test coverage maintained.
- Release-diff reviewed before the `v1.8.0` tag.
