---
phase: 35-accessibility-tier-4
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/JobsManager.tsx
  - src/components/JobsManager.test.tsx
autonomous: true
requirements: [A11Y-14, A11Y-15]
must_haves:
  truths:
    - "The tag-remove ✕ button has at least a 24×24px hit target and a visible focus ring on keyboard focus"
    - "The ✕ stays subtle at rest and is revealed on hover/focus; keyboard reaches and operates it at all times"
    - "The break-even bar exposes role=progressbar with aria-valuenow/min/max and a human-readable aria-valuetext"
  artifacts:
    - path: "src/components/JobsManager.tsx"
      provides: "24×24 tag-chip hit target + focus ring + break-even progressbar ARIA"
      contains: "role=\"progressbar\""
    - path: "src/components/JobsManager.test.tsx"
      provides: "Vitest assertions for chip hit-target classes + progressbar values"
      contains: "min-w-[24px]"
  key_links:
    - from: "tag chip ✕ button"
      to: "24×24 bounding box"
      via: "min-w-[24px] min-h-[24px] Tailwind classes"
      pattern: "min-w-\\[24px\\]"
---

<objective>
Close A11Y-14 (WCAG 2.5.8 AA) and the break-even-progressbar portion of A11Y-15 (4.1.2 / meter pattern) on `JobsManager.tsx`. Enlarge the tag-remove ✕ button's hit target to a 24×24 bounding box (via padding/min-size, keeping the glyph visually small) and add a focus-visible ring — honoring the LOCKED decision: subtle at rest, revealed on hover/focus, keyboard-operable at all times, touch users always get the full 24px target, no AAA 44×44. Expose the break-even bar as a `role="progressbar"` with value attributes.

Purpose: The current chip ✕ is a 14px (`w-3.5 h-3.5`) target — below the WCAG 2.5.8 minimum — and the break-even bar is a plain styled `<div>` with no semantics for AT.
Output: `JobsManager.tsx` with the chip hit-target + progressbar changes; extended `JobsManager.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-accessibility-tier-4/35-RESEARCH.md
@.planning/phases/35-accessibility-tier-4/35-CONTEXT.md

<interfaces>
<!-- Confirmed by code read: -->
<!--   Tag chip ✕ button: lines 538-544. Current className includes `w-3.5 h-3.5 ... opacity-0 group-hover/chip:opacity-100 focus-visible:opacity-100`. -->
<!--     Already has aria-label={`Remove tag ${tag}`} (line 542) and is keyboard-focusable (no tabIndex=-1) — keep both. -->
<!--     allow-raw-html comment at line 538 explains it intentionally stays a raw <button> (Button primitive's min-h would dwarf the chip). Keep it a raw <button>. -->
<!--   Break-even bar: lines ~697-714. Inner bar <div> uses style={{ width: `${Math.min(100, (job.copiesSold / info.breakEvenCopies) * 100)}%` }}. -->
<!--     info.breakEvenCopies may be null (line 697 handles the null branch separately) — progressbar only applies in the non-null branch. -->
<!--     info.isBreakEven and job.copiesSold are in scope. -->
<!--   Existing JobsManager.test.tsx already present — EXTEND it, do not replace. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enlarge tag-chip ✕ hit target to 24×24 + add focus ring (A11Y-14, LOCKED decision)</name>
  <files>src/components/JobsManager.tsx, src/components/JobsManager.test.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx (lines 530-548 — the tag chip wrapper with `group/chip` and the ✕ `<button>`; note the existing `opacity-0`/`group-hover/chip:opacity-100`/`focus-visible:opacity-100` reveal logic and the `allow-raw-html` comment)
    - .planning/phases/35-accessibility-tier-4/35-RESEARCH.md "A11Y-14 — Tag Chip ✕ Buttons" + "Code Examples › Tag Chip" + Pitfall 7 (bounding box vs spacing)
    - .planning/phases/35-accessibility-tier-4/35-CONTEXT.md "Tag-remove chip buttons (A11Y-14) — LOCKED"
  </read_first>
  <behavior>
    - Test: the chip ✕ button's className contains `min-w-[24px]` and `min-h-[24px]` (24×24 bounding box).
    - Test: the className retains the reveal-on-focus behavior (`focus-visible:opacity-100`) and gains a focus ring (`focus-visible:ring-1`).
    - Test: the button is keyboard-focusable (no `tabIndex="-1"`) and retains `aria-label` starting with "Remove tag ".
  </behavior>
  <action>
    On the chip ✕ `<button>` (lines 538-544), replace the size class `w-3.5 h-3.5` with `min-w-[24px] min-h-[24px]` so the element's bounding box is ≥24×24 while the `text-[10px]` glyph stays small (RESEARCH Pitfall 7 — padding/min-size enlarges the target without growing the glyph). Adjust the negative margin from `-mr-0.5` to `-mr-1` to keep the visual layout tight. Keep `inline-flex items-center justify-center` so the glyph stays centered. Keep `opacity-0 group-hover/chip:opacity-100 focus-visible:opacity-100` (subtle at rest, revealed on hover/focus — LOCKED) and ADD `focus-visible:ring-1 focus-visible:ring-blue-400` for a clear keyboard focus ring. Do NOT add `tabIndex={-1}` (must stay keyboard-reachable) and keep the existing `aria-label={\`Remove tag ${tag}\`}`. Keep it a raw `<button>` with the existing `allow-raw-html` comment (the Button primitive's `min-h-[44px]` would dwarf the chip). Do not adopt the AAA 44×44 target.
  </action>
  <verify>
    <automated>npx vitest run src/components/JobsManager.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The chip ✕ button className includes `min-w-[24px]` and `min-h-[24px]` and no longer includes `w-3.5 h-3.5`.
    - The className includes `focus-visible:opacity-100` and `focus-visible:ring-1`.
    - The button has no `tabIndex="-1"` and its `aria-label` starts with "Remove tag ".
    - `npx vitest run src/components/JobsManager.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Tag-remove ✕ buttons meet the 24×24 hit target with a keyboard focus ring while staying subtle at rest; vitest passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the break-even bar as role="progressbar" with values (A11Y-15)</name>
  <files>src/components/JobsManager.tsx, src/components/JobsManager.test.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx (lines 697-714 — the break-even bar: the null-branch guard at 697 and the inner bar `<div>` with the `width` style; confirm `info.breakEvenCopies`, `info.isBreakEven`, and `job.copiesSold` are in scope)
    - .planning/phases/35-accessibility-tier-4/35-RESEARCH.md §15b + "Code Examples › Progressbar"
  </read_first>
  <behavior>
    - Test: when `breakEvenCopies` is non-null, the inner bar `<div>` has `role="progressbar"` with `aria-valuenow={copiesSold}`, `aria-valuemin="0"`, `aria-valuemax={breakEvenCopies}`, and a non-empty `aria-valuetext`.
    - Test: the `aria-valuetext` mentions both the copies sold and the break-even target (e.g. "3 of 10 copies").
  </behavior>
  <action>
    On the inner break-even bar `<div>` (the one with the inline `width` style, ~line 711) inside the non-null `breakEvenCopies` branch, add `role="progressbar"`, `aria-valuenow={job.copiesSold}`, `aria-valuemin={0}`, `aria-valuemax={info.breakEvenCopies}`, and `aria-valuetext={\`${job.copiesSold} of ${info.breakEvenCopies} copies sold${info.isBreakEven ? ' — break-even reached' : ''}\`}`. Keep the existing `width` style and color classes. Only apply this in the branch where `info.breakEvenCopies` is non-null (the null branch renders different copy and has no measurable progress). Leave the outer track `<div>` as-is.
  </action>
  <verify>
    <automated>npx vitest run src/components/JobsManager.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The break-even bar (non-null branch) has `role="progressbar"` with `aria-valuenow`, `aria-valuemin="0"`, `aria-valuemax` set to the break-even copies, and a non-empty `aria-valuetext`.
    - The `aria-valuetext` includes the sold count and the break-even target.
    - `npx vitest run src/components/JobsManager.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The break-even bar announces its progress to AT via the progressbar role + value attributes; vitest passes.</done>
</task>

</tasks>

<threat_model>
No new attack surface — CSS hit-target sizing + ARIA value attributes on existing client-side UI. No new auth, network, input-parsing, or data-handling. STRIDE register N/A for this plan.
</threat_model>

<verification>
- `npx vitest run src/components/JobsManager.test.tsx` green.
- `npm test` (full suite) green.
- Manual (phase smoke test): on a touch emulator confirm the ✕ is tappable at ≥24px without hover; with VoiceOver confirm the break-even bar announces its value.
</verification>

<success_criteria>
- A11Y-14: tag-remove ✕ has a ≥24×24 hit target + keyboard focus ring; subtle at rest, revealed on hover/focus; keyboard-operable at all times (LOCKED decision honored).
- A11Y-15 (break-even portion): the bar exposes `role="progressbar"` with `aria-valuenow/min/max` + `aria-valuetext`.
</success_criteria>

<output>
Create `.planning/phases/35-accessibility-tier-4/35-04-SUMMARY.md` when done.
</output>
