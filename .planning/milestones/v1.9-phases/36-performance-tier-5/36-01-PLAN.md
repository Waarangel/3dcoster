---
phase: 36-performance-tier-5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/JobsManager.tsx
  - src/components/JobsManager.test.tsx
autonomous: true
requirements: [PERF-09, PERF-10]
must_haves:
  truths:
    - "An 80-job list holds exactly ONE quotes Dexie subscription, not one per visible job row"
    - "OrdersQuoteRows and OrdersSection receive their quotes via a quotesForJob prop instead of calling useQuotes() themselves"
    - "The Reopen action on a declined quote still flips it back to 'sent' via the same updateQuote path (no behavioral change)"
    - "getFilamentName resolves names via a materialsById Map (O(1)) and returns identical strings, including 'Unknown' for missing ids"
  artifacts:
    - path: "src/components/JobsManager.tsx"
      provides: "Single lifted useQuotes() in parent; quotesForJob threaded via getQuotesForJob callback; materialsById Map lookup"
      contains: "materialsById"
    - path: "src/components/JobsManager.test.tsx"
      provides: "Updated mounts passing quotesForJob; source-contract test for single useQuotes() call; PERF-10 output-equivalence test"
      contains: "quotesForJob"
  key_links:
    - from: "JobsManager useQuotes() (single call)"
      to: "OrdersSection / OrdersQuoteRows quotesForJob prop"
      via: "getQuotesForJob(jobId) callback threaded through rowProps -> JobCard"
      pattern: "getQuotesForJob"
    - from: "getFilamentName"
      to: "materialsById Map"
      via: "materialsById.get(filamentId)"
      pattern: "materialsById\\.get"
---

<objective>
Close PERF-09 (Audit 5.1) and PERF-10 (Audit 5.2) on `JobsManager.tsx` — two pure, behavior-preserving render/subscription refactors.

PERF-09: Today both `OrdersQuoteRows` (line 340) and `OrdersSection` (line 396) each call `useQuotes()` per visible job row. The comment at line 163 claiming `dexie-react-hooks` "dedupes the liveQuery emitter" is FALSE (RESEARCH §PERF-09, source-verified against `dexie.mjs`/`dexie-react-hooks.mjs`) — each call is an independent live `db.quotes.toArray()` subscription. With 80 rows that is 160 subscriptions firing on every quote write. Lift to the single existing `useQuotes()` call already in `JobsManager` (line 984) and thread quotes down as a `quotesForJob` slice via a stable `getQuotesForJob` callback. Remove/correct the false comment.

PERF-10: `getFilamentName` (lines 1260-1263) does `materials.find(...)` per filament per row (O(N×M)). Build a `materialsById` Map once with `useMemo([materials])` and look up via `.get()` with the identical `'Unknown'` fallback.

Purpose: Keep the Jobs surface responsive at scale. Hard constraint: ZERO behavioral change — every quote list, Reopen action, and filament name must render exactly as before.
Output: `JobsManager.tsx` with one lifted subscription + Map lookup; extended `JobsManager.test.tsx` proving equivalence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-performance-tier-5/36-RESEARCH.md

<interfaces>
<!-- All confirmed by code read of src/components/JobsManager.tsx: -->
<!--   line 5:    import { useSales, useAllSales, useQuotes } from '../hooks/useDatabase'; -->
<!--   line 163-165: FALSE comment claiming dexie-react-hooks dedupes the liveQuery emitter — REMOVE/CORRECT it. -->
<!--   line 329-371: export function OrdersQuoteRows({ jobId, onStartConversion, onEditQuote, onDeclineQuote }) -->
<!--       line 340: const { quotesByJobId, updateQuote } = useQuotes();  <- REMOVE this hook call -->
<!--       line 341: const quotesForJob = quotesByJobId.get(jobId) ?? [];  <- becomes a prop -->
<!--       line 362-365: Reopen action calls updateQuote({ ...quote, status: 'sent', ... }) — MUST keep working via prop. -->
<!--   line 381-415: function OrdersSection({ jobId, recentSales, onStartConversion, onEditQuote, onDeclineQuote, children }) -->
<!--       line 396: const { quotesByJobId } = useQuotes();  <- REMOVE this hook call -->
<!--       line 397-399: derives quotesForJob + visibleQuotes + hasQuotes -> compute from quotesForJob prop instead. -->
<!--       line 406-411: renders <OrdersQuoteRows jobId=... /> — must forward quotesForJob + updateQuote down. -->
<!--   line 422-: export const JobCard = memo(function JobCard({ job, isSelected, info, recentSales, userCurrency, getFilamentName, ... }) -->
<!--       JobCard renders <OrdersSection jobId=... /> around line 766. -->
<!--   line 799-: type JobRowProps = { ...; getFilamentName: (id: string) => string; ... }  <- ADD getQuotesForJob + updateQuote here -->
<!--   line 984: const { updateQuote: updateQuoteFromHook } = useQuotes();  <- EXPAND to also destructure quotesByJobId. ONLY useQuotes() call after refactor. -->
<!--   line 1260-1263: const getFilamentName = useCallback((filamentId) => { const filament = materials.find(m => m.id === filamentId); return filament ? `${filament.brand || ''} ${filament.filamentType || filament.name}`.trim() : 'Unknown'; }, [materials]); -->
<!--   line 1293-1321: const rowProps = useMemo<JobRowProps>(() => ({ ... getFilamentName, ... }), [searchedJobs, ..., getFilamentName, ...]);  <- ADD getQuotesForJob (and updateQuote) to object + dep array. -->
<!-- `materials` is a PROP into JobsManager (not a live query here) — confirmed by getFilamentName's [materials] dep. -->

<!-- Test harness (src/components/JobsManager.test.tsx) — confirmed by code read: -->
<!--   line 20-37: vi.mock('../hooks/useDatabase', ...) returns useQuotes()={ quotes, quotesByJobId, updateQuote: updateQuoteSpy, ... }. -->
<!--   Many mounts render <OrdersQuoteRows jobId="job-1" ... /> WITHOUT quotesForJob — after the lift these MUST pass quotesForJob (and updateQuote where Reopen is exercised). -->
<!--   updateQuoteSpy (line 17) is the spy the Reopen test asserts against — thread it as the updateQuote prop. -->
<!--   Test convention: raw createRoot + act (react-dom/client), NOT @testing-library. readFileSync for source-contract. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift useQuotes() to the JobsManager parent; thread quotesForJob + updateQuote into OrdersSection/OrdersQuoteRows (PERF-09)</name>
  <files>src/components/JobsManager.tsx, src/components/JobsManager.test.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx (lines 160-166 false comment; 329-415 OrdersQuoteRows + OrdersSection; ~760-790 JobCard's OrdersSection render; 799-840 JobRowProps type + JobRow destructure; 984 existing useQuotes() call; 1293-1321 rowProps memo + dep array)
    - .planning/phases/36-performance-tier-5/36-RESEARCH.md §PERF-09 (the prop-threading plan, the getQuotesForJob useCallback, Pitfalls 1/2/4)
    - src/components/JobsManager.test.tsx (lines 17-37 the useQuotes mock + updateQuoteSpy; every `<OrdersQuoteRows jobId="job-1" ... />` mount, incl. the Reopen test ~362/declined block ~232-252)
  </read_first>
  <behavior>
    - Test: OrdersQuoteRows renders its Pending/Declined quote list from the `quotesForJob` prop (same rows it previously produced from the internal useQuotes()).
    - Test: OrdersSection hides itself when `quotesForJob` (visible) is empty AND no sales, and renders the "Orders" heading + child rows otherwise — driven by the prop, not a hook.
    - Test: the Reopen action on a declined quote calls the `updateQuote` prop with `{ ...quote, status: 'sent', decisionAt: undefined, declineReason: undefined }` (updateQuoteSpy still observed).
    - Test (source-contract): `useQuotes()` appears EXACTLY ONCE in JobsManager.tsx (the parent call), proving no residual per-row subscription.
  </behavior>
  <action>
    Remove the false comment at lines 163-165 (replace with a one-line note that quotes are lifted to the parent and passed down — no dedup exists in dexie-react-hooks). 

    On `OrdersQuoteRows` (329-371): delete the `const { quotesByJobId, updateQuote } = useQuotes();` call and the `const quotesForJob = quotesByJobId.get(jobId) ?? [];` line. Add `quotesForJob: Quote[]` and `updateQuote: (quote: Quote) => Promise<void>` to its props type, and use the `quotesForJob` prop directly for `visible`. Keep the Reopen handler identical but call the `updateQuote` PROP (line 362-365 body unchanged).

    On `OrdersSection` (381-415): delete the `const { quotesByJobId } = useQuotes();` call and the `quotesForJob` derivation. Add `quotesForJob: Quote[]` and `updateQuote: (quote: Quote) => Promise<void>` to its props type; compute `visibleQuotes`/`hasQuotes` from the `quotesForJob` prop. Forward `quotesForJob={quotesForJob}` and `updateQuote={updateQuote}` to the nested `<OrdersQuoteRows>` (406-411).

    In `JobCard` (~766 where `<OrdersSection ...>` renders): pass `quotesForJob={getQuotesForJob(job.id)}` and `updateQuote={updateQuote}`. Add `getQuotesForJob: (jobId: string) => Quote[]` and `updateQuote: (quote: Quote) => Promise<void>` to `JobCard`'s destructured props and `JobRowProps` type (799). Thread them through `JobRow` the same way `getFilamentName` is already threaded.

    Expand the existing call at line 984 to `const { quotesByJobId, updateQuote: updateQuoteFromHook } = useQuotes();`. Add a stable accessor:
    `const getQuotesForJob = useCallback((jobId: string) => quotesByJobId.get(jobId) ?? [], [quotesByJobId]);` (useCallback REQUIRED — RESEARCH Pitfall 4: without it rowProps recreates every render and bypasses JobCard's memo). Add `getQuotesForJob` and `updateQuote: updateQuoteFromHook` to the `rowProps` object (1293) AND to the rowProps `useMemo` dep array (1321) (RESEARCH Pitfall 2: omitting `getQuotesForJob` from deps gives stale rows).

    Update `JobsManager.test.tsx`: every `<OrdersQuoteRows jobId="job-1" ... />` mount must now pass `quotesForJob={...}` (derive from `quotesFixture` the same way the mock previously built `quotesByJobId.get('job-1')`) and `updateQuote={updateQuoteSpy}` (so the existing Reopen assertion still observes the spy). The `useQuotes` mock can stay (the parent JobsManager isn't mounted in these subcomponent tests) — but the subcomponents no longer read it. Add the source-contract test asserting `(src.match(/useQuotes\(\)/g) ?? []).length === 1`.
  </action>
  <verify>
    <automated>npx vitest run src/components/JobsManager.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `OrdersQuoteRows` and `OrdersSection` no longer call `useQuotes()`; they accept `quotesForJob` + `updateQuote` props.
    - `useQuotes()` is called exactly once in JobsManager.tsx (asserted by the source-contract test).
    - `getQuotesForJob` is a `useCallback([quotesByJobId])` and is present in both the `rowProps` object and its `useMemo` dep array.
    - The Reopen test still observes `updateQuoteSpy` called with the reopened quote payload.
    - The false dedup comment (old lines 163-165) is removed/corrected.
    - `npx vitest run src/components/JobsManager.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The quotes subscription is lifted to one parent call; per-row components consume props; Reopen and quote-list rendering are byte-for-byte equivalent; vitest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace getFilamentName Array.find with a materialsById Map (PERF-10)</name>
  <files>src/components/JobsManager.tsx, src/components/JobsManager.test.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx (lines 1255-1265 — the `getFilamentName` useCallback with `materials.find`; confirm `materials` is the prop in scope and the exact fallback string formatting)
    - .planning/phases/36-performance-tier-5/36-RESEARCH.md §PERF-10 (the useMemo Map + identical-fallback requirement)
  </read_first>
  <behavior>
    - Test: a standalone Map-based getFilamentName returns `"Bambu PLA"` for `{ id:'a', brand:'Bambu', filamentType:'PLA', name:'PLA' }` (brand + filamentType, trimmed).
    - Test: it returns `"Unknown"` for a missing id (identical fallback).
    - Test: it falls back to `name` when `filamentType` is absent (e.g. `{ id:'b', brand:'', name:'Generic' }` → `"Generic"`).
  </behavior>
  <action>
    Above `getFilamentName` (~1260), add `const materialsById = useMemo(() => new Map(materials.map(m => [m.id, m])), [materials]);`. Rewrite `getFilamentName` to `useCallback((filamentId: string) => { const filament = materialsById.get(filamentId); return filament ? \`${filament.brand || ''} ${filament.filamentType || filament.name}\`.trim() : 'Unknown'; }, [materialsById]);`. The string-building expression and the `'Unknown'` fallback MUST be character-identical to the current implementation — only the lookup mechanism changes (`materials.find` → `materialsById.get`). Do NOT change `rowProps` (getFilamentName stays in its object + dep array as today; its identity is now stable when `materials` is unchanged, a strict improvement). Add the output-equivalence unit test to `JobsManager.test.tsx` (inline the logic, no component mount needed).
  </action>
  <verify>
    <automated>npx vitest run src/components/JobsManager.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `materialsById` is a `useMemo(() => new Map(...), [materials])`.
    - `getFilamentName` uses `materialsById.get(filamentId)` and `useCallback([materialsById])`; no `materials.find` remains.
    - The fallback string and `'Unknown'` are unchanged from the original.
    - The output-equivalence test covers the brand+type, name-fallback, and missing-id (Unknown) cases.
    - `npx vitest run src/components/JobsManager.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Filament names resolve via an O(1) Map with identical output and fallback; vitest green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + typecheck regression gate (behavioral equivalence)</name>
  <files>src/components/JobsManager.tsx, src/components/JobsManager.test.tsx</files>
  <read_first>
    - .planning/phases/36-performance-tier-5/36-RESEARCH.md §Verification Strategy (sampling rate; phase gate = full suite green + tsc -b clean)
  </read_first>
  <action>
    Run the full vitest suite and a strict TypeScript build to prove no behavioral or type regression from the lift + Map refactor. Both must be clean before this plan is considered done. If any pre-existing test now fails, it is because a mount still lacks `quotesForJob`/`updateQuote` — fix the mount, do NOT weaken the assertion.
  </action>
  <verify>
    <automated>npx vitest run && tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (entire suite, not just JobsManager.test.tsx).
    - `tsc -b` exits 0 with no new errors.
  </acceptance_criteria>
  <done>Whole test suite green and the project type-checks clean — refactor proven behavior- and type-preserving.</done>
</task>

</tasks>

<threat_model>
No new attack surface. PERF-09/10 are internal React render/subscription refactors on existing client-side components — no new auth, network, input-parsing, persistence, or data-flow boundaries. The lifted `useQuotes()` reads the same Dexie store with the same query; the Reopen write goes through the same `updateQuote` (`db.quotes.put`) path. STRIDE register N/A for this plan (security_enforcement: no externally-reachable surface introduced).
</threat_model>

<verification>
- `npx vitest run src/components/JobsManager.test.tsx` green (PERF-09 + PERF-10 contracts).
- `npx vitest run` (full suite) green.
- `tsc -b` clean.
- Source-contract: exactly one `useQuotes()` call in JobsManager.tsx; no `materials.find` in `getFilamentName`.
- Manual (phase smoke test): expand a job card with Pending + Declined quotes — list, Reopen, and filament names render exactly as before.
</verification>

<success_criteria>
- PERF-09: `useQuotes()` lifted to the single JobsManager parent call; `quotesForJob` + `updateQuote` passed down as props; `OrdersSection` and `OrdersQuoteRows` hold no subscription; an 80-row list = one quotes subscription. Reopen unchanged.
- PERF-10: `getFilamentName` resolves via a `materialsById` Map (O(1)); identical output + `'Unknown'` fallback.
- Zero behavioral change, proven by the existing suite staying green plus the new equivalence/source-contract tests.
</success_criteria>

<output>
Create `.planning/phases/36-performance-tier-5/36-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
