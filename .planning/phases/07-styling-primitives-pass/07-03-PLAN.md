---
phase: 07-styling-primitives-pass
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - scripts/lint-no-raw-html.mjs
  - .git/hooks/pre-commit
  - src/components/MaintenanceAlertModal.tsx
  - src/components/UpdateBanner.tsx
  - src/components/FilamentSelector.tsx
  - src/components/Header.tsx
  - src/components/GcodeImport.tsx
  - src/components/ImageCarousel.tsx
  - src/components/CsvImportModal.tsx
  - src/components/UserProfileModal.tsx
  - src/components/BambuImport.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "`scripts/lint-no-raw-html.mjs` exists and exits 0 on a clean tree"
    - "`scripts/lint-no-raw-html.mjs` exits 1 on a freshly-introduced raw `<button>` violation"
    - "`scripts/lint-no-raw-html.mjs` exits 0 when `// allow-raw-html` is placed on the line above a raw element"
    - "`.git/hooks/pre-commit` is executable and runs the lint script"
    - "`npm run build` chains `node scripts/lint-no-raw-html.mjs` BEFORE `tsc -b && vite build`"
    - "All 9 utility/modal files in this plan contain zero raw `<button>|<input>|<select>|<textarea>` outside `// allow-raw-html` opt-outs"
    - "Behavior preserved across all refactored files: onClick, onChange, disabled, type, required, name handlers and attributes intact"
  artifacts:
    - path: "scripts/lint-no-raw-html.mjs"
      provides: "Grep-based lint guard script (Node.js ESM)"
      contains: "PATTERN = /<(button|input|select|textarea)"
    - path: ".git/hooks/pre-commit"
      provides: "Pre-commit hook that runs the lint script"
      contains: "node scripts/lint-no-raw-html.mjs"
    - path: "package.json"
      provides: "lint:no-raw-html script + build chain + prepare hook"
      contains: "lint:no-raw-html"
  key_links:
    - from: "package.json build script"
      to: "scripts/lint-no-raw-html.mjs"
      via: "npm run build invokes node scripts/lint-no-raw-html.mjs first"
      pattern: "node scripts/lint-no-raw-html.mjs.*&&.*tsc -b"
    - from: ".git/hooks/pre-commit"
      to: "scripts/lint-no-raw-html.mjs"
      via: "shell script calls node scripts/lint-no-raw-html.mjs"
      pattern: "node scripts/lint-no-raw-html.mjs"
---

<objective>
Install the grep-based no-raw-HTML lint guard (UI-03), then refactor the 9 lightweight utility/modal components (47 raw elements total) onto shared `src/components/ui/` primitives. These files act as the battle-test of the lint guard before Plans 07-01 and 07-02 tackle the heavy-traffic components.

Purpose: Establishes the foundational lint guard FIRST so every subsequent task across the phase can use `npm run build` as its automated verification gate. Doing the small files in this plan exercises the full variety of patterns (file inputs, radios, checkboxes, refs, conditional classes, dot indicators) so that any pattern issue surfaces here rather than during the heavy refactors.

Output:
- `scripts/lint-no-raw-html.mjs` — grep-based lint script
- `package.json` — `lint:no-raw-html` script + chained into `build` + `prepare` hook
- `.git/hooks/pre-commit` — executable shell hook
- 9 refactored component files: MaintenanceAlertModal, UpdateBanner, FilamentSelector, Header, GcodeImport, ImageCarousel, CsvImportModal, UserProfileModal, BambuImport
- 5 adversarial lint tests proving the guard catches violations and honors opt-outs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-styling-primitives-pass/07-CONTEXT.md
@.planning/phases/07-styling-primitives-pass/07-RESEARCH.md
@.planning/phases/07-styling-primitives-pass/07-PATTERNS.md
@.planning/phases/07-styling-primitives-pass/07-VALIDATION.md

<interfaces>
<!-- Primitive contracts extracted from src/components/ui/ - executors should use these directly -->

From src/components/ui/index.ts:
```ts
export { Button, ButtonLink, getButtonClasses } from './Button';
export { Input } from './Input';
export { Select } from './Select';
export { Textarea } from './Textarea';
export { Card } from './Card';
```

From src/components/ui/Button.tsx (variantStyles, line ~15):
- variant: 'primary' | 'secondary' | 'success' | 'danger' | 'ghost'
- btnSize: 'sm' | 'md' | 'lg' (default 'md'; NEVER use `size`)
- fullWidth?: boolean
- forwardRef<HTMLButtonElement>
- DOES NOT default `type="button"` — pass `type=` explicitly inside any `<form>`
- disabledButtonStyles already baked in via getButtonClasses

From src/components/ui/Input.tsx:
- inputSize: 'sm' | 'md' | 'lg' (NEVER use `size` — Omit'd)
- error?: boolean
- baseStyles: 'w-full bg-slate-700 text-white rounded-lg border-0 placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500'
- forwardRef<HTMLInputElement>

From src/components/ui/Select.tsx:
- selectSize: 'sm' | 'md' | 'lg' (NEVER use `size` — Omit'd)
- forwardRef<HTMLSelectElement>
- baseStyles: 'w-full bg-slate-700 text-white rounded-lg border-0 focus:outline-none focus:ring-2 focus:ring-blue-500'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/lint-no-raw-html.mjs (Wave 0)</name>
  <files>scripts/lint-no-raw-html.mjs</files>
  <read_first>
    - .planning/phases/07-styling-primitives-pass/07-RESEARCH.md § Lint Guard Mechanism (script body verbatim)
    - .planning/phases/07-styling-primitives-pass/07-PATTERNS.md § Pattern Assignments — Tooling Files (analog reference: scripts/generate-icons.mjs)
    - .planning/phases/07-styling-primitives-pass/07-RESEARCH.md § Landmines (especially Pitfall 5: false-positive on src/components/ui/)
    - scripts/generate-icons.mjs (existing analog — ESM .mjs pattern)
  </read_first>
  <action>
    Create `scripts/lint-no-raw-html.mjs` as an ESM Node.js script following the exact body in 07-RESEARCH.md § Lint Guard Mechanism. Per D-03 and D-04:

    The script MUST:
    - Import only Node built-ins: `readdirSync`, `readFileSync` from `fs`; `join`, `relative` from `path`.
    - Scan `src/components` recursively for `.tsx` and `.ts` files.
    - Exclude any file whose path starts with `src/components/ui` (Pitfall 5).
    - Match the regex `/<(button|input|select|textarea)[\s>\/]/` per line.
    - For each match, check `lines[i-1]` for the substring `allow-raw-html`. If present, skip (D-04 opt-out marker).
    - On any unmatched violation, print one line per violation in `path:line: snippet` form and `process.exit(1)`.
    - On clean scan, print `lint:no-raw-html passed` and exit 0.

    Use the synchronous entry-point pattern (no async, no `main()` wrapper) since `readFileSync` is synchronous. Match the ESM import style of `scripts/generate-icons.mjs`. Do NOT include shebang (run via `node scripts/lint-no-raw-html.mjs`).

    Do NOT add additional features (no glob patterns, no config files, no severity levels). The script is intentionally minimal per D-03.
  </action>
  <verify>
    <automated>node scripts/lint-no-raw-html.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/lint-no-raw-html.mjs` exists.
    - Running `node scripts/lint-no-raw-html.mjs` BEFORE any refactor will exit 1 (existing raw elements in the 14 in-scope components).
    - The script does NOT report any violations from files in `src/components/ui/`.
    - The exclusion path-prefix check uses `file.startsWith('src/components/ui')` exactly.
    - `grep -c 'allow-raw-html' scripts/lint-no-raw-html.mjs` returns at least 1 (opt-out marker referenced).
    - `grep -c 'process.exit(1)' scripts/lint-no-raw-html.mjs` returns 1.
  </acceptance_criteria>
  <done>Lint script exists, exits 1 on the current dirty tree (proving it works), excludes `src/components/ui/`, honors `allow-raw-html` marker on the preceding line.</done>
</task>

<task type="auto">
  <name>Task 2: Wire lint script into package.json and install pre-commit hook (Wave 0)</name>
  <files>package.json, .git/hooks/pre-commit</files>
  <read_first>
    - package.json (current scripts section — verify `build` is `tsc -b && vite build`)
    - .planning/phases/07-styling-primitives-pass/07-RESEARCH.md § Lint Guard Mechanism (Steps 1–4 — the exact package.json updates and pre-commit hook contents)
    - .planning/phases/07-styling-primitives-pass/07-CONTEXT.md § D-05 (lint check runs in both pre-commit AND npm run build)
  </read_first>
  <action>
    Update `package.json` `scripts` per D-05:
    - Add `"lint:no-raw-html": "node scripts/lint-no-raw-html.mjs"`.
    - Change `"build"` from `"tsc -b && vite build"` to `"node scripts/lint-no-raw-html.mjs && tsc -b && vite build"` (lint runs first so fast failures don't waste compile time).
    - Add `"prepare": "chmod +x .git/hooks/pre-commit 2>/dev/null || true"` (re-enables hook on fresh clones after `npm install`).

    Create `.git/hooks/pre-commit` containing exactly:

    ```
    #!/usr/bin/env sh
    node scripts/lint-no-raw-html.mjs
    ```

    Make it executable: `chmod +x .git/hooks/pre-commit`.

    Note: `.git/hooks/` is not git-tracked. The `prepare` script bridges this for fresh clones. Do NOT add husky or lefthook (per D-03, mechanism is intentionally plain shell + node).
  </action>
  <verify>
    <automated>grep -c 'lint:no-raw-html' package.json && test -x .git/hooks/pre-commit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` scripts object contains `lint:no-raw-html`, `prepare`, and an updated `build` whose first command is `node scripts/lint-no-raw-html.mjs`.
    - `.git/hooks/pre-commit` exists, is executable (`test -x .git/hooks/pre-commit` exits 0), and runs the lint script.
    - `grep -c 'node scripts/lint-no-raw-html.mjs' package.json` returns at least 2 (in `build` and `lint:no-raw-html`).
    - `npm run lint:no-raw-html` invokes the script (will exit 1 currently — that's expected; refactors fix it).
  </acceptance_criteria>
  <done>package.json wired, build chained with lint guard first, pre-commit hook installed and executable.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor MaintenanceAlertModal + UpdateBanner + FilamentSelector (8 raw elements)</name>
  <files>src/components/MaintenanceAlertModal.tsx, src/components/UpdateBanner.tsx, src/components/FilamentSelector.tsx</files>
  <read_first>
    - src/components/MaintenanceAlertModal.tsx (current state — 1 raw button)
    - src/components/UpdateBanner.tsx (current state — 2 raw buttons; note `__IS_TAURI__` reference)
    - src/components/FilamentSelector.tsx (current state — 1 input, 1 button)
    - .planning/phases/07-styling-primitives-pass/07-PATTERNS.md § Pattern Assignments — Plan 07-03 (MaintenanceAlertModal, UpdateBanner, FilamentSelector sections — exact before/after pairs)
    - .planning/phases/07-styling-primitives-pass/07-RESEARCH.md § Landmines (especially L-7: UpdateBanner Tauri conditional — touch only JSX, not handlers)
    - src/components/ui/Button.tsx (variant + btnSize contract)
    - src/components/ui/Input.tsx (inputSize contract)
  </read_first>
  <action>
    Per PATTERNS.md § MaintenanceAlertModal: Replace the single `<button>` (lines ~70-75, "Got it" with `bg-blue-600`) with `<Button onClick={onDismiss}>Got it</Button>` — drop the full className (primary is default variant, md is default btnSize).

    Per PATTERNS.md § UpdateBanner: Replace the Download `<button>` (white bg on blue banner) with `<Button variant="ghost" btnSize="sm" onClick={handleDownload} className="bg-white text-blue-600 hover:bg-blue-50 font-medium">Download</Button>`. Replace the Dismiss icon `<button>` with `<Button variant="ghost" btnSize="sm" onClick={handleDismiss} className="text-white/80 hover:text-white" aria-label="Dismiss">`. Per L-7, do NOT touch the `handleDownload` callback or the `__IS_TAURI__` conditional — only the JSX `<button>` tags change.

    Per PATTERNS.md § FilamentSelector: Replace the `<input type="number" step="0.001">` with `<Input type="number" step="0.001" value={editedPrice || ''} onChange={...}/>` — drop style tokens. For the single remaining `<button>`, classify by its className and apply the ghost/primary recipe per PATTERNS.md § Shared Patterns. Preserve onClick, onChange, value, step, ref handlers verbatim per D-08.

    Add the `import { Button, Input } from './ui';` line to each file's import block (Button only for MaintenanceAlertModal and UpdateBanner; both Button and Input for FilamentSelector).
  </action>
  <verify>
    <automated>grep -E '^[^/]*<(button|input|select|textarea)[ />]' src/components/MaintenanceAlertModal.tsx src/components/UpdateBanner.tsx src/components/FilamentSelector.tsx | grep -v 'allow-raw-html' | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns `0` (no unopted raw elements in these 3 files).
    - `npm run build` exits 0 (TypeScript clean + Vite build clean + lint guard green for these 3 files).
    - `git diff` for `UpdateBanner.tsx` shows changes only to `<button>` JSX, not to `handleDownload`, `useEffect`, or `__IS_TAURI__` logic.
    - `grep -c 'onDismiss\|handleDownload\|handleDismiss' src/components/UpdateBanner.tsx src/components/MaintenanceAlertModal.tsx` count matches pre-refactor (handlers preserved).
  </acceptance_criteria>
  <done>All 8 raw elements across these 3 files replaced with primitives; behavior preserved; build green; Tauri conditional in UpdateBanner untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Refactor Header + GcodeImport + ImageCarousel (7 raw elements + 1 opt-out + 1 opt-out + N opt-outs)</name>
  <files>src/components/Header.tsx, src/components/GcodeImport.tsx, src/components/ImageCarousel.tsx</files>
  <read_first>
    - src/components/Header.tsx (current state — 1 raw button with `ref={buttonRef}`)
    - src/components/GcodeImport.tsx (current state — 1 file input + 2 buttons; file input MUST keep `// allow-raw-html`)
    - src/components/ImageCarousel.tsx (current state — 2 arrow buttons + N dot indicator buttons)
    - .planning/phases/07-styling-primitives-pass/07-PATTERNS.md § Pattern Assignments — Plan 07-03 (Header, GcodeImport, ImageCarousel sections)
    - .planning/phases/07-styling-primitives-pass/07-RESEARCH.md § Landmines (L-5: hidden file inputs; Pitfall 7: Header hamburger ref)
    - src/components/ui/Button.tsx
  </read_first>
  <action>
    Per PATTERNS.md § Header: Replace the single `<button ref={buttonRef}>` (hamburger) with `<Button ref={buttonRef} variant="ghost" btnSize="sm" onClick={() => setMobileMenuOpen((prev) => !prev)} className="md:hidden w-11 h-11 rounded-lg" aria-label={mobileMenuOpen ? 'Close menu' : 'Open menu'} aria-expanded={mobileMenuOpen}>`. Per Pitfall 7, `Button` uses `forwardRef<HTMLButtonElement>` so `buttonRef.current` remains a valid `HTMLButtonElement` — no special handling needed. Keep the SVG icon children unchanged. Drop style tokens (`bg-slate-700/50 hover:bg-slate-700/50 text-slate-300 hover:text-white transition-colors flex items-center justify-center`) — ghost covers them.

    Per PATTERNS.md § GcodeImport: Place `{/* allow-raw-html */}` immediately above the hidden `<input type="file" ref={fileInputRef} accept=".gcode,.gc,.3mf" onChange={handleFileSelect} className="hidden">` per L-5 — DO NOT replace this with `<Input>` (the primitive's `bg-slate-700` base styles would bleed through `hidden`). For the 2 remaining `<button>` elements, classify by className and apply the ghost/primary recipe per PATTERNS.md § Shared Patterns.

    Per PATTERNS.md § ImageCarousel: Replace the prev/next arrow `<button>` elements with `<Button variant="ghost" btnSize="sm" onClick={goPrev|goNext} className="absolute left-3|right-3 top-1/2 -translate-y-1/2 w-10 h-10 rounded-full backdrop-blur-sm border border-slate-700/50 opacity-0 group-hover:opacity-100 bg-slate-900/70 hover:bg-slate-900/90" aria-label="Previous|Next screenshot">`. Preserve positional/visual overrides via className (`absolute`, `rounded-full`, `w-10 h-10`, `backdrop-blur-sm`, `opacity-0 group-hover:opacity-100`) since no Button variant covers them. For the dot indicator `<button>` elements with conditional `w-6`/`w-2.5` and no children: leave as raw `<button>` with `{/* allow-raw-html */}` on the line above each (per PATTERNS.md § No Analog Found — conditional-class + dynamic-width pattern).

    Add appropriate `import { Button } from './ui';` to each file. GcodeImport keeps its raw `<input>` so no Input import needed for that file (verify).
  </action>
  <verify>
    <automated>grep -nE '<(button|input|select|textarea)[ />]' src/components/Header.tsx src/components/GcodeImport.tsx src/components/ImageCarousel.tsx | awk -F: 'NR==FNR{next}1' | wc -l > /dev/null; node scripts/lint-no-raw-html.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/lint-no-raw-html.mjs` reports NO violations for `Header.tsx`, `GcodeImport.tsx`, or `ImageCarousel.tsx` (all violations are either replaced or have `allow-raw-html` on the preceding line).
    - `grep -c 'allow-raw-html' src/components/GcodeImport.tsx` returns at least 1 (file input opt-out).
    - `grep -c 'allow-raw-html' src/components/ImageCarousel.tsx` returns at least 1 per dot indicator (count from current source).
    - `grep -c 'allow-raw-html' src/components/Header.tsx` returns 0 (hamburger fully replaced).
    - `npm run build` exits 0.
    - `git diff` for `Header.tsx` shows `buttonRef` still attached (now to `<Button>`); the click-outside handler in `useEffect` is unchanged.
  </acceptance_criteria>
  <done>All raw `<button>` elements replaced; file inputs and dot indicators correctly opted out; refs forward correctly; build green.</done>
</task>

<task type="auto">
  <name>Task 5: Refactor CsvImportModal + UserProfileModal + BambuImport (31 raw elements + 6 opt-outs)</name>
  <files>src/components/CsvImportModal.tsx, src/components/UserProfileModal.tsx, src/components/BambuImport.tsx</files>
  <read_first>
    - src/components/CsvImportModal.tsx (current state — 14 raw elements; 4 opt-outs needed: 1 file, 2 radio, 1 checkbox)
    - src/components/UserProfileModal.tsx (current state — 9 raw elements: 1 button, 7 input, 1 select)
    - src/components/BambuImport.tsx (current state — 8 raw elements; 1 checkbox opt-out)
    - .planning/phases/07-styling-primitives-pass/07-PATTERNS.md § Pattern Assignments — Plan 07-03 (CsvImportModal, UserProfileModal, BambuImport sections — exact before/after pairs)
    - .planning/phases/07-styling-primitives-pass/07-RESEARCH.md § Landmines (L-4 radio `name=`, L-5 file inputs)
    - src/components/ui/Button.tsx, src/components/ui/Input.tsx, src/components/ui/Select.tsx
  </read_first>
  <action>
    CsvImportModal (per PATTERNS.md):
    - Replace modal close/back ghost button with `<Button variant="ghost" btnSize="sm" onClick={...} title="Back to upload">`.
    - Hidden file input (`<input ref={fileInputRef} type="file" accept=".csv" onChange={onFileSelect} className="hidden">`): keep raw with `{/* allow-raw-html */}` on the line above (L-5).
    - Both radio inputs (`<input type="radio" name="duplicateMode">`): keep raw with `{/* allow-raw-html */}` above each (L-4 — `accent-blue-500` and native radio grouping required). DO NOT change `name=` attribute.
    - Row checkbox `<input type="checkbox">`: keep raw with `{/* allow-raw-html */}` above.
    - Text-link buttons (Select all / Deselect all): replace with `<Button variant="ghost" btnSize="sm" onClick={...} className="text-blue-400 hover:text-blue-300 text-xs">` and `<Button variant="ghost" btnSize="sm" onClick={...} className="text-xs">` respectively — preserve color override via className.
    - Remaining `<button>` elements: classify by className, apply primary/secondary/ghost recipe per PATTERNS.md § Shared Patterns.

    UserProfileModal (per PATTERNS.md):
    - Modal close `<button>` (text-slate-400 hover:text-white p-1): replace with `<Button variant="ghost" btnSize="sm" onClick={onClose}>` keeping the SVG child.
    - All 7 `<input>` elements: replace with `<Input>` preserving `type`, `step`, `min`, `max`, `value`, `onChange`, `placeholder`, `required`. Drop style tokens.
    - The single `<select>`: replace with `<Select>` preserving `value`, `onChange`, options children.

    BambuImport (per PATTERNS.md):
    - Trigger button (`bg-emerald-600/80`): replace with `<Button variant="success" btnSize="sm" onClick={handleOpen} title="Import Bambu Lab filament catalog with prices">` keeping the SVG icon child and the two responsive `<span>` labels.
    - Checkbox `<input type="checkbox">` (selection): keep raw with `{/* allow-raw-html */}` above per L-4 (accent-blue-500).
    - Remaining 6 `<button>` elements + 0 inputs/selects: classify by className and apply the standard recipe (primary/secondary/ghost). Preserve onClick, disabled, ref handlers.

    Add `import { Button, Input, Select } from './ui';` (or the subset each file needs) to import blocks. Confirm CsvImportModal needs Button + Input only (no Select). UserProfileModal needs all three. BambuImport needs Button + Input only.
  </action>
  <verify>
    <automated>node scripts/lint-no-raw-html.mjs && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/lint-no-raw-html.mjs` reports 0 violations for these 3 files (after opt-outs).
    - `grep -c 'allow-raw-html' src/components/CsvImportModal.tsx` returns at least 4 (1 file input + 2 radios + 1 checkbox).
    - `grep -c 'allow-raw-html' src/components/BambuImport.tsx` returns at least 1 (checkbox).
    - `grep -c 'allow-raw-html' src/components/UserProfileModal.tsx` returns 0 (no opt-outs needed in this file).
    - `npm run build` exits 0 (TypeScript clean, Vite build clean, lint guard clean).
    - Radio inputs retain `name="duplicateMode"` (L-4): `grep -c 'name="duplicateMode"' src/components/CsvImportModal.tsx` returns 2.
    - File input retains `ref={fileInputRef}` and `accept=".csv"`.
  </acceptance_criteria>
  <done>3 modal files fully refactored; all 6 legitimate opt-outs in place with `allow-raw-html`; radio grouping preserved; file picker ref preserved; build green.</done>
</task>

<task type="auto">
  <name>Task 6: Adversarial lint guard tests (UI-03 verification)</name>
  <files>src/components/CostCalculator.tsx, src/components/ui/Button.tsx, src/pages/LandingPage.tsx</files>
  <read_first>
    - .planning/phases/07-styling-primitives-pass/07-VALIDATION.md § Adversarial Lint Tests (the 5-row table — these are the exact tests to perform)
    - .planning/phases/07-styling-primitives-pass/07-RESEARCH.md § Validation of the Lint Guard (3 assertions)
    - scripts/lint-no-raw-html.mjs (the script under test — confirm exclusion paths)
  </read_first>
  <action>
    Execute the 5 adversarial tests from 07-VALIDATION.md § Adversarial Lint Tests. For each test: make the temporary modification, run the command, assert the expected exit code, then REVERT the modification before moving to the next test. Use `git checkout -- <file>` to revert; do NOT use `git reset`.

    Test 1 — Negative test (violation detected):
    Insert `<button onClick={() => {}}>x</button>` near the top of `src/components/CostCalculator.tsx` (after imports, before existing JSX). Run `node scripts/lint-no-raw-html.mjs`. Assert: exit code 1, output names `src/components/CostCalculator.tsx` and the inserted line. Revert with `git checkout -- src/components/CostCalculator.tsx`.

    Test 2 — Opt-out honored:
    Same insertion as Test 1, but with `{/* allow-raw-html */}` on the line immediately above the `<button>`. Run the script. Assert: exit code 0 (opt-out honored). Revert.

    Test 3 — ui/ exclusion:
    Insert a raw `<input type="text" />` line inside `src/components/ui/Button.tsx` (in a JSX-safe location, e.g., as a child of an existing div in a comment-stripped region). Run the script. Assert: exit code 0 (ui/ excluded per Pitfall 5). Revert.

    Test 4 — pages/ exclusion:
    Insert `<button onClick={() => {}}>test</button>` into `src/pages/LandingPage.tsx` (in a safe location). Run the script. Assert: exit code 0 (pages/ excluded per D-02 — script scans only `src/components`, not `src/pages`). Revert.

    Test 5 — Pre-commit hook rejection:
    Stage a raw `<button>` insertion into any in-scope file via `git add`. Attempt `git commit -m "test"`. Assert: pre-commit hook fires, lint script exits 1, commit is rejected with the violation message. Unstage with `git restore --staged <file>` and revert the working-tree change with `git checkout -- <file>`.

    Record results in a temporary task log; if any test fails, DO NOT proceed — fix the script per the failure mode and re-run the failed test plus Test 1 (to confirm the regression hasn't broken negative detection).
  </action>
  <verify>
    <automated>node scripts/lint-no-raw-html.mjs && echo "Final clean tree: lint passes"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 adversarial tests pass with the expected exit codes (1, 0, 0, 0, hook-reject).
    - After all tests complete, `git status` is clean (no leftover modifications from adversarial inserts).
    - `node scripts/lint-no-raw-html.mjs` on the final clean tree exits 0.
    - `npm run build` on the final clean tree exits 0.
    - The lint guard has been proven to: detect raw elements, honor `allow-raw-html`, exclude `src/components/ui/`, exclude `src/pages/`, and block commits via pre-commit hook.
  </acceptance_criteria>
  <done>5 adversarial tests executed successfully; lint guard proven functional; working tree clean; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer source → build toolchain | Raw HTML form elements introduced into source files cross this boundary; lint guard enforces invariant before commit + before CI build |
| local working tree → git | Pre-commit hook is the local enforcement gate (D-05) |
| pushed code → CI build | `npm run build` lint chain is the CI enforcement gate (D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | scripts/lint-no-raw-html.mjs | mitigate | Script is self-contained Node built-ins only; no npm install; reviewable diff during plan completion |
| T-07-02 | Repudiation | .git/hooks/pre-commit | accept | Hook is not git-tracked by design (D-03 chose grep over husky); `prepare` npm script restores executability on fresh clone |
| T-07-03 | Denial-of-Service | npm run build | mitigate | Lint runs first so fast failures don't waste compile time; script exits 1 on first batch of violations rather than scanning continuously |
| T-07-04 | Information Disclosure | UpdateBanner Tauri conditional (L-7) | accept | Refactor touches only JSX, not the `__IS_TAURI__` logic; no new attack surface introduced |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — refactor only, all primitives already in codebase (07-RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `node scripts/lint-no-raw-html.mjs` exits 0 on the final tree.
- `npm run build` exits 0 (chains lint guard before tsc + vite).
- All 5 adversarial tests pass.
- Pre-commit hook is executable and rejects raw-element commits.
- 9 utility/modal files contain zero unopted raw form elements.
- All `allow-raw-html` opt-outs match the inventory in 07-RESEARCH.md § Allow-Raw-HTML Opt-Out Inventory.
- Behavior preserved across all refactored files (D-08): onClick, onChange, disabled, type, required, name handlers and attributes verbatim.
</verification>

<success_criteria>
- 47 raw form elements across 9 files replaced or opted-out.
- Lint guard installed, wired into build + pre-commit, battle-tested via 5 adversarial assertions.
- Foundation set for Plan 07-01 (heavy forms) and Plan 07-02 (high-count + AssetLibrary) which run at Wave 1 after this plan ships.
- UI-03 fully satisfied; UI-01 and UI-02 partially satisfied (utility/modal slice).
</success_criteria>

<output>
Create `.planning/phases/07-styling-primitives-pass/07-03-SUMMARY.md` when done.
</output>
