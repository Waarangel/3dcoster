---
phase: 08-empty-states-with-ctas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/EmptyState.tsx
  - src/components/ui/EmptyState.test.ts
  - src/components/ui/icons/PackageIcon.tsx
  - src/components/ui/icons/ClipboardListIcon.tsx
  - src/components/ui/icons/PrinterIcon.tsx
  - src/components/ui/icons/index.ts
  - src/components/ui/index.ts
  - src/features.ts
autonomous: true
requirements:
  - UI-04
tags:
  - ui
  - primitive
  - empty-state

planner_decisions:
  - id: PD-01
    decision: "AssetLibrary CTA reuses `startAdding` directly without overriding `formData.category` to `'filament'`."
    rationale: "Parity with existing top-right Add Material button (filterCategory='all' → 'consumable'); minimal blast radius; user can switch category in the form dropdown."
    resolves: "RESEARCH.md Open Question 2"
  - id: PD-02
    decision: "Icon viewBox is 24×24 with strokeWidth 1.5 (overrides UI-SPEC's stated 48×48 viewBox)."
    rationale: "At consumer w-12 h-12 (48px) render size, a 24×24 viewBox + 1.5 stroke renders crisper (~3px effective stroke) than 48×48 (~1.5px effective stroke — too thin against dark slate). Falls within UI-SPEC's `Claude's Discretion` zone for SVG path data. Path data hand-derived from Lucide MIT-licensed `package`, `clipboard-list`, `printer` icons (no Lucide dependency installed)."
    resolves: "RESEARCH.md Assumption A7"
  - id: PD-03
    decision: "EmptyState uses `export function` named export, not `forwardRef`."
    rationale: "EmptyState is composite (icon + heading + body + CTA) and unlikely to need a ref. Card uses forwardRef as a low-level layout primitive; EmptyState's role is different."
    resolves: "RESEARCH.md Pattern 10"
  - id: PD-04
    decision: "Icon barrel `src/components/ui/icons/index.ts` does NOT re-export through top-level `src/components/ui/index.ts`."
    rationale: "Keep primitive surface focused; consumers import from `./ui/icons` for icons and `./ui` for primitives. Matches RESEARCH Pattern 8."
  - id: PD-05
    decision: "Test file is named `EmptyState.test.ts` (not `.tsx`) per vitest.config.ts `include: ['src/**/*.test.ts']` glob. JSX is expressed via `React.createElement` calls to keep the `.test.ts` extension valid."
    rationale: "Avoids touching vitest config mid-phase. Matches the only existing test (`src/utils/threeMfParser.test.ts`)."
  - id: PD-06
    decision: "`shouldShowEmptyState<T>(items: T[], isLoading: boolean): boolean` is exported from `src/components/ui/EmptyState.tsx` (same file, not a sibling util)."
    rationale: "Keeps the helper co-located with the primitive that uses the semantics. Single file for the executor to read."

must_haves:
  truths:
    - "An EmptyState primitive exists in src/components/ui/ that renders icon + headline + supporting copy + optional primary CTA button"
    - "Three icon components (PackageIcon, ClipboardListIcon, PrinterIcon) exist in src/components/ui/icons/ and render inline SVG with currentColor stroke"
    - "The CTA button inside EmptyState uses the Phase 7 Button primitive (no raw <button>)"
    - "`empty-states` is registered in src/features.ts with a release date"
    - "Unit tests prove EmptyState renders title/description, omits CTA when prop absent, and the shouldShowEmptyState predicate behaves correctly across all four input branches"
    - "`npm test` passes; `npm run lint:no-raw-html` exits 0; `tsc -b` succeeds"
  artifacts:
    - path: "src/components/ui/EmptyState.tsx"
      provides: "EmptyState primitive component + shouldShowEmptyState predicate"
      exports: ["EmptyState", "EmptyStateProps", "shouldShowEmptyState"]
      contains_classes: ["text-center py-12", "text-lg font-semibold text-white", "text-sm text-slate-500 leading-relaxed max-w-md mx-auto"]
    - path: "src/components/ui/EmptyState.test.ts"
      provides: "Unit tests for EmptyState component + shouldShowEmptyState predicate"
      contains_test_ids: ["UI-04 trigger logic (4 branches)", "UI-04 render with cta", "UI-04 render without cta"]
    - path: "src/components/ui/icons/PackageIcon.tsx"
      provides: "Inline SVG icon component for AssetLibrary empty state"
      exports: ["PackageIcon"]
    - path: "src/components/ui/icons/ClipboardListIcon.tsx"
      provides: "Inline SVG icon component for JobsManager empty state"
      exports: ["ClipboardListIcon"]
    - path: "src/components/ui/icons/PrinterIcon.tsx"
      provides: "Inline SVG icon component for PrinterSettings empty state"
      exports: ["PrinterIcon"]
    - path: "src/components/ui/icons/index.ts"
      provides: "Barrel export for icon namespace"
      exports: ["PackageIcon", "ClipboardListIcon", "PrinterIcon"]
    - path: "src/components/ui/index.ts"
      provides: "Top-level primitives barrel; adds EmptyState export"
      contains: "export { EmptyState } from './EmptyState'"
    - path: "src/features.ts"
      provides: "Feature release registry; adds 'empty-states' key"
      contains: "'empty-states': new Date("
  key_links:
    - from: "src/components/ui/EmptyState.tsx"
      to: "src/components/ui/Button.tsx"
      via: "import { Button } from './Button'"
      pattern: "import \\{ Button \\} from './Button'"
    - from: "src/components/ui/icons/index.ts"
      to: "src/components/ui/icons/PackageIcon.tsx + ClipboardListIcon.tsx + PrinterIcon.tsx"
      via: "barrel re-export"
      pattern: "export \\{ (Package|ClipboardList|Printer)Icon \\} from"
    - from: "src/components/ui/index.ts"
      to: "src/components/ui/EmptyState.tsx"
      via: "barrel re-export"
      pattern: "export \\{ EmptyState \\} from './EmptyState'"
---

<objective>
Ship the EmptyState primitive, three icon components, the icons barrel, the top-level primitives barrel update, the `empty-states` feature registration, and the unit tests that prove all of it. This plan is the foundation that Plan 02 wires into AssetLibrary, JobsManager, and PrinterSettings.

Purpose: A reviewable foundation pass. Plan 02 cannot fail in a way that the wave-0 tests in this plan don't catch first (predicate logic + rendering contract).

Output: 8 files created/modified; one vitest suite passing; lint guard + tsc green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-empty-states-with-ctas/08-CONTEXT.md
@.planning/phases/08-empty-states-with-ctas/08-RESEARCH.md
@.planning/phases/08-empty-states-with-ctas/08-PATTERNS.md
@.planning/phases/08-empty-states-with-ctas/08-UI-SPEC.md
@.planning/phases/08-empty-states-with-ctas/08-VALIDATION.md

<interfaces>
<!-- Locked contracts the executor needs. The full source is in UI-SPEC.md + PATTERNS.md; this is a fast-lookup summary. -->

EmptyStateProps (locked — UI-SPEC.md § EmptyState Primitive Contract):
- icon: ReactNode
- title: string
- description: string | ReactNode (ReactNode permitted for <br/> mid-paragraph)
- cta?: { label: string; onClick: () => void }
- className?: string

shouldShowEmptyState predicate signature:
- shouldShowEmptyState<T>(items: T[], isLoading: boolean): boolean
- returns: !isLoading && items.length === 0

Button (Phase 7 primitive — src/components/ui/Button.tsx):
- Use as: <Button variant="primary" btnSize="md" onClick={...}>{label}</Button>
- IMPORTANT: prop is `btnSize` NOT `size` (TypeScript HTML-attr conflict)

Existing barrel (src/components/ui/index.ts — lines 1-5):
  export { Button, ButtonLink, getButtonClasses } from './Button';
  export { Input } from './Input';
  export { Select } from './Select';
  export { Textarea } from './Textarea';
  export { Card } from './Card';

Existing test analog (src/utils/threeMfParser.test.ts — imports line 1-3):
  import { describe, it, expect } from 'vitest';
  // Optionally: import { renderToStaticMarkup } from 'react-dom/server';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EmptyState primitive + shouldShowEmptyState predicate + unit tests (Wave 0 + GREEN)</name>
  <files>src/components/ui/EmptyState.tsx, src/components/ui/EmptyState.test.ts</files>
  <read_first>
    - src/components/ui/Card.tsx (pattern analog — same primitive shape, type-only imports, className composition; PATTERNS.md § Pattern A)
    - src/components/ui/Button.tsx (consumed by EmptyState's CTA; note `btnSize` prop NOT `size`)
    - src/components/ui/index.ts (existing barrel style — one export per line)
    - src/utils/threeMfParser.test.ts (the only existing test in the repo — vitest import style, describe/it/expect pattern, leading line-comment IDs)
    - .planning/phases/08-empty-states-with-ctas/08-UI-SPEC.md (§ EmptyState Primitive Contract — locked props + render contract)
    - .planning/phases/08-empty-states-with-ctas/08-RESEARCH.md (§ Pattern 10 for boilerplate; § Validation Architecture for test strategy)
    - .planning/phases/08-empty-states-with-ctas/08-PATTERNS.md (§ Pattern A + § Pattern E)
    - .planning/phases/08-empty-states-with-ctas/08-VALIDATION.md (Wave 0 requirements + per-task verification map)
    - vitest.config.ts (confirm include glob is `src/**/*.test.ts` — extension matters)
  </read_first>
  <behavior>
    - Test 1 (UI-04 trigger): shouldShowEmptyState([], true) returns false
    - Test 2 (UI-04 trigger): shouldShowEmptyState([], false) returns true
    - Test 3 (UI-04 trigger): shouldShowEmptyState([1], false) returns false
    - Test 4 (UI-04 trigger): shouldShowEmptyState([1, 2, 3], true) returns false
    - Test 5 (UI-04 render with cta): renderToStaticMarkup of EmptyState — constructed via React.createElement(EmptyState, { icon: null, title: 'Test title', description: 'Test desc', cta: { label: 'Go', onClick: () => {} } }) — produces HTML that contains "Test title", "Test desc", and at least one "<button" tag
    - Test 6 (UI-04 render without cta): renderToStaticMarkup of EmptyState — constructed via React.createElement(EmptyState, { icon: null, title: 'No CTA title', description: 'No CTA desc' }) (no cta prop) — produces HTML that contains the title and description but NO "<button" tag (Button only renders when cta is defined)
    - Test 7 (UI-04 render description as ReactNode): renderToStaticMarkup of EmptyState — constructed via React.createElement(EmptyState, { icon: null, title: 'BR test', description: React.createElement(React.Fragment, null, 'A', React.createElement('br'), 'B') }) — produces HTML that contains a "<br" tag (proves the prop accepts ReactNode for the JobsManager <br/> case downstream)
  </behavior>
  <action>
    CRITICAL — JSX FORBIDDEN IN .test.ts FILE: this test file uses the `.test.ts` extension (per PD-05 and the vitest include glob `src/**/*.test.ts`). esbuild does NOT transform JSX inside `.ts` files — JSX literal will fail at parse time. Every React element in this file MUST be constructed via `React.createElement(...)` function calls. The file imports React with `import * as React from 'react'` (already prescribed below). Do NOT write `<EmptyState ... />` syntax anywhere. Do NOT write `description={<>...</>}` or any other `prop={...}` brace fragment that looks like JSX prop-passing — those are interpreted as JSX by esbuild and break the parse.

    Create `src/components/ui/EmptyState.tsx` exporting:
    1. `interface EmptyStateProps` matching the locked shape in UI-SPEC.md § EmptyState Primitive Contract (icon: ReactNode; title: string; description: string | ReactNode; cta?: { label: string; onClick: () => void }; className?: string).
    2. `export function EmptyState({ icon, title, description, cta, className = '' }: EmptyStateProps)` returning the locked render contract from UI-SPEC.md § EmptyState Primitive Contract — root `<div>` with `text-center py-12 ${className}`, icon wrapper `<div className="flex justify-center mb-4 text-slate-500">{icon}</div>`, heading `<h3 className="text-lg font-semibold text-white mb-2">{title}</h3>`, paragraph `<p className="text-sm text-slate-500 leading-relaxed max-w-md mx-auto">{description}</p>`, and a conditional `{cta && <div className="mt-6 flex justify-center"><Button variant="primary" btnSize="md" onClick={cta.onClick}>{cta.label}</Button></div>}` block. Import `Button` from `./Button`. Import `ReactNode` via `import type { ReactNode } from 'react'`.
    3. Also export `shouldShowEmptyState<T>(items: T[], isLoading: boolean): boolean` as a named export returning `!isLoading && items.length === 0`. Per PD-06, this lives in the same file as the primitive.

    Create `src/components/ui/EmptyState.test.ts` (extension `.test.ts` per PD-05 and vitest.config.ts glob). Imports: `import { describe, it, expect } from 'vitest';` then `import * as React from 'react';` then `import { renderToStaticMarkup } from 'react-dom/server';` then `import { EmptyState, shouldShowEmptyState } from './EmptyState';`. Wrap all assertions in one `describe('EmptyState', () => { … })` block. Each `it` block starts with a line comment `// --- UI-04 …` matching the threeMfParser.test.ts convention. Express all React element construction via `React.createElement(EmptyState, { ... })` per PD-05 and the JSX-FORBIDDEN warning above. For Test 7, the description prop is itself a React element built with `React.createElement(React.Fragment, null, 'A', React.createElement('br'), 'B')` — the entire EmptyState construction looks like `React.createElement(EmptyState, { icon: null, title: 'BR test', description: React.createElement(React.Fragment, null, 'A', React.createElement('br'), 'B') })`. Implement all seven tests from the `<behavior>` block using this construction style.

    Do NOT use `forwardRef` (per PD-03). Do NOT export `EmptyStateProps` from the top-level `src/components/ui/index.ts` (Task 3 covers the barrel — only the `EmptyState` value export is added, matching the existing barrel symmetry where Button/Card props are not re-exported).
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ui/EmptyState.tsx` exists.
    - `grep -E "^export function EmptyState" src/components/ui/EmptyState.tsx` returns one match.
    - `grep -E "^export function shouldShowEmptyState" src/components/ui/EmptyState.tsx` returns one match.
    - `grep -E "^export interface EmptyStateProps" src/components/ui/EmptyState.tsx` returns one match.
    - `grep -E "import \\{ Button \\} from './Button'" src/components/ui/EmptyState.tsx` returns one match.
    - `grep -E "text-center py-12" src/components/ui/EmptyState.tsx` returns at least one match.
    - `grep -E 'btnSize="md"' src/components/ui/EmptyState.tsx` returns one match (PROVES the Button primitive is used with the correct prop name, not `size`).
    - `grep -cE "<button|<input|<select|<textarea" src/components/ui/EmptyState.tsx` (filtered with `| grep -v '^#'`) returns 0 (no raw form elements introduced — primitive uses `<Button>`).
    - File `src/components/ui/EmptyState.test.ts` exists with extension `.test.ts` (NOT `.tsx`).
    - `grep -E "describe\\('EmptyState'" src/components/ui/EmptyState.test.ts` returns one match.
    - `grep -cE "\\bit\\(" src/components/ui/EmptyState.test.ts` returns at least 7 (seven test cases — format-agnostic match, tolerant of any indent style).
    - `grep -E "renderToStaticMarkup" src/components/ui/EmptyState.test.ts` returns at least one match.
    - `grep -E "React.createElement" src/components/ui/EmptyState.test.ts` returns at least one match (PD-05 — JSX via createElement, not literal).
    - `npm test` exits 0 with all EmptyState tests passing (vitest output shows 7 passing tests in the EmptyState describe block).
    - `npx tsc -b` exits 0.
    - Pre-existing test (threeMfParser) still passes — no regression.
  </acceptance_criteria>
  <done>EmptyState primitive ships with the locked render contract, a pure shouldShowEmptyState predicate, and seven passing unit tests covering all four predicate branches plus three render contract assertions. TypeScript and vitest both green.</done>
</task>

<task type="auto">
  <name>Task 2: Create three icon components + icons barrel</name>
  <files>src/components/ui/icons/PackageIcon.tsx, src/components/ui/icons/ClipboardListIcon.tsx, src/components/ui/icons/PrinterIcon.tsx, src/components/ui/icons/index.ts</files>
  <read_first>
    - .planning/phases/08-empty-states-with-ctas/08-RESEARCH.md (§ Pattern 11 for icon boilerplate; § Code Examples Example 6 for path data — Lucide-derived for `package`, `clipboard-list`, `printer`)
    - .planning/phases/08-empty-states-with-ctas/08-PATTERNS.md (§ src/components/ui/icons/PackageIcon.tsx + sibling icon sections + § Pattern B for barrel style)
    - .planning/phases/08-empty-states-with-ctas/08-UI-SPEC.md (§ Icon contract — locked aesthetic, size, currentColor, strokeLinecap/strokeLinejoin)
    - src/components/ui/index.ts (existing top-level barrel style — to be matched by the icons sub-barrel; do NOT modify in this task)
    - src/App.tsx lines 189-192 (existing inline SVG analog using currentColor stroke — anchors the project's existing SVG conventions)
    - scripts/lint-no-raw-html.mjs (confirms `src/components/ui/` and any subdirectory is excluded via `startsWith` — Pattern 8 Pitfall 1)
  </read_first>
  <action>
    Create three icon component files under `src/components/ui/icons/`. Each file uses the canonical pattern from RESEARCH.md § Pattern 11 + § Code Examples Example 6: type-only import `import type { SVGProps } from 'react'`; named export `export function {Name}Icon(props: SVGProps<SVGSVGElement>)`; returns a single `<svg>` element with attributes `xmlns="http://www.w3.org/2000/svg"`, `viewBox="0 0 24 24"` (per PD-02 — overrides UI-SPEC's stated 48×48), `fill="none"`, `stroke="currentColor"`, `strokeWidth={1.5}`, `strokeLinecap="round"`, `strokeLinejoin="round"`, then `{...props}` LAST (so consumers' className/aria-label overrides take effect). Path children are the exact path data from RESEARCH.md § Code Examples Example 6:
      - `PackageIcon.tsx` → four `<path>` children (package outline + lid + bottom + spine), path data at RESEARCH.md lines 769-772.
      - `ClipboardListIcon.tsx` → one `<rect>` + five `<path>` children (clipboard tab + body + two horizontal lines + two dots), path data at RESEARCH.md lines 795-800.
      - `PrinterIcon.tsx` → two `<path>` + one `<rect>` children (paper feed + printer body + output tray), path data at RESEARCH.md lines 823-825.

    Create `src/components/ui/icons/index.ts` as a barrel with exactly three lines:
      `export { PackageIcon } from './PackageIcon';`
      `export { ClipboardListIcon } from './ClipboardListIcon';`
      `export { PrinterIcon } from './PrinterIcon';`
    Per PD-04, do NOT also re-export these from the parent `src/components/ui/index.ts` (Task 3 handles only the EmptyState export there).
  </action>
  <verify>
    <automated>ls src/components/ui/icons/PackageIcon.tsx src/components/ui/icons/ClipboardListIcon.tsx src/components/ui/icons/PrinterIcon.tsx src/components/ui/icons/index.ts</automated>
    <automated>npx tsc -b</automated>
    <automated>npm run lint:no-raw-html</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist at the exact paths listed in `<files>`.
    - Each icon file contains `import type { SVGProps } from 'react'` (verify per file with `grep -E "import type \\{ SVGProps \\} from 'react'" src/components/ui/icons/{Name}.tsx` returns 1).
    - Each icon file has exactly one named function export matching its filename: `grep -cE "^export function (Package|ClipboardList|Printer)Icon\\(" src/components/ui/icons/{Name}.tsx` returns 1.
    - Each icon `<svg>` element contains `viewBox="0 0 24 24"` (NOT `0 0 48 48`) — `grep -E 'viewBox="0 0 24 24"' src/components/ui/icons/{Name}.tsx` returns at least 1 match per file.
    - Each icon `<svg>` element contains `stroke="currentColor"` and `strokeWidth={1.5}` and `fill="none"` (one `grep` per attribute per file returns ≥ 1).
    - The `{...props}` spread appears on the `<svg>` element AFTER the locked attributes (line-order check via `grep -nE 'strokeLinejoin="round"|\\{\\.\\.\\.props\\}' src/components/ui/icons/PackageIcon.tsx | head` shows the strokeLinejoin line number BEFORE the `{...props}` line number).
    - `src/components/ui/icons/index.ts` exists and `grep -cE "^export \\{ (Package|ClipboardList|Printer)Icon \\} from " src/components/ui/icons/index.ts` returns exactly 3.
    - `grep -cE "Icon" src/components/ui/index.ts` returns 0 (PD-04 — icons are NOT re-exported through the top-level primitives barrel).
    - `npx tsc -b` exits 0.
    - `npm run lint:no-raw-html` exits 0 (the new `icons/` subdirectory is correctly excluded by the guard's `startsWith('src/components/ui')` rule — Pitfall 1).
  </acceptance_criteria>
  <done>Three Lucide-style outline icons exist as standalone React components in `src/components/ui/icons/`, exported via a sub-barrel. Each accepts `SVGProps<SVGSVGElement>` and forwards `className` onto the root `<svg>` so consumers can size them. TypeScript and lint guard both green.</done>
</task>

<task type="auto">
  <name>Task 3: Add EmptyState to primitives barrel + register `empty-states` feature</name>
  <files>src/components/ui/index.ts, src/features.ts</files>
  <read_first>
    - src/components/ui/index.ts (existing barrel — full file is 5 lines; preserve exact line style: `export { Name } from './File';`)
    - src/features.ts (existing registry — preserve kebab-case key style + `new Date('YYYY-MM-DD')` value style; new entry goes BEFORE the trailing `// Add new features here…` comment per PATTERNS.md `src/features.ts` section)
    - .planning/phases/08-empty-states-with-ctas/08-PATTERNS.md (§ `src/components/ui/index.ts (modify…)` + § `src/features.ts (modify…)` for the exact append patterns)
    - .planning/phases/08-empty-states-with-ctas/08-RESEARCH.md (§ Pattern 8 — `EmptyStateProps` type re-export deliberately skipped to keep barrel symmetric; § Pitfall 6 — verify `'empty-states'` key does not collide)
  </read_first>
  <action>
    In `src/components/ui/index.ts`, append ONE new line AFTER the existing `Card` export and BEFORE the EOF: `export { EmptyState } from './EmptyState';`. Do NOT export `EmptyStateProps` (PD-04 — barrel symmetry with Button/Card which also do not re-export their Props types).

    In `src/features.ts`, append ONE new entry inside the `featureReleases` object literal, on the line immediately AFTER the existing `'default-profit-margin'` line and BEFORE the trailing `// Add new features here with their release date` comment: `'empty-states': new Date('2026-05-19'),`. The date `2026-05-19` matches the current ship-date target (per STATE.md `last_activity: 2026-05-19`); if Phase 8 actually ships on a later date the developer can bump it before tagging.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
    <automated>grep -cE "^export \\{ EmptyState \\} from './EmptyState';" src/components/ui/index.ts</automated>
    <automated>grep -cE "'empty-states': new Date\\(" src/features.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^export \\{ EmptyState \\} from './EmptyState';" src/components/ui/index.ts` returns exactly 1.
    - `grep -cE "EmptyStateProps" src/components/ui/index.ts` returns 0 (PD-04 — props type NOT re-exported through barrel).
    - `grep -cE "^export \\{ Button" src/components/ui/index.ts` returns 1 (existing Button export NOT broken).
    - `grep -cE "^export \\{ Card " src/components/ui/index.ts` returns 1 (existing Card export NOT broken).
    - `grep -cE "'empty-states': new Date\\(" src/features.ts` returns exactly 1.
    - `grep -cE "'default-profit-margin': new Date\\(" src/features.ts` returns 1 (pre-existing entry preserved).
    - `grep -cE "// Add new features here" src/features.ts` returns 1 (trailing comment preserved — new entry goes BEFORE it).
    - `npx tsc -b` exits 0 (proves the barrel + registry changes compile against the actual EmptyState.tsx export shape from Task 1).
  </acceptance_criteria>
  <done>`EmptyState` is importable from `./ui` via `import { EmptyState } from './ui'`. `empty-states` is registered in the feature release registry so Plan 02's NewBadge can fire. Both files preserve all pre-existing entries.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the full sampling-rate suite:

1. `npm test` — vitest passes (existing threeMfParser test + new EmptyState tests).
2. `npx tsc -b` — strict TypeScript passes including the new EmptyState/icon/barrel/registry changes.
3. `npm run lint:no-raw-html` — exits 0 (proves the new files under `src/components/ui/` and `src/components/ui/icons/` are correctly excluded, AND that EmptyState's CTA uses the `<Button>` primitive not a raw `<button>`).
4. `npm run build` — full build chain green (`node scripts/lint-no-raw-html.mjs && tsc -b && vite build`).

If all four pass, the foundation is solid and Plan 02 can wire it in.
</verification>

<success_criteria>
- EmptyState primitive renders the locked contract from UI-SPEC.md (verified by 3 render tests in EmptyState.test.ts).
- shouldShowEmptyState predicate is correct across all 4 input branches (verified by 4 predicate tests).
- Three Lucide-style icons exist and forward `className` for consumer sizing.
- `EmptyState` is exported from `./ui`; the three icons are exported from `./ui/icons` (separate namespace).
- `empty-states` is registered in `src/features.ts`.
- `npm test`, `npx tsc -b`, `npm run lint:no-raw-html`, and `npm run build` all exit 0.
- No raw `<button>`, `<input>`, `<select>`, or `<textarea>` introduced anywhere outside `src/components/ui/`.
- Pre-existing `printer-maintenance-alerts` and `default-profit-margin` registry entries preserved.
- Pre-existing `Button`/`Card`/`Input`/`Select`/`Textarea` barrel entries preserved.
</success_criteria>

<output>
Create `.planning/phases/08-empty-states-with-ctas/08-01-SUMMARY.md` when done. Include:
- Final list of files created/modified with line counts
- vitest output (7 EmptyState tests passing)
- Any PD-XX decisions revised during execution (e.g., if PD-02 viewBox swap needed reverting after visual review)
- Any open question for Plan 02 (e.g., a Button prop change required)
</output>
</output>
