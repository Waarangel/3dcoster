---
phase: 08-empty-states-with-ctas
plan: 02
type: execute
wave: 2
# depends_on enforces Wave 2 after Wave 1; SUMMARY.md @-load in <context>
# is populated by Plan 01 Task 3 before this plan runs.
depends_on:
  - 08-01
files_modified:
  - src/components/AssetLibrary.tsx
  - src/components/JobsManager.tsx
  - src/components/PrinterSettings.tsx
  - src/App.tsx
autonomous: false
requirements:
  - UI-04
tags:
  - ui
  - wiring
  - empty-state
  - new-badge

planner_decisions:
  - id: PD-07
    decision: "NewBadge for `empty-states` placed on ALL THREE relevant tabs: jobs, materials, settings."
    rationale: "Phase 8 ships empty-state CTAs on three tabs. Shared `feature='empty-states'` key means NewBadge's localStorage first-seen logic (36 h window) clears all three the moment the user glances at any one — no per-tab clutter, no missed discoverability if the user never visits one of the three. Resolves RESEARCH.md Open Question 1, Option A."
    resolves: "RESEARCH.md Open Question 1"
  - id: PD-08
    decision: "AssetLibrary CTA reuses `startAdding` verbatim (no wrapper, no category override)."
    rationale: "Per Plan 01 PD-01 — parity with the existing top-right Add Material button. When the library is empty, filterCategory='all' so startAdding sets formData.category='consumable'; the in-form dropdown lets the user switch to filament if needed. The minor mismatch with the 'No materials in your library yet' headline copy is acceptable; an alternative wrapper would expand blast radius for trivial gain."
    resolves: "RESEARCH.md Open Question 2"
  - id: PD-09
    decision: "Pre-existing inline `<NewBadge feature='printer-maintenance-alerts' />` at App.tsx:258 is LEFT AS-IS in Phase 8. Do NOT fix the inline-vs-overlay anti-pattern in this phase."
    rationale: "Out of UI-04 scope. The badge's release date (2026-04-15) is past the 14-day MAX_AGE window so it currently renders nothing — latent bug, not active regression. Tech-debt item logged for a focused cleanup phase. RESEARCH.md Open Question 3 + Pattern 7 alignment."
    resolves: "RESEARCH.md Open Question 3"
  - id: PD-10
    decision: "JobsManager prop callback signature is typed inline (`(tab: 'calculator' | 'jobs' | 'materials' | 'settings') => void`) — do NOT import the `Tab` union from App.tsx."
    rationale: "Avoids creating a cross-file type dependency for one prop. TypeScript structurally compares the inline signature against App.tsx's `setActiveTab` (typed as `Dispatch<SetStateAction<Tab>>`) — assignment-compatible without an import."
    resolves: "RESEARCH.md Anti-Patterns line 539"
  - id: PD-11
    decision: "All three consumers rely on App.tsx's existing global `isLoading` gate (line 106 + 149-155). Do NOT add local `!isLoading` checks in the empty-state ternaries."
    rationale: "D-08 already satisfied by the global gate — App.tsx never renders a consumer while any of the eight `*Loading` flags is true, so the per-consumer `length === 0` guard cannot fire prematurely. Adding redundant local checks would duplicate the contract and risk drift when Phase 9 changes the loading model."
    resolves: "CONTEXT.md D-08 + RESEARCH.md Pattern 6 + Pitfall 2"

must_haves:
  truths:
    - "When AssetLibrary is rendered with assets.length === 0, the screen shows the EmptyState primitive (Package icon + 'No materials in your library yet' headline + body copy + Add Material CTA)"
    - "Clicking the Add Material CTA invokes the existing startAdding handler — the Add Material form opens"
    - "When JobsManager is rendered with jobs.length === 0, the screen shows the EmptyState primitive (ClipboardList icon + 'No jobs saved yet' headline + body copy preserving the inline <br/> + Open Calculator CTA) inside the existing bg-slate-800 panel with the existing <h2>My Print Jobs</h2> heading"
    - "Clicking the Open Calculator CTA switches the active tab in App.tsx from 'jobs' to 'calculator'"
    - "When PrinterSettings is rendered with printerInstances.length === 0, the screen shows the EmptyState primitive (Printer icon + 'No printers added yet' headline + body copy + Add Printer CTA) inside the existing panel; the top-right Add Printer button at PrinterSettings.tsx:79-81 is NOT removed"
    - "Clicking the Add Printer CTA invokes setShowAddForm(true) — the Add Printer form opens inline"
    - "NewBadge with feature='empty-states' renders as an absolute overlay (`className=\"absolute -top-1 -right-1\"`) on the jobs, materials, AND settings tab buttons; it does NOT push, wrap, shrink, or reflow any sibling tab labels"
    - "Pre-existing inline `<NewBadge feature='printer-maintenance-alerts' />` at App.tsx:258 is preserved verbatim — Phase 8 neither fixes nor regresses it"
    - "Lint guard exits 0 — no raw <button>, <input>, <select>, or <textarea> introduced in any modified consumer"
    - "Full build green: `npm run build` (lint guard + tsc -b + vite build) exits 0"
  artifacts:
    - path: "src/components/AssetLibrary.tsx"
      provides: "Empty-state branch wrapping the existing filter/list section in a ternary on assets.length === 0"
      contains: ["EmptyState", "PackageIcon", "startAdding"]
    - path: "src/components/JobsManager.tsx"
      provides: "Empty-state branch replacing the inner block of the existing if(jobs.length === 0) early-return; adds onSwitchTab prop to JobsManagerProps"
      contains: ["EmptyState", "ClipboardListIcon", "onSwitchTab", "Open Calculator"]
    - path: "src/components/PrinterSettings.tsx"
      provides: "Empty-state branch replacing the single <p> at the empty branch of the printerInstances list"
      contains: ["EmptyState", "PrinterIcon", "setShowAddForm"]
    - path: "src/App.tsx"
      provides: "Passes setActiveTab to JobsManager via onSwitchTab prop; adds NewBadge overlay on jobs/materials/settings tabs"
      contains: ["onSwitchTab={setActiveTab}", "feature=\"empty-states\"", "absolute -top-1 -right-1"]
  key_links:
    - from: "src/components/AssetLibrary.tsx"
      to: "src/components/ui/EmptyState.tsx + src/components/ui/icons/PackageIcon.tsx"
      via: "EmptyState + PackageIcon imports"
      pattern: "import \\{ .*EmptyState.* \\} from './ui'.*\\n.*import \\{ PackageIcon \\} from './ui/icons'"
    - from: "src/components/JobsManager.tsx"
      to: "src/App.tsx setActiveTab"
      via: "onSwitchTab prop callback"
      pattern: "onSwitchTab\\(['\"]calculator['\"]\\)"
    - from: "src/components/PrinterSettings.tsx"
      to: "EmptyState + PrinterIcon"
      via: "barrel imports"
      pattern: "import \\{ .*EmptyState.* \\} from './ui'.*\\n.*import \\{ PrinterIcon \\} from './ui/icons'"
    - from: "src/App.tsx"
      to: "src/components/JobsManager.tsx onSwitchTab prop"
      via: "prop drilling"
      pattern: "<JobsManager[\\s\\S]*onSwitchTab=\\{setActiveTab\\}"
    - from: "src/App.tsx tab map"
      to: "src/features.ts 'empty-states' key"
      via: "NewBadge feature prop"
      pattern: "<NewBadge feature=\"empty-states\" className=\"absolute -top-1 -right-1\""
---

<objective>
Wire the EmptyState primitive into the three consumer screens (AssetLibrary, JobsManager, PrinterSettings), thread the new `onSwitchTab` prop from App.tsx to JobsManager, and add the `empty-states` NewBadge as an absolute overlay on the jobs/materials/settings tab buttons.

Purpose: Deliver the user-visible Phase 8 outcome (every blank screen guides users to the next action) on top of the Plan 01 foundation. After this plan ships, UI-04 is satisfied.

Output: 4 files modified; build chain green; manual UAT confirms all three CTAs work and the NEW badge appears correctly without breaking tab layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-empty-states-with-ctas/08-CONTEXT.md
@.planning/phases/08-empty-states-with-ctas/08-RESEARCH.md
@.planning/phases/08-empty-states-with-ctas/08-PATTERNS.md
@.planning/phases/08-empty-states-with-ctas/08-UI-SPEC.md
@.planning/phases/08-empty-states-with-ctas/08-VALIDATION.md
@.planning/phases/08-empty-states-with-ctas/08-01-SUMMARY.md

<interfaces>
<!-- Locked from Plan 01. The full source is in EmptyState.tsx; this is the call signature executors need. -->

EmptyStateProps (from ./ui):
- icon: ReactNode
- title: string
- description: string | ReactNode
- cta?: { label: string; onClick: () => void }
- className?: string

Icons (from ./ui/icons) — each accepts SVGProps<SVGSVGElement> and forwards className to the root <svg>:
- PackageIcon
- ClipboardListIcon
- PrinterIcon

Consumer-side handlers (existing, reuse — do NOT re-create):
- AssetLibrary.tsx:312-320 — `startAdding: () => void` (sets isAdding=true, formData={category: defaultCategory})
- PrinterSettings.tsx:26 — `setShowAddForm: Dispatch<SetStateAction<boolean>>` (call as `() => setShowAddForm(true)`)
- App.tsx:37 — `setActiveTab: Dispatch<SetStateAction<Tab>>` where Tab='calculator'|'jobs'|'materials'|'settings'

App.tsx existing precedent (lines 133-136 + 287-298) — pattern to mirror for onSwitchTab prop:
  // App.tsx line 133-136 — same shape, same destination tab:
  const handleEditJob = (job: PrintJob) => { setEditingJob(job); setActiveTab('calculator'); };
  // App.tsx line 287-298 — current <JobsManager /> render site (props drilled in)
  // For Phase 8: add `onSwitchTab={setActiveTab}` to the existing prop list.

Existing tab map structure (App.tsx lines 246-263) — host has `relative` className already; safe to add absolute-overlay badges as new children inside the <button>.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire EmptyState into AssetLibrary (zero-assets branch)</name>
  <files>src/components/AssetLibrary.tsx</files>
  <read_first>
    - src/components/AssetLibrary.tsx (full file; especially lines 1-10 for imports, lines 312-320 for the `startAdding` handler being reused, lines 351-389 for the top header bar that MUST be preserved above the new ternary, line 391-1066 for the existing filter/list/form/table block that becomes the falsy branch of the ternary, and lines 1069+ for the CsvImportModal that MUST stay outside the conditional)
    - .planning/phases/08-empty-states-with-ctas/08-RESEARCH.md (§ Pattern 6 + § Code Examples Example 1 for the insertion-point pattern; § Pattern 2 for the `startAdding` handler reuse + Option 1 rationale)
    - .planning/phases/08-empty-states-with-ctas/08-PATTERNS.md (§ `src/components/AssetLibrary.tsx (modify…)` for line-by-line analog citations)
    - .planning/phases/08-empty-states-with-ctas/08-UI-SPEC.md (§ Copywriting Contract → AssetLibrary section for locked headline/body/CTA-label copy)
    - .planning/phases/08-empty-states-with-ctas/08-01-SUMMARY.md (Plan 01 output — confirm EmptyState + PackageIcon shipped as expected; flag any prop changes)
  </read_first>
  <action>
    Extend the existing `from './ui'` import line (currently `import { Button, Input, Select } from './ui';` per AssetLibrary.tsx:5) to also include `EmptyState`. Add a new import line directly below: `import { PackageIcon } from './ui/icons';`.

    Locate the existing top header section (AssetLibrary.tsx lines 353-389 — the `<div className="flex flex-col sm:flex-row …">` with the heading, "+ Add Asset" button, "Import CSV" button, and "Reset" button). Leave that header block fully intact above the new ternary.

    Immediately after the closing `</div>` of the header section (around line 389) and BEFORE the filter/search row (currently line 391), introduce a ternary wrapping everything from line 391 through the end of the filter/list/form/table section (line 1066, the line directly before the `<CsvImportModal …/>`):

      `{assets.length === 0 ? ( <EmptyState icon={<PackageIcon className="w-12 h-12" />} title="No materials in your library yet" description="Add your first filament to start tracking material costs across jobs. You can also import from CSV if you already have a list." cta={{ label: 'Add Material', onClick: startAdding }} /> ) : ( <> {/* everything formerly at lines 391-1066 */} </> )}`

    Use the EXACT locked copy from UI-SPEC.md § Copywriting Contract → AssetLibrary (headline `No materials in your library yet`; body `Add your first filament to start tracking material costs across jobs. You can also import from CSV if you already have a list.`; CTA label `Add Material`). Pass `startAdding` as the CTA's onClick callback per PD-08 — the executor MAY write this as either `onClick: startAdding` OR `onClick: () => startAdding()` OR a block-arrow form; the acceptance criterion below tests intent (the CTA calls the right handler), not exact formatting.

    Keep the `<CsvImportModal …/>` at the original line 1069+ position — OUTSIDE the ternary — so the modal stays mountable when the user clicks the top-right Import CSV button (which remains visible in the empty state via the preserved header).

    Do NOT touch the filter-no-result fallbacks at lines 838-840 and 990-992 (CONTEXT.md D-07 — explicitly out of scope; existing `<p>No materials found</p>` text stays as-is).

    Do NOT add a local `!isLoading` guard — per PD-11 the global App.tsx gate already prevents premature render.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
    <automated>npm run lint:no-raw-html</automated>
    <automated>grep -cE "<EmptyState" src/components/AssetLibrary.tsx</automated>
    <automated>grep -cE "PackageIcon" src/components/AssetLibrary.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^import \\{ [^}]*EmptyState[^}]* \\} from './ui'" src/components/AssetLibrary.tsx` returns 1 match (EmptyState added to the existing barrel import).
    - `grep -E "^import \\{ PackageIcon \\} from './ui/icons'" src/components/AssetLibrary.tsx` returns 1 match (icons sub-barrel imported).
    - `grep -cE "<EmptyState" src/components/AssetLibrary.tsx` returns at least 1.
    - `grep -E "title=\"No materials in your library yet\"" src/components/AssetLibrary.tsx` returns 1 match (locked copy).
    - `grep -E "label: 'Add Material'" src/components/AssetLibrary.tsx` returns 1 match.
    - `grep -E "startAdding\\(\\)|onClick: startAdding" src/components/AssetLibrary.tsx` returns at least 1 match (CTA handler reuses `startAdding` — tolerates either `onClick: startAdding`, `onClick: () => startAdding()`, or a block-arrow form per PD-08; tests intent, not formatting).
    - `grep -E "assets\\.length === 0" src/components/AssetLibrary.tsx` returns at least 1 match (strict zero-items trigger).
    - `grep -cE "<CsvImportModal" src/components/AssetLibrary.tsx` returns 1 (modal preserved at its existing position).
    - `grep -cE "No materials found" src/components/AssetLibrary.tsx` returns 2 (the two filter-no-result fallbacks at lines 838 + 990 are preserved per D-07; the new empty state does NOT replace them).
    - `grep -cE "<button[^>]*>|<input[^>]*>|<select[^>]*>|<textarea[^>]*>" src/components/AssetLibrary.tsx` does not increase from pre-Phase-8 baseline — verified by `npm run lint:no-raw-html` exiting 0.
    - `npx tsc -b` exits 0 (no type errors from the new ternary or imports).
  </acceptance_criteria>
  <done>AssetLibrary renders the EmptyState (with Package icon + locked copy + "Add Material" CTA) when assets.length === 0; renders the existing filter/search/list/form/table tree otherwise. Top-right header buttons and CsvImportModal remain present in both branches. Filter-no-result text is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire EmptyState into JobsManager + add onSwitchTab prop wiring in App.tsx</name>
  <files>src/components/JobsManager.tsx, src/App.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx (full file; especially lines 1-15 for imports + props interface, line 17 for the function-signature destructure, and lines 193-207 for the current empty-state early-return that is being rewritten in place)
    - src/App.tsx (full file; especially line 17 for the `Tab` union, line 37 for `setActiveTab`, lines 133-136 for the `handleEditJob` precedent that already uses the same plumbing pattern, lines 287-298 for the current <JobsManager …/> render site that gets the new prop)
    - .planning/phases/08-empty-states-with-ctas/08-RESEARCH.md (§ Pattern 1 + § Pattern 4 + § Code Examples Example 2)
    - .planning/phases/08-empty-states-with-ctas/08-PATTERNS.md (§ `src/components/JobsManager.tsx (modify…)` + § `src/App.tsx (modify — pass onSwitchTab to JobsManager)`)
    - .planning/phases/08-empty-states-with-ctas/08-UI-SPEC.md (§ Copywriting Contract → JobsManager — copy preserved verbatim from existing JobsManager.tsx:197-203; note `<br/>` mid-paragraph)
    - .planning/phases/08-empty-states-with-ctas/08-01-SUMMARY.md (confirm EmptyState description prop accepts ReactNode for the `<br/>` case)
  </read_first>
  <action>
    In `src/components/JobsManager.tsx`:

    1. Extend the existing `from './ui'` import (line 4) to add `EmptyState` to the named-imports list. Add a new line directly below: `import { ClipboardListIcon } from './ui/icons';`.

    2. Add `onSwitchTab` to the existing `JobsManagerProps` interface (lines 6-15). Use the inline-typed callback signature per PD-10 — do NOT import the `Tab` union from App.tsx: `onSwitchTab: (tab: 'calculator' | 'jobs' | 'materials' | 'settings') => void;`. Append it as the last field of the interface for minimal diff.

    3. Add `onSwitchTab` to the function-parameter destructure (line 17): `export function JobsManager({ jobs, materials, printers, printerInstances, shippingConfig, userCurrency, onDeleteJob, onEditJob, onSwitchTab }: JobsManagerProps) { …`.

    4. Replace lines 193-207 entirely. KEEP the outer `<div className="bg-slate-800 rounded-xl p-6 border border-slate-700">` wrapper AND the `<h2 className="text-lg font-semibold text-white mb-4">My Print Jobs</h2>` heading (per PATTERNS § Pattern 4 preservation rule). REPLACE the inner `<div className="text-center py-12">…</div>` with a single `<EmptyState …/>` element using:
       - `icon={<ClipboardListIcon className="w-12 h-12" />}`
       - `title="No jobs saved yet"`
       - `description={<>Use the Cost Calculator to create and save print jobs.<br />Track sales and see how many copies you need to break even.</>}` — Pitfall 5: this MUST be passed as JSX (ReactNode) to preserve the inline `<br/>` from the existing copy. Do NOT flatten to a plain string. (This file is `.tsx`, so JSX literal is allowed and expected — unlike the Plan 01 `.test.ts` test file.)
       - `cta={{ label: 'Open Calculator', onClick: () => onSwitchTab('calculator') }}`

    Keep the trigger as `if (jobs.length === 0)` (per Pitfall 4 — the existing early-return is correct; do NOT restructure to a single-return-with-conditional). Do NOT add a local `!isLoading` guard (PD-11).

    In `src/App.tsx`:

    5. At the existing `<JobsManager …/>` render site (lines 287-298), append `onSwitchTab={setActiveTab}` as a new prop on the existing element. `setActiveTab` is already typed as `Dispatch<SetStateAction<Tab>>` (App.tsx:37) and is assignment-compatible with the inline-typed prop callback — no wrapper needed (mirrors the existing `handleEditJob` precedent at lines 133-136).

    Do NOT modify any other part of App.tsx in this task (the NewBadge wiring is Task 4).
  </action>
  <verify>
    <automated>npx tsc -b</automated>
    <automated>npm run lint:no-raw-html</automated>
    <automated>grep -cE "<EmptyState" src/components/JobsManager.tsx</automated>
    <automated>grep -cE "onSwitchTab=\\{setActiveTab\\}" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^import \\{ [^}]*EmptyState[^}]* \\} from './ui'" src/components/JobsManager.tsx` returns 1 match.
    - `grep -E "^import \\{ ClipboardListIcon \\} from './ui/icons'" src/components/JobsManager.tsx` returns 1 match.
    - `grep -cE "onSwitchTab: \\(tab: 'calculator' \\| 'jobs' \\| 'materials' \\| 'settings'\\) => void" src/components/JobsManager.tsx` returns exactly 1 (interface field with inline-typed callback per PD-10 — NO import of Tab union).
    - `grep -cE "from '.*App'" src/components/JobsManager.tsx | grep -v '^#'` returns 0 (PD-10 — Tab type NOT imported from App.tsx).
    - `grep -E "title=\"No jobs saved yet\"" src/components/JobsManager.tsx` returns 1 match.
    - `grep -E "label: 'Open Calculator'" src/components/JobsManager.tsx` returns 1 match.
    - `grep -E "onSwitchTab\\(.calculator.\\)" src/components/JobsManager.tsx` returns at least 1 match (CTA invokes the new prop with the 'calculator' tab — tolerates single or double quotes around the literal).
    - `grep -cE "<br ?/>" src/components/JobsManager.tsx` returns 1 (Pitfall 5 — inline <br/> preserved inside the description ReactNode, NOT flattened).
    - `grep -cE "<h2[^>]*>My Print Jobs</h2>" src/components/JobsManager.tsx` returns 1 (section heading preserved per PATTERNS Pattern 4).
    - `grep -cE "bg-slate-800 rounded-xl p-6 border border-slate-700" src/components/JobsManager.tsx` returns at least 1 (outer panel wrapper preserved).
    - `grep -cE "if \\(jobs\\.length === 0\\)" src/components/JobsManager.tsx` returns 1 (early-return pattern preserved per Pitfall 4).
    - `grep -cE "onSwitchTab=\\{setActiveTab\\}" src/App.tsx` returns exactly 1 (prop drilled at the existing <JobsManager …/> render site).
    - `grep -cE "<JobsManager" src/App.tsx` returns 1 (no duplicate render site introduced).
    - `npx tsc -b` exits 0 (proves the inline-typed callback is assignment-compatible with `setActiveTab`).
    - `npm run lint:no-raw-html` exits 0.
  </acceptance_criteria>
  <done>JobsManager's zero-jobs branch renders the EmptyState (with ClipboardList icon + verbatim copy preserving the inline <br/> + "Open Calculator" CTA) inside the existing panel + heading. The CTA, when clicked, invokes `setActiveTab('calculator')` via the new prop drilled from App.tsx — mirroring the existing handleEditJob precedent. Build chain green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire EmptyState into PrinterSettings (zero-instances branch)</name>
  <files>src/components/PrinterSettings.tsx</files>
  <read_first>
    - src/components/PrinterSettings.tsx (full file; especially lines 1-5 for imports, line 26 for the `showAddForm` state being reused, lines 73-82 for the panel wrapper + h2 + always-visible top-right "+ Add Printer" button that MUST be preserved, lines 197-200 for the current one-line empty state being replaced)
    - .planning/phases/08-empty-states-with-ctas/08-RESEARCH.md (§ Pattern 3 + § Pattern 5 + § Code Examples Example 3)
    - .planning/phases/08-empty-states-with-ctas/08-PATTERNS.md (§ `src/components/PrinterSettings.tsx (modify…)` for line-by-line analogs)
    - .planning/phases/08-empty-states-with-ctas/08-UI-SPEC.md (§ Copywriting Contract → PrinterSettings for locked copy)
    - .planning/phases/08-empty-states-with-ctas/08-01-SUMMARY.md (confirm EmptyState + PrinterIcon shipped as expected)
  </read_first>
  <action>
    In `src/components/PrinterSettings.tsx`:

    1. Extend the existing `from './ui'` import (line 3) to add `EmptyState`. Add a new line directly below: `import { PrinterIcon } from './ui/icons';`.

    2. Replace ONLY the `<p>` at line 199 (the truthy branch of the existing `{printerInstances.length === 0 ? (…) : (…)}` ternary). Keep the ternary structure and the falsy branch (the `<div className="space-y-3">…instance list…</div>` at lines 200+) UNCHANGED. The replacement is:

       `<EmptyState icon={<PrinterIcon className="w-12 h-12" />} title="No printers added yet" description="Add your first printer to track depreciation, electricity costs, and maintenance intervals across every job." cta={{ label: 'Add Printer', onClick: () => setShowAddForm(true) }} />`

    Use the EXACT locked copy from UI-SPEC.md § Copywriting Contract → PrinterSettings (headline `No printers added yet`; body `Add your first printer to track depreciation, electricity costs, and maintenance intervals across every job.`; CTA label `Add Printer`).

    Reuse `setShowAddForm` from the existing `useState(false)` at line 26. The executor MAY write the CTA onClick as either `onClick: () => setShowAddForm(true)` OR `onClick: () => { setShowAddForm(true); }` (block-arrow form) — both are semantically equivalent. The acceptance criterion below tests intent (the CTA invokes `setShowAddForm(true)`), not formatting.

    Do NOT remove the always-visible top-right "+ Add Printer" button at lines 79-81 (Pattern 5 subtlety — the populated state still needs it, and CTA-in-context is intentionally additive).

    Do NOT modify the outer panel wrapper or `<h2>My Printers</h2>` heading at lines 73-82.

    Do NOT add a local `!isLoading` guard (PD-11).
  </action>
  <verify>
    <automated>npx tsc -b</automated>
    <automated>npm run lint:no-raw-html</automated>
    <automated>grep -cE "<EmptyState" src/components/PrinterSettings.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^import \\{ [^}]*EmptyState[^}]* \\} from './ui'" src/components/PrinterSettings.tsx` returns 1 match.
    - `grep -E "^import \\{ PrinterIcon \\} from './ui/icons'" src/components/PrinterSettings.tsx` returns 1 match.
    - `grep -cE "<EmptyState" src/components/PrinterSettings.tsx` returns 1.
    - `grep -E "title=\"No printers added yet\"" src/components/PrinterSettings.tsx` returns 1 match.
    - `grep -E "label: 'Add Printer'" src/components/PrinterSettings.tsx` returns 1 match.
    - `grep -E "setShowAddForm\\(true\\)" src/components/PrinterSettings.tsx` returns at least 1 match (CTA handler invokes the setter — tolerates either `() => setShowAddForm(true)` or `() => { setShowAddForm(true); }` block-arrow form).
    - `grep -cE "printerInstances\\.length === 0" src/components/PrinterSettings.tsx` returns 1 (existing strict-zero trigger preserved).
    - `grep -cE "<h2[^>]*>My Printers</h2>" src/components/PrinterSettings.tsx` returns 1 (heading preserved).
    - `grep -cE "\\+ Add Printer" src/components/PrinterSettings.tsx` returns at least 1 (top-right button at lines 79-81 preserved — the CTA label inside EmptyState is `Add Printer` WITHOUT the leading `+ `, so the legacy "+ Add Printer" still grep-matches if and only if the original button is intact).
    - `grep -cE "No printers added yet\\. Add your first printer to start tracking\\." src/components/PrinterSettings.tsx` returns 0 (old one-line copy fully replaced).
    - `grep -cE "<p[^>]*>No printers added yet" src/components/PrinterSettings.tsx` returns 0 (old `<p>` element removed).
    - `npx tsc -b` exits 0.
    - `npm run lint:no-raw-html` exits 0.
  </acceptance_criteria>
  <done>PrinterSettings' zero-instances branch renders the EmptyState (with Printer icon + locked copy + "Add Printer" CTA). The CTA invokes the existing `setShowAddForm(true)`. The top-right "+ Add Printer" button, panel wrapper, and `<h2>My Printers</h2>` heading all remain unchanged. Build chain green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add NewBadge overlay for `empty-states` on jobs, materials, and settings tabs</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (full file; especially line 193 — the CORRECT overlay precedent `<NewBadge feature="settings-modal" className="absolute -top-1 -right-1" />` on the gear-icon button; lines 246-263 — the `tabs.map(…)` block where the new badges go; line 258 — the pre-existing inline `<NewBadge feature="printer-maintenance-alerts" />` that is LEFT AS-IS per PD-09)
    - .planning/phases/08-empty-states-with-ctas/08-RESEARCH.md (§ Pattern 7 — Open Question 1 + the inline-vs-overlay anti-pattern explanation; § Code Examples Example 4 for the exact JSX shape)
    - .planning/phases/08-empty-states-with-ctas/08-PATTERNS.md (§ `src/App.tsx (modify — add NewBadge overlay to tabs)` + § Pattern D)
    - .planning/phases/08-empty-states-with-ctas/08-UI-SPEC.md (§ Copywriting Contract → NEW badge copy — feature key locked to `'empty-states'`; placement locked to absolute overlay)
    - $HOME/.claude/projects/-Users-marcusdickinson-Projects-3DCoster/memory/MEMORY.md (NEW Badge Requirement — `absolute -top-1 -right-1` MANDATORY, never inline in flex containers)
  </read_first>
  <action>
    In `src/App.tsx`, modify the existing `tabs.map(tab => (…))` block (lines 246-263). Inside the existing `<button>` element body, AFTER the existing line `{tab.id === 'settings' && <NewBadge feature="printer-maintenance-alerts" />}` (line 258 — PD-09: leave this verbatim, do NOT add `className` to it) and BEFORE the existing line `{activeTab === tab.id && <div className="absolute bottom-0 left-0 right-0 h-0.5 bg-blue-500" />}` (line 260), insert a NEW conditional that renders the `empty-states` NewBadge as an absolute overlay on the jobs, materials, AND settings tabs (PD-07 — Option A):

      `{(tab.id === 'jobs' || tab.id === 'materials' || tab.id === 'settings') && (<NewBadge feature="empty-states" className="absolute -top-1 -right-1" />)}`

    Critical: the `className="absolute -top-1 -right-1"` is MANDATORY per project memory's NEW Badge Requirement (overlays, never inline in flex containers). The tab button host already has `relative` in its className (line 250) so positioning context is in place — no host change needed. If Prettier reformats the element across multiple lines (`feature` and `className` on separate lines), that is fine — the acceptance criteria below test each attribute independently, not their adjacency on a single line.

    Do NOT modify the pre-existing inline `<NewBadge feature="printer-maintenance-alerts" />` at line 258 (PD-09 — out of scope; tech debt logged in plan frontmatter).

    Do NOT add the `empty-states` badge to the `calculator` tab (Phase 8 ships no empty-state CTA on Calculator).

    Do NOT add the badge to the CTA buttons inside any consumer's EmptyState (CONTEXT.md D-10 — badge belongs on the tab, not the CTA, to avoid double-click confusion).
  </action>
  <verify>
    <automated>npx tsc -b</automated>
    <automated>npm run lint:no-raw-html</automated>
    <automated>grep -c 'feature="empty-states"' src/App.tsx</automated>
    <automated>grep -c 'className="absolute -top-1 -right-1"' src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'feature="empty-states"' src/App.tsx` returns exactly 1 (single shared NewBadge across all three tab conditions — same feature key per PD-07; whitespace-agnostic, tolerates Prettier reformatting `feature` onto its own line).
    - `grep -c 'className="absolute -top-1 -right-1"' src/App.tsx` returns at least 2 (overlay class MANDATORY per project memory — appears once on the pre-existing line 193 gear-icon NewBadge precedent AND at least once on the new `empty-states` NewBadge; whitespace-agnostic).
    - Visual cross-check: every `feature="empty-states"` occurrence in App.tsx is on the SAME `<NewBadge …/>` element as a `className="absolute -top-1 -right-1"` attribute. The two attributes need not be on the same line (Prettier may split them) but they must appear on the same JSX element. Executor confirms by inspecting the diff.
    - `grep -cE "tab\\.id === 'jobs' \\|\\| tab\\.id === 'materials' \\|\\| tab\\.id === 'settings'" src/App.tsx` returns exactly 1 (PD-07 — Option A — all three tabs).
    - `grep -cE "feature=\"printer-maintenance-alerts\"" src/App.tsx` returns exactly 1 (PD-09 — pre-existing inline badge preserved verbatim, NOT removed, NOT modified, NOT given a className).
    - `grep -E "feature=\"printer-maintenance-alerts\"[^/]*className=" src/App.tsx` returns 0 matches (PD-09 — pre-existing badge stays inline; do NOT add className to it).
    - `grep -cE "<NewBadge" src/App.tsx` returns exactly 3 (the pre-existing gear-icon badge at line 193, the pre-existing settings-tab inline badge, and the ONE new empty-states overlay shared across the three tab conditions).
    - `grep -c 'feature="empty-states"' src/components/AssetLibrary.tsx src/components/JobsManager.tsx src/components/PrinterSettings.tsx` returns 0 (badge belongs on the tab, NOT inside any consumer's CTA per D-10).
    - `npx tsc -b` exits 0.
    - `npm run lint:no-raw-html` exits 0 (NewBadge usage does not introduce raw form elements).
  </acceptance_criteria>
  <done>The `empty-states` NewBadge appears as an absolute overlay (`className="absolute -top-1 -right-1"`) on the jobs, materials, and settings tab buttons. The pre-existing inline `printer-maintenance-alerts` badge is left untouched (PD-09). No badge is placed on any CTA button (D-10). Build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual UAT — Verify all three empty-state CTAs + NEW badge placement</name>
  <files>n/a — checkpoint task; no files modified</files>
  <read_first>
    - .planning/phases/08-empty-states-with-ctas/08-UI-SPEC.md (the locked design contract — UAT must observe what was promised: monochromatic outline icons ~48px, locked copy, blue primary CTA buttons, absolute NewBadge overlay)
    - .planning/phases/08-empty-states-with-ctas/08-RESEARCH.md (§ Pitfall 3 — AssetLibrary auto-seed gotcha that UAT must work around)
    - .planning/phases/08-empty-states-with-ctas/08-VALIDATION.md (§ Manual-Only Verifications table — the per-behavior UAT criteria)
    - $HOME/.claude/projects/-Users-marcusdickinson-Projects-3DCoster/memory/MEMORY.md (NEW Badge Requirement — UAT must verify badge does NOT push, wrap, shrink, or reflow sibling tabs)
  </read_first>
  <what-built>
    Wave 2 completed: AssetLibrary, JobsManager, and PrinterSettings now render the EmptyState primitive when their underlying list is empty. JobsManager has a new `onSwitchTab` prop drilled from App.tsx. The `empty-states` NewBadge is overlaid on three tab buttons (jobs, materials, settings).
  </what-built>
  <how-to-verify>
    Start the dev server on the project's pinned port: `npm run dev` (port 4173 per `.claude/CLAUDE.md`).

    PRE-STEP — Reset feature-seen state so the new badge actually renders:
    1. Open http://localhost:4173 in a browser.
    2. Open DevTools → Application → Local Storage → http://localhost:4173 → DELETE any key starting with `new-feature-seen-empty-states` (if present from a prior session).
    3. Hard-reload the page.

    STEP A — NewBadge appearance (PD-07 + D-10 + project memory):
    1. Observe the top tab bar.
    2. EXPECTED: a small green "New" badge appears as an absolute overlay on the upper-right corner of the JOBS tab button, MATERIALS tab button, AND SETTINGS tab button. NO badge on the CALCULATOR tab.
    3. EXPECTED: the badge does NOT push the tab labels, does NOT cause any tab to wrap, does NOT cause horizontal scroll. Layout is identical to the pre-Phase-8 layout except for the small corner overlay.
    4. EXPECTED: NO badge appears on any of the three CTA buttons inside the empty states (D-10).
    5. If badges push/wrap/shift labels → FAIL; revisit Task 4 (likely missing the `absolute -top-1 -right-1` className).

    STEP B — JobsManager empty state (only fires when no jobs exist):
    1. Navigate to the JOBS tab.
    2. If there are saved jobs, delete them all via the existing delete buttons until the jobs list is empty.
    3. EXPECTED: the panel shows the `<h2>My Print Jobs</h2>` heading, then below it: a ClipboardList outline icon (~48px, monochromatic slate-500), then the headline "No jobs saved yet", then the body text "Use the Cost Calculator to create and save print jobs." then a line break, then "Track sales and see how many copies you need to break even.", then a blue primary "Open Calculator" button.
    4. Click "Open Calculator". EXPECTED: the active tab switches to CALCULATOR.
    5. If the line break is missing → FAIL Pitfall 5 (`<br/>` flattened to string).

    STEP C — PrinterSettings empty state:
    1. Navigate to SETTINGS → Printers section.
    2. Delete all printer instances via the existing delete buttons.
    3. EXPECTED: the panel shows the `<h2>My Printers</h2>` heading and the always-visible top-right "+ Add Printer" button at the top. Below the printer-types list, where the printer instances list is, EXPECTED: a Printer outline icon (~48px), headline "No printers added yet", body "Add your first printer to track depreciation, electricity costs, and maintenance intervals across every job.", and a blue primary "Add Printer" button.
    4. Click the empty-state "Add Printer" CTA. EXPECTED: the inline Add Printer form opens.
    5. Close the form. Click the top-right "+ Add Printer" button. EXPECTED: same form opens — both buttons reach the same handler (PD-08 / Pattern 5 — both Add Printer affordances are intentional).

    STEP D — AssetLibrary empty state (AUTO-SEED GOTCHA per RESEARCH Pitfall 3):
    1. Navigate to MATERIALS (Asset Library) tab.
    2. EXPECTED on first run: default seeded materials appear (NOT the empty state) — this is by design per `useAssets` auto-seed at `useDatabase.ts:21-23`.
    3. To test the empty state, manually delete every material via the delete buttons. (Alternative: DevTools → Application → IndexedDB → `3DCosterDB` → `materials` → `clear`, then hard-reload.)
    4. EXPECTED: the top-bar header (with "+ Add Asset", "Import CSV", "Reset" buttons) stays visible. Below it: Package outline icon, headline "No materials in your library yet", body "Add your first filament to start tracking material costs across jobs. You can also import from CSV if you already have a list.", and a blue primary "Add Material" button.
    5. Click "Add Material". EXPECTED: the Add Material form opens (handled by `startAdding`).
    6. EXPECTED: the form's category dropdown defaults to "consumable" (PD-08 — known minor mismatch with headline copy; acceptable per RESEARCH Option 1).

    STEP E — Loading-gate sanity (PD-11 / Pitfall 2):
    1. Hard-reload the app while DevTools → Network → Slow 3G throttling is on.
    2. EXPECTED: the global "Loading..." text shows for the throttled duration; no empty-state flashes appear before data arrives.
    3. If an empty state flashes during load → FAIL — investigate whether App.tsx's global `isLoading` gate at line 106 is intact.

    STEP F — NEW badge first-seen behavior:
    1. After viewing any of the three tabs (jobs/materials/settings), navigate away and back.
    2. EXPECTED: the badge persists for the 36-hour seen window (per `NewBadge.tsx` first-seen logic). It will disappear naturally after 36 hours from first sighting.

    STEP G — Build chain green:
    1. In a separate terminal: `npm run build`.
    2. EXPECTED: exits 0 (lint guard + tsc -b + vite build all pass).
  </how-to-verify>
  <action>
    No code change in this task. Execute the seven-step UAT script from `<how-to-verify>` above and observe the system against the EXPECTED outcomes. Report any deviation in `<resume-signal>`.
  </action>
  <verify>
    <human-check>All seven UAT steps (A-G) report EXPECTED behavior. STEP G's `npm run build` must exit 0.</human-check>
  </verify>
  <acceptance_criteria>
    - STEP A: NewBadge appears on jobs/materials/settings tabs as absolute overlay; layout unchanged (no push/wrap/shift); no badge on calculator tab or on any CTA button.
    - STEP B: With zero jobs, JobsManager renders heading + EmptyState + clicking "Open Calculator" switches the active tab to Calculator; the inline line break between the two sentences of the body copy is visible.
    - STEP C: With zero printer instances, PrinterSettings renders the EmptyState with locked copy; both the top-right and empty-state "Add Printer" affordances open the same Add Printer form.
    - STEP D: With manually-cleared materials, AssetLibrary renders the EmptyState; "Add Material" CTA opens the Add Material form; default category is "consumable" (acceptable per PD-08).
    - STEP E: Under Slow 3G throttling, the global "Loading..." text shows; NO empty state flashes before data arrives.
    - STEP F: NewBadge persists across navigation within the 36-hour seen window.
    - STEP G: `npm run build` exits 0 (lint guard + tsc -b + vite build all green).
  </acceptance_criteria>
  <done>Developer types "approved" after verifying all seven UAT steps pass. Phase 8 user-visible UX is confirmed working end-to-end against the locked design contract.</done>
  <resume-signal>Type "approved" if all seven steps pass; otherwise describe which step failed and what was observed (e.g., "Step A: badge pushed Materials tab to wrap on mobile width" or "Step B: Open Calculator did nothing").</resume-signal>
</task>

</tasks>

<verification>
After Task 5 is approved, the full Phase 8 verification stack:

1. `npm test` — vitest exits 0 (Plan 01's EmptyState tests + existing threeMfParser test).
2. `npx tsc -b` — strict TypeScript exits 0.
3. `npm run lint:no-raw-html` — exits 0 (no raw form elements introduced in any consumer).
4. `npm run build` — full build chain green.
5. Manual UAT (Task 5) — all seven interaction flows + badge placement + loading-gate sanity confirmed by the developer.
</verification>

<success_criteria>
- AssetLibrary, JobsManager, and PrinterSettings each render `<EmptyState …/>` on the strict zero-items branch with the locked copy from UI-SPEC.md.
- All three CTAs invoke existing handlers (startAdding / onSwitchTab → setActiveTab / setShowAddForm) — no new modals or routing introduced.
- JobsManager's `<br/>` mid-paragraph is preserved (description passed as ReactNode, not flattened string — Pitfall 5).
- App.tsx passes `setActiveTab` to JobsManager via `onSwitchTab` prop drilled at the existing render site.
- NewBadge with `feature="empty-states"` renders as an absolute overlay on the jobs, materials, and settings tab buttons — does NOT push/wrap/shrink any sibling tab labels (project memory NEW Badge Requirement).
- Pre-existing inline `<NewBadge feature="printer-maintenance-alerts" />` at App.tsx:258 preserved verbatim (PD-09 — out of scope).
- No NewBadge placed on any CTA button (D-10).
- All filter-no-result text in AssetLibrary preserved (D-07).
- App.tsx global loading gate (line 106 + 149-155) unchanged — empty states never flash during initial load (PD-11).
- Lint guard + tsc -b + vitest + vite build all exit 0.
- Manual UAT script in Task 5 passes all seven steps.
</success_criteria>

<output>
Create `.planning/phases/08-empty-states-with-ctas/08-02-SUMMARY.md` when done. Include:
- Final list of files modified with line counts and diff highlights
- Confirmation that PD-07/08/09/10/11 were honored
- UAT result (each of Steps A-G passed or failed)
- Screenshots of the three empty states + the badge placement on the tab bar (if reasonable to capture)
- Note: the AssetLibrary auto-seed gotcha for the next phase's UAT brief (so testers know to manually clear materials to see the empty state)
- Any tech debt logged for future cleanup (e.g., the pre-existing inline `printer-maintenance-alerts` badge)
</output>
</output>
