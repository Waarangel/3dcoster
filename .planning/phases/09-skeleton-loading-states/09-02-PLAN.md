---
phase: 09-skeleton-loading-states
plan: 02
type: execute
wave: 2
# depends_on enforces Wave 2 after Wave 1; SUMMARY.md @-load in <context>
# is populated by Plan 01 Task 3 before this plan runs.
depends_on:
  - 09-01
files_modified:
  - src/App.tsx
  - src/components/AssetLibrary.tsx
  - src/components/JobsManager.tsx
  - src/components/PrinterSettings.tsx
autonomous: false
requirements:
  - UI-05
tags:
  - ui
  - wiring
  - skeleton
  - loading

planner_decisions:
  - id: PD-07
    decision: "App.tsx drops the global `isLoading` aggregate at line 106 ENTIRELY (no narrower aggregate kept). The five unused `isLoading: xxxLoading` destructurings (settingsLoading, printersLoading, profileLoading, shippingLoading, feesLoading) are REMOVED from their respective hook calls; the three needed flags (assetsLoading, jobsLoading, instancesLoading) are KEPT and drilled into consumers as `isLoading` props."
    rationale: "RESEARCH § Architectural Responsibility Map locks per-consumer loading flags as the canonical pattern. The CLAUDE.md project rule mandates `tsc -b` which enforces `noUnusedLocals` — so unused destructurings would fail the build. Removing them is mandatory cleanup, not optional. D-01 explicitly authorizes the planner to decide aggregation: this plan chooses NO aggregate."
    resolves: "RESEARCH § App.tsx loading-flag landscape + CONTEXT.md D-01 + Open Question Q2 (drop unused App.tsx xxxLoading destructurings: YES)"
  - id: PD-08
    decision: "Three composed list skeletons (`AssetListSkeleton`, `JobsListSkeleton`, `PrinterListSkeleton`) are defined as TOP-LEVEL functions in their respective consumer files — placed AFTER the file imports and BEFORE the main `export function {Consumer}` definition. They are NOT exported (file-internal helpers). NO shared `<ListSkeleton>` helper is extracted."
    rationale: "CONTEXT.md D-03 locks co-location and explicitly forbids a separate `skeletons/` directory. Top-level (not nested-inside-Consumer) keeps the skeleton accessible to lint tools and avoids closing over Consumer's props. Resolves Open Question Q3 (extract shared <ListSkeleton>: NO — D-03 locks co-location)."
    resolves: "CONTEXT.md D-03 + Open Question Q3"
  - id: PD-09
    decision: "JobsManager converts from EARLY-RETURN pattern (current `if (jobs.length === 0) return <EmptyState/>` at line 195-207) to SINGLE-RETURN with D-10 ternary inside the existing panel. The Sale Form Modal (lines 347-472) and Delete Confirmation Modal (lines 475-498) stay mounted across ALL branches (loading / empty / list) because they live in the same single-return JSX tree at the bottom of the function."
    rationale: "RESEARCH Pitfall 1 (hook-ordering after dropping the global gate) mandates this refactor — a new `if (isLoading) return …` placed BEFORE the existing `if (jobs.length === 0) return …` would create a second early-return path; both early returns sit AFTER hooks today (lines 20-191 are all hooks), so adding a third early-return is technically safe, BUT the single-return ternary is strictly safer per RESEARCH § Per-Consumer Integration B and Pitfall 1's recommendation. Also keeps the modals mountable during transient `isLoading` flips (e.g., hot-reload)."
    resolves: "RESEARCH Pitfall 1 + RESEARCH § Per-Consumer Integration B"
  - id: PD-10
    decision: "AssetLibrary's `AssetListSkeleton` renders BOTH a mobile-card variant (`md:hidden`) AND a desktop-table variant (`hidden md:block`) — Tailwind responsive classes switch between them. JobsManager's `JobsListSkeleton` and PrinterSettings' `PrinterListSkeleton` render a SINGLE layout each (no responsive switch needed — those lists are single-layout in the real UI)."
    rationale: "Resolves Open Question Q5 (render BOTH mobile + desktop variants in AssetListSkeleton: YES) — AssetLibrary's real UI uses `md:hidden`/`hidden md:block` to switch between mobile cards (lines 741-865) and desktop table (lines 867-1017); the skeleton MUST mirror this per D-03's 'keep shapes in sync with real layouts' rationale and RESEARCH Pitfall 10 (`<table>` skeleton on mobile breaks layout)."
    resolves: "Open Question Q5 + RESEARCH Pitfall 10 + RESEARCH § Per-Consumer Integration A (AssetListSkeleton responsive shape)"
  - id: PD-11
    decision: "JobsManager's pre-existing dead function `_getPrinterName` and `void _getPrinterName;` silencer at lines 186-193 are LEFT AS-IS. Phase 9 does NOT delete them. The single-return refactor of the body preserves the dead-code block exactly where it is — only the JSX structure at line 195+ is rewritten."
    rationale: "RESEARCH Pitfall 2 explicitly mandates this — Phase 9 is NOT a cleanup phase for pre-existing tech debt (08-REVIEW IN-03 flagged it as out-of-scope). Resolves Open Question Q1 (clean _getPrinterName: NO — consistent with D-09 no-sweep)."
    resolves: "Open Question Q1 + RESEARCH Pitfall 2"
  - id: PD-12
    decision: "NO debounce, NO setTimeout, NO `useEffect` with delay added anywhere in the four modified files. Skeletons render IMMEDIATELY when `isLoading === true` and disappear IMMEDIATELY when `isLoading === false`."
    rationale: "CONTEXT.md D-05 explicitly accepts a single-frame flash on instant loads. Adding a debounce would contradict the locked decision. Resolves Open Question Q4 (add CSS fade-in: NO — D-05 says renders immediately)."
    resolves: "Open Question Q4 + CONTEXT.md D-05"
  - id: PD-13
    decision: "The `<JobsManager …/>` render site already passes `onSwitchTab={setActiveTab}` (Phase 8 plumbed it). This plan ONLY adds `isLoading={jobsLoading}` to that render site — does NOT add or modify any other JobsManager prop. Similarly for AssetLibrary (only `isLoading={assetsLoading}` added) and PrinterSettings (only `isLoading={instancesLoading}` added)."
    rationale: "Surgical scope. Phase 8 already wired all the props each consumer needs except the new `isLoading` flag. Minimum diff = minimum blast radius."
    resolves: "RESEARCH § Per-Consumer Integration (App.tsx update sections A/B/C)"

must_haves:
  truths:
    - "App.tsx no longer contains a global `if (isLoading)` block that blocks the entire UI shell — the loading-gate `<div>` at lines 149-155 is REMOVED entirely (D-01)"
    - "App.tsx no longer contains the aggregate `const isLoading = … || … || …` line at line 106 — REMOVED entirely (D-01 + PD-07)"
    - "App.tsx no longer contains the literal text `Loading...` (D-09 scope — the App.tsx 'Loading…' string is removed; modal/component inline 'Loading…' strings are out of scope and NOT swept)"
    - "App.tsx prunes 5 unused `isLoading` destructurings (settingsLoading at line 62, printersLoading at line 75, profileLoading at line 90, shippingLoading at line 96, feesLoading at line 103) so `tsc -b` `noUnusedLocals` passes (PD-07)"
    - "App.tsx keeps 3 `isLoading` destructurings (assetsLoading at line 47, jobsLoading at line 67, instancesLoading at line 80) and drills them into the three consumers as `isLoading` props"
    - "Each of AssetLibrary, JobsManager, PrinterSettings accepts a new `isLoading: boolean` prop in its Props interface and renders a skeleton when its respective loading flag is true (D-01)"
    - "Each of AssetLibrary, JobsManager, PrinterSettings defines its composed list skeleton (`AssetListSkeleton`, `JobsListSkeleton`, `PrinterListSkeleton`) as a top-level function in the same file, NOT in `src/components/ui/` and NOT in a `skeletons/` directory (D-03 + PD-08)"
    - "Composed list skeletons compose ONLY the `<Skeleton>` primitive (and `<div>`/`<table>`/`<tr>`/`<td>`/`<thead>`/`<tbody>` structural elements where appropriate) — no raw `<button>`, `<input>`, `<select>`, or `<textarea>` introduced (D-11)"
    - "AssetListSkeleton renders 3 mobile-card placeholders on `md:hidden` AND 5 desktop-table-row placeholders on `hidden md:block` — both layouts present in the same component, switched by Tailwind responsive classes (PD-10 + RESEARCH Pitfall 10)"
    - "JobsListSkeleton renders 4 placeholder job rows mirroring the real `bg-slate-700/50 border-slate-600` card shape"
    - "PrinterListSkeleton renders 3 placeholder printer rows mirroring the real `bg-slate-700/50` instance shape with Edit/Delete button placeholders"
    - "JobsManager converts from early-return pattern to single-return ternary so the Sale Form Modal (lines 347-472) and Delete Confirmation Modal (lines 475-498) stay mounted across all branches (loading / empty / list) — PD-09"
    - "All three consumers render in canonical D-10 order: `isLoading ? <ListSkeleton/> : shouldShowEmptyState(items, isLoading) ? <EmptyState/> : <RealList/>` — Skeleton is the FIRST ternary branch, EmptyState is the SECOND, real content is the third"
    - "All three consumers import `shouldShowEmptyState` from `./ui` (re-exported by Plan 01 Task 3) and call it as the second ternary's predicate"
    - "NO debounce, NO setTimeout, NO `useEffect` delay added in any file — skeletons render and disappear immediately (D-05 + PD-12)"
    - "NO inline `!isLoading` guard added to the existing empty-state predicate location — the D-10 ternary handles ordering; `shouldShowEmptyState(items, isLoading)` already returns `!isLoading && items.length === 0` (RESEARCH § Pitfall 6 confirms predicate is sufficient)"
    - "NO new dependencies installed (no npm install) — Skeleton primitive + composed skeletons are hand-authored against existing primitives and Tailwind utilities only"
    - "The pre-existing JobsManager dead-code block (`_getPrinterName` function + `void _getPrinterName;` silencer at lines 186-193) is LEFT AS-IS — Phase 9 does NOT delete or modify it (PD-11 + RESEARCH Pitfall 2 + CONTEXT.md D-09 no-sweep)"
    - "CostCalculator is not modified — the empty-dropdown flash during initial asset/printer load is acceptable (CONTEXT.md D-07)"
    - "src/features.ts is not modified — NO `skeleton-loading` feature key registered, NO NewBadge call added for it (CONTEXT.md D-08)"
    - "Lint guard exits 0 — no raw `<button>`, `<input>`, `<select>`, or `<textarea>` introduced in any modified consumer"
    - "Full build green: `npm run build` (lint guard + tsc -b + vite build) exits 0"
    - "Manual UAT (Task 5) confirms: AssetLibrary/JobsManager/PrinterSettings each show their skeleton during a cold-IndexedDB reload; skeletons transition smoothly to real content or EmptyState (no flicker/jitter); App shell + Calculator interactive during the loading window; no `skeleton-loading` NEW badge appears"
    - "D-01: per-consumer loading gate active — App.tsx global block removed; each consumer reads its own loading flag and renders its own skeleton"
    - "D-02: Skeleton primitive consumed (not duplicated) — composed list skeletons import `Skeleton` from `./ui`"
    - "D-03: list skeletons co-located in consumer files (NOT in src/components/ui/, NOT in a `skeletons/` directory)"
    - "D-04: animation is Tailwind `animate-pulse` (baked into the Skeleton primitive from Plan 01) — no custom CSS, no keyframes"
    - "D-05: skeletons render immediately, disappear immediately — no debounce, no setTimeout (PD-12)"
    - "D-06: scope is exactly the three list screens (AssetLibrary, JobsManager, PrinterSettings) — CostCalculator, marketing surfaces, modals untouched"
    - "D-07: CostCalculator NOT patched — empty-dropdown flash accepted as designed"
    - "D-08: NO `skeleton-loading` feature key in src/features.ts; NO NewBadge added"
    - "D-09: no inline `Loading…` sweep outside App.tsx — SettingsModal/UserProfileModal/etc. inline 'Loading…' strings stay as-is"
    - "D-10: canonical render order in all three consumers — `isLoading ? <Skeleton> : shouldShowEmptyState(items, isLoading) ? <EmptyState> : <RealList>` (Skeleton FIRST, EmptyState SECOND, list THIRD)"
    - "D-11: lint guard stays green — Skeleton primitive in src/components/ui/ (excluded from scan); composed list skeletons compose `<Skeleton>` + `<div>` only; no raw form elements introduced"
  artifacts:
    - path: "src/App.tsx"
      provides: "Per-consumer loading-gate wiring — removes global `if (isLoading)` block + aggregate; prunes 5 unused destructurings; drills `isLoading={assetsLoading|jobsLoading|instancesLoading}` to three consumers"
      contains: ["isLoading={assetsLoading}", "isLoading={jobsLoading}", "isLoading={instancesLoading}"]
      removes: ["const isLoading =", "if (isLoading)", "Loading..."]
    - path: "src/components/AssetLibrary.tsx"
      provides: "AssetListSkeleton co-located + isLoading prop wired + D-10 ternary replacing the Phase 8 `assets.length === 0 ?` ternary at line 406"
      contains: ["function AssetListSkeleton()", "isLoading: boolean", "shouldShowEmptyState(assets, isLoading)", "Skeleton", "md:hidden", "hidden md:block"]
    - path: "src/components/JobsManager.tsx"
      provides: "JobsListSkeleton co-located + isLoading prop wired + single-return refactor + D-10 ternary inside the existing panel wrapper"
      contains: ["function JobsListSkeleton()", "isLoading: boolean", "shouldShowEmptyState(jobs, isLoading)", "Skeleton"]
      removes: ["if (jobs.length === 0) {"]
    - path: "src/components/PrinterSettings.tsx"
      provides: "PrinterListSkeleton co-located + isLoading prop wired + D-10 ternary replacing the Phase 8 `printerInstances.length === 0 ?` ternary at line 199"
      contains: ["function PrinterListSkeleton()", "isLoading: boolean", "shouldShowEmptyState(printerInstances, isLoading)", "Skeleton"]
  key_links:
    - from: "src/App.tsx"
      to: "src/components/AssetLibrary.tsx isLoading prop"
      via: "prop drilling at the <AssetLibrary …/> render site"
      pattern: "<AssetLibrary[\\s\\S]*?isLoading=\\{assetsLoading\\}"
    - from: "src/App.tsx"
      to: "src/components/JobsManager.tsx isLoading prop"
      via: "prop drilling at the <JobsManager …/> render site"
      pattern: "<JobsManager[\\s\\S]*?isLoading=\\{jobsLoading\\}"
    - from: "src/App.tsx"
      to: "src/components/PrinterSettings.tsx isLoading prop"
      via: "prop drilling at the <PrinterSettings …/> render site"
      pattern: "<PrinterSettings[\\s\\S]*?isLoading=\\{instancesLoading\\}"
    - from: "src/components/AssetLibrary.tsx + JobsManager.tsx + PrinterSettings.tsx"
      to: "src/components/ui/Skeleton.tsx + shouldShowEmptyState from EmptyState.tsx"
      via: "barrel import"
      pattern: "import \\{ [^}]*Skeleton[^}]*shouldShowEmptyState[^}]* \\} from './ui'"
---

<objective>
Wire the `Skeleton` primitive (from Plan 09-01) into AssetLibrary, JobsManager, and PrinterSettings. Remove App.tsx's global "Loading…" gate. Drill `isLoading` props from App.tsx to the three consumers. Define one co-located composed list skeleton per consumer (`AssetListSkeleton`, `JobsListSkeleton`, `PrinterListSkeleton`). Convert JobsManager from early-return to single-return so modals stay mounted across branches. End with a manual UAT checkpoint verifying skeleton appearance on cold reload + smooth transition to real content / EmptyState + App shell interactivity during the loading window.

Purpose: Deliver the user-visible Phase 9 outcome (skeletons replace the plain "Loading…" text on the three list screens) on top of Plan 01's primitive foundation. After this plan ships, UI-05 is satisfied.

Output: 4 files modified; build chain green; manual UAT confirms all three skeletons appear on cold reload and transition cleanly to real content or EmptyState.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-skeleton-loading-states/09-CONTEXT.md
@.planning/phases/09-skeleton-loading-states/09-RESEARCH.md
@.planning/phases/09-skeleton-loading-states/09-VALIDATION.md
@.planning/phases/09-skeleton-loading-states/09-01-SUMMARY.md

<interfaces>
<!-- Locked from Plan 01. The full source is in Skeleton.tsx; this is the call signature executors need. -->

Skeleton (from ./ui, named export from Plan 01):
- variant?: 'line' | 'card' | 'circle' (default 'line')
- width?: string  (Tailwind class, e.g., 'w-24', 'w-1/2')
- height?: string (Tailwind class, e.g., 'h-4', 'h-12')
- rounded?: string (override default radius)
- className?: string (escape hatch)
- Renders: <div role="status" aria-label="Loading" aria-busy="true" className="bg-slate-700 animate-pulse ...">

shouldShowEmptyState (from ./ui, re-exported by Plan 01 Task 3):
- shouldShowEmptyState<T>(items: T[], isLoading: boolean): boolean
- Returns: !isLoading && items.length === 0

Consumer-side existing handlers / state (reuse — do NOT re-create):
- AssetLibrary.tsx:327 — `startAddingFilament: () => void` (Phase 8 CTA — already wired; this plan does NOT modify the EmptyState CTA)
- JobsManager.tsx:19 — `onSwitchTab: (tab: '...') => void` (Phase 8 prop — already drilled from App.tsx; this plan adds `isLoading` BESIDE it, not instead)
- PrinterSettings.tsx:27 — `setShowAddForm: Dispatch<SetStateAction<boolean>>` (Phase 8 CTA handler — already wired)

App.tsx current loading flag inventory (RESEARCH § App.tsx loading-flag landscape, verbatim):
- Line 47: `isLoading: assetsLoading` — KEEP, drill to AssetLibrary
- Line 62: `isLoading: settingsLoading` — REMOVE (D-01 + PD-07 — no consumer needs aggregate)
- Line 67: `isLoading: jobsLoading` — KEEP, drill to JobsManager
- Line 75: `isLoading: printersLoading` — REMOVE (same loading state as assetsLoading; usePrinters wraps useAssets)
- Line 80: `isLoading: instancesLoading` — KEEP, drill to PrinterSettings
- Line 90: `isLoading: profileLoading` — REMOVE (useUserProfile returns default object synchronously)
- Line 96: `isLoading: shippingLoading` — REMOVE (useShippingConfig returns default object synchronously)
- Line 103: `isLoading: feesLoading` — REMOVE (useMarketplaceFees returns default object synchronously)
- Line 106: `const isLoading = … || … || …` — REMOVE entirely
- Lines 149-155: `if (isLoading) { return … }` — REMOVE entirely

App.tsx current consumer render sites (verbatim from src/App.tsx as of pre-Phase-9 state):
- <JobsManager> at line 289-301 — already has onSwitchTab={setActiveTab} (Phase 8); add isLoading={jobsLoading}
- <AssetLibrary> at line 303-315 — needs isLoading={assetsLoading} added
- <PrinterSettings> at line 317-328 — needs isLoading={instancesLoading} added

Phase 8 wiring already in place (DO NOT modify):
- AssetLibrary.tsx:406-413 — Phase 8 `{assets.length === 0 ? <EmptyState ... /> : (...)}` — Plan 09-02 REPLACES this ternary with the D-10 three-branch order
- JobsManager.tsx:195-207 — Phase 8 early return `if (jobs.length === 0) { return ... }` — Plan 09-02 REMOVES this early return and rewrites as single-return ternary
- PrinterSettings.tsx:199-206 — Phase 8 `{printerInstances.length === 0 ? <EmptyState ... /> : (...)}` — Plan 09-02 REPLACES this ternary with the D-10 three-branch order
- NewBadge `empty-states` overlay at App.tsx:258-260 — UNCHANGED (Phase 8's badge; Phase 9 adds NO new badge per D-08)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: App.tsx — remove global loading gate, prune destructurings, drill isLoading props to three consumers</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (full file; especially lines 44-104 for the eight hook calls and their `isLoading: xxxLoading` destructurings; line 106 for the aggregate; lines 149-155 for the gate block; lines 289-301 for <JobsManager> render site; lines 303-315 for <AssetLibrary> render site; lines 317-328 for <PrinterSettings> render site)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § App.tsx loading-flag landscape (the verbatim line-numbered map + per-hook flag inventory)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Code Examples Example 7 (the canonical App.tsx after-edits diff — every change spelled out per line)
    - .planning/phases/09-skeleton-loading-states/09-CONTEXT.md D-01 + D-09 (per-consumer gate; no broader Loading sweep)
    - CLAUDE.md project rule: use `tsc -b` (NOT `tsc --noEmit`) — strict `noUnusedLocals` MUST pass after the prune
  </read_first>
  <action>
    Modify `src/App.tsx` with FIVE coordinated edits in a SINGLE atomic commit (`refactor(09): remove global loading gate and drill isLoading per consumer`):

    1. **Prune unused destructurings** from five hook calls:
       - Line 62 — `isLoading: settingsLoading,` — REMOVE the line.
       - Line 75 — `isLoading: printersLoading,` — REMOVE the line.
       - Line 90 — `isLoading: profileLoading,` — REMOVE the line.
       - Line 96 — `isLoading: shippingLoading,` — REMOVE the line.
       - Line 103 — `isLoading: feesLoading,` — REMOVE the line.

    2. **Keep three destructurings unchanged** (DO NOT touch these — they're load-bearing per PD-07):
       - Line 47 — `isLoading: assetsLoading,` — KEEP.
       - Line 67 — `isLoading: jobsLoading,` — KEEP.
       - Line 80 — `isLoading: instancesLoading,` — KEEP.

    3. **Remove the aggregate line** at line 106:
       - DELETE `const isLoading = assetsLoading || settingsLoading || jobsLoading || printersLoading || instancesLoading || profileLoading || shippingLoading || feesLoading;` ENTIRELY (PD-07 — no narrower aggregate kept).

    4. **Remove the gate block** at lines 149-155:
       - DELETE the entire `if (isLoading) { return ( <div className="min-h-screen bg-slate-900 text-white flex items-center justify-center"><div className="text-slate-400">Loading...</div></div> ); }` block ENTIRELY (D-01).

    5. **Drill `isLoading` props to three consumers** at their existing render sites:
       - At the existing `<JobsManager …/>` element (lines 289-301), ADD `isLoading={jobsLoading}` as a new prop. PD-13: keep the existing `onSwitchTab={setActiveTab}` prop (Phase 8 wiring) unchanged.
       - At the existing `<AssetLibrary …/>` element (lines 303-315), ADD `isLoading={assetsLoading}` as a new prop.
       - At the existing `<PrinterSettings …/>` element (lines 317-328), ADD `isLoading={instancesLoading}` as a new prop.

    Do NOT modify the header, tabs (lines 246-263 — Phase 8's NewBadge overlay stays), modals (lines 209-240), or `<CostCalculator …/>` render site (lines 272-287) — D-06 + D-07 + D-08 lock those as out of scope.

    Do NOT add any `setTimeout`, `useEffect` with delay, or debounce logic (PD-12 + D-05 — skeletons render immediately).

    Do NOT introduce any new `if (...) return ...` early-return in App.tsx after the existing hooks — there should be NO early-return guards in App.tsx after this edit (the only early-return was the `if (isLoading)` block being removed).

    After all five edits, run `npx tsc -b`. If it errors on `settingsLoading is declared but its value is never read` or similar, that means a removal was missed — finish pruning. If it errors on missing `isLoading` prop on any of the three consumers, that means a render-site prop drill was missed — add it.
  </action>
  <verify>
    <automated>npx tsc -b</automated>
    <automated>npm run lint:no-raw-html</automated>
    <automated>npm test</automated>
    <automated>grep -c 'Loading\.\.\.' src/App.tsx</automated>
    <automated>grep -cE '^  if \(isLoading\)' src/App.tsx</automated>
    <automated>grep -cE 'const isLoading =' src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Loading\.\.\.' src/App.tsx` returns exactly 0 (D-09 honored — App.tsx no longer contains the literal text `Loading...`).
    - `grep -cE 'const isLoading =' src/App.tsx` returns exactly 0 (aggregate line removed).
    - `grep -cE 'if \(isLoading\)' src/App.tsx` returns exactly 0 (gate block removed).
    - `grep -cE 'isLoading: settingsLoading' src/App.tsx` returns 0 (PD-07 — unused destructuring pruned).
    - `grep -cE 'isLoading: printersLoading' src/App.tsx` returns 0 (PD-07).
    - `grep -cE 'isLoading: profileLoading' src/App.tsx` returns 0 (PD-07).
    - `grep -cE 'isLoading: shippingLoading' src/App.tsx` returns 0 (PD-07).
    - `grep -cE 'isLoading: feesLoading' src/App.tsx` returns 0 (PD-07).
    - `grep -cE 'isLoading: assetsLoading' src/App.tsx` returns exactly 1 (kept — drilled to AssetLibrary).
    - `grep -cE 'isLoading: jobsLoading' src/App.tsx` returns exactly 1 (kept — drilled to JobsManager).
    - `grep -cE 'isLoading: instancesLoading' src/App.tsx` returns exactly 1 (kept — drilled to PrinterSettings).
    - `grep -cE 'isLoading=\{assetsLoading\}' src/App.tsx` returns exactly 1 (prop drilled at <AssetLibrary>).
    - `grep -cE 'isLoading=\{jobsLoading\}' src/App.tsx` returns exactly 1 (prop drilled at <JobsManager>).
    - `grep -cE 'isLoading=\{instancesLoading\}' src/App.tsx` returns exactly 1 (prop drilled at <PrinterSettings>).
    - `grep -cE '<JobsManager' src/App.tsx` returns 1 AND `grep -cE 'onSwitchTab=\{setActiveTab\}' src/App.tsx` returns 1 (PD-13 — Phase 8 wiring preserved).
    - `grep -cE 'feature="empty-states"' src/App.tsx` returns 1 (Phase 8 NewBadge overlay preserved).
    - `grep -cE 'feature="skeleton-loading"' src/App.tsx` returns 0 (D-08 — no skeleton badge introduced).
    - `grep -cE 'setTimeout|setInterval' src/App.tsx | head -1` value is unchanged from pre-Phase-9 baseline (PD-12 — no new debounce introduced).
    - `npx tsc -b` exits 0 (strict mode with `noUnusedLocals` passes — confirms all unused destructurings pruned).
    - `npm run lint:no-raw-html` exits 0.
    - `npm test` exits 0 (Plan 01's Skeleton tests + Phase 8 EmptyState tests + threeMfParser tests all still pass).
    - Single atomic commit with message `refactor(09): remove global loading gate and drill isLoading per consumer`.
  </acceptance_criteria>
  <done>App.tsx no longer contains a "Loading..." text or a global `if (isLoading)` block. The five unused `isLoading` destructurings are pruned. The three needed loading flags are drilled into AssetLibrary, JobsManager, and PrinterSettings as `isLoading` props. TypeScript strict mode (`tsc -b` with `noUnusedLocals`) passes. App shell + header + Calculator + modals render immediately on cold reload. Single atomic commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AssetLibrary — accept isLoading prop, define AssetListSkeleton (mobile+desktop), wire D-10 ternary</name>
  <files>src/components/AssetLibrary.tsx</files>
  <read_first>
    - src/components/AssetLibrary.tsx (full file; especially lines 1-7 for imports, lines 8-18 for Props interface, lines 61-71 for the function-signature destructure, lines 363-404 for the top header bar that stays UNCHANGED above the ternary, lines 406-1092 for the existing Phase 8 `assets.length === 0 ? <EmptyState/> : (...)` ternary that Plan 09-02 REPLACES with the D-10 three-branch ternary, lines 741-865 for the mobile-card real layout to mirror in `AssetListSkeleton`, lines 867-1017 for the desktop-table real layout to mirror, lines 1094-1100 for the CsvImportModal that stays OUTSIDE the ternary)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Per-Consumer Integration § A (AssetLibrary — full props interface + destructure + imports + AssetListSkeleton + ternary replacement)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Code Examples Example 4 (AssetListSkeleton verbatim — copy the responsive mobile+desktop structure)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Pitfall 10 (`<table>` skeleton on mobile breaks layout — MUST use `md:hidden` / `hidden md:block` switch)
    - .planning/phases/09-skeleton-loading-states/09-CONTEXT.md (D-03 + D-10 + D-11 — co-location, render order, lint guard)
    - .planning/phases/09-skeleton-loading-states/09-01-SUMMARY.md (Plan 01 output — confirm Skeleton + shouldShowEmptyState ship as expected from `./ui`)
  </read_first>
  <action>
    Modify `src/components/AssetLibrary.tsx` with FOUR coordinated edits in a single atomic commit (`feat(09): wire AssetListSkeleton with D-10 loading order`):

    1. **Extend the existing barrel import on line 5** — currently `import { Button, Input, Select, EmptyState } from './ui';` — to ADD `Skeleton` and `shouldShowEmptyState`:
       `import { Button, Input, Select, EmptyState, Skeleton, shouldShowEmptyState } from './ui';`
       Leave line 6 (`import { PackageIcon } from './ui/icons';`) unchanged.

    2. **Add `isLoading: boolean` to the Props interface** at lines 8-18. Append it as the LAST field of `AssetLibraryProps` for minimum diff. Final shape:
       ```
       interface AssetLibraryProps {
         assets: Asset[];
         onAddAsset: (asset: Asset) => void;
         onUpdateAsset: (asset: Asset) => void;
         onDeleteAsset: (id: string) => void;
         onBulkImportAssets: (assets: Asset[]) => Promise<void>;
         onResetMaterials: () => void;
         onResetPrinters: () => void;
         itemsPerPage: number;
         onItemsPerPageChange: (value: number) => void;
         isLoading: boolean;
       }
       ```

    3. **Add `isLoading` to the function-parameter destructure** at lines 61-71. Append it as the LAST destructured prop for minimum diff. The destructure list ends with `onItemsPerPageChange,` — add `isLoading,` after it:
       ```
       export function AssetLibrary({
         assets,
         onAddAsset,
         onUpdateAsset,
         onDeleteAsset,
         onBulkImportAssets,
         onResetMaterials,
         onResetPrinters,
         itemsPerPage,
         onItemsPerPageChange,
         isLoading,
       }: AssetLibraryProps) {
       ```

    4. **Define `AssetListSkeleton` as a top-level function** after line 59 (`const builtInCategories = [...]`) and BEFORE the `function getCategoryLabel` helper at line 46 — OR equivalently, place it AFTER the existing helpers and BEFORE `export function AssetLibrary` at line 61. The executor MAY choose either location as long as `AssetListSkeleton` is a top-level function (NOT nested inside `AssetLibrary`) per PD-08. Recommended insertion: immediately before `export function AssetLibrary`. The function takes NO arguments and renders BOTH a mobile-card variant AND a desktop-table variant per PD-10 + RESEARCH Pitfall 10:

       ```
       function AssetListSkeleton() {
         return (
           <>
             {/* Mobile cards — 3 placeholder cards (visible on viewports < md) */}
             <div className="md:hidden space-y-3">
               {[0, 1, 2].map(i => (
                 <div key={i} className="bg-slate-800/50 rounded-xl p-4 border border-slate-700/50 space-y-3">
                   <div className="flex items-center gap-2">
                     <Skeleton variant="line" width="w-32" />
                     <Skeleton variant="line" width="w-16" height="h-5" rounded="rounded-full" />
                   </div>
                   <Skeleton variant="line" width="w-24" />
                   <Skeleton variant="line" width="w-full" />
                   <div className="grid grid-cols-2 gap-2 pt-2 border-t border-slate-700/50">
                     <Skeleton variant="line" />
                     <Skeleton variant="line" />
                     <Skeleton variant="line" />
                     <Skeleton variant="line" />
                   </div>
                   <div className="flex gap-2 pt-2">
                     <Skeleton variant="line" height="h-9" className="flex-1" />
                     <Skeleton variant="line" height="h-9" className="flex-1" />
                   </div>
                 </div>
               ))}
             </div>

             {/* Desktop table — 5 placeholder rows (visible on viewports ≥ md) */}
             <div className="hidden md:block">
               <table className="w-full text-sm">
                 <thead>
                   <tr className="text-slate-400 text-left border-b border-slate-700">
                     <th className="pb-2 font-medium"><Skeleton variant="line" width="w-20" /></th>
                     <th className="pb-2 font-medium"><Skeleton variant="line" width="w-16" /></th>
                     <th className="pb-2 font-medium"><Skeleton variant="line" width="w-12" /></th>
                     <th className="pb-2 font-medium text-right"><Skeleton variant="line" width="w-16" className="ml-auto" /></th>
                     <th className="pb-2 font-medium text-right"><Skeleton variant="line" width="w-16" className="ml-auto" /></th>
                     <th className="pb-2 font-medium text-right"><Skeleton variant="line" width="w-16" className="ml-auto" /></th>
                   </tr>
                 </thead>
                 <tbody className="divide-y divide-slate-700/50">
                   {[0, 1, 2, 3, 4].map(i => (
                     <tr key={i}>
                       <td className="py-2"><Skeleton variant="line" width="w-32" /></td>
                       <td className="py-2"><Skeleton variant="line" width="w-24" /></td>
                       <td className="py-2"><Skeleton variant="line" width="w-16" /></td>
                       <td className="py-2"><Skeleton variant="line" width="w-20" className="ml-auto" /></td>
                       <td className="py-2"><Skeleton variant="line" width="w-20" className="ml-auto" /></td>
                       <td className="py-2"><Skeleton variant="line" width="w-24" className="ml-auto" /></td>
                     </tr>
                   ))}
                 </tbody>
               </table>
             </div>
           </>
         );
       }
       ```

       Do NOT export `AssetListSkeleton` (file-internal per PD-08). Do NOT extract it to a separate file or to `src/components/ui/` (D-03).

    5. **Replace the Phase 8 ternary at lines 406-413** with a D-10 three-branch ternary. The header bar at lines 363-404 stays UNCHANGED above the ternary. The CsvImportModal at lines 1094-1100 stays UNCHANGED below the closing `)}` of the ternary. The existing falsy branch `<>{...filter tabs, search, form, tables, pagination — lines 414-1090}</>` becomes the THIRD branch of the new ternary (unchanged content; just nested one level deeper). New ternary:

       ```
       {isLoading ? (
         <AssetListSkeleton />
       ) : shouldShowEmptyState(assets, isLoading) ? (
         <EmptyState
           icon={<PackageIcon className="w-12 h-12" />}
           title="No materials in your library yet"
           description="Add your first filament to start tracking material costs across jobs. You can also import from CSV if you already have a list."
           cta={{ label: 'Add Material', onClick: startAddingFilament }}
         />
       ) : (
         <>
           {/* filter tabs, search, form, mobile/desktop tables, pagination — UNCHANGED from Phase 8 (lines 414-1090) */}
         </>
       )}
       ```

       The EmptyState body MUST be IDENTICAL to the Phase 8 version (same icon, title, description, cta label, cta onClick handler — `startAddingFilament`). The locked copy from UI-SPEC § Copywriting Contract → AssetLibrary is preserved.

       Do NOT add a local `!isLoading` guard inside `shouldShowEmptyState`'s usage — the predicate already returns `!isLoading && items.length === 0` per Plan 01's barrel re-export (RESEARCH § Pitfall 6 confirms this is sufficient).

    Do NOT touch the filter-no-result fallbacks at lines 838-840 and 990-992 (CONTEXT.md D-07 + Phase 8 D-07 — explicitly out of scope; existing `<p>No materials found</p>` text stays as-is — Phase 8 PD-08 confirmed this).

    Do NOT add any `setTimeout` / `useEffect` delay (PD-12 + D-05).

    Do NOT introduce any raw `<button>` / `<input>` / `<select>` / `<textarea>` element in `AssetListSkeleton` (D-11 — only `<Skeleton>`, `<div>`, `<table>`, `<thead>`, `<tbody>`, `<tr>`, `<td>`, `<th>` are used).
  </action>
  <verify>
    <automated>npx tsc -b</automated>
    <automated>npm run lint:no-raw-html</automated>
    <automated>npm test</automated>
    <automated>grep -cE "function AssetListSkeleton" src/components/AssetLibrary.tsx</automated>
    <automated>grep -cE "shouldShowEmptyState\\(assets, isLoading\\)" src/components/AssetLibrary.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^import \\{ [^}]*Skeleton[^}]* \\} from './ui'" src/components/AssetLibrary.tsx` returns 1 match (Skeleton added to barrel import).
    - `grep -E "^import \\{ [^}]*shouldShowEmptyState[^}]* \\} from './ui'" src/components/AssetLibrary.tsx` returns 1 match (shouldShowEmptyState added to barrel import — same line as Skeleton).
    - `grep -E "^import \\{ [^}]*EmptyState[^}]* \\} from './ui'" src/components/AssetLibrary.tsx` returns 1 match (EmptyState preserved on same line).
    - `grep -E "^import \\{ PackageIcon \\} from './ui/icons'" src/components/AssetLibrary.tsx` returns 1 match (icon import preserved).
    - `grep -cE "^  isLoading: boolean;" src/components/AssetLibrary.tsx` returns 1 (Props interface field added).
    - `grep -cE "^  isLoading,$" src/components/AssetLibrary.tsx` returns 1 (function-parameter destructure added).
    - `grep -cE "^function AssetListSkeleton\\(\\) \\{" src/components/AssetLibrary.tsx` returns exactly 1 (top-level function — PD-08).
    - `grep -cE "^export function AssetListSkeleton" src/components/AssetLibrary.tsx` returns 0 (NOT exported per PD-08).
    - `grep -cE "md:hidden" src/components/AssetListSkeleton.tsx 2>/dev/null` returns "no such file" — proving the skeleton is NOT extracted to a separate file. (Alternative gate: `grep -cE "md:hidden" src/components/AssetLibrary.tsx` returns at least 1 — proving the responsive mobile variant lives inside AssetLibrary.tsx per D-03 + PD-10.)
    - `grep -cE "md:hidden" src/components/AssetLibrary.tsx` returns at least 1 (PD-10 — mobile variant present).
    - `grep -cE "hidden md:block" src/components/AssetLibrary.tsx` returns at least 1 (PD-10 — desktop variant present; RESEARCH Pitfall 10 mitigation).
    - `grep -cE "<Skeleton" src/components/AssetLibrary.tsx` returns at least 14 (4 in mobile card header + 4 in mobile card grid + 2 in mobile action row = 10 in mobile + 6 in desktop header + 6 in desktop body × 5 rows… count is illustrative, not exact; the assertion is that the file contains many `<Skeleton>` uses, NOT zero or one).
    - `grep -cE "<table" src/components/AssetLibrary.tsx | head -1` value is at least 2 — proving the desktop skeleton variant uses `<table>` (one new `<table>` in `AssetListSkeleton`, plus existing `<table>` in the real desktop list at line 867+).
    - `grep -cE "isLoading \\? \\(" src/components/AssetLibrary.tsx` returns at least 1 (D-10 ternary first branch — `isLoading ?`).
    - `grep -cE "shouldShowEmptyState\\(assets, isLoading\\)" src/components/AssetLibrary.tsx` returns exactly 1 (D-10 ternary second-branch predicate — PD-13 says only one such call).
    - `grep -cE "<AssetListSkeleton" src/components/AssetLibrary.tsx` returns at least 1 (D-10 first-branch render).
    - `grep -cE 'startAddingFilament' src/components/AssetLibrary.tsx` returns at least 2 (preserved Phase 8 CTA handler — definition at line 327 + usage in EmptyState ternary).
    - `grep -cE 'title="No materials in your library yet"' src/components/AssetLibrary.tsx` returns 1 (Phase 8 EmptyState copy preserved verbatim).
    - `grep -cE 'label: .Add Material.' src/components/AssetLibrary.tsx` returns 1 (Phase 8 CTA label preserved — single quotes per Phase 8 PD-08 tolerance for `'`/`"` either way).
    - `grep -cE "<CsvImportModal" src/components/AssetLibrary.tsx` returns 1 (modal preserved at bottom — outside the ternary).
    - `grep -cE "No materials found" src/components/AssetLibrary.tsx` returns 2 (the two filter-no-result fallbacks preserved per Phase 8 D-07).
    - `grep -v '^#' src/components/AssetLibrary.tsx | grep -cE "<button[^>]*>|<input[^>]*>|<select[^>]*>|<textarea[^>]*>"` is unchanged from pre-Phase-9 baseline (D-11 — no raw form elements introduced). Verified by `npm run lint:no-raw-html` exiting 0.
    - `grep -cE "setTimeout|setInterval|useEffect.*delay" src/components/AssetLibrary.tsx | head -1` value is unchanged from pre-Phase-9 baseline (PD-12 — no debounce added).
    - `npx tsc -b` exits 0 — proves the new `isLoading: boolean` prop is satisfied by App.tsx Task 1's `isLoading={assetsLoading}` drill.
    - `npm run lint:no-raw-html` exits 0.
    - `npm test` exits 0.
    - Single atomic commit with message `feat(09): wire AssetListSkeleton with D-10 loading order`.
  </acceptance_criteria>
  <done>AssetLibrary accepts the new `isLoading: boolean` prop, defines `AssetListSkeleton` as a top-level co-located function with both mobile-card and desktop-table variants switched by Tailwind responsive classes, and renders in canonical D-10 order: skeleton → EmptyState (predicate-gated) → real filter/search/list. The header bar, CsvImportModal, and filter-no-result fallbacks are preserved. Build chain green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: JobsManager — accept isLoading prop, define JobsListSkeleton, convert early-return to single-return with D-10 ternary</name>
  <files>src/components/JobsManager.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx (full file; especially lines 1-5 for imports, lines 7-17 for Props interface, line 19 for the function-signature destructure, lines 20-191 for ALL hook calls + helpers (the hook-ordering safe zone — any new early-return MUST come AFTER these), lines 186-193 for the pre-existing dead-code block `_getPrinterName` + `void` silencer that stays as-is per PD-11, lines 195-207 for the Phase 8 early-return that Plan 09-02 REWRITES, lines 209-344 for the populated-state JSX that becomes the third branch of the new ternary, lines 346-498 for the Sale Form Modal + Delete Confirmation Modal that MUST stay mounted across all branches via the single-return refactor per PD-09)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Per-Consumer Integration § B (JobsManager — props interface + destructure + JobsListSkeleton + single-return refactor)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Code Examples Example 5 (JobsListSkeleton verbatim — drop-in location is before `export function JobsManager`)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Pitfall 1 (hook-ordering — single-return refactor mandate) + § Pitfall 2 (`_getPrinterName` stays — do NOT delete)
    - .planning/phases/09-skeleton-loading-states/09-CONTEXT.md (D-03 + D-10 + D-11 + D-09)
    - .planning/phases/09-skeleton-loading-states/09-01-SUMMARY.md (confirm Skeleton + shouldShowEmptyState ship as expected from `./ui`)
  </read_first>
  <action>
    Modify `src/components/JobsManager.tsx` with FOUR coordinated edits in a single atomic commit (`feat(09): wire JobsListSkeleton with single-return D-10 refactor`):

    1. **Extend the existing barrel import on line 4** — currently `import { Button, Input, Select, EmptyState } from './ui';` — to ADD `Skeleton` and `shouldShowEmptyState`:
       `import { Button, Input, Select, EmptyState, Skeleton, shouldShowEmptyState } from './ui';`
       Leave line 5 (`import { ClipboardListIcon } from './ui/icons';`) unchanged.

    2. **Add `isLoading: boolean` to the Props interface** at lines 7-17. Append it as the LAST field for minimum diff. Final shape:
       ```
       interface JobsManagerProps {
         jobs: PrintJob[];
         materials: Material[];
         printers: PrinterConfig[];
         printerInstances: PrinterInstance[];
         shippingConfig: ShippingConfig;
         userCurrency: Currency;
         onDeleteJob: (id: string) => Promise<void>;
         onEditJob: (job: PrintJob) => void;
         onSwitchTab: (tab: 'calculator' | 'jobs' | 'materials' | 'settings') => void;
         isLoading: boolean;
       }
       ```

    3. **Add `isLoading` to the function-parameter destructure** at line 19. Append it as the LAST destructured prop:
       ```
       export function JobsManager({ jobs, materials, printers, printerInstances, shippingConfig, userCurrency, onDeleteJob, onEditJob, onSwitchTab, isLoading }: JobsManagerProps) {
       ```

    4. **Define `JobsListSkeleton` as a top-level function** between line 17 (`}` closing the Props interface) and line 19 (`export function JobsManager`). The function takes no arguments and renders 4 placeholder job rows mirroring the real `bg-slate-700/50 border-slate-600` card shape:
       ```
       function JobsListSkeleton() {
         return (
           <div className="space-y-3">
             {[0, 1, 2, 3].map(i => (
               <div key={i} className="p-4 rounded-lg border bg-slate-700/50 border-slate-600">
                 <div className="flex items-start justify-between">
                   <div className="flex-1 space-y-2">
                     <div className="flex items-center gap-3">
                       <Skeleton variant="line" width="w-40" />
                       <Skeleton variant="line" width="w-32" height="h-5" rounded="rounded-full" />
                     </div>
                     <Skeleton variant="line" width="w-3/4" />
                   </div>
                   <div className="text-right space-y-1">
                     <Skeleton variant="line" width="w-20" height="h-6" className="ml-auto" />
                     <Skeleton variant="line" width="w-16" className="ml-auto" />
                   </div>
                 </div>
               </div>
             ))}
           </div>
         );
       }
       ```

       Do NOT export `JobsListSkeleton` (file-internal per PD-08). Do NOT extract it to a separate file or to `src/components/ui/` (D-03). The function uses ONLY `<Skeleton>` and `<div>` (D-11).

    5. **Convert the early-return at lines 195-207 to a single-return ternary** per PD-09 + RESEARCH Pitfall 1. This is the most surgical edit in this plan — read carefully:

       **DELETE** the entire `if (jobs.length === 0) { return ( <div className="bg-slate-800 ..."><h2>...</h2><EmptyState ... /></div> ); }` block at lines 195-207.

       **REWRITE** the existing `return (...)` block at lines 209-501 so the OUTER structure becomes a single-return with the D-10 three-branch ternary INSIDE the existing `<div className="bg-slate-800 rounded-xl p-6 border border-slate-700">` panel wrapper, and the Sale Form Modal + Delete Confirmation Modal STAY mounted as siblings in the same `<div className="space-y-6">` outer wrapper. Target structure:

       ```
       return (
         <div className="space-y-6">
           {/* Jobs List */}
           <div className="bg-slate-800 rounded-xl p-6 border border-slate-700">
             <h2 className="text-lg font-semibold text-white mb-4">My Print Jobs</h2>

             {isLoading ? (
               <JobsListSkeleton />
             ) : shouldShowEmptyState(jobs, isLoading) ? (
               <EmptyState
                 icon={<ClipboardListIcon className="w-12 h-12" />}
                 title="No jobs saved yet"
                 description={<>Use the Cost Calculator to create and save print jobs.<br />Track sales and see how many copies you need to break even.</>}
                 cta={{ label: 'Open Calculator', onClick: () => onSwitchTab('calculator') }}
               />
             ) : (
               <div className="space-y-3">
                 {jobs.map(job => {
                   /* lines 217-342 UNCHANGED — the entire jobs.map(job => { … }) body stays exactly as it is */
                 })}
               </div>
             )}
           </div>

           {/* Sale Form Modal — lines 347-472 UNCHANGED */}
           {showSaleForm && selectedJob && (
             /* ... */
           )}

           {/* Delete Confirmation Modal — lines 475-498 UNCHANGED */}
           {deleteConfirmJobId && (
             /* ... */
           )}
         </div>
       );
       ```

       Critical preservation requirements per PD-09:
       - The OUTER `<div className="space-y-6">` wrapper stays.
       - The Sale Form Modal block (`{showSaleForm && selectedJob && (...)})` stays at the same position INSIDE the `space-y-6` wrapper.
       - The Delete Confirmation Modal block (`{deleteConfirmJobId && (...)})` stays at the same position INSIDE the `space-y-6` wrapper.
       - The inner panel `<div className="bg-slate-800 rounded-xl p-6 border border-slate-700">` AND the `<h2>My Print Jobs</h2>` heading both stay.
       - The `<div className="space-y-3">{jobs.map(...)}</div>` body is preserved verbatim INSIDE the third ternary branch — the `jobs.map(job => { ... })` callback body at lines 217-342 must be COPIED unchanged.

       Critical Pitfall 5 preservation (from Phase 8): the `description` prop value `<>Use the Cost Calculator to create and save print jobs.<br />Track sales and see how many copies you need to break even.</>` is a JSX fragment with an inline `<br/>`. The fragment expression MUST be preserved EXACTLY as in the Phase 8 wiring at line 202. Do NOT flatten the fragment to a string.

       The EmptyState branch's title (`"No jobs saved yet"`), description fragment, and cta (`{ label: 'Open Calculator', onClick: () => onSwitchTab('calculator') }`) MUST match the Phase 8 version verbatim.

       Do NOT add a local `!isLoading` guard — `shouldShowEmptyState` predicate handles it.

    Do NOT delete the pre-existing `_getPrinterName` function and `void _getPrinterName;` silencer at lines 186-193 (PD-11 + RESEARCH Pitfall 2). Do NOT modify their content. They stay verbatim above the new single-return.

    Do NOT add any `setTimeout` / `useEffect` delay (PD-12 + D-05).

    Do NOT introduce any raw `<button>` / `<input>` / `<select>` / `<textarea>` element in `JobsListSkeleton` (D-11 — only `<Skeleton>` and `<div>`).

    Do NOT change any prop being passed to the modal or to the `jobs.map(...)` body (PD-13 — surgical scope).
  </action>
  <verify>
    <automated>npx tsc -b</automated>
    <automated>npm run lint:no-raw-html</automated>
    <automated>npm test</automated>
    <automated>grep -cE "function JobsListSkeleton" src/components/JobsManager.tsx</automated>
    <automated>grep -cE "if \\(jobs\\.length === 0\\)" src/components/JobsManager.tsx</automated>
    <automated>grep -cE "shouldShowEmptyState\\(jobs, isLoading\\)" src/components/JobsManager.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^import \\{ [^}]*Skeleton[^}]* \\} from './ui'" src/components/JobsManager.tsx` returns 1 match.
    - `grep -E "^import \\{ [^}]*shouldShowEmptyState[^}]* \\} from './ui'" src/components/JobsManager.tsx` returns 1 match.
    - `grep -E "^import \\{ [^}]*EmptyState[^}]* \\} from './ui'" src/components/JobsManager.tsx` returns 1 match (preserved on the same line as Skeleton + shouldShowEmptyState).
    - `grep -E "^import \\{ ClipboardListIcon \\} from './ui/icons'" src/components/JobsManager.tsx` returns 1 match (Phase 8 icon import preserved).
    - `grep -cE "  isLoading: boolean;" src/components/JobsManager.tsx` returns 1 (Props interface field added).
    - `grep -cE "isLoading \\}: JobsManagerProps" src/components/JobsManager.tsx` returns 1 (destructure ends with isLoading — function signature).
    - `grep -cE "^function JobsListSkeleton\\(\\) \\{" src/components/JobsManager.tsx` returns exactly 1 (top-level function per PD-08).
    - `grep -cE "^export function JobsListSkeleton" src/components/JobsManager.tsx` returns 0 (NOT exported per PD-08).
    - `grep -cE "<Skeleton" src/components/JobsManager.tsx` returns at least 5 (JobsListSkeleton renders 5 Skeletons per row × 4 rows in iteration… count is illustrative; assertion is "many Skeletons present").
    - `grep -cE "if \\(jobs\\.length === 0\\)" src/components/JobsManager.tsx` returns exactly 0 (PD-09 — the Phase 8 early-return is REMOVED; single-return refactor complete).
    - `grep -cE "shouldShowEmptyState\\(jobs, isLoading\\)" src/components/JobsManager.tsx` returns exactly 1 (D-10 second-branch predicate).
    - `grep -cE "isLoading \\? \\(" src/components/JobsManager.tsx` returns at least 1 (D-10 first-branch).
    - `grep -cE "<JobsListSkeleton" src/components/JobsManager.tsx` returns at least 1 (D-10 first-branch render).
    - `grep -cE 'title="No jobs saved yet"' src/components/JobsManager.tsx` returns 1 (Phase 8 EmptyState copy preserved verbatim).
    - `grep -cE "label: 'Open Calculator'" src/components/JobsManager.tsx` returns 1 (Phase 8 CTA label preserved).
    - `grep -cE "onSwitchTab\\('calculator'\\)" src/components/JobsManager.tsx` returns 1 (CTA callback preserved).
    - `grep -cE "<br ?/>" src/components/JobsManager.tsx` returns 1 (RESEARCH/Phase 8 Pitfall 5 — inline `<br/>` preserved in the description fragment).
    - `grep -cE "<h2[^>]*>My Print Jobs</h2>" src/components/JobsManager.tsx` returns 1 (section heading preserved — single instance now since the early-return version was removed).
    - `grep -cE "bg-slate-800 rounded-xl p-6 border border-slate-700" src/components/JobsManager.tsx` returns at least 1 (panel wrapper preserved).
    - `grep -cE "showSaleForm && selectedJob" src/components/JobsManager.tsx` returns at least 1 (Sale Form Modal preserved per PD-09).
    - `grep -cE "deleteConfirmJobId &&" src/components/JobsManager.tsx` returns at least 1 (Delete Confirmation Modal preserved per PD-09).
    - `grep -cE "_getPrinterName" src/components/JobsManager.tsx` returns exactly 2 (PD-11 — function definition + `void` silencer both preserved verbatim).
    - `grep -cE "void _getPrinterName" src/components/JobsManager.tsx` returns 1 (PD-11 — silencer preserved).
    - `grep -cE "jobs\\.map\\(job =>" src/components/JobsManager.tsx` returns exactly 1 (the populated-state map body preserved — single occurrence; appears only inside the third ternary branch now, not duplicated).
    - `grep -v '^#' src/components/JobsManager.tsx | grep -cE "<button[^>]*>|<input[^>]*>|<select[^>]*>|<textarea[^>]*>"` is unchanged from pre-Phase-9 baseline (D-11). Verified by `npm run lint:no-raw-html` exiting 0.
    - `grep -cE "setTimeout|setInterval" src/components/JobsManager.tsx | head -1` value is unchanged from pre-Phase-9 baseline (PD-12).
    - `npx tsc -b` exits 0 (proves the new `isLoading` prop is wired and the single-return JSX type-checks; proves the existing hooks at lines 20-191 still come BEFORE any conditional return — RESEARCH Pitfall 1 mitigation).
    - `npm run lint:no-raw-html` exits 0.
    - `npm test` exits 0.
    - Single atomic commit with message `feat(09): wire JobsListSkeleton with single-return D-10 refactor`.
  </acceptance_criteria>
  <done>JobsManager accepts the new `isLoading` prop, defines `JobsListSkeleton` co-located, converts from early-return to single-return so the Sale Form and Delete Confirmation modals stay mounted across loading/empty/list branches (PD-09 + RESEARCH Pitfall 1), and renders in canonical D-10 order. The pre-existing `_getPrinterName` dead code is preserved verbatim per PD-11. Build chain green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: PrinterSettings — accept isLoading prop, define PrinterListSkeleton, wire D-10 ternary</name>
  <files>src/components/PrinterSettings.tsx</files>
  <read_first>
    - src/components/PrinterSettings.tsx (full file; especially lines 1-5 for imports, lines 6-15 for Props interface, lines 17-26 for the function-signature destructure, lines 71-82 for the panel wrapper + `<h2>My Printers</h2>` heading + always-visible top-right "+ Add Printer" button that MUST be preserved unchanged in ALL branches, lines 85-196 for the Add Printer form that sits OUTSIDE the loading/empty/list branch (also unchanged), lines 199-206 for the Phase 8 `printerInstances.length === 0 ?` ternary that Plan 09-02 REPLACES with the D-10 three-branch ternary, lines 207-352 for the instance list `.map(...)` body that becomes the third branch of the new ternary)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Per-Consumer Integration § C (PrinterSettings — props interface + destructure + PrinterListSkeleton + ternary replacement)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Code Examples Example 6 (PrinterListSkeleton verbatim)
    - .planning/phases/09-skeleton-loading-states/09-CONTEXT.md (D-03 + D-10 + D-11)
    - .planning/phases/09-skeleton-loading-states/09-01-SUMMARY.md (confirm Skeleton + shouldShowEmptyState ship as expected from `./ui`)
  </read_first>
  <action>
    Modify `src/components/PrinterSettings.tsx` with FOUR coordinated edits in a single atomic commit (`feat(09): wire PrinterListSkeleton with D-10 loading order`):

    1. **Extend the existing barrel import on line 3** — currently `import { Button, Input, Select, EmptyState } from './ui';` — to ADD `Skeleton` and `shouldShowEmptyState`:
       `import { Button, Input, Select, EmptyState, Skeleton, shouldShowEmptyState } from './ui';`
       Leave line 4 (`import { PrinterIcon } from './ui/icons';`) unchanged.

    2. **Add `isLoading: boolean` to the Props interface** at lines 6-15. Append it as the LAST field for minimum diff. Final shape:
       ```
       interface PrinterSettingsProps {
         printers: PrinterConfig[];
         printerInstances: PrinterInstance[];
         jobs: PrintJob[];
         electricity: ElectricityConfig;
         onAddInstance: (instance: PrinterInstance) => void;
         onUpdateInstance: (instance: PrinterInstance) => void;
         onDeleteInstance: (id: string) => void;
         onElectricityChange: (config: ElectricityConfig) => void;
         isLoading: boolean;
       }
       ```

    3. **Add `isLoading` to the function-parameter destructure** at lines 17-26. Append it as the LAST destructured prop:
       ```
       export function PrinterSettings({
         printers,
         printerInstances,
         jobs,
         electricity,
         onAddInstance,
         onUpdateInstance,
         onDeleteInstance,
         onElectricityChange,
         isLoading,
       }: PrinterSettingsProps) {
       ```

    4. **Define `PrinterListSkeleton` as a top-level function** between line 15 (`}` closing the Props interface) and line 17 (`export function PrinterSettings`). The function takes no arguments and renders 3 placeholder printer rows mirroring the real `bg-slate-700/50 border-slate-600` instance card shape:
       ```
       function PrinterListSkeleton() {
         return (
           <div className="space-y-3">
             {[0, 1, 2].map(i => (
               <div key={i} className="p-4 bg-slate-700/50 rounded-lg border border-slate-600">
                 <div className="flex items-center justify-between">
                   <div className="flex-1 space-y-2">
                     <div className="flex items-center gap-2">
                       <Skeleton variant="line" width="w-32" />
                       <Skeleton variant="line" width="w-24" />
                     </div>
                     <Skeleton variant="line" width="w-3/4" />
                     <Skeleton variant="line" width="w-full" height="h-1.5" className="max-w-md" />
                     <Skeleton variant="line" width="w-2/3" height="h-3" className="max-w-md" />
                   </div>
                   <div className="flex gap-2">
                     <Skeleton variant="line" width="w-16" height="h-9" />
                     <Skeleton variant="line" width="w-20" height="h-9" />
                   </div>
                 </div>
               </div>
             ))}
           </div>
         );
       }
       ```

       Do NOT export `PrinterListSkeleton` (file-internal per PD-08). Do NOT extract it to a separate file or to `src/components/ui/` (D-03). The function uses ONLY `<Skeleton>` and `<div>` (D-11).

    5. **Replace the Phase 8 ternary at lines 199-206** with a D-10 three-branch ternary. The panel wrapper + `<h2>My Printers</h2>` heading + always-visible top-right "+ Add Printer" button at lines 73-82 stay UNCHANGED above. The Add Printer form at lines 85-196 stays UNCHANGED above (sits between the heading area and the ternary). The existing falsy branch `<div className="space-y-3">{printerInstances.map(...)}</div>` at lines 207-352 becomes the THIRD branch of the new ternary (unchanged content; just nested one level deeper). New ternary:

       ```
       {/* Printer Instances List */}
       {isLoading ? (
         <PrinterListSkeleton />
       ) : shouldShowEmptyState(printerInstances, isLoading) ? (
         <EmptyState
           icon={<PrinterIcon className="w-12 h-12" />}
           title="No printers added yet"
           description="Add your first printer to track depreciation, electricity costs, and maintenance intervals across every job."
           cta={{ label: 'Add Printer', onClick: () => setShowAddForm(true) }}
         />
       ) : (
         <div className="space-y-3">
           {printerInstances.map(instance => {
             /* lines 208-352 UNCHANGED — the entire printerInstances.map(instance => { … }) body stays exactly as it is */
           })}
         </div>
       )}
       ```

       The EmptyState body MUST be IDENTICAL to the Phase 8 version at lines 200-205 (same icon, title, description, cta label, cta handler — `() => setShowAddForm(true)`). The locked copy from UI-SPEC § Copywriting Contract → PrinterSettings is preserved.

    Do NOT remove the always-visible top-right "+ Add Printer" button at lines 80-82 (Phase 8 PD-08 + this plan's surgical scope — both Add Printer affordances are intentional).

    Do NOT modify the outer panel wrapper or `<h2>My Printers</h2>` heading at lines 73-82.

    Do NOT modify the Add Printer form at lines 85-196.

    Do NOT modify the Electricity panel below the printers panel (if present in the file).

    Do NOT add a local `!isLoading` guard — `shouldShowEmptyState` predicate handles it.

    Do NOT add any `setTimeout` / `useEffect` delay (PD-12 + D-05).

    Do NOT introduce any raw `<button>` / `<input>` / `<select>` / `<textarea>` element in `PrinterListSkeleton` (D-11 — only `<Skeleton>` and `<div>`).
  </action>
  <verify>
    <automated>npx tsc -b</automated>
    <automated>npm run lint:no-raw-html</automated>
    <automated>npm test</automated>
    <automated>grep -cE "function PrinterListSkeleton" src/components/PrinterSettings.tsx</automated>
    <automated>grep -cE "shouldShowEmptyState\\(printerInstances, isLoading\\)" src/components/PrinterSettings.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^import \\{ [^}]*Skeleton[^}]* \\} from './ui'" src/components/PrinterSettings.tsx` returns 1 match.
    - `grep -E "^import \\{ [^}]*shouldShowEmptyState[^}]* \\} from './ui'" src/components/PrinterSettings.tsx` returns 1 match.
    - `grep -E "^import \\{ [^}]*EmptyState[^}]* \\} from './ui'" src/components/PrinterSettings.tsx` returns 1 match (preserved on the same line as Skeleton + shouldShowEmptyState).
    - `grep -E "^import \\{ PrinterIcon \\} from './ui/icons'" src/components/PrinterSettings.tsx` returns 1 match (Phase 8 icon import preserved).
    - `grep -cE "^  isLoading: boolean;" src/components/PrinterSettings.tsx` returns 1 (Props interface field added).
    - `grep -cE "^  isLoading,$" src/components/PrinterSettings.tsx` returns 1 (function-parameter destructure added).
    - `grep -cE "^function PrinterListSkeleton\\(\\) \\{" src/components/PrinterSettings.tsx` returns exactly 1 (top-level function per PD-08).
    - `grep -cE "^export function PrinterListSkeleton" src/components/PrinterSettings.tsx` returns 0 (NOT exported per PD-08).
    - `grep -cE "<Skeleton" src/components/PrinterSettings.tsx` returns at least 7 (PrinterListSkeleton renders 7 Skeletons per row × 3 rows in iteration; assertion is "many Skeletons present").
    - `grep -cE "shouldShowEmptyState\\(printerInstances, isLoading\\)" src/components/PrinterSettings.tsx` returns exactly 1 (D-10 second-branch predicate).
    - `grep -cE "isLoading \\? \\(" src/components/PrinterSettings.tsx` returns at least 1 (D-10 first-branch).
    - `grep -cE "<PrinterListSkeleton" src/components/PrinterSettings.tsx` returns at least 1 (D-10 first-branch render).
    - `grep -cE 'title="No printers added yet"' src/components/PrinterSettings.tsx` returns 1 (Phase 8 EmptyState copy preserved verbatim).
    - `grep -cE "label: 'Add Printer'" src/components/PrinterSettings.tsx` returns 1 (Phase 8 CTA label preserved).
    - `grep -cE "setShowAddForm\\(true\\)" src/components/PrinterSettings.tsx` returns at least 2 (one from the top-right "+ Add Printer" button at line 80, plus one from the EmptyState CTA — both preserved).
    - `grep -cE "<h2[^>]*>My Printers</h2>" src/components/PrinterSettings.tsx` returns 1 (heading preserved).
    - `grep -cE "\\+ Add Printer" src/components/PrinterSettings.tsx` returns at least 1 (top-right "+ Add Printer" button preserved — Phase 8 PD-08 alignment).
    - `grep -cE "printerInstances\\.length === 0" src/components/PrinterSettings.tsx` returns 0 (the Phase 8 ternary's bare-length check is REPLACED by `shouldShowEmptyState(printerInstances, isLoading)` per D-10).
    - `grep -cE "printerInstances\\.map\\(instance =>" src/components/PrinterSettings.tsx` returns exactly 1 (the populated-state map body preserved — single occurrence inside the third ternary branch).
    - `grep -v '^#' src/components/PrinterSettings.tsx | grep -cE "<button[^>]*>|<input[^>]*>|<select[^>]*>|<textarea[^>]*>"` is unchanged from pre-Phase-9 baseline (D-11). Verified by `npm run lint:no-raw-html` exiting 0.
    - `grep -cE "setTimeout|setInterval" src/components/PrinterSettings.tsx | head -1` value is unchanged from pre-Phase-9 baseline (PD-12).
    - `npx tsc -b` exits 0.
    - `npm run lint:no-raw-html` exits 0.
    - `npm test` exits 0.
    - Single atomic commit with message `feat(09): wire PrinterListSkeleton with D-10 loading order`.
  </acceptance_criteria>
  <done>PrinterSettings accepts the new `isLoading` prop, defines `PrinterListSkeleton` as a top-level co-located function, and renders in canonical D-10 order: skeleton → EmptyState (predicate-gated) → real instance list. The panel wrapper, heading, top-right "+ Add Printer" button, and Add Printer form are preserved across all branches. Build chain green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual UAT — verify all three skeletons + transitions + App shell interactivity</name>
  <files>n/a — checkpoint task; no files modified</files>
  <read_first>
    - .planning/phases/09-skeleton-loading-states/09-VALIDATION.md (the per-behavior manual UAT criteria — 7 visual / timing checks; this task's `<how-to-verify>` mirrors the table in VALIDATION § Manual-Only Verifications)
    - .planning/phases/09-skeleton-loading-states/09-CONTEXT.md (D-01 / D-05 / D-08 / D-10 — the locked behaviors the UAT must confirm)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Pitfall 3 (skeleton flash after data loads — UAT must confirm cold-reload behavior, not warm)
    - .planning/phases/09-skeleton-loading-states/09-RESEARCH.md § Pitfall 5 (CostCalculator first-paint with empty data — UAT must confirm no crash, no console error)
    - .claude/CLAUDE.md (project rule — dev server runs on port 4173)
  </read_first>
  <what-built>
    Wave 2 complete: App.tsx no longer shows the global "Loading…" gate. AssetLibrary, JobsManager, and PrinterSettings each accept an `isLoading` prop drilled from App.tsx and render their own composed list skeleton (`AssetListSkeleton`, `JobsListSkeleton`, `PrinterListSkeleton`) during initial IndexedDB load. JobsManager converted to a single-return so the Sale Form Modal and Delete Confirmation Modal stay mounted across all branches. NO new NewBadge added (D-08).
  </what-built>
  <how-to-verify>
    Start the dev server on the project's pinned port: `npm run dev` (port 4173 per `.claude/CLAUDE.md`).

    STEP A — AssetLibrary skeleton appears during cold load (UI-05 D-01):
    1. Open DevTools → Application → Storage → IndexedDB → `3DCosterDB` → Clear.
    2. Cold-reload the page (Ctrl+Shift+R or Cmd+Shift+R).
    3. Switch to the MATERIALS tab.
    4. EXPECTED: an `AssetListSkeleton` renders briefly — on mobile width (< md) you see 3 placeholder cards with pulsing slate-700 bars; on desktop width (≥ md) you see a 5-row table-shaped skeleton with header + body rows. The skeleton uses Tailwind `animate-pulse` (visible as pulsing opacity at ~2 Hz).
    5. EXPECTED: the skeleton is replaced by real materials (the auto-seeded materials from `useAssets`) after ~200-500 ms.
    6. If you never see the skeleton on cold reload → FAIL — the loading flag may be flipping `true` → `false` within a single frame on your machine. Try DevTools → Performance → CPU 4× slowdown and reload again. The skeleton MUST be observable under throttling per RESEARCH Pitfall 3.

    STEP B — JobsManager skeleton appears during cold load (UI-05 D-01):
    1. DevTools → Application → IndexedDB → `3DCosterDB` → Clear.
    2. Cold-reload.
    3. Switch to the JOBS tab.
    4. EXPECTED: `JobsListSkeleton` renders briefly — 4 placeholder job rows in `bg-slate-700/50 border border-slate-600` cards with pulsing bars for name + status badge + filament-summary + revenue figure.
    5. EXPECTED: the skeleton transitions to the Phase 8 EmptyState (no jobs default) — "No jobs saved yet" + ClipboardList icon + "Open Calculator" CTA. The transition is from skeleton → EmptyState; NO white flash, NO jitter.
    6. Click "Open Calculator" — EXPECTED: tab switches to Calculator (Phase 8 wiring preserved).

    STEP C — PrinterSettings skeleton appears during cold load (UI-05 D-01):
    1. DevTools → IndexedDB → Clear. Cold-reload.
    2. Switch to the SETTINGS tab (Printer Settings).
    3. EXPECTED: above the printers panel, the always-visible "+ Add Printer" top-right button is visible (does NOT pulse). Below the heading and Add Printer form area, `PrinterListSkeleton` renders briefly — 3 placeholder printer rows with pulsing bars for nickname + config-name + metadata row + progress bar + caption + Edit/Delete button placeholders.
    4. EXPECTED: the skeleton transitions to the Phase 8 EmptyState (no instances default) — "No printers added yet" + Printer icon + "Add Printer" CTA.
    5. Click the empty-state "Add Printer" CTA. EXPECTED: the inline Add Printer form opens.

    STEP D — No flicker after load (UI-05 D-05):
    1. Cold-reload each of the three list tabs after IDB clear. Observe each list transition.
    2. EXPECTED: skeleton smoothly replaced by real content or EmptyState. Acceptable: single-frame opacity dip if the animation isn't done playing.
    3. NOT acceptable: white flash, jitter, skeleton persisting AFTER real data has landed, skeleton flickering back and forth, or EmptyState appearing momentarily BEFORE the skeleton on a cold load.

    STEP E — App shell + Calculator interactive during load (UI-05 D-01):
    1. DevTools → Performance → CPU 4× slowdown enabled. Cold-reload.
    2. EXPECTED: the header (logo, page title, gear icon, profile icon) renders immediately — not blocked by any global "Loading…" screen.
    3. EXPECTED: the tab bar renders immediately and is clickable. The default-active CALCULATOR tab is visible.
    4. While skeletons are rendering on the list tabs, click the header gear icon (settings modal opens), navigate via tab buttons, type into Calculator inputs. All should be responsive.
    5. EXPECTED: Calculator dropdowns (Material, Printer) may be momentarily empty (CONTEXT.md D-07 + RESEARCH Pitfall 5) — but the dropdowns themselves render and accept clicks. After ~200-500 ms the dropdowns populate.
    6. No console errors should appear in DevTools → Console during the cold-load window (RESEARCH Pitfall 5 verification — empty arrays must not crash).

    STEP F — Empty-state-vs-skeleton ordering (UI-05 D-10):
    1. With ZERO jobs in DB (default state after IDB clear), cold-reload.
    2. Switch to JOBS tab. EXPECTED ordering: skeleton appears first → transitions to EmptyState. NOT acceptable: EmptyState appearing during loading window (D-10 violation; `shouldShowEmptyState` predicate must keep EmptyState suppressed while `isLoading === true`).

    STEP G — No NEW badge appears (UI-05 D-08):
    1. After all skeleton tests, inspect the tab bar (Phase 8's `empty-states` NewBadge may still be visible — that's Phase 8 wiring, NOT a Phase 9 badge).
    2. EXPECTED: NO additional badge specific to `skeleton-loading` on any tab heading or anywhere else in the UI.
    3. In a separate terminal: `grep -c "skeleton-loading" src/features.ts` returns 0 (negative check — no feature key registered per D-08).
    4. EXPECTED: `grep -c 'feature="skeleton-loading"'` across `src/` returns 0.

    STEP H — Build chain green:
    1. In a separate terminal: `npm run build`.
    2. EXPECTED: exits 0 (lint guard + tsc -b + vite build all pass).
    3. EXPECTED: `npm test` exits 0 (Plan 01's 7 Skeleton tests + Phase 8 EmptyState tests + threeMfParser tests all green).
  </how-to-verify>
  <action>
    No code change in this task. Execute the eight-step UAT script from `<how-to-verify>` above and observe the system against the EXPECTED outcomes. Report any deviation in `<resume-signal>`.
  </action>
  <verify>
    <human-check>All eight UAT steps (A–H) report EXPECTED behavior. STEP H's `npm run build` AND `npm test` must both exit 0. Any deviation requires a fix-and-retry loop before approval.</human-check>
  </verify>
  <acceptance_criteria>
    - STEP A: AssetLibrary skeleton (mobile cards on `md:hidden`; desktop table on `hidden md:block`) appears on cold-reload of Materials tab and transitions to real materials within ~500 ms.
    - STEP B: JobsManager skeleton (4 placeholder job rows) appears on cold-reload of Jobs tab and transitions to the Phase 8 EmptyState (no jobs default); clicking "Open Calculator" switches the tab.
    - STEP C: PrinterSettings skeleton (3 placeholder printer rows) appears on cold-reload of Settings tab and transitions to the Phase 8 EmptyState (no instances default); top-right "+ Add Printer" button stays visible above and is clickable.
    - STEP D: No flicker / white flash / jitter / persisting skeleton after load on any of the three tabs.
    - STEP E: Header + tab bar + Calculator render immediately on cold reload (no global "Loading…" screen). Under CPU 4× throttling, all are interactive while skeletons render. NO console errors during the cold-load window (RESEARCH Pitfall 5).
    - STEP F: Skeleton → EmptyState ordering correct — EmptyState NEVER appears during the loading window (D-10 + RESEARCH Pitfall 6).
    - STEP G: NO `skeleton-loading` feature key in `src/features.ts`; NO new NewBadge added (D-08). `grep -c "skeleton-loading" src/features.ts` returns 0.
    - STEP H: `npm run build` exits 0 AND `npm test` exits 0 (all 3 test suites pass).
  </acceptance_criteria>
  <done>Developer types "approved" after verifying all eight UAT steps pass. Phase 9 user-visible UX is confirmed working end-to-end against the locked D-01 through D-11 decisions. UI-05 is satisfied.</done>
  <resume-signal>Type "approved" if all eight steps pass; otherwise describe which step failed and what was observed (e.g., "Step A: skeleton renders only mobile cards on desktop width" or "Step E: console error on cold reload — `Cannot read property 'find' of undefined` from CostCalculator").</resume-signal>
</task>

</tasks>

<verification>
After Task 5 is approved, the full Phase 9 verification stack:

1. `npm test` — vitest exits 0 (Plan 01's Skeleton tests + Phase 8 EmptyState tests + threeMfParser tests all pass).
2. `npx tsc -b` — strict TypeScript exits 0 (`noUnusedLocals` confirms App.tsx prune complete; consumer Props interfaces all carry the new `isLoading` field).
3. `npm run lint:no-raw-html` — exits 0 (no raw `<button>`/`<input>`/`<select>`/`<textarea>` introduced in any composed list skeleton or modified consumer).
4. `npm run build` — full build chain green.
5. `grep -c "Loading\\.\\.\\." src/App.tsx` returns 0 (D-09 — App.tsx no longer contains the "Loading..." literal).
6. `grep -c "if (isLoading)" src/App.tsx` returns 0 (D-01 — global gate removed).
7. `grep -c "skeleton-loading" src/features.ts` returns 0 (D-08 — no feature key registered).
8. Manual UAT (Task 5) — all eight steps confirmed by the developer.
</verification>

<success_criteria>
- App.tsx no longer contains a "Loading..." literal or a global `if (isLoading)` block — D-01 + D-09 honored.
- App.tsx prunes 5 unused `isLoading` destructurings (settings/printers/profile/shipping/fees) so `tsc -b` `noUnusedLocals` passes — PD-07.
- App.tsx drills 3 `isLoading` props (assets/jobs/instances) into the three consumers at their existing render sites — PD-13.
- AssetLibrary, JobsManager, PrinterSettings each accept a new `isLoading: boolean` prop and define their composed list skeleton co-located in the same file — D-03 + PD-08.
- AssetListSkeleton renders both mobile-card and desktop-table variants switched by Tailwind responsive classes — PD-10 + RESEARCH Pitfall 10.
- JobsManager converts from early-return to single-return so Sale Form + Delete Confirmation modals stay mounted across all branches — PD-09 + RESEARCH Pitfall 1.
- All three consumers render in canonical D-10 order: `isLoading ? <Skeleton/> : shouldShowEmptyState(items, isLoading) ? <EmptyState/> : <RealList/>` (Skeleton FIRST, EmptyState SECOND, list THIRD).
- All three consumers import `shouldShowEmptyState` from `./ui` (re-exported by Plan 01 Task 3) — activates the predicate that 08-REVIEW IN-01 flagged as dead code.
- NO debounce / setTimeout / fade-in added anywhere — skeletons render immediately, disappear immediately (PD-12 + D-05).
- NO `skeleton-loading` feature key in `src/features.ts`; NO new NewBadge added (D-08).
- Pre-existing `_getPrinterName` dead code in JobsManager.tsx:186-193 preserved verbatim (PD-11 + RESEARCH Pitfall 2).
- Filter-no-result fallbacks in AssetLibrary preserved (Phase 8 D-07 alignment).
- CostCalculator unmodified (D-07 — empty-dropdown flash accepted).
- Lint guard + tsc -b + vitest + vite build all exit 0.
- Manual UAT (Task 5) all eight steps pass: skeleton appears on cold reload for each of the three lists, transitions cleanly to real content or EmptyState, App shell + Calculator interactive during load, no console errors, no `skeleton-loading` badge introduced.
- Four atomic commits (one per task) — Task 1 (App.tsx), Task 2 (AssetLibrary), Task 3 (JobsManager), Task 4 (PrinterSettings); Task 5 is the manual approval checkpoint with NO commit.
</success_criteria>

<output>
Create `.planning/phases/09-skeleton-loading-states/09-02-SUMMARY.md` when done. Include:
- Final list of files modified with line counts and diff highlights (App.tsx prune + 3 props drilled; AssetLibrary + JobsListSkeleton + JobsManager single-return + PrinterListSkeleton co-locations)
- Confirmation that PD-07/08/09/10/11/12/13 were honored
- UAT result (each of Steps A–H passed or failed)
- Confirmation that all 4 atomic commits landed in order (App.tsx refactor first, then 3 consumer wirings)
- Note for next phase: the activation of `shouldShowEmptyState` resolves 08-REVIEW IN-01 — the predicate is no longer dead code
- Any tech debt observed during execution (e.g., the `_getPrinterName` dead code is logged for a future focused cleanup phase, but NOT addressed here per PD-11)
- Confirmation that CostCalculator empty-dropdown flash was observed during STEP E but caused no crash (D-07 + RESEARCH Pitfall 5 confirmed acceptable)
</output>
</content>
</invoke>