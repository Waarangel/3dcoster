---
phase: 10-cost-calculation-unit-tests
plan: 04
type: execute
wave: 3
depends_on:
  - "10-02"
  - "10-03"
files_modified:
  - vitest.config.ts
  - package.json
autonomous: true
requirements:
  - TEST-02
tags:
  - testing
  - ci-gating
  - vitest-coverage

must_haves:
  truths:
    - "vitest.config.ts contains a coverage block with provider 'v8' scoped to src/utils/costCalc.ts only"
    - "Coverage thresholds are lines:95, functions:100, branches:90 (D-09)"
    - "package.json scripts.build chain is exactly: node scripts/lint-no-raw-html.mjs && vitest run --coverage && tsc -b && vite build (D-05, D-06 order)"
    - "package.json scripts has a new test:watch entry mapped to 'vitest' (D-07)"
    - "package.json scripts.test stays 'vitest run' (one-shot, unchanged per D-07)"
    - "npm run build exits 0 — tests pass, thresholds met, tsc clean, vite build succeeds"
    - "Threshold gate is real: if a costCalc.ts function loses its test, the build fails with a coverage violation"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with coverage block scoped to src/utils/costCalc.ts"
      contains: "coverage:"
    - path: "package.json"
      provides: "Build chain that gates Vercel deploys on test pass + coverage threshold"
      contains: "vitest run --coverage"
  key_links:
    - from: "package.json scripts.build"
      to: "vitest.config.ts"
      via: "vitest run --coverage invocation in the build chain"
      pattern: "vitest run --coverage"
    - from: "vitest.config.ts"
      to: "src/utils/costCalc.ts"
      via: "coverage.include scoping array"
      pattern: "src/utils/costCalc\\.ts"
---

<objective>
Wire the testing gate into the build chain. Extend `vitest.config.ts` with a scoped coverage block (file-level threshold on `src/utils/costCalc.ts` only — D-08), update `package.json` `scripts.build` to run `vitest run --coverage` before `tsc -b` (D-05, D-06 ordering), and add a `test:watch` script for local dev (D-07). After this plan, every Vercel deploy is gated on the test suite passing AND the coverage thresholds being met.

Purpose: Without this plan, the tests Plan 03 produced exist but nothing prevents them from rotting. Wiring the gate into `npm run build` (the Vercel deploy command) is the single change that satisfies ROADMAP success criterion 3 and REQUIREMENT TEST-02.

Output: Modified `vitest.config.ts` and `package.json` such that `npm run build` runs the four-step chain `lint-html → vitest run --coverage → tsc -b → vite build` and fails on any step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cost-calculation-unit-tests/10-CONTEXT.md
@.planning/phases/10-cost-calculation-unit-tests/10-PATTERNS.md
@.planning/phases/10-cost-calculation-unit-tests/10-01-SUMMARY.md
@.planning/phases/10-cost-calculation-unit-tests/10-02-SUMMARY.md
@.planning/phases/10-cost-calculation-unit-tests/10-03-SUMMARY.md

<interfaces>
<!-- Current vitest.config.ts (full file — 8 lines, will be extended) -->
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'jsdom',
    include: ['src/**/*.test.ts'],
  },
})
```

<!-- Current package.json scripts block (will be extended) -->
```json
"scripts": {
  "dev": "vite",
  "build": "node scripts/lint-no-raw-html.mjs && tsc -b && vite build",
  "lint": "eslint .",
  "lint:no-raw-html": "node scripts/lint-no-raw-html.mjs",
  "preview": "vite preview",
  "prepare": "chmod +x .git/hooks/pre-commit 2>/dev/null || true",
  "tauri": "tauri",
  "tauri:dev": "tauri dev",
  "tauri:build": "tauri build",
  "test": "vitest run"
}
```

<!-- @vitest/coverage-v8 ^4.1.4 is ALREADY in devDependencies (package.json:40). No install needed. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend vitest.config.ts with file-scoped v8 coverage block</name>
  <files>vitest.config.ts</files>
  <read_first>
    - vitest.config.ts (current 8-line file — confirm the test object shape before extending)
    - .planning/phases/10-cost-calculation-unit-tests/10-PATTERNS.md (section "vitest.config.ts" — has the exact extension pattern)
    - .planning/phases/10-cost-calculation-unit-tests/10-CONTEXT.md (D-08 coverage.include scoping; D-09 threshold numbers; D-10 runs in every build)
    - package.json (confirm @vitest/coverage-v8 ^4.1.4 is in devDependencies — should be at line 40)
  </read_first>
  <behavior>
    - `vitest.config.ts` adds a `coverage` key inside the `test` object
    - `coverage.provider` is `'v8'` (matches the installed `@vitest/coverage-v8` package)
    - `coverage.include` is `['src/utils/costCalc.ts']` — file-level scoping so the report measures only this file (no project-wide threshold per D-08)
    - `coverage.thresholds` is `{ lines: 95, functions: 100, branches: 90 }` per D-09
    - Existing config keys (`environment: 'jsdom'`, `include: ['src/**/*.test.ts']`) are preserved unchanged
    - `npx vitest run --coverage` reports coverage and exits 0 when thresholds are met
    - `npx tsc -b` continues to pass (no new TS error in the config file)
  </behavior>
  <action>
    Edit `vitest.config.ts`. Inside the `test: { ... }` object, after the existing `include: ['src/**/*.test.ts'],` line, add a new property:
    ```
    coverage: {
      provider: 'v8',
      include: ['src/utils/costCalc.ts'],
      thresholds: {
        lines: 95,
        functions: 100,
        branches: 90,
      },
    },
    ```
    Do NOT touch the `environment` or `include` properties — they stay exactly as they are. Do NOT add a project-wide coverage.include (the array MUST contain only `'src/utils/costCalc.ts'` per D-08). Do NOT add `reporter` or `reportsDirectory` options — defaults are sufficient. The resulting file is ~17 lines.
  </action>
  <verify>
    <automated>npx vitest run --coverage 2>&1 | tee /tmp/plan10-04-task1-cov.log | tail -25; grep -cE "All files +\\|" /tmp/plan10-04-task1-cov.log | grep -vE "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "provider: 'v8'" vitest.config.ts` returns 1
    - `grep -c "src/utils/costCalc.ts" vitest.config.ts` returns 1 (coverage.include scoping)
    - `grep -c "lines: 95" vitest.config.ts` returns 1
    - `grep -c "functions: 100" vitest.config.ts` returns 1
    - `grep -c "branches: 90" vitest.config.ts` returns 1
    - `grep -c "environment: 'jsdom'" vitest.config.ts` returns 1 (preserved)
    - `grep -c "include: \\['src/\\*\\*/\\*.test.ts'\\]" vitest.config.ts` returns 1 (preserved)
    - `npx vitest run --coverage` exits 0 and prints a coverage table that includes the `src/utils/costCalc.ts` row
    - The coverage table does NOT include `src/utils/threeMfParser.ts` or any other file (scoping confirmed)
  </acceptance_criteria>
  <done>vitest.config.ts has a working coverage block scoped to costCalc.ts. Running `npx vitest run --coverage` reports the costCalc.ts coverage numbers and exits 0 because Plan 03's tests meet the thresholds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update package.json — add test:watch and reorder build chain to gate Vercel deploys on tests + coverage</name>
  <files>package.json</files>
  <read_first>
    - package.json (current scripts block — confirm the existing build chain shape on line 8)
    - .planning/phases/10-cost-calculation-unit-tests/10-PATTERNS.md (section "package.json scripts" — has the exact resulting scripts block shape)
    - .planning/phases/10-cost-calculation-unit-tests/10-CONTEXT.md (D-05 new build script literal; D-06 deterministic ordering lint-html → vitest+coverage → tsc → vite; D-07 test:watch addition; D-10 coverage runs in every build)
    - vitest.config.ts (just-edited config from Task 1 — confirm the coverage block exists so the new build chain has something to run against)
  </read_first>
  <behavior>
    - `scripts.build` is changed from `node scripts/lint-no-raw-html.mjs && tsc -b && vite build` to `node scripts/lint-no-raw-html.mjs && vitest run --coverage && tsc -b && vite build`
    - Order is deterministic per D-06: lint-html → vitest+coverage → tsc → vite
    - `scripts.test` remains exactly `vitest run` (D-07 — one-shot, unchanged, this is what the build chain invokes)
    - `scripts.test:watch` is added with value `vitest` (D-07 — interactive local dev, no `--coverage` because watch mode does not gate)
    - `scripts.test:watch` is inserted immediately after `scripts.test` to keep test-related scripts grouped
    - All other scripts (`dev`, `lint`, `lint:no-raw-html`, `preview`, `prepare`, `tauri`, `tauri:dev`, `tauri:build`) are unchanged
    - `npm run build` exits 0 (full chain green)
    - `npm test` continues to exit 0 (one-shot, no coverage flag — same as before)
    - The JSON in package.json remains valid (no trailing commas, no syntax errors)
  </behavior>
  <action>
    Edit `package.json`:
    1. Change `scripts.build` value from `"node scripts/lint-no-raw-html.mjs && tsc -b && vite build"` to `"node scripts/lint-no-raw-html.mjs && vitest run --coverage && tsc -b && vite build"`. The order is the deterministic order from D-06 — do not reorder.
    2. Leave `scripts.test` exactly as `"vitest run"` — no change.
    3. Add a new property `"test:watch": "vitest"` immediately after the `"test": "vitest run"` line. Use a comma between them. The resulting scripts block ends with `"test": "vitest run", "test:watch": "vitest"` (last entry, no trailing comma after test:watch).
    4. Do NOT modify any other script. Do NOT modify dependencies or devDependencies.
    5. Preserve existing JSON formatting (two-space indent, no trailing whitespace).
    Resulting scripts block shape (verify by visual diff): see PATTERNS.md "package.json scripts" → "Resulting scripts block shape".
  </action>
  <verify>
    <automated>npm run build 2>&1 | tee /tmp/plan10-04-task2-build.log | tail -15; grep -cE "(error|failed)" /tmp/plan10-04-task2-build.log | grep -E "^0$" && grep -c "vitest run --coverage" package.json | grep -E "^1$" && grep -c '"test:watch"' package.json | grep -E "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'vitest run --coverage' package.json` returns 1 (build chain has the coverage invocation)
    - `grep -c '"test:watch": "vitest"' package.json` returns 1 (new script added with exact value)
    - `grep -c '"test": "vitest run"' package.json` returns 1 (existing one-shot script unchanged)
    - `node -e "JSON.parse(require('fs').readFileSync('package.json'))"` exits 0 (JSON validity sanity check)
    - The full build script value is exactly `"node scripts/lint-no-raw-html.mjs && vitest run --coverage && tsc -b && vite build"` — order matters per D-06, verify with: `node -e "const p=require('./package.json'); if(p.scripts.build !== 'node scripts/lint-no-raw-html.mjs && vitest run --coverage && tsc -b && vite build') process.exit(1)"` exits 0
    - `npm run build` exits 0 (the new chain succeeds end-to-end: lint passes, all tests pass, coverage thresholds met, tsc clean, vite build succeeds)
    - `npm test` exits 0 (one-shot test still works for local dev)
    - The vitest step output in the build log shows the costCalc.ts coverage row meets all three thresholds (lines >= 95, functions == 100, branches >= 90)
  </acceptance_criteria>
  <done>package.json scripts.build now gates the build on vitest passing AND the coverage thresholds. test:watch is available for local dev. npm run build exits 0 with the new four-step chain. Vercel deploys will now fail on any test or coverage-threshold violation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Build-script + config change — no runtime trust boundary altered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-N/A | N/A | vitest.config.ts + package.json scripts.build | accept | Build-tooling configuration change. The added `@vitest/coverage-v8` invocation runs the already-installed coverage provider (version ^4.1.4, already in devDependencies). No new package installed, no new attack surface introduced. Vercel build environment runs the same as before plus one additional command. |
| T-10-SC | Tampering | npm install chain | accept | No new packages added; `@vitest/coverage-v8 ^4.1.4` is pre-existing in devDependencies (verified at package.json:40 in PATTERNS.md). No `[ASSUMED]`/`[SUS]` legitimacy check needed because zero new packages enter the dependency tree. |

**Rationale:** Phase 10 ships ZERO new package installs. Coverage tooling (`@vitest/coverage-v8 ^4.1.4`) is pre-existing. The only changes are (a) a config block in vitest.config.ts referencing the existing package and (b) a script-chain reordering in package.json. The Package Legitimacy Gate from the planner contract is inapplicable because no `npm install` runs as part of executing this plan.
</threat_model>

<verification>
- `npm run build` exits 0 (full four-step chain green)
- `npm test` exits 0 (one-shot test still works)
- `npm run test:watch` is a valid script (smoke check: `node -e "if(!require('./package.json').scripts['test:watch']) process.exit(1)"` exits 0)
- The build chain ordering is exactly lint-html → vitest+coverage → tsc → vite per D-06 (verified by exact string match of `scripts.build` against the canonical value)
- Coverage report includes only `src/utils/costCalc.ts` (no other files in the table) — confirms file-scoped threshold per D-08
- The coverage thresholds (lines 95 / functions 100 / branches 90) are met — Plan 03's tests have sufficient coverage
- A failure-injection sanity check (optional, executor's discretion): temporarily comment out one test in costCalc.test.ts, run `npm run build`, confirm the build FAILS with a threshold violation, then restore the test. Confirms the gate is real.
</verification>

<success_criteria>
1. `vitest.config.ts` contains a `coverage` block with `provider: 'v8'`, `include: ['src/utils/costCalc.ts']`, and thresholds `{ lines: 95, functions: 100, branches: 90 }`
2. `package.json` `scripts.build` is exactly `"node scripts/lint-no-raw-html.mjs && vitest run --coverage && tsc -b && vite build"` — order matters per D-06
3. `package.json` `scripts.test` remains `"vitest run"` (unchanged)
4. `package.json` `scripts.test:watch` exists with value `"vitest"`
5. `npm run build` exits 0 — Vercel deploys will succeed when tests pass and thresholds are met
6. `npm run build` would FAIL if any test fails OR if costCalc.ts coverage drops below thresholds — the gate is real, not advisory
7. No new dependencies installed (zero diff to `dependencies` or `devDependencies` blocks)
</success_criteria>

<output>
Create `.planning/phases/10-cost-calculation-unit-tests/10-04-SUMMARY.md` when done. Include: the final `scripts.build` value (full string), the coverage block contents, the wall-clock duration of `npm run build` before and after (so future agents can see the test-step overhead), and the coverage table summary for `src/utils/costCalc.ts` (actual lines/functions/branches percentages achieved).
</output>
