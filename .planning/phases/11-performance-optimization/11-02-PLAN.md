---
phase: 11-performance-optimization
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - vite.config.ts
autonomous: true
requirements:
  - PERF-01
tags:
  - vite
  - manual-chunks
  - bundle-splitting

must_haves:
  truths:
    - "`vite.config.ts` defines `build.rollupOptions.output.manualChunks` per D-01"
    - "Running `npm run build` produces a `dist/assets/` directory containing AT LEAST 3 distinct vendor chunk filenames: `react-vendor-*.js`, `dexie-vendor-*.js`, `vendor-*.js`"
    - "`rollup-plugin-visualizer` is wired ONLY when `mode === 'analyze'`; default builds (Vercel deploy, local `npm run build`) load zero visualizer plugin code (D-10)"
    - "Route-level lazy-load chunks (LandingPage, FeaturesPage, DownloadPage, FAQPage) still emit as separate chunks — manualChunks does NOT swallow them (D-03)"
  artifacts:
    - path: "vite.config.ts"
      provides: "Vendor chunk splitting via function-style manualChunks; conditional visualizer plugin"
      contains: "manualChunks"
      contains_also: "react-vendor"
      contains_also2: "dexie-vendor"
  key_links:
    - from: "vite.config.ts manualChunks function"
      to: "dist/assets/*.js filenames after build"
      via: "Rollup output naming, observable via `ls dist/assets/`"
      pattern: "react-vendor-.*\\.js"
    - from: "vite.config.ts visualizer guard"
      to: "package.json scripts.analyze (Plan 01)"
      via: "Both keyed on `mode === 'analyze'` string match"
      pattern: "mode === 'analyze'"
---

<objective>
Add `build.rollupOptions.output.manualChunks` to `vite.config.ts` per D-01, splitting the production bundle into 3 named vendor chunks (`react-vendor`, `dexie-vendor`, `vendor`). Wire `rollup-plugin-visualizer` as a conditional plugin that loads only when `vite build --mode analyze` runs (D-10), so the regular Vercel deploy chain never pays the visualizer cost.

Purpose: Satisfies the chunk-splitting half of PERF-01. After this plan, `npm run build` produces vendor chunks that browsers can cache independently of the app code, and `npm run analyze` opens a treemap visualization on demand. Plan 03 then verifies the resulting main-chunk size against the 300 KB gzipped budget.

Output: Updated `vite.config.ts` with a `build` block sibling to `define` / `plugins` / `server`, and a functional `defineConfig` form that gates the visualizer plugin on the `mode` parameter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-performance-optimization/11-CONTEXT.md
@.planning/phases/11-performance-optimization/11-PATTERNS.md
@vite.config.ts
@vitest.config.ts
@package.json

<interfaces>
<!-- Current vite.config.ts shape and the analog (vitest.config.ts) for the nested-config pattern. -->

From vite.config.ts (current — 72 lines):
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  define: { __IS_TAURI__: JSON.stringify(!!process.env.TAURI_ENV_PLATFORM) },
  plugins: [
    react(),
    tailwindcss(),
    VitePWA({ /* ~50 lines of PWA config */ })
  ],
  server: { port: 4173 },
})
```
NO `build` block exists today.

From vitest.config.ts (Phase 10 analog — Pattern: defineConfig with nested config object):
```typescript
import { defineConfig } from 'vitest/config'
export default defineConfig({
  test: {
    environment: 'jsdom',
    include: ['src/**/*.test.ts'],
    coverage: {
      provider: 'v8',
      include: ['src/utils/costCalc.ts'],
      thresholds: { lines: 95, functions: 100, branches: 90 },
    },
  },
})
```
Mirror this nested-block insertion pattern: add a new top-level key (`build`) inside the existing `defineConfig({...})` call, sibling to `define`, `plugins`, `server`.

Verified facts:
- The 4 route-level lazy chunks (LandingPage, FeaturesPage, DownloadPage, FAQPage) are created by `React.lazy()` calls in `src/main.tsx` and similar — they are dynamic-import chunks. `manualChunks` only acts on **statically imported** modules from `node_modules`; dynamic-import chunks are a separate concern and must not be affected by this change (D-03).
- The visualizer plugin's default export is the named function `visualizer` (per rollup-plugin-visualizer docs); import as `import { visualizer } from 'rollup-plugin-visualizer'`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add manualChunks and conditional visualizer to vite.config.ts</name>
  <files>vite.config.ts</files>
  <read_first>
    - vite.config.ts (full file — 72 lines; identify the `defineConfig({...})` call shape and the `define` / `plugins` / `server` ordering)
    - vitest.config.ts (analog from Phase 10 — confirms the nested-block insertion pattern)
    - .planning/phases/11-performance-optimization/11-CONTEXT.md (D-01 specifies the 3 chunks and function-style; D-02 forbids further granular splitting; D-03 forbids touching route-level lazy splits; D-10 makes visualizer opt-in)
    - .planning/phases/11-performance-optimization/11-PATTERNS.md (`vite.config.ts` section — "New block shape (per D-01)" and the visualizer functional-form switch example)
  </read_first>
  <action>
    Rewrite `vite.config.ts` to:

    (1) Switch the `defineConfig` call from object-form to function-form: change `export default defineConfig({...})` to `export default defineConfig(({ mode }) => ({...}))`. This is the lowest-impact way to gain access to the `mode` parameter for the conditional visualizer plugin per D-10.

    (2) Add a new named import at the top: `import { visualizer } from 'rollup-plugin-visualizer'`. Keep the existing import order (vite, plugin-react, tailwindcss, VitePWA, then visualizer).

    (3) Inside the returned config object, modify the `plugins` array to append `...(mode === 'analyze' ? [visualizer({ open: true, gzipSize: true, brotliSize: true, filename: 'dist/stats.html' })] : [])` as the LAST entry. The spread-empty-array pattern keeps the array clean when not analyzing (zero plugin entries added). Keep the existing `react()`, `tailwindcss()`, `VitePWA({...})` entries unchanged and in the same order.

    (4) Insert a new top-level `build` key between `plugins` and `server` (preserves the "build-time concerns grouped" convention from PATTERNS.md). The block reads:
    ```
    build: {
      rollupOptions: {
        output: {
          manualChunks(id) {
            if (id.includes('node_modules')) {
              if (id.includes('/react/') || id.includes('/react-dom/')) return 'react-vendor';
              if (id.includes('/dexie/') || id.includes('/dexie-react-hooks/')) return 'dexie-vendor';
              return 'vendor';
            }
          },
        },
      },
    },
    ```
    The function is inline (D-01 says function-style; planner picks inline over hoisted — keeps the file self-contained, no helper to test in isolation). Order of the three `if` checks matters: `react-dom` matches before the fallback `vendor`, and `dexie-react-hooks` is intentionally captured by the `dexie-vendor` group (it has `/dexie-react-hooks/` in its path, NOT `/dexie/`, so the `id.includes('/dexie-react-hooks/')` check is what catches it). Verify both `react-dom` and `dexie-react-hooks` route correctly by reading the resulting build's chunk names.

    (5) Do NOT add a `chunkFileNames` or `assetFileNames` override — Vite's defaults (`[name]-[hash].js`) are correct and produce filenames like `react-vendor-AbCdEfGh.js` that the assert-bundle-size script can pattern-match in Plan 03.

    (6) Leave `server.port: 4173`, the `__IS_TAURI__` define, and the full VitePWA config untouched.

    Critical: the existing `define`/`plugins`/`server` keys MUST remain at the same nesting level (top-level keys of the config object). The new `build` key is a peer, NOT nested inside `plugins`.
  </action>
  <verify>
    <automated>npx tsc --noEmit vite.config.ts 2>&1 | tee /tmp/vite-ts.log; ! grep -q "error TS" /tmp/vite-ts.log</automated>
    <automated>grep -c "manualChunks" vite.config.ts | grep -q "^1$"</automated>
    <automated>grep -c "react-vendor" vite.config.ts | grep -q "^1$" && grep -c "dexie-vendor" vite.config.ts | grep -q "^1$"</automated>
    <automated>grep -c "mode === 'analyze'" vite.config.ts | grep -q "^1$"</automated>
    <automated>grep -c "from 'rollup-plugin-visualizer'" vite.config.ts | grep -q "^1$"</automated>
    <automated>npm run build 2>&1 | tail -40 | tee /tmp/build.log; ls dist/assets/react-vendor-*.js dist/assets/dexie-vendor-*.js dist/assets/vendor-*.js 1>/dev/null</automated>
    <automated>ls dist/assets/ | grep -E "^(LandingPage|FeaturesPage|DownloadPage|FAQPage)-[A-Za-z0-9_-]+\.js$" | wc -l | awk '{ if ($1 < 4) { print "FAIL: route lazy chunks missing"; exit 1 } else { print "OK: route lazy chunks preserved (" $1 ")" } }'</automated>
    <automated>! ls dist/stats.html 2>/dev/null; echo "OK: no visualizer artifact from default build (D-10 satisfied)"</automated>
  </verify>
  <done>
    `vite.config.ts` contains the `build.rollupOptions.output.manualChunks` function per D-01 and the conditional visualizer plugin per D-10. `npm run build` produces `dist/assets/react-vendor-*.js`, `dist/assets/dexie-vendor-*.js`, and `dist/assets/vendor-*.js`. The 4 route-level lazy chunks remain. No `dist/stats.html` exists after a default build. `npm run analyze` (Plan 01's wired script) opens the visualizer.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-build → production bundle | The visualizer plugin must never end up in the shipped chunks |
| chunk boundaries → browser cache | Splitting wrong (e.g., putting `react` into `vendor`) silently degrades cache-hit rate without breaking anything visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-LK | Information Disclosure | rollup-plugin-visualizer leaking into production | mitigate | Functional `defineConfig` with `mode === 'analyze'` guard around the plugin spread. Build verification step confirms `dist/stats.html` does NOT exist after `npm run build`. |
| T-11-CACHE | Tampering (silent perf regression) | manualChunks routing | mitigate | Three positive `ls` assertions verify all three chunks emit. Future regressions get caught by Plan 03's bundle-size gate (the main chunk's gzipped size will jump if `react`/`dexie` accidentally bundle into it). |
| T-11-LAZY | Tampering (silent perf regression) | Route-level lazy chunks | mitigate | `ls dist/assets/` regex confirms ≥4 page chunks still emit per D-03. |
</threat_model>

<verification>
1. `npm run build` succeeds (existing `lint:no-raw-html → vitest → tsc -b → vite build` chain unchanged; Plan 03 will append the size gate later).
2. `ls dist/assets/` shows all three vendor chunks plus the 4 page chunks plus the main `index-*.js`.
3. `npm run analyze` opens a treemap (visualizer behavior) and writes `dist/stats.html`.
4. `tsc -b` passes — confirms the function-form `defineConfig(({ mode }) => ({...}))` typechecks against Vite 7's exported types.
</verification>

<success_criteria>
- `vite.config.ts` exports a functional `defineConfig` with `mode` destructured
- The plugins array conditionally includes `visualizer(...)` only when `mode === 'analyze'`
- `build.rollupOptions.output.manualChunks` returns `'react-vendor'`, `'dexie-vendor'`, or `'vendor'` for any `node_modules` id
- A default `npm run build` produces ≥3 vendor chunks and ≥4 page lazy chunks; no `stats.html`
- A `npm run analyze` build produces the same chunks PLUS `dist/stats.html`
- `tsc -b` reports no new errors
</success_criteria>

<output>
Create `.planning/phases/11-performance-optimization/11-02-SUMMARY.md` when done. Record:
- Final `vite.config.ts` line count (sanity check that the file didn't bloat unexpectedly)
- Exact filenames of the three vendor chunks from one build (with byte sizes; gives Plan 03 a baseline)
- Confirmation that LandingPage / FeaturesPage / DownloadPage / FAQPage chunks still emit
- One sentence on the visualizer treemap output (which lib was the biggest contributor to `vendor-*.js`)
</output>
