---
phase: 12-schema-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/backfill.ts
  - src/db/backfill.test.ts
autonomous: true
requirements:
  - SCHEMA-01
tags:
  - schema
  - dexie-v6
  - testing

must_haves:
  truths:
    - "The `backfillTagsOnJob(job)` pure helper exists and is exported from `src/db/backfill.ts`"
    - "Calling `backfillTagsOnJob({})` sets `job.tags` to `[]`"
    - "Calling `backfillTagsOnJob({ tags: ['a', 'b'] })` preserves the existing array unchanged"
    - "Calling `backfillTagsOnJob({ tags: 'oops' })` (non-array corruption) replaces it with `[]`"
    - "Calling `backfillTagsOnJob({ tags: null })` replaces it with `[]`"
    - "`npx vitest run src/db/backfill.test.ts` passes"
    - "The test file does NOT import from `src/db/database.ts` (avoids jsdom IndexedDB-init crash — RESEARCH Pitfall 4)"
  artifacts:
    - path: "src/db/backfill.ts"
      provides: "Pure `backfillTagsOnJob(job)` helper used by the v6 Dexie upgrade callback"
      contains: "export function backfillTagsOnJob"
    - path: "src/db/backfill.test.ts"
      provides: "Vitest unit coverage for the backfill helper (jsdom-safe — no Dexie import)"
      contains: "describe('backfillTagsOnJob'"
  key_links:
    - from: "src/db/backfill.test.ts"
      to: "src/db/backfill.ts"
      via: "import { backfillTagsOnJob } from './backfill'"
      pattern: "from\\s+'\\.\\/backfill'"
---

<objective>
Extract the v6 Dexie upgrade's row-mutation body into a pure exported function (`backfillTagsOnJob`) and unit-test it. This converts SCHEMA-01 (b) — "v6 upgrade backfills `tags = []` on existing jobs" — from a manual-UAT-only check into an automated Vitest assertion, sidestepping the jsdom/IndexedDB constraint (RESEARCH Pitfall 3 — jsdom does NOT implement IndexedDB, so a full Dexie migration test cannot run).

Purpose: Maximum automated coverage of the migration logic. The helper is consumed by Plan 03 (the v6 upgrade block imports `backfillTagsOnJob` and passes it as the `modify()` callback), so this plan must complete before Plan 03 (Wave 2). Implements SCHEMA-01 (migration-body half).
Output: Two new sibling files in `src/db/` — the helper and its test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-schema-foundation/12-CONTEXT.md
@.planning/phases/12-schema-foundation/12-RESEARCH.md
@.planning/phases/12-schema-foundation/12-PATTERNS.md
@.planning/phases/12-schema-foundation/12-VALIDATION.md
@src/components/ui/EmptyState.test.ts
@vitest.config.ts

<interfaces>
<!-- The test scaffolding pattern from EmptyState.test.ts — copy this shape. -->

From src/components/ui/EmptyState.test.ts (the closest-size analog per PATTERNS.md):
```typescript
import { describe, it, expect } from 'vitest';
import { /* exported symbol */ } from './module-under-test';

describe('functionName', () => {
  it('does X when Y', () => { /* arrange / act / expect */ });
});
```

Vitest config (from vitest.config.ts):
- environment: 'jsdom'
- include: ['src/**/*.test.ts']
- `src/db/backfill.test.ts` is automatically picked up by the existing glob — no config changes needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/db/backfill.test.ts (RED — failing tests first)</name>
  <files>src/db/backfill.test.ts</files>
  <read_first>
    - src/components/ui/EmptyState.test.ts (full file — analog for test scaffolding shape; one describe block, individual it() blocks per behavior)
    - .planning/phases/12-schema-foundation/12-PATTERNS.md (§ `src/db/__tests__/backfillTags.test.ts` — has the exact test cases plus the file-location-convention rule that tests sit BESIDE source, NOT in a `__tests__/` folder; that override against the original CONTEXT prompt path is why this plan writes to `src/db/backfill.test.ts`)
    - .planning/phases/12-schema-foundation/12-RESEARCH.md (Pitfall 3 — jsdom has no IndexedDB; Pitfall 4 — never `import` from `src/db/database.ts` in a Vitest test because the module's `new Dexie(...)` top-level side effect throws under jsdom)
    - vitest.config.ts (jsdom env + `src/**/*.test.ts` glob)
  </read_first>
  <behavior>
    - Test 1: `backfillTagsOnJob({ id: '1' })` mutates the object so `job.tags === []` (the missing-field case — most common path for existing v5 records).
    - Test 2: `backfillTagsOnJob({ id: '1', tags: ['a', 'b'] })` leaves `job.tags` equal to `['a', 'b']` (the already-array case — idempotency / re-run safety).
    - Test 3: `backfillTagsOnJob({ id: '1', tags: 'oops' })` overwrites the string with `[]` (the manually-edited-IndexedDB corruption case — RESEARCH Pitfall 5).
    - Test 4: `backfillTagsOnJob({ id: '1', tags: null })` overwrites null with `[]` (defensive against null-injected rows).
    - Test 5: `backfillTagsOnJob({ id: '1', tags: 42 })` overwrites the number with `[]` (broad `Array.isArray` guard coverage).
  </behavior>
  <action>
    Create `src/db/backfill.test.ts` (SIBLING of source — see PATTERNS.md note that the repo has zero `__tests__/` folders, and all 4 existing test files use the sibling convention). The file:

    1. Imports `describe`, `it`, `expect` from `vitest`.
    2. Imports `backfillTagsOnJob` from `./backfill` (relative path — sibling).
    3. Does NOT import anything from `./database` or `dexie` — those side-effect-construct `new Dexie(...)` which throws under jsdom (RESEARCH Pitfall 4).
    4. Has one `describe('backfillTagsOnJob', () => { ... })` block.
    5. Inside the block, has 5 `it(...)` cases per the `<behavior>` list above. Each case constructs a plain object literal typed as `Record<string, unknown>`, calls `backfillTagsOnJob(job)`, then asserts `expect(job.tags).toEqual([...])` (or the preserved array).

    Run `npx vitest run src/db/backfill.test.ts` AFTER writing the test but BEFORE Task 2 — the test MUST fail with a module-not-found error (`Cannot find module './backfill'`) because `src/db/backfill.ts` doesn't exist yet. This is the RED phase: confirm the test would catch a missing implementation.

    Commit message format: `test(12-02): add failing tests for backfillTagsOnJob`
  </action>
  <verify>
    <automated>npx vitest run src/db/backfill.test.ts 2>&1 | grep -qE "Cannot find module|Failed to resolve|backfill"</automated>
  </verify>
  <acceptance_criteria>
    - The file `src/db/backfill.test.ts` exists at the SIBLING path (NOT inside `src/db/__tests__/`).
    - `find ./src/db -name "__tests__" -type d` returns no results (sibling convention preserved).
    - `grep -E "from\\s+'vitest'" src/db/backfill.test.ts` matches.
    - `grep -E "from\\s+'\\.\\/backfill'" src/db/backfill.test.ts` matches.
    - `grep -cE "from\\s+'\\.\\/database'|from\\s+'dexie'" src/db/backfill.test.ts` returns 0 (no Dexie imports — jsdom safety per RESEARCH Pitfall 4).
    - `grep -cE "^\\s*it\\(" src/db/backfill.test.ts` returns 5 (the 5 behavior cases).
    - `npx vitest run src/db/backfill.test.ts` exits non-zero with a module-resolution error referencing `backfill` (RED phase — implementation does not exist yet).
  </acceptance_criteria>
  <done>
    Test file exists in the sibling location with 5 `it()` cases. The test run fails with a module-resolution error pointing at `./backfill` — confirming the test is wired correctly but the helper is not yet implemented.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/db/backfill.ts (GREEN — implement backfillTagsOnJob so tests pass)</name>
  <files>src/db/backfill.ts</files>
  <read_first>
    - src/db/backfill.test.ts (the test file just created — confirms the expected signature `backfillTagsOnJob(job: Record<string, unknown>): void`)
    - .planning/phases/12-schema-foundation/12-PATTERNS.md (§ `src/db/backfill.ts` — has the exact implementation including the docstring example block per the user-global "Tool Usage Examples" CLAUDE.md rule)
    - .planning/phases/12-schema-foundation/12-RESEARCH.md (Pitfall 5 — `Array.isArray` guard, NOT `??=`, because manually-edited IndexedDB can produce `tags: 'string'` / `tags: 42` / `tags: null` and `??=` would only catch null/undefined)
    - .planning/phases/12-schema-foundation/12-CONTEXT.md (D-02 — only `tags` is backfilled; D-08 — Array.isArray defensive guard is the chosen form)
  </read_first>
  <action>
    Create `src/db/backfill.ts` exporting a single pure function: `export function backfillTagsOnJob(job: Record<string, unknown>): void`. The body is ONE statement: `if (!Array.isArray(job.tags)) job.tags = [];`.

    Include a JSDoc block above the function with:
    - One-line summary: "Backfill `tags = []` on a job record that came from a pre-v6 schema."
    - Detailed description explaining (1) usage from the v6 Dexie upgrade callback in `database.ts` Plan 03, (2) why this is a pure exported function rather than an inline arrow — jsdom doesn't support IndexedDB so the inline form can't be unit-tested (RESEARCH Pitfall 3), (3) why `Array.isArray` instead of `??=` — defends against manually-edited IndexedDB with non-array tags values (RESEARCH Pitfall 5).
    - An `Examples:` block with at least 3 input/output pairs (matches the user-global CLAUDE.md "Tool Usage Examples — Mandatory Standard" rule for any LLM-callable helper).

    Do NOT import from `'dexie'` or `'./database'` — keep this module pure and jsdom-safe so the sibling test can `import { backfillTagsOnJob } from './backfill'` without triggering `new Dexie(...)` side effects. Do NOT use a stronger type like `PrintJob` for the parameter (use `Record<string, unknown>`) — at migration time, Dexie passes a proxy over a raw IndexedDB row, NOT a typed `PrintJob`; using `Record<string, unknown>` is honest and matches the v5 upgrade callback signature in `database.ts:57`.

    Run `npx vitest run src/db/backfill.test.ts` — all 5 tests MUST pass. Commit message format: `feat(12-02): implement backfillTagsOnJob helper`
  </action>
  <verify>
    <automated>npx vitest run src/db/backfill.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The file `src/db/backfill.ts` exists (sibling of `database.ts`).
    - `grep -E '^export function backfillTagsOnJob\\(' src/db/backfill.ts` returns exactly one match.
    - `grep -E 'Array\\.isArray\\(job\\.tags\\)' src/db/backfill.ts` returns at least one match (confirms the defensive guard form, not `??=`).
    - `grep -cE "from\\s+'dexie'|from\\s+'\\.\\/database'" src/db/backfill.ts` returns 0 (zero Dexie imports — module stays jsdom-safe).
    - `grep -E 'Examples:' src/db/backfill.ts` returns at least one match (docstring example block per user-global CLAUDE.md rule).
    - `npx vitest run src/db/backfill.test.ts` exits 0 with all 5 tests passing.
    - `tsc -b` exits 0 (the helper compiles cleanly against the project's `verbatimModuleSyntax` + `strict` config).
  </acceptance_criteria>
  <done>
    `backfillTagsOnJob` is exported as a pure function. All 5 unit tests pass. `tsc -b` is green. No Dexie imports — the module is safe to import from a jsdom Vitest test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB row → migrator | Existing v1.0/v1.1 user records may contain unexpected `tags` shapes if a curious user edited IndexedDB via DevTools. The migrator must defensively normalize. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | `backfillTagsOnJob` input (`job.tags`) | mitigate | Use `Array.isArray(job.tags)` (not `??=`) so any non-array value — null, string, number, object — is normalized to `[]`. Verified by 5 unit-test cases including the `tags: 'oops'` and `tags: 42` corruption scenarios (RESEARCH Pitfall 5). |
| T-12-02-02 | Denial of Service | Test suite under jsdom | mitigate | The test file does NOT import from `src/db/database.ts` — that file's `new Dexie('3DCosterDB')` top-level side effect throws under jsdom (no `indexedDB` global), which would fail the entire test run on load (RESEARCH Pitfall 4). Acceptance criteria explicitly grep-checks for the absence of `dexie` / `./database` imports. |

No package installs in this plan — no supply-chain threat row needed.
</threat_model>

<verification>
- After Task 1 (RED): `npx vitest run src/db/backfill.test.ts` fails with a module-resolution error pointing at `./backfill`.
- After Task 2 (GREEN): `npx vitest run src/db/backfill.test.ts` passes all 5 cases.
- Final gate: `tsc -b && npx vitest run src/db/backfill.test.ts` is fully green.
</verification>

<success_criteria>
- `src/db/backfill.ts` exports `backfillTagsOnJob(job: Record<string, unknown>): void` with the `Array.isArray` defensive guard and a JSDoc-with-examples block.
- `src/db/backfill.test.ts` covers 5 cases (missing, array-preserved, string-corruption, null, number) and lives as a SIBLING of source (not in `__tests__/`).
- `npx vitest run src/db/backfill.test.ts` is green.
- `tsc -b` is green.
- Plan 03 (Wave 2) can now `import { backfillTagsOnJob } from './backfill'` in `database.ts` and pass it as the `modify()` callback.
</success_criteria>

<output>
Create `.planning/phases/12-schema-foundation/12-02-SUMMARY.md` when done. Include: confirmation that the test file lives at the sibling path (not `__tests__/`), the 5 test-case names, the final `Array.isArray` guard line, and a one-line note that Plan 03 will import this helper.
</output>
