---
phase: 13-tax-model-ui-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SettingsModal.tsx
autonomous: true
requirements:
  - TAX-01
  - UI-08
  - UI-09
tags:
  - tax
  - settings
  - ui-sweep
  - infotooltip

must_haves:
  truths:
    - "TAX-01: A new 'Default Tax Rate' h3 section exists in the SettingsModal 'Costs & Rates' tab (activeTab === 'costs') after the Default Profit Margin section"
    - "D-04 + Pitfall 3: The Default Tax Rate Input uses `value={userProfile.defaultTaxRate ?? ''}` (empty string, NOT `?? 0`); the onChange handler writes `defaultTaxRate: undefined` when the input is cleared"
    - "D-10 + D-13 + D-11: The Default Tax Rate label carries an InfoTooltip whose body is the locked US marketplace-facilitator string when userCurrency === 'USD', and the non-USD fallback string otherwise"
    - "Tab terminology resolved per RESEARCH Discrepancy 1: Default Tax Rate slots into the existing 'Costs & Rates' tab; no new 'Pricing' tab is created"
    - "D-15: Three pre-existing descriptive <p> blocks in SettingsModal (electricity description :213-215, hourly rate description :231-233, profit margin description :257-259) are MIGRATED into <InfoTooltip> on the field label — the <p> elements are deleted; the labels are rewritten from `block` to `flex items-center gap-1.5`"
    - "UI-08 sweep: 3 numeric inputs in SettingsModal that currently lack `compact` get the prop added — Custom Carrier Cost (line ~488-494) and the two Custom Marketplace Add fields (% + Fixed) at lines 740-754"
    - "InfoTooltip is imported (the file does not currently import it; the import is added)"
    - "NewBadge for 'default-tax-rate' is emitted via the absolute-positioned h3 pattern matching SettingsModal.tsx:240 — `className=\"absolute top-0 left-full ml-2 pointer-events-none\"` so the badge does not push layout; registry entry is added in Plan 06"
    - "tsc -b passes; SettingsModal.tsx still compiles and renders"
  artifacts:
    - path: "src/components/SettingsModal.tsx"
      provides: "Default Tax Rate field; 3 InfoTooltip migrations; 3 additional compact numeric inputs"
      contains: "Default Tax Rate"
  key_links:
    - from: "src/components/SettingsModal.tsx (Default Tax Rate Input.onChange)"
      to: "userProfile.defaultTaxRate (UserProfile)"
      via: "onUserProfileChange callback"
      pattern: "defaultTaxRate:\\s*(undefined|Math\\.min)"
    - from: "src/components/SettingsModal.tsx (Default Tax Rate InfoTooltip)"
      to: "userCurrency prop branching to D-11 / non-USD wording"
      via: "ternary on userCurrency === 'USD'"
      pattern: "userCurrency === 'USD'"
---

<objective>
Add the Default Tax Rate field to SettingsModal's "Costs & Rates" tab, migrate three descriptive `<p>` blocks into InfoTooltip-on-label per UI-09 / D-15, and sweep three remaining non-compact numeric inputs in the Custom Carrier and Custom Marketplace Add forms.

Purpose: TAX-01 (Default Tax Rate setting that seeds new jobs) + the SettingsModal portion of UI-08 + UI-09. The Default Tax Rate must start empty/unset (D-04 + Pitfall 3) so the resolveTaxRate fallback chain (Plan 01) fires correctly for first-time users. The US marketplace-facilitator note appears here (D-13) AND in the Cost Breakdown Tax row (Plan 05).
Output: One modified file — SettingsModal.tsx with the new field, 3 migrated tooltips, 3 added `compact` props, and one new InfoTooltip import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-tax-model-ui-sweep/13-CONTEXT.md
@.planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md
@.planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md
@.planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md
@src/components/SettingsModal.tsx
@src/components/ui/Input.tsx
@src/components/ui/InfoTooltip.tsx
@src/components/NewBadge.tsx
@src/types.ts

<interfaces>
<!-- Existing surface this plan modifies. Executor uses these directly. -->

From src/components/SettingsModal.tsx props (lines 7-20):
userProfile: UserProfile and onUserProfileChange callback already exist on the props. No prop changes needed.

From src/components/SettingsModal.tsx:42 (tab state):
activeTab is 'costs' | 'delivery' | 'marketplaces'. Tab label "Costs & Rates" at line 149. There is NO 'pricing' tab.

From src/components/SettingsModal.tsx:237-261 (Default Profit Margin section — the analog for Default Tax Rate, located inside `activeTab === 'costs'` block): uses h3 with `relative inline-block` host for the absolute-positioned NewBadge; Input with `compact`, `step="0.1"`, `min="0"`, `max="99.9"`, `value={... ?? 30}`, clamped onChange; trailing descriptive `<p className="text-xs text-slate-500 mt-2">`.

From src/components/ui/InfoTooltip.tsx (existing primitive): exports `InfoTooltip({ text, className }) => JSX`. Import path from SettingsModal.tsx: `./ui/InfoTooltip`.

From src/components/NewBadge.tsx (existing primitive): absolute positioning via className override is the anti-pattern-safe variant — `absolute top-0 left-full ml-2 pointer-events-none`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Default Tax Rate field with NewBadge h3 and InfoTooltip-branched label</name>
  <files>src/components/SettingsModal.tsx</files>
  <read_first>
    - src/components/SettingsModal.tsx (full file — see lines 1-5 for imports, lines 200-263 for the `activeTab === 'costs'` block and Default Profit Margin analog)
    - src/components/ui/InfoTooltip.tsx (verify export name and prop shape — `text: string`, `className?: string`)
    - src/components/NewBadge.tsx (verify the className override pattern — `absolute top-0 left-full ml-2 pointer-events-none` mounts the badge on a `relative inline-block` host)
    - .planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md lines 124-146 (locked copy + JSX structure: h3 text "Default Tax Rate", label "Tax Rate (%)", placeholder "e.g., 20", US/non-USD tooltip branching, NewBadge with absolute positioning)
    - .planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md lines 254-268 (numeric input keyboard contract — step="0.1", min="0", max="99.9", value=`?? ''`, onChange clears to `undefined` on empty)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 717-763 (RESEARCH Example 4: full drop-in JSX for the Default Tax Rate block, including the exact onChange handler with the undefined-on-empty pattern from Pitfall 3)
    - .planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md lines 247-289 (analog assignment + critical divergences from Default Profit Margin: `?? ''` instead of `?? 30`, undefined-on-empty onChange, no <p> below — D-04, Pitfall 3)
  </read_first>
  <action>
    Add `import { InfoTooltip } from './ui/InfoTooltip';` to the imports at top of SettingsModal.tsx. Existing imports already include Input from './ui' and NewBadge from './NewBadge' — keep those untouched.

    Inside the `activeTab === 'costs' && (...)` block, AFTER the existing Default Profit Margin section's closing `</div>` (currently at line ~262, after the existing `<p>Applied to new jobs...</p>`), append a new `<div className="pt-4 border-t border-slate-700">` block that mirrors the Default Profit Margin pattern with these concrete differences:

    Section h3: a `<h3 className="text-sm font-medium text-slate-300 mb-3 relative inline-block">` whose visible text is `Default Tax Rate` and which contains a NewBadge child with `feature="default-tax-rate"` and `className="absolute top-0 left-full ml-2 pointer-events-none"`. The `relative inline-block` on the h3 is the anti-pattern-safe host for the absolute badge (CLAUDE.md memory rule — never inline a NewBadge inside a flex-1 container).

    Field label: a `<label className="flex items-center gap-1.5 text-xs text-slate-400 mb-1">` containing a `<span>Tax Rate (%)</span>` followed by an `<InfoTooltip text={...} />`. The tooltip text is a ternary on `userCurrency`:
    - When `userCurrency === 'USD'`: the locked D-11 string verbatim — `"Most US states require marketplaces (Etsy, eBay, Amazon) to collect sales tax for you. Override only if you sell direct or in a non-facilitator state."`
    - Otherwise: `"Default tax rate applied to new jobs. Override per-job in the calculator."`

    Input: an `<Input>` with `type="number"`, `step="0.1"`, `min="0"`, `max="99.9"`, `compact`, `placeholder="e.g., 20"`, plus controlled value/onChange:
    - `value={userProfile.defaultTaxRate ?? ''}` — CRITICAL: empty string, NOT `?? 0` or `?? 30`. Per D-04 + Pitfall 3, the field starts empty/unset; writing `0` on first render breaks the fallback chain.
    - `onChange` handler: when `e.target.value === ''`, call `onUserProfileChange({ ...userProfile, defaultTaxRate: undefined })` (undefined — not 0). Otherwise `const parsed = parseFloat(e.target.value)`, then `if (Number.isFinite(parsed))` call `onUserProfileChange({ ...userProfile, defaultTaxRate: Math.min(Math.max(parsed, 0), 99.9) })`. The shape mirrors PrinterSettings.tsx:152-153 (`newInstancePurchasePrice`) — the canonical undefined-on-empty analog.

    NO descriptive `<p>` block below the Default Tax Rate Input (UI-SPEC lines 137-139: D-15 dictates description lives in the InfoTooltip on the label, not in a <p> below).

    Tab placement decision (RESEARCH Discrepancy 1): the Default Tax Rate section goes INSIDE the existing `activeTab === 'costs'` block — do NOT create a new 'pricing' tab. The h3 + field block sits BELOW Default Profit Margin (UI-SPEC line 126 placement decision; margin is the more frequently edited setting and stays on top).

    DO NOT modify any other section of SettingsModal.tsx in this task. The InfoTooltip migrations for electricity / hourly rate / profit margin descriptions are Task 2 of this plan.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/13-03-task1.tsc.log; ! grep -q "error TS" /tmp/13-03-task1.tsc.log && grep -n "Default Tax Rate" src/components/SettingsModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Default Tax Rate" src/components/SettingsModal.tsx` returns at least 1
    - `grep "import.*InfoTooltip.*from" src/components/SettingsModal.tsx` matches
    - `grep -F "userProfile.defaultTaxRate ?? ''" src/components/SettingsModal.tsx` matches (Pitfall 3 lock)
    - `grep -F "defaultTaxRate: undefined" src/components/SettingsModal.tsx` matches
    - `grep -F "feature=\"default-tax-rate\"" src/components/SettingsModal.tsx` matches
    - `grep -F "absolute top-0 left-full ml-2 pointer-events-none" src/components/SettingsModal.tsx` matches at least twice (Default Profit Margin already had it; Default Tax Rate is the second)
    - `grep -F "Most US states require marketplaces (Etsy, eBay, Amazon) to collect sales tax for you. Override only if you sell direct or in a non-facilitator state." src/components/SettingsModal.tsx` matches (D-11 verbatim)
    - `grep -F "Default tax rate applied to new jobs. Override per-job in the calculator." src/components/SettingsModal.tsx` matches (non-USD branch)
    - `grep -c "activeTab === 'costs'" src/components/SettingsModal.tsx` returns 1 (no new tab added)
    - Default Tax Rate is positioned BELOW Default Profit Margin: `grep -n "Default Profit Margin\|Default Tax Rate" src/components/SettingsModal.tsx` shows Default Profit Margin first
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>
    Default Tax Rate field renders below Default Profit Margin in the Costs & Rates tab with the locked US/non-USD tooltip branch, undefined-on-empty handling, NewBadge absolute-positioned on a `relative inline-block` h3, and zero new tabs. File compiles.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate 3 descriptive p blocks to InfoTooltip on label and sweep 3 remaining non-compact numeric inputs</name>
  <files>src/components/SettingsModal.tsx</files>
  <read_first>
    - src/components/SettingsModal.tsx (full file — see lines 205-215 for electricity description, lines 222-233 for hourly rate description, lines 244-259 for profit margin description, lines 488-494 for Custom Carrier Cost input, lines 740-754 for Custom Marketplace Add inputs)
    - src/components/CostCalculator.tsx:861-863 (canonical InfoTooltip-on-label pattern: `<label className="flex items-center gap-1.5 text-xs text-slate-400 mb-1"><span>...</span><InfoTooltip text="..." /></label>`)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 441-457 (UI Sweep Inventory for SettingsModal — 3 descriptive `<p>` migrations + 3 numeric inputs needing `compact`)
    - .planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md lines 281-289 + 552-571 (S2 InfoTooltip-on-label class-string contract: label transitions from `block` to `flex items-center gap-1.5`; children become `<span>` + `<InfoTooltip>`; original `<p>` is deleted)
  </read_first>
  <action>
    Migrate the 3 descriptive `<p>` blocks in SettingsModal into `<InfoTooltip>` on the label of the field above. For each one:
    1. Locate the existing `<label className="block text-xs text-slate-400 mb-1">[Label Text]</label>` immediately above the corresponding Input.
    2. Rewrite the label to `<label className="flex items-center gap-1.5 text-xs text-slate-400 mb-1"><span>[Label Text]</span><InfoTooltip text="[migrated description]" /></label>`.
    3. DELETE the corresponding `<p className="text-xs text-slate-500 mt-2">...</p>` block beneath the Input.

    Verbatim rule: copy the existing `<p>` body text into the InfoTooltip's `text` prop. Do not rephrase. If the existing text contains escaped quotes or special characters, preserve them as-is. The three sites are:
    1. Cost per kWh input (currently at ~205-212; description at ~213-215). Label text reads "Cost per kWh". Migrate the existing `<p>` body into the InfoTooltip `text` prop verbatim.
    2. Hourly Rate input (~222-230; description at ~231-233 currently reads "Value your time! Applied to prep, monitoring, and post-processing minutes you log on each job."). Label text "Hourly Rate". Tooltip text = that exact sentence.
    3. Profit Margin input (~244-256; description at ~257-259 currently reads "Applied to new jobs and when the form is cleared or a job is saved. You can still adjust the margin per job in the calculator."). Label text "Profit Margin (%)". Tooltip text = that exact sentence.

    Sweep the 3 remaining non-compact numeric inputs (RESEARCH UI Sweep Inventory) by adding the `compact` prop:
    1. Custom Carrier — Cost (line ~488-494): add `compact` to the existing `<Input type="number" ... />`.
    2. Custom Marketplace Add — Percent fee (line ~740-746): add `compact`.
    3. Custom Marketplace Add — Fixed fee (line ~748-754): add `compact`.

    Existing `compact` inputs (cost/kWh Input itself, hourly rate Input, profit margin Input, marketplace fees, carriers, etc.) — DO NOT TOUCH. Only add `compact` to the 3 inputs explicitly identified above.

    DO NOT delete or modify the Default Tax Rate section added in Task 1 — its `<p>` is already absent by design (D-15 lock).

    DO NOT migrate text inputs (Carrier name, Custom Marketplace name) — D-14 restricts the sweep to numeric inputs only.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/13-03-task2.tsc.log; ! grep -q "error TS" /tmp/13-03-task2.tsc.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "text-xs text-slate-500 mt-2" src/components/SettingsModal.tsx` returns 0 — all three descriptive `<p>` blocks have been deleted (the only references to that class were on the three migrated descriptions). If the codebase contains the same class elsewhere unrelated to this migration, scope the grep to the specific lines that previously held the descriptions.
    - `grep -F "Value your time! Applied to prep, monitoring, and post-processing minutes you log on each job." src/components/SettingsModal.tsx` matches at least once AND that match is INSIDE an `InfoTooltip text=` attribute (verify by inspecting the surrounding line)
    - `grep -F "Applied to new jobs and when the form is cleared or a job is saved. You can still adjust the margin per job in the calculator." src/components/SettingsModal.tsx` matches at least once and is inside an `InfoTooltip text=` attribute
    - `grep -c "InfoTooltip" src/components/SettingsModal.tsx` returns at least 4 — one import line plus at least 4 JSX usages (3 migrated descriptions + 1 from Task 1's Default Tax Rate; total components = 4, total occurrences ≥ 5 once including the import; relax to ≥ 4 to accommodate consolidated import/named refs)
    - Custom Carrier Cost Input (line ~488-494 region) contains `compact`: `awk '/Custom Carrier|newCarrierCost/,/\/>/' src/components/SettingsModal.tsx | grep "compact"` matches (planner-readable check; executor adjusts grep if line numbers shift)
    - The 3 newly-swept inputs have `compact`: cumulative `grep -c "compact" src/components/SettingsModal.tsx` increased by exactly 3 compared to pre-change baseline (baseline counted from PATTERNS.md: ~24 existing compact inputs before this plan; after Task 1 it is 25 with Default Tax Rate; after Task 2 it should be 28). If baseline is uncertain, executor verifies by spot-checking each of the 3 specified line ranges for `compact`.
    - All 3 input lines (488-494, 740-746, 748-754) contain the literal string `compact` somewhere within their range
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>
    SettingsModal.tsx has zero "text-xs text-slate-500 mt-2" `<p>` blocks describing electricity/hourly/margin (or those blocks have been verified migrated). The 3 InfoTooltip migrations carry the verbatim descriptions on `flex items-center gap-1.5` labels. The 3 named numeric inputs have `compact`. File compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Default Tax Rate input → userProfile | Local controlled-input; clamped to 0..99.9 numerically before persistence via onUserProfileChange. No network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Tampering | Default Tax Rate Input numeric range | mitigate | `Math.min(Math.max(parsed, 0), 99.9)` clamp + `Number.isFinite(parsed)` guard mirror the existing Default Profit Margin pattern at SettingsModal.tsx:251-253; NaN / Infinity / scientific notation cannot persist out-of-range values |
| T-13-09 | Tampering | XSS via InfoTooltip text content | mitigate | InfoTooltip body text is a hard-coded string literal (D-11 verbatim or the non-USD fallback) — not user-controllable input. React's JSX text escaping handles the literal safely. No `dangerouslySetInnerHTML` is introduced. |
| T-13-10 | Information Disclosure | userProfile in localStorage | accept | userProfile already stores defaultProfitMargin / laborHourlyRate / currency without crypto. defaultTaxRate is the same shape; consistent with the existing local-first threat model. No new exposure. |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan. RESEARCH.md "Package Legitimacy Audit" confirms zero new packages. Auto-accept. |
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `grep -c "Default Tax Rate" src/components/SettingsModal.tsx` ≥ 1
- `grep -F "userProfile.defaultTaxRate ?? ''" src/components/SettingsModal.tsx` matches
- `grep -F "defaultTaxRate: undefined" src/components/SettingsModal.tsx` matches
- `grep -c "InfoTooltip" src/components/SettingsModal.tsx` ≥ 4 (1 import + at least 4 usages)
- 3 newly-swept inputs contain `compact`
- 3 descriptive `<p>` blocks have been replaced by InfoTooltip on the label
- Manual UAT (deferred to phase wrap-up): Settings → Costs & Rates → Default Tax Rate field visible below Default Profit Margin; persists across reload (TAX-01 success criterion)
</verification>

<success_criteria>
- 1 modified file committed: src/components/SettingsModal.tsx
- TAX-01 fully wired: user can set, edit, and persist a Default Tax Rate via SettingsModal (Costs & Rates tab)
- UI-08 partial coverage: 3 inputs in SettingsModal moved to `compact`
- UI-09 partial coverage: 3 descriptive `<p>` blocks migrated to InfoTooltip on the label
- NewBadge for `default-tax-rate` emitted with anti-pattern-safe absolute positioning (registry entry added in Plan 06)
</success_criteria>

<output>
Create `.planning/phases/13-tax-model-ui-sweep/13-03-SUMMARY.md` when done. Confirm Default Tax Rate field renders with proper value=`?? ''` semantics and the 3 InfoTooltip migrations are intact.
</output>
