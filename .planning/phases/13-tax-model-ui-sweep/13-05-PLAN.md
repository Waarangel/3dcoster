---
phase: 13-tax-model-ui-sweep
plan: 05
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - src/components/CostCalculator.tsx
  - src/App.tsx
autonomous: true
requirements:
  - TAX-02
  - TAX-04
  - UI-08
  - UI-09
  - UI-10
tags:
  - tax
  - cost-calculator
  - ui-sweep
  - newbadge-cleanup
  - persistence

must_haves:
  truths:
    - "TAX-02: A per-job Tax Rate Input is added to the Set Financial Targets block (current `md:grid-cols-3` row at CostCalculator.tsx:1150 becomes `md:grid-cols-4`) using the wide pl-8 + % prefix overlay pattern — NOT compact (UI-SPEC line 178+188)"
    - "TAX-02 persistence: handleSaveJob writes `taxRate: editingJob?.taxRate` (or the resolved-rate equivalent — planner picks; see action) AND `taxAmount: tax.taxAmount` onto the PrintJob record in BOTH the update branch (~506-525) and the create branch (~534-553); historic jobs reproduce their saved math"
    - "TAX-02 round-trip: when editingJob is set, the per-job Tax Rate Input value uses `editingJob?.taxRate ?? ''` and the onChange writes `taxRate: undefined` on empty (mirrors PrinterSettings.tsx:152-153 pattern)"
    - "TAX-04: A Tax row JSX block is dropped in AFTER the Cost Per Unit total at line ~1313 and BEFORE the Marketplace Fee Note at ~1316. The block is wrapped in `{tax.ratePercent > 0 && (...)}` so it hides entirely at 0% (TAX-04 success criterion 4)"
    - "TAX-04: The Tax row uses `tax.ratePercent.toFixed(1)` for the label (one decimal place per D-07) and `tax.taxAmount.toFixed(2)` for the amount; the Total (with Tax) row displays `sellingPrice + tax.taxAmount`"
    - "TAX-04: Tax row label carries an InfoTooltip whose text comes from `tooltipForSource(taxSource, userCurrency)` (Plan 01's helper, signature extended for D-12 + D-13); this surfaces D-08 (rate source disclosure) and D-11 (US marketplace-facilitator note appended on all source.kind branches when userCurrency === 'USD') and D-03 (staleness hint inside the same tooltip)"
    - "useMemo wiring: `taxSource = useMemo(() => resolveTaxRate({jobOverride: editingJob?.taxRate, settingsDefault: userProfile.defaultTaxRate, currency: userCurrency}), [editingJob?.taxRate, userProfile.defaultTaxRate, userCurrency])` and `tax = useMemo(() => calculateTax(sellingPrice, taxSource.rate), [sellingPrice, taxSource.rate])`"
    - "CostCalculator receives userProfile (or at minimum userProfile.defaultTaxRate) as a NEW prop; App.tsx is updated to pass `userProfile={userProfile}` (or `defaultTaxRate={userProfile.defaultTaxRate}`) so the resolveTaxRate chain reaches the calculator"
    - "UI-08 sweep: 3 strong-yes numeric inputs in CostCalculator get `compact` — Materials Used Quantity (~922-928, replaces `w-20`), Shipping Distance (~970-981), Carrier Cost (~988-996)"
    - "UI-10 partial: 3 NewBadge JSX sites removed — `<NewBadge feature=\"per-unit-licensing\" />` at ~748, `<NewBadge feature=\"author-min-price\" />` at ~756, `<NewBadge feature=\"packaging-materials\" />` at ~1016"
    - "After NewBadge JSX removals, the import `{ NewBadge }` is RETAINED in CostCalculator.tsx because `<NewBadge feature=\"model-url\" />` at line ~713 still exists (NOT a stale entry — D-17 audit shows model-url is fresh)"
    - "tsc -b passes; vitest costCalc + taxResolution suites still pass; the calculator still functions"
    - "W-05 atomic commit: all three tasks land in a SINGLE commit; the intermediate state (Task 1 alone, reading editingJob?.taxRate directly) is never visible in main"
  artifacts:
    - path: "src/components/CostCalculator.tsx"
      provides: "Per-job Tax Rate input; Tax row in Cost Breakdown; persistence wire-up; 3 compact numeric inputs; 3 NewBadge sites removed"
      contains: "calculateTax\\|resolveTaxRate"
    - path: "src/App.tsx"
      provides: "userProfile (or userProfile.defaultTaxRate) prop passed to CostCalculator"
      contains: "defaultTaxRate\\|userProfile={"
  key_links:
    - from: "src/components/CostCalculator.tsx (useMemo taxSource)"
      to: "src/utils/taxResolution.ts (resolveTaxRate)"
      via: "named import from '../utils/taxResolution'"
      pattern: "resolveTaxRate"
    - from: "src/components/CostCalculator.tsx (useMemo tax)"
      to: "src/utils/costCalc.ts (calculateTax)"
      via: "named import from '../utils/costCalc'"
      pattern: "calculateTax"
    - from: "src/components/CostCalculator.tsx (Tax row label)"
      to: "src/utils/taxResolution.ts (tooltipForSource)"
      via: "JSX consumption — pass userCurrency as second argument (D-12 + D-13)"
      pattern: "tooltipForSource\\(taxSource, *userCurrency\\)"
    - from: "src/components/CostCalculator.tsx (handleSaveJob)"
      to: "PrintJob.taxRate, PrintJob.taxAmount"
      via: "object spread in update + create branches"
      pattern: "taxRate:|taxAmount:"
---

<objective>
Wire the tax engine into CostCalculator: add the per-job Tax Rate Input to the Set Financial Targets block, drop the Tax + Total-with-Tax row into the Cost Breakdown after the Cost Per Unit total, persist `taxRate` and `taxAmount` on the PrintJob record in handleSaveJob's update and create branches, sweep three remaining non-compact numeric inputs, and remove three stale NewBadge JSX sites.

Purpose: TAX-02 + TAX-04 land here — the user-visible per-job tax flow, the Tax row, the order-of-operations math (tax applies to sellingPrice, not subtotal), and the persistence path. This plan depends on Plans 01 (resolveTaxRate, tooltipForSource, TaxRateSource) and 02 (calculateTax) — those utilities must exist first.
Output: Two modified files — CostCalculator.tsx (the main work) and App.tsx (one prop addition). No new files.

ATOMIC COMMIT: This plan MUST be committed as a single atomic unit (one commit covering Tasks 1, 2, and 3), NOT per-task. Rationale: Task 1 introduces a useMemo binding (`taxSource`) that reads `editingJob?.taxRate` directly; Task 2 replaces that read with a new local-state `taxRateOverride`. Landing Task 1 alone leaves the codebase in a mid-state where the useMemo dependency tracking is wrong (it doesn't reflect in-progress edits). The atomic commit ensures the local-state version is the only version that ever lands. See `<execution_notes>` below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<execution_notes>
**Atomic commit (W-05 lock):** Plan 05 MUST be committed as a single atomic unit. Do NOT commit per task. The intermediate state after Task 1 alone is semantically incorrect — Task 1's `taxSource` useMemo reads `editingJob?.taxRate` directly, which Task 2 immediately replaces with a `taxRateOverride` local-state read. The local-state version is the only version that should ever land in main.

Execution sequence:
1. Complete all three tasks locally (Wave 0 file write order: Task 1 → Task 2 → Task 3).
2. Run the full verify gate on the final state: `npx tsc -b && npx vitest run`.
3. Stage all changes (`git add src/components/CostCalculator.tsx src/App.tsx`).
4. Create ONE commit covering all three tasks. Suggested message: `feat(13): wire per-job tax rate UI + persistence + sweep + NewBadge cleanup`.

If for any reason the execution must pause between tasks (context exhaustion, blocker), pause AFTER Task 3 — never between Task 1 and Task 2.
</execution_notes>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-tax-model-ui-sweep/13-CONTEXT.md
@.planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md
@.planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md
@.planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md
@.planning/phases/13-tax-model-ui-sweep/13-01-SUMMARY.md
@.planning/phases/13-tax-model-ui-sweep/13-02-SUMMARY.md
@src/components/CostCalculator.tsx
@src/App.tsx
@src/types.ts
@src/utils/costCalc.ts
@src/utils/taxResolution.ts
@src/components/ui/Input.tsx
@src/components/ui/InfoTooltip.tsx
@src/components/NewBadge.tsx

<interfaces>
<!-- Existing surface + the new util exports this plan consumes. -->

From src/utils/taxResolution.ts (Plan 01 output):
- `resolveTaxRate(input: { jobOverride: number | undefined; settingsDefault: number | undefined; currency: Currency }): TaxRateSource`
- `tooltipForSource(source: TaxRateSource): string`
- `type TaxRateSource = { kind: 'override' | 'settings' | 'region' | 'eu-average' | 'manual'; rate: number; ... }`

From src/utils/costCalc.ts (Plan 02 output):
- `calculateTax(sellingPrice: number, ratePercent: number): { taxAmount: number; ratePercent: number }`

From src/components/CostCalculator.tsx:14-27 (current props):
```typescript
interface CostCalculatorProps {
  materials: Material[];
  printers: PrinterConfig[];
  printerInstances: PrinterInstance[];
  electricity: ElectricityConfig;
  laborHourlyRate: number;
  defaultProfitMargin: number;
  userCurrency: Currency;
  shippingConfig: ShippingConfig;
  onSaveJob: (job: PrintJob, printHours: number) => void;
  onUpdateJob: (job: PrintJob) => void;
  editingJob: PrintJob | null;
  onCancelEdit: () => void;
}
```
CostCalculator does NOT currently receive userProfile. This plan adds the prop (or adds a narrow `defaultTaxRate: number | undefined` prop — planner picks; the simpler choice is adding `userProfile: UserProfile` for symmetry with SettingsModal).

From src/App.tsx:266-279 (the JSX site where CostCalculator is rendered):
```tsx
<CostCalculator
  materials={materials}
  printers={printers}
  printerInstances={printerInstances}
  electricity={electricity}
  laborHourlyRate={userProfile.laborHourlyRate}
  defaultProfitMargin={userProfile.defaultProfitMargin ?? 30}
  userCurrency={userProfile.currency}
  shippingConfig={shippingConfig}
  onSaveJob={handleSaveJob}
  onUpdateJob={updateJob}
  editingJob={editingJob}
  onCancelEdit={handleCancelEdit}
/>
```

From src/types.ts (Phase 12 deliverables, already landed):
- `PrintJob.taxRate?: number`
- `PrintJob.taxAmount?: number`
- `UserProfile.defaultTaxRate?: number`

From src/components/CostCalculator.tsx (key reference lines):
- 506-525: handleSaveJob update branch (editingJob path)
- 534-553: handleSaveJob create branch (new job path)
- 710-713: Model URL label (canonical InfoTooltip-on-label pattern with surviving NewBadge `model-url`)
- 748: stale `<NewBadge feature="per-unit-licensing" />` — REMOVE
- 756: stale `<NewBadge feature="author-min-price" />` — REMOVE
- 922-928: Materials Used Quantity (currently `className="w-20 text-right"`) — replace `w-20` with `compact`
- 970-981: Shipping Distance — add `compact`
- 988-996: Carrier Cost — add `compact`
- 1016: stale `<NewBadge feature="packaging-materials" />` — REMOVE
- 1150-1164: Profit Margin field (analog for the new per-job Tax Rate input — the `pl-8 + %` overlay pattern)
- 1294-1303: Subtotal / Failure Adjustment block (analog for the Tax row wrapper)
- 1305-1313: Cost Per Unit total (the new Tax row drops in immediately AFTER this block)
- 1316: Marketplace Fee Note (the Tax row drops in BEFORE this block)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire CostCalculator props + App.tsx pass-through; add taxSource + tax useMemo bindings</name>
  <files>src/components/CostCalculator.tsx, src/App.tsx</files>
  <read_first>
    - src/components/CostCalculator.tsx (full file — props interface at 14-27, function signature at 54, the existing useMemo + state declarations through ~200)
    - src/App.tsx (lines 90-100 for the useUserProfile hook, lines 266-279 for the CostCalculator JSX site)
    - src/utils/taxResolution.ts (verify exact named exports: resolveTaxRate, tooltipForSource, TaxRateSource)
    - src/utils/costCalc.ts (verify calculateTax export added in Plan 02)
    - src/types.ts (verify UserProfile interface includes defaultTaxRate?: number, and PrintJob.taxRate / taxAmount per Phase 12)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 660-672 (RESEARCH Example 2: full useMemo wiring with dependency arrays)
    - .planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md lines 384-396 (per-job tax state hookup analog assignment)
  </read_first>
  <action>
    Step 1 — Add a new prop to CostCalculator. Choose between two equivalent options (planner discretion per CONTEXT.md):
    (a) Add `userProfile: UserProfile` to the props interface and the function signature destructure; pass `userProfile.defaultTaxRate` into resolveTaxRate.
    (b) Add a narrower `defaultTaxRate: number | undefined` prop directly.

    Use option (a) for symmetry with SettingsModal (which already takes userProfile + onUserProfileChange). Update src/components/CostCalculator.tsx:
    - Add `userProfile: UserProfile` to the CostCalculatorProps interface (alphabetical position is fine; keeping near defaultProfitMargin is natural).
    - Add `userProfile` to the function-signature destructure at line 54.
    - Add `import type { UserProfile } from '../types';` to the existing type-import line (the file already imports Currency / PrintJob / others from '../types').

    Step 2 — Add named imports at top of CostCalculator.tsx:
    - `import { calculateTax } from '../utils/costCalc';` (or extend the existing import line if costCalc is already imported)
    - `import { resolveTaxRate, tooltipForSource } from '../utils/taxResolution';`
    - Verify InfoTooltip is already imported (it is — used at Model URL label line 710-713).

    Step 3 — Add the two useMemo bindings near the other useMemo declarations in the component body (after the costs useMemo, before the JSX return). Use the exact shapes from RESEARCH Example 2:
    - `const taxSource = useMemo(() => resolveTaxRate({ jobOverride: editingJob?.taxRate, settingsDefault: userProfile.defaultTaxRate, currency: userCurrency }), [editingJob?.taxRate, userProfile.defaultTaxRate, userCurrency]);`
    - `const tax = useMemo(() => calculateTax(sellingPrice, taxSource.rate), [sellingPrice, taxSource.rate]);`

    NOTE: `editingJob?.taxRate` resolves the override side of the chain — when the user is editing a saved job, the per-job override is read from the persisted record. When creating a new job, this is undefined and the chain falls through to settings → region → manual. Plan 05 Task 2 introduces a local state for in-progress edits to taxRate; until Task 2 lands, this useMemo only reads the persisted value (so the Tax row reflects the saved rate). After Task 2, the local state replaces this read (see Task 2 action).

    Step 4 — Update src/App.tsx at lines 266-279. Add `userProfile={userProfile}` as a new prop on the `<CostCalculator ... />` JSX. Keep all other props unchanged.

    DO NOT change any other section of either file in this task. The Tax row JSX, per-job Tax Rate Input, persistence, sweep, and NewBadge removals are subsequent tasks.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/13-05-task1.tsc.log; ! grep -q "error TS" /tmp/13-05-task1.tsc.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import.*calculateTax.*from.*utils/costCalc" src/components/CostCalculator.tsx` matches
    - `grep "import.*resolveTaxRate.*from.*utils/taxResolution" src/components/CostCalculator.tsx` matches
    - `grep "tooltipForSource" src/components/CostCalculator.tsx` matches (named in the same import as resolveTaxRate or a sibling line)
    - `grep "import type.*UserProfile" src/components/CostCalculator.tsx` matches
    - `grep -F "userProfile: UserProfile" src/components/CostCalculator.tsx` matches in the props interface
    - `grep -F "const taxSource = useMemo" src/components/CostCalculator.tsx` matches
    - `grep -F "const tax = useMemo" src/components/CostCalculator.tsx` matches
    - `grep -F "calculateTax(sellingPrice, taxSource.rate)" src/components/CostCalculator.tsx` matches
    - `grep -F "userProfile={userProfile}" src/App.tsx` matches (App passes the new prop)
    - `npx tsc -b` exits 0 (TypeScript verifies the new prop wire-up end-to-end)
  </acceptance_criteria>
  <done>
    CostCalculator now receives userProfile, imports calculateTax + resolveTaxRate + tooltipForSource, and exposes taxSource + tax useMemo values. App.tsx passes the prop. File compiles. No JSX rendering changes yet.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add per-job Tax Rate input + Tax row JSX + persistence in handleSaveJob</name>
  <files>src/components/CostCalculator.tsx</files>
  <read_first>
    - src/components/CostCalculator.tsx (lines 1150-1200 for the Set Financial Targets block, lines 1294-1313 for the Cost Breakdown total block, lines 506-558 for handleSaveJob update + create paths, lines 180-200 for the existing "load editingJob into local state" useEffect)
    - src/utils/taxResolution.ts (verify the TaxRateSource discriminator and tooltipForSource signature)
    - .planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md lines 147-167 (locked Tax row JSX: outer wrapper class strings, label flex, Total (with Tax) row with `text-white font-semibold mt-1`)
    - .planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md lines 176-188 (per-job Tax Rate Input contract: wide style with `pl-8`, `%` prefix overlay, NOT compact, value = `editingJob?.taxRate ?? ''`)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 674-689 (RESEARCH Example 2: Tax row JSX verbatim)
    - .planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md lines 318-345 (Tax row class-string contract)
    - .planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md lines 359-380 (handleSaveJob persistence pattern — add taxRate + taxAmount to BOTH branches)
  </read_first>
  <action>
    Step 1 — Add per-job Tax Rate local state. Near the other useState declarations in CostCalculator body (around line 120-180), add `const [taxRateOverride, setTaxRateOverride] = useState<number | undefined>(() => editingJob?.taxRate);`. In the existing "load editingJob into local state" useEffect (which currently sets printName, selectedInstanceId, etc. at lines ~179-200), add `setTaxRateOverride(editingJob.taxRate);` to the if-branch.

    Step 2 — Update the taxSource useMemo from Task 1 to read from local state instead of editingJob?.taxRate so that in-progress edits flow through resolveTaxRate. Change the dependency array and the input:
    - `const taxSource = useMemo(() => resolveTaxRate({ jobOverride: taxRateOverride, settingsDefault: userProfile.defaultTaxRate, currency: userCurrency }), [taxRateOverride, userProfile.defaultTaxRate, userCurrency]);`

    Step 3 — Per-job Tax Rate Input UI. Locate the Set Financial Targets block at line ~1150. Change the parent grid className from `md:grid-cols-3` to `md:grid-cols-4` (UI-SPEC line 178). Add a new 4th column `<div>` that mirrors the Profit Margin field's exact shape (lines 1150-1164 — the wide pl-8 + `%` prefix overlay pattern):

    The new column contains:
    - A `<label className="block text-sm font-medium text-white mb-1">Tax Rate</label>` (mirrors Profit Margin's label class verbatim)
    - A wrapping `<div className="relative">` for the prefix overlay
    - A `<span className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400 z-10">%</span>` (mirrors Profit Margin's `%` overlay)
    - An `<Input type="number" min="0" max="99.9" step="0.1" className="pl-8" value={taxRateOverride ?? ''} onChange={e => { const v = e.target.value; if (v === '') setTaxRateOverride(undefined); else { const parsed = parseFloat(v); if (Number.isFinite(parsed)) setTaxRateOverride(Math.min(Math.max(parsed, 0), 99.9)); } }} />`. NOTE: NO `compact` (UI-SPEC line 188 — primary financial-target inputs stay wide).

    Step 4 — Tax row JSX in Cost Breakdown. Locate the Cost Per Unit total block at line ~1305-1313 and the Marketplace Fee Note at line ~1316. Insert the Tax row block AFTER the Cost Per Unit total's closing `</div>` and BEFORE the Marketplace Fee Note. Wrap the entire block in `{tax.ratePercent > 0 && ( ... )}` per UI-SPEC line 152.

    The wrapper is `<div className="border-t border-slate-700 pt-2 mt-2">` (mirrors Subtotal/Failure wrapper).

    Inside, two rows:
    Row 1 (Tax line):
    `<div className="flex justify-between text-slate-300">`
    `  <span className="flex items-center gap-1.5">`
    `    <span>Tax ({tax.ratePercent.toFixed(1)}%)</span>`
    `    <InfoTooltip text={tooltipForSource(taxSource, userCurrency)} />`
    `  </span>`
    `  <span className="font-mono">{currencySymbol}{tax.taxAmount.toFixed(2)}</span>`
    `</div>`

    NOTE: `tooltipForSource(taxSource, userCurrency)` — the second `userCurrency` argument is REQUIRED for D-12 + D-13 (US marketplace-facilitator note persists on override / settings / eu-average / manual branches, not just region). `userCurrency` is already a prop on CostCalculator and is in scope here.

    Row 2 (Total with Tax):
    `<div className="flex justify-between text-white font-semibold mt-1">`
    `  <span>Total (with Tax)</span>`
    `  <span className="font-mono">{currencySymbol}{(sellingPrice + tax.taxAmount).toFixed(2)}</span>`
    `</div>`

    The `.toFixed(1)` on the percentage is the D-07 lock (one decimal). The `.toFixed(2)` on the amount is consistent with the rest of the Cost Breakdown. `currencySymbol` is the existing local variable at line 56.

    Step 5 — handleSaveJob persistence. Locate the two branches in handleSaveJob:
    - Update branch (lines ~506-525): the `const updatedJob: PrintJob = { ...editingJob, name: printName.trim(), ..., sellingPrice };` object literal. ADD `taxRate: taxRateOverride` and `taxAmount: tax.taxAmount` to this object spread.
    - Create branch (lines ~534-553): the `const job: PrintJob = { id: ..., ..., sellingPrice, copiesSold: 0 };` object literal. ADD `taxRate: taxRateOverride` and `taxAmount: tax.taxAmount` to this object spread.

    Both branches persist `taxRate` (potentially undefined when the user hasn't set an override) and `taxAmount` (always a number from calculateTax — 0 when rate is 0). Per RESEARCH line 72, `taxAmount` is the persisted derived value so historic jobs reproduce their math even if regional rates change later.

    DO NOT modify other parts of handleSaveJob (filaments, materialsUsed, etc.). DO NOT delete the existing onUpdateJob / onSaveJob calls.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/13-05-task2.tsc.log; ! grep -q "error TS" /tmp/13-05-task2.tsc.log; npx vitest run src/utils/costCalc.test.ts src/utils/taxResolution.test.ts 2>&1 | tee /tmp/13-05-task2.test.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "useState<number | undefined>" src/components/CostCalculator.tsx` matches (taxRateOverride state)
    - `grep -F "setTaxRateOverride(editingJob.taxRate)" src/components/CostCalculator.tsx` matches (load-from-editingJob path)
    - `grep -F "jobOverride: taxRateOverride" src/components/CostCalculator.tsx` matches (resolveTaxRate input wired to local state, not editingJob directly)
    - `grep -F "md:grid-cols-4" src/components/CostCalculator.tsx` matches (Set Financial Targets row expanded to 4 columns)
    - `grep -F "tax.ratePercent.toFixed(1)" src/components/CostCalculator.tsx` matches (D-07: 1 decimal display)
    - `grep -F "Total (with Tax)" src/components/CostCalculator.tsx` matches (UI-SPEC locked label)
    - `grep -F "tooltipForSource(taxSource, userCurrency)" src/components/CostCalculator.tsx` matches (the InfoTooltip wire-up with the required userCurrency argument for D-12 + D-13)
    - `grep -F "tax.ratePercent > 0" src/components/CostCalculator.tsx` matches (TAX-04: hide-at-0% gate)
    - `grep -F "(sellingPrice + tax.taxAmount).toFixed(2)" src/components/CostCalculator.tsx` matches (TAX-04: total = sellingPrice + taxAmount)
    - `grep -c "taxRate: taxRateOverride" src/components/CostCalculator.tsx` returns at least 2 (one in each handleSaveJob branch)
    - `grep -c "taxAmount: tax.taxAmount" src/components/CostCalculator.tsx` returns at least 2 (one in each handleSaveJob branch)
    - `npx tsc -b` exits 0
    - `npx vitest run src/utils/costCalc.test.ts src/utils/taxResolution.test.ts` exits 0 — Plan 01 + Plan 02 tests still pass (no util regressions)
  </acceptance_criteria>
  <done>
    The per-job Tax Rate Input lives in the Set Financial Targets block as the 4th column (wide, with `%` prefix overlay). The Tax row + Total (with Tax) row render after Cost Per Unit when tax.ratePercent > 0 and hide otherwise. handleSaveJob persists taxRate and taxAmount in both update and create branches. utils tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sweep 3 numeric inputs to compact + remove 3 stale NewBadge JSX sites in CostCalculator</name>
  <files>src/components/CostCalculator.tsx</files>
  <read_first>
    - src/components/CostCalculator.tsx (lines 748, 756, 1016 for the 3 NewBadge JSX sites; lines 922-928, 970-981, 988-996 for the 3 numeric Inputs)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 414-435 (UI Sweep Inventory for CostCalculator — strong-yes inputs: Materials Used Quantity, Shipping Distance, Carrier Cost)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 539-547 (NEW Badge Audit — confirms `per-unit-licensing`, `author-min-price`, `packaging-materials` keys are stale)
    - .planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md lines 196-198 (removal sites + "no `relative` cleanup needed" guidance for each)
    - .planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md lines 398-410 (CostCalculator NewBadge removal sites + retained `model-url` import rationale)
  </read_first>
  <action>
    Step 1 — Add `compact` to 3 numeric Inputs:
    1. Materials Used Quantity (line ~922-928): the current Input has `className="w-20 text-right"`. Replace with `compact className="text-right"` — preserve right-align, drop the explicit `w-20` since `compact` caps at `max-w-28`.
    2. Shipping Distance (line ~970-981): add `compact` prop to the existing `<Input type="number" ... />`. Keep all other props. UI-SPEC discretion: optionally add `<InfoTooltip text="Round-trip distance" />` to the label by rewriting the label from `block` to `flex items-center gap-1.5` with a `<span>` + InfoTooltip — RESEARCH calls this optional. Planner picks YES (do the tooltip) — D-16 says consistency wins.
    3. Carrier Cost (line ~988-996): add `compact` prop. The Input's value is a computed default — keep the placeholder if present.

    Step 2 — Remove 3 stale NewBadge JSX sites:
    1. Line ~748: delete `<NewBadge feature="per-unit-licensing" />` — the surrounding `<label className="flex items-center gap-2 mt-2 cursor-pointer ...">` keeps its `<span>` + checkbox after the badge is removed. UI-SPEC line 196: no `relative` cleanup needed.
    2. Line ~756: delete `<NewBadge feature="author-min-price" />` — the surrounding `<label className="flex items-center gap-1.5 text-xs text-slate-400 mb-1">` keeps its `<span>` + `<InfoTooltip>` after the badge is removed. UI-SPEC line 197: no `relative` cleanup needed.
    3. Line ~1016: delete `<NewBadge feature="packaging-materials" />` — the surrounding `<h3 className="flex items-center gap-2 text-sm font-medium text-white">` keeps its text after the badge is removed. UI-SPEC line 198: no `relative` cleanup needed.

    Step 3 — Verify NewBadge import is RETAINED. After Step 2 there should still be ONE `<NewBadge feature="model-url" />` JSX site at line ~713 (the Model URL label — RESEARCH NEW Badge Audit confirms `model-url` is fresh, not stale). The import line `import { NewBadge } from './NewBadge';` MUST remain in the file. Verify by greppping the file post-change.

    Step 4 — DO NOT touch other CostCalculator content: do not modify other Inputs that are already compact, do not change the Profit Margin / Target Profit / Selling Price wide inputs (UI-SPEC lock — these are primary financial inputs), do not modify the filament rows, do not touch the new per-job Tax Rate Input or Tax row added in Task 2.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/13-05-task3.tsc.log; ! grep -q "error TS" /tmp/13-05-task3.tsc.log</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -F "feature=\"per-unit-licensing\"" src/components/CostCalculator.tsx` (badge JSX removed)
    - `! grep -F "feature=\"author-min-price\"" src/components/CostCalculator.tsx` (badge JSX removed)
    - `! grep -F "feature=\"packaging-materials\"" src/components/CostCalculator.tsx` (badge JSX removed)
    - `grep -F "feature=\"model-url\"" src/components/CostCalculator.tsx` matches (fresh badge retained)
    - `grep "import.*NewBadge.*from" src/components/CostCalculator.tsx` matches (import retained because model-url still uses it)
    - The 3 newly-swept inputs contain `compact`: verify by reading line ranges 922-928 (Materials Used Quantity), 970-981 (Shipping Distance), 988-996 (Carrier Cost). Each should contain the literal `compact` prop.
    - `! grep -F "className=\"w-20 text-right\"" src/components/CostCalculator.tsx` if the Materials Used Quantity was the only `w-20 text-right` in the file (executor verifies this is the only such instance; otherwise scope by surrounding code)
    - If the planner picked YES on the Shipping Distance InfoTooltip: `grep -F "Round-trip distance" src/components/CostCalculator.tsx` matches
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>
    3 numeric Inputs in CostCalculator have `compact`. 3 stale NewBadge JSX sites are gone. `model-url` badge and the NewBadge import remain. File compiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-job tax rate input → PrintJob persistence | Local controlled input clamped 0..99.9, persisted to IndexedDB via Dexie. No network. |
| editingJob.taxRate read | Comes from the Dexie record persisted by this app; user's own local store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-14 | Tampering | Tax rate numeric range on per-job input | mitigate | `Math.min(Math.max(parsed, 0), 99.9)` clamp + `Number.isFinite(parsed)` guard mirrors Default Tax Rate handling from Plan 03 + the existing Profit Margin pattern; out-of-range inputs cannot persist |
| T-13-15 | Tampering | calculateTax order-of-operations | mitigate | The Tax row reads `tax.taxAmount` from `calculateTax(sellingPrice, taxSource.rate)` — tax is multiplied by sellingPrice, NEVER by subtotal or failureAdjusted; Plan 02's order-of-operations guard test catches drift |
| T-13-16 | Repudiation | Historic job tax recomputation | mitigate | handleSaveJob persists `taxAmount: tax.taxAmount` alongside `taxRate` — the saved amount is the source of truth for past jobs; future region-table changes won't silently rewrite historic numbers |
| T-13-17 | Information Disclosure | tooltipForSource leaking note text including US marketplace-facilitator string | accept | The note text is locked UI copy explaining tax policy; not sensitive data; same string appears in Settings and Cost Breakdown by D-13 design |
| T-13-18 | Tampering | XSS via Tax row label or tooltip text | mitigate | All strings rendered are React JSX text children with default escaping; tooltipForSource returns string literals composed from static data + numbers; no dangerouslySetInnerHTML |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan. RESEARCH.md "Package Legitimacy Audit" confirms zero new packages. Auto-accept. |
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx vitest run src/utils/costCalc.test.ts src/utils/taxResolution.test.ts` exits 0 (no util regressions)
- `npx vitest run` (full suite) exits 0 (broader regression safety)
- `npx vitest run src/utils/costCalc.test.ts -t "order-of-operations"` exits 0 (TAX-05 order-of-operations guard remains green — this plan didn't break the math)
- `grep -F "calculateTax(sellingPrice, taxSource.rate)" src/components/CostCalculator.tsx` matches (TAX-04 + TAX-05 order-of-operations: tax computed on sellingPrice, NOT subtotal)
- Per-job Tax Rate Input is wide (no `compact` on it): verify by reading the 4th column of the Set Financial Targets grid
- `grep -c "feature=\"per-unit-licensing\"\|feature=\"author-min-price\"\|feature=\"packaging-materials\"" src/components/CostCalculator.tsx` returns 0
- Manual UAT (deferred): Set sellingPrice=10, per-job Tax Rate=20%, confirm Tax row shows 2.00 and Total (with Tax) shows 12.00; clear the per-job rate, confirm Tax row reverts to Settings default or region rate; set sellingPrice non-zero with 0% rate, confirm Tax row hides entirely; reopen a saved job and confirm the per-job tax rate is restored from the persisted record (TAX-02 round-trip).
</verification>

<success_criteria>
- 2 modified files committed: src/components/CostCalculator.tsx, src/App.tsx
- TAX-02 wired end-to-end: per-job Tax Rate Input → local state → useMemo → handleSaveJob persistence → editingJob round-trip
- TAX-04 wired end-to-end: Tax row appears after Cost Per Unit when ratePercent > 0; total = sellingPrice + taxAmount; hidden at 0%
- TAX-05 reaffirmed: tax computed on sellingPrice via calculateTax (the function tested in Plan 02)
- UI-08 partial: 3 numeric inputs swept (Materials Used Quantity, Shipping Distance, Carrier Cost); the new per-job Tax Rate Input is intentionally wide
- UI-09 partial: optional Shipping Distance InfoTooltip added per D-16
- UI-10 partial: 3 stale NewBadge JSX sites removed; NewBadge import retained for model-url
</success_criteria>

<output>
Create `.planning/phases/13-tax-model-ui-sweep/13-05-SUMMARY.md` when done. Confirm the Tax row hides at 0%, the persistence round-trip works for both create and update paths, and the order-of-operations test still passes.
</output>
