---
phase: 13-tax-model-ui-sweep
plan: 06
type: execute
wave: 3
depends_on:
  - 13-03
  - 13-04
  - 13-05
files_modified:
  - src/components/UserProfileModal.tsx
  - src/components/GcodeImport.tsx
  - src/features.ts
autonomous: true
requirements:
  - UI-10
tags:
  - ui-10
  - newbadge-cleanup
  - feature-registry

must_haves:
  truths:
    - "UI-10: 3 remaining stale NewBadge JSX sites removed — `<NewBadge feature=\"multi-currency\" />` at UserProfileModal.tsx:~79, `<NewBadge feature=\"gcode-import\" />` at GcodeImport.tsx:~282, `<NewBadge feature=\"3mf-import\" />` at GcodeImport.tsx:~283"
    - "After JSX removals, both UserProfileModal.tsx and GcodeImport.tsx have zero `<NewBadge` references — the `import { NewBadge }` line is removed from each file (Pitfall 6: registry-only orphans avoided)"
    - "src/features.ts FINAL state has exactly 4 entries: `settings-reorg`, `default-profit-margin`, `model-url`, `default-tax-rate` (the new key Plan 03 emits JSX for); 9 stale entries removed (`per-unit-licensing`, `author-min-price`, `configurable-marketplace-fees`, `custom-carriers`, `multi-currency`, `packaging-materials`, `csv-import`, `gcode-import`, `3mf-import`)"
    - "The new `default-tax-rate` registry entry is `new Date('2026-05-21')` — today's date (the date Plan 03's JSX was authored)"
    - "Cross-repo sanity: `grep -rE 'NewBadge feature=\"(per-unit-licensing|author-min-price|configurable-marketplace-fees|custom-carriers|multi-currency|packaging-materials|csv-import|gcode-import|3mf-import)\"' src/` returns ZERO matches after this plan — VALIDATION row 19 at 13-VALIDATION.md:62 enforces this gate"
    - "tsc -b passes; all touched files compile"
  artifacts:
    - path: "src/components/UserProfileModal.tsx"
      provides: "Zero NewBadge references"
      contains: ""
    - path: "src/components/GcodeImport.tsx"
      provides: "Zero NewBadge references"
      contains: ""
    - path: "src/features.ts"
      provides: "4-entry featureReleases registry — settings-reorg, default-profit-margin, model-url, default-tax-rate"
      contains: "default-tax-rate"
  key_links:
    - from: "src/components/SettingsModal.tsx (Plan 03 NewBadge feature='default-tax-rate')"
      to: "src/features.ts (new 'default-tax-rate' entry)"
      via: "feature registry lookup"
      pattern: "'default-tax-rate'"
---

<objective>
Complete the UI-10 audit — remove the 3 remaining stale NewBadge JSX sites (UserProfileModal + GcodeImport), drop the NewBadge import from each file once they hit zero usages, prune `src/features.ts` from 12 entries down to the 4 fresh keys (including the new `default-tax-rate` key emitted by Plan 03), and verify cross-repo that no stale badge keys leak.

Purpose: This is the trailing cleanup wave. It runs AFTER Plans 03 (SettingsModal — emits the new `default-tax-rate` JSX badge), 04 (AssetLibrary — removes 2 NewBadge JSX sites + the AssetLibrary import), and 05 (CostCalculator — removes 3 NewBadge JSX sites). All file conflicts have already been resolved by sequencing; this plan owns the remaining files exclusively.

Output: 3 modified files. Strictly subtractive in two of them, plus the final registry edit in `src/features.ts`. No new features, no behavior changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-tax-model-ui-sweep/13-CONTEXT.md
@.planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md
@.planning/phases/13-tax-model-ui-sweep/13-PATTERNS.md
@.planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md
@.planning/phases/13-tax-model-ui-sweep/13-03-SUMMARY.md
@.planning/phases/13-tax-model-ui-sweep/13-04-SUMMARY.md
@.planning/phases/13-tax-model-ui-sweep/13-05-SUMMARY.md
@src/components/UserProfileModal.tsx
@src/components/GcodeImport.tsx
@src/features.ts
@src/components/NewBadge.tsx

<interfaces>
<!-- The final state of src/features.ts is locked. -->

Current src/features.ts:5-19 (pre-plan-06):
```typescript
export const featureReleases: Record<string, Date> = {
  'per-unit-licensing': new Date('2026-01-24'),
  'author-min-price': new Date('2026-01-24'),
  'configurable-marketplace-fees': new Date('2026-01-25'),
  'custom-carriers': new Date('2026-01-25'),
  'multi-currency': new Date('2026-01-25'),
  'packaging-materials': new Date('2026-01-25'),
  'settings-reorg': new Date('2026-05-20'),
  'csv-import': new Date('2026-02-14'),
  'gcode-import': new Date('2026-02-14'),
  '3mf-import': new Date('2026-04-15'),
  'default-profit-margin': new Date('2026-05-18'),
  'model-url': new Date('2026-05-20'),
};
```

Target src/features.ts (post-plan-06) — exactly 4 entries per UI-SPEC line 207:
```typescript
export const featureReleases: Record<string, Date> = {
  'settings-reorg': new Date('2026-05-20'),
  'default-profit-margin': new Date('2026-05-18'),
  'model-url': new Date('2026-05-20'),
  'default-tax-rate': new Date('2026-05-21'),
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove NewBadge JSX + import from UserProfileModal.tsx and GcodeImport.tsx</name>
  <files>src/components/UserProfileModal.tsx, src/components/GcodeImport.tsx</files>
  <read_first>
    - src/components/UserProfileModal.tsx (line ~79 for the `<NewBadge feature="multi-currency" />` JSX site; top of file for the import line)
    - src/components/GcodeImport.tsx (lines ~282-283 for the two NewBadge JSX sites; top of file for the import line)
    - src/components/NewBadge.tsx (component definition — verify the import path being removed: `from './NewBadge'`)
    - .planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md lines 199, 202-205 (locked guidance: verify each parent for stray `relative` classes; drop the import once zero usages remain)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 386-394 (Pitfall 6: always remove JSX first, then grep, then drop the import)
  </read_first>
  <action>
    Edit src/components/UserProfileModal.tsx:
    1. At line ~79, delete the entire `<NewBadge feature="multi-currency" />` JSX element (self-closing tag). Verify the parent JSX (whatever container the badge was inside): if the parent has a `relative` class that exists SOLELY to host the badge, remove it. If the parent uses `relative` for unrelated layout (the badge was a child but the parent's positioning serves other content), leave `relative` alone. UI-SPEC line 199 says "Planner must verify placement" — executor reads the surrounding 10 lines to make the call.
    2. After Step 1, grep the file for any remaining `NewBadge` references. Expect zero. Delete the `import { NewBadge } from './NewBadge';` line at the top of the file.

    Edit src/components/GcodeImport.tsx:
    1. At line ~282, delete `<NewBadge feature="gcode-import" />`.
    2. At line ~283, delete `<NewBadge feature="3mf-import" />`.
    3. Verify the parent JSX for both: UI-SPEC says "Verify parent" — read surrounding 10 lines to confirm no stray `relative` cleanup required.
    4. After Steps 1-2, grep the file for any remaining `NewBadge` references. Expect zero. Delete the `import { NewBadge } from './NewBadge';` line.

    DO NOT modify any other content in either file. DO NOT touch the underlying field labels or imports unrelated to NewBadge.

    If either file (UserProfileModal or GcodeImport) has additional NewBadge JSX sites the RESEARCH inventory missed, leave them alone and surface as a deviation in the SUMMARY — do not unilaterally remove un-audited sites.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tee /tmp/13-06-task1.tsc.log; ! grep -q "error TS" /tmp/13-06-task1.tsc.log</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -n "NewBadge" src/components/UserProfileModal.tsx` returns zero matches (no JSX, no import)
    - `! grep -n "NewBadge" src/components/GcodeImport.tsx` returns zero matches (no JSX, no import)
    - `! grep -F "feature=\"multi-currency\"" src/components/UserProfileModal.tsx`
    - `! grep -F "feature=\"gcode-import\"" src/components/GcodeImport.tsx`
    - `! grep -F "feature=\"3mf-import\"" src/components/GcodeImport.tsx`
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>
    UserProfileModal.tsx and GcodeImport.tsx have zero NewBadge references (JSX or import). Both files compile.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prune src/features.ts to 4 fresh entries (add default-tax-rate; remove 9 stale)</name>
  <files>src/features.ts</files>
  <read_first>
    - src/features.ts (full file — 27 lines, registry at lines 5-19, age constants at lines 23-26)
    - .planning/phases/13-tax-model-ui-sweep/13-UI-SPEC.md lines 207, 342 (target final state — exactly 4 entries)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 568-589 (NEW Badge Audit conclusion — 9 stale keys to delete, 3 fresh keys to keep, 1 new key to add)
    - .planning/phases/13-tax-model-ui-sweep/13-RESEARCH.md lines 386-394 (Pitfall 6: only delete a registry entry AFTER all JSX consumers are gone — verify in this task's verify gate)
  </read_first>
  <action>
    BEFORE editing the registry, verify there are no remaining JSX consumers for the 9 stale keys. Run a cross-repo grep:
    `grep -rE 'NewBadge feature="(per-unit-licensing|author-min-price|configurable-marketplace-fees|custom-carriers|multi-currency|packaging-materials|csv-import|gcode-import|3mf-import)"' src/`

    Expected result: zero matches. If ANY of the 9 stale keys still has a JSX consumer at this point, STOP. The pre-requisite plans (03, 04, 05, 06-Task-1) didn't fully complete the JSX removal; surface the leak in the SUMMARY and do not proceed with the registry deletion (Pitfall 6).

    If the cross-repo grep passes (zero matches), edit src/features.ts:
    - Delete the 9 stale entries:
      `'per-unit-licensing': new Date('2026-01-24'),`
      `'author-min-price': new Date('2026-01-24'),`
      `'configurable-marketplace-fees': new Date('2026-01-25'),`
      `'custom-carriers': new Date('2026-01-25'),`
      `'multi-currency': new Date('2026-01-25'),`
      `'packaging-materials': new Date('2026-01-25'),`
      `'csv-import': new Date('2026-02-14'),`
      `'gcode-import': new Date('2026-02-14'),`
      `'3mf-import': new Date('2026-04-15'),`
    - ADD one new entry: `'default-tax-rate': new Date('2026-05-21'),`
    - Retain three existing fresh entries: `'settings-reorg'`, `'default-profit-margin'`, `'model-url'`.

    The final featureReleases object has exactly 4 entries in any order. The trailing `// Add new features here with their release date` comment line may be retained or removed at executor discretion (cosmetic).

    DO NOT modify `NEW_FEATURE_MAX_AGE_DAYS` (lines 21-23) or `NEW_FEATURE_SEEN_HOURS` (lines 25-26) — those are tuning constants, not part of UI-10 scope.

    DO NOT modify any other file in this task.
  </action>
  <verify>
    <automated>
    npx tsc -b 2>&1 | tee /tmp/13-06-task2.tsc.log;
    ! grep -q "error TS" /tmp/13-06-task2.tsc.log &&
    ! grep -rE 'NewBadge feature="(per-unit-licensing|author-min-price|configurable-marketplace-fees|custom-carriers|multi-currency|packaging-materials|csv-import|gcode-import|3mf-import)"' src/ &&
    [ "$(grep -v '^//\|^\s*//' src/features.ts | grep -cE "new Date\(")" -eq 4 ] || echo "feature count mismatch"
    </automated>
  </verify>
  <acceptance_criteria>
    - VALIDATION row 19 satisfied: `! grep -rE 'NewBadge feature="(per-unit-licensing|author-min-price|configurable-marketplace-fees|custom-carriers|multi-currency|packaging-materials|csv-import|gcode-import|3mf-import)"' src/` returns zero matches
    - VALIDATION row 20 satisfied: `grep -v '^\s*//' src/features.ts | grep -cE "new Date\(" ` returns exactly 4 (strip comment lines then count). Use this filtered count rather than naive `grep -c "new Date("` to avoid counting commented references.
    - `grep -F "'default-tax-rate': new Date('2026-05-21')" src/features.ts` matches (the new entry with today's date)
    - `grep -F "'settings-reorg':" src/features.ts` matches (retained)
    - `grep -F "'default-profit-margin':" src/features.ts` matches (retained)
    - `grep -F "'model-url':" src/features.ts` matches (retained)
    - `! grep -F "'per-unit-licensing':" src/features.ts` (deleted)
    - `! grep -F "'author-min-price':" src/features.ts` (deleted)
    - `! grep -F "'configurable-marketplace-fees':" src/features.ts` (deleted)
    - `! grep -F "'custom-carriers':" src/features.ts` (deleted)
    - `! grep -F "'multi-currency':" src/features.ts` (deleted)
    - `! grep -F "'packaging-materials':" src/features.ts` (deleted)
    - `! grep -F "'csv-import':" src/features.ts` (deleted)
    - `! grep -F "'gcode-import':" src/features.ts` (deleted)
    - `! grep -F "'3mf-import':" src/features.ts` (deleted)
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>
    src/features.ts has exactly 4 entries (settings-reorg, default-profit-margin, model-url, default-tax-rate). Cross-repo grep for stale keys returns zero matches. File compiles. UI-10 audit is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/features.ts registry → NewBadge runtime | NewBadge looks up the feature key against featureReleases at render time. Removing a stale entry has no negative effect because the JSX consumer has already been deleted. |
| Cross-repo grep gate | Functions as a coherency check: registry edits cannot land before JSX consumers are gone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-19 | Tampering | Orphaned registry entry vs orphaned JSX site | mitigate | The cross-repo grep gate in Task 2 verifies zero stale JSX consumers BEFORE deleting registry entries; Pitfall 6 is the exact failure mode addressed |
| T-13-20 | Information Disclosure | Stale NewBadge leaking past `NEW_FEATURE_MAX_AGE_DAYS` | mitigate | Removing the JSX sites (Plans 03/04/05 + this plan's Task 1) prevents any stale badge from rendering even if the user's localStorage somehow had an old "seen" timestamp |
| T-13-21 | Denial of Service | NewBadge component called with a key not in featureReleases | accept | NewBadge.tsx already returns null when the key is absent — safe fallback. Adding `default-tax-rate` to the registry closes this gap intentionally for Plan 03's JSX. |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan. RESEARCH.md "Package Legitimacy Audit" confirms zero new packages. Auto-accept. |
</threat_model>

<verification>
- `npx tsc -b` exits 0
- All three files compile after edits
- Cross-repo gate (VALIDATION row 19): `! grep -rE 'NewBadge feature="(per-unit-licensing|author-min-price|configurable-marketplace-fees|custom-carriers|multi-currency|packaging-materials|csv-import|gcode-import|3mf-import)"' src/` returns zero
- features.ts entry-count gate (VALIDATION row 20): exactly 4 non-comment `new Date(` lines in src/features.ts
- UserProfileModal.tsx and GcodeImport.tsx each have zero NewBadge references
- Plan 03's `<NewBadge feature="default-tax-rate" />` in SettingsModal now resolves to a valid registry entry — the badge renders for users who installed/opened the app within the 14-day window of 2026-05-21
- Manual UAT (deferred to phase wrap-up): open the app; confirm no stale "NEW" badges appear on per-unit licensing, author min price, packaging materials, csv import, gcode import, 3mf import, multi-currency. Confirm new "NEW" badge appears on Default Tax Rate in Settings → Costs & Rates.
</verification>

<success_criteria>
- 3 modified files committed: src/components/UserProfileModal.tsx, src/components/GcodeImport.tsx, src/features.ts
- UI-10 fully complete: all 11 JSX sites removed (2 in AssetLibrary via Plan 04; 3 in CostCalculator via Plan 05; 1 in UserProfileModal + 2 in GcodeImport via this plan; the 3 already-orphaned registry entries with no JSX consumers — `configurable-marketplace-fees`, `custom-carriers` — are also pruned)
- features.ts contains exactly 4 entries: settings-reorg, default-profit-margin, model-url, default-tax-rate
- Plan 03's emitted JSX badge for default-tax-rate now has a valid registry entry → the badge renders
</success_criteria>

<output>
Create `.planning/phases/13-tax-model-ui-sweep/13-06-SUMMARY.md` when done. Confirm the cross-repo grep gate passed BEFORE the features.ts edit; record the final 4-entry registry contents; flag any unexpected NewBadge sites encountered.
</output>
