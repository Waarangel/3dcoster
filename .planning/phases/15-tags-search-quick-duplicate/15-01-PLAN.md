---
phase: 15-tags-search-quick-duplicate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/backfill.ts
  - src/db/backfill.test.ts
autonomous: true
requirements: [TAGS-01]
requirements_addressed: [TAGS-01]
tags: [tags, normalization, reconcile, pure-helper]
user_setup: []
must_haves:
  truths:
    - "Legacy tag arrays with uppercase/untrimmed/punctuation values get normalized to canonical form (lowercase + trim + dedupe + cap-at-10 + whitelist) on next app load"
    - "Normalization is idempotent: running normalizeTagsOnJob a second time on already-canonical tags returns false and does not mutate"
    - "normalizeTagsOnJob is safe to import in jsdom (no Dexie side effect at module load)"
  artifacts:
    - path: "src/db/backfill.ts"
      provides: "exported normalizeTagsOnJob(job): boolean pure helper sibling to backfillTagsOnJob"
      contains: "export function normalizeTagsOnJob"
    - path: "src/db/backfill.test.ts"
      provides: "Vitest describe block 'normalizeTagsOnJob (Phase 15 D-12)' with 6 cases"
      contains: "normalizeTagsOnJob (Phase 15 D-12)"
  key_links:
    - from: "src/db/backfill.ts::normalizeTagsOnJob"
      to: "src/db/backfill.test.ts"
      via: "Vitest import"
      pattern: "import.*normalizeTagsOnJob.*from.*backfill"
    - from: "normalizeTagsOnJob normalization rules"
      to: "D-02 character whitelist regex /[^a-z0-9\\s\\-_]/g + cap=10"
      via: "shared transform mirrored in parseTagsInput (Plan 15-03)"
      pattern: "replace\\(/\\[\\^a-z0-9.*\\]/g"
---

<objective>
Implement the `normalizeTagsOnJob` pure helper in `src/db/backfill.ts` per CONTEXT D-12 (the [[reconcile-legacy-data]] memory rule applied to D-02's new normalization constraints), with a complete Vitest describe block mirroring the existing `backfillTagsOnJob` test structure.

Purpose: Phase 12's `backfillTagsOnJob` already set `tags = []` for v5-era jobs but never enforced D-02's normalization rules (they didn't exist yet). Any user with hand-edited tags via DevTools could carry uppercase or untrimmed values that the chip filter (Plan 15-04) would treat as distinct from their normalized equivalents. This plan ships the pure helper; Plan 15-05 wires it into `useJobs` init.

Output: One new exported function in `src/db/backfill.ts`, one new describe block in `src/db/backfill.test.ts`, all existing tests still passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md
@.planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md
@src/db/backfill.ts
@src/db/backfill.test.ts

<interfaces>
<!-- Existing sibling in same file — mirror JSDoc + purity contract exactly -->
From src/db/backfill.ts:1-30 (backfillTagsOnJob convention):
- No imports from `dexie` or `./database` — jsdom-safe
- Pure function, in-place mutation, no return for the set-default variant
- JSDoc Examples: block enumerates input → output pairs
- Array.isArray guard handles all DevTools-injected corruption shapes (string/null/number)

From src/db/backfill.test.ts:1-3:
- import { describe, it, expect } from 'vitest';
- import { backfillTagsOnJob, ... } from './backfill';
- import type { PrintJob, ... } from '../types';

From src/db/backfill.test.ts:5-35 (backfillTagsOnJob describe block):
- Six it() cases: missing field, existing array preserved, string/null/number corruption modes
- Asserts toEqual([]) after mutation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add normalizeTagsOnJob pure helper to src/db/backfill.ts</name>
  <files>src/db/backfill.ts</files>
  <read_first>
    - src/db/backfill.ts (full file — to see backfillTagsOnJob JSDoc convention at lines 1-30 and the pure-helper module rules at lines 11-19)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 122-127 (D-12 lock — explains why this helper exists AND why it must mirror parseTagsInput rules)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 37-44 (D-02 normalization rule set: lowercase + trim + dedupe via Set + cap=10 + whitelist regex `/[^a-z0-9\s\-_]/g`)
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 187-252 (analog mapping for backfill.ts MODIFY — the `normalizeTagsOnJob` template with rationale)
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 715-729 (pure-helper module convention — non-negotiable jsdom-safety rule, no Dexie imports)
  </read_first>
  <action>
    Per D-12, append a new exported function `normalizeTagsOnJob(job: { tags?: string[] }): boolean` to `src/db/backfill.ts`, immediately after the existing `backfillTagsOnJob` block (before `backfillQuotesFromJobs`). Per D-02 the transform pipeline is: for each entry in `job.tags`, when typeof === 'string', apply `raw.trim().toLowerCase().replace(/[^a-z0-9\s\-_]/g, '')`; drop entries that become empty; dedupe via a `Set<string>` (case-insensitive after lowercase); push into a `cleaned` array; break early when `cleaned.length >= 10` (the D-02 cap-at-10). Return `false` (no mutation) when `job.tags` is not an array (the Phase 12 `backfillTagsOnJob` hasn't run yet — leave the undefined state alone). Implement an idempotency short-circuit: when `cleaned.length === job.tags.length` AND `cleaned.every((t, i) => t === job.tags![i])`, return `false` without assigning. Otherwise assign `job.tags = cleaned` and return `true` (the caller uses this to decide whether to bulkPut). JSDoc MUST mirror `backfillTagsOnJob`'s Examples block style — enumerate 6 cases verbatim from PATTERNS.md lines 224-229 (the `['PLA', ' phone-stand ']` → `['pla', 'phone-stand']` example, the all-duplicate case, the no-op case, the undefined case, etc.). JSDoc MUST cite "Phase 15 D-12 reconcile (per [[reconcile-legacy-data]])" in the first line. Per the PATTERNS.md jsdom-safety rule: do NOT add any imports — re-use the existing `import type { PrintJob, ... }` at the top of the file is sufficient (the function signature uses the structural type `{ tags?: string[] }`, no PrintJob import needed). DO NOT touch `backfillTagsOnJob`, `backfillQuotesFromJobs`, `backfillCustomersFromSales`, or `reconcileCopiesSoldFromSales`.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -20 && grep -c "export function normalizeTagsOnJob" src/db/backfill.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function normalizeTagsOnJob" src/db/backfill.ts` returns `1`
    - `grep -c "Phase 15 D-12" src/db/backfill.ts` returns at least `1` (JSDoc cite)
    - `grep -c "reconcile-legacy-data" src/db/backfill.ts` returns at least `1`
    - `grep "/\[\^a-z0-9.*\]/g" src/db/backfill.ts` matches the D-02 whitelist regex literal
    - `grep -c "cleaned.length >= 10" src/db/backfill.ts` returns `1` (D-02 cap)
    - `grep -c "import.*dexie\|import.*database" src/db/backfill.ts | grep -c "0"` — confirms zero dexie/database imports added (jsdom safety preserved)
    - `npx tsc -b` exits 0
    - `backfillTagsOnJob` function definition is unchanged (verified by re-reading lines 28-30: still `if (!Array.isArray(job.tags)) job.tags = [];`)
  </acceptance_criteria>
  <done>normalizeTagsOnJob exists as a pure exported function in src/db/backfill.ts with the D-02 transform pipeline, the D-12 cite in JSDoc, the idempotency short-circuit, and zero Dexie imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add normalizeTagsOnJob describe block to src/db/backfill.test.ts</name>
  <files>src/db/backfill.test.ts</files>
  <read_first>
    - src/db/backfill.test.ts lines 1-35 (existing `backfillTagsOnJob` describe block — mirror its structure verbatim)
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 259-301 (the locked test template for the new describe block — 6 it() cases)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 37-44 (D-02 — the exact rules the tests assert against)
  </read_first>
  <action>
    Update the import on line 2 to add `normalizeTagsOnJob` to the destructured import list (alphabetical order between `backfillTagsOnJob` and `backfillQuotesFromJobs` is acceptable, or append at the end of the list — match the existing comma-separated style). Append a new `describe('normalizeTagsOnJob (Phase 15 D-12)', () => { ... })` block at the end of the file (AFTER the existing `reconcileCopiesSoldFromSales` describe block, do not split files or move existing describes). The describe MUST contain six it() cases verbatim from PATTERNS.md lines 263-300: (a) `'lowercases + trims existing tags'` — input `{ tags: ['PLA', '  Phone-Stand  '] }`, expect `changed === true` and `job.tags` deep-equal `['pla', 'phone-stand']`; (b) `'dedupes case-insensitively'` — input `['pla', 'PLA', 'Pla']`, expect `['pla']`; (c) `'strips emoji and punctuation via /[^a-z0-9\\s\\-_]/g whitelist'` — input `['pla!!', 'phone💀stand', '@@@']`, expect `['pla', 'phonestand']` (`'@@@'` strips to empty and is dropped); (d) `'caps at 10 tags, silently dropping the rest'` — input is `Array.from({ length: 15 }, (_, i) => \`tag${i}\`)`, expect `job.tags.length === 10`; (e) `'returns false (no mutation) when already canonical'` — input `['pla', 'phone-stand']`, expect `changed === false` AND `job.tags` still deep-equal `['pla', 'phone-stand']`; (f) `'returns false when tags is undefined (Phase 12 backfill not yet run)'` — input `{}`, expect `changed === false`. Use the type annotation `{ tags?: string[] }` for the test fixture (Record<string, unknown> is NOT required here — the helper signature is structural).
  </action>
  <verify>
    <automated>npm test -- --run src/db/backfill.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe('normalizeTagsOnJob" src/db/backfill.test.ts` returns `1`
    - `grep -c "it(" src/db/backfill.test.ts` returns the prior count + 6 (six new cases added)
    - `grep -c "normalizeTagsOnJob" src/db/backfill.test.ts | tr -d ' '` returns at least `7` (one import + six call sites)
    - All 6 new test cases pass — `npm test -- --run src/db/backfill.test.ts` reports a passing run with the prior count + 6 it blocks
    - The existing `backfillTagsOnJob`, `backfillQuotesFromJobs`, `backfillCustomersFromSales`, `reconcileCopiesSoldFromSales` describe blocks are untouched (line-count of those regions unchanged)
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>All 6 new tests pass; total Vitest count for backfill.test.ts has grown by exactly 6; existing tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB → app memory | Legacy `tags` arrays read at app load; could carry user-injected (via DevTools) uppercase/punctuation/emoji values that violate D-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | normalizeTagsOnJob input (legacy/DevTools tag arrays) | mitigate | D-02 whitelist `/[^a-z0-9\s\-_]/g` strips all punctuation/emoji BEFORE the value enters the chip filter or any render path; `Array.isArray` guard rejects non-array shapes silently (returns false) |
| T-15-02 | Information Disclosure | normalizeTagsOnJob — accidental import of Dexie | accept | PATTERNS.md jsdom-safety rule enforces zero Dexie imports; verified by grep gate in acceptance criteria. No new network or filesystem surface introduced. |
</threat_model>

<verification>
- `npx tsc -b` clean exit
- `npm test -- --run src/db/backfill.test.ts` reports all backfill.test.ts cases passing (existing + 6 new = pre-existing baseline + 6)
- `grep -c "export function normalizeTagsOnJob" src/db/backfill.ts` returns 1
- `grep -c "describe('normalizeTagsOnJob" src/db/backfill.test.ts` returns 1
</verification>

<success_criteria>
- `normalizeTagsOnJob(job)` is exported from `src/db/backfill.ts` per D-12
- Function applies D-02 rules (lowercase + trim + dedupe + whitelist + cap=10) and returns true ONLY when it mutated `job.tags`
- Returns false on undefined / already-canonical input (idempotent — meets [[reconcile-legacy-data]] re-run safety)
- Six new tests pass under Vitest, covering all D-02 transform branches
- No Dexie import added — module remains jsdom-safe per project pure-helper convention
- ROADMAP Success Criterion #1 (tags lowercased/trimmed/deduped/capped) is now provably enforced at the pure-helper layer for both the input parser (Plan 15-03) and the reconcile path (Plan 15-05)
</success_criteria>

<output>
Create `.planning/phases/15-tags-search-quick-duplicate/15-01-SUMMARY.md` when done. Include: helper signature, 6 test case names with pass/fail, exact line ranges added in both files, baseline+new Vitest count for backfill.test.ts, and a one-line note confirming `backfillTagsOnJob` is unchanged.
</output>
