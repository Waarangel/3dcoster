---
phase: 15-tags-search-quick-duplicate
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/db/backfill.ts
  - src/components/CostCalculator.tsx
autonomous: true
requirements: [TAGS-01]
requirements_addressed: [TAGS-01]
tags: [tags, cost-calculator, input, parse]
user_setup: []
must_haves:
  truths:
    - "User can type a comma-separated tag string in the CostCalculator save form alongside Print Name / Model URL"
    - "On Save (both Create and Update branches), tags are parsed via the shared parseTagsInput helper that applies the SAME D-02 normalization rules as normalizeTagsOnJob (lowercase + trim + dedupe + cap-at-10 + whitelist /[^a-z0-9\\s\\-_]/g)"
    - "Multi-word tags are preserved: comma is the only delimiter; spaces inside a tag are kept (e.g. 'phone stand' is ONE tag)"
    - "Empty input → tags: undefined on the saved job (NOT [] — preserves 'no tags ever set' semantic per D-02 line 43)"
    - "When user types more than 10 tags, a small inline warning appears under the input: 'Maximum 10 tags — extras ignored' (D-02 silent-cap behavior surfaces a single non-blocking warning)"
    - "When editing an existing job, the tag input is pre-filled with the job's current tags joined by ', ' (comma-space)"
  artifacts:
    - path: "src/db/backfill.ts"
      provides: "exported parseTagsInput(raw: string): string[] | undefined that wraps the D-02 transform — single source of truth shared with normalizeTagsOnJob"
      contains: "export function parseTagsInput"
    - path: "src/components/CostCalculator.tsx"
      provides: "Tag input field with NewBadge + InfoTooltip in identity row; tagsInput local state; tags wired into both Update and Create job literals at lines 572-597 and 606-633"
      contains: "tagsInput"
  key_links:
    - from: "src/components/CostCalculator.tsx::handleSaveJob"
      to: "src/db/backfill.ts::parseTagsInput"
      via: "ESM import + call at both save branches"
      pattern: "tags: parseTagsInput\\(tagsInput\\)"
    - from: "src/db/backfill.ts::parseTagsInput"
      to: "src/db/backfill.ts::normalizeTagsOnJob"
      via: "shared D-02 normalization (re-uses the same transform internally so input + reconcile paths cannot drift)"
      pattern: "/\\[\\^a-z0-9\\\\s\\\\-_\\]/g"
---

<objective>
Add a shared `parseTagsInput` helper to `src/db/backfill.ts` and wire a comma-separated tag input into CostCalculator's save form. This satisfies TAGS-01 at the CostCalculator surface (one of the two surfaces D-01 calls for — the second surface, JobsManager inline-edit, lives in Plan 15-04 because it touches the same file as the chip filter and search bar).

Purpose: User can tag a job at save time (power-user workflow per D-01). The shared `parseTagsInput` ensures the input path and the reconcile path (Plan 15-01's `normalizeTagsOnJob`) cannot drift — both use the same regex, the same cap, the same dedupe.

Output: One exported function in `src/db/backfill.ts`, one new tag field UI in `src/components/CostCalculator.tsx`, both Update and Create save branches write the parsed tags field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md
@.planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md
@.planning/phases/15-tags-search-quick-duplicate/15-01-SUMMARY.md
@src/db/backfill.ts
@src/components/CostCalculator.tsx

<interfaces>
<!-- Model URL field (CostCalculator.tsx:788-801) is the canonical analog for "labeled input with InfoTooltip + NewBadge" -->
From src/components/CostCalculator.tsx:788-801 (Model URL pattern — MIRROR for tags):
  <div className="flex-1 min-w-[220px] max-w-md">
    <label className="flex items-center gap-1.5 text-xs text-slate-400 mb-1">
      <span>Model URL</span>
      <InfoTooltip text="..." />
      <NewBadge feature="model-url" />
    </label>
    <Input type="url" value={modelUrl} onChange={e => setModelUrl(e.target.value)} placeholder="..." />
  </div>

From src/components/CostCalculator.tsx:570-633 (handleSaveJob — two save branches):
  - Lines 572-597: Update existing job (the `{...editingJob, name: ..., updatedAt: ...}` literal)
  - Lines 606-633: Create new job (the fresh `{ id: \`job-${Date.now()}\`, ... copiesSold: 0 }` literal)
  - Both literals end with `marketplace` as the last assigned field — tags field should be appended at the same position in BOTH literals

NewBadge feature key `tags` — registered in src/features.ts in Plan 15-05 (this plan adds the JSX usage; the registry entry lands in Plan 15-05 alongside `search-jobs` and `quick-duplicate`).

D-13 placement rule: NewBadge on a LABEL (flex container with gap-1.5) is inline — NOT absolute-overlay. Mirror Model URL EXACTLY. The absolute-overlay variant is reserved for buttons (used in Plans 15-04 and 15-05 for the search input and overflow trigger).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add parseTagsInput shared helper to src/db/backfill.ts</name>
  <files>src/db/backfill.ts</files>
  <read_first>
    - src/db/backfill.ts (full file — to see normalizeTagsOnJob from Plan 15-01 and confirm the D-02 transform sequence)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 37-44 (D-02 — the input parse pipeline split: split on comma → trim → lowercase → filter empty → dedupe → whitelist → cap 10; empty input returns undefined NOT [])
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 230-249 (the canonical D-02 regex + cap-at-10 implementation — share the same transform with normalizeTagsOnJob)
  </read_first>
  <action>
    In `src/db/backfill.ts`, add a new exported function `parseTagsInput(raw: string): string[] | undefined` immediately after the `normalizeTagsOnJob` function (so the two co-locate as siblings — both apply D-02 rules). Per D-02 line 39 the input-path pipeline is: `raw.split(',').map(s => s.trim().toLowerCase()).filter(s => s.length > 0)`; then apply the same whitelist `s.replace(/[^a-z0-9\s\-_]/g, '')`; then drop entries that became empty after stripping; then dedupe via `Set<string>`; then enforce the cap (`cleaned.length >= 10 → break`). If the resulting `cleaned` array is empty (no usable tags), return `undefined` (D-02 line 43 lock — empty input maps to undefined, NOT []). Otherwise return `cleaned`. Implementation MUST re-use the IDENTICAL whitelist regex `/[^a-z0-9\s\-_]/g` and IDENTICAL cap `10` as `normalizeTagsOnJob` — if the executor wants to extract a private helper `_normalizeTagToken(raw: string): string` shared by both functions, that's acceptable (and recommended for DRY); the public API surface is still just `normalizeTagsOnJob` and `parseTagsInput`. JSDoc: cite "Phase 15 D-02 (tag input parse — shares the same normalization rules as normalizeTagsOnJob)"; give examples: `parseTagsInput('PLA, phone-stand,  GLOSS ')` → `['pla', 'phone-stand', 'gloss']`; `parseTagsInput('a, a, a')` → `['a']`; `parseTagsInput('phone stand, pla')` → `['phone stand', 'pla']` (multi-word preserved, comma is the only delimiter); `parseTagsInput('')` → `undefined`; `parseTagsInput('   ,,,  ')` → `undefined`; `parseTagsInput('emoji💀,@@@')` → `undefined` (both strip to empty); `parseTagsInput(Array.from({length: 15}, (_, i) => \`tag${i}\`).join(','))` → array of length 10 (cap). Add a sibling describe block to `src/db/backfill.test.ts` named `describe('parseTagsInput (Phase 15 D-02)', () => { ... })` with at least 6 cases: (a) basic normalize, (b) dedupe, (c) multi-word preserved, (d) empty input returns undefined, (e) cap-at-10, (f) emoji/punctuation strip → undefined when nothing remains.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -20 && npm test -- --run src/db/backfill.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function parseTagsInput" src/db/backfill.ts` returns `1`
    - `grep -c "Phase 15 D-02" src/db/backfill.ts` returns at least `1`
    - The whitelist regex `/\[\^a-z0-9\\s\\-_\]/g` appears in BOTH `normalizeTagsOnJob` AND `parseTagsInput` (or once in a shared private helper); confirm via `grep -c "/\\[\\^a-z0-9.*\\]/g" src/db/backfill.ts` returning 2 OR (1 if private helper extracted and called from both)
    - `grep -c "import.*dexie\|import.*\\./database" src/db/backfill.ts` returns `0`
    - `npm test -- --run src/db/backfill.test.ts` reports the new parseTagsInput describe block passing (at least 6 it() cases)
    - `npx tsc -b` exits 0
    - `normalizeTagsOnJob` from Plan 15-01 still passes its 6 cases (no regression)
  </acceptance_criteria>
  <done>parseTagsInput is exported, fully tested (≥6 it cases), shares the D-02 regex + cap with normalizeTagsOnJob, returns undefined for empty/all-stripped input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tag input field to CostCalculator save form + wire into both save branches</name>
  <files>src/components/CostCalculator.tsx</files>
  <read_first>
    - src/components/CostCalculator.tsx lines 540-640 (handleSaveJob — the two save-literal branches at 572-597 Update and 606-633 Create)
    - src/components/CostCalculator.tsx lines 750-810 (the identity row + Print Name + Model URL field — the canonical analog for adding a new labeled input)
    - src/components/CostCalculator.tsx lines 788-801 (Model URL EXACT analog — `<label className="flex items-center gap-1.5 text-xs text-slate-400 mb-1">` with `<span>` + `<InfoTooltip>` + `<NewBadge>` siblings; the wrapped `<Input>` below)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 34-44 (D-01 + D-02 — UX intent and the parse rules)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 129-140 (D-13 — NewBadge placement: `tags` badge on the LABEL INLINE per project memory rule, NOT absolute-overlay)
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 393-423 (analog mapping for CostCalculator MODIFY — Model URL pattern + save-block wiring at lines 593/632)
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 692-712 (project NewBadge placement memory rule — confirms label-inline for labels, absolute-overlay for square icon buttons)
  </read_first>
  <action>
    Add `tagsInput` local state to CostCalculator: `const [tagsInput, setTagsInput] = useState('');`. Pre-fill on edit: in the existing effect that hydrates form state from `editingJob` (find the `useEffect` that sets `printName`, `modelUrl`, etc. from `editingJob.*`), add a line `setTagsInput(editingJob?.tags?.join(', ') ?? '');` (comma-space join is the canonical re-display form so editing round-trips). Reset on `clearForm()` (find clearForm — the existing function that resets `setPrintName('')`, `setModelUrl('')`, etc.): add `setTagsInput('');`. Add a NEW `<div>` field in the identity row area (after Model URL — `CostCalculator.tsx:788-801`). MIRROR the Model URL JSX shape EXACTLY (the label-inline NewBadge pattern; do NOT use absolute-overlay here): `<div className="flex-1 min-w-[220px] max-w-md">` wrapping `<label className="flex items-center gap-1.5 text-xs text-slate-400 mb-1"><span>Tags</span><InfoTooltip text="Comma-separated. Lowercase, max 10. Use hyphens or spaces inside a tag — e.g. 'phone-stand, pla, gloss'." /><NewBadge feature="tags" /></label>` then `<Input type="text" value={tagsInput} onChange={e => setTagsInput(e.target.value)} placeholder="phone-stand, pla, gloss" />`. Below the input, conditionally render a one-line warning when the parsed result would be capped: `{(() => { const parsed = parseTagsInput(tagsInput); return parsed && parsed.length >= 10 ? <p className="text-xs text-amber-400 mt-1">Maximum 10 tags — extras ignored</p> : null; })()}` — D-02 line 43 says "one-time inline warning under the input"; this is acceptable as it only renders when at cap. Import `parseTagsInput` from `../db/backfill` at the top of the file (alongside any existing backfill imports if present, otherwise as a new import line). Wire into BOTH save literals: in the Update branch (`CostCalculator.tsx:572-597`) append the line `tags: parseTagsInput(tagsInput),` just before the closing `};` (positioning after `marketplace` on line 596 is fine — the field is `PrintJob.tags?: string[]` so the optional placement is flexible). In the Create branch (`CostCalculator.tsx:606-633`) append `tags: parseTagsInput(tagsInput),` after `marketplace` on line 631 and before `copiesSold: 0` on line 632. Do NOT modify `handleSaveJob`'s `filaments` array build, the `tax` resolution, or any other field. Do NOT touch `etsyChecks`, `customer`, or any Phase 14 logic. NewBadge: use the LABEL-INLINE pattern (matches Model URL exactly) — do NOT add `className="absolute -top-1 -right-1"` to this badge (that's the button-overlay variant reserved for the search input and overflow trigger in later plans). The `tags` feature key is registered in Plan 15-05's `src/features.ts` edit; the JSX consumer here is added now and the registry entry lands in Wave 2 (same wave — both plans run autonomously).
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -20 && grep -c "tagsInput\|parseTagsInput" src/components/CostCalculator.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const \[tagsInput, setTagsInput\]" src/components/CostCalculator.tsx` returns `1`
    - `grep -c "import.*parseTagsInput" src/components/CostCalculator.tsx` returns `1` (named import from `../db/backfill`)
    - `grep -c "NewBadge feature=\"tags\"" src/components/CostCalculator.tsx` returns `1`
    - `grep -c "<NewBadge feature=\"tags\" className=\"absolute" src/components/CostCalculator.tsx` returns `0` (must be label-inline, NOT absolute-overlay — D-13 + project memory rule)
    - `grep -c "tags: parseTagsInput(tagsInput)" src/components/CostCalculator.tsx` returns `2` (one in Update branch, one in Create branch)
    - `grep -c "Maximum 10 tags" src/components/CostCalculator.tsx` returns `1` (the inline warning)
    - `grep -c "setTagsInput(editingJob" src/components/CostCalculator.tsx` returns `1` (edit-mode pre-fill)
    - `grep -c "setTagsInput('')" src/components/CostCalculator.tsx` returns at least `1` (clearForm reset)
    - `npx tsc -b` exits 0
    - The Model URL field at the original lines 788-801 is structurally unchanged (still has `NewBadge feature="model-url"`)
  </acceptance_criteria>
  <done>Tag input renders in the identity row; both save branches write `tags: parseTagsInput(tagsInput)`; edit-mode pre-fills the input from `editingJob.tags`; NewBadge `tags` is label-inline (not absolute-overlay); cap warning surfaces only when ≥10 tags parsed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text → IndexedDB persistence | The `tagsInput` string is user-controlled; arbitrary characters including emoji, HTML-like content, and punctuation can be typed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Tampering / XSS-via-tag | parseTagsInput input | mitigate | D-02 whitelist regex `/[^a-z0-9\s\-_]/g` strips ALL characters outside the alphanumeric+space+hyphen+underscore set BEFORE persistence; no `<` or `>` can survive; chip renders are plain text (no `dangerouslySetInnerHTML`) so even if a `<` slipped through, React would escape it |
| T-15-07 | Denial of Service | parseTagsInput unbounded input | mitigate | D-02 cap-at-10 enforced inside the function; the Input field itself is a single-line `<Input>` with implicit browser-level input length cap (no maxLength attribute needed for our use case but the cap fires server-side in the helper) |
</threat_model>

<verification>
- `npx tsc -b` clean exit
- `npm test -- --run src/db/backfill.test.ts` passes (regression check on Plan 15-01 helper + new parseTagsInput tests)
- `npm test -- --run src/utils/duplicateJob.test.ts` still passes (no regression in Plan 15-02)
- Tag input visible in the CostCalculator identity row (manual visual check in Plan 15-06 UAT)
- `grep "tags: parseTagsInput" src/components/CostCalculator.tsx | wc -l` returns 2 (both save branches wired)
</verification>

<success_criteria>
- TAGS-01 input surface (a) ships: comma-separated tag input visible in CostCalculator save form
- Both Create and Update save branches persist `tags: string[] | undefined` via the shared parser
- `parseTagsInput` enforces D-02 rules (lowercase + trim + dedupe + cap-at-10 + whitelist + empty→undefined)
- Edit mode pre-fills the input from `editingJob.tags?.join(', ')`
- ROADMAP Success Criterion #1 (comma-separated, lowercased, trimmed, deduped, capped, persists as `tags: string[]`) is provably true for the CostCalculator save path
- NewBadge wiring honours D-13 + project memory: label-inline placement (NOT absolute-overlay) since the host is a label-flex container
</success_criteria>

<output>
Create `.planning/phases/15-tags-search-quick-duplicate/15-03-SUMMARY.md` when done. Include: parseTagsInput signature + test count; CostCalculator state additions; exact line numbers where tags field was inserted in BOTH save branches; confirmation that the NewBadge is label-inline (not absolute-overlay); the regex/cap shared correctly between parseTagsInput and normalizeTagsOnJob.
</output>
