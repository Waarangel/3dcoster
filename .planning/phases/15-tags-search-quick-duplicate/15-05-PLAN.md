---
phase: 15-tags-search-quick-duplicate
plan: 05
type: execute
wave: 3
depends_on: ["15-01", "15-02", "15-03", "15-04"]
files_modified:
  - src/features.ts
  - src/hooks/useDatabase.ts
  - src/components/JobsManager.tsx
autonomous: true
requirements: [TAGS-01, TAGS-04, DUP-01]
requirements_addressed: [TAGS-01, TAGS-04, DUP-01]
tags: [jobs-manager, tag-chips, inline-edit, overflow-menu, duplicate, new-badge, reconcile-wiring]
user_setup: []
must_haves:
  truths:
    - "Each JobCard summary line renders every tag as a non-interactive chip (mirror AssetLibrary.tsx:192-198 styling exactly: text-xs px-1.5 py-0.5 rounded bg-slate-600/50 text-slate-400)"
    - "Each JobCard has an inline tag editor affordance — small pencil button reveals a text input for re-tagging without round-tripping through CostCalculator (D-01 surface b)"
    - "A new [⋯] overflow menu button appears in each JobCard action row (alongside Record Sale / Create Quote / Edit / Delete) — mirrors the QuoteRow overflow menu pattern at JobsManager.tsx:130/164-199"
    - "Clicking [⋯] reveals a single 'Duplicate' menu item; clicking it creates a duplicate via duplicateJob + nextCopyName immediately (no confirmation modal — D-07)"
    - "Post-duplicate UX: list scrolls to and highlights the new row (the lower-risk substitute for a toast per PATTERNS.md No-Analog note; D-07 wording 'scrolls to and highlights')"
    - "The duplicate appears at the TOP of the list (newest by createdAt, since the duplicateJob assigns a fresh now() createdAt and the existing JobsManager sort is descending by createdAt)"
    - "NewBadge entries land in src/features.ts: tags (2026-05-24), search-jobs (2026-05-24), quick-duplicate (2026-05-24) — D-13 lock"
    - "NewBadge `search-jobs` lives as absolute-overlay on the search input wrapper (Plan 15-04 wrapper); NewBadge `quick-duplicate` lives as absolute-overlay on the [⋯] trigger; NewBadge `tags` already lives label-inline (Plan 15-03 CostCalculator + this plan's inline editor) — D-13"
    - "src/hooks/useDatabase.ts useJobs init wires normalizeTagsOnJob with a tagsNormalizeRan module flag — mirroring copiesSoldReconcileRan pattern; idempotent and silently retries on next mount if the transaction fails"
  artifacts:
    - path: "src/features.ts"
      provides: "3 new entries (tags, search-jobs, quick-duplicate) with release date 2026-05-24"
      contains: "'tags': new Date('2026-05-24')"
    - path: "src/hooks/useDatabase.ts"
      provides: "tagsNormalizeRan module flag + useEffect inside useJobs that runs normalizeTagsOnJob on the live job set and bulkPut's any dirty rows"
      contains: "tagsNormalizeRan"
    - path: "src/components/JobsManager.tsx"
      provides: "JobCard tag chips display block (per D-11), inline tag edit affordance with NewBadge label-inline, [⋯] overflow menu with Duplicate item, NewBadge quick-duplicate absolute-overlay on the [⋯], NewBadge search-jobs absolute-overlay on the Plan 15-04 search input wrapper, post-duplicate scroll-and-highlight effect"
      contains: "overflowOpenJobId"
  key_links:
    - from: "JobsManager.tsx::[⋯] Duplicate handler"
      to: "src/utils/duplicateJob.ts::duplicateJob + nextCopyName"
      via: "ESM import + call inside the menu item onClick"
      pattern: "duplicateJob\\(job"
    - from: "JobsManager.tsx::Duplicate handler"
      to: "useJobs::addJob (or db.jobs.put — whichever the existing surface exposes)"
      via: "the same job-create primitive the CostCalculator save uses"
      pattern: "addJob\\(|db\\.jobs\\.add\\(|db\\.jobs\\.put\\("
    - from: "useDatabase.ts::useJobs init"
      to: "src/db/backfill.ts::normalizeTagsOnJob"
      via: "named import + per-job loop inside the tagsNormalizeRan effect"
      pattern: "normalizeTagsOnJob\\("
    - from: "src/features.ts entries"
      to: "JobsManager.tsx + CostCalculator.tsx NewBadge consumers"
      via: "feature key string match (tags, search-jobs, quick-duplicate)"
      pattern: "feature=\"(tags|search-jobs|quick-duplicate)\""
---

<objective>
Complete the JobsManager surface for Phase 15: render tag chips on each JobCard summary line (D-11), add an inline tag editor affordance (D-01 surface b), add the `[⋯]` overflow menu with a Quick Duplicate item (D-07 + DUP-01) wired through `duplicateJob` + `nextCopyName` from Plan 15-02, post-duplicate scroll-and-highlight, NewBadge JSX wiring on three surfaces, register three feature keys in `src/features.ts` (D-13), and wire `normalizeTagsOnJob` into `useJobs` init (D-12) using the existing `copiesSoldReconcileRan` template.

Purpose: This is the user-visible payoff plan — chips are visible on cards, the user can re-tag without leaving JobsManager, the `[⋯] → Duplicate` action ships DUP-01 visibly, the badges signal "new" to existing users, and the reconcile silently fixes any DevTools-corrupted tags from before D-02 existed.

Output: Three files modified; one new exported state pattern in JobsManager (overflowOpenJobId); the existing JobsManager surfaces from Plan 15-04 untouched; all six requirements (TAGS-01..04, DUP-01..02) now covered across the plan set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md
@.planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md
@.planning/phases/15-tags-search-quick-duplicate/15-02-SUMMARY.md
@.planning/phases/15-tags-search-quick-duplicate/15-04-SUMMARY.md
@src/components/JobsManager.tsx
@src/components/AssetLibrary.tsx
@src/features.ts
@src/hooks/useDatabase.ts
@src/db/backfill.ts
@src/utils/duplicateJob.ts

<interfaces>
<!-- features.ts registry — append after existing entries -->
From src/features.ts:5-15 (existing registry):
  export const featureReleases: Record<string, Date> = {
    'settings-reorg': new Date('2026-05-20'),
    'default-profit-margin': new Date('2026-05-18'),
    'model-url': new Date('2026-05-20'),
    'default-tax-rate': new Date('2026-05-21'),
    'customer-details': new Date('2026-05-21'),
    'etsy-helper': new Date('2026-05-21'),
    'customer-library': new Date('2026-05-22'),
    'pdf-quote': new Date('2026-05-23'),
    // Add new features here with their release date
  };

<!-- Module-flag pattern — mirror exactly -->
From src/hooks/useDatabase.ts:12-16 (existing flags):
  let saleCustomerBackfillRan = false;
  let copiesSoldReconcileRan = false;
  // Phase 15 D-12 adds: let tagsNormalizeRan = false;

From src/hooks/useDatabase.ts:6 (existing backfill import):
  import { backfillCustomersFromSales, reconcileCopiesSoldFromSales } from '../db/backfill';
  // Phase 15: extend with `, normalizeTagsOnJob`

From src/hooks/useDatabase.ts:443-485 (canonical reconcile effect — copiesSoldReconcileRan):
  useEffect(() => {
    if (copiesSoldReconcileRan) return;
    if (jobs === undefined) return;
    copiesSoldReconcileRan = true;
    let cancelled = false;
    (async () => {
      try {
        const allSales = await db.sales.toArray();
        if (cancelled) return;
        const patches = reconcileCopiesSoldFromSales(jobs, allSales);
        if (patches.length > 0) {
          await db.transaction('rw', db.jobs, async () => { ... });
        }
      } catch (err) {
        console.error('copiesSold reconcile failed:', err);
        copiesSoldReconcileRan = false;
      }
    })();
    return () => { cancelled = true; };
  }, [jobs === undefined]);

<!-- Overflow menu — canonical analog in same file -->
From src/components/JobsManager.tsx:130 (QuoteRow state):
  const [overflowOpen, setOverflowOpen] = useState(false);

From src/components/JobsManager.tsx:164-199 (QuoteRow trigger + menu — the D-07 template):
  <button type="button" onClick={(e) => { e.stopPropagation(); setOverflowOpen(o => !o); }} aria-label="More actions" aria-haspopup="menu" aria-expanded={overflowOpen} className="inline-flex items-center justify-center w-8 h-8 rounded hover:bg-slate-700 text-slate-300">⋯</button>
  {overflowOpen && (<div role="menu" className="absolute right-0 top-full mt-1 z-10 min-w-[160px] bg-slate-800 border border-slate-700 rounded-lg shadow-lg py-1"><button role="menuitem" onClick={...} className="w-full text-left px-3 py-2 text-sm hover:bg-slate-700 text-slate-100">Edit Quote</button>...</div>)}

<!-- Phase 15 adaptation: many JobCards exist on screen; replace local boolean with shared `overflowOpenJobId: string | null` at the JobsManager parent so only one menu opens at a time -->

<!-- JobCard action row (current Plan 16 ext2 form) — lines 455-492 -->
From src/components/JobsManager.tsx:455-492 (the action row Plan 15-05 augments):
  <div className="flex gap-2 flex-wrap"> ... Record Sale, Create Quote, Edit, Delete ... </div>
  // Adaptation: add `relative` to the wrapping div so the [⋯] menu's absolute right-0 top-full anchors correctly; append the [⋯] button + menu after the Delete button.

<!-- Chip styling (D-11 LOCK — byte-identical to AssetLibrary.tsx:192-198) -->
text-xs px-1.5 py-0.5 rounded bg-slate-600/50 text-slate-400
Wrapper: <div className="flex flex-wrap gap-1 mt-1">

<!-- NewBadge placement rules per D-13 + project memory -->
- `tags` → label-inline (already on CostCalculator from Plan 15-03; this plan adds label-inline on the JobsManager inline tag editor)
- `search-jobs` → absolute-overlay on the search input wrapper (Plan 15-04 wrapper; this plan ADDS the `<NewBadge>` JSX inside it)
- `quick-duplicate` → absolute-overlay on the [⋯] trigger
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register 3 new feature keys in src/features.ts and wire normalizeTagsOnJob into useJobs reconcile</name>
  <files>src/features.ts, src/hooks/useDatabase.ts</files>
  <read_first>
    - src/features.ts (full file — 23 lines; see existing entries at lines 5-15 + the `// Add new features here` comment marker)
    - src/hooks/useDatabase.ts lines 6-16 (existing backfill import + module flags `saleCustomerBackfillRan`, `copiesSoldReconcileRan`)
    - src/hooks/useDatabase.ts lines 443-485 (the `copiesSoldReconcileRan` effect — the verbatim template to mirror)
    - src/db/backfill.ts (after Plan 15-01 — contains `normalizeTagsOnJob` and after Plan 15-03 `parseTagsInput`)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 129-140 (D-13 — three feature keys + dates)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 122-127 (D-12 — wire into useJobs init, mirror the existing reconcile pattern)
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 308-388 (analog mapping for the useJobs effect — full template with the loop body adapted for in-place mutation + bulkPut)
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 681-689 (reconcile error-swallowing rule — never throw to React, always console.error + reset flag)
  </read_first>
  <action>
    Step A (src/features.ts): Append three entries to the `featureReleases` registry IMMEDIATELY BEFORE the `// Add new features here with their release date` comment (so the comment stays as a future-extension marker): `'tags': new Date('2026-05-24'),`, `'search-jobs': new Date('2026-05-24'),`, `'quick-duplicate': new Date('2026-05-24'),`. Keep the existing entries unchanged (8 entries before, 11 after). Step B (src/hooks/useDatabase.ts): Update the import on line 6 to add `normalizeTagsOnJob`: `import { backfillCustomersFromSales, reconcileCopiesSoldFromSales, normalizeTagsOnJob } from '../db/backfill';`. Add a new module-scope flag immediately after line 16 (sibling to `copiesSoldReconcileRan`): `let tagsNormalizeRan = false;` with a brief one-line comment `// Phase 15 D-12 — process-lifetime flag for the tag-normalization reconcile (sibling to copiesSoldReconcileRan).`. Inside the `useJobs` hook, add a new useEffect IMMEDIATELY AFTER the existing `copiesSoldReconcileRan` effect (after line 485). The effect template mirrors PATTERNS.md lines 354-382 — guard on `tagsNormalizeRan` and `jobs === undefined`; set flag true; build a `dirty: PrintJob[]` array by iterating `jobs`, making a SHALLOW copy of each job (`const copy: PrintJob = { ...job, tags: job.tags ? [...job.tags] : undefined };` — IMPORTANT so the liveQuery cache isn't mutated), calling `normalizeTagsOnJob(copy)`, and pushing `{ ...copy, updatedAt: new Date() }` to `dirty` when the helper returns true. If `dirty.length > 0`, wrap in `await db.transaction('rw', db.jobs, async () => { for (const job of dirty) await db.jobs.put(job); });`. Wrap the entire async work in try/catch — on error: `console.error('tag normalize reconcile failed:', err); tagsNormalizeRan = false;` (allow retry on next mount). Return a cleanup `() => { cancelled = true; }`. Dependency array: `[jobs === undefined]` matching the sibling pattern; add the same `// eslint-disable-next-line react-hooks/exhaustive-deps` comment above the closing `}` per the precedent. Do NOT touch the `copiesSoldReconcileRan` block or the `saleCustomerBackfillRan` block — they remain unchanged.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -20 && grep -c "'tags':\|'search-jobs':\|'quick-duplicate':" src/features.ts && grep -c "tagsNormalizeRan\|normalizeTagsOnJob" src/hooks/useDatabase.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'tags': new Date('2026-05-24')" src/features.ts` returns `1`
    - `grep -c "'search-jobs': new Date('2026-05-24')" src/features.ts` returns `1`
    - `grep -c "'quick-duplicate': new Date('2026-05-24')" src/features.ts` returns `1`
    - The 8 prior entries (settings-reorg, default-profit-margin, model-url, default-tax-rate, customer-details, etsy-helper, customer-library, pdf-quote) all still appear: `grep -c "'pdf-quote':\|'settings-reorg':\|'customer-library':" src/features.ts` returns `3`
    - `grep -c "let tagsNormalizeRan = false" src/hooks/useDatabase.ts` returns `1`
    - `grep -c "normalizeTagsOnJob" src/hooks/useDatabase.ts` returns at least `2` (import + call site)
    - `grep -c "tag normalize reconcile failed" src/hooks/useDatabase.ts` returns `1` (error log per error-handling convention)
    - `grep -c "tagsNormalizeRan = false" src/hooks/useDatabase.ts` returns `2` (declaration + reset-on-error)
    - The existing `copiesSoldReconcileRan` and `saleCustomerBackfillRan` patterns remain in place: `grep -c "copiesSoldReconcileRan\|saleCustomerBackfillRan" src/hooks/useDatabase.ts | head -1` returns at least `6` (3+ references each unchanged from before)
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>features.ts has 11 entries (8 prior + 3 new); useDatabase.ts has a tagsNormalizeRan module flag, a fresh useEffect mirroring the copiesSoldReconcileRan pattern, error-swallowing with flag-reset for retry; normalizeTagsOnJob is imported and called per-job inside the effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add JobCard tag chips display + inline tag editor + [⋯] overflow menu with Duplicate + post-duplicate scroll+highlight + NewBadge wiring on search input and overflow trigger</name>
  <files>src/components/JobsManager.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx lines 1-20 (imports — confirm `NewBadge` is imported; if not, add `import { NewBadge } from './NewBadge';`)
    - src/components/JobsManager.tsx lines 60-80 (any existing `import` of `addJob` / `useJobs` — find how Plan 15-04's `searchedJobs` source `jobs` is exposed and what mutation primitive is available; if `addJob` is not destructured, use `db.jobs.add` directly per the project pattern)
    - src/components/JobsManager.tsx lines 130, 164-199 (the QuoteRow overflow trigger + menu — the EXACT template to mirror for the JobCard `[⋯]`)
    - src/components/JobsManager.tsx lines 370-500 (the JobCard render — current shape including action row at 455-492 + the meta line where chips go BETWEEN the filament/print-time block and the break-even progress bar)
    - src/components/JobsManager.tsx (Plan 15-04 additions — the sticky sub-header with search input wrapper, the selectedChips state, the cache key) — confirm the search input is currently wrapped in `<div className="relative mb-3">`
    - src/components/AssetLibrary.tsx lines 192-198 (chip styling — D-11 EXACT mirror; byte-identical)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 69-75 (D-07 overflow menu + post-duplicate UX: scroll + highlight; no confirmation modal)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 114-120 (D-11 tag chip rendering rules: every tag, no max-visible cap, mt-1 wrapper, non-interactive)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md lines 76-81 (D-08 duplicate name format — drives the nextCopyName call below)
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 534-583 (overflow menu template + NewBadge absolute-overlay on the `[⋯]` trigger)
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 614-624 (inline tag editor — claude's-discretion pattern: tiny inline Input revealed on pencil-icon click, with a stable hover target for the NewBadge)
    - .planning/phases/15-tags-search-quick-duplicate/15-PATTERNS.md lines 644-651 (tag chips on JobCard — position after filament+print-time meta, before isSelected expanded block)
  </read_first>
  <action>
    Step A — JobCard tag chips display (D-11). Locate the JobCard summary block (between the filament/print-time meta line around lines 378-388 and the `isSelected` expanded block around line 398). Insert a block that renders the tag chips when `job.tags && job.tags.length > 0`: `{job.tags && job.tags.length > 0 && (<div className="flex flex-wrap gap-1 mt-1">{job.tags.map(tag => (<span key={tag} className="text-xs px-1.5 py-0.5 rounded bg-slate-600/50 text-slate-400">{tag}</span>))}</div>)}`. Chip styling MUST be byte-identical to AssetLibrary.tsx:192-198 (D-11 lock — `text-xs px-1.5 py-0.5 rounded bg-slate-600/50 text-slate-400`). Per D-11 these chips are NON-INTERACTIVE (plain `<span>`, no onClick). Step B — Inline tag editor on JobCard. Add per-card state via lifting (or per-card local — both acceptable; recommend a single shared `editingTagsJobId: string | null` state in the JobsManager parent for one-at-a-time editing). Render a small pencil-icon `<Button variant="ghost" btnSize="sm">` next to the chip row (or in the action row — pick whichever doesn't push the existing buttons to wrap; ghost+pencil in the action row is the safer placement). On click, toggle to a small inline `<Input>` revealed in place (or render an `<Input>` with `<NewBadge feature="tags">` label-inline beside it — the badge needs a stable hover target per CONTEXT open question; wrap the pencil-button + input in a `<div className="relative">` so the NewBadge label-inline pattern has a label-flex parent to live in). On blur or Enter, parse via `parseTagsInput(value)` (imported from `../db/backfill`) and call the existing job-update primitive (find by grep: `db.jobs.put` or `updateJob` or `onUpdateJob` — use whichever JobsManager already invokes for the Edit flow's persist path; if uncertain, use `await db.jobs.put({ ...job, tags: parseTagsInput(value), updatedAt: new Date() });` matching how Plan 15-01's reconcile effect writes). Use the label-inline NewBadge pattern: `<label className="flex items-center gap-1.5 text-xs text-slate-400"><span>Tags</span><NewBadge feature="tags" /></label>` wrapping the input. Do NOT use absolute-overlay for the inline editor (the parent context is a label-flex container — same rule as CostCalculator Plan 15-03). Step C — `[⋯]` overflow menu with Duplicate. Add parent-level state `const [overflowOpenJobId, setOverflowOpenJobId] = useState<string | null>(null);` so only one menu is open across the whole list at any time. In the JobCard action row at `JobsManager.tsx:455-492`, add `relative` to the wrapping div className (change `<div className="flex gap-2 flex-wrap">` → `<div className="flex gap-2 flex-wrap relative">`). After the Delete button, mount: `<div className="relative">` wrapping (a) `{/* allow-raw-html: overflow toggle is a small icon button, not a CTA — Button primitive would dwarf the row; mirrors QuoteRow precedent at JobsManager.tsx:164-165 */}` then `<button type="button" onClick={(e) => { e.stopPropagation(); setOverflowOpenJobId(prev => prev === job.id ? null : job.id); }} aria-label="More actions" aria-haspopup="menu" aria-expanded={overflowOpenJobId === job.id} className="inline-flex items-center justify-center w-8 h-8 rounded hover:bg-slate-700 text-slate-300">⋯</button>` then `<NewBadge feature="quick-duplicate" className="absolute -top-1 -right-1" />` (D-13 + project memory absolute-overlay for icon buttons). Conditionally render the menu when `overflowOpenJobId === job.id`: `<div role="menu" className="absolute right-0 top-full mt-1 z-10 min-w-[160px] bg-slate-800 border border-slate-700 rounded-lg shadow-lg py-1">` containing the menuitem: `{/* allow-raw-html: native menuitem styling per WAI-ARIA menu pattern, mirrors QuoteRow */}` then `<button type="button" role="menuitem" onClick={(e) => { e.stopPropagation(); setOverflowOpenJobId(null); handleDuplicate(job); }} className="w-full text-left px-3 py-2 text-sm hover:bg-slate-700 text-slate-100">Duplicate</button>`. Add an effect to close the menu on outside click: `useEffect(() => { if (!overflowOpenJobId) return; const handler = () => setOverflowOpenJobId(null); window.addEventListener('click', handler); return () => window.removeEventListener('click', handler); }, [overflowOpenJobId]);` (mirrors common click-outside patterns; the `e.stopPropagation()` inside the trigger keeps the toggle working). Step D — handleDuplicate. Import `duplicateJob, nextCopyName` from `../utils/duplicateJob` at the top of the file. Add: `const handleDuplicate = useCallback(async (source: PrintJob) => { const existingNames = new Set(jobs.map(j => j.name)); const newName = nextCopyName(source.name, existingNames); const dup = duplicateJob(source, newName); await db.jobs.add(dup); setHighlightedJobId(dup.id); setTimeout(() => setHighlightedJobId(null), 2000); }, [jobs]);` — and add a parent state `const [highlightedJobId, setHighlightedJobId] = useState<string | null>(null);`. Per D-07 "scrolls to and highlights the new row": apply an extra className to the JobCard root when `job.id === highlightedJobId` — e.g. `${job.id === highlightedJobId ? 'ring-2 ring-blue-400 ring-offset-2 ring-offset-slate-900' : ''}`; this also serves as the toast substitute (PATTERNS.md No-Analog note — toast infrastructure does not exist in this project; scroll+highlight is the lower-risk option). For the "scroll to top" part: since `duplicateJob` sets `createdAt: new Date()` (now), and JobsManager sorts descending by createdAt, the duplicate naturally appears at the top of the list when the liveQuery emits. The cache-key includes `selectedJobId` which does not change, so the cache stays valid — react-window may need a tiny scroll-to-top call; consult the existing `<List>` ref usage in JobsManager (grep `scrollTo`) — if a ref exists, call `listRef.current?.scrollTo(0)` inside `handleDuplicate`. If no ref, accept "list naturally shows the duplicate at top because it's the newest" as the D-07-compatible outcome (the highlight provides the visual confirmation per the spec). Step E — NewBadge `search-jobs` on the search input wrapper from Plan 15-04. Find the `<div className="relative mb-3">` wrapping the search Input. Mount `<NewBadge feature="search-jobs" className="absolute -top-1 -right-1" />` as a child of that wrapper (absolute-overlay per D-13; the wrapper already has `relative` positioning from Plan 15-04). Do NOT modify the search input value/onChange or any Plan 15-04 logic. Do NOT change the cache key. Do NOT touch the chip-filter row or the filter empty state. Do NOT modify CostCalculator.tsx (Plan 15-03 owns its NewBadge `tags`).
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -30 && grep -c "overflowOpenJobId\|handleDuplicate\|highlightedJobId\|NewBadge feature=\"quick-duplicate\"\|NewBadge feature=\"search-jobs\"\|NewBadge feature=\"tags\"" src/components/JobsManager.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import.*duplicateJob.*nextCopyName" src/components/JobsManager.tsx` returns `1` (named imports from `../utils/duplicateJob`)
    - `grep -c "import.*parseTagsInput" src/components/JobsManager.tsx` returns `1` (named import from `../db/backfill`)
    - `grep -c "const \[overflowOpenJobId" src/components/JobsManager.tsx` returns `1`
    - `grep -c "const \[highlightedJobId" src/components/JobsManager.tsx` returns `1`
    - `grep -c "const handleDuplicate" src/components/JobsManager.tsx` returns `1`
    - `grep -c "duplicateJob(source" src/components/JobsManager.tsx` returns `1` (call site inside handleDuplicate)
    - `grep -c "nextCopyName(source.name" src/components/JobsManager.tsx` returns `1`
    - `grep -c "NewBadge feature=\"quick-duplicate\"" src/components/JobsManager.tsx` returns `1` (one per JobCard render — the JSX appears once in source but renders per-card)
    - `grep -c "NewBadge feature=\"search-jobs\"" src/components/JobsManager.tsx` returns `1`
    - `grep -c "NewBadge feature=\"tags\"" src/components/JobsManager.tsx` returns `1` (the inline editor on JobCard)
    - `grep -c "className=\"absolute -top-1 -right-1\"" src/components/JobsManager.tsx` returns at least `3` (pdf-quote pre-existing + quick-duplicate + search-jobs; tags on the inline editor is LABEL-INLINE not absolute-overlay — confirm there are NOT 4 hits, which would indicate D-13 violation)
    - `grep -B1 "NewBadge feature=\"tags\"" src/components/JobsManager.tsx | grep -c "label\|span" | head -1` returns at least `1` (proves the tags NewBadge is inside a label flex, not on an absolute wrapper)
    - `grep -c "text-xs px-1.5 py-0.5 rounded bg-slate-600/50 text-slate-400" src/components/JobsManager.tsx` returns at least `2` (the JobCard tag chips block + the unselected chip in Plan 15-04's chip filter row)
    - `grep -c "ring-2 ring-blue-400" src/components/JobsManager.tsx` returns at least `1` (the highlight class)
    - `grep -c "Duplicate\"\|>Duplicate<" src/components/JobsManager.tsx` returns at least `1` (the menu item label)
    - `grep -c "role=\"menuitem\"" src/components/JobsManager.tsx` returns at least `2` (pre-existing QuoteRow + new JobCard menuitem)
    - `grep -c "allow-raw-html" src/components/JobsManager.tsx` returns at least `4` (pre-existing QuoteRow uses + Plan 15-04 chip toggles + Plan 15-05 [⋯] trigger + Plan 15-05 menuitem)
    - `npx tsc -b` exits 0
    - All existing JobsManager features (Record Sale, Create Quote, Edit, Delete, Orders sub-section) work
  </acceptance_criteria>
  <done>JobCard renders chips per D-11 (byte-identical AssetLibrary styling); inline tag editor reveals + persists via parseTagsInput; [⋯] menu shows Duplicate; clicking Duplicate creates a new job via duplicateJob + nextCopyName and the new row highlights for 2s; NewBadge wiring: tags label-inline, search-jobs + quick-duplicate absolute-overlay (D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → IndexedDB | Inline tag editor + Duplicate menu both write to db.jobs via the existing primitives — no new persistence layer |
| Outside click → menu close | Window-level click handler closes the menu — bounded scope, no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Tampering | inline tag editor — XSS via tag content | mitigate | parseTagsInput strips all non-whitelisted chars BEFORE persistence; chip renders use `{tag}` as React children (escaped); no innerHTML anywhere |
| T-15-11 | Information Disclosure | handleDuplicate inadvertently carries PII | mitigate | duplicateJob (Plan 15-02) explicitly resets customer to undefined; D-15 test contract guards this; no spread of source per D-09 |
| T-15-12 | Tampering | normalizeTagsOnJob writes during reconcile could corrupt live data | mitigate | The effect makes shallow copies before mutation (`const copy = { ...job, tags: job.tags ? [...job.tags] : undefined }`); only `dirty.length > 0` writes are committed; transaction is `rw` scope-limited to db.jobs; helper is idempotent so repeat runs are safe; error path swallows and resets the flag for retry |
</threat_model>

<verification>
- `npx tsc -b` clean exit
- `npm test -- --run src` reports all existing tests still passing (regression check)
- Visual (Plan 15-06 UAT): chips appear on JobCard; inline editor opens on pencil click and saves on blur; `[⋯]` reveals Duplicate; clicking Duplicate spawns a new row at the top with a 2s blue ring highlight
- `grep "let tagsNormalizeRan = false" src/hooks/useDatabase.ts` matches the new flag
- features.ts entries verified with 3 grep hits at expected version dates
</verification>

<success_criteria>
- DUP-01 ships visibly: `[⋯] → Duplicate` row action available on every JobCard
- TAGS-01 surface (b) ships: inline tag editor on JobsManager (paired with Plan 15-03's CostCalculator surface)
- TAGS-04 reinforced: post-duplicate UX confirms the cache-key contract holds — the new row appears without stale heights
- D-12 ships: normalizeTagsOnJob runs once per page-load in useJobs init; any DevTools-corrupted legacy tags self-heal silently
- D-13 ships: 3 features.ts entries; 3 NewBadge JSX consumers wired (tags label-inline ×2 sites, search-jobs absolute-overlay, quick-duplicate absolute-overlay)
- ROADMAP Success Criterion #5 (Duplicate via row action; duplicate appears immediately) is satisfied at the UI layer; the unit-test piece (Plan 15-02) was the prerequisite
- Project memory rule honoured: zero NewBadges inline-child in flex-1 containers; absolute-overlay used ONLY on square icon buttons (`[⋯]` and search input wrapper); label-inline used on labels with InfoTooltip-style flex layout
</success_criteria>

<output>
Create `.planning/phases/15-tags-search-quick-duplicate/15-05-SUMMARY.md` when done. Include: features.ts before/after entry counts; useDatabase.ts new flag + effect line range; JobsManager imports added; line ranges for the chip display block, inline editor, [⋯] menu, handleDuplicate; one-line confirmation that the JobsManager `tags` NewBadge is label-inline (not absolute-overlay) and the `quick-duplicate` + `search-jobs` NewBadges ARE absolute-overlay (D-13 + project memory rule compliance).
</output>
