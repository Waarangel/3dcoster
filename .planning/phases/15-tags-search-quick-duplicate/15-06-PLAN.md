---
phase: 15-tags-search-quick-duplicate
plan: 06
type: execute
wave: 4
depends_on: ["15-01", "15-02", "15-03", "15-04", "15-05"]
files_modified:
  - .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md
autonomous: false
requirements: [TAGS-01, TAGS-02, TAGS-03, TAGS-04, DUP-01, DUP-02]
requirements_addressed: [TAGS-01, TAGS-02, TAGS-03, TAGS-04, DUP-01, DUP-02]
tags: [verification, uat, build-gate, checkpoint]
user_setup: []
must_haves:
  truths:
    - "tsc -b clean exits with zero errors"
    - "npm run build clean exits and main chunk stays under the existing 300 KB gz Phase 11 gate"
    - "All Vitest cases pass (existing + Plan 15-01 + Plan 15-02 + Plan 15-03 additions)"
    - "All 5 ROADMAP Phase 15 Success Criteria pass under real-browser UAT"
    - "No new console errors / warnings in dev or build"
    - "VERIFICATION.md records the UAT outcome (gap-free or gaps-found verdict)"
  artifacts:
    - path: ".planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md"
      provides: "UAT report with one entry per ROADMAP Success Criterion and per D-XX decision; verdict (gap-free OR gaps-found with closure plan suggestions)"
      contains: "verdict:"
  key_links:
    - from: "15-VERIFICATION.md::Success Criterion entries"
      to: "ROADMAP.md Phase 15 Success Criteria #1..5"
      via: "1:1 mapping"
      pattern: "Success Criterion #[1-5]"
    - from: "15-VERIFICATION.md::Decision coverage"
      to: "15-CONTEXT.md D-01..D-15"
      via: "1:1 mapping"
      pattern: "D-(01|02|03|04|05|06|07|08|09|10|11|12|13|14|15)"
---

<objective>
Run the automated verification chain (tsc + build + Vitest), then perform a real-browser UAT against every ROADMAP Success Criterion and every D-XX decision, recording the outcome in `15-VERIFICATION.md`. This is the gate that decides whether Phase 15 closes or whether a gap-closure round is required.

Purpose: Phase 15 is the last v1.2 phase; closing it unlocks `/gsd:complete-milestone v1.2`. The UAT must cover all 6 requirements (TAGS-01..04, DUP-01..02) AND the 15 locked decisions — gap-free verdict → close + commit + advance; gaps-found verdict → record the gaps with enough detail that the planner can author a 15.x gap-closure plan set without re-discovering the issues.

Output: 15-VERIFICATION.md with verdict + per-criterion + per-decision rows + console-error log + bundle-size delta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md
@.planning/phases/15-tags-search-quick-duplicate/15-01-SUMMARY.md
@.planning/phases/15-tags-search-quick-duplicate/15-02-SUMMARY.md
@.planning/phases/15-tags-search-quick-duplicate/15-03-SUMMARY.md
@.planning/phases/15-tags-search-quick-duplicate/15-04-SUMMARY.md
@.planning/phases/15-tags-search-quick-duplicate/15-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification chain (tsc + build + vitest) and capture results</name>
  <files>(none — runs commands and captures output)</files>
  <read_first>
    - .planning/phases/15-tags-search-quick-duplicate/15-01-SUMMARY.md through 15-05-SUMMARY.md (the cumulative SUMMARYs — to know what should be verified)
    - package.json scripts (to confirm `build`, `test`, `tsc` invocations)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md (full file — to enumerate D-01..D-15 for the UAT checklist)
  </read_first>
  <action>
    Run the following four commands in order, capturing the output of each to a temporary log file (any project-side scratch path is fine — `/tmp/15-verify-{step}.log`). If any step fails, STOP and record the failure in the UAT report rather than proceeding. (1) `npx tsc -b 2>&1 | tee /tmp/15-verify-tsc.log` — expect exit 0, zero error lines. (2) `npm test -- --run 2>&1 | tee /tmp/15-verify-vitest.log` — expect all tests pass; note the test count delta vs the baseline recorded in Plan 12 (the latest known baseline is the 110/110 from Phase 14 closure plus Phase 16 ext2 additions; Plan 15-01 adds 6 tests, Plan 15-02 adds 7+, Plan 15-03 adds 6 — net delta ≥ 19). (3) `npm run build 2>&1 | tee /tmp/15-verify-build.log` — expect clean exit; capture the main chunk size from the Vite output (look for the `dist/assets/index-*.js` size line). (4) If the project has a bundle-size assertion script (per Phase 11 ROADMAP — `scripts/assert-bundle-size.mjs`), run it and capture: `node scripts/assert-bundle-size.mjs 2>&1 | tee /tmp/15-verify-bundle.log` (if the script does not exist, skip and note that the manual bundle-size check from step 3 is the verification surface). Aggregate all four outputs into a single summary section that will be embedded in 15-VERIFICATION.md.
  </action>
  <verify>
    <automated>cat /tmp/15-verify-tsc.log /tmp/15-verify-vitest.log /tmp/15-verify-build.log 2>/dev/null | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/15-verify-tsc.log` exists and contains zero "error TS" matches: `grep -c "error TS" /tmp/15-verify-tsc.log` returns `0`
    - `/tmp/15-verify-vitest.log` shows ALL test files passing (no `FAIL` lines): `grep -c "^FAIL\|Test Files.*failed" /tmp/15-verify-vitest.log` returns `0`
    - `/tmp/15-verify-vitest.log` shows at least 19 more passing tests than the pre-Phase-15 baseline (Plan 15-01: +6, Plan 15-02: +7, Plan 15-03: +6) — record the actual delta in VERIFICATION.md
    - `/tmp/15-verify-build.log` shows `built in` with no error lines: `grep -c "✓ built\|built in" /tmp/15-verify-build.log` returns at least `1`
    - The main chunk gz size (extracted from the build log) is recorded for the VERIFICATION.md bundle-size delta row; the absolute number is captured even when no assertion script exists
  </acceptance_criteria>
  <done>All four automated steps captured to /tmp logs; tsc clean; vitest all-pass with delta ≥ 19; build clean; main chunk size recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — verify every ROADMAP Success Criterion + every D-XX decision in a real browser</name>
  <what-built>
    The full Phase 15 surface has been implemented across Plans 15-01 through 15-05:
    - normalizeTagsOnJob pure helper + parseTagsInput input parser (D-02 + D-12)
    - duplicateJob + nextCopyName pure helpers with D-15 locked test contract (D-08 + D-09)
    - CostCalculator tag input field with NewBadge `tags` label-inline (D-01 + D-13)
    - JobsManager sticky sub-header: search input (250ms debounce) + chip filter row + Clear filters + filter-empty-state (D-03 + D-04 + D-06 + D-10 + D-14)
    - JobsManager useDynamicRowHeight cache key extended to pipe-delimited tri-key (D-05)
    - JobsManager JobCard: tag chips display + inline tag editor + [⋯] overflow menu with Duplicate + post-duplicate scroll+highlight (D-07 + D-11)
    - features.ts: 3 new entries (tags, search-jobs, quick-duplicate) with NewBadge JSX consumers wired per D-13
    - useDatabase.ts: tagsNormalizeRan flag + reconcile effect mirroring copiesSoldReconcileRan (D-12)
    Automated chain in Task 1 verified tsc + vitest + build all clean.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev` (port 4173). Open http://localhost:4173 in a real browser (NOT a headless harness — Vercel-style DevTools session).

    **Per-Success-Criterion checks** (5 total — every one must PASS or be recorded as a gap):

    SC#1 (TAGS-01 — tag input lifecycle):
      - Create a new job via the CostCalculator. In the Tags field, type `PLA, Phone-Stand, GLOSS , pla, 🎉, , ` and Save.
      - Reload the page; open the saved job. Confirm `editingJob.tags` displays as `pla, phone-stand, gloss` in the input (lowercase, trimmed, deduped, emoji stripped, empties dropped).
      - Try typing 12 tags; confirm the "Maximum 10 tags — extras ignored" warning appears and only 10 persist on save.
      - Save a job with an empty Tags field; reload and confirm in DevTools → IndexedDB that `tags: undefined` (NOT `tags: []`).

    SC#2 (TAGS-02 — chip filter with AND logic):
      - Have at least 3 saved jobs covering tags `pla`, `phone-stand`, `vase-mode` with varied overlap. Confirm the chip filter row at top of JobsManager shows every tag in use alphabetically with counts like `phone-stand · 2`.
      - Click `pla` chip; list filters to PLA jobs. Click `phone-stand` while `pla` is selected; list filters to jobs with BOTH (AND logic). Click `pla` again to deselect; only phone-stand jobs visible.
      - Click "Clear filters" link; full list returns.

    SC#3 (TAGS-03 — case-insensitive substring search across title + customer + tags):
      - In the search input, type `Alice` — only jobs whose ANY Sale has a customer named "Alice" (or any tag matches "alice", or job name contains "alice") appear.
      - Type `phone` — jobs with the substring in name OR any tag appear.
      - Confirm typing is debounced — the list doesn't refire on every keystroke (≈250ms delay).

    SC#4 (TAGS-04 — virtualization cache invalidation):
      - With several jobs visible, click a job to expand it (selectedJobId set, row grows). Now type a search query that hides that row. Clear the search.
      - Watch for stale row heights or visual overlap on the previously-expanded row — there should be NONE; the list should re-measure cleanly. Open DevTools and confirm no `react-window` warnings in the console.
      - Toggle 2-3 chips on and off; confirm the list scrolls to top each time the filter changes (this falls out of the cache key changing — react-window resets scroll position when the key invalidates).

    SC#5 (DUP-01 + DUP-02 — Quick Duplicate from row action):
      - On a job with a customer attached (record a Sale with customer name), click `[⋯] → Duplicate`.
      - Confirm: new row appears at the top of the list (`{name} (copy)`); the duplicate has NO customer (open the Orders sub-section — empty); the duplicate's tax row resolves from current region/Settings default (taxRate not inherited); copiesSold is 0; new row briefly highlights with a blue ring.
      - Duplicate the same job again; confirm name `{name} (copy 2)`.
      - Confirm the unit test in Plan 15-02 still passes (already verified in Task 1's vitest run; this is a re-confirmation that the human UI flow matches the unit-tested contract).

    **Per-Decision checks** (15 D-XX — at least visually confirm each):
      - D-01: Tag input visible on BOTH CostCalculator (save form) AND JobsManager (inline editor on JobCard pencil button)
      - D-02: Normalization rules confirmed above (SC#1)
      - D-03: Sticky sub-header layout — search on top, chip row below; sorted alphabetically with counts
      - D-04: AND combination confirmed above (SC#2)
      - D-05: Cache key in DevTools sources — view the React component tree, find useDynamicRowHeight, confirm key string contains pipe delimiters
      - D-06: Search scope — confirmed above; ALSO confirm that searching for an address substring does NOT match (D-06 excludes address/notes)
      - D-07: [⋯] overflow menu UX — single item Duplicate; no confirmation modal; toast/highlight visible
      - D-08: Collision-counter format confirmed above (SC#5)
      - D-09: customer + taxRate + quoteNumber reset confirmed above (SC#5)
      - D-10: Filter empty state — apply a filter that matches no jobs; confirm the "No jobs match your filter" block AND the filter sub-header remain visible together
      - D-11: Chip rendering — chips appear inline on JobCard summary line; styling matches AssetLibrary (open AssetLibrary tab and side-by-side compare — they MUST look identical: `text-xs px-1.5 py-0.5 rounded bg-slate-600/50 text-slate-400`)
      - D-12: Reconcile — open DevTools → IndexedDB → 3DCosterDB → jobs; manually edit a tag to `'PLA'` (uppercase); reload page; confirm tag is now `'pla'` after reload (the normalize ran)
      - D-13: NewBadge wiring — confirm THREE badges visible in correct positions: `tags` on the CostCalculator tag input label (inline, doesn't push siblings), `search-jobs` on the JobsManager search input wrapper (absolute overlay top-right), `quick-duplicate` on the JobCard [⋯] button (absolute overlay top-right). Confirm NO layout reflow when a badge appears (project memory rule)
      - D-14: At < 640px (resize browser), chip row scrolls horizontally; search input stays full-width; [⋯] menu opens as a dropdown (NOT a full-screen sheet)
      - D-15: Confirmed by Vitest in Task 1

    **Console error check:** Open DevTools → Console; confirm zero error/warning lines added by Phase 15 work (existing warnings from prior phases acceptable if pre-existing).

    **Desktop (Tauri) smoke test:** `npm run tauri:dev`; confirm the new surfaces render in the Tauri webview without errors. Spot-check chip filter + search + duplicate flow.

    **Reply with the verdict:** "approved-gap-free" if every criterion + decision passes; "approved-with-gaps" with a numbered list of failures (e.g. "Gap A: chip count doesn't update when tag is added inline; Gap B: duplicate scroll-to-top fires but doesn't visually scroll on Firefox"); "rejected" if a critical regression broke prior functionality.
  </how-to-verify>
  <resume-signal>Reply with "approved-gap-free", "approved-with-gaps" + numbered gap list, or "rejected" + reason.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 15-VERIFICATION.md recording verdict + per-criterion + per-decision + automated chain results</name>
  <files>.planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md</files>
  <read_first>
    - /tmp/15-verify-tsc.log, /tmp/15-verify-vitest.log, /tmp/15-verify-build.log (Task 1 outputs)
    - The user's resume-signal verdict from Task 2 (gap-free / gaps-found / rejected)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md (D-01..D-15 — full list for the per-decision coverage table)
    - .planning/ROADMAP.md Phase 15 Success Criteria #1..5
    - Prior VERIFICATION.md examples in the repo (e.g. .planning/phases/14-customer-details/14-VERIFICATION.md or .planning/phases/16-printable-pdf-quote/16-VERIFICATION.md) — use whichever format matches the project convention
  </read_first>
  <action>
    Create `.planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md` with: (1) YAML frontmatter with `phase`, `type: verification`, `verified: 2026-05-24`, `verdict: gap-free|gaps-found|rejected` (reflect the user's resume-signal). (2) An `## Automated Chain` section listing tsc/vitest/build results with the exact test-count delta and main-chunk gz size from Task 1's logs. (3) A `## Per Success Criterion (5)` table with one row per SC#1..5 listing: criterion ID, expected behavior summary, UAT result (PASS / FAIL — gap description if FAIL), references to the implementing plan(s). (4) A `## Per Decision (D-01..D-15)` table with one row per decision listing: D-XX, brief expectation, verified-by (UAT step or unit test), result (PASS / FAIL). (5) An `## Requirement Closure` section listing TAGS-01, TAGS-02, TAGS-03, TAGS-04, DUP-01, DUP-02 each marked Complete or Outstanding based on the per-SC results. (6) A `## Gaps` section — empty if verdict is gap-free; otherwise a numbered list with each gap's symptom + suspected cause + the D-XX or SC# it violates + recommended fix surface (file + brief approach). (7) If verdict is `gap-free`: a `## Next Steps` section noting that Phase 15 is ready to close and the milestone v1.2 audit/complete commands can run; update `.planning/STATE.md` to mark Phase 15 complete (`progress.completed_phases` from 4 to 5; `progress.percent` from 67 to 83; `status: complete-pending-milestone-close`; add a Decisions row "[v1.2 Phase 15 closure 2026-05-24]: 6/6 requirements verified PASS"). If verdict is `gaps-found`: do NOT update STATE.md — leave the phase open and note in the report that `/gsd:plan-phase 15 --gaps` is the next command. (8) Commit the VERIFICATION.md (and STATE.md if updated) with message `docs(15): record UAT verification — {verdict}` per the project commit convention.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md && grep -c "verdict:\|Success Criterion\|D-01\|D-15" .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md`
    - YAML frontmatter has `phase: 15-tags-search-quick-duplicate`, `type: verification`, `verdict: <one of gap-free|gaps-found|rejected>`
    - Per-Success-Criterion table has exactly 5 rows (one per ROADMAP SC#1..5)
    - Per-Decision table has exactly 15 rows (D-01 through D-15)
    - Requirement Closure section lists all 6 requirement IDs (TAGS-01, TAGS-02, TAGS-03, TAGS-04, DUP-01, DUP-02) with explicit Complete/Outstanding status
    - Automated chain section records tsc exit, vitest delta, build size
    - If verdict is gap-free: STATE.md `completed_phases` incremented to 5 AND a Decisions row added
    - If verdict is gaps-found: a numbered Gaps section enumerates each, each gap has a "violates" D-XX or SC# cite
    - Commit created with the docs(15) message
  </acceptance_criteria>
  <done>VERIFICATION.md committed; STATE.md updated (only on gap-free verdict); user has the next-step command (close milestone OR plan-gap-closure) ready to run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification → STATE.md | The auto-update on gap-free verdict is the only mutating action; gated by the human verdict from Task 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-13 | Tampering | premature STATE.md "complete" mark | mitigate | STATE.md only updated when the human checkpoint (Task 2) returns `approved-gap-free`; gaps-found / rejected verdicts leave the phase open |
| T-15-14 | Information Disclosure | UAT report leaks PII from test data | accept | Test data is local-only; VERIFICATION.md should not include literal customer names from the user's IndexedDB — use placeholder names like "Alice" in the UAT instructions only |
</threat_model>

<verification>
- tsc + vitest + build all clean (Task 1)
- Human UAT signs off (Task 2)
- VERIFICATION.md committed with verdict matching the signal
- STATE.md updated only when verdict is gap-free
- All 5 ROADMAP Success Criteria explicitly addressed
- All 15 D-XX decisions explicitly addressed
</verification>

<success_criteria>
- All 6 phase requirements verified PASS (TAGS-01, TAGS-02, TAGS-03, TAGS-04, DUP-01, DUP-02) on a gap-free verdict
- Bundle-size delta recorded (Phase 11 300 KB gz gate holds)
- VERIFICATION.md serves as the audit trail for milestone closure
- Phase 15 is either marked complete (gap-free) or has an actionable gap list ready for a 15.x gap-closure plan set
- v1.2 milestone is one `gsd:complete-milestone v1.2` command away on gap-free verdict (5 of 6 v1.2 phases would now be complete with Phase 16 already at 12 of 13 plans)
</success_criteria>

<output>
Create `.planning/phases/15-tags-search-quick-duplicate/15-06-SUMMARY.md` when done. Include: verdict; test-count delta; main-chunk gz size; gap list (empty if gap-free); the exact `git log -1` line for the verification commit; next command for the user (either `/gsd:complete-milestone v1.2` or `/gsd:plan-phase 15 --gaps`).
</output>
