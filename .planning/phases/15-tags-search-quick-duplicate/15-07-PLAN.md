---
phase: 15-tags-search-quick-duplicate
plan: 07
type: execute
wave: 5
depends_on: []
files_modified:
  - src/components/CostCalculator.tsx
autonomous: true
gap_closure: true
gap_id: A
requirements: [TAGS-01]
requirements_addressed: [TAGS-01]
tags: [cost-calculator, tag-input-removal, scope-refinement, gap-closure]

must_haves:
  truths:
    - "Saving a job from CostCalculator (Create branch) no longer accepts a tag value from the form — the user enters tags exclusively from JobsManager."
    - "Saving an edited job from CostCalculator (Update branch) preserves any pre-existing tags on the source job — tags are NOT cleared on update."
    - "The Tags <label> + <input> row that previously sat under the Author Min Price row is gone from the CostCalculator form."
    - "The `tags` feature key in src/features.ts is preserved (the JSX consumer site moves to JobsManager in a later gap; the entry stays at 2026-05-24)."
    - "parseTagsInput remains exported from src/db/backfill.ts — JobsManager's inline tag editor (Plan 15-05, and the Gap B reshape) still imports it."
  artifacts:
    - path: src/components/CostCalculator.tsx
      provides: "Cost calculator with the Tags row, tagsInput state, and parseTagsInput call sites removed; tags pass-through on update preserves editingJob.tags."
      contains_not:
        - "tagsInput"
        - "setTagsInput"
        - "parseTagsInput"
        - 'feature="tags"'
  key_links:
    - from: src/components/CostCalculator.tsx (Update branch save handler ~line 605)
      to: editingJob.tags
      via: "Direct pass-through — `tags: editingJob.tags` (preserve source) instead of `tags: parseTagsInput(tagsInput)`"
      pattern: "tags:\\s*editingJob\\.tags"
    - from: src/components/CostCalculator.tsx (Create branch save handler ~line 641)
      to: PrintJob literal
      via: "Tags field omitted from new-job literal — undefined per D-02 'no tags ever set' semantic"
      pattern: 'tags:\\s*undefined|^(?!.*tags:\\s*parseTagsInput).*$'
---

<objective>
Gap A — Remove the Tags input row from the CostCalculator save form (both Create and Update branches).

This is a scope refinement gap from VERIFICATION.md (verdict gaps-found 2026-05-24): user product feedback determined that tags are not entered during costing. The CostCalculator surface (D-01 "surface a") is being WITHDRAWN. The JobsManager surface (D-01 "surface b") is the canonical tag input site after gap closure — it is reshaped separately in Plan 15-10 (Gap B).

Purpose: Honor the user's product feedback that "the user reasons about cost on the calculator; they reason about library organization on JobsManager" (VERIFICATION.md Gap A). Plan 15-07 closes Gap A by stripping the tag input surface from CostCalculator without touching the pure-helper parser, the JobsManager surface, the features.ts entry, or any persisted job.tags data.

Output: A surgical CostCalculator.tsx diff that removes the row + state + sessionStorage key + parser call sites. Locked behavior contracts: existing job.tags MUST be preserved on update; the tags feature key MUST stay in features.ts (consumer moves in Gap B); parseTagsInput MUST remain exported.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md
@.planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md
@.planning/phases/15-tags-search-quick-duplicate/15-03-SUMMARY.md
@.claude/CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted directly from CostCalculator.tsx so no codebase scan is required. -->

From src/components/CostCalculator.tsx (current state — pre-Gap-A):

Line 11 — import of parseTagsInput (KEEP, still needs to compile but the import becomes unused → either remove the import line, or keep it if a future task in this phase will re-add a consumer; in Gap A we REMOVE the import because there is no remaining consumer in CostCalculator):
```
import { parseTagsInput } from '../db/backfill';
```

Line 121 — tagsInput state (REMOVE entire line):
```
const [tagsInput, setTagsInput] = useState(() => getStoredValue('tagsInput', ''));
```

Line 174 — sessionStorage form state field (REMOVE):
```
tagsInput,
```

Line 194 — useEffect dependency array (REMOVE `tagsInput` token from the array):
```
..., modelUrl, tagsInput, prepTimeMinutes, ...
```

Line 210 — editingJob re-hydration (REMOVE entire line — editingJob.tags is preserved on update without round-tripping through state):
```
setTagsInput(editingJob?.tags?.join(', ') ?? '');
```

Line 527 — clearForm reset (REMOVE entire line):
```
setTagsInput('');
```

Line 605 — Update branch (REPLACE `parseTagsInput(tagsInput)` with the source-tags pass-through):
```
tags: parseTagsInput(tagsInput),   →   tags: editingJob.tags,
```

Line 641 — Create branch (REMOVE the `tags:` line entirely — `PrintJob.tags?: string[]` is optional and `undefined` is the D-02 "no tags ever set" semantic):
```
tags: parseTagsInput(tagsInput),   →   (line removed)
```

Lines 857-879 — the entire Tags `<div>` row (REMOVE — the wrapping `<div className="flex flex-wrap items-start gap-x-4 gap-y-3">` + inner `<div className="flex-1 min-w-[220px] max-w-md">` + label + Input + IIFE for the "Maximum 10 tags" warning):
```jsx
{/* Tags row — Phase 15 TAGS-01. Mirrors the Model URL JSX shape (label-inline
    NewBadge per D-13 + project memory rule; absolute-overlay reserved for buttons). */}
<div className="flex flex-wrap items-start gap-x-4 gap-y-3">
  <div className="flex-1 min-w-[220px] max-w-md">
    <label className="flex items-center gap-1.5 text-xs text-slate-400 mb-1">
      <span>Tags</span>
      <InfoTooltip text="Comma-separated. Lowercase, max 10. ..." />
      <NewBadge feature="tags" />
    </label>
    <Input ... value={tagsInput} onChange={e => setTagsInput(e.target.value)} ... />
    {(() => {
      const parsed = parseTagsInput(tagsInput);
      return parsed && parsed.length >= 10
        ? <p className="text-xs text-amber-400 mt-1">Maximum 10 tags — extras ignored</p>
        : null;
    })()}
  </div>
</div>
```

From src/db/backfill.ts:
```
export function parseTagsInput(input: string): string[] | undefined
```
KEEP this export — still consumed by JobsManager.tsx (Plan 15-05 inline editor; will continue to be consumed by Gap B's reshaped surface in Plan 15-10).

From src/features.ts:
```
'tags': new Date('2026-05-24'),
```
KEEP this entry — Plan 15-10 (Gap B) will re-target the JSX consumer in JobsManager.

From src/types.ts (PrintJob):
```
tags?: string[];   // optional — undefined is "no tags ever set" (D-02 line 43)
```
Omitting the `tags:` field in the Create literal is equivalent to `tags: undefined` which is the D-02 spec for "no tags ever set" semantics.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the Tags row, state, and parser call sites from CostCalculator</name>

  <read_first>
    - src/components/CostCalculator.tsx — lines 1-15 (imports), 119-125 (tagsInput state), 165-200 (sessionStorage useEffect + deps), 200-215 (editingJob re-hydration), 515-530 (clearForm), 575-650 (save handlers — both branches), 855-880 (Tags row JSX)
    - .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md — "Gap A — Tag input surface should not exist on CostCalculator save form" section
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md — D-02 (line 43: "Empty input → tags: undefined (not [])")
  </read_first>

  <files>src/components/CostCalculator.tsx</files>

  <action>
    Remove the Tags input surface from CostCalculator. All edits are in src/components/CostCalculator.tsx. Do NOT touch src/db/backfill.ts, src/features.ts, src/components/JobsManager.tsx, or any test file.

    Step 1 — Imports (~line 11): Remove the `import { parseTagsInput } from '../db/backfill';` line. No remaining call site in this file after the rest of the edits.

    Step 2 — State (~line 121): Remove the `const [tagsInput, setTagsInput] = useState(() => getStoredValue('tagsInput', ''));` declaration.

    Step 3 — sessionStorage form state (~line 174): Remove the `tagsInput,` field from the `formState` object literal inside the persist-form-state useEffect.

    Step 4 — useEffect dependency array (~line 194): Remove the `tagsInput,` token from the dependency array of the same useEffect.

    Step 5 — Edit re-hydration (~line 210): Remove the `setTagsInput(editingJob?.tags?.join(', ') ?? '');` line from the `if (editingJob) { ... }` block. editingJob.tags is preserved on update via the source pass-through in Step 7 — no round-trip through state is needed.

    Step 6 — clearForm (~line 527): Remove the `setTagsInput('');` line from the `clearForm` callback.

    Step 7 — Update branch save handler (~line 605): Change `tags: parseTagsInput(tagsInput),` to `tags: editingJob.tags,` so existing job.tags are preserved on update. CRITICAL: This is a pass-through, NOT a clear-on-update. The user's product intent is "tags are not entered on the calculator" — but tags ALREADY ON a saved job MUST survive a CostCalculator round-trip; otherwise editing the cost of a tagged job would silently drop its tags.

    Step 8 — Create branch save handler (~line 641): Remove the `tags: parseTagsInput(tagsInput),` line entirely from the new-job PrintJob literal. PrintJob.tags is optional; omitting the property is equivalent to `tags: undefined`, which is the D-02 "no tags ever set" semantic (CONTEXT.md line 43).

    Step 9 — JSX Tags row (~lines 857-879): Remove the entire outer `<div className="flex flex-wrap items-start gap-x-4 gap-y-3">` block that wraps the inner Tags `<div className="flex-1 min-w-[220px] max-w-md">` (label + Input + IIFE warning). Remove the JSDoc-style comment immediately above it ("Tags row — Phase 15 TAGS-01. Mirrors the Model URL JSX shape...") as well.

    Step 10 — Optional cleanup: After the above, verify no `tagsInput` / `setTagsInput` / `parseTagsInput` token remains anywhere in src/components/CostCalculator.tsx. If found, delete the remaining occurrence.

    Decision log to note in the SUMMARY (no need to modify the plan):
    - Why omit `tags:` in Create rather than write `tags: undefined`: per D-02 line 43 in CONTEXT.md, `tags: undefined` (not `[]`) preserves the "no tags ever set" semantic. PrintJob.tags is `tags?: string[]`. TypeScript treats an omitted optional property as identical to `tags: undefined` at type-check; either expression is correct. Choosing to omit is the smaller diff and reads cleaner.
    - Why preserve editingJob.tags on Update: gap-closure CONSTRAINT does NOT permit silently dropping pre-existing tags during a cost-edit round-trip. Confirmed by VERIFICATION.md Gap A — the surface is being removed, but the data on existing tagged jobs MUST survive Edit. The pass-through is the safe pattern.

    Do NOT touch:
    - src/db/backfill.ts (parseTagsInput export stays — JobsManager.tsx still imports it)
    - src/features.ts (the `'tags'` entry stays — Plan 15-10 re-targets the JSX consumer to JobsManager)
    - src/components/JobsManager.tsx (Plan 15-10 owns the JobsManager-side reshape)
    - src/utils/duplicateJob.ts and src/utils/duplicateJob.test.ts (LOCKED — DUP-02)
    - Any test file (existing parseTagsInput Vitest cases continue to pass; no new tests are required for this plan per VERIFICATION.md "Tests:" line for Gap A)
  </action>

  <verify>
    <automated>
      grep -nE 'tagsInput|setTagsInput|parseTagsInput|feature="tags"' src/components/CostCalculator.tsx | grep -v '^#' | wc -l   # MUST output 0

      grep -nE 'tags:\s*editingJob\.tags' src/components/CostCalculator.tsx | wc -l   # MUST output 1 (Update branch pass-through)

      grep -nE 'tags:\s*parseTagsInput' src/components/CostCalculator.tsx | wc -l   # MUST output 0

      grep -n "^import.*parseTagsInput" src/components/CostCalculator.tsx | wc -l   # MUST output 0

      grep -n "^import.*parseTagsInput" src/components/JobsManager.tsx | wc -l   # MUST output 1 (Plan 15-05 import survives untouched)

      grep -n "'tags':" src/features.ts | wc -l   # MUST output 1 (entry preserved for Gap B re-target)

      npx tsc -b   # MUST exit 0 (no unused-imports lint error since import is fully removed; no missing-binding error since all references are removed)

      npm test -- --run   # MUST be 263 passed / 1 todo (no Vitest regressions — gap A does not modify test files)
    </automated>
  </verify>

  <acceptance_criteria>
    - CostCalculator.tsx contains ZERO occurrences of `tagsInput`, `setTagsInput`, `parseTagsInput`, or `feature="tags"`.
    - The Update-branch save handler contains EXACTLY ONE `tags: editingJob.tags` line (pass-through preserves source).
    - The Create-branch save handler contains ZERO `tags:` lines (omitted → undefined per D-02).
    - The Tags JSDoc comment ("Tags row — Phase 15 TAGS-01") and its entire `<div>` block are removed.
    - The `import { parseTagsInput } from '../db/backfill';` line is removed from CostCalculator.tsx.
    - src/db/backfill.ts is UNCHANGED — `parseTagsInput` is still exported (JobsManager.tsx still consumes it).
    - src/features.ts is UNCHANGED — the `'tags': new Date('2026-05-24')` entry is preserved (Plan 15-10 will move the JSX consumer to JobsManager).
    - `npx tsc -b` exits 0.
    - `npm test -- --run` reports 263 passed / 1 todo / 0 failed (no test changes in this plan).
    - src/utils/duplicateJob.ts and src/utils/duplicateJob.test.ts are byte-identical to their pre-plan state (verify via `git diff src/utils/duplicateJob.ts src/utils/duplicateJob.test.ts` — output MUST be empty).
  </acceptance_criteria>

  <done>
    CostCalculator no longer renders a Tags input row. The save form has zero tag-related state, zero parseTagsInput call sites, and zero JSX for the Tags label/input/warning. Editing an existing tagged job round-trips through CostCalculator without losing its tags (pass-through). Creating a new job from CostCalculator writes the job with `tags === undefined`. JobsManager remains the sole tag-entry surface (current state pre-Gap-B; Gap B reshapes it). parseTagsInput, the features.ts `tags` entry, and the JobsManager Plan 15-05 inline editor are all untouched.
  </done>
</task>

</tasks>

<verification>
- Run all 9 grep / tsc / vitest commands listed in `<verify><automated>` above.
- Manually scan CostCalculator.tsx for stray references to `Tags row`, `Tags input`, `tagsInput` in comments — remove any leftover comments that name the removed surface.
- Confirm `git diff src/utils/duplicateJob.ts src/utils/duplicateJob.test.ts` is empty.
- Confirm `git diff src/db/backfill.ts src/features.ts src/components/JobsManager.tsx` is empty for this plan (other gap plans modify these in later waves).
</verification>

<success_criteria>
- The Tags input surface is gone from CostCalculator (Gap A closed).
- Existing tagged jobs survive a CostCalculator Edit round-trip.
- New jobs from CostCalculator save with `tags === undefined`.
- All locked artifacts (DUP-02 helper + tests, parseTagsInput export, `tags` features.ts entry, JobsManager Plan 15-05 surfaces) are untouched.
- Test count: 263 passed / 1 todo / 0 failed (no delta from Phase 15 verification baseline).
- TypeScript compiles clean.
</success_criteria>

<output>
Create .planning/phases/15-tags-search-quick-duplicate/15-07-SUMMARY.md when done.

Summary must record:
- Files modified (CostCalculator.tsx only) and the line-count delta
- Confirmation that parseTagsInput, features.ts tags entry, JobsManager imports, and DUP-02 helper+tests are all untouched
- Vitest result (expected 263 passed / 1 todo)
- tsc result (expected exit 0)
- Decision: chose `omit` over `tags: undefined` literal in Create branch (justified by D-02 + smaller diff)
- Decision: preserved editingJob.tags via pass-through on Update branch (NOT clear-on-update — protects existing tagged jobs)
- Confirmation that Gap A's "Recommended fix surface" from VERIFICATION.md was executed verbatim
</output>
