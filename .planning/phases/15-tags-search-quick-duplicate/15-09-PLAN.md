---
phase: 15-tags-search-quick-duplicate
plan: 09
type: execute
wave: 6
depends_on: ["15-08"]
files_modified:
  - src/components/JobsManager.tsx
autonomous: true
gap_closure: true
gap_id: C
requirements: [TAGS-02, TAGS-04]
requirements_addressed: [TAGS-02, TAGS-04]
tags: [jobs-manager, chip-filter-removal, cache-key-narrowing, scope-refinement, tags-02-withdrawn, gap-closure]

must_haves:
  truths:
    - "The multi-select chip-filter row beneath the search input is gone from the JobsManager sticky sub-header."
    - "The search input remains the canonical filter mechanism (D-06 scope: title + tag + Sale.customer.{name,email,company}; 250ms debounce)."
    - "The useDynamicRowHeight cache key is narrowed from the pipe-delimited tri-key (`selectedJobId|selectedChipsKey|debouncedSearchQuery`) to the pipe-delimited bi-key (`selectedJobId|debouncedSearchQuery`) — TAGS-04 contract preserved with one fewer segment."
    - "The filter-empty-state block still fires when search returns 0 results — the message still appears, but the 'Clear filters' link is renamed to 'Clear search' (and `clearFilters` is simplified to a search-only clearer)."
    - "TAGS-02 (the chip-filter requirement) is closed as Withdrawn — REQUIREMENTS.md already records this; this plan ships the code change that aligns the runtime with the locked withdrawal."
    - "D-03 (sticky sub-header chip row), D-04 (AND combination semantics), D-14 (mobile chip horizontal scroll) all collapse — no chip surface remains."
  artifacts:
    - path: src/components/JobsManager.tsx
      provides: "JobsManager with the chip-filter row + selectedChips state + selectedChipsKey memo + tagCounts memo + jobsAfterChipFilter memo all removed; useDynamicRowHeight key narrowed to bi-key; filter-empty-state messaging updated to 'Clear search'."
      contains_not:
        - "selectedChips"
        - "selectedChipsKey"
        - "tagCounts"
        - "jobsAfterChipFilter"
        - "Clear filters"
        - "overflow-x-auto sm:overflow-visible"
      contains:
        - "key: `${selectedJobId ?? ''}|${debouncedSearchQuery}`"
        - "Clear search"
  key_links:
    - from: src/components/JobsManager.tsx (useDynamicRowHeight call)
      to: react-window cache invalidation
      via: "Bi-key (selectedJobId + debouncedSearchQuery) pipe-delimited per D-05 (reshaped per VERIFICATION.md Gap C)"
      pattern: 'key:\s*`\$\{selectedJobId \?\? ''\}\|\$\{debouncedSearchQuery\}`'
    - from: src/components/JobsManager.tsx (searchedJobs memo)
      to: virtualized list filter
      via: "Filter chain reduces from `jobs → jobsAfterChipFilter → searchedJobs` to `jobs → searchedJobs` directly — `jobs` is the input to the search memo after Gap C"
      pattern: "useMemo\\(\\(\\)\\s*=>\\s*\\{[^}]*const q = debouncedSearchQuery"
---

<objective>
Gap C — Remove the multi-select chip-filter row from the JobsManager sticky sub-header. Per VERIFICATION.md (verdict gaps-found 2026-05-24): the search input already substring-matches tag names, making the chip strip redundant clutter. WITHDRAWS TAGS-02 + D-03 chip row + D-04 AND-combination semantics + D-14 mobile horizontal scroll. ROADMAP Phase 15 SC#2 is already annotated as withdrawn; REQUIREMENTS.md already marks TAGS-02 as Withdrawn. This plan ships the code change that aligns the runtime with those locked documentary decisions.

Purpose: Honor the user's product feedback ("the chip-filter strip is redundant with the search bar"). Reduce visual clutter, simplify the filter pipeline (one filter dimension instead of two), and narrow the TAGS-04 cache key from tri-key to bi-key. The search input becomes the canonical and only filter mechanism.

Output: A surgical JobsManager.tsx diff that:
1. Removes the chip-filter row JSX from the sticky sub-header.
2. Removes the `selectedChips` state, `selectedChipsKey` memo, `tagCounts` memo, and `jobsAfterChipFilter` memo.
3. Inlines the search filter to operate directly on `jobs` (the new input) instead of `jobsAfterChipFilter`.
4. Narrows the `useDynamicRowHeight` cache key from `${selectedJobId ?? ''}|${selectedChipsKey}|${debouncedSearchQuery}` to `${selectedJobId ?? ''}|${debouncedSearchQuery}` (TAGS-04 contract: still pipe-delimited, collision-proof; one fewer segment).
5. Simplifies the `clearFilters` callback to `clearSearch` (search-only).
6. Renames the filter-empty-state CTA from "Clear filters" to "Clear search".

The search input affordance (with its `×` clear-X button and `feature="search-jobs"` NewBadge) is preserved verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md
@.planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md
@.planning/phases/15-tags-search-quick-duplicate/15-04-SUMMARY.md
@.planning/phases/15-tags-search-quick-duplicate/15-08-SUMMARY.md
@.claude/CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted directly from JobsManager.tsx as it will exist post-Plan-15-08. -->

Plan 15-08 has already landed in Wave 5 (Gap D removed the `[⋯]` overflow menu, the duplicate handlers, etc.). The line numbers below are approximate (pre-Plan-15-08); the executor must locate identifiers by name, not line number, because Plan 15-08 has shifted everything.

The following state slots / memos / handlers must be removed (located by name):

1. `selectedChips` state declaration (was ~line 959 pre-08):
```
const [selectedChips, setSelectedChips] = useState<Set<string>>(() => new Set());
```

2. `tagCounts` memo (was ~lines 1026-1036 pre-08):
```
const tagCounts = useMemo(() => {
  const counts = new Map<string, number>();
  for (const job of jobs) {
    for (const tag of job.tags ?? []) {
      counts.set(tag, (counts.get(tag) ?? 0) + 1);
    }
  }
  return [...counts.entries()].sort(([a], [b]) => a.localeCompare(b));
}, [jobs]);
```

3. `selectedChipsKey` memo (was ~lines 1038-1043 pre-08):
```
const selectedChipsKey = useMemo(
  () => [...selectedChips].sort().join(','),
  [selectedChips]
);
```

4. `jobsAfterChipFilter` memo (was ~lines 1045-1055 pre-08):
```
const jobsAfterChipFilter = useMemo(() => {
  if (selectedChips.size === 0) return jobs;
  return jobs.filter(job => {
    const jobTags = new Set(job.tags ?? []);
    for (const chip of selectedChips) {
      if (!jobTags.has(chip)) return false;
    }
    return true;
  });
}, [jobs, selectedChips]);
```

5. `searchedJobs` memo (was ~lines 1064-1077 pre-08) — MUST be edited (NOT removed). Replace its input `jobsAfterChipFilter` with `jobs` so the filter chain becomes `jobs → searchedJobs` directly:

BEFORE:
```
const searchedJobs = useMemo(() => {
  const q = debouncedSearchQuery.toLowerCase().trim();
  if (!q) return jobsAfterChipFilter;
  return jobsAfterChipFilter.filter(job => {
    if (job.name.toLowerCase().includes(q)) return true;
    if (job.tags?.some(t => t.toLowerCase().includes(q))) return true;
    const jobSales = salesByJob.get(job.id) ?? [];
    return jobSales.some(s =>
      (s.customer?.name || '').toLowerCase().includes(q) ||
      (s.customer?.email || '').toLowerCase().includes(q) ||
      (s.customer?.company || '').toLowerCase().includes(q)
    );
  });
}, [jobsAfterChipFilter, salesByJob, debouncedSearchQuery]);
```

AFTER:
```
const searchedJobs = useMemo(() => {
  const q = debouncedSearchQuery.toLowerCase().trim();
  if (!q) return jobs;
  return jobs.filter(job => {
    if (job.name.toLowerCase().includes(q)) return true;
    if (job.tags?.some(t => t.toLowerCase().includes(q))) return true;
    const jobSales = salesByJob.get(job.id) ?? [];
    return jobSales.some(s =>
      (s.customer?.name || '').toLowerCase().includes(q) ||
      (s.customer?.email || '').toLowerCase().includes(q) ||
      (s.customer?.company || '').toLowerCase().includes(q)
    );
  });
}, [jobs, salesByJob, debouncedSearchQuery]);
```

6. `useDynamicRowHeight` cache key (was ~lines 1606-1609 pre-08) — MUST be edited (NOT removed). Narrow from tri-key to bi-key:

BEFORE:
```
const rowHeightCache = useDynamicRowHeight({
  defaultRowHeight: 88,
  key: `${selectedJobId ?? ''}|${selectedChipsKey}|${debouncedSearchQuery}`,
});
```

AFTER:
```
const rowHeightCache = useDynamicRowHeight({
  defaultRowHeight: 88,
  key: `${selectedJobId ?? ''}|${debouncedSearchQuery}`,
});
```

ALSO update the comment block immediately above the `useDynamicRowHeight` call to mention "bi-key (selectedJobId + debouncedSearchQuery)" instead of "tri-key (selectedJobId + chip filter + debounced search)". The comment is the only reader-facing explanation of the D-05 contract; keep it accurate.

7. `clearFilters` callback (was ~lines 1651-1655 pre-08) — RENAME and simplify:

BEFORE:
```
const clearFilters = useCallback(() => {
  setSelectedChips(new Set());
  setSearchQuery('');
}, []);
```

AFTER:
```
const clearSearch = useCallback(() => {
  setSearchQuery('');
}, []);
```

Rename ALL call sites of `clearFilters` to `clearSearch` (there are two: the in-header link inside the chip row — which is being removed in this plan anyway — and the filter-empty-state CTA below the sticky sub-header).

8. Chip-row JSX inside the sticky sub-header (was ~lines 1706-1746 pre-08) — REMOVE the entire `{tagCounts.length > 0 && ( ... )}` block. This deletes the chip-row `<div>`, its `flex-nowrap sm:flex-wrap gap-1.5 items-center overflow-x-auto sm:overflow-visible` styling, the `tagCounts.map(...)` body, and the in-header "Clear filters" link.

9. Filter-empty-state JSX (was ~lines 1749-1764 pre-08) — UPDATE the CTA copy:
- Change the heading from "No jobs match your filter" to "No jobs match your search".
- Change the help text from "Try different search terms or chip selections." to "Try a different search term, or clear search to see all jobs."
- Change the button text from "Clear filters" to "Clear search".
- Update the onClick handler reference from `clearFilters` to `clearSearch`.

10. Sticky sub-header wrapper styling (was ~line 1677 pre-08) — KEEP unchanged:
```
<div className="sticky top-0 z-10 bg-slate-800 pb-3">
```
The search input wrapper (with its `relative mb-3`, Input, clear-X button, and NewBadge) is preserved verbatim.

CRITICAL — DO NOT TOUCH:
- The `searchQuery` state, `debouncedSearchQuery` state, and 250ms debounce useEffect.
- The search input JSX (`<Input>`, `<SearchIcon>`, `×` clear-X button, `<NewBadge feature="search-jobs" ... />`).
- The `salesByJob` memo (used by the search filter; survives untouched).
- The `searchedJobs` size threshold at `searchedJobs.length > 100 ? <List ...> : <div>...map...</div>` — this branching survives.
- All Gap-B / Gap-D / pre-Plan-15-04 surfaces.
- src/utils/duplicateJob.ts and src/utils/duplicateJob.test.ts (LOCKED).
- src/db/backfill.ts (parseTagsInput remains exported).
- src/features.ts (entries left untouched in this plan — `tags` re-targets in Plan 15-10 / Gap B).
- src/hooks/useDatabase.ts (D-12 reconcile wiring intact).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove chip-filter UI, state, and memos; narrow cache key; rename clear-filters to clear-search</name>

  <read_first>
    - src/components/JobsManager.tsx — full file (find identifiers by name; Plan 15-08 has shifted line numbers). Specifically locate: `selectedChips`, `tagCounts`, `selectedChipsKey`, `jobsAfterChipFilter`, `searchedJobs`, `rowHeightCache`, `clearFilters`, the chip-row JSX (block starting with `{tagCounts.length > 0 && (`), and the filter-empty-state JSX (block starting with `{searchedJobs.length === 0 && jobs.length > 0`).
    - .planning/phases/15-tags-search-quick-duplicate/15-VERIFICATION.md — "Gap C — Multi-select chip-filter row in JobsManager sub-header is redundant with search and should be removed" section
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md — D-03 (chip filter — being WITHDRAWN), D-04 (AND combination — being WITHDRAWN), D-05 (cache key — being RESHAPED to bi-key), D-06 (search scope — UNCHANGED), D-14 (mobile chip strip — being WITHDRAWN)
    - .planning/phases/15-tags-search-quick-duplicate/15-04-SUMMARY.md — original sticky sub-header layout that is being narrowed
    - .planning/REQUIREMENTS.md — TAGS-02 (already marked Withdrawn) + TAGS-04 (cache-key reshape)
  </read_first>

  <files>src/components/JobsManager.tsx</files>

  <action>
    Execute Gap C. All edits are in src/components/JobsManager.tsx. Do NOT modify any other file.

    The executor MUST use grep to locate identifiers by name (the line numbers below are approximate; Plan 15-08 has shifted everything in this file).

    Step 1 — Remove `selectedChips` state: Delete the `const [selectedChips, setSelectedChips] = useState<Set<string>>(() => new Set());` line and its JSDoc comment block above it (which references "Phase 15 plan 04 (TAGS-02, TAGS-03, TAGS-04) — filter sub-header state... `selectedChips` holds the set of tags...").

    Step 2 — Remove `tagCounts` memo: Delete the entire `const tagCounts = useMemo(() => { const counts = new Map<string, number>(); for (const job of jobs) { for (const tag of job.tags ?? []) { counts.set(tag, (counts.get(tag) ?? 0) + 1); } } return [...counts.entries()].sort(([a], [b]) => a.localeCompare(b)); }, [jobs]);` block, including its JSDoc comment ("Phase 15 plan 04 — derive the in-use tag set with counts (D-03)...").

    Step 3 — Remove `selectedChipsKey` memo: Delete the entire `const selectedChipsKey = useMemo(() => [...selectedChips].sort().join(','), [selectedChips]);` block, including its JSDoc comment ("Stable string repr of the chip selection — feeds the D-05 cache key...").

    Step 4 — Remove `jobsAfterChipFilter` memo: Delete the entire `const jobsAfterChipFilter = useMemo(() => { if (selectedChips.size === 0) return jobs; ... }, [jobs, selectedChips]);` block, including its JSDoc comment ("Chip filter — AND across all selected chips (D-04)...").

    Step 5 — Edit `searchedJobs` memo (DO NOT REMOVE — this memo is the search filter and survives):
    - Replace every `jobsAfterChipFilter` token inside the body with `jobs`.
    - Update the dependency array from `[jobsAfterChipFilter, salesByJob, debouncedSearchQuery]` to `[jobs, salesByJob, debouncedSearchQuery]`.
    - Update the JSDoc comment above the memo: remove references to the chip-filter dimension, keep the D-06 scope description.

    Step 6 — Edit `useDynamicRowHeight` cache key (DO NOT REMOVE):
    - Change the `key:` line from `key: \`${selectedJobId ?? ''}|${selectedChipsKey}|${debouncedSearchQuery}\`,` to `key: \`${selectedJobId ?? ''}|${debouncedSearchQuery}\`,`.
    - Update the JSDoc comment block immediately above the `useDynamicRowHeight` call: change "tri-key" to "bi-key" and remove the "chip filter" segment from the explanation. The comment now reads (paraphrased): "key encodes selectedJobId + debounced search — pipe-delimited so the two segments can never collide. Either changing invalidates the cache → react-window resets row heights → the list scrolls to top (TAGS-04 cache invalidation contract)."

    Step 7 — Rename `clearFilters` → `clearSearch` and simplify:
    - Find the callback declaration: `const clearFilters = useCallback(() => { setSelectedChips(new Set()); setSearchQuery(''); }, []);`.
    - Replace with: `const clearSearch = useCallback(() => { setSearchQuery(''); }, []);`.
    - Update the JSDoc comment above the callback ("Phase 15 plan 04 — handler that clears both filter dimensions in one click.") to "Search-only clearer — used by the filter-empty-state CTA below the sticky sub-header (Gap C: TAGS-02 withdrawn 2026-05-24)."
    - Find every reference to `clearFilters` in JSX and rename to `clearSearch`. After Step 8 there is exactly one remaining reference (in the filter-empty-state CTA).

    Step 8 — Remove the chip-row JSX block from the sticky sub-header:
    - Find the JSX block that starts with the JSDoc comment "D-03 chip filter row + D-14 mobile overflow-x..." and renders `{tagCounts.length > 0 && ( <div className="flex flex-nowrap sm:flex-wrap gap-1.5 items-center overflow-x-auto sm:overflow-visible"> ... </div> )}`.
    - Delete the entire conditional block including its JSDoc comment.
    - The sticky sub-header now contains ONLY:
      - The wrapping `<div className="sticky top-0 z-10 bg-slate-800 pb-3">`.
      - The search input wrapper (`<div className="relative mb-3">` with SearchIcon + Input + clear-X button + NewBadge).
      - Nothing else.

    Step 9 — Update the filter-empty-state CTA copy:
    - Find the JSX block: `{searchedJobs.length === 0 && jobs.length > 0 ? ( <div className="text-center py-12"> <h3>No jobs match your filter</h3> ... <button onClick={clearFilters}>Clear filters</button> </div> )`.
    - Change `<h3 ...>No jobs match your filter</h3>` to `<h3 ...>No jobs match your search</h3>`.
    - Change the help text `Try different search terms or chip selections.` to `Try a different search term, or clear search to see all jobs.`.
    - Change `onClick={clearFilters}` to `onClick={clearSearch}`.
    - Change the button text `Clear filters` to `Clear search`.

    Step 10 — Audit: grep the file for any leftover chip-filter / clear-filters references. Expected post-edit grep counts:
    - `selectedChips`: 0
    - `selectedChipsKey`: 0
    - `tagCounts`: 0
    - `jobsAfterChipFilter`: 0
    - `clearFilters`: 0
    - `clear filters`: 0 (case-insensitive — check the "Clear filters" string is gone)
    - `Clear search`: 1 (filter-empty-state CTA)
    - `clearSearch`: 2 (callback declaration + onClick handler in filter-empty-state)
    - `tri-key`: 0 (comment updated to "bi-key")
    - `overflow-x-auto`: 0 in JobsManager.tsx (chip-row was the only consumer)

    Do NOT touch:
    - The `searchQuery`, `debouncedSearchQuery`, 250ms debounce useEffect.
    - The search input JSX wrapper (`<div className="relative mb-3">` + SearchIcon + Input + clear-X `×` button + `<NewBadge feature="search-jobs" className="absolute -top-1 -right-1" />`).
    - The `salesByJob` memo.
    - The `searchedJobs.length > 100 ? <List ...> : <div>...searchedJobs.map(...)</div>` branching (the threshold survives; the JSX inside both branches survives).
    - JobCard tag chips (`{job.tags && job.tags.length > 0 && (<div className="flex flex-wrap gap-1 mt-1">{job.tags.map(...)}</div>)}`) — those are owned by Plan 15-10 / Gap B (NOT being removed; they continue to render on each JobCard).
    - The pencil-button tag-edit affordance and inline tag editor — owned by Plan 15-10 (Gap B).
    - src/features.ts — Plan 15-10 will re-target the `tags` NewBadge; Gap C does not modify it.
    - src/db/backfill.ts (parseTagsInput remains exported).
    - src/utils/duplicateJob.ts and src/utils/duplicateJob.test.ts (LOCKED).
    - src/hooks/useDatabase.ts (D-12 reconcile wiring intact).

    Decision log for SUMMARY:
    - Chose `clearSearch` as the new callback name (semantically narrower than `clearFilters` now that there is only one filter dimension; matches the new CTA copy "Clear search").
    - Chose to RENAME the filter-empty-state heading from "No jobs match your filter" → "No jobs match your search" because "filter" is misleading now that the only filter is search. Mirrors the language users see in the search input placeholder ("Search jobs by title, tag, or customer").
    - Did NOT replace the "Clear search" link with an inline clear-X button on the search input — the existing search input ALREADY HAS a clear-X `×` button (line ~1693, conditionally rendered when `searchQuery`). The filter-empty-state CTA is a redundant-but-discoverable affordance; keeping it satisfies VERIFICATION.md Gap C's "decide between rename / inline clear / remove entirely" by choosing RENAME (lowest blast radius, preserves the keyboard / mouse / screen-reader discoverable target). Document this choice in the SUMMARY.
  </action>

  <verify>
    <automated>
      grep -nE 'selectedChips|selectedChipsKey|tagCounts|jobsAfterChipFilter' src/components/JobsManager.tsx | grep -v '^#' | wc -l   # MUST output 0

      grep -nE 'clearFilters' src/components/JobsManager.tsx | wc -l   # MUST output 0

      grep -c 'Clear filters' src/components/JobsManager.tsx   # MUST output 0

      grep -c 'Clear search' src/components/JobsManager.tsx   # MUST output 1

      grep -c 'clearSearch' src/components/JobsManager.tsx   # MUST output 2

      grep -nE 'key:\s*`\$\{selectedJobId \?\? ''\'?'\'?\}\|\$\{debouncedSearchQuery\}`' src/components/JobsManager.tsx | wc -l   # MUST output 1 (the narrowed bi-key)

      grep -nE 'key:\s*`\$\{selectedJobId.*\}\|\$\{selectedChipsKey\}\|\$\{debouncedSearchQuery\}`' src/components/JobsManager.tsx | wc -l   # MUST output 0 (the old tri-key is gone)

      grep -c 'overflow-x-auto' src/components/JobsManager.tsx   # MUST output 0

      grep -c 'No jobs match your search' src/components/JobsManager.tsx   # MUST output 1

      grep -c 'No jobs match your filter' src/components/JobsManager.tsx   # MUST output 0

      # LOCKED file integrity:
      git diff src/utils/duplicateJob.ts src/utils/duplicateJob.test.ts   # MUST output nothing

      npx tsc -b   # MUST exit 0

      npm test -- --run   # MUST report 263 passed / 1 todo / 0 failed (no new test cases for Gap C; existing tests untouched)
    </automated>
  </verify>

  <acceptance_criteria>
    - JobsManager.tsx contains ZERO occurrences of `selectedChips`, `selectedChipsKey`, `tagCounts`, `jobsAfterChipFilter`, or `clearFilters`.
    - JobsManager.tsx contains EXACTLY ONE occurrence of `Clear search` (filter-empty-state CTA button text).
    - JobsManager.tsx contains EXACTLY TWO occurrences of `clearSearch` (callback declaration + onClick handler).
    - JobsManager.tsx contains EXACTLY ONE `useDynamicRowHeight` call site, with key = `\`${selectedJobId ?? ''}|${debouncedSearchQuery}\`` (bi-key, pipe-delimited). Zero call sites use the old tri-key.
    - JobsManager.tsx contains ZERO occurrences of `overflow-x-auto` (the chip-row was the only consumer; AssetLibrary etc. use different scrolling primitives).
    - JobsManager.tsx contains ZERO occurrences of `No jobs match your filter`; exactly ONE occurrence of `No jobs match your search`.
    - The `searchedJobs` memo no longer references `jobsAfterChipFilter` anywhere — its body operates directly on `jobs`.
    - The search input JSX (`<div className="relative mb-3">` + SearchIcon + Input + clear-X + `<NewBadge feature="search-jobs">`) is byte-identical to its pre-plan state.
    - The 250ms debounce useEffect is byte-identical to its pre-plan state.
    - `git diff src/utils/duplicateJob.ts src/utils/duplicateJob.test.ts` outputs nothing.
    - `git diff src/features.ts` outputs nothing (this plan does not modify features.ts).
    - `npx tsc -b` exits 0.
    - `npm test -- --run` reports 263 passed / 1 todo / 0 failed.
  </acceptance_criteria>

  <done>
    The chip-filter row is gone from the sticky sub-header. The `selectedChips` state, `selectedChipsKey` memo, `tagCounts` memo, and `jobsAfterChipFilter` memo are removed. The `searchedJobs` memo now operates directly on `jobs`. The `useDynamicRowHeight` cache key is narrowed from tri-key to bi-key (TAGS-04 contract preserved). The `clearFilters` callback is renamed to `clearSearch` and simplified to set-search-query-only. The filter-empty-state CTA copy reads "No jobs match your search" with a "Clear search" button. TAGS-02 is now visibly Withdrawn at the runtime layer (consistent with REQUIREMENTS.md). TypeScript compiles clean. Test count holds at 263 passed / 1 todo / 0 failed. LOCKED helpers + features.ts untouched.
  </done>
</task>

</tasks>

<verification>
- Run all 12 grep / git-diff / tsc / vitest commands listed in `<verify><automated>` above.
- Manually scan JobsManager.tsx for stray references to "chip filter", "TAGS-02", "AND logic", "D-03", "D-04", "D-14" in comments — remove any leftover commentary that names removed surfaces (keep references in JSDoc on the `useDynamicRowHeight` block ONLY if they correctly describe the post-Gap-C shape).
- Confirm `git diff src/utils/duplicateJob.ts src/utils/duplicateJob.test.ts` is byte-empty.
- Confirm `git diff src/features.ts` is byte-empty.
- Visually inspect the sticky sub-header: only the search input wrapper remains, then immediately the (possibly empty) virtualized list or the filter-empty-state.
</verification>

<success_criteria>
- The chip-filter row is gone from JobsManager (Gap C closed).
- TAGS-02 is now visibly Withdrawn at the runtime layer.
- TAGS-04 cache-key contract preserved (bi-key, pipe-delimited).
- Search input remains the canonical and only filter mechanism.
- Filter-empty-state still fires for 0-result search; CTA renamed "Clear search".
- Test count: 263 passed / 1 todo / 0 failed.
- TypeScript compiles clean.
- LOCKED files untouched; src/features.ts untouched.
</success_criteria>

<output>
Create .planning/phases/15-tags-search-quick-duplicate/15-09-SUMMARY.md when done.

Summary must record:
- Files modified (JobsManager.tsx only) and the line-count delta
- Vitest result (expected 263 passed / 1 todo)
- tsc result (expected exit 0)
- The exact new value of the `useDynamicRowHeight` key (paste the one-line expression for posterity)
- Confirmation that `searchedJobs` now operates directly on `jobs` (no intermediate memo)
- Decision: kept the filter-empty-state CTA as a renamed link ("Clear search") rather than replacing it with the inline clear-X button (which already exists on the search input). Rationale: lowest blast radius; preserves discoverable affordance.
- Confirmation that LOCKED files (DUP-02 helper+tests, features.ts) are untouched
- Confirmation that the search input affordance (NewBadge `search-jobs`, clear-X, SearchIcon, Input) is byte-identical to its pre-plan state
- Confirmation that TAGS-02 is consistently Withdrawn at REQUIREMENTS.md + ROADMAP + runtime (cite the three locations)
</output>
