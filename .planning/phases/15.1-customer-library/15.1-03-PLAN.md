---
phase: 15.1-customer-library
plan: 03
type: execute
wave: 3
depends_on:
  - 15.1-01
  - 15.1-02
files_modified:
  - src/utils/customerCsv.ts
  - src/utils/customerCsv.test.ts
  - src/components/CustomerCsvImportModal.tsx
  - src/components/CustomerLibrary.tsx
autonomous: true
requirements:
  - CL-03

must_haves:
  truths:
    - "D-08: User can click `Import CSV` on the Customers tab and see a two-step Upload → Preview flow rendered by a new sibling `CustomerCsvImportModal.tsx` (parser is `customerCsv.ts` — NOT a genericized CsvImportModal<T>)"
    - "D-09: User can drop or pick a CSV with `name, email, company, address, notes` columns (case-insensitive headers, order irrelevant) — Name OR Email is required (rows blank on both are rejected with error `Name or Email required`)"
    - "Row validation rejects rows where an email is present but malformed (error: `Email format invalid`)"
    - "D-10: Email-matched duplicates show a `Duplicate` chip; user picks a single GLOBAL radio `Skip duplicates` (default) or `Update duplicates` applied to ALL email matches in the file in one shot (NOT per-row)"
    - "D-11: `Update duplicates` uses merge semantics — only non-empty incoming fields overwrite existing library fields (empty cells preserve existing values; re-importing a partial export does NOT wipe manually-typed notes/address)"
    - "Emails are normalized to lowercase on write (UI-SPEC discretion #8)"
    - "`npm run build` passes; the parser unit test passes"
  artifacts:
    - path: "src/utils/customerCsv.ts"
      provides: "Pure CSV parser + validator + duplicate detector + import builder for customer rows"
      exports: ["parseCustomerCsv", "buildCustomersForImport"]
    - path: "src/utils/customerCsv.test.ts"
      provides: "Vitest unit tests for the parser — required-field rule (D-09), duplicate detection (D-07), merge semantics (D-11), lowercase email (discretion #8)"
      contains: "describe('parseCustomerCsv'"
    - path: "src/components/CustomerCsvImportModal.tsx"
      provides: "Modal UI for the Upload → Preview flow (file picker + dropzone + duplicate-mode radio + row table + import button)"
      contains: "export function CustomerCsvImportModal"
    - path: "src/components/CustomerLibrary.tsx"
      provides: "Mount of `<CustomerCsvImportModal>` controlled by the existing `showCsvImport` state hook landed in Plan 02"
      contains: "CustomerCsvImportModal"
  key_links:
    - from: "src/components/CustomerLibrary.tsx"
      to: "src/components/CustomerCsvImportModal.tsx"
      via: "modal mount controlled by showCsvImport state"
      pattern: "<CustomerCsvImportModal"
    - from: "src/components/CustomerCsvImportModal.tsx"
      to: "src/utils/customerCsv.ts (parseCustomerCsv + buildCustomersForImport)"
      via: "imports + invocations on file drop and on submit"
      pattern: "parseCustomerCsv|buildCustomersForImport"
    - from: "src/components/CustomerCsvImportModal.tsx"
      to: "src/hooks/useDatabase.ts (bulkImportCustomers via onBulkImportCustomers prop)"
      via: "submit handler awaits onBulkImportCustomers(records)"
      pattern: "onBulkImportCustomers"
---

<objective>
Build the CSV bulk import surface for Customer records (CL-03): a sibling of `CsvImportModal.tsx` shaped specifically for customers, plus its parser/validator utility and unit tests. Wire the modal into `CustomerLibrary.tsx` via the `showCsvImport` state hook already landed in Plan 02.

Purpose: Power users with existing customer lists (Stripe / Etsy / Shopify exports, hand-maintained spreadsheets) can hydrate the Customers tab in one shot without retyping. Closes the second of the five ROADMAP Phase 15.1 success criteria.

Output: New parser utility `src/utils/customerCsv.ts` + tests, new modal `CustomerCsvImportModal.tsx`, and the JSX mount inside `CustomerLibrary.tsx`.

**Wave note:** This plan lands in Wave 3 (after Plan 02) because Task 4 amends `src/components/CustomerLibrary.tsx` — the file Plan 02 created. Plan 04 (combobox picker) runs in parallel in Wave 2 because it only touches `JobsManager.tsx` (no overlap with this plan's files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-customer-library/15.1-CONTEXT.md
@.planning/phases/15.1-customer-library/15.1-UI-SPEC.md
@.planning/phases/15.1-customer-library/15.1-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@./CLAUDE.md
@.planning/phases/15.1-customer-library/15.1-01-SUMMARY.md
@.planning/phases/15.1-customer-library/15.1-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (Wave 1): -->

`Customer` interface (src/types.ts), `useCustomers()` hook including `bulkImportCustomers(toImport: Customer[]): Promise<void>`.

<!-- From Plan 02 (Wave 2): -->

`CustomerLibrary` has local state `const [showCsvImport, setShowCsvImport] = useState(false);` already declared. The `Import CSV` button's onClick is `() => setShowCsvImport(true)`. The `onBulkImportCustomers` prop is already plumbed through from `App.tsx`. Task 4 of THIS plan adds the `<CustomerCsvImportModal>` mount and the `onBulkImportCustomers` invocation inside the modal's submit handler.

<!-- THIS plan creates: -->

`parseCustomerCsv(csvString: string, existingCustomers: Customer[]): CustomerCsvParseResult`

```typescript
interface ParsedCustomerRow {
  rowNumber: number;            // 1-indexed; header is row 0
  data: Record<string, string>; // raw row data
  customer: Partial<Customer> | null; // null when row is invalid (will not be imported)
  errors: string[];             // user-facing strings; empty array means valid
  warnings: string[];
  isDuplicate: boolean;         // email matches an existing library record
  existingCustomerId?: string;  // set iff isDuplicate
}

interface CustomerCsvParseResult {
  rows: ParsedCustomerRow[];
  globalErrors: string[];       // empty CSV / Papa parse failures
}
```

`buildCustomersForImport(rows: ParsedCustomerRow[], existingById: Map<string, Customer>, selected: Set<number>, duplicateMode: 'skip' | 'overwrite'): Customer[]`

Returns the Customer records to bulkPut. Handles skip vs merge per D-11.

`<CustomerCsvImportModal />` props:
```typescript
interface CustomerCsvImportModalProps {
  isOpen: boolean;
  existingCustomers: Customer[];
  onImportCustomers: (toImport: Customer[]) => Promise<void>;
  onClose: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build `customerCsv.ts` parser + builder utility</name>
  <files>src/utils/customerCsv.ts</files>
  <read_first>
    - src/utils/csvHelpers.ts (read the FULL file once — structural template for parse / validate / dedup / build pipeline; CONVENTIONS.md confirms PapaParse is the parser, header-normalization via `transformHeader`, and the `ParsedRow` / `CsvParseResult` result shape)
    - src/types.ts (after Plan 01 — needed for the new `Customer` interface and inherited `JobCustomer` fields)
    - .planning/phases/15.1-customer-library/15.1-PATTERNS.md §4 (the verbatim parser body — imports, PapaParse config, ParsedCustomerRow shape, validateCustomerRow, checkCustomerDuplicates, buildCustomersForImport)
    - .planning/phases/15.1-customer-library/15.1-CONTEXT.md §decisions D-08, D-09, D-10, D-11 (parser as sibling not generic, Name OR Email rule, global duplicate mode, merge semantics) and §"Claude's Discretion" bullet #8 (lowercase email on write) and #14 (single `address` column only)
  </read_first>
  <behavior>
    - `parseCustomerCsv` accepts CSV text and returns `{ rows, globalErrors }`. Trim handling, header case-insensitivity, and the Name-OR-Email rule are all locked.
    - `validateCustomerRow` (internal):
      - Returns `errors: ['Name or Email required']` and `customer: null` if both `name` and `email` are blank/whitespace
      - Returns `errors: ['Email format invalid']` and `customer: null` if `email` is non-empty but does not include `@`
      - Otherwise returns `{ customer: Partial<Customer>, errors: [], warnings: [] }` where the customer object contains:
        - `name`: present iff non-empty after trim
        - `email`: present iff non-empty after trim — normalized to LOWERCASE (UI-SPEC discretion #8)
        - `company`: present iff non-empty after trim
        - `address`: present iff non-empty after trim (single column — discretion #14)
        - `notes`: present iff non-empty after trim
    - `checkCustomerDuplicates` (internal): builds an email → existingCustomerId map from `existingCustomers`, then for each `row.customer` with a non-empty email, sets `row.isDuplicate = true` and `row.existingCustomerId = existingMap.get(emailKey)` when the lowercased email matches.
    - `buildCustomersForImport`:
      - Skips rows where `row.customer === null`
      - Skips rows whose `rowNumber` is NOT in `selected`
      - For non-duplicate rows: builds a NEW `Customer` with `id = crypto.randomUUID()`, `createdAt = new Date()`, and spreads `row.customer` fields (does NOT set `lastUsedAt` — created via import, not used in business yet; D-03 semantics)
      - For duplicate rows with `duplicateMode === 'skip'`: skip
      - For duplicate rows with `duplicateMode === 'overwrite'`: merge — fetch existing by `row.existingCustomerId`; create a new object that preserves the existing `id`, `createdAt`, `lastUsedAt`; spreads the existing fields; then conditionally overwrites ONLY the non-empty incoming fields (D-11 merge semantics). Empty cells in the CSV preserve existing library values.

    Tests required (Task 2 covers all):
    - Parser: empty CSV → globalErrors `['CSV file is empty']`
    - Parser: minimal valid row (Name only) → 1 row, customer non-null, no errors
    - Parser: minimal valid row (Email only) → 1 row, customer.email lowercased, no errors
    - Parser: row with both Name AND Email blank → error `Name or Email required`, customer null
    - Parser: row with email missing `@` → error `Email format invalid`, customer null
    - Parser: row with `NAME,EMAIL,COMPANY` headers (uppercase) → headers normalized, fields parsed correctly
    - Parser: row with extra unknown column (`phone`) → ignored silently
    - Duplicate detection: existing customer with email `Alice@Example.com`, CSV row with email `alice@example.com` → `isDuplicate: true`, `existingCustomerId` set
    - Builder: non-duplicate row + Skip mode → new Customer with fresh UUID + createdAt; lastUsedAt undefined
    - Builder: duplicate row + Skip mode → no record in output
    - Builder: duplicate row + Update mode + CSV row only has updated `name` (other fields blank) → output Customer has new `name`, preserves existing `email`, `company`, `address`, `notes`, `id`, `createdAt`, `lastUsedAt` (D-11 merge)
  </behavior>
  <action>
    Create `src/utils/customerCsv.ts`. Imports:
    - `import Papa from 'papaparse';`
    - `import type { Customer } from '../types';`

    Export the two interfaces `ParsedCustomerRow` and `CustomerCsvParseResult` (shapes locked in this plan's `<interfaces>` block).

    Export `parseCustomerCsv(csvString: string, existingCustomers: Customer[]): CustomerCsvParseResult`. Body:
    - If `!csvString.trim()` return `{ rows: [], globalErrors: ['CSV file is empty'] }`
    - Call `Papa.parse<Record<string, string>>(csvString, { header: true, skipEmptyLines: true, transformHeader: h => h.trim().toLowerCase() })`
    - If `parsed.errors.length > 0`: copy each error's `message` into `globalErrors` (but still proceed if any rows were parsed — PapaParse is forgiving)
    - Map each row in `parsed.data` to a `ParsedCustomerRow` by calling `validateCustomerRow` (internal) with rowNumber starting at 2 (1-indexed where row 1 is the header)
    - Call `checkCustomerDuplicates(rows, existingCustomers)` (mutates rows in place to set isDuplicate/existingCustomerId)
    - Return `{ rows, globalErrors }`

    Define internal `validateCustomerRow(row: Record<string, string>)`:
    - Read `row.name`, `row.email`, `row.company`, `row.address`, `row.notes` — all may be undefined
    - Trim each
    - If both name and email are blank → return `{ customer: null, errors: ['Name or Email required'], warnings: [] }`
    - If email is non-empty and does NOT include `@` → return `{ customer: null, errors: ['Email format invalid'], warnings: [] }`
    - Build `customer: Partial<Customer> = {}` adding fields only when non-empty after trim; lowercase the email
    - Return `{ customer, errors: [], warnings: [] }`

    Define internal `checkCustomerDuplicates(rows, existing)`:
    - Build `existingMap: Map<string, string>` keyed by `(c.email || '').trim().toLowerCase()` → `c.id`; skip records with empty email
    - For each row with a non-empty customer email, look up `existingMap.get(emailKey)`; if found, set `row.isDuplicate = true` and `row.existingCustomerId = existingId`

    Export `buildCustomersForImport(rows: ParsedCustomerRow[], existingById: Map<string, Customer>, selected: Set<number>, duplicateMode: 'skip' | 'overwrite'): Customer[]`:
    - Iterate rows; skip null-customer rows; skip rows not in `selected`
    - For non-duplicates: construct `{ id: <uuid>, createdAt: new Date(), ...(row.customer ?? {}) } as Customer`
    - For duplicates with `duplicateMode === 'skip'`: skip
    - For duplicates with `duplicateMode === 'overwrite'`: fetch `existing = existingById.get(row.existingCustomerId!)`; if missing, skip. Otherwise build `merged = { ...existing }`, then for each of `name`, `email`, `company`, `address`, `notes` from `row.customer`: assign onto `merged` ONLY if the value is truthy (non-empty after the trim in validateCustomerRow). This implements D-11's "only non-empty incoming fields overwrite" rule.
    - Use `crypto.randomUUID()` with the fallback in PATTERNS.md §4

    Naming: camelCase exported functions, PascalCase interfaces (CONVENTIONS.md).

    Place the file at `src/utils/customerCsv.ts` (flat layout matching `csvHelpers.ts`).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    <automated>grep -c "^export function parseCustomerCsv" src/utils/customerCsv.ts</automated>
    <automated>grep -c "^export function buildCustomersForImport" src/utils/customerCsv.ts</automated>
    <automated>grep -c "Name or Email required" src/utils/customerCsv.ts</automated>
    <automated>grep -c "Email format invalid" src/utils/customerCsv.ts</automated>
    <automated>grep -c "toLowerCase" src/utils/customerCsv.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: exports `parseCustomerCsv` and `buildCustomersForImport`
    - Source: contains the literal strings `'Name or Email required'` and `'Email format invalid'` (validation error copy locked by UI-SPEC §Copy)
    - Source: contains `toLowerCase()` calls applied to email values (discretion #8)
    - Source: `Papa.parse` called with `transformHeader: (h: string) => h.trim().toLowerCase()` (case-insensitive headers per D-09)
    - Source: builder's overwrite path uses field-by-field conditional spread (NOT `{ ...existing, ...row.customer }` — that would let empty fields nuke existing data; verify by reading the code)
    - Build: `npm run build` exits 0
  </acceptance_criteria>
  <done>
    The parser and import builder are exported, type-safe, and ready for both the modal (Task 3) and the unit tests (Task 2) to consume.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-test the customerCsv parser</name>
  <files>src/utils/customerCsv.test.ts</files>
  <read_first>
    - src/utils/customerCsv.ts (from Task 1)
    - src/types.ts (`Customer` interface)
    - Any existing project test file as the Vitest format template (e.g. `src/utils/threeMfParser.test.ts` if it exists, or any `*.test.ts` under `src/`)
    - .planning/phases/15.1-customer-library/15.1-CONTEXT.md §"Claude's Discretion" bullet "Test coverage strategy" (locks the parser+merge tests in this plan)
  </read_first>
  <behavior>
    Test cases (each maps 1:1 to an `it(...)` block):
    - Test 1: empty string → globalErrors equals `['CSV file is empty']`, rows is empty
    - Test 2: a header-only CSV → 0 rows, no global errors
    - Test 3: row with only `name` → 1 row, customer.name preserved, customer.email undefined
    - Test 4: row with only `email` in uppercase (`ALICE@EXAMPLE.COM`) → customer.email is `alice@example.com` (lowercased on write)
    - Test 5: row with both name and email blank (whitespace-only) → errors `['Name or Email required']`, customer null
    - Test 6: row with email `not-an-email` (no @) → errors `['Email format invalid']`, customer null
    - Test 7: uppercase headers (`NAME,EMAIL,COMPANY,ADDRESS,NOTES`) → fields parsed correctly (case-insensitive)
    - Test 8: extra unknown column (`phone`) → ignored silently; the known columns parse fine
    - Test 9: duplicate detection — existing customer `[{ id: 'c-1', createdAt: new Date(), email: 'Alice@Example.com' }]` + CSV row email `alice@example.com` → `isDuplicate: true`, `existingCustomerId: 'c-1'`
    - Test 10 (builder, fresh insert): non-duplicate row → output Customer has fresh UUID, `createdAt` is a Date, `lastUsedAt` is undefined
    - Test 11 (builder, skip duplicate): duplicate row + `duplicateMode: 'skip'` → output array does NOT include it
    - Test 12 (builder, merge): existing customer `{ id: 'c-1', createdAt: <oldDate>, lastUsedAt: <someDate>, name: 'OldName', email: 'a@x.com', company: 'OldCo', address: '123 Old St', notes: 'old notes' }`; CSV row provides ONLY `name: 'NewName'` (other fields empty); `duplicateMode: 'overwrite'` → output Customer has `name: 'NewName'` AND `email: 'a@x.com'` AND `company: 'OldCo'` AND `address: '123 Old St'` AND `notes: 'old notes'` AND `id: 'c-1'` AND original `createdAt` AND original `lastUsedAt`. This is the D-11 merge rule — empty cells preserve existing library values.
  </behavior>
  <action>
    Create `src/utils/customerCsv.test.ts`. Use Vitest:
    - `import { describe, it, expect } from 'vitest';`
    - `import { parseCustomerCsv, buildCustomersForImport, type ParsedCustomerRow } from './customerCsv';`
    - `import type { Customer } from '../types';`

    Structure two `describe` blocks:
    - `describe('parseCustomerCsv (Phase 15.1 — CL-03 / D-09 / D-07)', () => { ... })` — tests 1-9
    - `describe('buildCustomersForImport (Phase 15.1 — D-11 merge semantics)', () => { ... })` — tests 10-12

    Helper: a small CSV string builder e.g.
    ```typescript
    const csv = (headers: string[], ...rows: string[][]) =>
      [headers.join(','), ...rows.map(r => r.join(','))].join('\n');
    ```

    For Test 9, construct an `existingCustomers` array with one entry. Make sure to use the MIXED-CASE email in the existing record AND the LOWERCASE email in the CSV row — the test proves case-insensitive dedup.

    For Test 12, supply the merge inputs as: a single existing customer in `existingById` (built via `new Map([[c.id, c]])`); a `ParsedCustomerRow` with `customer = { name: 'NewName' }`, `isDuplicate: true`, `existingCustomerId: 'c-1'`, `rowNumber: 2`, `errors: []`, `warnings: []`, `data: {}`; and `selected = new Set([2])`, `duplicateMode = 'overwrite'`. Assert ALL six preserved fields PLUS the newly-set `name`.

    Pure tests, no Dexie, no DOM. Should complete in <100ms total.
  </action>
  <verify>
    <automated>npx vitest run src/utils/customerCsv.test.ts 2>&1 | tail -40</automated>
    <automated>grep -c "describe('parseCustomerCsv" src/utils/customerCsv.test.ts</automated>
    <automated>grep -c "describe('buildCustomersForImport" src/utils/customerCsv.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: contains both `describe(...)` blocks (parser + builder)
    - Test: `npx vitest run src/utils/customerCsv.test.ts` exits 0
    - Test: output shows 12 passing tests in this file (no skipped, no todo)
    - Behavior: the merge test (Test 12) explicitly asserts that empty incoming fields PRESERVE existing library values — this is the architectural invariant D-11 locks
  </acceptance_criteria>
  <done>
    Twelve passing Vitest tests document the parser + builder behavior, including the locked D-11 merge semantics and the case-insensitive email dedup.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build `CustomerCsvImportModal.tsx`</name>
  <files>src/components/CustomerCsvImportModal.tsx</files>
  <read_first>
    - src/components/CsvImportModal.tsx (read the FULL file once — entire file is the structural template; pay particular attention to the step state at lines 22-30, processFile at 55-87, escape/reset effects at 33-53, header pattern at 158-186, dropzone at 297-325, duplicate-mode radio at 418-444, row item at 487-565, footer at 220-238)
    - src/components/ui/index.ts (barrel exports)
    - src/utils/customerCsv.ts (from Task 1)
    - .planning/phases/15.1-customer-library/15.1-UI-SPEC.md §Component Inventory §4 (the full importer spec — chrome, step model, upload step, preview step, row table, validation copy, footer)
    - .planning/phases/15.1-customer-library/15.1-UI-SPEC.md §Copywriting Contract (verbatim copy for every label, button, error)
    - .planning/phases/15.1-customer-library/15.1-PATTERNS.md §3 (all 7 sub-patterns: step state, processFile, two-step header, dropzone, duplicate-mode radio, row item, footer)
  </read_first>
  <action>
    Create `src/components/CustomerCsvImportModal.tsx` exporting `CustomerCsvImportModal({ isOpen, existingCustomers, onImportCustomers, onClose })`.

    Imports (barrel only):
    - `import { useCallback, useEffect, useMemo, useRef, useState } from 'react';`
    - `import { Button } from './ui';`
    - `import { parseCustomerCsv, buildCustomersForImport, type ParsedCustomerRow, type CustomerCsvParseResult } from '../utils/customerCsv';`
    - `import type { Customer } from '../types';`

    State:
    - `type Step = 'upload' | 'preview';`
    - `const [step, setStep] = useState<Step>('upload');`
    - `const [isDragOver, setIsDragOver] = useState(false);`
    - `const [error, setError] = useState<string | null>(null);`
    - `const [parseResult, setParseResult] = useState<CustomerCsvParseResult | null>(null);`
    - `const [selected, setSelected] = useState<Set<number>>(new Set());`
    - `const [duplicateMode, setDuplicateMode] = useState<'skip' | 'overwrite'>('skip');`
    - `const [isImporting, setIsImporting] = useState(false);`
    - `const fileInputRef = useRef<HTMLInputElement>(null);`

    Effects (PATTERNS.md §3):
    - Escape-to-close: window keydown listener with cleanup
    - Reset-on-close: when `isOpen` flips false, reset all the local state to initial values

    Early return: `if (!isOpen) return null;`

    Chrome — copy verbatim from CsvImportModal.tsx:152-186 (overlay, panel `max-w-3xl max-h-[85vh] flex flex-col shadow-xl`, two-step header with back-arrow on preview step and X-close on both steps). Header title strings (UI-SPEC §Copy):
    - upload step: `Import customers from CSV`
    - preview step: `Preview customer import`

    Upload-step body (UI-SPEC §4):
    - Error banner (red, conditional on `error`): copy AssetLibrary's error banner classes
    - Dropzone (PATTERNS.md §3 "Drop zone"): same `border-2 border-dashed` toggle on `isDragOver`; inline cloud-upload SVG; helper text `Drop a .csv file here, or click to browse`; hidden `<input type="file" accept=".csv" />` triggered by ref
    - Below the dropzone: a small "Accepted columns" expandable block. Initial implementation: render the locked column reference text as a `<p className="text-xs text-slate-400 mt-4">` (the UI-SPEC also lists toggle buttons; for v1 render the column reference inline — simpler and matches UI-SPEC §4 fallback). Text (UI-SPEC §Copy verbatim): `Accepted columns: name, email, company, address, notes. Column order doesn't matter. Headers are case-insensitive. Either name or email is required per row.`

    `processFile(file)` (PATTERNS.md §3 "File-processing"):
    - If filename does not end in `.csv` (case-insensitive): `setError('Please select a .csv file.')`; return
    - `setError(null)`; try-catch the parse:
      - `const text = await file.text();`
      - `const result = parseCustomerCsv(text, existingCustomers);`
      - If `result.globalErrors.length > 0 && result.rows.length === 0` → `setError(result.globalErrors.join('\n'))`; return
      - `setParseResult(result);`
      - Build initial selection: for each row whose `customer !== null && errors.length === 0`, add its `rowNumber` to a Set; set as `selected`
      - `setStep('preview')`
    - On catch: `setError('Failed to read CSV file. Check the file isn\'t corrupted and try again.')`

    Wire `onFileSelect`, `onDragOver`, `onDragLeave`, `onDrop` handlers (copy verbatim from CsvImportModal — same shape).

    Preview-step body (UI-SPEC §4):

    1. Stats bar (above the table):
       - Compute counts via `useMemo` over `parseResult.rows`: `total = rows.length`, `okCount = rows.filter(r => r.customer && !r.isDuplicate && r.errors.length === 0).length`, `dupCount = rows.filter(r => r.isDuplicate).length`, `errCount = rows.filter(r => r.errors.length > 0).length`
       - Render `text-sm text-slate-400`: `Found {total} rows · {okCount} ready to import · {dupCount} duplicates · {errCount} with errors`

    2. Duplicate-mode radio (always rendered — D-10 says "applied to ALL email matches in the file in one shot"; not gated on dupCount):
       - Copy the radio markup from PATTERNS.md §3 "Duplicate-mode radio"
       - Labels: `Skip duplicates` (default), `Update duplicates`
       - Below the radio render the active explainer (UI-SPEC §4 / §Copy):
         - skip → `<p className="text-xs text-slate-400 mt-1">Existing customers matched by email will not change.</p>`
         - overwrite → `<p className="text-xs text-slate-400 mt-1">Existing customers matched by email will be updated (only non-empty incoming fields overwrite — empty cells keep existing values).</p>`

    3. Row list — render each row from `parseResult.rows` as a card-style row inside a scrollable container. Each row shows:
       - A checkbox (controls `selected` set membership; disabled when `row.customer === null` — invalid rows can't be imported)
       - Name (bold, line 1): `text-sm font-medium text-white`; falls back to `(no name)` if blank
       - Subline (line 2): `text-xs text-slate-400`: `[email, company].filter(Boolean).join(' · ')`
       - Status chip (right edge, locked colors per UI-SPEC §4):
         - `OK` → `bg-green-500/20 text-green-400 border-green-500/30`
         - `Duplicate` → `bg-yellow-500/20 text-yellow-400 border-yellow-500/30`
         - `Error: {message}` → `bg-red-500/20 text-red-400 border-red-500/30` (display only the first error from `row.errors`)

    4. Footer (UI-SPEC §4 + PATTERNS.md §3 "Footer"):
       - Compute `importCount` via `useMemo` over selection + duplicate mode:
         ```typescript
         const importCount = useMemo(() => {
           if (!parseResult) return 0;
           let count = 0;
           for (const row of parseResult.rows) {
             if (!row.customer) continue;
             if (!selected.has(row.rowNumber)) continue;
             if (row.isDuplicate && duplicateMode === 'skip') continue;
             count++;
           }
           return count;
         }, [parseResult, selected, duplicateMode]);
         ```
       - Cancel button (left): `<Button variant="ghost" onClick={onClose}>Cancel</Button>`
       - Import button (right): `<Button variant="primary" onClick={handleImport} disabled={importCount === 0 || isImporting}>{isImporting ? 'Importing...' : `Import ${importCount} customer${importCount !== 1 ? 's' : ''}`}</Button>`

    `handleImport`:
    - Guard: if `!parseResult || importCount === 0` return
    - Build `existingById: Map<string, Customer>` from `existingCustomers` (id → customer)
    - `const toImport = buildCustomersForImport(parseResult.rows, existingById, selected, duplicateMode);`
    - `setIsImporting(true);`
    - try `await onImportCustomers(toImport);` then `onClose();`
    - catch `e` → `setError(\`Import failed: \${e instanceof Error ? e.message : String(e)}\`);` (UI-SPEC §Copy "generic import failure")
    - finally `setIsImporting(false);`

    Naming convention: PascalCase component, camelCase callbacks (CONVENTIONS.md).

    All text strings are pulled VERBATIM from UI-SPEC §Copywriting Contract.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    <automated>grep -c "^export function CustomerCsvImportModal" src/components/CustomerCsvImportModal.tsx</automated>
    <automated>grep -c "Import customers from CSV" src/components/CustomerCsvImportModal.tsx</automated>
    <automated>grep -c "Preview customer import" src/components/CustomerCsvImportModal.tsx</automated>
    <automated>grep -c "Skip duplicates" src/components/CustomerCsvImportModal.tsx</automated>
    <automated>grep -c "Update duplicates" src/components/CustomerCsvImportModal.tsx</automated>
    <automated>grep -c "Accepted columns: name, email, company, address, notes" src/components/CustomerCsvImportModal.tsx</automated>
    <automated>grep -c "Existing customers matched by email will be updated" src/components/CustomerCsvImportModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: exports `CustomerCsvImportModal`
    - Source: contains both literal header strings (`Import customers from CSV`, `Preview customer import`)
    - Source: contains both literal radio labels (`Skip duplicates`, `Update duplicates`) and both literal explainer strings
    - Source: contains the literal column-reference string (`Accepted columns: name, email, company, address, notes. Column order doesn't matter...`)
    - Source: import button label uses the template `Import ${importCount} customer${importCount !== 1 ? 's' : ''}` (singular when 1)
    - Source: imports from `./ui` barrel only — assert `grep -cE "from './ui/[A-Z]" src/components/CustomerCsvImportModal.tsx` returns 0
    - Source: file uses `parseCustomerCsv` AND `buildCustomersForImport` from `../utils/customerCsv`
    - Build: `npm run build` exits 0
  </acceptance_criteria>
  <done>
    `CustomerCsvImportModal` renders the two-step Upload → Preview flow with all locked copy, validation chips, duplicate-mode radio, and import button. It calls the Task 1 utilities for parsing and the `onImportCustomers` prop to write the records (which CustomerLibrary will route to `bulkImportCustomers`).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Mount `CustomerCsvImportModal` inside `CustomerLibrary.tsx`</name>
  <files>src/components/CustomerLibrary.tsx</files>
  <read_first>
    - src/components/CustomerLibrary.tsx (from Plan 02 — read once; the file should already declare `const [showCsvImport, setShowCsvImport] = useState(false);` plus an `Import CSV` button that toggles it, plus the `onBulkImportCustomers` prop in the props interface)
    - src/components/CustomerCsvImportModal.tsx (from Task 3 — confirms the prop shape)
  </read_first>
  <action>
    Two surgical edits to `src/components/CustomerLibrary.tsx`:

    (a) Add the import at the top of the file:
    ```typescript
    import { CustomerCsvImportModal } from './CustomerCsvImportModal';
    ```

    (b) Add the modal mount near the existing `<CustomerEditModal>` mount (likely at the bottom of the component's JSX, just before the closing wrapper `</div>`):
    ```typescript
    <CustomerCsvImportModal
      isOpen={showCsvImport}
      existingCustomers={customers}
      onImportCustomers={onBulkImportCustomers}
      onClose={() => setShowCsvImport(false)}
    />
    ```

    Do NOT touch any other code in the file. Do NOT modify the `Import CSV` button — it already calls `setShowCsvImport(true)` from Plan 02. Do NOT remove the `// TODO Plan 03 — wire CustomerCsvImportModal` comment if it's still there; replace it with a one-line comment noting the wiring completed:
    ```typescript
    // CSV import modal (Plan 03)
    ```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    <automated>grep -c "import { CustomerCsvImportModal }" src/components/CustomerLibrary.tsx</automated>
    <automated>grep -c "<CustomerCsvImportModal" src/components/CustomerLibrary.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/components/CustomerLibrary.tsx` imports `CustomerCsvImportModal`
    - Source: contains exactly one `<CustomerCsvImportModal` JSX usage
    - Source: the JSX props include `isOpen={showCsvImport}`, `existingCustomers={customers}`, `onImportCustomers={onBulkImportCustomers}`, `onClose={() => setShowCsvImport(false)}`
    - Behavior: clicking `Import CSV` in the Customers tab opens the modal (functionally verified during Plan 05 UAT)
    - Build: `npm run build` exits 0
  </acceptance_criteria>
  <done>
    `CustomerLibrary.tsx` now mounts the CSV import modal; clicking `Import CSV` opens the Upload → Preview flow; on import the rows route via `onBulkImportCustomers` → `bulkImportCustomers` in the hook → `db.customers.bulkPut`.
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes after all 4 tasks
- `npx vitest run src/utils/customerCsv.test.ts` exits 0 with 12 passing tests
- Manual sanity: `npm run dev` → Customers tab → `Import CSV` → drop a small CSV with 3 rows (one OK, one duplicate of an existing customer, one with both Name and Email blank) → confirm:
  - Upload step accepts the file
  - Preview step shows stats `Found 3 rows · 1 ready to import · 1 duplicates · 1 with errors`
  - The duplicate row shows the `Duplicate` chip; the invalid row shows the `Error: Name or Email required` chip with a disabled checkbox
  - Toggling `Skip duplicates` → `Update duplicates` updates the import button count
  - Clicking `Import 1 customer` (singular when count=1) closes the modal and a new row appears in the list
</verification>

<success_criteria>
- CSV with up to 5 columns (name, email, company, address, notes) can be imported in one shot
- Name-OR-Email rule enforced (D-09); malformed emails rejected
- Global `Skip duplicates` / `Update duplicates` radio applies to all email matches (D-10)
- `Update duplicates` uses merge semantics — empty cells in CSV preserve existing library values (D-11)
- Emails are normalized to lowercase on write (discretion #8)
- Parser is pure and tested (12 passing tests)
- No new external deps (PapaParse is already a project dependency)
</success_criteria>

<output>
Create `.planning/phases/15.1-customer-library/15.1-03-SUMMARY.md` when done. Include:
- Final path of the parser utility and its public API
- Final path of the modal and its prop interface
- Number of passing tests (should be 12)
- Confirmation that emails are lowercased on write
- Note for Plan 05: `customersByEmail` from `useCustomers` is the same Map shape used by the picker; the dedup key is `(c.email || '').trim().toLowerCase()` everywhere
- Confirmation that `npm run build` passes
</output>
