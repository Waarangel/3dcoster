---
phase: 15.1-customer-library
plan: 05
type: execute
wave: 4
depends_on:
  - 15.1-02
  - 15.1-03
  - 15.1-04
files_modified:
  - src/features.ts
  - src/App.tsx
autonomous: false
requirements:
  - CL-01
  - CL-02
  - CL-03
  - CL-04
  - CL-05

must_haves:
  truths:
    - "D-16: `src/features.ts` contains a single `customer-library` entry dated to the actual ship date (planner writes the date on ship, not the discussion date)"
    - "D-16: The Customers tab button in main nav renders a single `<NewBadge feature=\"customer-library\">` as an absolute overlay (`absolute -top-1 -right-1 pointer-events-none`, never inline); NO `<NewBadge>` is placed on the picker combobox, the Add customer button, or the Import CSV button (the tab badge is the single entry point)"
    - "The badge does NOT push, wrap, shrink, or reflow any sibling tab button — the flex tab row layout is byte-identical to its pre-badge state"
    - "`npm run build` passes"
    - "Manual UAT verifies all five ROADMAP success criteria end-to-end"
  artifacts:
    - path: "src/features.ts"
      provides: "`customer-library` entry in the `featureReleases` map"
      contains: "'customer-library'"
    - path: "src/App.tsx"
      provides: "Absolute-overlay NewBadge inside the Customers tab button"
      contains: 'feature="customer-library"'
  key_links:
    - from: "src/App.tsx (Customers tab button)"
      to: "src/features.ts (customer-library entry)"
      via: "<NewBadge feature='customer-library' />"
      pattern: "feature=\"customer-library\""
---

<objective>
Wire the NEW badge for Phase 15.1 onto the Customers tab button, add the `customer-library` entry to `src/features.ts`, and run a final UAT pass that validates all five ROADMAP Phase 15.1 success criteria end-to-end.

Purpose: This is the user-facing "this is new" signal. Without the badge users will not discover the new tab. The UAT step is the human gate that closes the phase — it validates that Plans 01–04 actually work together end-to-end before the phase is committed.

Output: One added entry in `src/features.ts`, one added JSX node inside `App.tsx`'s Customers tab button, and a `15.1-VERIFICATION.md` record of the UAT outcome.

**Project memory rule (mandatory — see ~/.claude/projects/.../MEMORY.md):**
The NewBadge MUST be an absolute overlay — never inline. The main nav is a flex row of tab buttons; an inline badge would push siblings and break the layout. The Customers tab button is already `position: relative` (existing tab pattern); the badge goes inside the button as an absolutely-positioned child with `pointer-events-none` so the button stays clickable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-customer-library/15.1-CONTEXT.md
@.planning/phases/15.1-customer-library/15.1-UI-SPEC.md
@.planning/phases/15.1-customer-library/15.1-PATTERNS.md
@./CLAUDE.md
@.planning/phases/15.1-customer-library/15.1-01-SUMMARY.md
@.planning/phases/15.1-customer-library/15.1-02-SUMMARY.md
@.planning/phases/15.1-customer-library/15.1-03-SUMMARY.md
@.planning/phases/15.1-customer-library/15.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `customer-library` entry to `src/features.ts`</name>
  <files>src/features.ts</files>
  <read_first>
    - src/features.ts (read the FULL file — confirm the existing `featureReleases` map shape; pay attention to the 6 existing entries from Phase 13 and Phase 14 so the new one matches format)
    - .planning/phases/15.1-customer-library/15.1-PATTERNS.md §13 (verbatim entry shape)
    - .planning/phases/15.1-customer-library/15.1-CONTEXT.md §decisions D-16 (single entry, placed as absolute overlay on Customers tab; D-16 also locks: planner writes ACTUAL ship date)
    - ~/.claude/projects/-Users-marcusdickinson-Projects-3DCoster/memory/MEMORY.md (the global NewBadge placement rule the executor MUST follow — never inline, always absolute corner overlay)
  </read_first>
  <action>
    Add one entry to the `featureReleases` map in `src/features.ts`:

    ```typescript
    'customer-library': new Date('2026-05-22'),
    ```

    Placement: insert the line BEFORE the trailing comment `// Add new features here with their release date` (or, if no comment exists, append as the last entry of the map).

    Date selection: today's date is `2026-05-22`. If the actual ship/merge date differs (e.g. the executor lands this after the calendar rolls), update to the actual ship date — the badge has a 14-day visibility gate (`NEW_FEATURE_MAX_AGE_DAYS`), so the date determines how long the badge stays visible to users.

    Do NOT alter any of the existing 6 entries. Do NOT remove any existing entries (Phase 13's UI-10 audit + Phase 14's audit-holds verification already pruned stale entries; this plan adds, it does not prune).

    Format: single-quoted feature key (matches existing entries), `new Date('YYYY-MM-DD')` value, trailing comma.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    <automated>grep -c "'customer-library':" src/features.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/features.ts` contains exactly one line matching `'customer-library':`
    - Source: the value is a `new Date('YYYY-MM-DD')` literal (assert grep `new Date('20\d\d-\d\d-\d\d')` on the matching line)
    - Source: the existing 6 entries are unchanged (assert via grep counts — `settings-reorg`, `default-profit-margin`, `model-url`, `default-tax-rate`, `customer-details`, `etsy-helper` all still present)
    - Build: `npm run build` exits 0
  </acceptance_criteria>
  <done>
    `customer-library` is registered in `src/features.ts`. The `<NewBadge>` consumer in Task 2 will read this entry to compute visibility.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add absolute-overlay NewBadge to the Customers tab button in `App.tsx`</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (read the FULL file — pay particular attention to the `tabs.map(tab => (...)` render block around line 242-258. Confirm that each tab `<button>` already has the `relative` className, which means the badge can be absolutely positioned inside it without any extra wrapper)
    - src/components/NewBadge.tsx (read once — confirms the `feature` prop and the `className` pass-through; the badge component handles its own visibility logic via `featureReleases` and localStorage gates)
    - .planning/phases/15.1-customer-library/15.1-UI-SPEC.md §Component Inventory §1 (locked: badge inside the button, absolute `-top-1 -right-1`, `pointer-events-none`)
    - .planning/phases/15.1-customer-library/15.1-PATTERNS.md §10 (the verbatim JSX excerpt showing the badge inside the `tabs.map` render block)
    - ~/.claude/projects/-Users-marcusdickinson-Projects-3DCoster/memory/MEMORY.md (the global rule — re-read for safety)
  </read_first>
  <action>
    Surgical edit to `src/App.tsx` — inside the existing `tabs.map(tab => (...))` block (around line 242-258), add a conditional `<NewBadge>` child INSIDE the tab `<button>`:

    ```jsx
    {tab.id === 'customers' && (
      <NewBadge feature="customer-library" className="absolute -top-1 -right-1 pointer-events-none" />
    )}
    ```

    Placement guidance (PATTERNS.md §10):
    - Place this conditional AFTER the existing active-tab indicator `<div>` (the `bg-blue-500` underline) and AFTER the text spans. The button already has `relative` in its className, so the badge anchors to the button itself.
    - Place it INSIDE the `<button>` element, not as a sibling. The badge needs the button as its positioning anchor.

    The `<NewBadge>` import must exist at the top of `App.tsx`. If it doesn't already exist (check by reading the file — settings gear button at App.tsx:187 uses `<NewBadge>` per PATTERNS.md §10), add:
    ```typescript
    import { NewBadge } from './components/NewBadge';
    ```

    Mandatory project memory rule check (re-stated for clarity):
    - The badge is `absolute`, NOT inline (would push tab siblings in the flex row)
    - The badge uses `-top-1 -right-1` for upper-right corner placement
    - The badge has `pointer-events-none` so clicks pass through to the underlying tab button
    - The badge is rendered ONLY on the Customers tab button — NOT on every tab (gate via `tab.id === 'customers'`)

    Do NOT add a `<NewBadge>` anywhere else (NOT on the picker combobox, NOT on the Add customer button, NOT inside CustomerLibrary). Per D-16 the tab badge is the single entry point; the picker and CSV import are surfaces of the same feature.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
    <automated>grep -c 'feature="customer-library"' src/App.tsx</automated>
    <automated>grep -c 'tab.id === ' "'customers'" src/App.tsx</automated>
    <automated>grep -c "absolute -top-1 -right-1 pointer-events-none" src/App.tsx</automated>
    <automated>grep -c "NewBadge" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Source: contains exactly one `<NewBadge feature="customer-library" ... />` JSX (assert via grep)
    - Source: the badge's className is exactly `absolute -top-1 -right-1 pointer-events-none` (verbatim — locked by UI-SPEC §1 + project memory)
    - Source: the badge JSX is wrapped in a `tab.id === 'customers' &&` conditional inside the `tabs.map` render block
    - Source: `NewBadge` is imported from `./components/NewBadge` (or the existing import that the settings gear button uses)
    - Source: NO `<NewBadge feature="customer-library"` anywhere ELSE in the codebase (assert: `grep -rc 'feature="customer-library"' src/` returns at most 1, and that match is in `App.tsx`)
    - Behavior: the badge appears as a small green overlay in the upper-right corner of the Customers tab button — does NOT push sibling tab buttons (verified by Task 3 UAT)
    - Build: `npm run build` exits 0
  </acceptance_criteria>
  <done>
    The Customers tab button shows the `customer-library` NEW badge as an absolute overlay; no sibling tabs are displaced; clicking the tab continues to work (`pointer-events-none` lets clicks through to the button).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end UAT for all 5 ROADMAP success criteria</name>
  <what-built>
    Phase 15.1 is now feature-complete:
    - Plan 01: Foundation — `Customer` interface, Dexie v7 `customers` store, `useCustomers()` hook with `customersByEmail` map + `bumpLastUsed` + `bulkImportCustomers`, by-value snapshot audit test
    - Plan 02: Customers tab UI — `CustomerLibrary.tsx`, `CustomerEditModal.tsx`, `formatRelativeDate.ts`, App.tsx tab wiring
    - Plan 03: CSV import — `customerCsv.ts` + tests, `CustomerCsvImportModal.tsx`, modal mount inside CustomerLibrary
    - Plan 04: Record Sale combobox picker + email-match auto-link + auto-create-on-save
    - Plan 05: NewBadge wiring + `features.ts` entry

    This checkpoint validates that all five ROADMAP Phase 15.1 success criteria are met end-to-end before we close the phase.
  </what-built>
  <how-to-verify>
    1. **Kill any existing dev server on port 4173, then start fresh:**
       ```bash
       pkill -f "vite.*4173" 2>/dev/null; npm run dev
       ```
       Visit http://localhost:4173. Confirm the app loads.

    2. **(Success criterion #1) Customers tab presence + NewBadge:**
       - Confirm a `Customers` tab is visible in the main nav, positioned between `Asset Library` and `Printers`.
       - Confirm a small green NEW badge appears in the upper-right corner of the Customers tab button.
       - Confirm the badge does NOT push any adjacent tab buttons (Asset Library / Printers stay in their pre-Phase-15.1 horizontal positions).
       - Click the Customers tab. Confirm `<CustomerLibrary />` renders with the empty state heading `No customers yet` and an `Add customer` CTA.

    3. **(Success criterion #1, continued) Add / Edit / Delete + virtualized list + search + sort:**
       - Click `Add customer`. Fill `Name: Alice Test`, `Email: alice@test.com`, `Company: Acme`, `Address: 123 Lane`, `Notes: VIP customer`. Click `Save customer`.
       - Confirm the row appears in the list with `Alice Test` (bold), `alice@test.com · Acme` (muted subline), and `Last used: Never used` on the right rail (or below subline on mobile).
       - Add a second customer: `Name: Bob`, `Email: bob@example.com`. Save.
       - Add a third customer: `Name: Carol`, `Email: carol@example.com`. Save.
       - Confirm BOTH new customers appear FIRST in the list (newer never-used customers float to top per D-12 + UI-SPEC §2 sort rule).
       - Click `Edit` on Alice's row. Change the address to `999 Updated Ave`. Click `Save customer`. Confirm the row updates.
       - Type `alic` in the search input. Confirm only Alice's row remains visible.
       - Type `acme` in the search input. Confirm Alice's row appears (Company match).
       - Type `123` (an address fragment) in the search input. Confirm NO rows appear (Address is intentionally NOT in the search scope per D-15).
       - Clear search. Click `Delete` on Bob's row. Confirm the native confirm dialog reads exactly: `Delete Bob? They have no past sales linked.` (since Bob has no sales yet). Confirm the dialog. Bob disappears from the list.

    4. **(Success criterion #2) CSV bulk import:**
       - Create a sample CSV file on disk (e.g. via `pbpaste > /tmp/customers.csv` from clipboard, or use `echo`). Content:
         ```csv
         name,email,company,address,notes
         Dana,dana@example.com,Beta Co,456 Side St,
         ,,,,
         Eve,not-an-email,Gamma,789 Far Rd,
         Alice Test Updated,alice@test.com,Updated Acme,,
         ```
       - Click `Import CSV` on the Customers tab. Confirm the upload step renders with the dropzone.
       - Drop or pick the CSV. Confirm the preview step renders with:
         - Stats: `Found 4 rows · 1 ready to import · 1 duplicates · 2 with errors`
         - Row 1 (Dana): OK chip; checkbox checked
         - Row 2 (empty): `Error: Name or Email required` chip; checkbox disabled
         - Row 3 (Eve, bad email): `Error: Email format invalid` chip; checkbox disabled
         - Row 4 (Alice Test Updated, same email as existing Alice): `Duplicate` (yellow) chip; checkbox checked
       - Default radio is `Skip duplicates`. Import button reads `Import 1 customer` (singular).
       - Toggle to `Update duplicates`. Import button updates to `Import 2 customers`. Below the radio: `Existing customers matched by email will be updated (only non-empty incoming fields overwrite — empty cells keep existing values).`
       - Click `Import 2 customers`. Confirm the modal closes, the list now shows `Dana` AND Alice's row now reads `Alice Test Updated` with company `Updated Acme` — BUT her notes are still `VIP customer` and address is still `999 Updated Ave` (D-11 merge rule: empty incoming cells preserve existing values).

    5. **(Success criterion #3) Combobox picker in Record Sale:**
       - Go to the `Jobs` tab. Pick any saved job (create one via the Cost Calculator if needed). Click `Record Sale`.
       - Confirm a new field labeled `Existing customer` appears ABOVE the 4-field Customer block.
       - Type `ali`. Confirm the dropdown opens with Alice as a match. The dropdown row shows `Alice Test Updated` (bold) and `alice@test.com · Updated Acme` (muted).
       - Press ArrowDown to highlight the row (blue background). Press Enter. Confirm:
         - The combobox closes
         - The Name field is filled with `Alice Test Updated`
         - The Email field is filled with `alice@test.com`
         - The Company field is filled with `Updated Acme`
         - The Address field is filled with `999 Updated Ave`
         - The Notes field is EMPTY (D-05: Notes is NOT auto-filled)
       - Click `Save sale`. Confirm the sale is recorded.
       - Go to the Customers tab. Confirm Alice's row now reads `Last used: today` (or similar — `formatRelativeDate` output) and Alice is at the TOP of the list (most-recently-used customer).

    6. **(Success criterion #3, continued) Auto-create on save + silent email-match link:**
       - Back to the Jobs tab. Record another sale on the same job. Without picking from the combobox, type into the 4-field block: `Name: Frank Newman`, `Email: frank@new.com`, `Company: New Co`. Click `Save sale`.
       - Go to the Customers tab. Confirm `Frank Newman` now appears in the library list (D-06 auto-create).
       - Back to Jobs. Record another sale. Type Carol's existing email `carol@example.com` into the Email field but type `Carol Doe (variant name)` into the Name field. Click `Save sale`.
       - Go to the Customers tab. Confirm Carol's row STILL reads `Carol` (NOT `Carol Doe (variant name)`) — D-07: silent email-match link does NOT overwrite library fields. Confirm Carol is at the top of the list (lastUsedAt was bumped).

    7. **(Success criterion #4) By-value snapshot audit — manual + automated:**
       - Run the unit test: `npx vitest run src/utils/__tests__/byValueSnapshot.test.ts` (or wherever Plan 01 placed it). Confirm 3 tests pass.
       - Manual check: go to Jobs tab → expand the job with the Alice sale → in the Recent Sales accordion confirm the sale's customer block shows the EXACT name/email/company/address that were on the sale when it was recorded (`Alice Test Updated` + `999 Updated Ave`). Now go to Customers tab → edit Alice → change Name to `Alice (After Edit)`. Save. Return to Jobs tab → re-expand the same sale. Confirm the sale's customer block STILL reads `Alice Test Updated` (NOT `Alice (After Edit)`). The Sale snapshot is unchanged.

    8. **(Success criterion #5) Dexie v7 migration:**
       - Open the browser DevTools → Application → IndexedDB → `3DCosterDB`. Confirm a `customers` object store exists alongside `materials`, `printers`, `printerInstances`, `jobs`, `sales`, `settings`.
       - Confirm the database `version` is `7` (visible in the IndexedDB DevTools panel).
       - Confirm no other store schemas changed (open `jobs` → spot-check that an existing job from before Phase 15.1 still loads).

    9. **NewBadge dismissal verification:**
       - After clicking the Customers tab (any interaction that triggers the badge's "seen" gate), the badge should still show briefly. After `NEW_FEATURE_SEEN_HOURS` (36h) it should disappear from localStorage tracking. This is not testable in-session — just confirm the badge IS rendered now (step 2 above) and trust the existing `NewBadge.tsx` logic.

    10. **Final build gate:**
       - `npm run build` exits 0
       - `npx vitest run` (all tests) exits 0 (including the byValueSnapshot.test.ts from Plan 01 and customerCsv.test.ts from Plan 03 — should be 15+ new passing tests for Phase 15.1)

    Record the UAT outcome in `.planning/phases/15.1-customer-library/15.1-VERIFICATION.md` with a PASS/FAIL line for each numbered step above. Any FAIL → file an issue and decide whether to revise or gap-close.
  </how-to-verify>
  <resume-signal>Type `approved` if all 10 steps PASS. If any step fails, describe the failure and which success criterion regressed.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- `npx vitest run` exits 0 (all tests pass — including the two new Phase 15.1 test files)
- `grep -rc "feature=\"customer-library\"" src/` returns exactly 1
- `grep -c "'customer-library':" src/features.ts` returns 1
- UAT in Task 3 produces a `.planning/phases/15.1-customer-library/15.1-VERIFICATION.md` with all 10 steps marked PASS
</verification>

<success_criteria>
- All five ROADMAP Phase 15.1 success criteria are met end-to-end (manually verified)
- The NewBadge renders as an absolute overlay on the Customers tab button only — no layout displacement
- `customer-library` is the only Phase 15.1 entry in `src/features.ts`
- The phase is ready for commit + ship
</success_criteria>

<output>
Create `.planning/phases/15.1-customer-library/15.1-05-SUMMARY.md` AND `.planning/phases/15.1-customer-library/15.1-VERIFICATION.md` when done. The VERIFICATION file records the human UAT outcome step-by-step; the SUMMARY records the artifacts shipped and the final ship-date in `src/features.ts`.

If any UAT step FAILED, write the failure narrative into the SUMMARY and recommend either:
- A revision plan (`/gsd:plan-phase 15.1 --gaps`) to close the specific gap, OR
- An escalation to the user (locked architectural decision regressed — needs re-discussion)
</output>
