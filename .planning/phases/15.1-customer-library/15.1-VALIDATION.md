---
phase: 15.1
slug: customer-library
status: passed
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-25
reconstructed_from:
  - 15.1-01-SUMMARY.md
  - 15.1-02-SUMMARY.md
  - 15.1-03-SUMMARY.md
  - 15.1-04-SUMMARY.md
  - 15.1-05-SUMMARY.md
  - 15.1-VERIFICATION.md
---

# Phase 15.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> **State B reconstruction:** Phase 15.1 closed on 2026-05-22 (`15.1-VERIFICATION.md` status: passed, 5/5 must-haves). No VALIDATION.md was authored at the time. This contract is reconstructed from the 5 plan SUMMARYs in this directory plus the closure VERIFICATION.md, per `/gsd:validate-phase` State B workflow. Rows mark COVERED against shipped tests — no Wave 0 work needed.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.4 |
| **Config file** | `vitest.config.ts` (repo root, existing — v1.1 Phase 10 infrastructure; environment: jsdom) |
| **Quick run command** | `npx vitest run src/utils/byValueSnapshot.test.ts src/utils/customerCsv.test.ts` |
| **Full suite command** | `npm test` (alias for `vitest run`) |
| **Build verification** | `npm run build` (runs the project's 8-gate chain: lint-no-raw-html → assert-no-static-jspdf → vitest --coverage → tsc -b → vite build → assert-bundle-size → assert-no-pdf-preload → assert-no-static-pdf-import) |
| **Estimated runtime** | ~1 second (quick: 15 tests across 2 files), ~4 seconds (full: 18 files / 272 tests + 1 todo) |

---

## Sampling Rate

- **After every task commit:** Run `npx vitest run src/utils/byValueSnapshot.test.ts src/utils/customerCsv.test.ts`
- **After every plan wave:** Run `npm test`
- **Before `/gsd:verify-work`:** Full suite green + `npm run build` exits 0 + manual UAT signed off on visual-only criteria (see `15.1-VERIFICATION.md` § Human Verification)
- **Max feedback latency:** ~5 seconds (quick), ~30 seconds (full build chain)

---

## Per-Task Verification Map

> Reconstructed from the 5 Phase 15.1 plan SUMMARYs. Each row maps a shipped task to its declared requirement (CL-01..05) and the automated command that proves it. Rows are marked ✅ green because the tests shipped 2026-05-22 and the full suite re-ran green on 2026-05-25 during this audit (18 files, 272 tests passed).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 15.1-01-01 | 01 | 1 | CL-02 | — | Customer interface extends JobCustomer; library-only fields (id/createdAt/lastUsedAt) NEVER leak into per-Sale snapshot | grep | `grep -c "^export interface Customer extends JobCustomer" src/types.ts` returns 1 | ✅ existing | ✅ green |
| 15.1-01-02 | 01 | 1 | CL-02 | — | Dexie v7 adds `customers` store as additive migration; v6 block byte-identical | grep | `grep -c "db.version(7).stores" src/db/database.ts` returns 1 AND `grep -c "customers: 'id, name, email, lastUsedAt'" src/db/database.ts` returns 1 | ✅ existing | ✅ green |
| 15.1-01-03 | 01 | 1 | CL-02 | — | useCustomers() hook surface (customers + customersByEmail + isLoading + add/update/delete + bumpLastUsed + bulkImportCustomers) | grep | `grep -c "^export function useCustomers" src/hooks/useDatabase.ts` returns 1 AND `grep -c "customersByEmail" src/hooks/useDatabase.ts` >= 2 | ✅ existing | ✅ green |
| 15.1-01-04 | 01 | 1 | CL-05 | — | By-value snapshot — editing/deleting a library Customer does NOT mutate any historical Sale.customer; library-only fields never appear on the snapshot | unit | `npx vitest run src/utils/byValueSnapshot.test.ts` (3/3 tests pass) | ✅ existing | ✅ green |
| 15.1-02-01 | 02 | 2 | CL-01 | — | formatRelativeDate returns literal `'Never used'` for undefined; otherwise `Intl.RelativeTimeFormat('en', { numeric: 'auto' })` output | grep | `grep -c "Never used" src/utils/formatRelativeDate.ts` returns 1 AND `grep -c "Intl.RelativeTimeFormat" src/utils/formatRelativeDate.ts` returns 1 | ✅ existing | ✅ green |
| 15.1-02-02 | 02 | 2 | CL-01 | — | CustomerEditModal exists with 5-field Add/Edit form; Name+Email asterisks; `Provide at least one of Name or Email.` helper | grep | `grep -c "^export function CustomerEditModal" src/components/CustomerEditModal.tsx` returns 1 AND `grep -c "Provide at least one of Name or Email" src/components/CustomerEditModal.tsx` returns 1 | ✅ existing | ✅ green |
| 15.1-02-03 | 02 | 2 | CL-01 | — | CustomerLibrary renders virtualized list (`useDynamicRowHeight`); sort places undefined `lastUsedAt` FIRST (never-used floats); search filter touches Name + Email + Company only (Address/Notes excluded per D-15); delete uses `window.confirm` with "this only removes them from your library" copy | grep | `grep -c "useDynamicRowHeight" src/components/CustomerLibrary.tsx` >= 2 AND `grep -c "this only removes them from your library" src/components/CustomerLibrary.tsx` returns 1 AND `grep -c "window.confirm" src/components/CustomerLibrary.tsx` >= 1 | ✅ existing | ✅ green |
| 15.1-02-04 | 02 | 2 | CL-01 | — | App.tsx Tab union includes `'customers'`; activeTab branch renders `<CustomerLibrary />`; all 6 useCustomers() members destructured | grep | `grep -c "'customers'" src/App.tsx` >= 1 AND `grep -c "<CustomerLibrary" src/App.tsx` >= 1 | ✅ existing | ✅ green |
| 15.1-03-01 | 03 | 2 | CL-03 | — | parseCustomerCsv (case-insensitive headers, Name-OR-Email rule, email format check, email lowercased on write); buildCustomersForImport with D-11 field-by-field merge (empty cells preserve existing values); case-insensitive email dedup | grep | `grep -c "^export function parseCustomerCsv" src/utils/customerCsv.ts` returns 1 AND `grep -c "^export function buildCustomersForImport" src/utils/customerCsv.ts` returns 1 AND `grep -c "Name or Email required" src/utils/customerCsv.ts` returns 1 AND `grep -c "Email format invalid" src/utils/customerCsv.ts` returns 1 | ✅ existing | ✅ green |
| 15.1-03-02 | 03 | 2 | CL-03 | — | 12 parser tests cover: empty input, header-only, name-only, uppercase-email-lowercased, both-blank → error, malformed-email → error, uppercase headers, unknown column ignored, case-insensitive dedup against existing record + merge semantics (skip/overwrite + D-11 empty-cell-preserves-existing) | unit | `npx vitest run src/utils/customerCsv.test.ts` (12/12 tests pass) | ✅ existing | ✅ green |
| 15.1-03-03 | 03 | 2 | CL-03 | — | CustomerCsvImportModal renders Upload → Preview with `Skip duplicates` / `Update duplicates` radio; mounts inside CustomerLibrary with the four locked props (`isOpen`, `existingCustomers`, `onImportCustomers`, `onClose`) | grep | `grep -c "^export function CustomerCsvImportModal" src/components/CustomerCsvImportModal.tsx` returns 1 AND `grep -c "Skip duplicates" src/components/CustomerCsvImportModal.tsx` returns 1 AND `grep -c "Update duplicates" src/components/CustomerCsvImportModal.tsx` returns 1 AND `grep -c "<CustomerCsvImportModal" src/components/CustomerLibrary.tsx` returns 1 | ✅ existing | ✅ green |
| 15.1-04-01 | 04 | 3 | CL-04 | — | Record Sale combobox state + handlers — WAI-ARIA keyboard (ArrowDown/Up/Enter/Escape/Tab); D-05 LOCK: handlePickCustomer NEVER calls setSaleCustomerNotes; bumpLastUsed wired on pick | grep | `grep -c "handlePickCustomer" src/components/JobsManager.tsx` >= 2 AND `grep -c "handlePickerKeyDown" src/components/JobsManager.tsx` >= 2 AND `grep -c "D-05: DO NOT setSaleCustomerNotes" src/components/JobsManager.tsx` returns 1 AND `grep -c "bumpLastUsed" src/components/JobsManager.tsx` >= 2 | ✅ existing | ✅ green |
| 15.1-04-02 | 04 | 3 | CL-04 | — | Combobox JSX with `role="combobox"`, `role="listbox"`, `role="option"`, `aria-activedescendant`; "Existing customer" label; top-8 visible-slice overflow footer | grep | `grep -c 'role="combobox"' src/components/JobsManager.tsx` returns 1 AND `grep -c 'role="listbox"' src/components/JobsManager.tsx` returns 1 AND `grep -c "Existing customer" src/components/JobsManager.tsx` returns 1 AND `grep -c "Showing first 8 of" src/components/JobsManager.tsx` returns 1 | ✅ existing | ✅ green |
| 15.1-04-03 | 04 | 3 | CL-04 | — | handleRecordSale auto-link (D-07 silent bumpLastUsed on email match) + auto-create (D-06 addCustomer for new identity); `!editingSale && customer && (customer.name \|\| customer.email)` three-condition guard ensures NEW-sale-only writeback; by-value snapshot of Sale.customer byte-identical pre/post block | grep | `grep -c "customersByEmail.get" src/components/JobsManager.tsx` returns 1 AND `grep -c "!editingSale" src/components/JobsManager.tsx` returns 1 AND `grep -c "await addCustomer" src/components/JobsManager.tsx` returns 1 | ✅ existing | ✅ green |
| 15.1-05-01 | 05 | 4 | CL-01 | — | `customer-library` entry in featureReleases registry with ship date | grep | `grep -c "'customer-library':" src/features.ts` returns 1 | ✅ existing | ✅ green |
| 15.1-05-02 | 05 | 4 | CL-01 | — | Single absolute-overlay NewBadge on Customers tab button (not inline; flex tab row layout unchanged); no badges on combobox / Add customer / Import CSV (single feature entry-point pattern) | grep | `grep -rc 'feature="customer-library"' src/` returns 1 AND `grep -c "absolute -top-1 -right-1 pointer-events-none" src/App.tsx` >= 1 | ✅ existing | ✅ green |
| 15.1-05-03 | 05 | 4 | CL-01..CL-05 | — | 10-step end-to-end UAT (dev server load, tab + badge + empty state, Add/Edit/Delete + search + sort, CSV import skip/update + D-11 merge, picker fills 4 fields not Notes, auto-create + silent link, by-value Library-edit isolation, DevTools IndexedDB v7, NewBadge dismissal gates, final build+test gate) — verdict 10/10 PASS | manual UAT | n/a — recorded in `15.1-05-SUMMARY.md` Tasks Completed Task 3 + `15.1-VERIFICATION.md` § Human Verification | n/a | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Per-requirement row count:**
- CL-01 (Customers tab + virtualized list + Add/Edit/Delete): 5 rows (15.1-02-01..04 + 15.1-05-01..03 partial)
- CL-02 (Customer interface + Dexie v7 customers store + useCustomers hook): 3 rows (15.1-01-01..03)
- CL-03 (CSV bulk import with merge semantics): 3 rows (15.1-03-01..03)
- CL-04 (Record Sale combobox + auto-link/auto-create): 3 rows (15.1-04-01..03)
- CL-05 (By-value snapshot rule): 1 row (15.1-01-04) + cross-reference rows in CL-04 (snapshot preservation invariant) and CL-01 UAT (manual sale-snapshot independence)

All 5 declared Phase 15.1 requirements have ≥1 dedicated row, satisfying the must-haves `key_links` contract.

---

## Wave 0 Requirements

None — Phase 15.1 tests already shipped. Phase closed 2026-05-22 (`15.1-VERIFICATION.md` status: passed, 5/5 must-haves verified). All test files referenced in the Per-Task Map exist on disk; the full Vitest suite (`npm test`) runs green at 18 files / 272 tests + 1 todo / 0 failures as of the 2026-05-25 audit re-run.

For reference, the test artifacts that Phase 15.1 produced are:

- `src/utils/byValueSnapshot.test.ts` — 3 tests (CL-05 architectural lock; pure-TypeScript, no Dexie/React/DOM)
- `src/utils/customerCsv.test.ts` — 12 tests (CL-03 parser + builder)
- Implementation files (covered by grep-style row checks above): `src/types.ts`, `src/db/database.ts`, `src/hooks/useDatabase.ts`, `src/utils/formatRelativeDate.ts`, `src/utils/customerCsv.ts`, `src/components/CustomerLibrary.tsx`, `src/components/CustomerEditModal.tsx`, `src/components/CustomerCsvImportModal.tsx`, `src/components/JobsManager.tsx`, `src/App.tsx`, `src/features.ts`

No framework install needed — Vitest 4.1.4 already in `package.json` and `vitest.config.ts` already exists at repo root (v1.1 Phase 10 infrastructure).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Customers tab appears in main nav with NEW badge upper-right and no sibling tab displacement | CL-01 | Visual / DOM layout | Open dev server at :4173 → confirm "Customers" tab visible between existing tabs → absolute-overlay NEW badge sits in `-top-1 -right-1` corner of the tab button → sibling tabs unchanged in width and spacing |
| Empty state renders on first load (no customers yet) | CL-01 | Visual / DOM | Fresh IndexedDB → switch to Customers tab → confirm empty-state copy + CTA |
| Virtualized list renders correctly with sort + search | CL-01 | Visual / functional | Seed >50 customers via CSV → confirm list virtualizes (react-window) → confirm sort places never-used (undefined `lastUsedAt`) FIRST → search by Name, Email, Company filters live; Address/Notes are intentionally NOT searched (D-15) |
| Add/Edit/Delete affordances behave correctly | CL-01 | Visual / DOM | Click Add → CustomerEditModal opens with 5 fields → save with only Name OR only Email succeeds → save with both blank is blocked → Edit hydrates initial values → Delete triggers `window.confirm` with the locked "this only removes them from your library" copy |
| CSV import dropzone + Skip vs Update radio + D-11 merge semantics | CL-03 | Visual / functional | Click Import CSV → modal opens → drop a CSV → Preview step shows status chips (OK/Duplicate/Error) → toggle Skip vs Update → import → on Update mode, confirm an existing customer's manually-typed notes/address are PRESERVED when the CSV row has those cells empty (D-11 invariant) |
| Record Sale combobox picker keyboard semantics | CL-04 | Visual / functional | Open Record Sale modal → type 2+ chars in "Existing customer" input → dropdown opens with matching customers → ArrowDown/Up navigate; Enter picks; Escape closes; Tab closes without picking; visual highlight matches `aria-activedescendant` |
| Picker fills 4 fields, NEVER fills Notes (D-05) | CL-04 | Visual / DOM | Type a customer record's Notes value first → pick that customer from the combobox → confirm Name/Email/Company/Address are filled but Notes is UNCHANGED |
| Auto-create on save for new identity (D-06) | CL-04 | Visual / functional | New sale → type a NAME+EMAIL not in the library → save sale → switch to Customers tab → confirm a new library record appears |
| Silent email-match auto-link on save (D-07) | CL-04 | Visual / functional | New sale → type the email of an EXISTING customer (case-insensitive) → save sale → switch to Customers tab → confirm NO new record appears; the existing record's `lastUsedAt` has moved forward; the existing record's fields (name/company/address/notes) are UNCHANGED |
| Editing a previously-saved sale does NOT retroactively auto-create/auto-link | CL-04 | Functional | Open an existing sale (saved before Plan 04 ship) → save without changes → confirm the Customers tab list is unchanged |
| By-value snapshot — Sale.customer block unchanged after Library edit | CL-05 | Functional | Record a sale with a picked customer → switch to Customers tab → edit that customer's Name/Email/Company/Address/Notes → reopen the original Sale → confirm Sale.customer block shows the ORIGINAL pre-edit values (byte-identical to the snapshot taken at sale time) |
| DevTools IndexedDB shows `customers` store on database version 7 | CL-02 | DB inspection | Open DevTools → Application → IndexedDB → `3DCosterDB` → confirm version is `7` and `customers` store exists with index keys `id, name, email, lastUsedAt` |

These manual-only verifications were performed end-to-end on 2026-05-22 as the 10-step UAT recorded in `15.1-05-SUMMARY.md` § UAT Outcome and `15.1-VERIFICATION.md` § Human Verification (10/10 PASS).

> **Note on the deferred component tests:** REQUIREMENTS.md TEST-01/02/03 (HIGH — `CustomerEditModal.test.tsx`, `CustomerCsvImportModal.test.tsx`, `CustomerLibrary.test.tsx`) target the Customer UI surface but are explicitly scoped to **Phase 23** (Test coverage hardening) per REQUIREMENTS.md lines 157–159 and the v1.3 ROADMAP. They are NOT validation gaps in Phase 15.1's shipped-functionality contract — Phase 15.1 shipped the implementation and the parser/architectural tests; Phase 23 will add the component-level tests as test-debt closure. This VALIDATION.md remains `nyquist_compliant: true` for Phase 15.1's scope.

---

## Validation Sign-Off

- [x] All shipped Phase 15.1 tasks have an `<automated>` verify command or grep-style row check (see Per-Task Verification Map; manual UAT covers the remaining visual/DOM surfaces)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every plan ships at least one grep or unit-test row)
- [x] Wave 0 covers all MISSING references — N/A; nothing MISSING (phase closed)
- [x] No watch-mode flags (`vitest run`, not `vitest`)
- [x] Feedback latency < 5s (quick); < 30s (full)
- [x] `nyquist_compliant: true` set in frontmatter
- [x] State B reconstruction sourced from the 5 plan SUMMARYs + 15.1-VERIFICATION.md (cited in frontmatter `reconstructed_from`)

**Approval:** approved 2026-05-25 (State B reconstruction during Phase 24 NYQ-03; closes TECH-DEBT D3)
