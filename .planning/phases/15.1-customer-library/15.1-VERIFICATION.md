---
phase: 15.1-customer-library
verified: 2026-05-22T13:23:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 15.1: Customer Library Verification Report

**Phase Goal:** Customers as first-class assets — new main nav tab with CRUD, bulk CSV import, and a dropdown picker (or add-new inline) in the Record Sale modal. Unblocks the "pick a saved customer" workflow Phase 16 (PDF Quote) will draw on so quotes can be generated for a chosen customer without retyping fields. Customer Library uses its own Dexie store; per-Sale customer remains a snapshot of values at sale time (not a foreign-key reference) so historical sales never mutate when a Customer record is later edited.

**Verified:** 2026-05-22T13:23:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 (CL-01) | Customers tab in main nav with virtualized list + Add/Edit/Delete affordances | VERIFIED | `App.tsx:18` Tab union includes `'customers'`; `App.tsx:158` tabs entry; `App.tsx:326` activeTab renders `<CustomerLibrary />`. `CustomerLibrary.tsx:3` imports `useDynamicRowHeight` from `react-window`; `:106` `export function CustomerLibrary`; `:153` `useDynamicRowHeight({ defaultRowHeight: 88 })`; `:189` delete-confirm includes "this only removes them from your library" copy; `:190` uses `window.confirm`; search filter at `:142-150` covers Name + Email + Company only (Address/Notes excluded per D-15); sort at `:132` places undefined `lastUsedAt` FIRST (never-used floats). `CustomerEditModal.tsx:12` exports modal used for both Add and Edit. |
| 2 (CL-03) | CSV bulk import with preview + validation + duplicate handling (skip OR update) | VERIFIED | `CustomerCsvImportModal.tsx:30` `export function CustomerCsvImportModal`; `:77` calls `parseCustomerCsv`; `:154` calls `buildCustomersForImport`; `:157` calls `onImportCustomers`; `:192` two-step header copy `Import customers from CSV` / `Preview customer import`; `:379` + `:390` radio labels `Skip duplicates` + `Update duplicates`. `customerCsv.ts:43` exports `parseCustomerCsv`; `:56` case-insensitive header normalization; `:109` `Name or Email required` validation; `:115` `Email format invalid` validation; `:122` email lowercased on write; `:182` exports `buildCustomersForImport` with field-by-field conditional merge (D-11 — empty cells preserve existing). `CustomerLibrary.tsx:288` mounts `<CustomerCsvImportModal>` with all four locked props. Tests: 12 parser tests passing. |
| 3 (CL-04) | Record Sale modal combobox picker that fills 4 fields; Add-new auto-create on submit | VERIFIED | `JobsManager.tsx:4` imports `useCustomers`; `:384` destructures `customers, customersByEmail, bumpLastUsed, addCustomer`; `:548-559` `handlePickCustomer` fills Name/Email/Company/Address and bumps `lastUsedAt` — does NOT call `setSaleCustomerNotes` (D-05 lock confirmed by grep: 5 occurrences of `setSaleCustomerNotes`, none in `handlePickCustomer`); `:561-595` `handlePickerKeyDown` implements ArrowDown/Up/Enter/Escape/Tab WAI-ARIA keyboard semantics; `:852` label `Existing customer`; `:858` `role="combobox"`; `:879` `role="listbox"`; `:910` empty-results copy. Auto-link/auto-create at `:639` guarded by `if (!editingSale && customer && (customer.name || customer.email))`; `:641` `customersByEmail.get(emailKey)`; `:644` `await bumpLastUsed(existing.id)` (silent link, D-07); `:659` `await addCustomer(newCustomer)` (auto-create, D-06). |
| 4 (CL-05) | By-value snapshot — `Sale.customer` is byte-identical after any later library edit | VERIFIED | `src/utils/byValueSnapshot.test.ts` exists with `describe('Customer library by-value snapshot rule (CL-05)')`. Vitest run: 3/3 tests pass — (a) editing a Customer does not mutate Sale.customer snapshot; (b) deleting a Customer does not mutate the snapshot; (c) library-only fields (id/createdAt/lastUsedAt) never appear on the snapshot. Architectural enforcement at types level: `types.ts:243` `Sale.customer?: JobCustomer` (NOT `Customer`) means library-only fields cannot leak into per-Sale snapshots. `JobsManager.tsx` `handleRecordSale` auto-link path bumps `lastUsedAt` on the library record only — does NOT call `updateCustomer` or mutate the `customer` payload going into `addSale`. |
| 5 (CL-02) | Dexie v6→v7 migration adds `customers` store; existing v6 data untouched | VERIFIED | `src/types.ts:163` `export interface Customer extends JobCustomer` with `id: string`, `createdAt: Date`, `lastUsedAt?: Date`. `src/db/database.ts:19` `customers: EntityTable<Customer, 'id'>` intersection; `:91` `db.version(7).stores({...})` block; `:98` `customers: 'id, name, email, lastUsedAt'` (email is non-unique secondary index); no `.upgrade()` callback (no-op — new store starts empty per D-02). `src/hooks/useDatabase.ts:536` `export function useCustomers()`; `:552` `customersByEmail` memoized Map; `:577` `bumpLastUsed` read-modify-write callback; `:586` `bulkImportCustomers` upsert callback. v6 block byte-identical to pre-plan content. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/types.ts` | `Customer` interface extending `JobCustomer` | VERIFIED | Line 163: `export interface Customer extends JobCustomer { id: string; createdAt: Date; lastUsedAt?: Date; }`. JobCustomer at line 140 unchanged. |
| `src/db/database.ts` | Dexie v7 + `customers` store | VERIFIED | Line 19: EntityTable intersection. Lines 91-99: `db.version(7).stores({...})` with `customers: 'id, name, email, lastUsedAt'`. |
| `src/hooks/useDatabase.ts` | `useCustomers()` with full public surface | VERIFIED | Line 536: function declared. Lines 552, 577, 586, 592-598: all members of return shape present (customers, customersByEmail, isLoading, addCustomer, updateCustomer, deleteCustomer, bumpLastUsed, bulkImportCustomers). |
| `src/utils/byValueSnapshot.test.ts` | CL-05 audit test | VERIFIED | File exists; 3 tests pass via `npx vitest run`. |
| `src/utils/formatRelativeDate.ts` | Date helper for "Last used" copy | VERIFIED | File exists; module-scope `Intl.RelativeTimeFormat` cached; returns `'Never used'` for undefined. |
| `src/utils/customerCsv.ts` | CSV parser + merge builder | VERIFIED | Exports `parseCustomerCsv` (line 43) and `buildCustomersForImport` (line 182). Field-by-field conditional merge implements D-11. Email lowercased at line 122. |
| `src/utils/customerCsv.test.ts` | Parser unit tests | VERIFIED | 12 tests pass via `npx vitest run`. |
| `src/components/CustomerLibrary.tsx` | Customers tab body with virtualized list | VERIFIED | Line 106 export; line 153 uses `useDynamicRowHeight`; all required copy strings present; native confirm for delete; mounts both `CustomerEditModal` and `CustomerCsvImportModal`. |
| `src/components/CustomerEditModal.tsx` | 5-field Add/Edit modal | VERIFIED | Line 12 export; `max-w-md` panel; `Provide at least one of Name or Email.` helper; `Save customer` button label; Name+Email asterisks; OR-required invariant enforced via disabled state. |
| `src/components/CustomerCsvImportModal.tsx` | Upload → Preview modal | VERIFIED | Line 30 export; calls `parseCustomerCsv` at line 77 and `buildCustomersForImport` at line 154; both header copy strings + radio labels + merge explainer all present. |
| `src/components/JobsManager.tsx` | Combobox picker + auto-link/auto-create | VERIFIED | Line 384 `useCustomers` destructure; lines 548-595 picker handlers; lines 629-662 auto-link/auto-create in `handleRecordSale`; lines 849-917 combobox JSX with WAI-ARIA semantics. |
| `src/features.ts` | `customer-library` entry | VERIFIED | Line 12: `'customer-library': new Date('2026-05-22')`. |
| `src/App.tsx` | Tab wiring + NewBadge | VERIFIED | Line 3 imports `useCustomers`; line 8 imports `CustomerLibrary`; line 18 Tab union; line 76 destructure; line 158 tabs array; lines 269-271 conditional `<NewBadge feature="customer-library" />` (single badge); line 326 activeTab render. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `App.tsx` Customers tab button | `features.ts` customer-library entry | `<NewBadge feature="customer-library" />` | WIRED | Exactly one badge in src/ — `grep -rc 'feature="customer-library"' src/` = 1, in App.tsx. |
| `App.tsx` | `CustomerLibrary.tsx` | `activeTab === 'customers'` render branch | WIRED | Line 326 renders component with all 6 props from `useCustomers()`. |
| `CustomerLibrary.tsx` | `useCustomers()` hook | Props passed from App.tsx | WIRED | All 6 callbacks (`onAddCustomer`, `onUpdateCustomer`, `onDeleteCustomer`, `onBulkImportCustomers`, `customers`, `isLoading`) destructured and consumed. |
| `CustomerLibrary.tsx` | `CustomerEditModal.tsx` | Modal mount with `isAdding/editingCustomer` state | WIRED | Lines 280-286 mount with `onSave={handleSaveFromModal}` routing to `onAddCustomer` or `onUpdateCustomer`. |
| `CustomerLibrary.tsx` | `CustomerCsvImportModal.tsx` | Modal mount with `showCsvImport` state | WIRED | Lines 288-293 mount with `onImportCustomers={onBulkImportCustomers}` routing to `db.customers.bulkPut`. |
| `CustomerCsvImportModal.tsx` | `customerCsv.ts` | Imports `parseCustomerCsv` + `buildCustomersForImport` | WIRED | Lines 4-5 imports; line 77 invokes parser; line 154 invokes builder. |
| `JobsManager.tsx` combobox JSX | `useCustomers()` (customers/customersByEmail/bumpLastUsed/addCustomer) | Hook invocation at line 384 | WIRED | All four members consumed: `customers` (filter source), `customersByEmail` (auto-link lookup), `bumpLastUsed` (pick + silent link), `addCustomer` (auto-create). |
| `JobsManager.tsx` `handlePickCustomer` | saleCustomer* state setters | Direct setter calls | WIRED | Lines 549-552 call 4 setters (Name/Email/Company/Address); explicit `// D-05: DO NOT setSaleCustomerNotes` comment confirms Notes intentionally omitted. |
| `JobsManager.tsx` `handleRecordSale` auto-link | `db.customers` via `bumpLastUsed`/`addCustomer` | Sequential awaits before `addSale` | WIRED | Guard `!editingSale && customer && (customer.name || customer.email)` ensures NEW-sale-only writeback. |
| `useCustomers()` hook | `db.customers` Dexie store | `useLiveQuery(() => db.customers.toArray())` + EntityTable intersection | WIRED | Live query + bulkPut + add/put/delete/get all consume the v7 store. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `CustomerLibrary.tsx` | `customers` prop | `useCustomers()` → `useLiveQuery(() => db.customers.toArray())` in `useDatabase.ts:543` | Yes — live Dexie subscription on the v7 `customers` store | FLOWING |
| `CustomerLibrary.tsx` | `searchedCustomers` | Derived from `customers` via sort+filter | Yes — propagates upstream Dexie data | FLOWING |
| `CustomerEditModal.tsx` | Form state | Hydrated from `initialCustomer` prop on Edit; submit calls `onSave` which routes to `addCustomer`/`updateCustomer` Dexie writes | Yes — writes propagate via `useLiveQuery` back to `customers` | FLOWING |
| `CustomerCsvImportModal.tsx` | `parseResult` | `parseCustomerCsv(text, existingCustomers)` on file upload; `onImportCustomers` invokes `bulkImportCustomers` → `db.customers.bulkPut` | Yes — real parse + real DB write | FLOWING |
| `JobsManager.tsx` combobox | `filteredCustomers` | `customers` from `useCustomers()` filtered by query | Yes — live data | FLOWING |
| `JobsManager.tsx` `handleRecordSale` auto-link | `customersByEmail.get(emailKey)` | Memoized Map from `useCustomers()` | Yes — fresh on each render via `useLiveQuery` | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TypeScript build | `npx tsc -b` | exit 0, no output | PASS |
| Foundation by-value test (CL-05) | `npx vitest run src/utils/byValueSnapshot.test.ts` | 3/3 tests passing | PASS |
| CSV parser tests (CL-03) | `npx vitest run src/utils/customerCsv.test.ts` | 12/12 tests passing | PASS |
| Combined Phase 15.1 tests | `npx vitest run src/utils/byValueSnapshot.test.ts src/utils/customerCsv.test.ts` | 2 files, 15 tests pass (808ms) | PASS |
| Single badge instance | `grep -rc 'feature="customer-library"' src/` | 1 (App.tsx only) | PASS |
| features.ts entry | `grep -c "'customer-library':" src/features.ts` | 1 | PASS |
| D-05 Notes lock | `grep -c "setSaleCustomerNotes" src/components/JobsManager.tsx` | 5 occurrences, none inside `handlePickCustomer` body (explicit `// D-05: DO NOT setSaleCustomerNotes` comment present) | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CL-01 | 15.1-02, 15.1-05 | Customers tab in main nav with virtualized list + Add/Edit/Delete | SATISFIED | App.tsx tab wiring + CustomerLibrary.tsx + CustomerEditModal.tsx + react-window virtualization confirmed |
| CL-02 | 15.1-01, 15.1-05 | Customer interface + Dexie v7 customers store + useCustomers hook | SATISFIED | types.ts:163 + database.ts:91-99 + useDatabase.ts:536 all verified |
| CL-03 | 15.1-03, 15.1-05 | CSV bulk import with merge semantics | SATISFIED | customerCsv.ts + customerCsv.test.ts (12 passing) + CustomerCsvImportModal.tsx + mount in CustomerLibrary.tsx verified |
| CL-04 | 15.1-04, 15.1-05 | Record Sale combobox + auto-link/auto-create | SATISFIED | JobsManager.tsx picker state, JSX, handleRecordSale guarded side-effect all confirmed; D-05 Notes lock verified |
| CL-05 | 15.1-01, 15.1-05 | By-value snapshot — Sale.customer byte-identical after library edits | SATISFIED | byValueSnapshot.test.ts (3 passing); types.ts uses `JobCustomer` (not `Customer`) for `Sale.customer` snapshot type |

All 5 requirement IDs accounted for across plans 01–05. REQUIREMENTS.md status table still reads "Pending" but that's a documentation lag — the implementation evidence is conclusive.

### Anti-Patterns Found

None. Grep for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER|placeholder text|coming soon` across all 10 Phase 15.1 modified/created source files returned zero matches. SUMMARY 02 deviations document an `aria-pressed` workaround for `noUnusedLocals` (legitimate UX improvement, not a stub) and SUMMARY 04 documents one `allow-raw-html` exemption for the WAI-ARIA listbox option (matches project precedent at ImageCarousel.tsx:124).

### Human Verification — Already Performed

Per SUMMARY 15.1-05, a 10-step human UAT was run on 2026-05-22 covering all 5 ROADMAP success criteria:

| # | UAT Step | SC | Verdict |
|---|----------|-----|---------|
| 1 | Dev server loads at :4173 | — | PASS |
| 2 | Customers tab + NEW badge + no sibling displacement + empty state | CL-01 | PASS |
| 3 | Add/Edit/Delete + virtualized list + search (name/email/company) + sort | CL-01 | PASS |
| 4 | CSV bulk import — skip vs update radio, D-11 merge | CL-03 | PASS |
| 5 | Combobox picker fills 4 fields not Notes (D-05), bumps lastUsedAt | CL-04 | PASS |
| 6 | Auto-create on save (D-06) + silent email-match link (D-07) | CL-04 | PASS |
| 7 | By-value snapshot — Sale.customer unchanged after Library edit | CL-05 | PASS |
| 8 | DevTools → IndexedDB → customers store visible, version 7 | CL-02 | PASS |
| 9 | NewBadge dismissal gates | — | PASS |
| 10 | Final `npm run build` + `npx vitest run` — 125/125 tests | — | PASS |

10/10 PASS. One follow-up captured (`customer-csv-template-download` polish item filed at `.planning/todos/`); does not block ship.

### Gaps Summary

None. All five ROADMAP success criteria are implemented in code, all five requirement IDs map to plans and have artifacts, all key links are wired, the human UAT recorded 10/10 PASS, and the in-session test/build re-verification passes (TypeScript exit 0, 15/15 Phase 15.1 tests passing).

The phase goal — "Customers as first-class assets — new main nav tab with CRUD, bulk CSV import, and a dropdown picker (or add-new inline) in the Record Sale modal" — is fully achieved. The unblocked downstream "pick a saved customer" workflow that Phase 16 (PDF Quote) will draw on is in place. The architectural invariant that "per-Sale customer remains a snapshot of values at sale time" is locked both by the type system (`Sale.customer: JobCustomer`, not `Customer`) and by an executable Vitest test that fails the build if a future change ever violates it.

---

_Verified: 2026-05-22T13:23:00Z_
_Verifier: Claude (gsd-verifier)_
