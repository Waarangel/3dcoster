---
phase: 16-printable-pdf-quote
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - src-tauri/Cargo.toml
  - src-tauri/src/main.rs
  - src-tauri/capabilities/default.json
  - scripts/subset-noto-sans.mjs
  - scripts/assert-no-static-jspdf.mjs
  - scripts/assert-no-pdf-preload.mjs
  - src/pdf/notoSansBase64.ts
  - src/pdf/generateQuotePdf.test.ts
  - src/utils/format.test.ts
  - src/utils/taxResolution.test.ts
  - src/components/CostCalculator.test.tsx
autonomous: true
requirements: [PDF-03, PDF-04, PDF-05]
nyquist_compliant: false
must_haves:
  truths:
    - "npm install adds jspdf@^4.2.1 + jspdf-autotable@^5.0.8 + @tauri-apps/plugin-dialog@^2.7.1 + @tauri-apps/plugin-fs@^2.5.1 to package.json"
    - "Tauri dialog + fs plugins are registered in main.rs and capabilities JSON"
    - "scripts/assert-no-static-jspdf.mjs exits 0 today (no static jspdf imports exist yet)"
    - "scripts/assert-no-pdf-preload.mjs runs as part of npm run build"
    - "src/pdf/notoSansBase64.ts contains base64 strings for Noto Sans Regular + Bold (Latin + Latin-Ext + U+20AC)"
    - "Wave 0 test files exist as skeletons with at least one passing scaffold test each"
  artifacts:
    - path: "package.json"
      provides: "Production deps for jspdf, autotable, Tauri plugins; build script wiring for CI gates"
      contains: "jspdf"
    - path: "src-tauri/Cargo.toml"
      provides: "Rust crate dependencies for dialog + fs plugins"
      contains: "tauri-plugin-dialog"
    - path: "src-tauri/src/main.rs"
      provides: ".plugin() calls for dialog + fs"
      contains: "tauri_plugin_dialog"
    - path: "src-tauri/capabilities/default.json"
      provides: "Permissions: dialog:allow-save, fs:allow-write-file, fs:scope $DOWNLOAD"
      contains: "dialog:allow-save"
    - path: "scripts/subset-noto-sans.mjs"
      provides: "One-time font subsetting script (Latin + Latin-Ext + U+20AC)"
      contains: "subset-font"
    - path: "scripts/assert-no-static-jspdf.mjs"
      provides: "CI gate (PDF-03) — scans src/ for static jspdf imports"
      contains: "process.exit(1)"
    - path: "scripts/assert-no-pdf-preload.mjs"
      provides: "CI gate (PDF-04) — scans dist/index.html for modulepreload referencing pdf chunk"
      contains: "modulepreload"
    - path: "src/pdf/notoSansBase64.ts"
      provides: "Base64-encoded Noto Sans subsetted fonts (Regular + Bold)"
      contains: "notoSansRegularBase64"
    - path: "src/pdf/generateQuotePdf.test.ts"
      provides: "Wave 0 integration test scaffold (PDF-02)"
      contains: "describe"
    - path: "src/utils/format.test.ts"
      provides: "Wave 0 unit test scaffold for formatQuoteNumber + customerNameSlug"
      contains: "describe"
    - path: "src/utils/taxResolution.test.ts"
      provides: "Wave 0 unit test scaffold for taxLabelFor (extends existing file)"
      contains: "taxLabelFor"
    - path: "src/components/CostCalculator.test.tsx"
      provides: "Wave 0 component test scaffold for PDF button disabled-state"
      contains: "Generate PDF"
  key_links:
    - from: "package.json build script"
      to: "scripts/assert-no-static-jspdf.mjs"
      via: "npm run build chain"
      pattern: "assert-no-static-jspdf"
    - from: "package.json build script"
      to: "scripts/assert-no-pdf-preload.mjs"
      via: "npm run build chain (post-vite)"
      pattern: "assert-no-pdf-preload"
    - from: "src-tauri/src/main.rs"
      to: "tauri-plugin-dialog + tauri-plugin-fs crates"
      via: ".plugin(...::init())"
      pattern: "tauri_plugin_dialog::init"
---

<objective>
Wave 0 sets up every dependency, CI gate, font asset, and test scaffold that the Phase 16 feature work depends on. Without this plan, plans 02/03/04 cannot install jspdf, cannot run their tests, cannot ship the Tauri save path, and cannot rely on CI to catch lazy-loading regressions.

Purpose: Eliminate "missing dependency" and "missing test infrastructure" blockers BEFORE any feature code lands. Every later plan's verify hooks reference scripts or test files created here.

Output: All dependencies installed, all CI gate scripts committed, font subset committed, all Wave 0 test files scaffolded. After this plan, `nyquist_compliant: true` can be flipped in 16-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-printable-pdf-quote/16-CONTEXT.md
@.planning/phases/16-printable-pdf-quote/16-RESEARCH.md
@.planning/phases/16-printable-pdf-quote/16-PATTERNS.md
@.planning/phases/16-printable-pdf-quote/16-VALIDATION.md
@.planning/phases/11-performance-optimization/11-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From package.json (current build script):
```
"build": "node scripts/lint-no-raw-html.mjs && vitest run --coverage && tsc -b && vite build && node scripts/assert-bundle-size.mjs"
```

From src-tauri/Cargo.toml (current dependencies):
```toml
[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
tauri-plugin-window-state = "2"
```

From src-tauri/src/main.rs (current plugin registration):
```rust
tauri::Builder::default()
    .plugin(tauri_plugin_shell::init())
    .plugin(
        tauri_plugin_window_state::Builder::default()
            .build(),
    )
    .run(tauri::generate_context!())
```

From src-tauri/capabilities/default.json (current permissions):
```json
"permissions": [
  "core:default",
  "shell:default",
  "window-state:default"
]
```

From scripts/assert-bundle-size.mjs (analog pattern — exit codes, logging):
- Reads dist/assets, gzips largest index-*.js, fails if > 300 KB
- Uses console.error + process.exit(1) on failure; console.log + implicit exit 0 on success
- Prefix output with "assert-<name>" for grep-ability

From RESEARCH.md (jspdf-autotable v5 named-function import is required):
```typescript
import { autoTable } from 'jspdf-autotable';  // v5 pattern
autoTable(doc, options);                       // NOT doc.autoTable()
```

From RESEARCH.md (U+20AC escalation):
- Latin + Latin-Ext blocks DO NOT include `€` (U+20AC is in Currency Symbols block)
- The subset script MUST explicitly include U+20AC
- Documented as a justified technical clarification of D-10, not a scope change
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install runtime deps + Tauri plugin (npm + Rust + capabilities)</name>
  <files>
    package.json,
    package-lock.json,
    src-tauri/Cargo.toml,
    src-tauri/Cargo.lock,
    src-tauri/src/main.rs,
    src-tauri/capabilities/default.json
  </files>
  <read_first>
    package.json,
    src-tauri/Cargo.toml,
    src-tauri/src/main.rs,
    src-tauri/capabilities/default.json,
    .planning/phases/16-printable-pdf-quote/16-RESEARCH.md (Pattern 8 — Tauri Save Dialog),
    .planning/phases/16-printable-pdf-quote/16-PATTERNS.md (sections: package.json, Cargo.toml, main.rs, capabilities/default.json)
  </read_first>
  <action>
    Install runtime + dev deps and wire the Tauri plugins (per RESEARCH.md Pattern 8, PATTERNS.md mapping).

    Step 1 — npm install (production deps): run `npm install jspdf@^4.2.1 jspdf-autotable@^5.0.8 @tauri-apps/plugin-dialog@^2.7.1 @tauri-apps/plugin-fs@^2.5.1`. Confirm versions land in `dependencies` (NOT devDependencies). Versions are pinned per RESEARCH.md — CONTEXT.md's "jspdf 2.x / autotable 3.x" assumption is wrong; the current API surface (jspdf 4.2.1 + autotable 5.0.8) is what the rest of Phase 16 targets.

    Step 2 — npm install (dev dep, build-time font subsetting): run `npm install --save-dev subset-font@^2.5.0`. This is used by `scripts/subset-noto-sans.mjs` (Task 3); kept as a devDep so the script remains reproducible.

    Step 3 — Cargo.toml: append two lines after `tauri-plugin-window-state = "2"`:
    `tauri-plugin-dialog = "2"`
    `tauri-plugin-fs = "2"`
    Match the existing bare `"2"` version specifier style.

    Step 4 — main.rs: add two `.plugin()` calls before `.run(tauri::generate_context!())`. Use `::init()` form (no Builder): `.plugin(tauri_plugin_dialog::init())` and `.plugin(tauri_plugin_fs::init())`. Order: place AFTER `.plugin(tauri_plugin_window_state::Builder::default().build())` and BEFORE `.run(...)`.

    Step 5 — capabilities/default.json: append three entries to the `permissions` array (preserve existing order):
    - `"dialog:allow-save"` (string)
    - `"fs:allow-write-file"` (string)
    - `{ "identifier": "fs:scope", "allow": [{ "path": "$DOWNLOAD/*" }] }` (object form)
    The `$DOWNLOAD` variable expands to OS Downloads folder. JSON must remain valid (trailing comma rules, no duplicate keys).

    Do NOT run `cargo build` or `npm run tauri:build` — those happen in plan 05. Just register the deps. Cargo.lock + package-lock.json updates are expected commit artifacts.
  </action>
  <verify>
    <automated>grep -q '"jspdf"' package.json &amp;&amp; grep -q '"jspdf-autotable"' package.json &amp;&amp; grep -q '"@tauri-apps/plugin-dialog"' package.json &amp;&amp; grep -q '"@tauri-apps/plugin-fs"' package.json &amp;&amp; grep -q '"subset-font"' package.json &amp;&amp; grep -q 'tauri-plugin-dialog = "2"' src-tauri/Cargo.toml &amp;&amp; grep -q 'tauri-plugin-fs = "2"' src-tauri/Cargo.toml &amp;&amp; grep -q 'tauri_plugin_dialog::init' src-tauri/src/main.rs &amp;&amp; grep -q 'tauri_plugin_fs::init' src-tauri/src/main.rs &amp;&amp; grep -q 'dialog:allow-save' src-tauri/capabilities/default.json &amp;&amp; grep -q 'fs:allow-write-file' src-tauri/capabilities/default.json &amp;&amp; grep -q 'fs:scope' src-tauri/capabilities/default.json &amp;&amp; node -e "JSON.parse(require('fs').readFileSync('src-tauri/capabilities/default.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/jspdf/package.json` exists with `version` matching `^4.2.1`
    - `node_modules/jspdf-autotable/package.json` exists with `version` matching `^5.0.8`
    - `node_modules/@tauri-apps/plugin-dialog/package.json` exists
    - `node_modules/@tauri-apps/plugin-fs/package.json` exists
    - `node_modules/subset-font/package.json` exists in devDependencies tree
    - `src-tauri/Cargo.toml` contains both `tauri-plugin-dialog = "2"` and `tauri-plugin-fs = "2"` lines
    - `src-tauri/src/main.rs` contains both `tauri_plugin_dialog::init()` and `tauri_plugin_fs::init()` plugin calls
    - `src-tauri/capabilities/default.json` is valid JSON containing `dialog:allow-save`, `fs:allow-write-file`, and the object-form `fs:scope` entry
  </acceptance_criteria>
  <done>All dependencies are registered at both the npm and Rust layers; capability permissions JSON is valid; nothing yet imports jspdf in src/.</done>
</task>

<task type="auto">
  <name>Task 2: Author CI gate scripts (assert-no-static-jspdf + assert-no-pdf-preload) and wire build chain</name>
  <files>
    scripts/assert-no-static-jspdf.mjs,
    scripts/assert-no-pdf-preload.mjs,
    package.json
  </files>
  <read_first>
    scripts/assert-bundle-size.mjs,
    package.json,
    .planning/phases/16-printable-pdf-quote/16-RESEARCH.md (Pattern 6 + Pattern 7),
    .planning/phases/16-printable-pdf-quote/16-PATTERNS.md (sections: scripts/assert-no-static-jspdf.mjs, scripts/assert-no-pdf-preload.mjs, package.json build script)
  </read_first>
  <action>
    Author two new Node ESM CI gate scripts and wire them into `npm run build`. Both follow the exit-code + console.error/console.log conventions established by `scripts/assert-bundle-size.mjs` (PATTERNS.md "CI Gate Script Exit Codes").

    Script A — `scripts/assert-no-static-jspdf.mjs` (PDF-03 enforcement):
    - Use only Node built-ins (`fs`, `path`); no external deps
    - Recursively walk `src/` looking for `.ts` or `.tsx` files
    - For each file, read content and test against regex: `/from ['"]jspdf['"]|from ['"]jspdf-autotable['"]|import ['"]jspdf/`
    - If any match found, print `assert-no-static-jspdf FAILED: static jspdf import detected in:` then list each violating file path; remediation hint: `Use dynamic import(): await import('../pdf/generateQuotePdf') instead.`; exit 1
    - On clean scan, print `✓ no static jspdf imports found in src/` and exit 0 (implicit)
    - Header comment: explain this is a Phase 16 PDF-03 enforcement gate, runs BEFORE vitest/tsc/vite (cheapest check first)

    Script B — `scripts/assert-no-pdf-preload.mjs` (PDF-04 enforcement):
    - Use only Node built-ins (`fs`)
    - Read `dist/index.html` (post-build artifact)
    - If file missing, print `assert-no-pdf-preload: dist/index.html not found. Did vite build run?` and exit 1
    - Match against regex: `/modulepreload[^>]*href="[^"]*pdf[^"]*"/i`
    - If match found, print `assert-no-pdf-preload FAILED: dist/index.html contains a modulepreload link for the pdf chunk.` with remediation hints (per RESEARCH.md Pattern 6); exit 1
    - On clean, print `✓ pdf chunk: no modulepreload link in dist/index.html` and exit 0
    - Header comment: explain this is Phase 16 PDF-04 enforcement, runs AFTER vite build (needs dist/index.html)

    package.json — update the `build` script. New chain (per PATTERNS.md package.json adaptation):
    `"build": "node scripts/lint-no-raw-html.mjs && node scripts/assert-no-static-jspdf.mjs && vitest run --coverage && tsc -b && vite build && node scripts/assert-bundle-size.mjs && node scripts/assert-no-pdf-preload.mjs"`
    Order rationale: `assert-no-static-jspdf` is a cheap source-tree scan — runs before expensive vitest/tsc/vite. `assert-no-pdf-preload` needs `dist/index.html` so runs LAST.

    Do NOT add `assert-bundle-size.mjs` twice or change its position. Do NOT alter `lint-no-raw-html.mjs` placement.
  </action>
  <verify>
    <automated>node scripts/assert-no-static-jspdf.mjs &amp;&amp; ls dist/index.html 2&gt;/dev/null &amp;&amp; node scripts/assert-no-pdf-preload.mjs || node scripts/assert-no-pdf-preload.mjs 2&gt;&amp;1 | grep -q "dist/index.html not found"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/assert-no-static-jspdf.mjs` exits 0 today (no static jspdf imports exist yet) and prints `✓ no static jspdf imports found in src/`
    - `node scripts/assert-no-pdf-preload.mjs` exits 1 with "dist/index.html not found" if no build has run; exits 0 with `✓ pdf chunk: no modulepreload link in dist/index.html` after a successful build with `modulePreload: false` (this is what plan 04 will achieve)
    - `package.json` build script contains exactly: `lint-no-raw-html.mjs`, `assert-no-static-jspdf.mjs`, `vitest run --coverage`, `tsc -b`, `vite build`, `assert-bundle-size.mjs`, `assert-no-pdf-preload.mjs` (in that order)
    - Scripts use only Node built-ins (no `import` from external npm package)
  </acceptance_criteria>
  <done>Both CI gate scripts exist, exit codes follow Phase 11 conventions, and the `npm run build` chain runs them in the correct order. Today the static-import gate passes (no jspdf imports yet); the preload gate fails until plan 04 ships `modulePreload: false`.</done>
</task>

<task type="auto">
  <name>Task 3: Author font subset script + generate notoSansBase64.ts (Noto Sans Regular + Bold, Latin + Latin-Ext + U+20AC)</name>
  <files>
    scripts/subset-noto-sans.mjs,
    src/pdf/notoSansBase64.ts
  </files>
  <read_first>
    scripts/assert-bundle-size.mjs,
    .planning/phases/16-printable-pdf-quote/16-RESEARCH.md (Pattern 4 — Font Subsetting),
    .planning/phases/16-printable-pdf-quote/16-RESEARCH.md (GLYPH COVERAGE VERIFICATION section — U+20AC escalation),
    .planning/phases/16-printable-pdf-quote/16-PATTERNS.md (sections: src/pdf/notoSansBase64.ts, scripts/subset-noto-sans.mjs)
  </read_first>
  <action>
    Author the one-shot subsetting script and run it to produce the committed base64 module. Both files are committed; the script is reproducible and the output is what production uses.

    Step 1 — `scripts/subset-noto-sans.mjs` (Node ESM, runs locally — NOT part of the build chain):
    - Import `subset-font` (devDependency from Task 1)
    - Fetch or load Noto Sans Regular + Bold TTF files. Use Google Fonts download URLs OR include the TTF files in a `scripts/fonts/` directory (preferred for reproducibility; commit the .ttf source files alongside the script). Source: `https://fonts.google.com/noto/specimen/Noto+Sans` (download "Noto Sans Regular" + "Noto Sans Bold" — the unhinted `.ttf` form, NOT variable font)
    - Subset character set MUST include ALL of:
      - Basic Latin (U+0020 to U+007E — printable ASCII)
      - Latin-1 Supplement (U+00A0 to U+00FF — accented Latin like é à ñ ß ç ö ü)
      - Latin Extended-A (U+0100 to U+017F — Polish ą ę ć ł ś ź ż, Czech ň ř, Hungarian ő, Turkish İ ı ş ğ)
      - Latin Extended-B (U+0180 to U+024F)
      - **U+20AC (Euro sign) — MUST be added explicitly** per RESEARCH.md GLYPH COVERAGE VERIFICATION section. `€` is NOT in Latin or Latin-Ext blocks; without explicit inclusion, EUR currency strings render as tofu. This is a justified technical clarification of D-10, not a scope change.
    - Use `subset-font` API: `subsetFont(ttfBuffer, characters, { targetFormat: 'truetype' })` returns a `Buffer`
    - For each subsetted buffer, base64-encode (`buffer.toString('base64')`) and prepend the `data:font/ttf;base64,` URI prefix (REQUIRED by jspdf `addFileToVFS` per Pattern 3) — OR confirm whether jspdf needs raw base64 vs full data URI. Cross-check with RESEARCH.md Pattern 3: jspdf's `addFileToVFS` expects raw base64-encoded TTF bytes (string), NOT a data URI prefix. Use raw base64.
    - Write `src/pdf/notoSansBase64.ts` with two named exports: `notoSansRegularBase64: string` and `notoSansBoldBase64: string`. Each is a multi-line backticked template literal or a regular quoted string — readability over line length. Header comment: `// AUTO-GENERATED by scripts/subset-noto-sans.mjs — do not edit manually.` + `// Noto Sans Regular + Bold, Latin + Latin-Ext + Euro sign (U+20AC) subset.` + `// Regenerate: node scripts/subset-noto-sans.mjs`

    Step 2 — Run the script: `node scripts/subset-noto-sans.mjs`. Output `src/pdf/notoSansBase64.ts` is committed.

    Step 3 — Sanity check on output: `notoSansRegularBase64` and `notoSansBoldBase64` are non-empty strings. Combined raw size budget (per RESEARCH.md): ~70–100 KB gz. After base64 expansion in the .ts file, raw text is ~150–200 KB (base64 inflates by 4/3). When Vite bundles the .ts file, gz brings it back to ~70–100 KB inside the lazy pdf chunk.

    If `subset-font` API differs in detail from what RESEARCH.md describes, the AUTHORITATIVE source is the installed package's README at `node_modules/subset-font/README.md` — read it from the installed module.
  </action>
  <verify>
    <automated>test -f scripts/subset-noto-sans.mjs &amp;&amp; test -f src/pdf/notoSansBase64.ts &amp;&amp; grep -q "notoSansRegularBase64" src/pdf/notoSansBase64.ts &amp;&amp; grep -q "notoSansBoldBase64" src/pdf/notoSansBase64.ts &amp;&amp; grep -q "U+20AC\|0x20AC\|20AC\|Euro" scripts/subset-noto-sans.mjs &amp;&amp; node -e "import('./src/pdf/notoSansBase64.ts').catch(()=>require('fs').readFileSync('src/pdf/notoSansBase64.ts','utf8')).then(()=>0,(e)=&gt;{const s=require('fs').readFileSync('src/pdf/notoSansBase64.ts','utf8');if(!s.includes('notoSansRegularBase64'))process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/subset-noto-sans.mjs` exists and is valid Node ESM (importable, doesn't throw on parse)
    - The script's source code explicitly references U+20AC (Euro sign) inclusion — comment AND character set
    - `src/pdf/notoSansBase64.ts` exists with two non-empty named exports: `notoSansRegularBase64` and `notoSansBoldBase64`
    - Both exports are typed as `string` (not `Buffer` or `Uint8Array`) — jspdf's `addFileToVFS` accepts string base64
    - Total raw size of `notoSansBase64.ts` is between 100 KB and 250 KB (base64-expanded, before gzip); if &gt; 300 KB, the subset failed to filter properly
    - `node scripts/subset-noto-sans.mjs` is idempotent — running it twice produces byte-identical output
    - Header comment explicitly says "AUTO-GENERATED" and "Latin + Latin-Ext + Euro sign (U+20AC)"
  </acceptance_criteria>
  <done>`scripts/subset-noto-sans.mjs` is committed and reproducible; `src/pdf/notoSansBase64.ts` contains Latin + Latin-Ext + € subsetted base64 fonts ready for jspdf's VFS API.</done>
</task>

<task type="auto">
  <name>Task 4: Author Wave 0 test scaffolds (4 test files — must fail-cleanly before features ship)</name>
  <files>
    src/pdf/generateQuotePdf.test.ts,
    src/utils/format.test.ts,
    src/utils/taxResolution.test.ts,
    src/components/CostCalculator.test.tsx
  </files>
  <read_first>
    src/utils/costCalc.test.ts,
    src/utils/taxResolution.test.ts,
    src/utils/byValueSnapshot.test.ts,
    .planning/phases/16-printable-pdf-quote/16-VALIDATION.md (Per-Task Verification Map),
    .planning/phases/16-printable-pdf-quote/16-PATTERNS.md (sections: src/pdf/generateQuotePdf.test.ts, src/utils/format.test.ts, src/utils/taxResolution.test.ts, src/components/CostCalculator.test.tsx)
  </read_first>
  <action>
    Author four Wave 0 test files. Each MUST be runnable today (`vitest run &lt;path&gt;` exits 0) but tests assert real behavior that will exist after plans 02/03/04 ship. Until those plans ship, use `it.todo` to scaffold the un-implementable cases so the file compiles and runs.

    File A — `src/pdf/generateQuotePdf.test.ts` (PDF-02 integration coverage):
    - Use vitest `describe` + `it` + `expect` (analog: `src/utils/costCalc.test.ts` lines 1–12)
    - Wrap all assertions in a top-level `describe('generateQuotePdf', ...)` block
    - Add `it.todo('output starts with %PDF- magic bytes')` — flips to real test in plan 03
    - Add `it.todo('PDF contains the formatted quote number (e.g. Q-0042)')`
    - Add `it.todo('PDF includes the customer block when sale.customer is provided')`
    - Add `it.todo('PDF omits the customer block when no customer is provided')`
    - Add `it.todo('PDF shows Tax row when taxRate > 0 and hides it when taxRate === 0')`
    - Add `it.todo('PDF Notes/Terms section omits when both PrintJob.notes and UserProfile.defaultTerms are empty')`
    - Add at least one scaffold helper: `function makeJob(overrides: Partial&lt;PrintJob&gt; = {}): PrintJob { ... }` returning a minimal valid PrintJob. Same pattern for `makeUserProfile(overrides)`. (Same fixture style as `src/utils/costCalc.test.ts` `makePrinter`)
    - File MUST compile under `tsc -b` today: import `PrintJob` and `UserProfile` types only; do NOT import from `'../pdf/generateQuotePdf'` yet (that file doesn't exist). Cast fixtures as `as PrintJob` to satisfy TS without all required fields.

    File B — `src/utils/format.test.ts` (NEW FILE — formatQuoteNumber + customerNameSlug coverage):
    - vitest describe/it/expect
    - `describe('formatQuoteNumber', ...)` block with `it.todo` placeholders: `formatQuoteNumber(1) → 'Q-0001'`, `formatQuoteNumber(42) → 'Q-0042'`, `formatQuoteNumber(1000) → 'Q-1000'`, `formatQuoteNumber(10000) → 'Q-10000'` (acceptable overflow per RESEARCH.md Pattern 10)
    - `describe('customerNameSlug', ...)` block with `it.todo` placeholders: empty/undefined → `''`, `'Alice Test' → 'alice-test'`, `'Ça va' → 'a-va'` (strips non-ASCII), 31-char name → 30-char slug max
    - Today: imports from `'../utils/format'` — that module doesn't exist yet, so use `// @ts-expect-error - implemented in plan 02` above the import, OR use only `it.todo` (no imports). The simpler approach: use only `it.todo` with descriptive strings; add real imports + `it(...)` calls in plan 02.

    File C — `src/utils/taxResolution.test.ts` (EXTEND existing file — taxLabelFor coverage):
    - Read the current file first; append a new `describe('taxLabelFor', ...)` block AFTER the existing describes (do NOT remove existing tests)
    - Do NOT import `taxLabelFor` yet — the function doesn't exist yet. Add `it.todo` placeholders:
      - `it.todo('EU code (DE, FR, IT, NL, ES, ...) → VAT')`
      - `it.todo('GB → VAT')`
      - `it.todo('AU, NZ, IN, CA → GST')`
      - `it.todo('ES, MX → IVA')` (note: ES is BOTH in EU set and IVA list — EU check fires first per RESEARCH.md Pattern 9; the planner should document this in test name)
      - `it.todo('US → Sales Tax')`
      - `it.todo('undefined / unknown code → Tax')`
    - When plan 02 adds `taxLabelFor` to `src/utils/taxResolution.ts`, plan 02 will flip these to real assertions

    File D — `src/components/CostCalculator.test.tsx` (NEW FILE — Generate PDF button disabled-state):
    - Use vitest + `@testing-library/react` IF installed; if not, use a lighter approach: import the component, render it, query for the button via text content
    - Check first: `grep -c "@testing-library/react" package.json`. If 0, do NOT add the dep here — write the test using a minimal jsdom approach (render via `createRoot`, query via `document.querySelector`)
    - For now, scaffold with `it.todo('Generate PDF button is disabled when sellingPrice <= 0')` and `it.todo('Generate PDF button is enabled when editing a saved job with sellingPrice > 0')`. Plan 04 (UI wiring) will activate these.
    - The file MUST compile and run under `vitest run` today (skipped-as-todo tests count as passing)

    Constraint for ALL four files: Running `vitest run src/pdf/generateQuotePdf.test.ts src/utils/format.test.ts src/utils/taxResolution.test.ts src/components/CostCalculator.test.tsx` MUST exit 0 (all tests are either real-passing or it.todo skipped). No red. No type errors under `tsc -b`.
  </action>
  <verify>
    <automated>vitest run src/pdf/generateQuotePdf.test.ts src/utils/format.test.ts src/utils/taxResolution.test.ts src/components/CostCalculator.test.tsx 2>&amp;1 | tail -10 &amp;&amp; tsc -b --noEmit 2&gt;&amp;1 | (grep -E "error TS" || true) | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/pdf/generateQuotePdf.test.ts` exists with `describe('generateQuotePdf', ...)` block and at least 6 `it.todo(...)` entries
    - `src/utils/format.test.ts` exists with `describe('formatQuoteNumber', ...)` and `describe('customerNameSlug', ...)` blocks
    - `src/utils/taxResolution.test.ts` — existing describes preserved; new `describe('taxLabelFor', ...)` block appended with 6 `it.todo` entries
    - `src/components/CostCalculator.test.tsx` exists with 2 `it.todo` entries for Generate PDF disabled-state
    - `vitest run` on all four files exits 0 (todo tests count as passing/skipped)
    - `tsc -b` produces NO new errors caused by these files
    - The four files each contain the string `"todo"` (proves placeholders are in place)
  </acceptance_criteria>
  <done>All four Wave 0 test scaffolds exist, compile, run green (todo-skipped is green), and reserve the test names that plans 02/03/04 will flip to real assertions. Set `16-VALIDATION.md` frontmatter `nyquist_compliant: true` and `wave_0_complete: true` as the final write of this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → npm registry | npm packages (jspdf, autotable, Tauri plugins, subset-font) cross this boundary on install |
| user file system → Tauri capability scope | `fs:scope` defines what paths writeFile is permitted to touch |
| browser → jspdf VFS | Font base64 strings registered via `addFileToVFS` into jspdf's in-memory VFS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | npm dependency installs (jspdf, autotable, Tauri plugins, subset-font) | mitigate | Package Legitimacy Audit in 16-RESEARCH.md verified all 5 packages as Approved [OK]. Versions pinned with `^` (caret) to allow patches but not majors. `npm audit` runs as part of CI for new vulns (existing chain). |
| T-16-SC | Tampering | npm postinstall scripts | accept | Research verified no postinstall scripts on jspdf/autotable performing network calls. Tauri plugins and subset-font are official packages. No `[ASSUMED]/[SUS]` packages requiring blocking human checkpoint. |
| T-16-02 | Tampering | Tauri fs writeFile path arbitrary write | mitigate | `fs:scope` capability restricts to `$DOWNLOAD/*`. Dialog-selected paths get implicit per-path grant (Tauri 2 documented). If user picks outside Downloads, Tauri shows permission denial — fail-closed. |
| T-16-03 | Information Disclosure | PDF chunk inadvertently bundled into main chunk via static import | mitigate | `scripts/assert-no-static-jspdf.mjs` CI gate (created in this plan) fails the build if any `from 'jspdf'` appears in `src/**`. |
</threat_model>

<verification>
After all four tasks complete:
1. `git status` shows: modified package.json + package-lock.json + Cargo.toml + Cargo.lock + main.rs + capabilities/default.json + 16-VALIDATION.md; new scripts/{subset-noto-sans,assert-no-static-jspdf,assert-no-pdf-preload}.mjs; new src/pdf/notoSansBase64.ts; new src/pdf/generateQuotePdf.test.ts + src/utils/format.test.ts + src/components/CostCalculator.test.tsx; modified src/utils/taxResolution.test.ts
2. `vitest run` exits 0 (Wave 0 tests are scaffolded with todos; existing tests still pass)
3. `tsc -b` exits 0 (no type errors)
4. `node scripts/assert-no-static-jspdf.mjs` exits 0 with `✓` (no jspdf imports yet)
5. `node scripts/subset-noto-sans.mjs` runs without error and produces byte-identical `src/pdf/notoSansBase64.ts` (idempotent)
6. 16-VALIDATION.md frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`
</verification>

<success_criteria>
- All 4 production deps (jspdf, jspdf-autotable, @tauri-apps/plugin-dialog, @tauri-apps/plugin-fs) installed at pinned versions
- subset-font installed as devDependency
- Tauri plugins wired at npm + Cargo + main.rs + capabilities layers
- 2 CI gate scripts authored, both pass with current empty src/ (static-jspdf clean; preload gate fails-clean if no build, passes after future build)
- 1 font subset script + 1 generated base64 module committed; subset includes Latin + Latin-Ext + U+20AC
- 4 Wave 0 test scaffolds exist; `vitest run` exits 0; `tsc -b` exits 0
- `npm run build` chain references both new CI scripts in correct order
- 16-VALIDATION.md updated: `nyquist_compliant: true`, `wave_0_complete: true`
</success_criteria>

<output>
Create `.planning/phases/16-printable-pdf-quote/16-01-SUMMARY.md` when done. Document: package.json deps added, scripts created, font subset character set used, Wave 0 test scaffolds with todo count per file, any deviations from RESEARCH.md (e.g., if subset-font API differed from researcher's reading).
</output>
