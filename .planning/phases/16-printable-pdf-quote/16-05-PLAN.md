---
phase: 16-printable-pdf-quote
plan: 05
type: execute
wave: 4
depends_on: ["16-01", "16-02", "16-03", "16-04"]
files_modified:
  - .planning/phases/16-printable-pdf-quote/16-VERIFICATION.md
autonomous: false
requirements: [PDF-01, PDF-02, PDF-03, PDF-04, PDF-05]
nyquist_compliant: true
must_haves:
  truths:
    - "Web UAT: clicking Generate PDF on a saved job in the cost calculator downloads a PDF that opens cleanly in Preview/Adobe and visually matches CONTEXT.md D-01..D-12 (header, customer block, line items, totals, footer, Q-NNNN, valid-until)"
    - "Tauri UAT: npm run tauri:dev launches; clicking Generate PDF in JobsManager triggers a native save dialog; saving writes a PDF that opens cleanly"
    - "Unicode UAT: PDF generated for a job whose customer name contains 'François Müller' and price in €120 renders all glyphs correctly with no tofu/rectangle placeholders"
    - "0% tax UAT: PDF generated for a job with taxRate=0 hides the Tax row; the totals block shows only Subtotal and Total"
    - "EU tax UAT: PDF generated for a German user (country='DE') with taxRate=19 shows 'VAT (19%): €X' between Subtotal and Total"
    - "Notes/Terms UAT: setting UserProfile.defaultTerms = 'Payment due 14d' and leaving PrintJob.notes empty produces a PDF with only the Terms subsection — and clearing both produces a PDF with NO Notes/Terms area"
    - "NewBadge UAT: the absolute-overlay NewBadge on the Generate PDF buttons (CostCalculator + JobsManager) does NOT push/wrap/shrink/reflow surrounding siblings on either desktop or narrow-mobile widths"
    - "Bundle UAT: dist/assets/ contains a pdf-*.js chunk; main index-*.js is < 300 KB gz; dist/index.html contains no modulepreload referencing pdf chunk"
    - "16-VERIFICATION.md records the per-requirement PASS/FAIL status and the human-verification approvals"
  artifacts:
    - path: ".planning/phases/16-printable-pdf-quote/16-VERIFICATION.md"
      provides: "Phase verification record: per-requirement status, UAT results, build gate outputs"
      contains: "PDF-01"
  key_links:
    - from: "16-VERIFICATION.md"
      to: "Phase 16 closure"
      via: "human-verify checkpoint approval"
      pattern: "approved"
---

<objective>
Wave 4 runs end-to-end UAT — every requirement PDF-01..PDF-05 verified, every CONTEXT.md D-01..D-12 visually inspected, NewBadge non-regression, and the build gate chain confirmed green. After this plan, Phase 16 is shippable: the user signs off, the executor writes 16-VERIFICATION.md, and `/gsd:verify-work` can close the phase.

Purpose: Surface visual regressions (NewBadge layout breakage, glyph tofu, Notes/Terms gap when empty, 0%-tax row leaking) BEFORE the user is asked to merge. Plans 01-04 cover code correctness; this plan covers user-perceived correctness.

Output: 16-VERIFICATION.md with per-requirement evidence + UAT pass status. Phase closure unblocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-printable-pdf-quote/16-CONTEXT.md
@.planning/phases/16-printable-pdf-quote/16-VALIDATION.md
@.planning/phases/16-printable-pdf-quote/16-01-SUMMARY.md
@.planning/phases/16-printable-pdf-quote/16-02-SUMMARY.md
@.planning/phases/16-printable-pdf-quote/16-03-SUMMARY.md
@.planning/phases/16-printable-pdf-quote/16-04-SUMMARY.md
@.planning/phases/14-customer-details-etsy-helper/14-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification chain (npm run build + vitest full suite + grep audits) and record evidence</name>
  <files>
    .planning/phases/16-printable-pdf-quote/16-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/14-customer-details-etsy-helper/14-VERIFICATION.md,
    .planning/phases/16-printable-pdf-quote/16-VALIDATION.md,
    .planning/phases/16-printable-pdf-quote/16-01-SUMMARY.md,
    .planning/phases/16-printable-pdf-quote/16-02-SUMMARY.md,
    .planning/phases/16-printable-pdf-quote/16-03-SUMMARY.md,
    .planning/phases/16-printable-pdf-quote/16-04-SUMMARY.md
  </read_first>
  <action>
    Run the full automated verification chain and capture exact outputs (commands + last 20 lines of output) for each step. Write these into a partial 16-VERIFICATION.md draft. Task 2 (human checkpoint) will add UAT sign-off.

    Chain to run:
    1. `npm run build` — capture exit code, last 30 lines of output; verify ALL gates pass (lint-no-raw-html, assert-no-static-jspdf, vitest run --coverage, tsc -b, vite build, assert-bundle-size, assert-no-pdf-preload)
    2. `ls -lh dist/assets/` — list chunk filenames + sizes; confirm `pdf-*.js` exists; confirm `index-*.js` is &lt; 300 KB (check raw size; gz happens in assert-bundle-size already)
    3. `grep -c "^\s*'[a-z-]+': new Date(" src/features.ts` — confirm count is 8 (7 prior + pdf-quote)
    4. `grep -rn "<NewBadge feature=\"pdf-quote\"" src/` — confirm exactly 2 JSX consumers (CostCalculator + JobsManager)
    5. `grep -rn "from 'jspdf'" src/` — confirm exactly 1 match (src/pdf/generateQuotePdf.ts only)
    6. `grep -rn "from 'jspdf-autotable'" src/` — confirm exactly 1 match (same file)
    7. `grep -rn "await import('../pdf/generateQuotePdf')" src/` — confirm exactly 2 matches (CostCalculator + JobsManager)
    8. `grep -rn "doc.html\|doc.autoTable" src/pdf/` — confirm 0 matches (no v3 antipatterns, no HTML injection vector)
    9. `vitest run` — full suite exit code + summary line ("Test Files X passed | Y total")

    Initialize 16-VERIFICATION.md with frontmatter mirroring 14-VERIFICATION.md:
    ```
    ---
    phase: 16-printable-pdf-quote
    verified: <ISO timestamp>
    status: in-progress
    score: 0/5 — automated chain green; awaiting human UAT
    verified_by: automated + human-uat-pending
    gaps: []
    deferred: []
    ---
    ```

    Body sections (fill in from captured outputs):
    - "## Automated Verification Chain" — table mapping each command to exit code + key output line
    - "## Requirement Status" — preliminary table for PDF-01..PDF-05 with "automated PASS / awaiting UAT" entries
    - "## Static Audits" — the grep-count audits (#3-8 above) each as a PASS/FAIL row
    - "## Bundle Size" — exact dist/assets/ contents + gz sizes from assert-bundle-size output
    - "## Pending Human UAT" — list of UAT scenarios (Task 2 will check these off)

    Do NOT mark status=passed yet; that flips in Task 2 after human verifies the UAT scenarios.

    If ANY automated step FAILS, do NOT proceed to Task 2. Surface the failure: write the failing output into 16-VERIFICATION.md, set status to "blocked", and emit a clear message: "❌ {step} failed — gap-closure plan required before UAT." The orchestrator handles re-routing.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -20 &amp;&amp; vitest run 2&gt;&amp;1 | tail -5 &amp;&amp; test -f .planning/phases/16-printable-pdf-quote/16-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with all gates passing (lint, assert-no-static-jspdf, vitest, tsc, vite, assert-bundle-size, assert-no-pdf-preload)
    - `vitest run` exits 0
    - All 7 grep audits return the expected counts (1, 2, 1, 1, 2, 0, 8)
    - `dist/assets/` contains a `pdf-*.js` chunk
    - `dist/assets/index-*.js` is &lt; 300 KB gzipped (printed by assert-bundle-size)
    - `dist/index.html` does NOT contain `modulepreload` href for pdf chunk
    - 16-VERIFICATION.md exists with frontmatter + populated automated chain section + preliminary requirement table
  </acceptance_criteria>
  <done>Automated evidence collected. 16-VERIFICATION.md exists as a draft with the automated chain section filled in; ready for the human UAT in Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — web PDF + Tauri PDF + Unicode + 0% tax + EU tax + Notes/Terms + NewBadge layout</name>
  <files>
    .planning/phases/16-printable-pdf-quote/16-VERIFICATION.md (updated with UAT scenario results after sign-off)
  </files>
  <read_first>
    .planning/phases/16-printable-pdf-quote/16-VERIFICATION.md (draft from Task 1),
    .planning/phases/16-printable-pdf-quote/16-CONTEXT.md (D-01..D-12 — the visual contract being verified),
    .planning/phases/16-printable-pdf-quote/16-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <action>
    Run the human UAT scenarios listed in `<how-to-verify>` below. This task is BLOCKING — execution pauses here until the user types "approved" or a failure description. Do NOT proceed to Task 3 without explicit user input on the resume-signal.

    When user signals approval, append the per-scenario PASS markers + user's brief approval into 16-VERIFICATION.md under a new `## UAT Scenario Results` section. Do NOT flip status=passed in this task; Task 3 does that finalization step.

    When user signals a failure: append the failure detail into 16-VERIFICATION.md under `## UAT Failures (Gap List)`, set frontmatter `status: blocked`, and stop. The orchestrator handles gap-closure planning.
  </action>
  <verify>
    <human-check>User types "approved" or a failure description on the resume-signal prompt; the response is captured into 16-VERIFICATION.md</human-check>
  </verify>
  <acceptance_criteria>
    - User has explicitly responded to the resume-signal (no silent advance)
    - If approved: 16-VERIFICATION.md `## UAT Scenario Results` section is populated with PASS markers for scenarios 1-7
    - If failed: 16-VERIFICATION.md `## UAT Failures (Gap List)` section is populated and frontmatter status="blocked"
    - The user's verbatim response is captured in 16-VERIFICATION.md (audit trail)
  </acceptance_criteria>
  <done>Human UAT is signed off (or explicitly blocked). 16-VERIFICATION.md records the outcome verbatim.</done>
  <what-built>
    Phase 16 plans 01-04 are complete. The Generate PDF buttons exist in both surfaces (CostCalculator + JobsManager); UserProfileModal exposes Default Quote Terms; vite.config.ts locks the lazy-chunk contract; CI gates enforce all invariants; the automated verification chain in Task 1 is green.

    What the user needs to verify with their own eyes:
    1. The PDF actually looks correct (D-01 print-shop aesthetic, D-02 header, D-03 invoice section order, D-04 collapsed line items)
    2. The Tauri desktop save flow works (native dialog → file written → file opens)
    3. Unicode (€, accented Latin, Latin-Ext) renders without tofu
    4. Tax row hidden at 0%, shown with region-aware label otherwise
    5. Notes/Terms layered model behaves correctly across the 4 combinations (neither / notes-only / terms-only / both)
    6. NewBadge overlay does not break button layout on desktop OR narrow-mobile widths in both CostCalculator and JobsManager
  </what-built>
  <how-to-verify>
    Run through these UAT scenarios in order. Mark each PASS / FAIL with a short note. After ALL scenarios complete, the executor updates 16-VERIFICATION.md with the results and the human signs off.

    PRE-UAT: kill any running dev servers (`pkill -f "vite\|tauri\|node.*4173"` or similar), then start fresh:
    - Terminal 1 (web): `npm run dev` → opens at http://localhost:4173
    - Terminal 2 (Tauri, run AFTER web tests succeed): `npm run tauri:dev`

    SCENARIO 1 — Web PDF, no customer, no tax:
    1. Open http://localhost:4173 in browser
    2. Create or edit a saved job (Print name = "Test Phone Stand"); set Selling Price = 24.00; save the job
    3. Re-open the saved job (the cost calc must show editingJob context — sellingPrice > 0)
    4. Click "Generate PDF"
    5. Expected: browser save dialog opens, default name `Quote-Q-0001.pdf` (or whatever the user's nextQuoteNumber currently is); save it
    6. Open the PDF in Preview / Adobe Reader
    7. Verify visually: 3DCoster header top-left + QUOTE + Q-NNNN top-right; thin horizontal rule below header; "Quote #:", "Issue date:", "Valid until: (today + 30 days)" in left column under header; right column under header is BLANK (no customer); "Custom 3D print — Test Phone Stand (qty: 1)" single line item with €24.00 unit price + amount; Subtotal: €24.00; NO Tax row (taxRate=0); Total: €24.00 (bold); no Notes/Terms section; footer "Made with 3DCoster — 3dcoster.vercel.app" in subtle gray
    8. PASS criteria: layout matches D-01 (B&W minimal), D-02 (wordmark header), D-03 (section order), D-04 (collapsed row), D-07 (0% tax row hidden)

    SCENARIO 2 — Web PDF with customer + EU tax + Notes/Terms:
    1. In UserProfileModal: set Country = "DE" (Germany), set Default Quote Terms = "Payment due within 14 days. All sales final."
    2. In CostCalculator: edit a saved job (price = 119.00); set per-job Tax Rate = 19; set Notes = "PLA, gloss finish"; save
    3. Open JobsManager → expand the job in the accordion
    4. Record a sale on this job with Customer Name = "François Müller", Email = "francois@example.de", Company = "Müller Druck GmbH", Address = "Hauptstraße 12\n10115 Berlin\nGermany"
    5. From the expanded accordion, click "Generate PDF" (the one between Record Sale and Edit)
    6. Expected: browser saves `Quote-Q-NNNN-francois-mller.pdf` (or similar — non-ASCII stripped from slug)
    7. Open PDF
    8. Verify: customer block in right column under header shows "Bill To:" + Name + Email + Company + Address; line item still single-row but at €119.00; Subtotal €119.00; "VAT (19%): €22.61" between Subtotal and Total; Total €141.61 in bold; Notes subsection with "PLA, gloss finish"; Terms subsection with "Payment due within 14 days. All sales final."; footer correct
    9. Unicode check: ü, ç, € all render correctly with no rectangle/tofu placeholders
    10. PASS criteria: D-06 region-aware label (VAT), D-07 tax row shown with rate, D-08 layered Notes + Terms, D-10 Unicode coverage, D-11 filename with slug

    SCENARIO 3 — Web PDF, terms only (no notes):
    1. Clear Notes field on the same saved job (empty string); save
    2. Generate PDF from the cost calc (same job, no sale → no customer block)
    3. Verify: PDF Notes subsection is OMITTED; Terms subsection still present with the saved defaultTerms
    4. PASS criteria: D-08 independent subsection omission

    SCENARIO 4 — Web PDF, neither notes nor terms:
    1. Clear UserProfile.defaultTerms (empty string); save profile
    2. Notes is still empty
    3. Generate PDF
    4. Verify: PDF has NO Notes/Terms area entirely; the footer reflows up to fill the space; no awkward empty gap between line items/totals and footer
    5. PASS criteria: D-08 whole-area omission

    SCENARIO 5 — Tauri desktop PDF + native save dialog:
    1. Stop the web dev server. Run `npm run tauri:dev`. Wait for the desktop app to launch.
    2. In the desktop app: navigate to a saved job (same data as Scenarios 1-4)
    3. Click Generate PDF in JobsManager expanded accordion
    4. Expected: NATIVE OS save dialog appears (NOT a browser-style dialog); default filename matches `Quote-Q-NNNN[-slug].pdf`; pick Downloads as the destination
    5. Confirm the file is written to Downloads
    6. Open the saved file in Preview / Adobe Reader; verify same content as web
    7. Cancel test: click Generate PDF again; in the save dialog, press Cancel/Escape; verify NO error appears in the app, no file is written
    8. PASS criteria: D-12 Tauri save plumbing, RESEARCH.md Pattern 8 cancel-returns-null semantic

    SCENARIO 6 — NewBadge non-regression:
    1. In a fresh browser (incognito if needed) — `localStorage.clear()` first to reset the NewBadge "seen" tracking
    2. Verify CostCalculator: the Generate PDF button has a small "New" badge in the top-right corner. The badge is an absolute overlay — siblings (Save Job button, Cancel Edit if shown) do NOT shift, wrap, or shrink
    3. Verify JobsManager: expand a saved job; the Generate PDF button between Record Sale and Edit has the same "New" badge overlay. On a narrow-mobile width (resize browser to 375px), all 4 buttons (Record Sale, Generate PDF, Edit, Delete) still fit in their row OR wrap cleanly — no overlap, no badge pushing siblings
    4. PASS criteria: project memory NEW Badge rule (absolute overlay only, no inline)

    SCENARIO 7 — Quote number lifecycle:
    1. On a job that has NEVER had a PDF generated: note the current `UserProfile.nextQuoteNumber` value (open IndexedDB via DevTools or use any existing UI surface that shows it)
    2. Generate PDF for that job. The PDF should display Q-NNNN where NNNN = the prior nextQuoteNumber padded to 4 digits
    3. Check IndexedDB AFTER generation: `UserProfile.nextQuoteNumber` should be incremented by 1; the saved job should now have `quoteNumber` = the value the PDF displayed
    4. Generate PDF for the SAME job a second time. The PDF should display the SAME Q-NNNN (no re-increment); `UserProfile.nextQuoteNumber` is unchanged from after step 3
    5. PASS criteria: D-05 lifetime counter, no re-assignment on second gen

    After all scenarios pass, type "approved" to sign off. The executor then updates 16-VERIFICATION.md status=passed and writes the SUMMARY.

    If ANY scenario FAILS, type the failure detail. Executor adds the failure to 16-VERIFICATION.md gaps list. The orchestrator routes to a gap-closure plan-phase pass.
  </how-to-verify>
  <resume-signal>Type "approved" to mark all UAT scenarios as PASS and proceed to close the phase. Or type a failure description (e.g. "scenario 2: € renders as a rectangle") to document a gap that needs gap-closure.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize 16-VERIFICATION.md with UAT results + write 16-05-SUMMARY.md + flip ROADMAP Phase 16 to complete</name>
  <files>
    .planning/phases/16-printable-pdf-quote/16-VERIFICATION.md,
    .planning/phases/16-printable-pdf-quote/16-05-SUMMARY.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/phases/16-printable-pdf-quote/16-VERIFICATION.md (draft from Task 1),
    .planning/ROADMAP.md (Phase 16 section + progress table),
    .planning/REQUIREMENTS.md (traceability table)
  </read_first>
  <action>
    After Task 2 human checkpoint approval ("approved"), finalize the closing artifacts.

    1. Update 16-VERIFICATION.md:
       - Frontmatter status → `passed`
       - Frontmatter score → `5/5 requirements verified — 5/5 roadmap criteria verified`
       - Frontmatter verified_by → `automated + human-uat`
       - Append UAT scenario results table (one row per scenario from Task 2, with PASS marker and the user's brief approval)
       - Append "Requirement Verification" table per PDF-01..PDF-05 (per the 14-VERIFICATION.md analog — original ROADMAP wording, verification method, result, reconciliation if any)
       - Append "ROADMAP Success Criteria" section (per 14-VERIFICATION.md analog) mapping each of the 5 criteria from Phase 16 ROADMAP to PASS evidence
       - Append "Next Phase Hand-off" section noting: Phase 16 ships v1.2 milestone closure; nothing follows in this milestone; the user signals milestone retro if desired

    2. Write 16-05-SUMMARY.md: brief plan-level summary of UAT + verification work. Include: commit hashes for plan 01-04 deliverables; final bundle sizes; total test count delta; any gaps deferred (if Task 2 surfaced any minor non-blockers).

    3. Update .planning/ROADMAP.md:
       - Phase 16 status row: `[x] **Phase 16: Printable PDF Quote** — ...` (change checkbox to [x])
       - Progress Table row for Phase 16: `5/5 | Complete | YYYY-MM-DD` (today's date)
       - Update the top-level progress numbers if a `completed_phases` counter exists

    4. Update .planning/REQUIREMENTS.md traceability table: flip PDF-01..PDF-05 rows from "Pending" to "Complete"

    5. Final commit message format: `docs(16): phase complete — PDF quote shipped` (the orchestrator handles the actual commit; just leave the deliverable files in a clean state)

    Do NOT close the phase if Task 2 surfaced a failure. Failure path: update 16-VERIFICATION.md with the gap entry, set frontmatter status="blocked", do NOT flip ROADMAP/REQUIREMENTS, return control to orchestrator with a "gap closure needed" signal.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-printable-pdf-quote/16-VERIFICATION.md &amp;&amp; grep -q "^status: passed" .planning/phases/16-printable-pdf-quote/16-VERIFICATION.md &amp;&amp; grep -q "Phase 16.*Complete" .planning/ROADMAP.md &amp;&amp; grep "PDF-01" .planning/REQUIREMENTS.md | grep -q "Complete"</automated>
  </verify>
  <acceptance_criteria>
    - 16-VERIFICATION.md frontmatter `status: passed` and `score: 5/5 requirements verified`
    - 16-VERIFICATION.md has tables for: Automated Chain, Static Audits, UAT Scenario Results, Requirement Verification, ROADMAP Success Criteria
    - 16-05-SUMMARY.md exists with brief UAT summary
    - ROADMAP.md Phase 16 checkbox flipped to [x] AND the Progress Table row updated to "Complete | YYYY-MM-DD"
    - REQUIREMENTS.md traceability rows for PDF-01..PDF-05 all say "Complete"
    - No regressions: `npm run build` + `vitest run` still green
  </acceptance_criteria>
  <done>Phase 16 is closed: VERIFICATION.md records the full UAT evidence; ROADMAP and REQUIREMENTS reflect completion; SUMMARY.md is committed. v1.2 milestone closes when Phase 15 also completes (separate plan-phase run).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human UAT sign-off | The user's typed "approved" signal is the source of truth for visual correctness |
| Dexie persistence boundary | Quote number assignment writes verified via UAT scenario 7 (read-back check) |
| Tauri save dialog | UAT scenario 5 exercises the actual desktop dialog (cannot be automated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-16 | Repudiation | UAT pass without actual verification | mitigate | 7 explicit scenarios; user must type "approved" verbatim; gap-claim path requires written failure detail |
| T-16-17 | Tampering | NewBadge layout silently regressing in narrow widths | mitigate | UAT scenario 6 explicitly tests 375px mobile width |
| T-16-18 | Information Disclosure | UAT PDFs leaking real customer data into shared screenshots | accept | UAT happens locally on the user's machine; PDFs are not committed; user controls what gets shared |
</threat_model>

<verification>
1. Task 1: `npm run build` exits 0; 16-VERIFICATION.md draft exists with automated chain section
2. Task 2: Human types "approved" or a failure description; gate is blocking — cannot proceed without input
3. Task 3 (only after "approved"):
   - 16-VERIFICATION.md status=passed; 5/5 score
   - ROADMAP.md Phase 16 marked [x] with completion date
   - REQUIREMENTS.md PDF-01..PDF-05 all "Complete"
   - 16-05-SUMMARY.md exists
   - Final `npm run build` exits 0 (regression check)
</verification>

<success_criteria>
- Automated chain: 100% green (npm run build + vitest run + all grep audits)
- UAT scenarios 1-7: all PASS (user-verified)
- 16-VERIFICATION.md: complete record of automated + UAT evidence with the per-requirement + per-criterion reconciliation tables
- ROADMAP.md + REQUIREMENTS.md: reflect Phase 16 completion
- 16-05-SUMMARY.md: documents any nuances (e.g., bundle size deltas, any minor deviations from plans)
- Phase 16 is committable, mergeable, shippable
</success_criteria>

<output>
Create `.planning/phases/16-printable-pdf-quote/16-05-SUMMARY.md` and `.planning/phases/16-printable-pdf-quote/16-VERIFICATION.md` when done. The orchestrator picks up VERIFICATION.md as the phase-closure signal.
</output>
