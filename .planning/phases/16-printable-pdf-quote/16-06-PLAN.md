---
phase: 16-printable-pdf-quote
plan: "06"
type: execute
wave: 0
depends_on: ["16-04"]
gap_closure: true
gap_ids: [A]
decisions: [D-13]
files_modified:
  - src/components/CostCalculator.tsx
  - src/components/CostCalculator.test.tsx
requirements: [PDF-01]
autonomous: true
nyquist_compliant: true
must_haves:
  truths:
    - "Per D-13, the CostCalculator surface no longer renders a Generate PDF button — `grep -c 'GeneratePdfButton' src/components/CostCalculator.tsx` returns 0."
    - "Per D-13, the CostCalculator no longer dynamically imports the PDF module — `grep -c \"await import('../pdf/generateQuotePdf')\" src/components/CostCalculator.tsx` returns 0."
    - "After this plan, the global `await import('../pdf/generateQuotePdf')` audit count across `src/` drops from 2 → 1 — only `src/components/JobsManager.tsx` retains the dynamic import call site."
    - "The 4 GeneratePdfButton tests in CostCalculator.test.tsx are deleted; the file either drops the GeneratePdfButton describe block entirely or replaces with an `it.todo` placeholder noting the surface was retired in 16-06 / D-13."
    - "`vitest run` and `tsc -b` both exit 0 after the removal — no orphan imports, no dead symbols, no broken prop chains in App.tsx."
  artifacts:
    - path: "src/components/CostCalculator.tsx"
      provides: "CostCalculator without Generate PDF surface (D-13)"
    - path: "src/components/CostCalculator.test.tsx"
      provides: "Test file with GeneratePdfButton suite removed"
---

<objective>
Per gap A (D-13), the CostCalculator surface must not expose a Generate PDF action. The CostCalculator runs pre-save and pre-customer; a quote from this surface is wrong-by-default because it has no Sale/Customer context. The new quote workflow (plan 16-10) is JobsManager-modal-driven and the CostCalculator surface is retired from the PDF path entirely.

Purpose: Eliminate the wrong-by-default quote entry point so the only quote path is the Print Quote modal opened from JobsManager (D-14 + D-18).
Output: CostCalculator.tsx with the GeneratePdfButton subcomponent, handler, dynamic import, isGenerating state, and onPersistQuoteNumber prop wiring all removed; CostCalculator.test.tsx with the 4 GeneratePdfButton tests removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-printable-pdf-quote/16-CONTEXT.md
@.planning/phases/16-printable-pdf-quote/16-VERIFICATION.md
@.planning/phases/16-printable-pdf-quote/16-04-SUMMARY.md
@src/components/CostCalculator.tsx
@src/components/CostCalculator.test.tsx
@src/App.tsx
@scripts/assert-no-static-jspdf.mjs

<interfaces>
<!-- Wave-3 (16-04) introduced the GeneratePdfButton subcomponent + handler -->
<!-- in CostCalculator.tsx. The Plan 16-04 summary documents the exact wiring -->
<!-- being removed here: -->
<!--   - GeneratePdfButton: exported subcomponent (line ~50) -->
<!--   - handleGeneratePdf: handler at line ~700 using `await import('../pdf/generateQuotePdf')` -->
<!--   - isGenerating: useState boolean for double-click prevention (T-16-12) -->
<!--   - onPersistQuoteNumber: REQUIRED prop in CostCalculatorProps (no ?:) -->
<!--   - <GeneratePdfButton> JSX in the Save Job row (line ~1668) -->

<!-- App.tsx passes `onPersistQuoteNumber={handlePersistQuoteNumber}` to BOTH -->
<!-- CostCalculator and JobsManager. After this plan, JobsManager still needs -->
<!-- the callback (it owns the remaining PDF entry point). The prop stays on -->
<!-- JobsManagerProps; App.tsx stops passing it to CostCalculator only. -->

<!-- 16-04 SUMMARY.md notes that handlePersistQuoteNumber lives in App.tsx and -->
<!-- writes db.jobs.put + dbSetUserProfile + setEditingJob. That function is -->
<!-- still consumed by JobsManager — DO NOT delete it from App.tsx. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Generate PDF surface from CostCalculator.tsx</name>
  <files>src/components/CostCalculator.tsx</files>
  <read_first>
    - src/components/CostCalculator.tsx (entire file — read once; identify all sites listed below before editing)
    - .planning/phases/16-printable-pdf-quote/16-04-SUMMARY.md (confirms exactly what was wired in)
    - src/App.tsx (only the section that passes props to CostCalculator — identify the `onPersistQuoteNumber` prop being passed; do NOT edit this file in this task)
  </read_first>
  <action>
    Delete from src/components/CostCalculator.tsx per D-13:

    1. The `GeneratePdfButton` exported function component (declared near line 50; identify by `export function GeneratePdfButton`) including its `GeneratePdfButtonProps` interface.
    2. The `handleGeneratePdf` async handler that uses `await import('../pdf/generateQuotePdf')` (around line 700).
    3. The `isGenerating` useState declaration and any references to it.
    4. The `<GeneratePdfButton .../>` JSX element from the Save Job row (around line 1668) and the surrounding `<div className="relative ...">` wrapper IF it exists solely to host the GeneratePdfButton's NewBadge.
    5. The `<NewBadge feature="pdf-quote" ... />` JSX inside the wrapper above. **DO NOT** remove the `'pdf-quote'` entry from `src/features.ts` — JobsManager still uses it. **DO NOT** remove the `NewBadge` import line if other badges in this file still consume it.
    6. The `onPersistQuoteNumber` prop from the `CostCalculatorProps` interface AND from the function signature destructure. App.tsx still passes this prop today, so this is a breaking prop-shape change that App.tsx must mirror in Task 2.
    7. Any orphan imports left behind: `NewBadge` (if unused elsewhere in this file), `Button` (if no other button consumers), and any type imports tied solely to PDF wiring. **Verify by re-grepping the file after edits.**
    8. Leave a 1-line code comment at the former insertion site (Save Job row) noting `// Generate PDF removed in Phase 16 gap closure (D-13) — use Print Quote in JobsManager.`

    Per CLAUDE.md project rules: prefer the shared `<Button>` primitive over raw `<button>`. This task only removes code — no new components introduced.
  </action>
  <acceptance_criteria>
    - `grep -c "GeneratePdfButton" src/components/CostCalculator.tsx` returns `0`.
    - `grep -c "await import('../pdf/generateQuotePdf')" src/components/CostCalculator.tsx` returns `0`.
    - `grep -c "isGenerating" src/components/CostCalculator.tsx` returns `0`.
    - `grep -c "onPersistQuoteNumber" src/components/CostCalculator.tsx` returns `0`.
    - `grep -c 'NewBadge feature="pdf-quote"' src/components/CostCalculator.tsx` returns `0`.
    - `npx tsc -b` exits `0`.
    - `npx vitest run src/components/CostCalculator.test.tsx` does NOT regress non-PDF tests (existing non-GeneratePdfButton tests still pass — note: the 4 GeneratePdfButton tests will fail temporarily until Task 2 removes them; that is expected here).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "GeneratePdfButton\|onPersistQuoteNumber\|await import('../pdf/" src/components/CostCalculator.tsx | grep -qx 0 && npx tsc -b</automated>
  </verify>
  <done>CostCalculator.tsx contains no GeneratePdfButton symbols, no PDF dynamic import, no isGenerating state, no onPersistQuoteNumber prop. tsc -b exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Remove GeneratePdfButton tests + update App.tsx prop chain</name>
  <files>src/components/CostCalculator.test.tsx, src/App.tsx</files>
  <read_first>
    - src/components/CostCalculator.test.tsx (read in full — identify the 4 GeneratePdfButton tests added in 16-04 Task 2)
    - src/App.tsx (read sections that pass props to CostCalculator — the `<CostCalculator ... onPersistQuoteNumber={handlePersistQuoteNumber} ... />` call site)
    - .planning/phases/16-printable-pdf-quote/16-04-SUMMARY.md (confirms 4 tests added: disabled-when-no-editingJob, disabled-when-sellingPrice-zero, disabled-when-isGenerating, click-calls-onGenerate)
  </read_first>
  <action>
    A. **src/components/CostCalculator.test.tsx:**
       1. Delete the entire `describe('GeneratePdfButton', ...)` block and any helper imports it pulled in (jsdom `createRoot`, `act`, etc.) that are NOT used by remaining tests in the file. Re-check imports after deletion.
       2. If the file becomes empty of `it()` and `it.todo()` blocks after deletion, leave a single `it.todo('CostCalculator tests TBD — GeneratePdfButton retired per D-13')` placeholder so the file still parses as a valid test file. The Wave 0 scaffold rule (16-01) requires every `.test.tsx` to have at least one assertion or todo.

    B. **src/App.tsx:**
       1. Remove the `onPersistQuoteNumber={handlePersistQuoteNumber}` prop from the `<CostCalculator ... />` JSX call site. `JobsManager` still receives this prop — do not touch its JSX.
       2. **DO NOT** delete the `handlePersistQuoteNumber` function in App.tsx — JobsManager still uses it. **DO NOT** delete the `db` and `setUserProfile as dbSetUserProfile` imports — they are still consumed by `handlePersistQuoteNumber`.

    Per CLAUDE.md: act like a senior developer — verify what JobsManager still uses before deleting anything from App.tsx.
  </action>
  <acceptance_criteria>
    - `grep -c "GeneratePdfButton" src/components/CostCalculator.test.tsx` returns `0`.
    - `grep -c "onPersistQuoteNumber" src/App.tsx` returns exactly `2` (the function definition `handlePersistQuoteNumber` and the JobsManager prop pass — NOT 3 as before).
    - `npx vitest run src/components/CostCalculator.test.tsx` exits `0` (no failing tests; either remaining real tests pass OR the single `it.todo` placeholder is recognized).
    - Full `npx vitest run` exits `0` with no regressions in any other test file.
    - `npx tsc -b` exits `0`.
    - `node scripts/assert-no-static-jspdf.mjs` exits `0` (no change expected — the static-import gate scans src/ for static jspdf imports; this plan only removes a dynamic import).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "GeneratePdfButton" src/components/CostCalculator.test.tsx | grep -qx 0 && grep -c "onPersistQuoteNumber" src/App.tsx | grep -qx 2 && npx vitest run && npx tsc -b</automated>
  </verify>
  <done>GeneratePdfButton tests deleted, App.tsx no longer passes onPersistQuoteNumber to CostCalculator (still passes it to JobsManager), full vitest run and tsc -b both green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → CostCalculator form | No change — form still accepts user input. The removed PDF button never crossed a trust boundary by itself; this plan reduces attack surface by removing one entry point. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-21 | Information Disclosure | CostCalculator quote path | mitigate | Removing the surface eliminates the wrong-by-default code path that fabricated quotes without customer/snapshot context. Audit count for `await import('../pdf/generateQuotePdf')` drops from 2 → 1. |
| T-16-22 | Tampering | App.tsx prop chain | accept | The shared `handlePersistQuoteNumber` function stays in App.tsx for JobsManager's use. Single-consumer pattern reduces blast radius vs the prior dual-consumer pattern. |
</threat_model>

<verification>
- Static checks: `grep -c "GeneratePdfButton" src/components/CostCalculator.tsx` → 0; `grep -rn "await import('../pdf/generateQuotePdf')" src/ | wc -l` → 1 (only JobsManager.tsx:781 remains, give or take line shifts).
- Type check: `npx tsc -b` exits 0.
- Unit tests: `npx vitest run` exits 0.
- Static-import gate: `node scripts/assert-no-static-jspdf.mjs` exits 0 (unchanged behavior — gate only scans for static `from 'jspdf'` imports, not dynamic imports).
- Manual smoke (executor): open `src/components/CostCalculator.tsx` in your editor; confirm no Generate PDF button JSX remains in the Save Job row.
</verification>

<success_criteria>
- The CostCalculator surface no longer renders a Generate PDF button or imports the PDF module — confirmed by grep and by line-by-line read of the Save Job row.
- The 4 GeneratePdfButton tests are deleted (no orphaned describe block, no dead imports).
- App.tsx no longer passes `onPersistQuoteNumber` to CostCalculator but DOES still pass it to JobsManager.
- Full build chain green: `npx tsc -b` + `npx vitest run` + `node scripts/assert-no-static-jspdf.mjs` all exit 0.
- The `pdf-quote` feature entry in `src/features.ts` is UNTOUCHED (still consumed by the JobsManager surface, soon-to-be-renamed Print Quote button per D-14 / plan 16-07).
</success_criteria>

<output>
Create `.planning/phases/16-printable-pdf-quote/16-06-SUMMARY.md` when done.
</output>
