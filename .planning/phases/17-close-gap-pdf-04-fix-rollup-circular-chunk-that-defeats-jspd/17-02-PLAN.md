---
phase: 17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DUP-02
  - PDF-01
  - PDF-02
  - PDF-03
  - PDF-04
  - PDF-05
  - D-05
  - D-06
must_haves:
  truths:
    - "REQUIREMENTS.md shows `[x]` (ticked) for DUP-02, PDF-01, PDF-02, PDF-03, PDF-04, PDF-05 — all six checkboxes that were stale per the v1.2 audit"
    - "ROADMAP.md top bullet for Phase 16 reads `[x]` with completion date 2026-05-23"
    - "ROADMAP.md Progress Table row for Phase 16 reads `12/12 | Complete    | 2026-05-23` (matching the column-spacing convention of the rows above)"
    - "v1.2 milestone doc state matches what shipped — no stale `[ ]` boxes on satisfied requirements, no `In Progress` rows on closed phases"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "All six post-audit checkbox ticks landed (DUP-02 + PDF-01..PDF-05)"
      contains: "- [x] **DUP-02**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 16 top-bullet ticked + Progress Table row updated to Complete"
      contains: "12/12 | Complete    | 2026-05-23"
  key_links:
    - from: "v1.2-MILESTONE-AUDIT.md housekeeping section"
      to: ".planning/REQUIREMENTS.md DUP-02 + PDF-01..PDF-05 lines"
      via: "single-character edits inside the `[ ]` brackets"
      pattern: "- \\[x\\] \\*\\*(DUP-02|PDF-0[1-5])\\*\\*"
    - from: "Phase 16 VERIFICATION.md verdict (`gaps_closed`, 5/5, 2026-05-23)"
      to: ".planning/ROADMAP.md Progress Table line 174"
      via: "row update from `12/13 | In Progress|  |` to `12/12 | Complete    | 2026-05-23 |`"
      pattern: "16\\. Printable PDF Quote \\| 12/12 \\| Complete"
---

<objective>
Finalize the v1.2 milestone documentation so REQUIREMENTS.md and ROADMAP.md reflect what actually shipped — before `/gsd:audit-milestone v1.2` is re-run and `/gsd:complete-milestone v1.2` archives the milestone. Two surgical doc edits, no code touched.

Purpose: The v1.2 milestone audit (95ddae6) flagged six stale `[ ]` checkboxes in REQUIREMENTS.md (DUP-02 + PDF-01..PDF-05 — all satisfied per phase verification) and one stale ROADMAP.md row (Phase 16 reads `12/13 | In Progress` despite VERIFICATION.md verdict `gaps_closed` with final UAT "this is perfect. Approved." on 2026-05-23). Plan 17-01 closed the milestone's last open blocker (PDF-04 lazy-load defeated by Rollup ordering); PDF-04 can now also tick `[x]`. After this plan lands, the v1.2 doc state is consistent and milestone close can proceed.

Output: `.planning/REQUIREMENTS.md` with 6 ticks; `.planning/ROADMAP.md` with 1 bullet flip + 1 Progress Table row update. Zero code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.2-MILESTONE-AUDIT.md
@.planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-CONTEXT.md
@.planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-PATTERNS.md

<dependency_gate>
This plan is Wave 2 — it depends on Plan 17-01 being shipped AND its build verification having passed (`npm run build` exits 0 with both `✓ pdf chunk: no modulepreload link...` and `✓ pdf chunk: no static import from non-entry chunks...` lines present). PDF-04 in particular is the contract repaired by 17-01; it MUST NOT tick `[x]` until 17-01 lands. The executor of this plan should confirm 17-01-SUMMARY.md exists in the phase directory before starting the doc edits.

Per the goal-backward truth: "v1.2 milestone doc state matches what shipped" — if 17-01 silently failed, ticking PDF-04 would create a false claim. The executor should treat 17-01's clean exit as a precondition, not a courtesy.
</dependency_gate>

<line_locations>
<!-- Exact line numbers and current vs target text, sourced from 17-PATTERNS.md sections 5 + 6. -->
<!-- Verify line numbers by reading REQUIREMENTS.md and ROADMAP.md before editing — file content can shift if other edits land before this one. -->

REQUIREMENTS.md (six `[ ]` → `[x]` edits, contiguous lines 46 + 50-54):

| Line | Current | Target |
|------|---------|--------|
| 46 (DUP-02) | `- [ ] **DUP-02**: ...` | `- [x] **DUP-02**: ...` |
| 50 (PDF-01) | `- [ ] **PDF-01**: ...` | `- [x] **PDF-01**: ...` |
| 51 (PDF-02) | `- [ ] **PDF-02**: ...` | `- [x] **PDF-02**: ...` |
| 52 (PDF-03) | `- [ ] **PDF-03**: ...` | `- [x] **PDF-03**: ...` |
| 53 (PDF-04) | `- [ ] **PDF-04**: ...` | `- [x] **PDF-04**: ...` |
| 54 (PDF-05) | `- [ ] **PDF-05**: ...` | `- [x] **PDF-05**: ...` |

Status-summary table at the bottom (current lines 156-160) shows `PDF-* | Phase 16 | Pending` — D-05 in CONTEXT.md names only the six checkbox lines explicitly, but if the executor finds these table rows mid-edit, flipping `Pending → Complete` is consistent with the checkbox changes and is part of "doc state matches what shipped". Planner discretion: include the table-row updates as part of this task if low-friction.

ROADMAP.md (two single-line edits):

| Line | Current | Target |
|------|---------|--------|
| 36 (Phase 16 bullet) | `- [ ] **Phase 16: Printable PDF Quote** — Lazy-loaded jsPDF quote generation, CI modulePreload assertion, font strategy, 300 KB gate verification` | `- [x] **Phase 16: Printable PDF Quote** — Lazy-loaded jsPDF quote generation, CI modulePreload assertion, font strategy, 300 KB gate verification (completed 2026-05-23)` |
| 174 (Progress Table row) | `\| 16. Printable PDF Quote \| 12/13 \| In Progress\|  \|` | `\| 16. Printable PDF Quote \| 12/12 \| Complete    \| 2026-05-23 \|` |

Spacing convention for line 174: the precedent rows (lines 169-173) use 4 trailing spaces after `Complete` for column alignment. Match exactly — the column should align with rows 169 (`Complete    | 2026-05-21`), 170 (`Complete    | 2026-05-21`), 171 (`Complete    | 2026-05-22`), 172 (`Complete    | 2026-05-25`), 173 (`Complete    | 2026-05-22`). The completion date `2026-05-23` matches Phase 16's VERIFICATION.md final-UAT timestamp. The plan count drops from `12/13` to `12/12` because Plan 16-05 was a verification-only plan that produced `16-VERIFICATION.md` (not a substantive `16-05-SUMMARY.md`); counting 16-05 as one of the 12 plans rather than 13 including a phantom doc-only plan reflects what actually shipped (per D-06 reasoning).
</line_locations>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tick six stale REQUIREMENTS.md checkboxes — DUP-02 + PDF-01..PDF-05 (D-05)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (entire file, ≤180 lines — confirm current state of the 6 checkboxes and the status-summary table at the bottom)
    - .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-PATTERNS.md (section 5 — the exact line-by-line table at lines 273-283 + the diff-shape note)
    - .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-CONTEXT.md (D-05 at lines 53-62 — the six checkboxes named explicitly, with the PDF-04 caveat: "ticks ONLY after Phase 17's fix lands AND build verification passes")
    - .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-01-SUMMARY.md (precondition — confirm Plan 17-01 shipped with `npm run build` green; if SUMMARY does not exist or the build did not pass, STOP and surface the conflict instead of ticking PDF-04)
    - .planning/v1.2-MILESTONE-AUDIT.md (lines 132-138 — the "STALE — update checkbox to [x]" annotations confirming all 6 are satisfied per phase verification, with PDF-04's status now `partial → satisfied` after Plan 17-01)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, change the `[ ]` brackets to `[x]` on exactly six lines, all in the DUP and PDF sections (currently lines 46 + 50-54). The lines after edit:

    - Line 46: `- [x] **DUP-02**: An explicit-allowlist `duplicateJob()` function inherits cost inputs / tags / notes ...` (rest of line unchanged)
    - Line 50: `- [x] **PDF-01**: User can generate a printable PDF quote from a saved job ...` (rest of line unchanged)
    - Line 51: `- [x] **PDF-02**: The PDF contains: 3DCoster header, quote number ...` (rest of line unchanged)
    - Line 52: `- [x] **PDF-03**: `jspdf` + `jspdf-autotable` load only via dynamic `import()` ...` (rest of line unchanged)
    - Line 53: `- [x] **PDF-04**: `vite.config.ts` sets `build.modulePreload: false`; a CI assertion ...` (rest of line unchanged)
    - Line 54: `- [x] **PDF-05**: Main app chunk remains under 300 KB gzipped ...` (rest of line unchanged)

    These are six single-character edits inside the existing `[ ]` brackets. Do NOT modify the requirement description text on any of these lines. Do NOT renumber, reformat, or reflow surrounding markdown. Do NOT touch the Phase 14 D-21..D-24 wording-drift items (CUST-01, CUST-02, ETSY-01) — those are explicitly OUT OF SCOPE per CONTEXT.md D-07 (captured under "Deferred Ideas" for a follow-up cleanup PR).

    Additionally, update the Traceability table at the bottom of the file (lines 156-160) to flip `Pending → Complete` for PDF-01, PDF-02, PDF-03, PDF-04, PDF-05 — match the existing convention used for already-Complete entries on lines 131-148 (column 3 reads `Complete` for satisfied requirements). The DUP-02 row (line 150) already reads `Complete`, so no edit needed there. This keeps the checkbox section and the traceability table in sync — both reflect "shipped" for these six items.

    PRECONDITION CHECK: Before editing PDF-04 specifically, confirm `.planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-01-SUMMARY.md` exists AND its output includes `npm run build` exit 0 with both `✓ pdf chunk: no modulepreload link...` and `✓ pdf chunk: no static import from non-entry chunks...` success lines. If either condition is unmet, stop and report — do NOT tick PDF-04 against a broken build.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*\(DUP-02\|PDF-0[1-5]\)\*\*" .planning/REQUIREMENTS.md</automated>
    <!-- Must return exactly 6. If it returns less, an edit was missed. -->
  </verify>
  <done>
    - `grep -c "^- \[x\] \*\*\(DUP-02\|PDF-0[1-5]\)\*\*" .planning/REQUIREMENTS.md` returns exactly `6`.
    - `grep -c "^- \[ \] \*\*\(DUP-02\|PDF-0[1-5]\)\*\*" .planning/REQUIREMENTS.md` returns exactly `0` (no stale `[ ]` boxes remain on these requirements).
    - The Traceability table rows for PDF-01, PDF-02, PDF-03, PDF-04, PDF-05 read `Complete` in column 3.
    - No other lines in REQUIREMENTS.md changed (verifiable by `git diff --stat .planning/REQUIREMENTS.md` showing only the changed-lines count consistent with 6 checkbox flips + up to 5 table-row word edits).
    - CUST-01, CUST-02, ETSY-01 wording untouched (D-07 honored).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md — tick Phase 16 bullet + update Progress Table row (D-06)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 30-40 (the bullet list — confirm current state of the Phase 16 `[ ]` bullet on line 36) and lines 165-180 (the Progress Table — confirm current state of the Phase 16 row on line 174 AND the spacing convention used by the 5 already-Complete rows on lines 169-173)
    - .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-PATTERNS.md (section 6 — exact current vs target text at lines 296-322 + the trailing-spaces convention warning)
    - .planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-CONTEXT.md (D-06 at lines 64-67 — the 12/13 → 12/12 reasoning + the 2026-05-23 completion date sourced from Phase 16's VERIFICATION.md timestamp)
    - .planning/phases/16-printable-pdf-quote/16-VERIFICATION.md (if present — confirm the final UAT verdict "this is perfect. Approved." + the 2026-05-23 timestamp); if 16-VERIFICATION.md is at a different path, locate it via `ls .planning/phases/16*/16*VERIFICATION*.md`
  </read_first>
  <action>
    In `.planning/ROADMAP.md`, make exactly TWO line-level edits:

    **Edit 1** — flip the Phase 16 top-of-file bullet at line 36 from `[ ]` to `[x]` and append a completion date. Current:

    `- [ ] **Phase 16: Printable PDF Quote** — Lazy-loaded jsPDF quote generation, CI modulePreload assertion, font strategy, 300 KB gate verification`

    Target:

    `- [x] **Phase 16: Printable PDF Quote** — Lazy-loaded jsPDF quote generation, CI modulePreload assertion, font strategy, 300 KB gate verification (completed 2026-05-23)`

    Append ` (completed 2026-05-23)` after `verification` — no other text changes on this line.

    **Edit 2** — update the Progress Table row for Phase 16 at line 174. Current:

    `| 16. Printable PDF Quote | 12/13 | In Progress|  |`

    Target (note 4 trailing spaces after `Complete` to match the alignment of rows 169-173):

    `| 16. Printable PDF Quote | 12/12 | Complete    | 2026-05-23 |`

    Three sub-changes inside this one row: `12/13` → `12/12` (Plan 16-05 was verification-only, not a substantive plan — see D-06 reasoning in CONTEXT.md); `In Progress` → `Complete    ` (with 4 trailing spaces — match the convention used by the 5 already-Complete rows above this one; visual column alignment matters); empty 4th column → `2026-05-23` (the date matches Phase 16's VERIFICATION.md final-UAT timestamp).

    DO NOT touch any other content in ROADMAP.md. In particular, do NOT touch:
    - The Phase 17 row at the bottom (line 236-244 — that's THIS phase, still in flight; it will be updated by the wrap-up of Phase 17 itself, not by this task)
    - The Coverage Map at lines 180-213
    - The Dependency Graph at lines 215-234
    - Any plan-list bullets within the Phase 16 detail section (lines 147-160 — those internal `[x]`/`[ ]` boxes are already correctly marked from prior phase work)

    Two single-line edits only.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*Phase 16: Printable PDF Quote\*\*" .planning/ROADMAP.md</automated>
    <!-- Must return 1. Also: `grep -c "^| 16\. Printable PDF Quote | 12/12 | Complete    | 2026-05-23 |" .planning/ROADMAP.md` must return 1. -->
  </verify>
  <done>
    - `grep -c "^- \[x\] \*\*Phase 16: Printable PDF Quote\*\*" .planning/ROADMAP.md` returns `1`.
    - `grep -c "^- \[ \] \*\*Phase 16: Printable PDF Quote\*\*" .planning/ROADMAP.md` returns `0` (the old form is gone).
    - `grep -c "| 16\. Printable PDF Quote | 12/12 | Complete    | 2026-05-23 |" .planning/ROADMAP.md` returns `1` (the new row, with exact spacing).
    - `grep -c "| 16\. Printable PDF Quote | 12/13 | In Progress" .planning/ROADMAP.md` returns `0` (the old row is gone).
    - The Phase 17 row at the bottom of the file is unchanged (still reads `**Goal:** [To be planned]` / `**Plans:** 0 plans` / `[ ] TBD` — that gets finalized later by the Phase 17 wrap-up step, not by this plan).
    - `git diff --stat .planning/ROADMAP.md` shows only 2 changed lines.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan modifies two project-internal planning documents. No code, no build artifacts, no runtime surface, no user input, no network, no auth. The doc-tick edits are first-party authored content updates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | Doc state vs shipped state divergence | mitigate | The grep-based acceptance gates (Task 1 returns exactly 6 ticked boxes; Task 2 returns exactly 1 bullet flip + 1 row update with exact spacing) prevent the "ticked wrong line" or "missed one of six" failure modes. The PRECONDITION CHECK in Task 1 prevents the more serious failure of ticking PDF-04 against a still-broken build. |
| T-17-05 | Repudiation | Audit trail for milestone close | accept | The git commit message for this phase's work + the Phase 17 SUMMARY.md narrative document why each box was ticked and reference the v1.2-MILESTONE-AUDIT.md as evidence. Standard audit trail. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | N/A — zero package installs in this plan. Pure markdown edits. |

This phase introduces no new threat surface. Existing build-output assertions and the dynamic-import boundary are untouched.

</threat_model>

<verification>

## Wave-Level Verification

After both tasks land:

1. **REQUIREMENTS.md state matches shipped state:**
   - `grep -c "^- \[x\] \*\*\(DUP-02\|PDF-0[1-5]\)\*\*" .planning/REQUIREMENTS.md` = 6
   - `grep -c "^- \[ \] \*\*\(DUP-02\|PDF-0[1-5]\)\*\*" .planning/REQUIREMENTS.md` = 0
   - The 5 PDF rows in the Traceability table read `Complete` (DUP-02 already did)

2. **ROADMAP.md state matches shipped state:**
   - `grep -c "^- \[x\] \*\*Phase 16: Printable PDF Quote\*\*" .planning/ROADMAP.md` = 1
   - `grep -c "12/12 | Complete    | 2026-05-23" .planning/ROADMAP.md` ≥ 1 (the updated Phase 16 row)

3. **D-07 / D-08 honored:**
   - `grep -c "CUST-01\|CUST-02\|ETSY-01" .planning/REQUIREMENTS.md` returns the same count as before this plan started (no wording revisions introduced — those items are explicitly deferred per CONTEXT.md)
   - No VALIDATION.md files created or modified for Phases 13, 15, or 15.1 (explicitly deferred per D-08)

4. **Out-of-scope guards held:** No code files touched. `git diff --name-only` for this phase shows only `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.

</verification>

<success_criteria>

The plan succeeds when:

- [ ] All 6 REQUIREMENTS.md checkboxes (DUP-02 + PDF-01..PDF-05) ticked `[x]` (D-05).
- [ ] REQUIREMENTS.md Traceability table for PDF-01..PDF-05 reads `Complete` (DUP-02 already did).
- [ ] ROADMAP.md Phase 16 top-bullet ticked `[x]` with `(completed 2026-05-23)` suffix (D-06).
- [ ] ROADMAP.md Progress Table row for Phase 16 reads `12/12 | Complete    | 2026-05-23` with exact column-alignment spacing (D-06).
- [ ] Wave-1 precondition was honored: Plan 17-01's SUMMARY exists and confirmed `npm run build` green (specifically the new `assert-no-static-pdf-import.mjs` gate's `✓` line) BEFORE PDF-04 ticked.
- [ ] Out-of-scope guards held: no code files modified; no CUST-01/CUST-02/ETSY-01 wording changes (D-07 deferred); no VALIDATION.md backfill (D-08 deferred); no other Progress Table rows touched; Phase 17's own row at the bottom of ROADMAP.md unchanged (it gets finalized at phase wrap-up, not by this plan).

</success_criteria>

<output>
Create `.planning/phases/17-close-gap-pdf-04-fix-rollup-circular-chunk-that-defeats-jspd/17-02-SUMMARY.md` when done. The summary should include:
- The 6 REQUIREMENTS.md checkbox lines (line numbers + before/after)
- The 2 ROADMAP.md edits (bullet + table row, with the trailing-spaces verification)
- Confirmation that the Wave-1 precondition was checked (Plan 17-01 SUMMARY exists, build green)
- A grep-based audit showing zero stale `[ ]` boxes on DUP-02/PDF-01..PDF-05 and zero `In Progress` mention of Phase 16 in the Progress Table
- Explicit note that D-07 (CUST/ETSY wording drift) and D-08 (VALIDATION.md backfill) were honored as deferred — no edits made
</output>
