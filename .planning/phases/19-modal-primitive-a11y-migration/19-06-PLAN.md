---
phase: 19-modal-primitive-a11y-migration
plan: 06
type: execute
wave: 3
depends_on:
  - 19-04
  - 19-05
files_modified:
  - src/components/JobsManager.tsx
  - src/components/CustomerLibrary.tsx
  - src/components/AssetLibrary.tsx
  - .planning/phases/19-modal-primitive-a11y-migration/19-06-UAT.md
autonomous: false
requirements: [A11Y-05]
must_haves:
  truths:
    - "JobsManager's <List> container has role='list' and aria-rowcount={searchedJobs.length} (D-15)"
    - "CustomerLibrary's <List> container has role='list' and aria-rowcount={visibleCustomers.length}"
    - "AssetLibrary's 3 <List> containers each have role='list' and aria-rowcount={items.length}"
    - "Each per-row outer <div> in JobsManager / CustomerLibrary / AssetLibrary virtualized rows has role='listitem'"
    - "AssetLibrary desktop table parent divs (lines 1189 and 1220) wrap with role='grid' or role='table'"
    - "VoiceOver UAT smoke-tested against all 10 migrated modal surfaces (5 from 19-04 + 5 from 19-05); each surface announces 'dialog' + title; Escape closes; focus returns to trigger or sensible fallback"
    - "All 10 migrated modal surfaces announce as a dialog with their title when opened under VoiceOver (or failures are captured as follow-on items in 19-06-UAT.md)"
  artifacts:
    - path: "src/components/JobsManager.tsx"
      provides: "JobsManager virtualized list has list role + aria-rowcount + per-row listitem"
      contains: "role=\"list\""
    - path: "src/components/CustomerLibrary.tsx"
      provides: "CustomerLibrary virtualized list has list role + aria-rowcount + per-row listitem"
      contains: "role=\"list\""
    - path: "src/components/AssetLibrary.tsx"
      provides: "AssetLibrary 3 virtualized lists have list role + aria-rowcount; desktop table parents have role=grid"
      contains: "role=\"list\""
    - path: ".planning/phases/19-modal-primitive-a11y-migration/19-06-UAT.md"
      provides: "VoiceOver UAT checklist + pass/fail results for the 10 migrated modal surfaces"
      min_lines: 60
  key_links:
    - from: "src/components/JobsManager.tsx <List>"
      to: "row count"
      via: "aria-rowcount={searchedJobs.length}"
      pattern: "aria-rowcount=\\{"
    - from: "src/components/CustomerLibrary.tsx row component"
      to: "outer <div>"
      via: "role=\"listitem\" on the row's outer wrapper"
      pattern: "role=\"listitem\""
---

<objective>
Close the final two pieces of Phase 19: A11Y-05 (virtualized-list ARIA attributes) and the VoiceOver UAT validation for the entire a11y surface migrated in plans 19-04 and 19-05.

Adds `role="list"` + `aria-rowcount={count}` to 5 `<List>` containers (1 in JobsManager, 1 in CustomerLibrary, 3 in AssetLibrary), `role="listitem"` to each row's outer `<div>`, and `role="grid"` to AssetLibrary's 2 desktop table parents (lines 1189 + 1220).

Performs a VoiceOver smoke test against each of the 10 migrated modal surfaces and documents the results in `19-06-UAT.md` — pass/fail per surface, with follow-on items captured for any failures (NOT blocking Phase 19 completion).

This plan is scheduled to wave 3 to avoid `files_modified` overlap with 19-05 (both touch `JobsManager.tsx`). 19-06 must run AFTER BOTH 19-04 and 19-05 complete because (a) JobsManager.tsx is touched by 19-05, and (b) Task 2's UAT script covers 5 surfaces shipped in 19-04 (PrintQuoteModal, SettingsModal, UserProfileModal, CustomerEditModal, MaintenanceAlertModal) — running before 19-04 lands would UAT un-migrated modals. The other two files (CustomerLibrary.tsx, AssetLibrary.tsx) are not touched by any prior plan in Phase 19, so they can be edited freely once 19-04 + 19-05 land.

Purpose: Close A11Y-05 (1 HIGH finding) and validate A11Y-01 through A11Y-08 in production via VoiceOver UAT.

Output: 3 source files modified + 1 UAT document created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-modal-primitive-a11y-migration/19-CONTEXT.md
@.planning/phases/19-modal-primitive-a11y-migration/19-04-SUMMARY.md
@.planning/phases/19-modal-primitive-a11y-migration/19-05-SUMMARY.md

<interfaces>
<!-- react-window v2 <List> passes role/aria-* through via HTMLAttributes -->

From node_modules/react-window/dist/react-window.d.ts line 310:
```typescript
export type ListProps<T> = Omit<HTMLAttributes<HTMLDivElement>, "onResize"> & { ... };
```

`role` and `aria-rowcount` pass through naturally as HTMLDivElement attributes — no react-window API change needed. Just add them as JSX props on the `<List>` element.

<!-- Per-row component pattern (JobsManager) -->

Module-scope adapter at line 783+:
```tsx
const JobRow = ({ index, style, ...rest }: { index: number; style: React.CSSProperties; ... }) => (
  <div style={style} className="...">{/* row content */}</div>
);
```

Row's outer `<div>` adds `role="listitem"`.

<!-- AssetLibrary desktop table parent (line 1189 + 1220) -->

```tsx
<div role="row" className="grid grid-cols-7 gap-x-4 text-slate-400 text-left border-b border-slate-700">
  {/* header cells */}
</div>
<List ...>
```

The PARENT element wrapping this header `<div role="row">` AND the `<List>` below it needs `role="grid"` (or `role="table"`). Read the file to identify the exact parent — likely a `<div>` that contains both the header row and the list. If the parent doesn't exist as a wrapper, wrap the header + List together in a new `<div role="grid">`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add role + aria-rowcount + listitem to JobsManager + CustomerLibrary + AssetLibrary virtualized lists</name>
  <files>src/components/JobsManager.tsx, src/components/CustomerLibrary.tsx, src/components/AssetLibrary.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx (POST plan 19-05 — read the file with the 3 Modal-wrapped overlays already in place; line 1738 — the `<List>` declaration; line 783+ — the per-row JobRow adapter; identify the outer `<div>` of each row)
    - src/components/CustomerLibrary.tsx (line 275 — the `<List>` declaration; line 88 — the row component; read both to identify the outer `<div>`)
    - src/components/AssetLibrary.tsx (lines 1159 + 1200 + 1230 — the 3 `<List>` declarations; lines 1189 + 1220 — the 2 desktop table header rows that need `role="grid"` parents; identify each List's row component and its outer `<div>`)
    - node_modules/react-window/dist/react-window.d.ts line 310 (confirms HTMLAttributes pass-through)
    - .planning/phases/19-modal-primitive-a11y-migration/19-CONTEXT.md (D-15 — exact aria attribute lock)
  </read_first>
  <action>
    Three files, same pattern in each. Read each file's relevant sections to determine the EXACT row component and its outer `<div>` before editing.

    **JobsManager.tsx (line ~1738):**
    1. Add `role="list"` and `aria-rowcount={searchedJobs.length}` (or whatever the current rendered-row-count expression is — read line ~1738's surroundings to confirm the variable name; common names: `searchedJobs`, `filteredJobs`, `visibleJobs`) as props on the `<List>` element.
    2. In the JobRow adapter (line ~783+), add `role="listitem"` to the outer `<div>` of each row. If JobRow renders a Card or other component as its outermost element, add `role="listitem"` to that component's root `<div>`. If the outermost element doesn't accept a `role` prop directly (e.g., a fragment), restructure minimally — wrap with a `<div role="listitem">` only if no equivalent exists.

    **CustomerLibrary.tsx (line ~275):**
    1. Add `role="list"` and `aria-rowcount={visibleCustomers.length}` (or current variable name — read line 275's surroundings) as props on the `<List>`.
    2. In the row component (line ~88), add `role="listitem"` to the outer `<div>`.

    **AssetLibrary.tsx (lines ~1159, ~1200, ~1230):**
    1. For each of the 3 `<List>` instances, add `role="list"` and `aria-rowcount={itemsCount}` (use the appropriate count expression for each list — read the file to confirm what variable provides the count for each List).
    2. For each List's row component, add `role="listitem"` to the row's outer `<div>`.
    3. Lines ~1189 and ~1220 — the desktop table header `<div role="row">` elements. Wrap each header + its following `<List>` in a parent with `role="grid"`. Read the existing structure to determine if such a parent already exists; if so, add `role="grid"` to it; if not, introduce a minimal wrapping `<div role="grid">` that contains both the header row and the List.

    **Critical constraints:**
    - The `<List>` already accepts arbitrary HTMLAttributes — adding `role` and `aria-rowcount` requires NO react-window API change.
    - Do NOT change the virtualization logic, row heights, scroll behavior, dynamic-row-height invalidation, search/filter logic, or any other behavior.
    - Do NOT touch the `useDynamicRowHeight` hook or its keys.
    - Do NOT change any consumer-facing prop API.
    - Do NOT re-touch the Modal-wrapped overlay regions in JobsManager (that's 19-05's territory — leave the migrations from 19-05 untouched).
    - If adding `role="listitem"` requires changing a Fragment (`<></>`) to a `<div>` wrapper, prefer the smallest possible change — most rows already wrap their content in a `<div>`.
    - Read each file's structure before guessing the count expression — pass the EXACT variable that drives the rendered row count.
    - tsc -b after each file edit.
  </action>
  <verify>
    <automated>npx tsc -b && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/components/JobsManager.tsx` contains `role="list"` and `aria-rowcount=` on the `<List>` element near line 1738 (`grep -c 'role="list"' src/components/JobsManager.tsx` returns `>= 1`; `grep -c "aria-rowcount=" src/components/JobsManager.tsx` returns `>= 1`).
    - Source: `src/components/JobsManager.tsx` contains `role="listitem"` on the JobRow outer `<div>`.
    - Source: `src/components/CustomerLibrary.tsx` contains `role="list"` and `aria-rowcount=` on the `<List>` element near line 275.
    - Source: `src/components/CustomerLibrary.tsx` contains `role="listitem"` on the row outer `<div>`.
    - Source: `src/components/AssetLibrary.tsx` contains `role="list"` at least 3 times (one per `<List>`) and `aria-rowcount=` at least 3 times.
    - Source: `src/components/AssetLibrary.tsx` contains `role="grid"` at least 2 times (one per desktop table parent — lines ~1189 + ~1220).
    - Source: `src/components/AssetLibrary.tsx` contains `role="listitem"` at least 3 times (one per row component).
    - Source: 19-05's Modal-wrapped overlays in JobsManager.tsx are preserved (no regression — `grep -c "<Modal" src/components/JobsManager.tsx` still returns `>= 3`).
    - CLI: `npx tsc -b` exits 0.
    - CLI: `npx vitest run` exits 0 (no test regressed).
  </acceptance_criteria>
  <done>
    A11Y-05 closed for all 5 virtualized lists across 3 files + 2 desktop table parents. No behavioral change — just ARIA attributes that assistive tech consumes. 19-05's overlay migrations preserved.
    Atomic commit: `gsd-sdk query commit "feat(19-06): add list role + aria-rowcount + listitem to virtualized lists (A11Y-05)" --files src/components/JobsManager.tsx src/components/CustomerLibrary.tsx src/components/AssetLibrary.tsx`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: VoiceOver UAT — smoke test 10 migrated modal surfaces; document results in 19-06-UAT.md</name>
  <files>.planning/phases/19-modal-primitive-a11y-migration/19-06-UAT.md</files>
  <read_first>
    - .planning/phases/19-modal-primitive-a11y-migration/19-CONTEXT.md (discretion block — VoiceOver UAT script template at the bottom of the Decisions section)
    - .planning/phases/19-modal-primitive-a11y-migration/19-04-SUMMARY.md (confirm which 5 surfaces shipped in 19-04)
    - .planning/phases/19-modal-primitive-a11y-migration/19-05-SUMMARY.md (confirm which 5 surfaces shipped in 19-05)
    - macOS VoiceOver shortcut reference: Cmd+F5 toggles VoiceOver on/off
  </read_first>
  <what-built>
    - 10 modal surfaces migrated to Modal primitive (plans 19-04 + 19-05): PrintQuoteModal, SettingsModal, UserProfileModal, CustomerEditModal, MaintenanceAlertModal, CustomerCsvImportModal, DeclineQuoteModal, CsvImportModal, Record Sale overlay (JobsManager), Delete Job confirm (JobsManager), Delete Sale confirm (JobsManager).
    - Note: A11Y-03's ROADMAP criterion lists 10 surfaces total — the 3 JobsManager inline overlays together count as 3 of those 10 (Record Sale + Delete Job + Delete Sale).
    - Virtualized lists in JobsManager, CustomerLibrary, AssetLibrary now expose list/listitem/grid roles (Task 1 of this plan).
  </what-built>
  <how-to-verify>
    **Setup:**
    1. `npm run dev` (port 4173 per project rule — `vite.config.ts` pins this).
    2. Open Safari (VoiceOver works best in Safari on macOS; Chrome/Firefox also work but Safari is the canonical pairing).
    3. Toggle VoiceOver: `Cmd+F5`.

    **Per-surface smoke test — repeat for all 10 surfaces below:**
    1. Navigate to the trigger element (button or link that opens the modal).
    2. Activate the trigger.
    3. **Assertion A:** VoiceOver announces "dialog" or "web dialog" and reads the modal title within 1 second of opening.
    4. **Assertion B:** Tab through the modal — focus stays within the modal (does not escape to the background page).
    5. **Assertion C:** Press Escape — modal closes; VoiceOver announces the page or returns to a sensible focus target.
    6. **Assertion D:** Focus returns to the trigger element (or a sensible fallback — `document.body` is acceptable if the trigger was unmounted).

    **10 surfaces to test:**
    1. PrintQuoteModal — trigger: the "Print Quote" button on a job card.
    2. SettingsModal — trigger: the gear icon in the header.
    3. UserProfileModal — trigger: the user avatar in the header.
    4. CustomerEditModal — trigger: the "Add customer" or "Edit" button in CustomerLibrary.
    5. CustomerCsvImportModal — trigger: the "Import CSV" button in CustomerLibrary.
    6. DeclineQuoteModal — trigger: the "Decline" action on a pending quote.
    7. MaintenanceAlertModal — trigger: a maintenance threshold being exceeded (may require seeding a printer with high cumulative hours). If the trigger flow is non-trivial to reproduce manually, mark this surface as "trigger-untested; structural assertions still validate" and note in 19-06-UAT.md.
    8. CsvImportModal — trigger: the "Import CSV" button on the Materials or Printers tab.
    9. Record Sale overlay — trigger: the "Record Sale" button on a job card.
    10. Delete Job confirm — trigger: the trash icon on a job card.
    11. Delete Sale confirm — trigger: the delete icon on a sale row within a job's accordion.

    (That's 11 distinct triggers because Delete Job + Delete Sale are 2 separate inline overlays — both count under A11Y-03's "10 modal surfaces" framing because the 3 JobsManager inline overlays roll up into the "10" count alongside the 7 standalone modals.)

    **Virtualized-list smoke test (additional):**
    1. With VoiceOver on, navigate to the JobsManager page.
    2. Tab/arrow into the virtualized jobs list.
    3. **Assertion E:** VoiceOver announces "list, N items" (where N is the visible job count).
    4. **Assertion F:** Stepping through rows, VoiceOver announces each row as a list item.
    5. Repeat the same two assertions for CustomerLibrary and AssetLibrary.

    **Document results in `.planning/phases/19-modal-primitive-a11y-migration/19-06-UAT.md`:**
    Use a table format:
    | Surface | A: dialog+title | B: focus trap | C: Escape closes | D: focus returns | Notes |
    | PrintQuoteModal | pass / fail | ... | ... | ... | ... |
    | ... |

    Append a "Follow-on Items" section listing any failures — these are NOT Phase 19 blockers (per CONTEXT.md discretion block); they're captured for v1.4+ scope.

    **Resume signal:** After completing UAT, write the 19-06-UAT.md document with the results table. Then type "approved" (if all 11 surfaces pass A-D) OR list the failing surfaces with brief notes (failures are accepted as follow-on, not blockers).
  </how-to-verify>
  <action>
    This is a checkpoint task — the executor pauses here for the user to perform VoiceOver UAT manually on macOS, then writes the results to 19-06-UAT.md.

    Steps for the executor:
    1. Confirm `npm run dev` is running on port 4173 (or start it if not).
    2. Pause execution and present the `<how-to-verify>` checklist to the user.
    3. The user performs the 11-surface VoiceOver smoke test + 3 virtualized-list checks per `<how-to-verify>`.
    4. The user reports pass/fail per surface.
    5. The executor writes `.planning/phases/19-modal-primitive-a11y-migration/19-06-UAT.md` with the results table (11 modal surfaces + 3 list checks) and a "Follow-on Items" section listing any failures.
    6. Failures are documented as follow-on items, NOT Phase 19 blockers (per CONTEXT.md discretion block — VoiceOver UAT failures do not block Phase 19 closure).
    7. Commit the UAT document via the atomic-commit pattern in `<done>`.
  </action>
  <verify>
    <human-check>User performs the 11-surface VoiceOver smoke test per `<how-to-verify>` and reports results; the executor writes 19-06-UAT.md from those results.</human-check>
  </verify>
  <resume-signal>
    Type "approved" if UAT complete (regardless of pass/fail count — failures captured as follow-on, not blockers) OR describe what's blocking documentation completion.
  </resume-signal>
  <acceptance_criteria>
    - File: `.planning/phases/19-modal-primitive-a11y-migration/19-06-UAT.md` exists.
    - File: 19-06-UAT.md contains a results table with rows for all 11 surfaces (10 of A11Y-03 + the additional virtualized-list test).
    - File: each table row has explicit pass/fail entries for each of the 4 (or 2 for list test) assertion columns.
    - File: a "Follow-on Items" section exists; lists every failure as a follow-on item (empty section is acceptable if all pass).
    - User: types "approved" to resume; or provides documented failures (failures are accepted as follow-on).
    - Atomic commit: `gsd-sdk query commit "docs(19-06): VoiceOver UAT smoke test results for 10 migrated modal surfaces" --files .planning/phases/19-modal-primitive-a11y-migration/19-06-UAT.md`
  </acceptance_criteria>
  <done>
    VoiceOver UAT complete; results documented in 19-06-UAT.md with explicit pass/fail per surface. Follow-on items captured for any failures (NOT Phase 19 blockers per CONTEXT.md discretion block).
    Atomic commit: `gsd-sdk query commit "docs(19-06): VoiceOver UAT smoke test results for 10 migrated modal surfaces" --files .planning/phases/19-modal-primitive-a11y-migration/19-06-UAT.md`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code → assistive tech | ARIA attributes are the contract with screen readers — incorrect attrs are worse than absent attrs |
| dev environment → user device | VoiceOver UAT validates the contract on the developer's macOS device only — Windows/NVDA + iOS/Android out of scope for Phase 19 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-15 | Information disclosure | Incorrect aria-rowcount value | mitigate | Pass the exact rendered-row-count variable (e.g., `searchedJobs.length`) — not `allJobs.length` — so screen readers announce the filtered count, matching visible state |
| T-19-16 | Tampering | role=grid breaks visual table layout | mitigate | `role="grid"` is announcement-only; does not change visual layout. Verified by visual diff post-edit (no layout shift expected) |
| T-19-17 | Repudiation | VoiceOver UAT failures dismissed | accept | UAT failures are captured as follow-on items, NOT Phase 19 blockers — per CONTEXT.md discretion block. Documented in 19-06-UAT.md. |
</threat_model>

<verification>
- `npx tsc -b` exits 0
- `npx vitest run` exits 0
- Source grep across all 3 files: each `<List>` has `role="list"` and `aria-rowcount=`; each row component has `role="listitem"`; AssetLibrary has 2x `role="grid"`
- 19-05's Modal-wrapped overlays in JobsManager.tsx remain present (`grep -c "<Modal" src/components/JobsManager.tsx` returns `>= 3`)
- File exists: `.planning/phases/19-modal-primitive-a11y-migration/19-06-UAT.md`
- 19-06-UAT.md has explicit pass/fail entries per surface per assertion column
- User typed "approved" or documented failures (both accepted)
</verification>

<success_criteria>
- A11Y-05 closed for all 5 virtualized lists (1 in JobsManager, 1 in CustomerLibrary, 3 in AssetLibrary)
- 2 AssetLibrary desktop table parents wrapped with `role="grid"`
- VoiceOver UAT smoke-tested 10 migrated modal surfaces + 3 virtualized lists
- UAT results documented in 19-06-UAT.md with pass/fail per surface
- Failures captured as follow-on items (NOT Phase 19 blockers per CONTEXT.md discretion)
- TypeScript compiles cleanly under `tsc -b`
</success_criteria>

<output>
Create `.planning/phases/19-modal-primitive-a11y-migration/19-06-SUMMARY.md` when done — include:
- LOC delta per source file (small — just role/aria attribute additions)
- The 5 `<List>` instances modified + the 2 `role="grid"` wrappers
- A summary of UAT results (X/11 surfaces passed all assertions; list the failures with one-line notes)
- Confirmation that 19-06-UAT.md exists and contains the full results table
- Confirmation that failures (if any) are scoped as follow-on, not blockers
- `tsc -b` + `vitest run` exit codes (both must be 0)
- A final note: "Phase 19 a11y surface closure — A11Y-01 through A11Y-08 + HYG-09 closed; A11Y-09 was already closed by Phase 25"
</output>
