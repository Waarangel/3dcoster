---
phase: 21-csv-url-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/customerCsv.test.ts
requirements:
  - SEC-03
autonomous: true
tags:
  - csv
  - test-coverage
  - regression-lock
  - unicode
  - formula-injection
user_setup: []

must_haves:
  truths:
    - "A new `describe('Phase 21 SEC-03 — formula-injection + Unicode pass-through')` block is appended to `src/utils/customerCsv.test.ts`"
    - "The new block contains at least 4 test cases per D-08: formula-injection in `name`, formula-injection in `notes`, Unicode characters in `name`, emoji in `notes`"
    - "Each test case asserts that `parseCustomerCsv` returns a `customer.name` (or `customer.notes`) that is character-for-character identical to the raw CSV input — locking parser pass-through behavior"
    - "The new tests DO NOT modify, rename, or restructure the existing `describe('parseCustomerCsv (Phase 15.1 — CL-03 / D-09 / D-07)')` or `describe('buildCustomersForImport (Phase 15.1 — D-11 merge semantics)')` blocks"
    - "The new tests DO NOT call `sanitizeCsvCell` — that helper is the EXPORT-path concern and is tested in plan 21-01's `csvHelpers.test.ts`. Comment in the new describe block makes this separation explicit"
    - "The parser implementation in `src/utils/customerCsv.ts` is NOT modified — these tests lock existing pass-through behavior without changing it"
  artifacts:
    - path: "src/utils/customerCsv.test.ts"
      provides: "new describe block with ≥4 tests locking parser pass-through for formula-injection + Unicode inputs"
      contains: "describe('Phase 21 SEC-03"
  key_links:
    - from: "src/utils/customerCsv.test.ts (new describe block)"
      to: "parseCustomerCsv in src/utils/customerCsv.ts"
      via: "existing named import (already present at the top of the test file)"
      pattern: "parseCustomerCsv\\("
---

<objective>
Close SEC-03 by appending a new `describe('Phase 21 SEC-03 — formula-injection + Unicode pass-through')` block to `src/utils/customerCsv.test.ts` covering ≥4 cases per D-08:

1. Formula-injection string in the `name` column (`=HYPERLINK("https://evil.com","click")`)
2. Formula-injection string in the `notes` column (`+CMD|' /C calc'!A0`)
3. Unicode characters in the `name` column (`Müller`, `张三`)
4. Emoji in the `notes` column (`Great client 🎉`)

Each test asserts the parser preserves the raw input character-for-character — no NFC/NFD normalization, no character loss, no formula escaping. This locks PARSER PASS-THROUGH behavior — the sanitization layer added in plan 21-01 lives at the EXPORT boundary, so a value that round-trips through CSV import must equal the raw CSV input byte-for-byte.

Purpose: Regression-lock the contract between import-side pass-through and export-side sanitization. If a future contributor adds `sanitizeCsvCell` to `customerCsv.ts` (the parser) by mistake, these tests fail loudly — making it impossible to silently corrupt round-trip identity. This is a TEST-ONLY plan; no production code changes.

Output: 1 modified test file (additive — describe block appended below existing blocks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-csv-url-security/21-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/utils/customerCsv.ts
@src/utils/customerCsv.test.ts

<interfaces>
<!-- Key signatures already imported in customerCsv.test.ts. Executor needs no codebase exploration. -->

From src/utils/customerCsv.ts (already imported in the test file at lines 1-7):

  export function parseCustomerCsv(csvString: string, existingCustomers: Customer[]): CustomerCsvParseResult;
  export function buildCustomersForImport(...): Customer[];

  // Parser behavior (verified by reading customerCsv.ts:113-153 validateCustomerRow):
  // - name and email are .trim()'d
  // - email is .toLowerCase()'d on write (UI-SPEC discretion #8)
  // - notes, company, address are .trim()'d but otherwise pass through verbatim
  // - No formula escaping, no Unicode normalization
  //
  // What this means for the new tests:
  // - For `name`: the assertion is `customer.name === rawNameTrimmed`. A `=HYPERLINK("…")` input has no leading/trailing whitespace, so the trimmed value equals the raw value.
  // - For `notes`: same — `customer.notes === rawNotesTrimmed`.
  // - For `name = 'Müller'` and `name = '张三'`: trimmed equals raw. No normalization.
  // - For `notes = 'Great client 🎉'`: trimmed equals raw.

From src/utils/customerCsv.test.ts (existing structure to preserve):

  // Line 1-7: imports — already include `describe, it, expect` from vitest + `parseCustomerCsv, buildCustomersForImport, ParsedCustomerRow` from './customerCsv' + `Customer` type.
  //   The new describe block uses `parseCustomerCsv` and `expect` — both already imported. No new imports needed.
  // Line 10-11: tiny `csv` helper for readable test data — REUSE THIS in the new block.
  // Lines 13-119: existing `describe('parseCustomerCsv (Phase 15.1 — CL-03 / D-09 / D-07)')` block — locked, DO NOT modify.
  // Lines 121-217: existing `describe('buildCustomersForImport (Phase 15.1 — D-11 merge semantics)')` block — locked, DO NOT modify.
  // After line 217 (end of file): append the new SEC-03 describe block here.

The existing `csv` helper signature (line 10-11):

  const csv = (headers: string[], ...rows: string[][]) =>
    [headers.join(','), ...rows.map(r => r.join(','))].join('\n');

This helper joins fields with `,` — for the formula-injection test cases that contain commas (e.g., `=HYPERLINK("https://evil.com","click")` contains a comma between the URL and the link text), the test must EITHER wrap the cell in double-quotes in the raw string OR use a row with the formula in a column that has no neighboring populated fields — but PapaParse needs proper CSV quoting for commas inside cells. The simplest approach: pass the formula in a single-field column AND quote the cell explicitly in the raw row data, e.g. `['"=HYPERLINK(""https://evil.com"",""click"")"', '', '', '', '']` (CSV escaping: a literal `"` inside a quoted field is encoded as `""`).

Verify the CSV-quoting strategy by reading PapaParse documentation if unsure, but the test cases in D-08 are explicit enough that a direct hand-built CSV string is reasonable when the tiny `csv` helper would not produce correct quoting. Pick whichever path keeps the tests readable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append SEC-03 describe block to customerCsv.test.ts</name>
  <files>src/utils/customerCsv.test.ts</files>
  <read_first>
    - src/utils/customerCsv.test.ts (full file — lines 1-217 — verify the file ends at line 217 with the closing `});` of the `buildCustomersForImport` describe block; the new describe is appended below that)
    - src/utils/customerCsv.ts (full file — confirm `validateCustomerRow` at lines 113-153 just `.trim()`s name and notes; the parser already passes through raw characters; the new tests LOCK existing behavior, they do NOT motivate a code change)
    - .planning/phases/21-csv-url-security/21-CONTEXT.md (D-08 enumerates the exact test cases + the comment requirement explaining separation from sanitizeCsvCell)
  </read_first>
  <behavior>
    - Test 1: CSV row with `name = '=HYPERLINK("https://evil.com","click")'` → `parseCustomerCsv` returns 1 row, `customer.name` equals the literal `'=HYPERLINK("https://evil.com","click")'` byte-for-byte (no escaping, no `'` prefix, no truncation). `errors` is empty.
    - Test 2: CSV row with `name = 'safe customer'` and `notes = '+CMD|\' /C calc\'!A0'` → `parseCustomerCsv` returns 1 row, `customer.notes` equals the literal `"+CMD|' /C calc'!A0"` byte-for-byte. `errors` is empty.
    - Test 3a: CSV row with `name = 'Müller'` → `parseCustomerCsv` returns 1 row, `customer.name` equals the literal `'Müller'` byte-for-byte; codePointAt(0) of the result equals codePointAt(0) of the raw input (rules out NFC/NFD normalization). `errors` is empty.
    - Test 3b: CSV row with `name = '张三'` → `parseCustomerCsv` returns 1 row, `customer.name` equals the literal `'张三'` byte-for-byte; `[...customer.name].length === 2` (two CJK ideographs preserved as separate code points). `errors` is empty.
    - Test 4: CSV row with `name = 'Customer'` and `notes = 'Great client 🎉'` → `parseCustomerCsv` returns 1 row, `customer.notes` equals the literal `'Great client 🎉'`; the test includes either `expect(customer.notes).toBe('Great client 🎉')` or an equivalent that proves the emoji surrogate pair survived.
  </behavior>
  <action>
    Append a new `describe('Phase 21 SEC-03 — formula-injection + Unicode pass-through')` block to `src/utils/customerCsv.test.ts` BELOW the existing closing `});` of the `buildCustomersForImport` describe block (around line 217). Do NOT reorganize, rename, or modify the two existing describe blocks (`parseCustomerCsv (Phase 15.1 — CL-03 / D-09 / D-07)` and `buildCustomersForImport (Phase 15.1 — D-11 merge semantics)`).

    Open the new describe block with a leading comment that explicitly states:
    1. This block locks PARSER PASS-THROUGH behavior — character-for-character preservation of raw CSV input.
    2. This block does NOT test `sanitizeCsvCell` — that helper is the EXPORT-path concern, owned by `src/utils/csvHelpers.test.ts` (plan 21-01).
    3. If a future contributor adds escaping logic to `customerCsv.ts` (the PARSER), these tests will fail — preserving round-trip identity.

    Implement the test cases enumerated in `<behavior>`. Each `it` should:
    - Construct a CSV string using either the existing `csv` helper at line 10-11 OR a hand-built quoted string when the input contains commas requiring proper CSV escaping (PapaParse interprets `"` as a quote delimiter inside a field, encoded as `""` per RFC 4180). For the formula-injection case in `name`, the raw cell value `=HYPERLINK("https://evil.com","click")` contains a comma — encode the row as `'"=HYPERLINK(""https://evil.com"",""click"")"'` to keep PapaParse from splitting at the inner comma.
    - Call `parseCustomerCsv(csvString, [])` (empty existingCustomers — these tests are parse-only, not dedup-focused).
    - Assert `result.rows.length === 1` and `result.globalErrors === []`.
    - Assert `result.rows[0].customer` is not null AND `result.rows[0].errors === []`.
    - Assert the specific field (`customer.name` or `customer.notes`) equals the raw expected string via `toBe(...)`.
    - For the Unicode cases: add an additional assertion proving code-point integrity. For `Müller`: `expect(result.rows[0].customer!.name!.codePointAt(0)).toBe('Müller'.codePointAt(0))` — this rules out NFC/NFD normalization changing the ü character class. For `张三`: `expect([...result.rows[0].customer!.name!].length).toBe(2)` — confirms two distinct CJK code points preserved.
    - For the emoji case: `expect(result.rows[0].customer!.notes).toBe('Great client 🎉')` is sufficient (the surrogate pair is preserved as a single grapheme by `toBe` string comparison).

    Reuse the imports already present at the top of the file (`describe, it, expect` from vitest; `parseCustomerCsv` from `./customerCsv`) — DO NOT add new imports. DO NOT add a new `Customer` import or new types — these tests use only `parseCustomerCsv`'s return type.

    DO NOT modify `src/utils/customerCsv.ts` (the parser implementation). The parser already passes through raw characters via `.trim()`-only processing in `validateCustomerRow` (lines 113-153). These tests LOCK existing behavior; they do not motivate a code change. If any of these tests fail on first run, the parser has changed in an unexpected way and the failing case must be investigated as a regression — NOT fixed by sanitizing the parser.

    DO NOT add tests for `sanitizeCsvCell` here — that helper lives in `csvHelpers.ts` and its contract is tested in `csvHelpers.test.ts` (plan 21-01). The separation is intentional and must be preserved.
  </action>
  <verify>
    <automated>npm test -- customerCsv</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/utils/customerCsv.test.ts` contains `describe('Phase 21 SEC-03` (grep: `grep -c "describe('Phase 21 SEC-03" src/utils/customerCsv.test.ts` returns 1)
    - Source: the existing `describe('parseCustomerCsv (Phase 15.1` is still present (grep: `grep -c "describe('parseCustomerCsv (Phase 15.1" src/utils/customerCsv.test.ts` returns 1)
    - Source: the existing `describe('buildCustomersForImport (Phase 15.1` is still present (grep: `grep -c "describe('buildCustomersForImport (Phase 15.1" src/utils/customerCsv.test.ts` returns 1)
    - Source: the new describe block contains at least 4 `it(` invocations covering formula-injection-name, formula-injection-notes, Unicode-name (Müller and/or 张三), emoji-notes (grep: count `it(` lines added after the original line 217; should be ≥4)
    - Source: `src/utils/customerCsv.ts` is UNCHANGED — `git diff --stat src/utils/customerCsv.ts` shows zero changed lines (these tests lock parser behavior; they do not motivate a parser change)
    - Source: `src/utils/csvHelpers.ts` and `src/utils/csvHelpers.test.ts` are UNCHANGED by this plan (plans 21-01 owns those files; this plan's diff scope is `customerCsv.test.ts` only)
    - Source: the new describe block contains a leading comment that mentions both PASS-THROUGH (parser behavior locked) and the separation from `sanitizeCsvCell` (export-side, owned by `csvHelpers.test.ts`) — verifiable by inspecting the new block's opening lines
    - Test: `npm test -- customerCsv` exits 0 with all new tests passing AND all existing Phase 15.1 tests passing
    - Test: `npm test` (full suite) exits 0 — no regressions elsewhere
    - Build: `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>
    A new `describe('Phase 21 SEC-03 — formula-injection + Unicode pass-through')` block is appended to `customerCsv.test.ts` with ≥4 passing tests that lock parser pass-through for formula-injection inputs (in `name` and `notes`) and Unicode inputs (Latin diacritics, CJK ideographs, emoji). The existing Phase 15.1 describe blocks are untouched. The parser implementation in `customerCsv.ts` is unchanged. The full `tsc -b` build is clean and the full test suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External CSV (user-uploaded file) → `parseCustomerCsv` → IndexedDB | Raw CSV cell values cross from an external trust domain (file picked by user, possibly from a third-party export or shared spreadsheet) into the in-process IndexedDB store. |
| IndexedDB → CSV export (future) → External spreadsheet | Plan 21-01 closes this boundary via `sanitizeCsvCell` at the EXPORT path. This plan locks the INVERSE invariant: the IMPORT path must NEVER re-escape values, or round-trip identity breaks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Tampering / Repudiation (CWE-707 — Improper Neutralization regression) | `parseCustomerCsv` in `src/utils/customerCsv.ts` | mitigate (regression lock) | New `describe('Phase 21 SEC-03')` block in `customerCsv.test.ts` locks character-for-character pass-through for formula-injection + Unicode inputs. If a future contributor adds `sanitizeCsvCell` to the parser by mistake (a tempting but wrong "defense in depth" move), these tests fail loudly and surface the regression at PR time. |
| T-21-09 | Information Disclosure (CWE-176 — Improper Handling of Unicode Encoding) | Customer name with CJK characters or emoji corrupted by NFC/NFD normalization or surrogate-pair mishandling | mitigate (regression lock) | Unicode test cases (`Müller`, `张三`, `Great client 🎉`) lock that the parser preserves code points exactly. PapaParse + JS string handling preserves these natively today; this test traps any future tooling change that would silently re-encode. |
| T-21-10 | Tampering | Formula-injection round-trip — value imported via CSV must not be pre-escaped before reaching the export path (would double-prefix on re-export) | mitigate (lock by complement) | The parser does NOT prefix `'` to formula-like inputs. The export path (`sanitizeCsvCell`, plan 21-01) does. If both paths escaped, a round-trip would produce `''=HYPERLINK(...)` (double-prefix). These tests guarantee the parser side stays clean. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies introduced by this plan. `vitest` and `papaparse` are already pinned. |

## Residual Risks (Explicitly Deferred per 21-CONTEXT.md `<deferred>`)

- **Customer CSV export path** — does not exist today (only import). The new tests lock INPUT behavior; output sanitization is by-construction inherited from the universal `sanitizeCsvCell` coverage in `csvHelpers.ts` (plan 21-01) IF and WHEN an `exportCustomersToCsv` function is added in a future phase. 21-CONTEXT.md `<deferred>` records this.
- **Adversarial CSV with embedded `\r\n` or null bytes** — not covered by these test cases. PapaParse handles common edge cases; specific exploit-driven coverage is deferred unless a CVE surfaces.
- **Punycode / IDN homograph attacks** — out of scope. Customer name is a free-text field; treating it as a URL is not a goal.

**ASVS Level 1 mapping:** V8.3.4 (verify that input is preserved without unintended normalization) + V14.4.1 (test coverage for security-sensitive utilities). Severity: MEDIUM (per v1.2-CODE-AUDIT.md finding #15).
</threat_model>

<verification>
- `npm test -- customerCsv` exits 0 — new SEC-03 tests pass alongside the existing 12 Phase 15.1 tests
- `npm test` (full suite) exits 0 — no regressions
- `npx tsc -b` exits 0
- `git diff --stat src/utils/customerCsv.ts` shows zero changed lines (parser is untouched — this is a test-only plan)
- `git diff --stat src/utils/csvHelpers.ts` shows zero changed lines (plan 21-01 owns that file)
- The two existing describe blocks in `customerCsv.test.ts` are byte-identical to before this plan (additive append at end of file only)
</verification>

<success_criteria>
SEC-03 closed: `customerCsv.test.ts` has a new `describe('Phase 21 SEC-03 — formula-injection + Unicode pass-through')` block with ≥4 passing tests that lock parser pass-through for formula-injection and Unicode inputs. ROADMAP Phase 21 Success Criterion #3 satisfied. The parser implementation is unchanged — these tests document and protect existing correct behavior at the regression boundary, and complement plan 21-01's sanitization at the export boundary.
</success_criteria>

<output>
Create `.planning/phases/21-csv-url-security/21-03-SUMMARY.md` when done.
</output>
