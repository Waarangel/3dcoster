---
phase: 22-jobsmanager-decomposition-perf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useCustomerPicker.ts
  - src/hooks/useCustomerPicker.test.ts
autonomous: true
requirements: [HYG-08, HYG-02]
requirements_addressed: [HYG-08, HYG-02]
tags: [react, hook, picker, refactor]

must_haves:
  truths:
    - "useCustomerPicker(customers, { onPick }) compiles and exports from src/hooks/useCustomerPicker.ts (D-01)"
    - "Return shape exposes { query, open, activeIndex, visibleCustomers, filteredCustomers, setQuery, setOpen, setActiveIndex, handleKeyDown, pick, reset } (D-02)"
    - "PICKER_VISIBLE_LIMIT = 8 is exported from useCustomerPicker.ts (HYG-02, D-05)"
    - "handleKeyDown branches cover ArrowDown WR-04 guard, ArrowUp wrap, Enter-with-match, Enter-no-match, Escape stopPropagation (CR-04), Tab close-no-pick (D-03)"
    - "pick(c) invokes onPick then resets query/open/activeIndex internally (D-04)"
    - "Hook is standalone — no JobsManager or PrintQuoteModal changes in this plan (D-25)"
    - "All 27 CONTEXT decisions D-01..D-27 are reachable through this plan + downstream plans"
  artifacts:
    - path: "src/hooks/useCustomerPicker.ts"
      provides: "Reusable customer-picker hook + PICKER_VISIBLE_LIMIT export"
      exports: ["useCustomerPicker", "PICKER_VISIBLE_LIMIT"]
      min_lines: 70
    - path: "src/hooks/useCustomerPicker.test.ts"
      provides: "Hook unit tests using raw createRoot + act (no @testing-library/react)"
      contains: "PickerHarness"
      min_lines: 120
  key_links:
    - from: "src/hooks/useCustomerPicker.ts"
      to: "react"
      via: "useState, useMemo, useCallback imports"
      pattern: "from 'react'"
    - from: "src/hooks/useCustomerPicker.test.ts"
      to: "src/hooks/useCustomerPicker"
      via: "PickerHarness wrapper component"
      pattern: "import.*useCustomerPicker.*from.*useCustomerPicker"
---

<objective>
Create the `useCustomerPicker` hook + colocated test file. This plan is the foundation for plan 22-03 (`<RecordSaleModal>` extraction) and the PrintQuoteModal picker migration. No consumers are touched in this plan — the hook is standalone code that compiles and tests on its own.

Purpose: Close HYG-08 (consolidate picker state triplet + 60-LOC keyDown handler that today duplicates between JobsManager.tsx:1080-1370 and PrintQuoteModal.tsx:69-178) and HYG-02 (centralize PICKER_VISIBLE_LIMIT = 8 — currently duplicated at JobsManager.tsx:993 and PrintQuoteModal.tsx:31).

Output: `src/hooks/useCustomerPicker.ts` + `src/hooks/useCustomerPicker.test.ts`. Both files compile under `tsc -b` and the test passes under `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-jobsmanager-decomposition-perf/22-CONTEXT.md
@.planning/phases/22-jobsmanager-decomposition-perf/22-RESEARCH.md
@.planning/phases/22-jobsmanager-decomposition-perf/22-PATTERNS.md
@.planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md

<interfaces>
<!-- Key types and exports the executor needs. Extracted from codebase. -->

From src/types.ts:
```typescript
export interface Customer {
  id: string;
  name?: string;
  email?: string;
  company?: string;
  address?: string;
  notes?: string;
  // ... plus lastUsedAt and other fields not relevant to picker
}
```

From src/components/JobsManager.tsx:993 (source of PICKER_VISIBLE_LIMIT — DELETE in 22-03):
```typescript
const PICKER_VISIBLE_LIMIT = 8;
```

From src/components/PrintQuoteModal.tsx:31 (duplicate — DELETE in 22-03):
```typescript
const PICKER_VISIBLE_LIMIT = 8;
```

Hook return type (D-02 locked):
```typescript
export function useCustomerPicker(
  customers: Customer[],
  { onPick }: { onPick: (c: Customer) => void }
): {
  query: string;
  open: boolean;
  activeIndex: number;
  visibleCustomers: Customer[];
  filteredCustomers: Customer[];
  setQuery: React.Dispatch<React.SetStateAction<string>>;
  setOpen: React.Dispatch<React.SetStateAction<boolean>>;
  setActiveIndex: React.Dispatch<React.SetStateAction<number>>;
  handleKeyDown: (e: React.KeyboardEvent<HTMLInputElement>) => void;
  pick: (c: Customer) => void;
  reset: () => void;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useCustomerPicker hook with PICKER_VISIBLE_LIMIT export</name>
  <files>src/hooks/useCustomerPicker.ts</files>
  <read_first>
    - src/components/JobsManager.tsx (read lines 990-1000 for PICKER_VISIBLE_LIMIT definition; lines 1080-1100 for state triplet; lines 1275-1300 for filteredCustomers + visibleCustomers memos; lines 1305-1370 for handlePickCustomer + handlePickerKeyDown — these are the source-of-truth bodies to copy)
    - src/components/PrintQuoteModal.tsx (read lines 31, 55-178 for the duplicate picker pattern — confirm it matches JobsManager byte-for-byte except for `pickedExistingCustomerId` which is PrintQuoteModal-only and STAYS OUT of the hook per RESEARCH §Pitfall 1)
    - src/types.ts (read Customer interface — only `id`, `name`, `email` are referenced by the hook filter)
    - src/hooks/useLocalStorage.ts (read for the project hook convention: named export, no default, react imports at top)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-CONTEXT.md (D-01 through D-05 — hook signature, return shape, keyDown contract, PICKER_VISIBLE_LIMIT location)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-PATTERNS.md (read §`src/hooks/useCustomerPicker.ts` for the verbatim handler body and rename rules)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-RESEARCH.md (read §Pitfall 1 — pickedExistingCustomerId MUST NOT leak into the hook)
  </read_first>
  <behavior>
    - Test 1: `useCustomerPicker([], { onPick: vi.fn() })` returns object with all 11 keys: query, open, activeIndex, visibleCustomers, filteredCustomers, setQuery, setOpen, setActiveIndex, handleKeyDown, pick, reset
    - Test 2: filteredCustomers is `[]` when `query.trim()` is empty (D-02; matches JobsManager.tsx:1281)
    - Test 3: filteredCustomers matches by `name` OR `email` substring (case-insensitive); query "ali" matches customer { name: "Alice" } and { email: "ali@example.com" }
    - Test 4: visibleCustomers slices filteredCustomers to PICKER_VISIBLE_LIMIT (=8). With 12 matching customers, visibleCustomers.length === 8
    - Test 5: ArrowDown opens picker (`open === true`) and sets activeIndex=0; second ArrowDown advances activeIndex; wraps with modulo
    - Test 6: ArrowDown when visibleCustomers.length === 0 is a no-op (WR-04 fix — does NOT open empty dropdown)
    - Test 7: ArrowUp wraps last→first (with 3 items, from index 0 goes to index 2)
    - Test 8: Enter while open with active match calls onPick(picked) AND resets `{ query: '', open: false, activeIndex: 0 }`
    - Test 9: Enter while open with non-empty query but no visibleCustomers[activeIndex] closes dropdown WITHOUT calling onPick
    - Test 10: Escape while open closes dropdown AND calls e.stopPropagation() (CR-04 — prevents parent Modal's Escape listener from firing)
    - Test 11: Tab closes dropdown without auto-picking (UI-SPEC §5)
    - Test 12: pick(c) explicitly invokes onPick(c) then resets state to `{ query: '', open: false, activeIndex: 0 }`
    - Test 13: reset() sets `{ query: '', open: false, activeIndex: 0 }` without calling onPick
    - Test 14: `PICKER_VISIBLE_LIMIT === 8` is exported as a named constant
  </behavior>
  <action>
    Create `src/hooks/useCustomerPicker.ts` with two named exports: `PICKER_VISIBLE_LIMIT` (const = 8) and `useCustomerPicker(customers, { onPick })`.

    Imports (exact):
    ```
    import { useState, useMemo, useCallback } from 'react';
    import type { Customer } from '../types';
    ```

    Body structure (per PATTERNS.md §useCustomerPicker hook):
    1. Declare `export const PICKER_VISIBLE_LIMIT = 8;` at module top (D-05).
    2. Inside the hook: three useState calls — `query` (string, ''), `open` (boolean, false), `activeIndex` (number, 0). Rename short (no `customerPicker` prefix from JobsManager — that's the consumer's old local name).
    3. `filteredCustomers = useMemo(...)`: trim+lowercase query; if empty return []; else filter customers by `(c.name || '').toLowerCase().includes(q) || (c.email || '').toLowerCase().includes(q)`. Deps: `[customers, query]`. Copy logic VERBATIM from JobsManager.tsx:1280-1290.
    4. `visibleCustomers = useMemo(() => filteredCustomers.slice(0, PICKER_VISIBLE_LIMIT), [filteredCustomers])`.
    5. `pick = useCallback((c: Customer) => { onPick(c); setQuery(''); setOpen(false); setActiveIndex(0); }, [onPick])` (D-04).
    6. `reset = useCallback(() => { setQuery(''); setOpen(false); setActiveIndex(0); }, [])`.
    7. `handleKeyDown = useCallback((e) => { ... }, [open, activeIndex, query, visibleCustomers, pick])` — port the 60-LOC handler from JobsManager.tsx:1317-1370 VERBATIM but rename `customerPickerOpen`→`open`, `customerPickerActiveIndex`→`activeIndex`, `customerPickerQuery`→`query`, `setCustomerPickerOpen`→`setOpen`, etc. PRESERVE all inline comments (WR-04 fix comment on ArrowDown, WR-01 implicit guard on Enter, CR-04 fix comment on Escape with stopPropagation, UI-SPEC §5 comment on Tab). Replace inline `void handlePickCustomer(picked)` with `pick(picked)`.
    8. Return `{ query, open, activeIndex, visibleCustomers, filteredCustomers, setQuery, setOpen, setActiveIndex, handleKeyDown, pick, reset }` (D-02).

    CRITICAL constraints (per RESEARCH §Pitfall 1):
    - Do NOT add `pickedExistingCustomerId` to the hook. That is a PrintQuoteModal-only consumer concept.
    - Do NOT subscribe to `useCustomers()` inside the hook. Consumers pass `customers` as an argument (D-01).
    - The hook returns BOTH `filteredCustomers` AND `visibleCustomers` — even if only `visibleCustomers` is used by JSX. `filteredCustomers` is needed for the "Showing first 8 of N matches" overflow footer (`{filteredCustomers.length > PICKER_VISIBLE_LIMIT && ...}`). Both must be returned per D-02 verbatim.

    Do NOT modify any other file in this task. JobsManager.tsx and PrintQuoteModal.tsx continue to have their local PICKER_VISIBLE_LIMIT + state — those are deleted in plan 22-03.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | grep -E "useCustomerPicker|error TS" | head -20</automated>
    <automated>grep -c "export const PICKER_VISIBLE_LIMIT = 8" src/hooks/useCustomerPicker.ts</automated>
    <automated>grep -c "export function useCustomerPicker" src/hooks/useCustomerPicker.ts</automated>
    <automated>grep -c "e.stopPropagation()" src/hooks/useCustomerPicker.ts</automated>
    <automated>grep -c "pickedExistingCustomerId" src/hooks/useCustomerPicker.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useCustomerPicker.ts` exists and `tsc -b` exits 0
    - `grep -c 'export const PICKER_VISIBLE_LIMIT = 8' src/hooks/useCustomerPicker.ts` returns `1`
    - `grep -c 'export function useCustomerPicker' src/hooks/useCustomerPicker.ts` returns `1`
    - `grep -c 'e.stopPropagation()' src/hooks/useCustomerPicker.ts` returns `1` (CR-04 fix preserved)
    - `grep -c 'pickedExistingCustomerId' src/hooks/useCustomerPicker.ts` returns `0` (Pitfall 1 — must not leak)
    - `grep -c 'useCustomers' src/hooks/useCustomerPicker.ts` returns `0` (D-01 — hook does NOT subscribe; takes customers as arg)
    - File contains all of: `query`, `open`, `activeIndex`, `setQuery`, `setOpen`, `setActiveIndex`, `handleKeyDown`, `pick`, `reset`, `visibleCustomers`, `filteredCustomers` as returned keys
    - File contains the WR-04 inline comment string `WR-04` and the CR-04 inline comment string `CR-04`
  </acceptance_criteria>
  <done>useCustomerPicker.ts exists, compiles under tsc -b, exports PICKER_VISIBLE_LIMIT + useCustomerPicker, preserves all 5 keyDown branches with proper semantics, and does NOT subscribe to useCustomers or reference pickedExistingCustomerId.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useCustomerPicker.test.ts with raw createRoot + act</name>
  <files>src/hooks/useCustomerPicker.test.ts</files>
  <read_first>
    - src/components/PrintQuoteModal.test.tsx (read lines 1-200 — convention reference: imports, beforeEach/afterEach setup, vi.mock factory, createRoot+act pattern, fireKeyDown helper)
    - src/hooks/useCustomerPicker.ts (the file just created in task 1 — confirm the export surface)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-PATTERNS.md (read §`src/hooks/useCustomerPicker.test.ts` for the PickerHarness pattern)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md (Wave 0 requirements — this file IS the Wave 0 gate for HYG-08)
  </read_first>
  <behavior>
    All 14 test cases from Task 1 must be covered as `it(...)` blocks inside a single `describe('useCustomerPicker', ...)`. Each test uses `act(() => { ... })` for state changes and DOM queries for assertions (no `renderHook`, no `@testing-library/react`).
  </behavior>
  <action>
    Create `src/hooks/useCustomerPicker.test.ts` (note: .ts not .tsx — but the file CONTAINS JSX for the PickerHarness wrapper; rename to `.tsx` if vitest jsdom config requires it. The project uses `.test.tsx` for component tests with JSX — match convention. Final filename: `src/hooks/useCustomerPicker.test.tsx`).

    Wait — re-check: VALIDATION.md and CONTEXT.md both say `useCustomerPicker.test.ts`. Vitest with jsdom CAN parse .ts containing JSX if esbuild is configured to. PrintQuoteModal.test.tsx uses .tsx. Convention here:
    - If the test file contains JSX in PickerHarness (it must, to exercise the keyDown handler in a real DOM), use `.tsx` extension.
    - But CONTEXT.md/VALIDATION.md explicitly say `.test.ts`. If the project's vitest config supports JSX-in-.ts, keep `.ts`. Otherwise rename to `.tsx`.
    - Action: create as `src/hooks/useCustomerPicker.test.ts` first. If `tsc -b` or `npm test` errors on JSX syntax, rename to `.test.tsx` and update plan internal references.

    Imports (exact):
    ```
    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
    import { createRoot, type Root } from 'react-dom/client';
    import { act } from 'react';
    import { useCustomerPicker, PICKER_VISIBLE_LIMIT } from './useCustomerPicker';
    import type { Customer } from '../types';
    ```

    Structure:
    1. Top: declare `PickerHarness({ customers, onPick })` function component that calls the hook and renders a `<div>` with: an `<input data-testid="picker-input">` wired to `onKeyDown={handleKeyDown}`; a `<span data-testid="open">{String(open)}</span>`; a `<span data-testid="active-index">{String(activeIndex)}</span>`; a `<span data-testid="filtered-count">{filteredCustomers.length}</span>`; a `<span data-testid="visible-count">{visibleCustomers.length}</span>`; and a list of `<button data-testid={"option-" + c.id} onClick={() => pick(c)}>{c.name}</button>` mapped from visibleCustomers.
    2. `let container: HTMLDivElement | null = null; let root: Root | null = null;` outside describe.
    3. `beforeEach`: create container, append to document.body, create root.
    4. `afterEach`: unmount root inside act(), remove container.
    5. Helper `function fireKeyDown(input: HTMLElement, key: string) { act(() => { input.dispatchEvent(new KeyboardEvent('keydown', { key, bubbles: true })); }); }` per PATTERNS.md.
    6. Helper `function makeCustomer(overrides: Partial<Customer> & { id: string }): Customer { return { id: overrides.id, name: overrides.name, email: overrides.email, ...overrides }; }`.
    7. `describe('useCustomerPicker', () => { ... })` with the 14 `it(...)` blocks from <behavior>.

    Critical test details:
    - Test 8 (Escape stopPropagation): create a parent `<div>` with `onKeyDown={parentSpy}` that wraps PickerHarness; press Escape on the input — assert `parentSpy` was called 0 times (or, more robustly, assert the event's `defaultPrevented` and `cancelBubble` flags). Simpler approach: spy on `Event.prototype.stopPropagation` and assert it was called when Escape fires while open=true.
    - Test 12 (pick): render PickerHarness with customers, set query to match one, ArrowDown to open, click the `<button data-testid="option-{id}">` — assert onPick was called with that customer AND open is now "false" AND query DOM reflects empty string after re-render.
    - Use `PICKER_VISIBLE_LIMIT` constant directly in Test 4 (do not hardcode 8 again — locks the export).

    Do NOT use `renderHook` from any library. Do NOT import `@testing-library/react`.
  </action>
  <verify>
    <automated>npm test -- useCustomerPicker --run 2>&1 | tail -30</automated>
    <automated>grep -c "@testing-library/react" src/hooks/useCustomerPicker.test.ts src/hooks/useCustomerPicker.test.tsx 2>/dev/null || echo "0"</automated>
    <automated>grep -cE "^\s*it\(" src/hooks/useCustomerPicker.test.ts src/hooks/useCustomerPicker.test.tsx 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- useCustomerPicker --run` exits 0 with all tests passing (no `.skip` or `.todo`)
    - The test file contains 14 or more `it(...)` blocks (covering Tests 1–14 in <behavior>)
    - `grep -c '@testing-library/react'` against the test file returns `0` (project precedent)
    - The test file imports `PICKER_VISIBLE_LIMIT` from './useCustomerPicker' (locks the export)
    - The test file imports `createRoot` from 'react-dom/client' AND `act` from 'react'
    - `tsc -b` exits 0
  </acceptance_criteria>
  <done>useCustomerPicker.test (.ts or .tsx) exists, all 14 tests pass via npm test --run, uses raw createRoot+act pattern, no @testing-library/react.</done>
</task>

</tasks>

<verification>
- `npx tsc -b` exits 0
- `npm test -- useCustomerPicker --run` exits 0 (≥14 tests passing)
- `grep -c 'export const PICKER_VISIBLE_LIMIT' src/hooks/useCustomerPicker.ts` returns `1`
- `grep -c 'export function useCustomerPicker' src/hooks/useCustomerPicker.ts` returns `1`
- JobsManager.tsx and PrintQuoteModal.tsx are UNTOUCHED by this plan (verify: `git diff --stat src/components/JobsManager.tsx src/components/PrintQuoteModal.tsx` returns empty)
- No new dependencies added (`git diff package.json` returns empty)
</verification>

<success_criteria>
- HYG-08 hook foundation in place: useCustomerPicker exists with the full D-02 return shape and verified keyDown semantics
- HYG-02 constant centralized: PICKER_VISIBLE_LIMIT exported from useCustomerPicker.ts (deletion of local copies happens in plan 22-03)
- Wave 0 file `useCustomerPicker.test.(ts|tsx)` created and green per VALIDATION.md
- Zero consumer changes — plan 22-03 will perform the integration
- All commits via `gsd-sdk query commit` helper; no `--no-verify`; `tsc -b` for TypeScript check
</success_criteria>

<output>
Create `.planning/phases/22-jobsmanager-decomposition-perf/22-01-SUMMARY.md` when done, recording: final file paths (incl. whether `.ts` or `.tsx` was used for the test), test count + names, the verified handleKeyDown branch coverage, and any deviations from the planned implementation.
</output>
