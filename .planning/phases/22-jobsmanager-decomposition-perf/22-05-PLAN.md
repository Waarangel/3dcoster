---
phase: 22-jobsmanager-decomposition-perf
plan: 05
type: execute
wave: 3
depends_on: ["22-03", "22-04"]
files_modified:
  - src/components/JobsManager.tsx
  - src/components/RecordSaleModal.tsx
  - src/components/CustomerLibrary.tsx
autonomous: true
requirements: [PERF-01, PERF-02, PERF-03, PERF-04]
requirements_addressed: [PERF-01, PERF-02, PERF-03, PERF-04]
tags: [perf, useMemo, refactor]

must_haves:
  truths:
    - "computeBreakEvenInfo(job, salesByJob) is a pure module-scope function inside JobsManager.tsx; it replaces the in-component getBreakEvenInfo useCallback (PERF-01, D-17)"
    - "breakEvenMap = useMemo(() => new Map(...), [searchedJobs, salesByJob]) is built once at the JobsManager component root before rowProps (PERF-01, D-18)"
    - "All three break-even consumers (rowProps.getBreakEvenInfo, JobCard info prop, non-virtualized branch) flow through breakEvenMap.get(job.id) — no other call to computeBreakEvenInfo survives in render paths (PERF-01)"
    - "rowProps.getBreakEvenInfo remains a function (job) => breakEvenMap.get(job.id)! — JobRowProps shape unchanged (D-19, Pitfall 6)"
    - "calculateMarketplaceFee in RecordSaleModal.tsx is at MODULE scope (not inside the component function) per PERF-03, D-20"
    - "marketplaceFee = useMemo(() => calculateMarketplaceFee(saleQuantity * salePrice, saleMarketplace), [saleQuantity, salePrice, saleMarketplace]) is computed once and used in all three JSX sites (PERF-02, D-21)"
    - "CustomerLibrary.tsx:153 useDynamicRowHeight call gains a `key: searchQuery` argument; searchQuery is the existing local state at CustomerLibrary.tsx:115 (PERF-04, D-22)"
    - "JobsManager.test.tsx existing renderJobCard helper at line 340 continues to work without modification (D-19 preservation)"
  artifacts:
    - path: "src/components/JobsManager.tsx"
      provides: "breakEvenMap memo + computeBreakEvenInfo at module scope"
      contains: "function computeBreakEvenInfo"
    - path: "src/components/RecordSaleModal.tsx"
      provides: "module-scope calculateMarketplaceFee + memoized marketplaceFee"
      contains: "const marketplaceFee = useMemo"
    - path: "src/components/CustomerLibrary.tsx"
      provides: "useDynamicRowHeight with searchQuery key"
      contains: "key: searchQuery"
  key_links:
    - from: "JobsManager.tsx rowProps.getBreakEvenInfo"
      to: "breakEvenMap (useMemo Map)"
      via: "(job) => breakEvenMap.get(job.id)!"
      pattern: "breakEvenMap\\.get"
    - from: "RecordSaleModal.tsx marketplaceFee"
      to: "calculateMarketplaceFee (module-scope)"
      via: "useMemo computation"
      pattern: "useMemo\\(\\(\\) => calculateMarketplaceFee"
    - from: "CustomerLibrary.tsx useDynamicRowHeight"
      to: "searchQuery (useState)"
      via: "key arg in options object"
      pattern: "useDynamicRowHeight\\(\\{[^}]*key:\\s*searchQuery"
---

<objective>
Perf bundle plan — three orthogonal one-shot edits sharing the perf theme. After Wave 2 lands (RecordSaleModal extracted in 22-03, SaleRow extracted in 22-04), this plan:

1. **PERF-01**: Pre-compute `getBreakEvenInfo` into a `useMemo<Map<string, BreakEvenInfo>>` keyed by job id; lift the body to a module-scope pure `computeBreakEvenInfo(job, salesByJob)`. Eliminates the double-compute on every virtualized row render and the all-rows re-render on any sale write. Closes CODE-AUDIT #16 + #29.
2. **PERF-02 + PERF-03**: Hoist `calculateMarketplaceFee` to module scope in `RecordSaleModal.tsx` (no React closures) and collapse the 3 independent in-component calls to a single `useMemo`-cached `const marketplaceFee` consumed by all 3 JSX sites. Closes CODE-AUDIT #17 + #31.
3. **PERF-04**: Add `key: searchQuery` arg to `CustomerLibrary.tsx:153`'s `useDynamicRowHeight` call to invalidate the row-height cache when search filter changes. Mirrors the Phase 15 plan 04 D-05 bi-key pattern. Closes CODE-AUDIT #18.

Plan depends on 22-03 because PERF-02 + PERF-03 edits land INSIDE the new `RecordSaleModal.tsx` (created by 22-03). Wave 3.

Output: 3 file edits. Zero behavioral / visual change. Confirm via tsc -b + full test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-jobsmanager-decomposition-perf/22-CONTEXT.md
@.planning/phases/22-jobsmanager-decomposition-perf/22-RESEARCH.md
@.planning/phases/22-jobsmanager-decomposition-perf/22-PATTERNS.md

<interfaces>
PERF-01 — computeBreakEvenInfo signature (D-17):
```typescript
// Module scope in JobsManager.tsx — pure function
function computeBreakEvenInfo(job: PrintJob, salesByJob: Map<string, Sale[]>): BreakEvenInfo {
  // body from JobsManager.tsx:1180-1209 — reads job.copiesSold/sellingPrice/costPerUnit/modelCost
  // and salesByJob.get(job.id) — no other JobsManager-scope captures (RESEARCH §breakEvenMap dependency set)
}
```

PERF-01 — breakEvenMap useMemo (D-18):
```typescript
const breakEvenMap = useMemo(
  () => new Map(searchedJobs.map(j => [j.id, computeBreakEvenInfo(j, salesByJob)])),
  [searchedJobs, salesByJob]
);
```
Placed AFTER salesByJob and searchedJobs are defined; BEFORE rowProps.

PERF-01 — rowProps.getBreakEvenInfo wrapper (D-19, Pitfall 6):
```typescript
// In rowProps object — function signature unchanged so JobRowProps stays the same
getBreakEvenInfo: (job: PrintJob) => breakEvenMap.get(job.id)!,
```

PERF-03 — calculateMarketplaceFee at module scope in RecordSaleModal.tsx (D-20):
```typescript
function calculateMarketplaceFee(price: number, marketplace: MarketplaceType): number {
  switch (marketplace) {
    case 'facebook_shipped':
      return Math.max(0.80, price * 0.10) + price * 0.029;
    case 'etsy':
      return price * 0.065 + price * 0.03 + 0.45;
    default:
      return 0;
  }
}
```
Pure function. No React closures. (Body is JobsManager.tsx:1157-1165 verbatim — verify the exact constants/percentages against the original before moving.)

PERF-02 — marketplaceFee memoized inside RecordSaleModal (D-21):
```typescript
const marketplaceFee = useMemo(
  () => calculateMarketplaceFee(saleQuantity * salePrice, saleMarketplace),
  [saleQuantity, salePrice, saleMarketplace]
);
```
All 3 JSX sites that previously called `calculateMarketplaceFee(...)` independently now read this `marketplaceFee` const.

PERF-04 — CustomerLibrary line 153 (D-22):
```typescript
// BEFORE
const customerRowHeightCache = useDynamicRowHeight({ defaultRowHeight: 88 });
// AFTER
const customerRowHeightCache = useDynamicRowHeight({ defaultRowHeight: 88, key: searchQuery });
```
`searchQuery` confirmed as the local state variable at CustomerLibrary.tsx:115 (RESEARCH §D-22).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement breakEvenMap memo + lift computeBreakEvenInfo to module scope (PERF-01)</name>
  <files>src/components/JobsManager.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx (read lines 1-30 imports; lines 1180-1210 for getBreakEvenInfo useCallback body — THE EXTRACTION SOURCE; lines surrounding salesByJob and searchedJobs definitions to find the insertion point for breakEvenMap; lines 881 for JobCard usage; line 1806 for non-virtualized branch; rowProps construction site for getBreakEvenInfo wiring)
    - src/types.ts (BreakEvenInfo, PrintJob, Sale)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-CONTEXT.md (D-17, D-18, D-19)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-PATTERNS.md (§`src/components/JobsManager.tsx` — breakEvenMap section)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-RESEARCH.md (§breakEvenMap dependency set + §Pitfall 6)
  </read_first>
  <action>
    Edit `src/components/JobsManager.tsx`. Three coordinated edits in this single task.

    Step 1 — Add module-scope `computeBreakEvenInfo` pure function (D-17):
    - Locate a good module-scope position (after imports but before any component definition; or near `<QuoteRow>` at line 149 if that area has other module-scope helpers).
    - Add the function:
      ```
      // Lifted from in-component getBreakEvenInfo useCallback (PERF-01, D-17).
      // Pure function — no React closures. Takes salesByJob Map explicitly so
      // it can be called from a useMemo without capturing component-scope state.
      // No 2nd consumer today — stays at module scope per No-Analog rule.
      function computeBreakEvenInfo(
        job: PrintJob,
        salesByJob: Map<string, Sale[]>
      ): BreakEvenInfo {
        // BODY: copy from JobsManager.tsx:1181-1209 VERBATIM.
        // Replace `salesByJob.get(job.id)` reference (already correct).
        // Replace any closure-captured `userCurrency` / `shippingConfig` / other vars
        //   — RESEARCH confirmed there are NONE; body reads only job + salesByJob.
        // Return BreakEvenInfo object.
      }
      ```
      Verify against the actual current body of getBreakEvenInfo before moving. If the body has any non-obvious captures (e.g., `shippingConfig`, `userCurrency`), those become additional parameters — flag this in the SUMMARY and update the signature accordingly. RESEARCH says there are none.

    Step 2 — Delete the in-component `getBreakEvenInfo` useCallback (~lines 1180-1210). It is replaced by the module-scope function + the breakEvenMap memo.

    Step 3 — Insert `breakEvenMap` useMemo at the JobsManager component root, AFTER `salesByJob` and `searchedJobs` are defined, BEFORE `rowProps`:
       ```
       const breakEvenMap = useMemo(
         () => new Map(searchedJobs.map(j => [j.id, computeBreakEvenInfo(j, salesByJob)])),
         [searchedJobs, salesByJob]
       );
       ```
       Locate the existing definitions of `salesByJob` and `searchedJobs` (use grep to find them). The order is enforced because `breakEvenMap` depends on both. Place it immediately after the later of the two declarations.

    Step 4 — Update the three consumer sites:
       a. `rowProps.getBreakEvenInfo` (D-19 — function wrapper, NOT a Map):
          ```
          getBreakEvenInfo: (job: PrintJob) => breakEvenMap.get(job.id)!,
          ```
          Find the rowProps construction in JobsManager. The `getBreakEvenInfo` field today probably reads `getBreakEvenInfo: getBreakEvenInfo` or `getBreakEvenInfo` (shorthand). Replace with the arrow-fn wrapper above. JobRowProps interface stays unchanged.
       b. JobCard `info={getBreakEvenInfo(job)}` call at ~line 881:
          - If JobCard receives `getBreakEvenInfo` as a prop today, it now receives the same prop (now an arrow fn closing over the Map). Verify: the call site `info={getBreakEvenInfo(job)}` still works because `getBreakEvenInfo(job)` returns `breakEvenMap.get(job.id)!` which is the same BreakEvenInfo object.
          - If JobsManager passes `getBreakEvenInfo` directly through to JobCard (likely), no change needed at the call site.
       c. Non-virtualized branch at ~line 1806: locate the inline `getBreakEvenInfo(job)` call and update if needed (likely it now references the rowProps `getBreakEvenInfo` field or computes directly; verify and update to use `breakEvenMap.get(job.id)!` directly or via the function wrapper — choose whichever requires fewer surrounding edits).

    Step 5 — Final assertion: no other call to `computeBreakEvenInfo` exists in render paths. Specifically, no `.map(j => computeBreakEvenInfo(j, ...))` outside the breakEvenMap useMemo. Grep: `grep -c 'computeBreakEvenInfo(' src/components/JobsManager.tsx` should return exactly `2` (the function definition + the call inside `breakEvenMap = useMemo(...)`).

    Step 6 — Verify `npx tsc -b` exits 0 and `npm test -- JobsManager --run` exits 0. The existing `JobsManager.test.tsx:340-382` `renderJobCard` helper passes `info` as a direct prop (not `getBreakEvenInfo`), so it survives D-19 unchanged.
  </action>
  <verify>
    <automated>grep -c "^function computeBreakEvenInfo" src/components/JobsManager.tsx</automated>
    <automated>grep -c "const breakEvenMap = useMemo" src/components/JobsManager.tsx</automated>
    <automated>grep -c "breakEvenMap.get" src/components/JobsManager.tsx</automated>
    <automated>grep -c "computeBreakEvenInfo(" src/components/JobsManager.tsx</automated>
    <automated>grep -c "const getBreakEvenInfo = useCallback" src/components/JobsManager.tsx</automated>
    <automated>npx tsc -b 2>&1 | grep -E "error TS" | head -10</automated>
    <automated>npm test -- JobsManager --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^function computeBreakEvenInfo' src/components/JobsManager.tsx` returns `1` (module-scope function present)
    - `grep -c 'const breakEvenMap = useMemo' src/components/JobsManager.tsx` returns `1`
    - `grep -c 'breakEvenMap.get' src/components/JobsManager.tsx` returns at least `1`
    - `grep -c 'computeBreakEvenInfo(' src/components/JobsManager.tsx` returns exactly `2` (definition + breakEvenMap memo's `.map(j => computeBreakEvenInfo(j, salesByJob))`)
    - `grep -c 'const getBreakEvenInfo = useCallback' src/components/JobsManager.tsx` returns `0` (old useCallback deleted)
    - `npx tsc -b` exits 0
    - `npm test -- JobsManager --run` exits 0
  </acceptance_criteria>
  <done>computeBreakEvenInfo lives at module scope; breakEvenMap useMemo built once per render; all 3 consumers route through breakEvenMap.get; JobRowProps unchanged per D-19 / Pitfall 6.</done>
</task>

<task type="auto">
  <name>Task 2: Hoist calculateMarketplaceFee + memoize marketplaceFee in RecordSaleModal (PERF-02 + PERF-03)</name>
  <files>src/components/RecordSaleModal.tsx</files>
  <read_first>
    - src/components/RecordSaleModal.tsx (the file created by plan 22-03 — verify calculateMarketplaceFee location: per plan 22-03 it was moved verbatim WITH the modal but may still be defined inside the component. This plan's job is to confirm it's at module scope AND wrap usage in useMemo)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-CONTEXT.md (D-20, D-21)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-PATTERNS.md (§`src/components/RecordSaleModal.tsx` — calculateMarketplaceFee at module scope, marketplaceFee useMemo)
    - src/types.ts (MarketplaceType — confirm the enum values facebook_shipped, etsy, and any default)
  </read_first>
  <action>
    Edit `src/components/RecordSaleModal.tsx`. Two coordinated changes:

    Step 1 (PERF-03) — Confirm `calculateMarketplaceFee` is at MODULE scope (above the RecordSaleModal function definition), NOT inside the component:
    - Locate the function. If plan 22-03 already placed it at module scope (per PATTERNS.md guidance), verify with `grep -n 'function calculateMarketplaceFee' src/components/RecordSaleModal.tsx` returns a line BEFORE `export function RecordSaleModal`.
    - If it is still inside the component (e.g., as a `const calculateMarketplaceFee = (...) => {...}`), MOVE it to module scope:
      ```
      // PERF-03: module-scope pure function — no React closures.
      // No 2nd consumer today; stays here per No-Analog rule.
      function calculateMarketplaceFee(price: number, marketplace: MarketplaceType): number {
        // body unchanged from JobsManager.tsx:1157-1165
        switch (marketplace) {
          case 'facebook_shipped':
            return Math.max(0.80, price * 0.10) + price * 0.029;
          case 'etsy':
            return price * 0.065 + price * 0.03 + 0.45;
          default:
            return 0;
        }
      }
      ```
      VERIFY the body against the actual current JobsManager.tsx:1157-1165 contents before writing — exact percentages and constants must match. If the live code uses different numbers, use the live numbers.

    Step 2 (PERF-02) — Inside `RecordSaleModal`'s component body, add a single memoized `marketplaceFee`:
       ```
       const marketplaceFee = useMemo(
         () => calculateMarketplaceFee(saleQuantity * salePrice, saleMarketplace),
         [saleQuantity, salePrice, saleMarketplace]
       );
       ```
       Place it AFTER the form-state useState declarations and BEFORE the JSX return. (`useMemo` must be imported from 'react' — verify the existing import line at the top.)

    Step 3 — Update the 3 JSX call sites that currently call `calculateMarketplaceFee(...)` independently. From CONTEXT and RESEARCH, those correspond to JobsManager.tsx lines 2061, 2064, 2070 originally — which moved into RecordSaleModal.tsx. Locate them with `grep -n 'calculateMarketplaceFee(' src/components/RecordSaleModal.tsx`. Each call should be replaced with the const reference `marketplaceFee`:
       - Site 1 (saleTotal computation): `calculateMarketplaceFee(saleQuantity * salePrice, saleMarketplace)` → `marketplaceFee`
       - Site 2 (marketplace fee row): same replacement
       - Site 3 (net revenue computation): same replacement

       After replacement, `grep -c 'calculateMarketplaceFee(' src/components/RecordSaleModal.tsx` should return exactly `2`: the function definition + the call inside useMemo.

    Step 4 — Verify:
    - `npx tsc -b` exits 0.
    - `npm test -- RecordSaleModal --run` exits 0 (3-mode tests still pass; marketplaceFee value used internally produces identical output).
    - `grep -c '^function calculateMarketplaceFee' src/components/RecordSaleModal.tsx` returns `1` (module-scope confirmed by `^` line anchor).
  </action>
  <verify>
    <automated>grep -c "^function calculateMarketplaceFee" src/components/RecordSaleModal.tsx</automated>
    <automated>grep -c "const marketplaceFee = useMemo" src/components/RecordSaleModal.tsx</automated>
    <automated>grep -c "calculateMarketplaceFee(" src/components/RecordSaleModal.tsx</automated>
    <automated>npx tsc -b 2>&1 | grep -E "error TS" | head -5</automated>
    <automated>npm test -- RecordSaleModal --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^function calculateMarketplaceFee' src/components/RecordSaleModal.tsx` returns `1` (module-scope — line starts with `function`)
    - `grep -c 'const marketplaceFee = useMemo' src/components/RecordSaleModal.tsx` returns `1`
    - `grep -c 'calculateMarketplaceFee(' src/components/RecordSaleModal.tsx` returns exactly `2` (definition signature + useMemo body)
    - All 3 previous JSX call sites now read `marketplaceFee` instead of calling the function inline — verify by grep+sed inspection that no JSX context has a bare `calculateMarketplaceFee(` outside the useMemo body
    - `npx tsc -b` exits 0
    - `npm test -- RecordSaleModal --run` exits 0
  </acceptance_criteria>
  <done>calculateMarketplaceFee at module scope; marketplaceFee memoized once per render; all 3 JSX consumers read the const; RecordSaleModal tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add key: searchQuery to CustomerLibrary useDynamicRowHeight (PERF-04)</name>
  <files>src/components/CustomerLibrary.tsx</files>
  <read_first>
    - src/components/CustomerLibrary.tsx (read lines 110-160 — confirm searchQuery state at line 115 and useDynamicRowHeight call at line 153)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-CONTEXT.md (D-22)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-PATTERNS.md (§`src/components/CustomerLibrary.tsx` — PERF-04 one-line change)
    - src/components/JobsManager.tsx (read around line 1666 — the precedent bi-key useDynamicRowHeight call from Phase 15 plan 04 D-05, for confirmation of the API shape)
  </read_first>
  <action>
    Edit `src/components/CustomerLibrary.tsx`. Single one-line change.

    Step 1 — Locate the existing `useDynamicRowHeight` call at line 153 (or thereabouts):
       ```
       const customerRowHeightCache = useDynamicRowHeight({ defaultRowHeight: 88 });
       ```
       (Variable name may differ — find via `grep -n 'useDynamicRowHeight' src/components/CustomerLibrary.tsx`.)

    Step 2 — Change to:
       ```
       const customerRowHeightCache = useDynamicRowHeight({ defaultRowHeight: 88, key: searchQuery });
       ```
       `searchQuery` is the local state declared at line 115 (`const [searchQuery, setSearchQuery] = useState('');` — confirmed in RESEARCH §D-22).

    Step 3 — Verify no other changes needed:
    - `npx tsc -b` exits 0.
    - `npm test -- CustomerLibrary --run` exits 0 if a test exists; if not, just verify the file compiles.
    - Manual smoke: search-filtering customer list should now correctly invalidate row heights (row heights of newly-visible search results computed afresh, not reusing stale heights for different customers).
  </action>
  <verify>
    <automated>grep -cE "useDynamicRowHeight\(\{[^}]*key:\s*searchQuery" src/components/CustomerLibrary.tsx</automated>
    <automated>grep -c "useDynamicRowHeight" src/components/CustomerLibrary.tsx</automated>
    <automated>npx tsc -b 2>&1 | grep -E "CustomerLibrary|error TS" | head -5</automated>
    <automated>npm test -- CustomerLibrary --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "useDynamicRowHeight\(\{[^}]*key:\s*searchQuery" src/components/CustomerLibrary.tsx` returns `1`
    - `grep -c 'useDynamicRowHeight' src/components/CustomerLibrary.tsx` returns exactly `2` (1 import or hook source reference + 1 call site — verify with grep that the count matches the existing file structure; if only 1 call, return value is `1`)
    - `npx tsc -b` exits 0
    - Existing tests pass (`npm test -- CustomerLibrary --run` exits 0, OR `npm test --run` if no CustomerLibrary-specific test exists)
  </acceptance_criteria>
  <done>useDynamicRowHeight in CustomerLibrary.tsx receives `key: searchQuery`; tsc -b passes; no behavioral regression in search-filter row heights.</done>
</task>

</tasks>

<verification>
- `npx tsc -b` exits 0
- `npm test --run` exits 0 (full suite — JobsManager, RecordSaleModal, CustomerLibrary tests all green)
- `grep -c 'breakEvenMap' src/components/JobsManager.tsx` returns at least `2` (declaration + consumer)
- `grep -c 'marketplaceFee' src/components/RecordSaleModal.tsx` returns at least `4` (declaration + 3 JSX consumers)
- `grep -c 'key: searchQuery' src/components/CustomerLibrary.tsx` returns at least `1`
- Manual smoke (port 4173):
  - JobsManager: open list of jobs; expand/collapse a job's break-even pill — values match pre-Phase-22 behavior (Validation §Manual-Only)
  - RecordSaleModal: open Record Sale; type into Price field; Etsy fee row in real-time recomputes correctly
  - CustomerLibrary: type into search; row heights compute correctly for the filtered set (no stale-height visual artifacts)
</verification>

<success_criteria>
- PERF-01 closed: computeBreakEvenInfo at module scope; breakEvenMap built once per render; O(1) lookup at all 3 consumer sites; JobRowProps shape unchanged
- PERF-02 closed: marketplaceFee computed exactly once per RecordSaleModal render (verifiable via call-count test if needed; minimally proven by grep of remaining `calculateMarketplaceFee(` invocations)
- PERF-03 closed: calculateMarketplaceFee at module scope; no React closures
- PERF-04 closed: CustomerLibrary's useDynamicRowHeight invalidates on searchQuery change
- All commits via `gsd-sdk query commit`; no `--no-verify`; `tsc -b` typecheck
</success_criteria>

<output>
Create `.planning/phases/22-jobsmanager-decomposition-perf/22-05-SUMMARY.md` when done, recording: final position of computeBreakEvenInfo (module scope in JobsManager.tsx — confirm line number), the breakEvenMap useMemo dependency array (`[searchedJobs, salesByJob]`), the calculateMarketplaceFee body's exact constants/percentages, the 3 JSX consumer sites in RecordSaleModal that now read `marketplaceFee`, and the exact CustomerLibrary line number where the `key: searchQuery` arg was added.
</output>
