---
phase: 22-jobsmanager-decomposition-perf
plan: 06
type: execute
wave: 4
depends_on: ["22-03", "22-05"]
files_modified:
  - src/hooks/useDatabase.ts
  - src/components/JobsManager.tsx
  - src/components/JobsManager.test.tsx
  - .planning/ROADMAP.md
autonomous: false
requirements: [PERF-07]
requirements_addressed: [PERF-07]
tags: [perf, hook, refactor, dexie, useLiveQuery]

must_haves:
  truths:
    - "src/hooks/useDatabase.ts gains a new exported function useAllSales(): Sale[] inserted immediately after useSales(jobId?) per D-23(a)"
    - "useAllSales wraps useLiveQuery(() => db.sales.orderBy('soldAt').reverse().toArray(), []) and returns the array (?? []) — shape mirrors useCustomers"
    - "JobsManager.tsx drops the duplicate useSales() call at line 1071 (`const { sales: allSales } = useSales();`); replaced with `const allSales = useAllSales();`"
    - "JobsManager.tsx import line for useDatabase now includes useAllSales"
    - "JobsManager.test.tsx vi.mock factory for '../hooks/useDatabase' at line 19 adds `useAllSales: () => []` (plain array, NOT { sales: [] }) per D-24 + Pitfall 4"
    - "Cumulative end-of-phase verdict: `wc -l src/components/JobsManager.tsx` returns < 1500 (ROADMAP success criterion #2 — enforced here because plans 22-03/22-04/22-05/22-06 collectively reduce JobsManager.tsx; only this final plan can prove the aggregate)"
    - "ROADMAP.md Phase 22 success criterion #1 RecordSaleModal prop list updated to the actual D-06 signature ({ job, userProfile, userCurrency, shippingConfig, editingSale, convertingFromQuote, isOpen, onClose, onSaved? }) — the stale 'customers, customersByEmail' entries are removed"
    - "Manual UAT confirms break-even pill values + revenue numbers match across the JobsManager surface (Validation §Manual-Only — PERF-01 visual lock)"
    - "Test re-mount via npm test -- JobsManager succeeds with the updated mock"
  artifacts:
    - path: "src/hooks/useDatabase.ts"
      provides: "New exported useAllSales hook"
      exports: ["useAllSales"]
      contains: "function useAllSales"
    - path: "src/components/JobsManager.tsx"
      provides: "useAllSales consumer; single global sales subscription; final size < 1500 lines"
      contains: "useAllSales"
    - path: "src/components/JobsManager.test.tsx"
      provides: "Updated mock factory exposing useAllSales"
      contains: "useAllSales: () => []"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 22 success criterion #1 prop list synced to D-06 implemented signature"
      contains: "editingSale, convertingFromQuote, isOpen, onClose"
  key_links:
    - from: "src/hooks/useDatabase.ts"
      to: "dexie-react-hooks useLiveQuery"
      via: "useLiveQuery(() => db.sales.orderBy('soldAt').reverse().toArray(), [])"
      pattern: "useLiveQuery"
    - from: "src/components/JobsManager.tsx"
      to: "src/hooks/useDatabase useAllSales"
      via: "named import + call"
      pattern: "useAllSales"
    - from: "src/components/JobsManager.test.tsx"
      to: "vi.mock useDatabase factory"
      via: "useAllSales mock returning []"
      pattern: "useAllSales:\\s*\\(\\)\\s*=>\\s*\\[\\]"
---

<objective>
Final plan in Phase 22. Lifts the duplicate global `useSales()` subscription out of JobsManager into a dedicated `useAllSales(): Sale[]` hook in `useDatabase.ts`, drops the duplicate call from JobsManager, updates the JobsManager test mock to expose the new hook, AND closes out two phase-level housekeeping items: (a) verifies JobsManager.tsx final cumulative line count is below ROADMAP's <1500 target, (b) syncs ROADMAP.md's stale RecordSaleModal prop list with the actually-implemented D-06 signature.

Purpose: Close PERF-07 (CODE-AUDIT #30 — `useSales()` global call duplicated alongside scoped). After this plan, JobsManager has exactly one scoped `useSales(selectedJobId)` subscription AND one explicit `useAllSales()` subscription for the global sales array. The intent is no longer hidden behind a destructure-rename (`{ sales: allSales } = useSales()`) — it's an explicitly-named hook.

Marked `autonomous: false` because the success criteria require a manual UAT (Validation §Manual-Only #2: break-even pill values must visually match pre-Phase-22 behavior).

Plan depends on 22-03 (RecordSaleModal extracted; reduces JobsManager.tsx churn) and 22-05 (breakEvenMap landed; consumers stable). Wave 4 (sequential after Wave 3 because of the JobsManager.tsx file-ownership conflict with 22-05).

Output: 3 source-file edits + 1 ROADMAP.md update + manual UAT checkpoint + cumulative-line-count assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-jobsmanager-decomposition-perf/22-CONTEXT.md
@.planning/phases/22-jobsmanager-decomposition-perf/22-RESEARCH.md
@.planning/phases/22-jobsmanager-decomposition-perf/22-PATTERNS.md
@.planning/phases/22-jobsmanager-decomposition-perf/22-VALIDATION.md

<interfaces>
useAllSales contract (D-23(a) confirmed):
```typescript
export function useAllSales(): Sale[] {
  const sales = useLiveQuery(
    () => db.sales.orderBy('soldAt').reverse().toArray(),
    []
  );
  return sales ?? [];
}
```

Insertion point in useDatabase.ts: immediately AFTER the closing `}` of `useSales(jobId?)` (around line 650 per RESEARCH, but verify in source as line numbers may have shifted with v1.3 phase work). Before useCustomers function.

Existing useSales signature (src/hooks/useDatabase.ts:569+ — UNCHANGED in this plan):
```typescript
export function useSales(jobId?: string) {
  const sales = useLiveQuery(
    () => jobId
      ? db.sales.where('jobId').equals(jobId).toArray()
      : db.sales.orderBy('soldAt').reverse().toArray(),
    [jobId]
  );
  // ...
  return { sales: sales ?? [], addSale, updateSale, deleteSale, getTotals };
}
```

JobsManager.tsx target:
```typescript
// BEFORE (lines 1070-1071):
const { sales, addSale, updateSale, deleteSale } = useSales(selectedJobId || undefined);
const { sales: allSales } = useSales();  // global call — TARGET

// AFTER:
const { sales, addSale, updateSale, deleteSale } = useSales(selectedJobId || undefined);
const allSales = useAllSales();  // explicit, plain array return
```

JobsManager.test.tsx mock factory addition (D-24):
```typescript
// Add this entry to the vi.mock('../hooks/useDatabase', ...) factory at line 19:
useAllSales: () => [],  // plain array — DIFFERENT shape from useSales: () => ({...})
```

ROADMAP.md Phase 22 success criterion #1 — current stale text mentions `customers, customersByEmail` as RecordSaleModal props. The actually-implemented D-06 signature has NO such props (RecordSaleModal subscribes internally via useCustomers). Target replacement text:
```
RecordSaleModal extracted with implemented props: { job, userProfile, userCurrency, shippingConfig, editingSale, convertingFromQuote, isOpen, onClose, onSaved? }
```
(Verify the exact line/heading wording when editing ROADMAP.md; this is a documentation correction, not a behavioral change.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useAllSales hook to useDatabase.ts</name>
  <files>src/hooks/useDatabase.ts</files>
  <read_first>
    - src/hooks/useDatabase.ts (read lines 1-30 for imports — confirm useLiveQuery + db are imported; lines 569-660 for useSales function + opener of useCustomers — find the exact insertion point immediately after useSales' closing `}`)
    - src/types.ts (Sale type)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-CONTEXT.md (D-23)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-RESEARCH.md (§D-23 + §Pattern 4: useAllSales)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-PATTERNS.md (§`src/hooks/useDatabase.ts` — add useAllSales)
  </read_first>
  <action>
    Edit `src/hooks/useDatabase.ts`. Single addition: insert a new exported function `useAllSales` immediately after `useSales`.

    Step 1 — Locate the closing `}` of `useSales(jobId?)`. This is the function defined around line 569 that returns `{ sales: sales ?? [], addSale, updateSale, deleteSale, getTotals }`. Its closing brace is approximately line 650 per RESEARCH (line numbers may have shifted).

    Step 2 — Confirm the next function below is `useCustomers` (opens ~line 654 per RESEARCH).

    Step 3 — Insert the following block in the gap between `useSales` closing `}` and the `// Hook for customer library` comment (or whatever the comment block above useCustomers reads):
       ```
       /**
        * Hook for reading all sales globally.
        *
        * Used by JobsManager to build the salesByJob Map. Separate from
        * useSales(jobId) which exposes addSale/updateSale/deleteSale for a
        * scoped subscription on a single job.
        *
        * Returns Sale[] directly (not { sales: [...] }) to make intent clear
        * and simplify mocking: `useAllSales: () => []`.
        *
        * PERF-07: closes the global-vs-scoped useSales duplication that today
        * lives at JobsManager.tsx:1070-1071.
        */
       export function useAllSales(): Sale[] {
         const sales = useLiveQuery(
           () => db.sales.orderBy('soldAt').reverse().toArray(),
           []
         );
         return sales ?? [];
       }
       ```
       The body MUST mirror the global branch of `useSales(jobId?)` when `jobId` is undefined (the `else` branch): `db.sales.orderBy('soldAt').reverse().toArray()`. Empty dep array `[]` — never re-runs because db.sales liveQuery already subscribes to changes.

    Step 4 — Verify imports at the top of `useDatabase.ts`:
    - `useLiveQuery` from 'dexie-react-hooks' — should already be imported (used by useSales). Confirm with `grep -n "useLiveQuery" src/hooks/useDatabase.ts | head -1`.
    - `db` from '../db/database' — should already be imported.
    - `Sale` from '../types' — should already be imported (used by useSales return type).
    - If any is missing, add it.

    Step 5 — Do NOT modify any other function in useDatabase.ts. The existing `useSales(jobId?)` keeps its full body and full return shape unchanged (still exposes `addSale`/`updateSale`/`deleteSale`/`getTotals` for both scoped and global callers — but PERF-07 only addresses the global-call duplication; the global useSales() can theoretically still be called by other consumers if any exist).

    Step 6 — Grep for other consumers of `useSales()` (no-arg) across `src/` — `grep -rn "useSales()" src/ --include='*.ts' --include='*.tsx'`. If JobsManager is the ONLY no-arg consumer, this plan's PERF-07 closure is complete after Task 2 also removes it. If other files also call `useSales()` no-arg, document them in the SUMMARY but do NOT migrate them in this plan (out of scope — those consumers might want addSale/etc. and need separate consideration). RESEARCH and the codebase audit suggest JobsManager is the sole no-arg consumer.

    Step 7 — Verify `npx tsc -b` exits 0.
  </action>
  <verify>
    <automated>grep -c "^export function useAllSales" src/hooks/useDatabase.ts</automated>
    <automated>grep -c "function useAllSales(): Sale\[\]" src/hooks/useDatabase.ts</automated>
    <automated>grep -c "db.sales.orderBy('soldAt').reverse().toArray()" src/hooks/useDatabase.ts</automated>
    <automated>npx tsc -b 2>&1 | grep -E "useDatabase|error TS" | head -10</automated>
    <automated>grep -rn "useSales()" src/ --include='*.ts' --include='*.tsx' | grep -v "useSales(jobId" | grep -v "useSales(selectedJob" | grep -v "useSales(job.id"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export function useAllSales' src/hooks/useDatabase.ts` returns `1`
    - `grep -c "function useAllSales(): Sale\\[\\]" src/hooks/useDatabase.ts` returns `1` (explicit return type matches D-23(a))
    - `grep -c "db.sales.orderBy('soldAt').reverse().toArray()" src/hooks/useDatabase.ts` returns at least `2` (one in useSales' global branch + one in useAllSales)
    - useAllSales is inserted AFTER useSales (verify visually with `grep -n 'export function use\(Sales\|AllSales\|Customers\)' src/hooks/useDatabase.ts` — order must be useSales, useAllSales, useCustomers)
    - `npx tsc -b` exits 0
    - No other no-arg `useSales()` consumer exists in src/ besides JobsManager.tsx (verify; if any exist, document in SUMMARY but do NOT touch them in this plan)
  </acceptance_criteria>
  <done>useAllSales exported from useDatabase.ts, returns Sale[] directly, inserted right after useSales; tsc -b passes; no other no-arg useSales callers exist outside JobsManager.</done>
</task>

<task type="auto">
  <name>Task 2: Replace duplicate useSales() in JobsManager + update test mock + assert cumulative <1500 line target</name>
  <files>src/components/JobsManager.tsx, src/components/JobsManager.test.tsx</files>
  <read_first>
    - src/components/JobsManager.tsx (read lines 1-30 for the useDatabase import; lines 1065-1080 for the two useSales calls — the scoped one stays, the global one moves to useAllSales)
    - src/components/JobsManager.test.tsx (read lines 1-100 — confirm the vi.mock factory at line 19 shape and the existing useSales mock at lines 47-52)
    - src/hooks/useDatabase.ts (just-updated — confirm useAllSales export)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-CONTEXT.md (D-23, D-24)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-RESEARCH.md (§D-24 + §Pitfall 4 — mock must return plain array not object)
    - .planning/ROADMAP.md (Phase 22 success criterion #2 — confirms target: JobsManager.tsx < 1500 lines)
  </read_first>
  <action>
    Edit `src/components/JobsManager.tsx` AND `src/components/JobsManager.test.tsx` together (single atomic commit per D-27 — both must land together or the test breaks).

    **JobsManager.tsx edits:**

    Step 1 — Update the useDatabase import. Locate the existing line like `import { useSales, useCustomers, ... } from '../hooks/useDatabase';` and add `useAllSales`:
       ```
       import { useSales, useAllSales, useCustomers, ... } from '../hooks/useDatabase';
       ```
       (Keep all other named imports in the same import statement.)

    Step 2 — Replace the duplicate useSales call. Locate the two consecutive calls around lines 1070-1071:
       ```
       // BEFORE:
       const { sales, addSale, updateSale, deleteSale } = useSales(selectedJobId || undefined);
       const { sales: allSales } = useSales();   // ← target for removal
       ```
       Change the second line to:
       ```
       // AFTER:
       const { sales, addSale, updateSale, deleteSale } = useSales(selectedJobId || undefined);
       const allSales = useAllSales();
       ```
       The variable name `allSales` is preserved — every existing consumer of `allSales` elsewhere in JobsManager (e.g., the `salesByJob` useMemo construction at lines 1088-1096) works without modification.

    Step 3 — Verify no other `useSales()` no-arg call exists in JobsManager.tsx:
       `grep -n 'useSales()' src/components/JobsManager.tsx` should return 0 results (the scoped call has `selectedJobId || undefined` as its argument and won't match the no-arg pattern).

    Step 4 — **Cumulative ROADMAP line target assertion.** After making the above edit, run:
       ```
       wc -l src/components/JobsManager.tsx
       ```
       The result MUST be strictly less than `1500` (ROADMAP success criterion #2). This is the cumulative end-state of all Phase 22 reductions (22-03 removed ~600 LOC, 22-04 removed ~60-80 LOC, 22-05 added a memo but kept the file balanced, 22-06 removes 1 line and adds 1 line). The file should already be well under 1500 by the time this plan runs.

       If `wc -l` returns ≥ 1500 STOP and surface the issue. Possible causes:
       (a) plan 22-03 stopped at the ≤1700 task-level criterion and didn't continue trimming — check whether unused imports, dead code paths, or unreferenced helpers remain in JobsManager.tsx that this plan should also delete to hit the cumulative target.
       (b) plan 22-04 or 22-05 added more lines than expected — verify their SUMMARYs and the diff history.
       Do NOT mark Task 2 done until `wc -l src/components/JobsManager.tsx` is < 1500. If the number is close (1500-1550), identify low-risk removals (e.g., commented-out experimental code, redundant blank lines, helper functions whose only caller moved to RecordSaleModal) and trim until the target is met.

    **JobsManager.test.tsx edits:**

    Step 5 — Locate the `vi.mock('../hooks/useDatabase', ...)` factory at line 19. The current factory looks roughly like:
       ```
       vi.mock('../hooks/useDatabase', () => ({
         useQuotes: () => ({...}),
         useCustomers: () => ({...}),
         useSales: () => ({
           sales: [],
           addSale: vi.fn(),
           updateSale: vi.fn(),
           deleteSale: vi.fn(),
         }),
       }));
       ```

    Step 6 — Add a new entry to the factory return object, placed adjacent to the `useSales` entry:
       ```
       useAllSales: () => [],
       ```
       CRITICAL: the mock returns a PLAIN ARRAY `[]`, NOT `{ sales: [] }`. This matches the D-23(a) shape — `useAllSales(): Sale[]` returns `Sale[]` directly. Pitfall 4 lock: getting the shape wrong here produces `TypeError: useAllSales is not a function` OR worse, runtime errors when JobsManager destructures non-existent properties.

    Step 7 — Verify the test still mounts via `npm test -- JobsManager --run`. The existing `renderJobCard` helper at line 340 is untouched; the change is only to the module mock + the parent's consumption.

    Step 8 — Verify `npx tsc -b` exits 0.

    Single commit message recommendation: `perf(22-06): lift global useSales subscription to useAllSales hook (PERF-07)`. Includes both files (JobsManager.tsx + JobsManager.test.tsx). D-27 atomic-commit lock honored — the test mock and the consumer MUST land in the same commit or test fails on intermediate state.
  </action>
  <verify>
    <automated>grep -c "useAllSales" src/components/JobsManager.tsx</automated>
    <automated>grep -c "const allSales = useAllSales()" src/components/JobsManager.tsx</automated>
    <automated>grep -cE "const \{ sales: allSales \} = useSales\(\)" src/components/JobsManager.tsx</automated>
    <automated>grep -cE "useAllSales:\s*\(\)\s*=>\s*\[\]" src/components/JobsManager.test.tsx</automated>
    <automated>wc -l src/components/JobsManager.tsx | awk '{ if ($1 < 1500) print "OK ("$1" lines)"; else print "FAIL ("$1" lines, need <1500)" }'</automated>
    <automated>npx tsc -b 2>&1 | grep -E "error TS" | head -10</automated>
    <automated>npm test -- JobsManager --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'useAllSales' src/components/JobsManager.tsx` returns at least `2` (import + call site)
    - `grep -c 'const allSales = useAllSales()' src/components/JobsManager.tsx` returns `1`
    - `grep -cE "const \{ sales: allSales \} = useSales\(\)" src/components/JobsManager.tsx` returns `0` (the old destructure-rename pattern is gone)
    - **`wc -l src/components/JobsManager.tsx` returns a number strictly less than `1500`** (ROADMAP success criterion #2 — cumulative phase target; this is the only plan that can verify it because it runs last)
    - `grep -cE "useAllSales:\s*\(\)\s*=>\s*\[\]" src/components/JobsManager.test.tsx` returns `1` (mock entry present with plain-array return)
    - `grep -c '@testing-library/react' src/components/JobsManager.test.tsx` returns `0` (existing convention preserved)
    - `npx tsc -b` exits 0
    - `npm test -- JobsManager --run` exits 0 (mock + consumer change land atomically; no `TypeError: useAllSales is not a function`)
  </acceptance_criteria>
  <done>JobsManager.tsx imports + calls useAllSales; old destructure-rename pattern gone; cumulative `wc -l src/components/JobsManager.tsx` < 1500 (ROADMAP criterion #2 met); JobsManager.test.tsx mock includes useAllSales returning []; tsc -b passes; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md Phase 22 success criterion #1 prop list to match implemented D-06 signature</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (read the Phase 22 section in full — locate success criterion #1; the current text references "customers, customersByEmail" as RecordSaleModal props, which is stale per D-06 lock)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-CONTEXT.md (D-06 — confirms the actual implemented prop signature: { job, userProfile, userCurrency, shippingConfig, editingSale, convertingFromQuote, isOpen, onClose, onSaved? })
    - src/components/RecordSaleModal.tsx (verify the live prop interface to confirm the D-06 signature is what actually shipped)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`. Documentation-only change: replace the stale prop list in Phase 22 success criterion #1 with the actually-implemented signature.

    Step 1 — Find the Phase 22 section in ROADMAP.md (look for `## Phase 22` or `### Phase 22:` heading; the success criteria list is below it).

    Step 2 — Locate success criterion #1. The current text references `customers, customersByEmail` as RecordSaleModal props. Example of the stale text (exact wording may differ — read the file before editing):
       ```
       1. RecordSaleModal extracted with props: { customers, customersByEmail, job, ... }
       ```

    Step 3 — Replace the prop list with the actual D-06 implemented signature:
       ```
       1. RecordSaleModal extracted with props: { job, userProfile, userCurrency, shippingConfig, editingSale, convertingFromQuote, isOpen, onClose, onSaved? } — customers/customersByEmail removed from prop surface per D-06; RecordSaleModal subscribes to useCustomers internally
       ```
       Preserve the criterion's number (#1) and any surrounding bullet structure. Only the prop list text changes.

    Step 4 — Verify no other Phase 22 success criterion references `customers, customersByEmail` as RecordSaleModal props:
       ```
       grep -n "customers, customersByEmail" .planning/ROADMAP.md
       ```
       Should return 0 results after the edit. If any other criterion has the same stale list, update it too.

    Step 5 — Verify the criterion now accurately describes the shipped contract by cross-checking against `src/components/RecordSaleModal.tsx`'s `RecordSaleModalProps` interface (it should list exactly the 9 fields above).

    Step 6 — No code/test verification needed for this task — it's pure documentation. But ensure the edit is small and surgical (only the prop list text changes; do NOT restructure the criterion or renumber).

    Suggested commit message: `docs(22-06): sync ROADMAP Phase 22 criterion #1 RecordSaleModal prop list with implemented D-06 signature`. Single file edit. D-27 honored (atomic commit per file).
  </action>
  <verify>
    <automated>grep -n "customers, customersByEmail" .planning/ROADMAP.md | head -5</automated>
    <automated>grep -n "editingSale, convertingFromQuote, isOpen, onClose" .planning/ROADMAP.md | head -5</automated>
    <automated>grep -c "Phase 22" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "customers, customersByEmail" .planning/ROADMAP.md` returns `0` (stale prop list removed from Phase 22 section; if other phases legitimately reference these as props elsewhere in ROADMAP, that's acceptable — the gate is specifically the Phase 22 success criterion #1)
    - `grep -c "editingSale, convertingFromQuote, isOpen, onClose" .planning/ROADMAP.md` returns at least `1` (new accurate signature inserted)
    - The Phase 22 success criterion #1 still exists (not deleted) — confirmed by `grep -A2 "Phase 22" .planning/ROADMAP.md` showing the criterion structure intact
    - No other Phase 22 success criteria were modified
    - File still parses as valid markdown (no broken list indentation)
  </acceptance_criteria>
  <done>.planning/ROADMAP.md Phase 22 criterion #1 lists the actually-implemented D-06 RecordSaleModal prop signature; stale "customers, customersByEmail" references are gone from the Phase 22 section.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT — break-even pill + revenue numbers match pre-Phase-22 behavior</name>
  <files>(no files modified — verification only)</files>
  <action>Run the manual UAT described in <how-to-verify>. This is a human-only verification step. No source-file edits.</action>
  <verify>Verification is performed by the human verifier following the steps in <how-to-verify>. There is no automated command for this checkpoint.</verify>
  <done>Verifier returns the resume signal "approved" after confirming all 5 visual contracts (A-E) match the pre-Phase-22 baseline. If any contract fails, a follow-up task is opened in this plan to fix the regression and the UAT is re-run. ONLY after this Task 4 returns "approved" may the SUMMARY.md be created (see <output> below).</done>
  <what-built>
    PERF-07 closure: the global `useSales()` subscription duplication at `JobsManager.tsx:1070-1071` is removed in favor of an explicit `useAllSales(): Sale[]` hook in `useDatabase.ts`. Both subscriptions are now distinct, named, and have non-overlapping responsibilities (scoped CRUD vs global read).

    Additionally, Phase 22 as a whole reorganized rendering paths: `breakEvenMap` (PERF-01, plan 22-05) replaced per-call `getBreakEvenInfo` recomputation; the `<RecordSaleModal>` and `<SaleRow>` extractions changed the JSX tree composition. This is the only manual UAT for Phase 22 — it confirms the cumulative behavior is identical to the pre-phase baseline.

    Tasks 2 and 3 of this plan also closed two documentation/scale items: JobsManager.tsx is now < 1500 lines (ROADMAP criterion #2) and ROADMAP.md's Phase 22 success criterion #1 lists the actually-implemented RecordSaleModal prop signature.
  </what-built>
  <how-to-verify>
    1. Kill any running dev server first (project rule from project memory): `lsof -i :4173 | grep LISTEN | awk '{print $2}' | xargs -r kill -9 2>/dev/null || true`
    2. Start the dev server: `npm run dev` (port 4173 pinned via vite.config.ts)
    3. Open `http://localhost:4173` in a browser.
    4. Navigate to the Jobs Manager tab.
    5. **Pre-check** — IndexedDB should contain real data (jobs + sales). If empty, import a backup or create at least 3 jobs with mixed states: one with no sales, one partway to break-even, one over break-even.
    6. **Visual contract A — break-even pill values:**
       - For each visible job row, expand the job's break-even pill (the colored "X / Y break-even" pill near the price block).
       - Confirm the displayed pill text + color matches what you would have seen before Phase 22:
         - "0 / N break-even" + neutral color when no sales
         - "K / N break-even" + warning color when partway
         - "Break-even" or similar success-state copy + green when revenue >= cost
       - If any pill displays NaN, undefined, $0.00 when sales exist, or a wrong color, this is a FAIL.
    7. **Visual contract B — recent sales list:**
       - Click into a job that has sales.
       - Expand the recent sales accordion.
       - For each sale row: confirm the summary line shows `{quantity}x @ ${price} ({customerName})`, the right-aligned `${totalRevenue.toFixed(2)}`, and (when applicable) the "(from quote Q-NNNN)" subtext.
       - Click into a sale's `<details>`: confirm the chevron rotates, the customer block expands, and Edit/Delete buttons render.
    8. **Visual contract C — Record Sale modal:**
       - Click "Record Sale" on a job. Modal opens via the new `<RecordSaleModal>`.
       - Type into the customer picker: dropdown opens, ArrowDown navigates, Enter picks → Name/Email/Company/Address fields populate. Notes field is empty (D-05 lock).
       - Press Escape: dropdown closes, modal stays open.
       - Press Escape again: modal closes (no double-close glitch).
       - Re-open, fill all fields, click Save: a new sale is recorded; the job's recent sales list updates; the break-even pill refreshes if applicable.
    9. **Visual contract D — Convert from Quote:**
       - Find a job with a Pending quote (or create one via Print Quote).
       - Click "Convert to Sale". Modal opens with the blue "Converting Q-NNNN — review and adjust if needed." helper banner at the top.
       - Save. Confirm: new Sale appears in recent sales; the Quote disappears from the Pending list; the job's copiesSold counter increments by the saved quantity.
    10. **Visual contract E — CustomerLibrary search (PERF-04):**
        - Navigate to the Customer Library tab.
        - Pre-check: library should contain >5 customers with varied address/notes lengths (so row heights naturally differ).
        - Type into the search box and progressively narrow. The filtered list's row heights should compute correctly for each filtered subset — no visible "stale tall row" from a previously-visible customer leaking into the new layout.
    11. **Verdict**:
        - All 5 contracts visually identical to pre-Phase-22? → Type "approved"
        - Any deltas (wrong number, wrong color, missing element, glitched interaction)? → Describe the issue + the affected contract

    Re-start instructions if dev server crashes: `npm run dev` (it auto-reloads on file change; port 4173 should be free if the kill at step 1 worked).
  </how-to-verify>
  <resume-signal>Type "approved" to mark UAT passed, OR describe issues found. If issues found, open a follow-up task in this plan to fix; do NOT mark PERF-07 closed.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc -b` exits 0
- `npm test --run` exits 0 (full suite — JobsManager test mock updated, all existing tests pass)
- `grep -c 'export function useAllSales' src/hooks/useDatabase.ts` returns `1`
- `grep -c 'useAllSales' src/components/JobsManager.tsx` returns at least `2`
- `wc -l src/components/JobsManager.tsx` returns strictly less than `1500` (ROADMAP criterion #2)
- `grep -c "customers, customersByEmail" .planning/ROADMAP.md` returns `0` in the Phase 22 success criteria block
- Manual UAT (Task 4) verdict: "approved"
- No other source file references `useSales()` (no-arg) outside JobsManager (verified in Task 1 step 6)
</verification>

<success_criteria>
- PERF-07 closed: useAllSales lives in useDatabase.ts and is the sole global-sales subscription; JobsManager no longer has the destructure-rename pattern; test mock honors the plain-array return shape per D-24
- ROADMAP success criterion #2 closed: `wc -l src/components/JobsManager.tsx` < 1500 (cumulative phase target met)
- ROADMAP success criterion #1 documentation accuracy closed: prop list in ROADMAP.md Phase 22 section now matches the implemented D-06 signature
- Manual UAT visually confirms break-even pill + revenue numbers + Record Sale modal + Convert-from-Quote + CustomerLibrary search all behave identically to pre-Phase-22 baseline (Validation §Manual-Only)
- Phase 22 complete after this plan: all 10 requirement IDs (HYG-02/03/06/07/08, PERF-01/02/03/04/07) closed; ROADMAP success criteria 1-11 met
- All commits via `gsd-sdk query commit`; no `--no-verify`; `tsc -b` typecheck
</success_criteria>

<output>
Create `.planning/phases/22-jobsmanager-decomposition-perf/22-06-SUMMARY.md` ONLY after Task 4 UAT verdict is approved. Task 2 completion (PERF-07 code change + cumulative <1500 line assertion) does NOT trigger SUMMARY.md creation. Task 3 completion (ROADMAP.md doc edit) does NOT trigger SUMMARY.md creation. The UAT-approval gate (Task 4) is mandatory before the SUMMARY may be written.

When the SUMMARY is finally written (post-UAT-approval), record: final useAllSales insertion line number in useDatabase.ts, list of other no-arg useSales() callers found (should be empty), final `wc -l src/components/JobsManager.tsx` (cumulative Phase 22 result — must be < 1500), JobsManager.test.tsx mock additions, the ROADMAP.md success-criterion #1 edit confirmation, and the UAT verdict + any approved deltas.
</output>
</content>
</invoke>