---
phase: 22.1-break-even-formula-reconciliation
plan: 02
type: execute
wave: 2
depends_on: [22.1-01]
files_modified:
  - src/components/CostCalculator.tsx
  - src/components/JobsManager.tsx
  - src/components/JobsManager.test.tsx
autonomous: true
requirements: [PERF-08]
requirements_addressed: [PERF-08]
tags: [break-even, formula, snapshot, calculator, jobs-manager, round-trip]

must_haves:
  truths:
    - "CostCalculator.tsx Update payload (~line 600) includes fixedCostsAtSave: { depreciation: fixedCosts.depreciation, nozzleWear: fixedCosts.nozzleWear }"
    - "CostCalculator.tsx Create payload (~line 635) includes fixedCostsAtSave: { depreciation: fixedCosts.depreciation, nozzleWear: fixedCosts.nozzleWear }"
    - "JobsManager.tsx computeBreakEvenInfo numerator at line 61-63 uses fixedNumerator = job.modelCost + (job.fixedCostsAtSave?.depreciation ?? 0) + (job.fixedCostsAtSave?.nozzleWear ?? 0)"
    - "JobsManager.tsx computeBreakEvenInfo Infinity guard uses fixedNumerator > 0 (not job.modelCost > 0) so jobs with modelCost=0 and non-zero depreciation surface a valid break-even"
    - "JobsManager.test.tsx contains a PERF-08 round-trip test asserting Calculator-style Break-even Units === computeBreakEvenInfo breakEvenCopies for the same job"
    - "Round-trip test calls reconcileFixedCostsAtSave on a newly-saved job (verifies no-op for newly-saved jobs since they already have fixedCostsAtSave set)"
  artifacts:
    - path: "src/components/CostCalculator.tsx"
      provides: "fixedCostsAtSave written to both Save (Create) and Update payloads"
      contains: "fixedCostsAtSave: {"
    - path: "src/components/JobsManager.tsx"
      provides: "computeBreakEvenInfo widened to include depreciation + nozzleWear in numerator"
      contains: "fixedNumerator"
    - path: "src/components/JobsManager.test.tsx"
      provides: "PERF-08 round-trip test (Calculator displayed Break-even Units === pill breakEvenCopies)"
      contains: "PERF-08"
  key_links:
    - from: "src/components/CostCalculator.tsx fixedCosts memo (line 449-457)"
      to: "PrintJob.fixedCostsAtSave"
      via: "destructured reads inside both save handlers"
      pattern: "fixedCostsAtSave: \\{\\s*depreciation: fixedCosts\\.depreciation"
    - from: "src/components/JobsManager.tsx computeBreakEvenInfo"
      to: "PrintJob.fixedCostsAtSave snapshot field (from plan 22.1-01)"
      via: "optional-chaining nullish-coalesce read"
      pattern: "job\\.fixedCostsAtSave\\?\\.depreciation"
---

<objective>
Land PERF-08 formula correctness: write the fixedCostsAtSave snapshot from CostCalculator on both Create and Update paths, widen the JobsManager break-even pill's numerator to match the Calculator widget's formula, and lock the agreement with a round-trip test asserting Calculator displayed Break-even Units === JobsManager pill breakEvenCopies for the same job.

Purpose: Closes the silent formula divergence surfaced during Phase 22 UAT (Calculator widget = `(modelCost + depreciation + nozzleWear) / profitPerUnit`, JobsManager pill = `modelCost / profitPerUnit`). The user saves a job under the Calculator's mental model and now reads it back consistently in the JobsManager pill.

Output: CostCalculator writes the snapshot at save time (D-03); JobsManager pill reads the snapshot in computeBreakEvenInfo (D-01); JobsManager.test.tsx ships the PERF-08 round-trip test (D-11 #2). Depends on Plan 22.1-01 — without the schema field, neither change compiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-CONTEXT.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-RESEARCH.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-PATTERNS.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-01-PLAN.md
@src/components/CostCalculator.tsx
@src/components/JobsManager.tsx
@src/components/JobsManager.test.tsx
@src/db/backfill.ts

<interfaces>
<!-- Key types and contracts the executor uses verbatim. -->

PrintJob.fixedCostsAtSave (NEW from Plan 22.1-01, src/types.ts after line 355):
  fixedCostsAtSave?: { depreciation: number; nozzleWear: number }

CostCalculator fixedCosts memo (src/components/CostCalculator.tsx:449-457) — already in scope inside handleSaveJob:
  const fixedCosts = useMemo(() => ({
    depreciation: costs.printerDepreciation,
    nozzleWear: costs.nozzleWear,
    modelCost: ...,
    total: ...,
  }), [...]);

CostCalculator breakEvenInfo memo (src/components/CostCalculator.tsx:460-478) — the Calculator's displayed "Break-even Units":
  breakEvenCopies = profitPerUnit > 0
    ? Math.ceil(fixedCosts.total / profitPerUnit)
    : fixedCosts.total > 0 ? Infinity : 0;
  where profitPerUnit = sellingPrice - trueCost, fixedCosts.total = depreciation + nozzleWear + fixedModelCost.

JobsManager computeBreakEvenInfo (src/components/JobsManager.tsx:45-78):
  effectiveProfitPerUnit (job.copiesSold > 0 → actualProfitPerUnit else theoreticalProfitPerUnit) — unchanged.
  Lines 61-63 are the numerator/Infinity-guard block to replace.

reconcileFixedCostsAtSave (NEW from Plan 22.1-01, src/db/backfill.ts):
  export function reconcileFixedCostsAtSave(
    jobs: PrintJob[],
    printerInstances: PrinterInstance[],
    printers: PrinterConfig[],
    materials: Material[],
  ): PrintJob[]
  Skips jobs that already have fixedCostsAtSave set (returns empty array for newly-saved jobs).
</interfaces>

<formula_alignment>
Calculator (CostCalculator.tsx:460-478, model — already correct):
  breakEvenCopiesCalc = Math.ceil(fixedCosts.total / (sellingPrice - trueCost))
    where fixedCosts.total = depreciation + nozzleWear + fixedModelCost
    and  fixedModelCost = modelCostPerUnit ? 0 : modelCost
    and  trueCost = costs.failureAdjusted + totalShippingCost  (per-unit consumable)

JobsManager pill — AFTER this plan (computeBreakEvenInfo line 61-63):
  fixedNumerator = job.modelCost
                 + (job.fixedCostsAtSave?.depreciation ?? 0)
                 + (job.fixedCostsAtSave?.nozzleWear   ?? 0)
  breakEvenCopiesRaw = effectiveProfitPerUnit > 0
    ? Math.ceil(fixedNumerator / effectiveProfitPerUnit)
    : fixedNumerator > 0 ? Infinity : 0;
  // Surrounding Number.isFinite guard + remainingToBreakEven null-handling stays unchanged.

Note: `job.modelCost` on the PrintJob is already `fixedModelCost` (the Calculator stored 0 when modelCostPerUnit=true, per existing behavior). Therefore the two formulas are arithmetically identical for newly-saved jobs whose fixedCostsAtSave snapshot matches the in-memory fixedCosts memo. The round-trip test below locks this exact equality.
</formula_alignment>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add round-trip test to JobsManager.test.tsx asserting Calculator formula === pill breakEvenCopies (PERF-08, written FIRST per TDD)</name>

  <files>
    src/components/JobsManager.test.tsx
  </files>

  <read_first>
    src/components/JobsManager.test.tsx full file — confirm createRoot+act harness, vi.mock pattern, fixture helpers
    src/components/JobsManager.tsx lines 40-78 (the computeBreakEvenInfo function being asserted against)
    src/components/CostCalculator.tsx lines 449-478 (fixedCosts memo + breakEvenInfo memo — the Calculator's reference formula)
    .planning/phases/22.1-break-even-formula-reconciliation/22.1-PATTERNS.md §`src/components/JobsManager.test.tsx`
    .planning/phases/22.1-break-even-formula-reconciliation/22.1-RESEARCH.md §Validation Architecture + §Code Examples
    src/components/RecordSaleModal.test.tsx lines 1-50 + 172-248 (createRoot+act analog for harness shape — only if JobsManager.test.tsx doesn't already have the analog)
  </read_first>

  <behavior>
    Test (RED first — runs BEFORE Task 2 lands the production change; expected to FAIL until Task 2 is committed):
    - Title: 'PERF-08 — Calculator Break-even Units === JobsManager pill breakEvenCopies (round-trip after save)'
    - Construct a PrintJob with KNOWN values: modelCost=100, fixedCostsAtSave={ depreciation: 20, nozzleWear: 5 }, sellingPrice=50, costPerUnit=30, copiesSold=0 (so effectiveProfitPerUnit = theoreticalProfitPerUnit = 20).
    - Call computeBreakEvenInfo(job, new Map()) (no sales) and capture the returned `breakEvenCopies`.
    - Compute the Calculator-side expected value arithmetically inside the test, mirroring CostCalculator.tsx:460-467:
        const fixedTotal = job.modelCost + job.fixedCostsAtSave.depreciation + job.fixedCostsAtSave.nozzleWear;  // 125
        const profitPerUnit = job.sellingPrice - job.costPerUnit;  // 20
        const expectedCalcBreakEven = profitPerUnit > 0 ? Math.ceil(fixedTotal / profitPerUnit) : (fixedTotal > 0 ? Infinity : 0);
        // expectedCalcBreakEven = Math.ceil(125 / 20) = 7
    - Assert `breakEvenCopies === expectedCalcBreakEven` (both equal 7).
    - Second case in the same it block (or a sibling it under the same describe): pass `fixedCostsAtSave: undefined` on a legacy job → assert breakEvenCopies = Math.ceil(modelCost / profitPerUnit) (falls back to pre-22.1 behavior because the snapshot is undefined and the `?? 0` defaults kick in). Use job with modelCost=100, sellingPrice=50, costPerUnit=30 → expectedLegacy = Math.ceil(100/20) = 5.
    - Third case: pass `fixedCostsAtSave: { depreciation: 20, nozzleWear: 5 }` on a job with modelCost=0 and sellingPrice=50, costPerUnit=49 (profitPerUnit=1) → expected breakEvenCopies = Math.ceil(25/1) = 25 (verifies the Infinity guard now uses `fixedNumerator > 0` not `job.modelCost > 0`).

    The test calls computeBreakEvenInfo directly — DO NOT render JobsManager. computeBreakEvenInfo is a module-scope pure function (PERF-01 D-17) and is testable in isolation. If it is not currently exported, the test imports it; this plan exports it from JobsManager.tsx (see Task 2 action).

    Wrap the new tests in `describe('PERF-08 — break-even formula round-trip (Phase 22.1)', () => { ... })` placed at the END of the test file (after all existing describe blocks).
  </behavior>

  <action>
    Open src/components/JobsManager.test.tsx. Append a new describe block at the end of the file:
    `describe('PERF-08 — break-even formula round-trip (Phase 22.1)', () => { ... })`

    Inside the describe, define a small `makeJob` fixture helper that returns a minimal PrintJob (mirror the existing fixture shape in the file — same field set used by existing JobsManager tests; consult lines 40-100 for the established pattern in this file). Allow overrides via spread.

    Import `computeBreakEvenInfo` from `./JobsManager` at the top of the file (extend the existing import line; if computeBreakEvenInfo is not yet exported, this test will fail at import time until Task 2 adds the export — which is intentional TDD: RED before GREEN).

    Three `it` cases in the new describe:
    1. `it('snapshotted job — pill breakEvenCopies === Math.ceil((modelCost + depreciation + nozzleWear) / profitPerUnit)')` — assertions per the behavior section above (expected = 7).
    2. `it('legacy job (no snapshot) — pill falls back to Math.ceil(modelCost / profitPerUnit)')` — assertions per behavior (expected = 5; uses `fixedCostsAtSave: undefined`).
    3. `it('zero-modelCost + non-zero depreciation — pill returns finite breakEven (Infinity guard widened to fixedNumerator > 0)')` — assertions per behavior (expected = 25).

    Use `expect(...).toBe(...)` for the integer comparisons. No need to render anything — `computeBreakEvenInfo` is a pure module-scope function.

    Do NOT touch the existing test blocks in this file.
  </action>

  <verify>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 &amp;&amp; npm test -- JobsManager.test.tsx 2>&amp;1 | grep -E "(PERF-08|FAIL|PASS)" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - At this point in the plan, the new describe block exists and EITHER fails (RED — because computeBreakEvenInfo is not exported yet) OR fails (RED — because the formula change in Task 2 hasn't landed) — RED is acceptable. Task 2 lands GREEN.
    - `grep -c "describe.*PERF-08.*break-even formula round-trip.*Phase 22.1" src/components/JobsManager.test.tsx` returns 1
    - `grep -c "^  it\\b" -A0 src/components/JobsManager.test.tsx | tail -3` shows at least 3 new it blocks added
    - The three test names appear as substrings in the file (grep for the literal substrings 'snapshotted job', 'legacy job (no snapshot)', 'zero-modelCost')
    - Test file imports computeBreakEvenInfo at the top (or the import line is queued; will resolve when Task 2 exports it)
  </acceptance_criteria>

  <done>
    The PERF-08 round-trip describe block exists in JobsManager.test.tsx with three named it cases that encode the Calculator's formula identity. Tests may be RED until Task 2 ships the formula switch + the export — this is intentional TDD ordering.
  </done>
</task>

<task type="auto">
  <name>Task 2: Widen computeBreakEvenInfo numerator + write fixedCostsAtSave on both CostCalculator save paths (GREEN)</name>

  <files>
    src/components/JobsManager.tsx
    src/components/CostCalculator.tsx
  </files>

  <read_first>
    src/components/JobsManager.tsx lines 40-80 (computeBreakEvenInfo — the function whose numerator changes)
    src/components/CostCalculator.tsx lines 440-460 (fixedCosts memo definition)
    src/components/CostCalculator.tsx lines 570-640 (handleSaveJob — both Update and Create payload literals)
    .planning/phases/22.1-break-even-formula-reconciliation/22.1-PATTERNS.md §`src/components/JobsManager.tsx`, §`src/components/CostCalculator.tsx`
    .planning/phases/22.1-break-even-formula-reconciliation/22.1-RESEARCH.md §Code Examples (lines 425-500)
    src/components/JobsManager.test.tsx — the new describe block from Task 1 (the GREEN target)
  </read_first>

  <behavior>
    JobsManager.tsx — computeBreakEvenInfo (lines 45-78):
    - The function MUST be exported (add `export` keyword to the function declaration at line 45 if not already exported) so JobsManager.test.tsx can import it (Task 1 imports it).
    - Replace lines 61-63 (the breakEvenCopiesRaw computation) with the fixedNumerator block. The surrounding effectiveProfitPerUnit logic (lines 52-57), the `Number.isFinite(breakEvenCopiesRaw)` guard (line 64-66), the `remainingToBreakEven` computation (line 67-69), and the return object (line 71-77) MUST remain byte-identical.
    - The Infinity guard CHANGES from `job.modelCost > 0 ? Infinity : 0` to `fixedNumerator > 0 ? Infinity : 0`. This is strictly more correct per RESEARCH Open Question #2.

    CostCalculator.tsx — Update payload (line 574-600):
    - Add `fixedCostsAtSave: { depreciation: fixedCosts.depreciation, nozzleWear: fixedCosts.nozzleWear }` to the `updatedJob` object literal. Place it after `tags: editingJob.tags,` at line 599, before the closing brace at line 600. fixedCosts is already in scope (computed at line 449).

    CostCalculator.tsx — Create payload (line 609-636):
    - Add `fixedCostsAtSave: { depreciation: fixedCosts.depreciation, nozzleWear: fixedCosts.nozzleWear }` to the new `job` object literal. Place it after `copiesSold: 0,` at line 635, before the closing brace at line 636.

    After both edits, the Task 1 tests turn GREEN.
  </behavior>

  <action>
    JobsManager.tsx changes:
    1. At line 45, ensure the function declaration reads `export function computeBreakEvenInfo(` (add `export` if missing — the function is currently module-scope but not exported).
    2. Replace lines 61-63 with:
       ```
       // Phase 22.1 D-01 — widen numerator to include fixed-cost snapshot.
       // Calculator's formula wins: break-even covers modelCost + depreciation + nozzleWear,
       // not just modelCost. Legacy jobs without fixedCostsAtSave fall back to modelCost
       // alone via the `?? 0` defaults (round-trip equivalent to pre-22.1 behavior).
       const fixedNumerator = job.modelCost
         + (job.fixedCostsAtSave?.depreciation ?? 0)
         + (job.fixedCostsAtSave?.nozzleWear ?? 0);
       const breakEvenCopiesRaw = effectiveProfitPerUnit > 0
         ? Math.ceil(fixedNumerator / effectiveProfitPerUnit)
         : fixedNumerator > 0 ? Infinity : 0;
       ```
    3. Confirm by spot-check that lines 64-77 (Number.isFinite guard, remainingToBreakEven null-handling, the return object) are untouched.

    CostCalculator.tsx changes:
    1. Update path (around line 599-600): after the existing `tags: editingJob.tags,` line, insert a new line:
       `fixedCostsAtSave: { depreciation: fixedCosts.depreciation, nozzleWear: fixedCosts.nozzleWear },`
       Place it before the closing brace of the `updatedJob` literal at line 600. Mirror surrounding indentation.
    2. Create path (around line 635-636): after the existing `copiesSold: 0,` line, insert:
       `fixedCostsAtSave: { depreciation: fixedCosts.depreciation, nozzleWear: fixedCosts.nozzleWear },`
       Place it before the closing brace of the new `job` literal at line 636. Mirror surrounding indentation.

    Do NOT touch the existing `fixedCosts` memo (lines 449-457) or `breakEvenInfo` memo (lines 460-478) — both are already correct on the Calculator side.

    After both file edits, the Task 1 tests turn GREEN.
  </action>

  <verify>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 &amp;&amp; npx tsc -b &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc -b` exits 0 with no new errors
    - `grep -c "^export function computeBreakEvenInfo" src/components/JobsManager.tsx` returns 1
    - `grep -c "fixedNumerator" src/components/JobsManager.tsx` returns at least 3 (declaration + Math.ceil call + Infinity-guard comparison)
    - `grep -nE "fixedNumerator > 0 \\? Infinity : 0" src/components/JobsManager.tsx` returns one match (the widened guard)
    - `grep -nE "job\\.modelCost > 0 \\? Infinity : 0" src/components/JobsManager.tsx` returns 0 matches (the old guard is gone)
    - `grep -c "fixedCostsAtSave: { depreciation: fixedCosts.depreciation, nozzleWear: fixedCosts.nozzleWear }" src/components/CostCalculator.tsx` returns exactly 2 (Update path + Create path)
    - `npm test` exits 0 — full suite passes including the PERF-08 describe block from Task 1 (now GREEN)
    - The PERF-08 describe block reports 3 passing it cases
  </acceptance_criteria>

  <done>
    JobsManager pill computes the same fixed-cost-inclusive break-even as the Calculator widget for the same job. CostCalculator writes the snapshot on both Save (Create) and Update paths. Three PERF-08 round-trip tests pass. tsc -b clean. Full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | No new attack surfaces — pure formula/UI changes touching only the user's own local IndexedDB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.1-03 | Information Disclosure | break-even pill displays slightly different number than before for legacy jobs | accept | This is the intended behavior — silent fix is in scope (CONTEXT.md domain section). The number is the user's own data displayed correctly. No PII exposure |
| T-22.1-04 | Tampering | CostCalculator writes fixedCostsAtSave on Save/Update | accept | Local-only write; existing save path already trusted. Field is additive (no overwrite of other fields) |

No new packages, no new permissions, no schema-version bump. Severity: none. No blockers.
</threat_model>

<verification>
After both tasks land:
- `npx tsc -b` exits 0
- `npm test` exits 0 — full vitest suite green including PERF-08 round-trip describe (3 it cases)
- `grep -nE "fixedCostsAtSave: \\{ depreciation: fixedCosts\\.depreciation, nozzleWear: fixedCosts\\.nozzleWear \\}" src/components/CostCalculator.tsx` returns exactly 2 hits
- `grep -nE "fixedNumerator" src/components/JobsManager.tsx` returns 3+ hits
- `grep -nE "PERF-08 — break-even formula round-trip" src/components/JobsManager.test.tsx` returns 1 hit
- Round-trip verification: the three PERF-08 it cases pass — explicit assertion that computeBreakEvenInfo returns the same number the Calculator memoizes for the same input job
</verification>

<success_criteria>
- PERF-08 satisfied: JobsManager break-even pill formula matches the Calculator widget formula; round-trip test locks the agreement
- ROADMAP Phase 22.1 success criterion #2 satisfied: CostCalculator writes both fields on Save (new) and Update (edit) — both line :589 and :625 payload sites
- ROADMAP Phase 22.1 success criterion #3 satisfied: JobsManager computeBreakEvenInfo numerator widened to `job.modelCost + (job.fixedCostsAtSave?.depreciation ?? 0) + (job.fixedCostsAtSave?.nozzleWear ?? 0)`
- ROADMAP Phase 22.1 success criterion #5 satisfied: Calculator displayed Break-even Units === JobsManager pill breakEvenCopies for the same job (round-trip test locks this)
- D-01, D-03, D-11 (round-trip case) implemented per CONTEXT.md
- Infinity guard widened (RESEARCH Open Question #2 resolved): `fixedNumerator > 0` not `job.modelCost > 0` — zero-modelCost + non-zero depreciation jobs now surface a finite break-even
</success_criteria>

<output>
Create `.planning/phases/22.1-break-even-formula-reconciliation/22.1-02-SUMMARY.md` when both tasks complete.

Summary MUST include:
- Confirmation that `computeBreakEvenInfo` is now exported from JobsManager.tsx
- Final line numbers of the two `fixedCostsAtSave: {...}` insertions in CostCalculator.tsx
- The 3 PERF-08 it case names and their pass status
- Any deviations from the plan (e.g., the function was already exported, file-line numbers drifted from the plan)
</output>
