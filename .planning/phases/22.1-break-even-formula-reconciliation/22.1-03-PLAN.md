---
phase: 22.1-break-even-formula-reconciliation
plan: 03
type: execute
wave: 2
depends_on: [22.1-01]
files_modified:
  - src/components/RecordSaleModal.tsx
  - src/components/RecordSaleModal.test.tsx
  - src/components/JobsManager.tsx
autonomous: true
requirements: []
requirements_addressed: []
carryover: [WR-01, WR-02]
tags: [code-review-carryover, hygiene, dep-array, eslint-disable, dead-prop]

must_haves:
  truths:
    - "RecordSaleModalProps interface no longer declares a userProfile field (WR-01 / D-10)"
    - "RecordSaleModal.tsx imports section no longer imports UserProfile from ../types"
    - "JobsManager.tsx <RecordSaleModal> callsite (~line 1404-1413) no longer passes userProfile={userProfile} (the only line removed; sibling <PrintQuoteModal> callsite at line 1474 keeps userProfile — those are different components)"
    - "RecordSaleModal.tsx hydration useEffect dep array (~line 155) is [editingSale?.id, convertingFromQuote?.id] with explanatory Pitfall 3 comment + eslint-disable-next-line react-hooks/exhaustive-deps directive immediately above (D-09 / WR-02)"
    - "RecordSaleModal.test.tsx contains a WR-02 regression test asserting salePrice form state survives a useLiveQuery re-emission that mutates job.sellingPrice while the modal is open in EDIT mode"
  artifacts:
    - path: "src/components/RecordSaleModal.tsx"
      provides: "WR-01 dead-prop removal + WR-02 dep-array fix with Pitfall 3 comment"
      contains: "// Pitfall 3: DO NOT add job.sellingPrice"
    - path: "src/components/RecordSaleModal.test.tsx"
      provides: "WR-02 regression test (form state survives liveQuery re-emission)"
      contains: "WR-02"
    - path: "src/components/JobsManager.tsx"
      provides: "RecordSaleModal callsite no longer passes userProfile"
      contains: "<RecordSaleModal"
  key_links:
    - from: "src/components/JobsManager.tsx <RecordSaleModal> callsite"
      to: "src/components/RecordSaleModal.tsx RecordSaleModalProps interface"
      via: "prop type-check"
      pattern: "<RecordSaleModal[^>]*"
---

<objective>
Close the two Phase 22 code-review warnings that touch RecordSaleModal (WR-01 + WR-02) and lock the WR-02 behavior with a regression test. Both warnings were deferred from Phase 22 because they touch the same surface this phase is already editing — folding them in here costs less context than a separate phase.

Purpose: WR-01 removes a dead prop (userProfile declared on the interface, passed by parent, never destructured/read inside the component). WR-02 stabilizes the hydration useEffect dep array so a concurrent useLiveQuery emission updating job.sellingPrice does NOT clobber the user's in-progress edit form state.

Output: RecordSaleModal interface trimmed (D-10), callsite cleaned up, hydration dep array narrowed with Pitfall 3 comment (D-09), and a Vitest regression test that exercises a re-render with a mutated job.sellingPrice (D-11 case #3).

Independence: Plan 22.1-02 and 22.1-03 share no files (no overlap in files_modified) — both can run in parallel in Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-CONTEXT.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-RESEARCH.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-PATTERNS.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-01-PLAN.md
@src/components/RecordSaleModal.tsx
@src/components/RecordSaleModal.test.tsx
@src/components/JobsManager.tsx

<interfaces>
<!-- Key types and contracts the executor uses verbatim. Extracted from src/components/RecordSaleModal.tsx. -->

RecordSaleModalProps (src/components/RecordSaleModal.tsx:58-68) — CURRENT shape:
  job: PrintJob
  userProfile: UserProfile           ← REMOVE this line (WR-01 / D-10)
  userCurrency: Currency
  shippingConfig: ShippingConfig
  editingSale: Sale | null
  convertingFromQuote: Quote | null
  isOpen: boolean
  onClose: () => void
  onSaved?: () => void

Function destructuring (lines 70-79) — does NOT include userProfile (already correctly absent — Pitfall 5):
  function RecordSaleModal({ job, userCurrency, shippingConfig, editingSale, convertingFromQuote, isOpen, onClose, onSaved }: RecordSaleModalProps)

Hydration useEffect dep array (line 155) — CURRENT shape:
  }, [editingSale, convertingFromQuote, job.sellingPrice]);
NEW shape (D-09 / WR-02):
  // Pitfall 3: ...explanatory comment...
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [editingSale?.id, convertingFromQuote?.id]);

Imports (line 4) — UserProfile is imported from ../types alongside other types; after WR-01 this import line should no longer reference UserProfile.

JobsManager.tsx RecordSaleModal callsite (line 1403-1413):
  <RecordSaleModal
    job={selectedJob}
    userProfile={userProfile}     ← REMOVE this line (WR-01 / D-10)
    userCurrency={userCurrency}
    shippingConfig={shippingConfig}
    editingSale={editingSale}
    convertingFromQuote={convertingFromQuote}
    isOpen={showSaleForm}
    onClose={handleCloseSaleModal}
  />

DO NOT touch the separate <PrintQuoteModal> callsite at JobsManager.tsx:1474 — that component still consumes userProfile and is out of scope for WR-01.
</interfaces>

<test_harness>
<!-- src/components/RecordSaleModal.test.tsx already has the harness. Mirror it. -->

Existing harness style (RecordSaleModal.test.tsx lines 1-50, 172-248): raw createRoot + act, no @testing-library/react. Existing fixture helpers (makeJob, makeSale, etc.) cover all required setup. A `renderModal(props)` helper is in place — reuse it.

WR-02 regression — append at the end of the file:
  describe('WR-02 — hydration dep array stability (Phase 22.1 D-09)', () => {
    it('salePrice form state survives a useLiveQuery re-emission that mutates job.sellingPrice', async () => {
      // 1. Render in EDIT mode with editingSale.unitPrice = 50.
      // 2. Read the salePrice input value via container.querySelector + (el as HTMLInputElement).value — assert it equals '50'.
      // 3. Wrap a re-render in `await act(async () => { ... })`: root!.render(<RecordSaleModal {...props} job={{ ...props.job, sellingPrice: 999 }} />);
      // 4. Read the salePrice input value again — assert it STILL equals '50' (the editingSale-driven value), NOT '999'.
    });
  });

The id selector for the salePrice input uses the useId-generated id (line 98: `const salePriceId = useId();`). Because useId() is opaque, the test reads the value by another selector — pick whichever the existing file uses (probably an aria-label or a label-for traversal). If the existing file's pattern is to query by name attribute or by descendant index, follow that pattern.
</test_harness>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add WR-02 regression test in RecordSaleModal.test.tsx (RED first per TDD)</name>

  <files>
    src/components/RecordSaleModal.test.tsx
  </files>

  <read_first>
    src/components/RecordSaleModal.test.tsx full file — confirm harness, makeJob, makeSale, renderModal pattern
    src/components/RecordSaleModal.tsx lines 70-160 (function destructuring + state + hydration useEffect)
    .planning/phases/22.1-break-even-formula-reconciliation/22.1-PATTERNS.md §`src/components/RecordSaleModal.test.tsx`
    .planning/phases/22.1-break-even-formula-reconciliation/22.1-RESEARCH.md §Common Pitfalls 5 + 6 + §Code Examples for WR-02
  </read_first>

  <behavior>
    Append a new describe block at the END of the file:
      `describe('WR-02 — hydration dep array stability (Phase 22.1 D-09)', () => { ... })`

    Single it case: 'salePrice form state survives a useLiveQuery re-emission that mutates job.sellingPrice':
    - Build a Sale fixture via the existing makeSale helper with unitPrice=50.
    - Render the modal in EDIT mode (editingSale set, convertingFromQuote=null, isOpen=true) using existing renderModal-style helper or root!.render directly.
    - Read the salePrice input value — assert it equals '50' (the editingSale.unitPrice).
    - Inside `await act(async () => { ... })`, re-render with the same editingSale but with a mutated job: `job={{ ...props.job, sellingPrice: 999 }}`.
    - After re-render, read the salePrice input value AGAIN — assert it STILL equals '50' (NOT '999'). This proves the hydration useEffect did NOT re-fire on the job.sellingPrice mutation, so the in-progress edit form state survived the liveQuery re-emission.

    The test will be RED before Task 2 (the current dep array `[editingSale, convertingFromQuote, job.sellingPrice]` causes re-hydration on sellingPrice mutation → input flips to 999 in create-mode fallback). Task 2 ships the dep array fix that turns this GREEN.
  </behavior>

  <action>
    Open src/components/RecordSaleModal.test.tsx. Append at the END of the file (after all existing describe blocks):

    A new `describe('WR-02 — hydration dep array stability (Phase 22.1 D-09)', () => { ... })` block.

    Inside the describe:
    1. Use the existing fixture helpers (`makeJob`, `makeSale`, `renderModal` or equivalent). If `renderModal` doesn't exist, inline the createRoot+act render using the same harness as the existing tests (consult lines 172-248 for the pattern).
    2. Create a single `it('salePrice form state survives a useLiveQuery re-emission that mutates job.sellingPrice', async () => { ... })`.
    3. Body sequence:
       a. `const job = makeJob({ sellingPrice: 0 });` (initial sellingPrice doesn't matter — EDIT mode reads from editingSale).
       b. `const editingSale = makeSale({ unitPrice: 50 });`
       c. `const props = { job, userCurrency: 'USD' as const, shippingConfig, editingSale, convertingFromQuote: null, isOpen: true, onClose: () => {} };` (omit userProfile — Task 2 removes it from the interface; reuse the existing test file's shippingConfig fixture if present).
       d. Initial render: `await act(async () => { root!.render(<RecordSaleModal {...props} />); });`
       e. Query the salePrice input — use the existing test file's strategy (probably `container!.querySelector('input[type="number"]')` filtered by sibling label text, OR a more specific selector if the file uses one). Assert `(input as HTMLInputElement).value === '50'`.
       f. Re-render with mutated job: `await act(async () => { root!.render(<RecordSaleModal {...props} job={{ ...job, sellingPrice: 999 }} />); });`
       g. Query salePrice input again. Assert value === '50' (not '999').
    4. If the existing harness uses a different render strategy (e.g., a helper that constructs the wrapper component), match that exact strategy.

    Do NOT modify any existing describe block. Do NOT remove any existing tests.
  </action>

  <verify>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 &amp;&amp; npm test -- RecordSaleModal.test.tsx 2>&amp;1 | grep -E "(WR-02|FAIL|PASS)" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "describe.*WR-02.*hydration dep array stability.*Phase 22.1 D-09" src/components/RecordSaleModal.test.tsx` returns 1
    - `grep -c "salePrice form state survives" src/components/RecordSaleModal.test.tsx` returns 1
    - The new it case contains a re-render call with `job={{ ...job, sellingPrice: 999 }}` or equivalent shape — `grep -c "sellingPrice: 999" src/components/RecordSaleModal.test.tsx` returns at least 1
    - At this point in the plan, the new test EITHER fails (RED) OR passes (GREEN). RED is acceptable; Task 2 ships the GREEN dep-array fix
    - Other existing tests in the file remain unchanged
  </acceptance_criteria>

  <done>
    The WR-02 regression describe block exists with a single it case that exercises a re-render with mutated job.sellingPrice. Test may be RED until Task 2 lands the dep-array fix — this is intentional TDD ordering.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply WR-01 dead-prop removal + WR-02 dep-array fix (GREEN) and update JobsManager callsite</name>

  <files>
    src/components/RecordSaleModal.tsx
    src/components/JobsManager.tsx
  </files>

  <read_first>
    src/components/RecordSaleModal.tsx lines 1-20 (imports — confirm UserProfile is on the import line from ../types)
    src/components/RecordSaleModal.tsx lines 58-79 (RecordSaleModalProps interface + destructuring — confirm userProfile is in interface but NOT in destructuring, per Pitfall 5)
    src/components/RecordSaleModal.tsx lines 107-160 (the hydration useEffect — confirm it ends at line 155 with `}, [editingSale, convertingFromQuote, job.sellingPrice]);`)
    src/components/JobsManager.tsx lines 1395-1420 (the RecordSaleModal callsite — confirm userProfile is passed at line 1406)
    src/components/JobsManager.tsx lines 1470-1490 (CONFIRM the userProfile passed to PrintQuoteModal at line 1477 is on a DIFFERENT component and must NOT be touched)
    .planning/phases/22.1-break-even-formula-reconciliation/22.1-PATTERNS.md §`src/components/RecordSaleModal.tsx`, §`src/components/JobsManager.tsx`
    .planning/phases/22.1-break-even-formula-reconciliation/22.1-RESEARCH.md §Critical Findings + §Common Pitfalls 5, 6
  </read_first>

  <behavior>
    WR-01 (D-10) — dead-prop removal:
    - RecordSaleModalProps interface (line 60): remove the `userProfile: UserProfile;` line entirely.
    - Imports (line 4): remove `UserProfile` from the named imports from `../types`. If it's the only one removed, the line stays; if removing it leaves a dangling comma, clean up the comma.
    - The function destructuring at line 70-79 already does NOT include userProfile (Pitfall 5 confirms this) — no change needed there.
    - JobsManager.tsx callsite (line 1406): remove the line `userProfile={userProfile}`. Leave the surrounding callsite (lines 1403-1413) otherwise untouched. The PrintQuoteModal callsite at line 1477 keeps its `userProfile={userProfile}` — that's a different component.

    WR-02 (D-09) — dep array stabilization:
    - The hydration useEffect at line 114-155 has its dep array at line 155: `}, [editingSale, convertingFromQuote, job.sellingPrice]);`.
    - Replace with: a multi-line Pitfall 3 explanation comment, then an eslint-disable-next-line directive, then the new dep array `[editingSale?.id, convertingFromQuote?.id]`.
    - The body of the useEffect (the three-branch if/else if/else hydration logic) is UNCHANGED.

    After both changes, the WR-02 regression test from Task 1 turns GREEN: re-rendering with a mutated job.sellingPrice no longer triggers the hydration useEffect (because the dep array now keys on `editingSale?.id` + `convertingFromQuote?.id`, both of which are stable across the re-render).

    Pitfall 6 enforcement: the eslint-disable comment MUST be preceded by a multi-line `//` comment explaining WHY job.sellingPrice was dropped — otherwise future developers will "fix" it back.
  </behavior>

  <action>
    RecordSaleModal.tsx changes:

    1. Imports cleanup (line 4): open the import line that brings in types from `'../types'`. Remove `UserProfile` from the named imports. Verify the surrounding comma-separated list stays valid (no double commas, no trailing comma issues).

    2. RecordSaleModalProps interface (lines 58-68): delete the `userProfile: UserProfile;` declaration at line 60. The interface ends up 9 lines instead of 10.

    3. Hydration useEffect dep array (line 155): replace the entire dep array line with:
       ```
       // Pitfall 3: DO NOT add job.sellingPrice — a concurrent useLiveQuery emission
       // that updates the job's selling price while the modal is open must NOT clobber
       // the user's in-progress form state. Only editingSale/convertingFromQuote
       // identity drives re-hydration (Phase 22.1 D-09 / WR-02).
       // eslint-disable-next-line react-hooks/exhaustive-deps
       }, [editingSale?.id, convertingFromQuote?.id]);
       ```
       The comment block is INSIDE the function body but BEFORE the closing `}, [...])` of the useEffect — i.e., the comments precede the closing line.

    JobsManager.tsx changes:

    1. RecordSaleModal callsite (lines 1403-1413): remove the single line `userProfile={userProfile}` at line 1406. The callsite becomes 8 props instead of 9. Do not touch the PrintQuoteModal callsite at line 1474 onwards.

    Verify the unrelated `userProfile` references in JobsManager.tsx are NOT touched. The only edit in JobsManager.tsx for this plan is the removal of the single line at 1406.
  </action>

  <verify>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 &amp;&amp; npx tsc -b &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc -b` exits 0 with no new errors
    - `grep -nE "userProfile: UserProfile" src/components/RecordSaleModal.tsx` returns 0 matches
    - `grep -nE "^import.*UserProfile.*from '\\.\\./types'" src/components/RecordSaleModal.tsx` returns 0 matches (UserProfile no longer imported)
    - `grep -nE "userProfile" src/components/RecordSaleModal.tsx` returns 0 matches (no orphan references remain in the modal file)
    - `grep -nE "userProfile=\\{userProfile\\}" src/components/JobsManager.tsx` returns exactly 1 match (the PrintQuoteModal callsite at line ~1477 — confirms only the RecordSaleModal pass was removed)
    - `grep -c "Pitfall 3: DO NOT add job.sellingPrice" src/components/RecordSaleModal.tsx` returns 1
    - `grep -c "eslint-disable-next-line react-hooks/exhaustive-deps" src/components/RecordSaleModal.tsx` returns at least 1 (the WR-02 fix; possibly more if the file has other disables)
    - `grep -cE "\\}, \\[editingSale\\?\\.id, convertingFromQuote\\?\\.id\\]" src/components/RecordSaleModal.tsx` returns 1
    - `grep -cE "\\}, \\[editingSale, convertingFromQuote, job\\.sellingPrice\\]" src/components/RecordSaleModal.tsx` returns 0 (the old dep array is gone)
    - `npm test` exits 0 — full vitest suite green including the WR-02 regression case (now GREEN)
  </acceptance_criteria>

  <done>
    RecordSaleModalProps no longer declares userProfile; UserProfile no longer imported in the modal file; JobsManager no longer passes userProfile to RecordSaleModal (PrintQuoteModal callsite untouched); hydration dep array narrowed to [editingSale?.id, convertingFromQuote?.id] with Pitfall 3 comment + eslint-disable directive; WR-02 regression test passes; tsc -b clean; full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | No new attack surfaces — pure code-cleanup + dep-array stabilization touching only local component state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.1-05 | Information Disclosure | RecordSaleModal stops accepting userProfile prop | accept | Prop was already dead inside the component (Pitfall 5 — declared in interface but never destructured/read). Removing it shrinks the public API; no behavior change |
| T-22.1-06 | Tampering | hydration dep array narrowed — could mask a real re-hydration need | accept | The intentional behavior change is in scope (D-09): in-progress form state survives liveQuery re-emissions. Locked by a regression test (Task 1). Pitfall 3 comment + eslint-disable directive document the WHY so future "fixes" don't revert it |

No new packages, no new permissions, no schema-version bump. Severity: none. No blockers.
</threat_model>

<verification>
After both tasks land:
- `npx tsc -b` exits 0
- `npm test` exits 0 — full vitest suite green
- `grep -nE "userProfile" src/components/RecordSaleModal.tsx` returns 0 matches (clean)
- `grep -nE "userProfile=\\{userProfile\\}" src/components/JobsManager.tsx` returns 1 match only (the surviving PrintQuoteModal callsite at line ~1477)
- WR-02 regression test passes (RecordSaleModal.test.tsx new describe block)
</verification>

<success_criteria>
- WR-01 (D-10) satisfied: userProfile prop removed from RecordSaleModalProps interface and from JobsManager callsite; UserProfile import dropped from RecordSaleModal.tsx
- WR-02 (D-09) satisfied: hydration useEffect dep array narrowed to [editingSale?.id, convertingFromQuote?.id] with Pitfall 3 explanatory comment + eslint-disable-next-line directive
- ROADMAP Phase 22.1 success criterion #6 satisfied: WR-02 (RecordSaleModal hydration useEffect drops job.sellingPrice from deps with a Pitfall 3 comment), WR-01 (dead userProfile prop removed from RecordSaleModalProps)
- D-11 case #3 (WR-02 regression test) implemented per CONTEXT.md
- No regression in any existing test (full vitest suite green)
- D-09 / D-10 traced to source: every change references the originating decision in code comments
</success_criteria>

<output>
Create `.planning/phases/22.1-break-even-formula-reconciliation/22.1-03-SUMMARY.md` when both tasks complete.

Summary MUST include:
- Confirmation that `userProfile` is gone from RecordSaleModal.tsx and from the JobsManager RecordSaleModal callsite (with line numbers)
- Confirmation that the PrintQuoteModal callsite still receives userProfile (untouched)
- Final shape of the dep array + the Pitfall 3 comment block
- WR-02 regression test name + pass status
- Any deviations from the plan
</output>
