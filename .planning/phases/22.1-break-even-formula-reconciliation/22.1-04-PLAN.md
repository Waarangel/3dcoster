---
phase: 22.1-break-even-formula-reconciliation
plan: 04
type: execute
wave: 3
depends_on: [22.1-01, 22.1-02, 22.1-03]
files_modified: []
autonomous: false
requirements: [PERF-08, DATA-07]
requirements_addressed: [PERF-08, DATA-07]
tags: [uat, manual-verification, reconcile, formula-correctness]

must_haves:
  truths:
    - "Dev server runs on port 4173 (project standard)"
    - "User reloads the app and the existing 'Complex Test' job (from Phase 22 UAT) shows a NEW (higher) break-even pill number reflecting the widened formula"
    - "No job in JobsManager displays NaN, Infinity, or undefined in the break-even pill"
    - "Calculator widget Break-even Units number === JobsManager pill breakEvenCopies for the same job when opened in Calculator (after the user clicks Edit)"
    - "After one reload, the console shows no repeated 'fixedCosts reconcile' errors (the reconcile flag flipped to true and stayed there)"
    - "Orphan jobs (printerInstanceId references a deleted printer, if any exist) log a single [reconcileFixedCostsAtSave] orphan warning on the FIRST reload only; subsequent reloads are silent (job-level idempotency)"
    - "All existing data is preserved — no jobs deleted, no fields wiped, no surprise data loss"
  artifacts: []
  key_links: []
---

<objective>
Manual verification that Phase 22.1's formula reconciliation works against the real user data the divergence was originally surfaced on. This plan does NOT modify code — it only runs the live app and inspects the result of the reconcile + formula change against the user's existing IndexedDB.

Purpose: Locks Phase 22.1 success criterion #8 (existing user data shows corrected break-even pill values after one reload, with no surprise data loss or NaN). The automated round-trip test in Plan 22.1-02 proves the formula is correct on synthetic data; this UAT proves it works against the real production data the user reported during Phase 22 UAT.

Output: A human checkpoint approving the rollout, OR a list of issues blocking ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-CONTEXT.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-01-PLAN.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-02-PLAN.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight build + start dev server in background</name>

  <files></files>

  <read_first>
    .planning/phases/22.1-break-even-formula-reconciliation/22.1-VALIDATION.md §Manual-Only Verifications
    .claude/CLAUDE.md (dev server port pinning + Vercel tsc -b enforcement)
  </read_first>

  <behavior>
    Before opening the browser:
    1. Run `npx tsc -b` — MUST exit 0 (no TypeScript errors that would block the Vercel build).
    2. Run `npm test` — MUST exit 0 (the round-trip test from 22.1-02 + WR-02 regression from 22.1-03 + 4 reconcile cases from 22.1-01 are all GREEN).
    3. Kill any process currently using port 4173.
    4. Start `npm run dev` in the background.

    Per project memory rule (feedback_start_dev_server_for_uat.md): the agent starts the dev server itself, does not paste the command at the user and wait.
  </behavior>

  <action>
    Sequence:
    1. `npx tsc -b` — confirm exit code 0.
    2. `npm test` — confirm exit code 0.
    3. Check for an existing process on port 4173: `lsof -ti:4173 | xargs -r kill -9 2>/dev/null || true`.
    4. Start the dev server in the background via the Bash tool with run_in_background=true: `npm run dev`.
    5. Wait for the server's "Local: http://localhost:4173" log line in the background output, then proceed to Task 2. Do NOT poll in a sleep loop — read the background output stream.
  </action>

  <verify>
    <automated>cd /Users/marcusdickinson/Projects/3DCoster/.claude/worktrees/pedantic-ride-ab48c5 &amp;&amp; npx tsc -b &amp;&amp; npm test &amp;&amp; curl -sf http://localhost:4173 > /dev/null &amp;&amp; echo "DEV_SERVER_READY"</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc -b` exit code is 0
    - `npm test` exit code is 0
    - `curl -sf http://localhost:4173` returns HTTP 200 (the dev server is reachable)
    - Background process for `npm run dev` is alive
  </acceptance_criteria>

  <done>
    Pre-flight gates green; dev server reachable at http://localhost:4173.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT — verify Phase 22 Complex Test job + spot-check every other job</name>

  <files></files>

  <action>
    Drive the user through the manual UAT below using the dev server from Task 1. The agent does not write code or files in this task — it presents the verification checklist to the user, waits for the user to test in the browser, and records the verdict.
  </action>

  <what-built>
    The full Phase 22.1 stack:
    - PrintJob.fixedCostsAtSave schema field (plan 01)
    - reconcileFixedCostsAtSave helper + fixedCostsReconcileRan module flag in useJobs() (plan 01)
    - CostCalculator writes the snapshot on Save + Update (plan 02)
    - JobsManager computeBreakEvenInfo widens the numerator (plan 02)
    - RecordSaleModal WR-01 / WR-02 carry-overs (plan 03)
  </what-built>

  <how-to-verify>
    1. Open http://localhost:4173 in the browser. Open DevTools → Console.
    2. **First-reload reconcile check:** Observe the console. Expected:
       - NO red errors mentioning `fixedCosts reconcile failed`
       - At MOST a small number of `[reconcileFixedCostsAtSave] orphan job ...` warnings (only if any of your jobs reference a deleted printer)
    3. **Phase 22 UAT regression target:** Navigate to JobsManager. Locate the `Complex Test` job from the Phase 22 UAT screenshot (the one that previously read "1 more to break even" using the narrow formula).
       - Confirm: the new "more to break even" number is HIGHER than 1 (or "fully recovered" — either is acceptable). The number should reflect modelCost + depreciation + nozzleWear divided by the per-unit profit, not modelCost alone.
       - Open the job in Calculator (click Edit). Read the "Break-even Units" number in the Calculator's break-even section.
       - Go back to JobsManager. The pill's `breakEvenCopies` (the number in the "X more to break even" copy) MUST equal the Calculator's "Break-even Units" minus the job's `copiesSold` (or equal it exactly if the job has zero sales).
    4. **NaN / Infinity / undefined sweep:** Scroll through ALL jobs in JobsManager. For every job, confirm:
       - The break-even pill copy is a finite integer (e.g. "3 more to break even" or "Fully recovered"), NOT "Infinity more to break even", NOT "NaN more to break even", NOT "undefined more to break even"
       - The "Profit per unit" + "Revenue earned" copy renders without NaN/undefined
       - Tags, customer info on sales rows, prices all render normally
    5. **Reload idempotency:** Hard-reload the page (Cmd+Shift+R / Ctrl+Shift+R). Re-open DevTools console.
       - Expected: NO new `[reconcileFixedCostsAtSave] orphan job ...` warnings on the second reload (every job now has `fixedCostsAtSave` set from the first reload's reconcile bulkPut — orphan warnings only fire on the first run because subsequent runs skip the already-snapshotted jobs).
    6. **Record Sale modal smoke check (WR-01 + WR-02):** From JobsManager, click any job's "Record Sale" button. The modal opens (Phase 22 Modal primitive). Close it. Open it again with a different job. Edit any existing sale (click pencil on a sale row). The modal opens with the sale's existing values populated. While the modal is open, do NOT type — wait 3-5 seconds (giving any concurrent liveQuery emission a chance to fire), then close it. Form should not have spontaneously cleared or changed values mid-wait.
  </how-to-verify>

  <resume-signal>
    Type "approved" to confirm Phase 22.1 ships, OR list specific failures (job name + observed value + expected value) for gap-closure planning.
  </resume-signal>

  <verify>
    <human-check>User reviews the 6-step UAT checklist in the running app and either approves or returns a specific failure list.</human-check>
  </verify>

  <done>
    User explicitly approves Phase 22.1 (types "approved" or equivalent confirmation), OR returns a concrete list of failures that becomes the input to /gsd:plan-phase 22.1 --gaps.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Manual verification only — no code changes, no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.1-07 | Tampering | reconcile runs against the user's real IndexedDB on app load | accept | The reconcile is idempotent (jobs with fixedCostsAtSave defined are skipped), errors reset the flag for retry rather than corrupting state, and the user verifies the result before approving ship. If the UAT reveals data drift, the gap-closure plan addresses it before merge |

No new packages, no new permissions, no schema-version bump. Severity: none. No blockers.
</threat_model>

<verification>
- Pre-flight: tsc -b green + npm test green + dev server reachable at port 4173
- UAT: User confirms break-even values are corrected for the `Complex Test` job and that no NaN/Infinity/undefined values appear anywhere in the JobsManager
- Reconcile idempotency confirmed: second reload produces no new orphan warnings in the console
</verification>

<success_criteria>
- ROADMAP Phase 22.1 success criterion #8 satisfied: "Manual UAT: existing user data shows the corrected break-even pill values after one reload (with no surprise data-loss or NaN)"
- ROADMAP Phase 22.1 success criterion #7 fully satisfied (now that all three predecessor plans + the round-trip test are green): "`npx tsc -b` clean; full vitest suite passes; the round-trip test from #5 passes"
- PERF-08 + DATA-07 close out
- User explicitly approves the phase or returns a specific failure list for gap closure
</success_criteria>

<output>
Create `.planning/phases/22.1-break-even-formula-reconciliation/22.1-04-SUMMARY.md` when the human UAT completes.

Summary MUST include:
- The dev server URL + pre-flight test status
- The user's verdict (approved / failures)
- For each failure (if any): job name + observed value + expected value + console output
- Confirmation that the dev server was cleanly stopped before phase close
</output>
