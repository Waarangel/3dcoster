---
plan: 22.1-04
phase: 22.1-break-even-formula-reconciliation
type: execute
wave: 3
status: complete
completed: 2026-05-28
requirements_addressed: [PERF-08, DATA-07]
files_modified: []
---

## Plan 22.1-04 — Manual UAT (Human Verification)

Manual verification against the user's real IndexedDB data on `Complex Test` (the regression target from Phase 22 UAT). No code modified. Plan ran the live app, confirmed the reconcile fired, and validated the Calculator ↔ JobsManager round-trip.

## What was verified

**Pre-flight gates (all green):**
- `npx tsc -b` exit 0
- Full vitest suite: 437 pass (1 pre-existing todo)
- Dev server reachable at http://localhost:4173 (HTTP 200)

**Live data verification (Complex Test job, id `job-1779821127120`):**

The user reloaded the app and inspected the job row in DevTools → Application → IndexedDB → 3DCosterDB → jobs. The reconcile wrote a populated snapshot:

| Field | Value |
|---|---|
| `modelCost` | $10.00 |
| `fixedCostsAtSave.depreciation` | $11.805 |
| `fixedCostsAtSave.nozzleWear` | $0.296052 |
| `copiesSold` | 1 |
| `sellingPrice` | $50.00 |
| `costPerUnit` | $34.574210 |
| `profit per unit` | $15.43 |
| `updatedAt` | 2026-05-28 (same day — confirms reconcile bulkPut fired) |

**Round-trip (PERF-08 success criterion):**

| Source | Value |
|---|---|
| Snapshot inputs to widened formula | $10 + $11.805 + $0.296 = **$22.101** |
| Calculator widget "Total Fixed Costs" | **$22.10** ✓ |
| `ceil($22.10 / $15.43)` | **2** |
| Calculator widget "Break-even Units" | **2** ✓ |
| JobsManager pill (1 sold + "1 more" remaining) | **2** ✓ |

**Calculator widget and JobsManager pill arrived at identical break-even numbers** from the same snapshot. The pre-22.1 divergence is closed.

## Initial concern → resolution

The user's first observation was *"after hard refresh the Complex Test still shows '1 more to break even'"* — the same copy that appeared in the Phase 22 UAT screenshot. That looked like the widening hadn't fired.

DevTools data showed otherwise:
- Pre-22.1 narrow numerator = `modelCost` = $10 → `ceil(10 / 15.43)` = **1** → 1 sold ≥ 1 → would have shown **"Break-even reached"** (green pill) on this current data.
- Post-22.1 wide numerator = $22.10 → `ceil(22.10 / 15.43)` = **2** → 1 sold, 1 remaining → shows **"1 more to break even"** (yellow pill).

The widening DID change the pill from "Break-even reached" → "1 more to break even". The Phase 22 UAT screenshot showed "1 more" on different underlying data; the coincidence of copy was confusing but not a regression. The plan's success criterion ("HIGHER than 1 OR Fully recovered") referred to the narrow-formula pre-state at Phase 22 UAT time, not the current data state.

## Other UAT items (user reported)

- **NaN / Infinity / undefined sweep:** User reported jobs other than Complex Test show no break-even pill, which is expected — those jobs have no real costs (modelCost=0, no sales). No `NaN`, `Infinity`, or `undefined` observed anywhere.
- **Reload idempotency** + **Record Sale modal** items were not separately reported on by the user but were folded into the overall approval verdict.

## User verdict

**APPROVED — ship Phase 22.1.**

## Cleanup

- Dev server (PID on port 4173) stopped cleanly after approval.

## Acceptance criteria

- [x] `npx tsc -b` exit 0 (pre-flight)
- [x] Full vitest suite green (pre-flight)
- [x] Dev server reachable at http://localhost:4173 (pre-flight)
- [x] Complex Test job: reconcile wrote a populated `fixedCostsAtSave` snapshot
- [x] Calculator "Break-even Units" === JobsManager pill `breakEvenCopies` (both = 2)
- [x] No `NaN` / `Infinity` / `undefined` anywhere in JobsManager (per user sweep)
- [x] User explicitly approved phase ship
- [x] Dev server stopped cleanly

## Self-Check: PASSED

PERF-08 + DATA-07 success criteria are met. Phase 22.1 closes the Phase 22 break-even divergence bug and the two deferred WR-01 + WR-02 RecordSaleModal warnings, with a snapshot field that survives liveQuery emissions for future no-recompute renders.
