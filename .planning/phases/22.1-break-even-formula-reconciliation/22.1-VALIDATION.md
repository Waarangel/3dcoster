---
phase: 22.1
slug: break-even-formula-reconciliation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-27
---

# Phase 22.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Source: 22.1-RESEARCH.md §Validation Architecture (researcher confidence: HIGH).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest ^4.1.4 + raw `createRoot` + `act` (no `@testing-library/react` per project convention) |
| **Config file** | `vite.config.ts` (vitest config inline) |
| **Quick run command** | `npm test` |
| **Full suite command** | `npm run build` (runs `tsc -b && vite build`; includes vitest run) |
| **Estimated runtime** | ~5s for `npm test`, ~30s for `npm run build` |

---

## Sampling Rate

- **After every task commit:** Run `npm test`
- **After every plan wave:** Run `npm run build`
- **Before `/gsd:verify-work`:** `npm run build` must be green
- **Max feedback latency:** ~5 seconds (per-task), ~30 seconds (per-wave)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 22.1-01-01 | 01 | 1 | DATA-07 | — | N/A (local-only IndexedDB) | unit | `npm test src/db/backfill.test.ts` | ❌ W0 — extend existing 584-line file with `describe('reconcileFixedCostsAtSave (Phase 22.1 DATA-07)')` block | ⬜ pending |
| 22.1-02-01 | 02 | 2 | PERF-08 | — | N/A | unit | `npm test src/components/JobsManager.test.tsx` | ❌ W0 — new round-trip case in existing file | ⬜ pending |
| 22.1-03-01 | 03 | 2 | (D-09/WR-02) | — | N/A | regression | `npm test src/components/RecordSaleModal.test.tsx` | ❌ W0 — extend 400+-line file with new `describe('WR-02 regression: form state survives liveQuery re-emission')` block | ⬜ pending |
| 22.1-04-01 | 04 | 3 | PERF-08, DATA-07 | — | N/A | manual UAT | `npm run dev` (port 4173) | ✅ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Final Task IDs assigned by planner; this row layout reflects the expected plan structure (helper + tests, computeBreakEvenInfo + round-trip test, RecordSaleModal carry-overs, UAT). Planner may consolidate or split.*

---

## Wave 0 Requirements

- [ ] `src/db/backfill.test.ts` — extend with `describe('reconcileFixedCostsAtSave (Phase 22.1 DATA-07)')` block covering 4 cases:
  - job with `fixedCostsAtSave` already set → returned unchanged (idempotent on job level)
  - legacy job + live printer + valid materials → correct depreciation + nozzleWear snapshot (mirrors `calculateCost()`)
  - legacy job + missing printer → `{ depreciation: 0, nozzleWear: 0 }` + `console.warn` with job id + name
  - running helper twice in sequence → bit-identical output (idempotent over full run)
- [ ] `src/components/JobsManager.test.tsx` — extend with PERF-08 round-trip test: save job through `CostCalculator` save handler → invoke `reconcileFixedCostsAtSave` (no-op for newly-saved jobs) → render `JobsManager` pill → assert Calculator's "Break-even Units" displayed number === JobsManager pill's `breakEvenCopies`
- [ ] `src/components/RecordSaleModal.test.tsx` — extend with WR-02 regression: render with `editingSale` set, simulate `useLiveQuery` emission mutating `job.sellingPrice`, assert form state for `salePrice` is unchanged

*Vitest, raw createRoot + act, and the existing test files are all present — no new framework install required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Existing user data shows corrected break-even pill values after one reload | Phase 22.1 success criterion #8 | Reconcile runs only against real IndexedDB data populated outside of test fixtures | 1) Build + run dev (`npm run dev`, port 4173). 2) Open the running app with existing jobs (`Complex Test` job from Phase 22 UAT). 3) Open browser DevTools console; observe one log line per orphan job (if any). 4) Open `JobsManager`; verify pill values now reflect `(modelCost + depreciation + nozzleWear) / effectiveProfitPerUnit` — for the `Complex Test` job, the number should be higher than before. 5) Open the same job in `CostCalculator`; confirm "Break-even Units" matches the pill exactly. 6) Reload the page; confirm no `console.warn` repeats for the same orphan (idempotent on job level — `fixedCostsAtSave` is now populated). |
| No surprise data loss or NaN | Phase 22.1 success criterion #8 | Side-effect-free verification requires inspecting user's real DB | After reload, open every job in `JobsManager` and `CostCalculator`; assert no `NaN`, `Infinity`, or `undefined` displayed in cost/break-even fields. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (backfill.test.ts, JobsManager.test.tsx, RecordSaleModal.test.tsx — all extended in-place per RESEARCH.md §Critical Findings #3)
- [ ] No watch-mode flags (use `npm test` which runs `vitest run`, not `vitest watch`)
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
