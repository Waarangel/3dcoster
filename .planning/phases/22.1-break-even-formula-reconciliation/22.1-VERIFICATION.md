---
phase: 22.1-break-even-formula-reconciliation
verified: 2026-05-28T09:32:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
---

# Phase 22.1: Break-even formula reconciliation Verification Report

**Phase Goal:** The CostCalculator "Break-even Units" widget and the JobsManager break-even pill compute the same number for the same job. Calculator's formula wins — break-even fixed costs = `modelCost + printerDepreciation + nozzleWear` (not `modelCost` only as JobsManager currently does). Existing IndexedDB jobs are reconciled in place so the change isn't forward-only.
**Verified:** 2026-05-28T09:32:00Z
**Status:** passed
**Re-verification:** No — initial verification (REVIEW.md already marked status=resolved with fix commits before verification began; this verifier independently confirmed the fixes in the codebase)

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth                                                                                                                                                                                                            | Status     | Evidence                                                                                                                                                                                                                                                                                       |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `PrintJob` carries a `fixedCostsAtSave: { depreciation: number; nozzleWear: number }` snapshot; optional + non-indexed; Dexie schema string unchanged                                                            | ✓ VERIFIED | `src/types.ts:364` — field declared as `fixedCostsAtSave?: { depreciation: number; nozzleWear: number };` after the Phase 14 D-18 optional-field block (line 355's `marketplace?`). Multi-line comment block at 357-363 names Phase 22.1 D-02 and confirms non-indexed. Dexie schema unchanged. |
| 2   | `CostCalculator` writes both fields on Save (new) and Update (edit)                                                                                                                                              | ✓ VERIFIED | `grep -n "fixedCostsAtSave" src/components/CostCalculator.tsx` → 2 hits at lines 605 (Update path inside `updatedJob` literal) and 647 (Create path inside new `job` literal). Both read from existing `fixedCosts` memo (lines 449-457).                                                      |
| 3   | `JobsManager.tsx`'s `computeBreakEvenInfo` uses `modelCost + (fixedCostsAtSave?.depreciation ?? 0) + (fixedCostsAtSave?.nozzleWear ?? 0)` as numerator                                                            | ✓ VERIFIED | `src/components/JobsManager.tsx:73-76` — `fixedNumerator = fixedModelCost + (job.fixedCostsAtSave?.depreciation ?? 0) + (job.fixedCostsAtSave?.nozzleWear ?? 0)`. Infinity guard widened to `fixedNumerator > 0` at line 82. `computeBreakEvenInfo` is exported (line 49).                      |
| 4   | One-time reconcile helper populates `fixedCostsAtSave` on every legacy PrintJob; runs on app startup behind idempotent flag; ships with unit test                                                                | ✓ VERIFIED | `src/db/backfill.ts:494-549` — `reconcileFixedCostsAtSave(jobs, printerInstances, printers, materials)` is pure, idempotent (skips jobs with snapshot set), mirrors `calculateDepreciation` + `calculateNozzleWear`, no failureRate multiplier. Wired in `src/hooks/useDatabase.ts:518-567` behind `fixedCostsReconcileRan` module flag. **4 Vitest cases** in `src/db/backfill.test.ts` at lines 660-740. |
| 5   | Calculator's "Break-even Units" === JobsManager pill's `breakEvenCopies` for the same job (after save+reload) — locked by round-trip test                                                                        | ✓ VERIFIED | `src/components/JobsManager.test.tsx:795-914` — PERF-08 describe block with 5 it cases (4 originally planned + 1 CR-02 regression). All 5 pass. Live UAT in `22.1-04-SUMMARY.md` confirmed `Complex Test` job: Calculator displays Break-even=2, JobsManager pill computes 2 (1 sold + 1 more). |
| 6   | Folded-in Phase 22 carry-overs: WR-02 (RecordSaleModal hydration dep array drops `job.sellingPrice` with Pitfall 3 comment), WR-01 (dead `userProfile` prop removed)                                             | ✓ VERIFIED | WR-01: `grep -n "userProfile" src/components/RecordSaleModal.tsx` → 0 hits (interface trimmed, UserProfile import dropped). `grep -nE "userProfile=\{userProfile\}" src/components/JobsManager.tsx` → 1 hit at line 1495 (PrintQuoteModal callsite — different component). RecordSaleModal callsite at lines 1423-1430 has no userProfile. WR-02: `src/components/RecordSaleModal.tsx:154-159` — Pitfall 3 comment block + `eslint-disable-next-line react-hooks/exhaustive-deps` + `}, [editingSale?.id, convertingFromQuote?.id]);`. WR-02 regression test at `src/components/RecordSaleModal.test.tsx:471-515`. |
| 7   | `npx tsc -b` clean; full vitest suite passes; round-trip test passes                                                                                                                                             | ✓ VERIFIED | `npx tsc -b` → exit 0 (no errors). `npx vitest run` → 28 test files passed (28/28), 438 tests passed + 1 todo (439 total). All 5 PERF-08 it cases pass (including CR-02 regression). All 4 backfill reconcile cases pass.                                                                       |
| 8   | Manual UAT: existing user data shows corrected pill values after one reload (no data loss, no NaN) — already approved by human user                                                                              | ✓ VERIFIED | `.planning/phases/22.1-break-even-formula-reconciliation/22.1-04-SUMMARY.md` records the user's "APPROVED — ship Phase 22.1" verdict. Real `Complex Test` job (id `job-1779821127120`): pre-22.1 narrow numerator = $10 ($modelCost only) → 1 copy; post-22.1 wide numerator = $22.10 ($10 + $11.805 + $0.296) → 2 copies (1 sold, 1 remaining). Calculator widget and pill both arrived at 2 — round-trip confirmed against real data. |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact                                       | Expected                                                                                | Status     | Details                                                                                                                                                                                                                                              |
| ---------------------------------------------- | --------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/types.ts`                                 | PrintJob.fixedCostsAtSave optional non-indexed field                                    | ✓ VERIFIED | Line 364, optional `?:`, nested `{ depreciation: number; nozzleWear: number }`. Multi-line D-02 comment block preserves Dexie schema string unchanged.                                                                                              |
| `src/db/backfill.ts`                           | `reconcileFixedCostsAtSave` pure helper (Path B faithful reconstruction)                | ✓ VERIFIED | Lines 494-549. No `failureRate` in formulas (only in pedagogical comments). No `dexie` imports (jsdom-safe). Imports `getMaterialDensity` from `gcodeParser`. CR-01 contract documented in JSDoc line 435.                                            |
| `src/db/backfill.test.ts`                      | `describe('reconcileFixedCostsAtSave (Phase 22.1 DATA-07)')` with 4 cases               | ✓ VERIFIED | Line 601. Four it cases at lines 660, 666, 694, 715: idempotency, snapshot, orphan+warn, full-run idempotency. All pass.                                                                                                                             |
| `src/hooks/useDatabase.ts`                     | `fixedCostsReconcileRan` module flag + useEffect calling reconcileFixedCostsAtSave      | ✓ VERIFIED | Flag at line 27. useEffect at lines 518-567. **CR-01 fix in place** at lines 530-538: reads `db.materials.where('category').equals('printer')` and maps via `assetToPrinterConfig`, NOT `db.printers.toArray()`. **WR-01 fix at line 557**: flag set AFTER work completes. **WR-03 fix at lines 549-553**: `db.jobs.put(updatedJob)` preserves original `updatedAt` (no `new Date()` stamp). |
| `src/components/JobsManager.tsx`               | `computeBreakEvenInfo` exported + widened numerator + CR-02 modelCostPerUnit honor      | ✓ VERIFIED | Function exported at line 49. `fixedNumerator` at lines 73-76. **CR-02 fix at line 73**: `const fixedModelCost = job.modelCostPerUnit ? 0 : job.modelCost;` mirrors CostCalculator behavior. Comment at 69-72 names CR-02 explicitly. Infinity guard widened at line 82.                                                  |
| `src/components/JobsManager.test.tsx`          | PERF-08 round-trip test + CR-02 regression case                                         | ✓ VERIFIED | PERF-08 describe at line 795. 5 it cases: snapshotted job (line 796, expects 7), legacy fallback (line 820, expects 5), zero-modelCost (line 836, expects 25), reconcile no-op (line 853, expects []), CR-02 (line 894, expects 0 for modelCostPerUnit=true). All pass. |
| `src/components/CostCalculator.tsx`            | fixedCostsAtSave snapshot writes on Save + Update                                       | ✓ VERIFIED | 2 hits at lines 605 (Update path) and 647 (Create path). Both read `fixedCosts.depreciation` + `fixedCosts.nozzleWear` from existing memo.                                                                                                          |
| `src/components/RecordSaleModal.tsx`           | userProfile prop removed; hydration dep array narrowed                                  | ✓ VERIFIED | `grep -n "userProfile" src/components/RecordSaleModal.tsx` → 0 hits (interface trimmed at line ~60, UserProfile import dropped at line ~4). Pitfall 3 block at lines 154-158 + `eslint-disable-next-line react-hooks/exhaustive-deps` at line 158 + new dep array `[editingSale?.id, convertingFromQuote?.id]` at line 159. |
| `src/components/RecordSaleModal.test.tsx`      | WR-02 regression test                                                                   | ✓ VERIFIED | `describe('WR-02 — hydration dep array stability (Phase 22.1 D-09)')` at line 471. Single it case at line 472 renders modal in EDIT mode with editingSale.unitPrice=50, re-renders with mutated `job.sellingPrice=999`, asserts price input still reads '50'. Pass. |
| `src/data/defaultMaterials.ts`                 | `assetToPrinterConfig` exported (for CR-01 fix)                                         | ✓ VERIFIED | `export function assetToPrinterConfig(asset: Asset): PrinterConfig | null` at line 449. Used at `src/hooks/useDatabase.ts:537` in the reconcile useEffect.                                                                                              |

### Key Link Verification

| From                                                          | To                                                              | Via                                                                       | Status   | Details                                                                                                                                                                                  |
| ------------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CostCalculator.tsx` `handleSaveJob`                          | `PrintJob.fixedCostsAtSave`                                     | Direct write inside Update (line 605) + Create (line 647) payload literals | WIRED    | Both reads come from existing `fixedCosts` memo at line 449-457 (`depreciation: costs.printerDepreciation, nozzleWear: costs.nozzleWear`). Snapshot is byte-identical to displayed memo. |
| `JobsManager.tsx` `computeBreakEvenInfo`                      | `PrintJob.fixedCostsAtSave`                                     | Optional-chaining nullish-coalesce reads on lines 75-76                   | WIRED    | Used inside `fixedNumerator` declaration (line 74), feeds `Math.ceil(fixedNumerator / effectiveProfitPerUnit)` at line 81 and the Infinity guard at line 82.                            |
| `useDatabase.ts useJobs()`                                    | `src/db/backfill.ts reconcileFixedCostsAtSave`                  | Named import on line 6 + invocation inside useEffect at line 539           | WIRED    | Reads three tables (printerInstances, printer-category materials, all materials), maps via `assetToPrinterConfig`, then calls helper. Writes via `db.transaction('rw', db.jobs, ...)`.   |
| `JobsManager.test.tsx`                                        | `computeBreakEvenInfo` (JobsManager.tsx)                        | Named import at top + 5 inline calls in PERF-08 describe block             | WIRED    | All 5 PERF-08 cases call `computeBreakEvenInfo(job, new Map<string, Sale[]>())` directly — verifies the pure module-scope helper.                                                       |
| `JobsManager.test.tsx`                                        | `reconcileFixedCostsAtSave` (backfill.ts)                       | Named import + call inside 4th PERF-08 case (line 890)                    | WIRED    | Consumer-side cross-validation of no-op contract — asserts result is `[]` when job already has snapshot.                                                                                |
| `useDatabase.ts useJobs() reconcile effect`                   | `db.materials` (printer-category filter)                        | `db.materials.where('category').equals('printer').toArray()` at line 532   | WIRED    | **CR-01 critical fix** — reads from canonical source (not the always-empty `db.printers` table). Maps via `assetToPrinterConfig`. CR-01 BLOCKER resolved.                                |

### Data-Flow Trace (Level 4)

| Artifact                              | Data Variable      | Source                                                                                                              | Produces Real Data | Status     |
| ------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------- | ------------------ | ---------- |
| `JobsManager.tsx` pill                | `fixedNumerator`   | Reads `job.modelCost` + `job.fixedCostsAtSave?.depreciation/nozzleWear` (written by CostCalculator at save time; backfilled by reconcileFixedCostsAtSave) | Yes — UAT confirmed `Complex Test` job has depreciation=$11.805, nozzleWear=$0.296 written by reconcile bulkPut | ✓ FLOWING  |
| `CostCalculator.tsx` snapshot         | `fixedCosts.depreciation`, `fixedCosts.nozzleWear` | `useMemo` at line 449-457 reads from `costs.printerDepreciation` + `costs.nozzleWear` (computed live from printer settings via `calculateCost`) | Yes — Calculator widget displayed $22.10 fixed total → snapshot $11.805 + $0.296 + $10 modelCost matches | ✓ FLOWING  |
| `useDatabase.ts` reconcile useEffect  | `printerConfigs`   | `db.materials.where('category').equals('printer').toArray()` mapped via `assetToPrinterConfig` (CR-01 fix)         | Yes — production data confirmed via UAT (reconcile wrote populated fixedCostsAtSave to `Complex Test` job) | ✓ FLOWING  |

### Behavioral Spot-Checks

| Behavior                                                | Command                                                     | Result                                                          | Status |
| ------------------------------------------------------- | ----------------------------------------------------------- | --------------------------------------------------------------- | ------ |
| TypeScript build is clean                               | `npx tsc -b`                                                | exit 0, no errors                                               | ✓ PASS |
| Full vitest suite passes                                | `npx vitest run`                                            | 28 files passed (28/28), 438 tests passed + 1 todo (439 total)  | ✓ PASS |
| PERF-08 round-trip tests pass                           | `npx vitest run src/components/JobsManager.test.tsx`        | 5/5 PERF-08 cases ✓ (all listed under `PERF-08 — break-even formula round-trip (Phase 22.1)`) | ✓ PASS |
| Reconcile helper tests pass                             | `npx vitest run src/db/backfill.test.ts`                    | 4/4 PERF-22.1 DATA-07 cases ✓                                   | ✓ PASS |
| WR-02 regression test passes                            | `npx vitest run src/components/RecordSaleModal.test.tsx`    | WR-02 case ✓                                                    | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan       | Description                                                                                                                              | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                            |
| ----------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PERF-08     | 22.1-02, 22.1-04 | JobsManager break-even pill computes `(modelCost + fixedCostsAtSave.depreciation + fixedCostsAtSave.nozzleWear) / profitPerUnit` matching Calculator | ✓ SATISFIED | computeBreakEvenInfo widened (JobsManager.tsx:73-82); CostCalculator writes snapshot at save (CostCalculator.tsx:605, 647); 5 round-trip test cases pass (including CR-02 modelCostPerUnit fix); real-data UAT confirmed Calculator==pill==2 for `Complex Test` job                                                                                                |
| DATA-07     | 22.1-01, 22.1-04 | reconcileFixedCostsAtSave pure helper, idempotent on job level, handles missing-printer with zero snapshot + console.warn, wired behind module flag | ✓ SATISFIED | Helper at backfill.ts:494-549 (no failureRate; no dexie imports); 4 unit tests cover idempotency, snapshot, orphan, full-run; wired in useDatabase.ts:518-567 behind `fixedCostsReconcileRan` flag (WR-01 fix: flag set after completion). **CR-01 fix confirmed**: helper called with printers from `db.materials` filtered by category==='printer', not the empty `db.printers` table. |
| WR-01       | 22.1-03           | Phase 22 code-review carry-over — dead userProfile prop removed from RecordSaleModalProps and JobsManager callsite                       | ✓ SATISFIED | RecordSaleModal.tsx: 0 references to userProfile; JobsManager.tsx: only line 1495 (PrintQuoteModal callsite) retains userProfile — RecordSaleModal callsite at 1423-1430 is clean.                                                                                                                                                                                |
| WR-02       | 22.1-03           | Phase 22 code-review carry-over — hydration dep array drops job.sellingPrice with Pitfall 3 comment + eslint-disable                     | ✓ SATISFIED | RecordSaleModal.tsx:154-159: 4-line Pitfall 3 comment naming D-09 / WR-02 + eslint-disable directive + `[editingSale?.id, convertingFromQuote?.id]`. Regression test at RecordSaleModal.test.tsx:471-515 passes.                                                                                                                                                  |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |

**None.** Anti-pattern scan on phase-modified files (`src/types.ts`, `src/db/backfill.ts`, `src/db/backfill.test.ts`, `src/hooks/useDatabase.ts`, `src/components/CostCalculator.tsx`, `src/components/JobsManager.tsx`, `src/components/JobsManager.test.tsx`, `src/components/RecordSaleModal.tsx`, `src/components/RecordSaleModal.test.tsx`) found no debt markers introduced by this phase. The 4 `failureRate` occurrences in `src/db/backfill.ts` are explicitly-negative pedagogical comments (NOT code), correctly explaining why the helper does not apply the failureRate multiplier — this is intentional documentation, not a stub.

### Code Review Fixes Verified

All four post-review fixes from REVIEW.md `fixes_applied` are confirmed present in code:

- **CR-01 (commit 642a3bf)** — `useDatabase.ts:530-538` reads printers from `db.materials.where('category').equals('printer').toArray()` and maps via `assetToPrinterConfig`. The vestigial `db.printers.toArray()` call is gone. JSDoc on `reconcileFixedCostsAtSave` (backfill.ts:435) now documents the call-site contract.
- **CR-02 (commit 1499111 + test c5d779a)** — `JobsManager.tsx:73` introduces `const fixedModelCost = job.modelCostPerUnit ? 0 : job.modelCost;` mirroring CostCalculator behavior. New regression case at `JobsManager.test.tsx:894-914` asserts `breakEvenCopies === 0` when `modelCostPerUnit=true, modelCost=100, snapshot zeroed`.
- **WR-01 (commit 1f445f7)** — `fixedCostsReconcileRan = true` set at `useDatabase.ts:557` AFTER the work completes (line 549 transaction); on `cancelled` early-return (line 535) the flag stays false. Symmetric pattern applied to `copiesSoldReconcileRan` (line 488) and `tagsNormalizeRan` (line 604).
- **WR-03 (commit 098b088)** — Snapshot-only reconcile preserves `updatedAt`: line 551 calls `db.jobs.put(updatedJob)` without re-stamping `updatedAt: new Date()`. Comment at lines 542-548 explicitly contrasts this with the content-changing reconciles (copiesSold, tags) which DO bump updatedAt.

### Human Verification Required

**None.** The phase-04 plan was an explicit `checkpoint:human-verify` task and the user already approved with "APPROVED — ship Phase 22.1" (documented in `22.1-04-SUMMARY.md`). The UAT covered: first-reload reconcile output (no errors, populated snapshot on `Complex Test`), Calculator widget=2 / JobsManager pill=2 round-trip, NaN/Infinity/undefined sweep (none), reload idempotency, and Record Sale modal smoke check. The verifier confirmed all of these against the codebase and the SUMMARY file; no remaining human-only checks are open.

### Gaps Summary

No gaps. Every ROADMAP success criterion has codebase evidence. Every requirement (PERF-08, DATA-07, WR-01, WR-02) is satisfied. All four post-review fixes (CR-01, CR-02, WR-01, WR-03) are confirmed present and functioning. The full vitest suite is green (28 files, 438 tests + 1 todo) and `npx tsc -b` exits 0. The human UAT has already returned an explicit approval verdict for the live production data (`Complex Test` job round-trip confirmed Calculator==pill==2).

---

_Verified: 2026-05-28T09:32:00Z_
_Verifier: Claude (gsd-verifier)_
