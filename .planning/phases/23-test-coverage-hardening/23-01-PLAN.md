---
phase: 23-test-coverage-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CustomerEditModal.tsx
  - src/components/CustomerEditModal.test.tsx
  - src/db/backfill.ts
  - src/db/backfill.test.ts
  - src/hooks/useDatabase.ts
autonomous: true
requirements: [TEST-01]
tags: [test, customer, modal, reconcile, email-lowercase]

must_haves:
  truths:
    - "CustomerEditModal.handleSubmit canonicalizes email to lowercase on save (per D-01)"
    - "reconcileCustomerEmailLowercase helper exists in src/db/backfill.ts as a pure function (per D-02)"
    - "useCustomers() runs the email-lowercase reconcile exactly once per page load behind customerEmailLowercaseRan module flag, set AFTER bulkPut completes (per D-02 + WR-01)"
    - "Sale.customer.email snapshots are NEVER mutated by the reconcile — only db.customers rows (per D-03 / CL-05)"
    - "CustomerEditModal.test.tsx covers Add hydration, Edit hydration, Name-OR-Email validation, Escape close path, submit-disable during save, error recovery, and the D-01 lowercase contract (test file path locked by D-11: src/components/CustomerEditModal.test.tsx, sibling to source)"
    - "Test uses raw createRoot + act + the real <Modal> primitive — NO @testing-library/react, NO Modal stub (per D-07, D-08)"
    - "All existing tests still pass after the modal one-line change"
  artifacts:
    - path: "src/components/CustomerEditModal.tsx"
      provides: "Customer edit modal with email-lowercase canonicalization on save"
      contains: "email.trim().toLowerCase()"
    - path: "src/components/CustomerEditModal.test.tsx"
      provides: "First test coverage for CustomerEditModal — locks D-01 email-lowercase contract + 6 baseline behavior tests"
      exports: ["describe", "it"]
      min_lines: 200
    - path: "src/db/backfill.ts"
      provides: "Pure reconcile helper reconcileCustomerEmailLowercase(customers)"
      exports: ["reconcileCustomerEmailLowercase"]
    - path: "src/db/backfill.test.ts"
      provides: "Vitest cases locking reconcileCustomerEmailLowercase pure-helper contract (idempotent, undefined-skip, mixed-case-patch)"
    - path: "src/hooks/useDatabase.ts"
      provides: "useCustomers() useEffect wiring + customerEmailLowercaseRan module flag"
      contains: "customerEmailLowercaseRan"
  key_links:
    - from: "src/components/CustomerEditModal.tsx"
      to: "Customer type"
      via: "handleSubmit returns Customer with lowercased email"
      pattern: "email: email\\.trim\\(\\)\\.toLowerCase\\(\\)"
    - from: "src/components/CustomerEditModal.test.tsx"
      to: "src/components/CustomerEditModal"
      via: "dynamic import after vi.mock"
      pattern: "await import\\('./CustomerEditModal'\\)"
    - from: "src/hooks/useDatabase.ts"
      to: "src/db/backfill.ts"
      via: "named import of reconcileCustomerEmailLowercase"
      pattern: "import.*reconcileCustomerEmailLowercase.*from '\\.\\./db/backfill'"
    - from: "src/hooks/useDatabase.ts useCustomers"
      to: "db.customers.bulkPut"
      via: "bulkPut(patches) inside async IIFE before customerEmailLowercaseRan = true"
      pattern: "db\\.customers\\.bulkPut\\(patches\\)"
---

<objective>
Ship the email-lowercase resolution + first test coverage for CustomerEditModal in one atomic plan. Three coupled deliverables:

1. **Behavior change (D-01)**: One-line edit in `src/components/CustomerEditModal.tsx` line 79 — `email.trim() || undefined` becomes `email.trim().toLowerCase() || undefined`. Aligns the modal with `customerCsv.ts:139-140` (UI-SPEC discretion #8).
2. **Reconcile helper + wiring (D-02)**: New pure helper `reconcileCustomerEmailLowercase(customers: Customer[]): Customer[]` in `src/db/backfill.ts` (mirrors `reconcileCopiesSoldFromSales` shape at lines 391-432). Wired into `useDatabase.ts` `useCustomers()` behind the new `customerEmailLowercaseRan` module flag — runs once per page load, sets flag AFTER `await db.customers.bulkPut(patches)` per WR-01. Pure-helper unit tests live in `src/db/backfill.test.ts`.
3. **Test coverage (TEST-01)**: New `src/components/CustomerEditModal.test.tsx` mirroring the `RecordSaleModal.test.tsx` scaffold (raw createRoot + act, no @testing-library/react). Covers ROADMAP §Phase 23 SC#1: Add hydration, Edit hydration, Name-OR-Email validation, Escape close, submit-disable during save, error recovery, AND locks the D-01 lowercase contract via `expect(onSaveSpy.mock.calls[0][0].email).toBe('john@example.com')` after typing `John@Example.com`.

Purpose: Closes TEST-01. The 3-piece bundling is mandatory — the new test locks the new contract; shipping them separately leaves a window where either (a) the modal lowercases but no test guards against regression, or (b) the test passes against the old non-lowercased behavior because the test was written first.

Output: Modal canonicalizes email; legacy IndexedDB rows reconcile on first reload; test suite has 7+ new `it` blocks for CustomerEditModal; full `npm test` + `npm run build` green.

Per D-03: Sales are NOT touched. `Sale.customer.email` historical snapshots stay byte-identical (CL-05 by-value lock).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-test-coverage-hardening/23-CONTEXT.md
@.planning/phases/23-test-coverage-hardening/23-PATTERNS.md
@.planning/phases/22.1-break-even-formula-reconciliation/22.1-CONTEXT.md
@.planning/phases/15.1-customer-library/15.1-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor uses these directly — no codebase exploration needed beyond <read_first>. -->

From src/types.ts (Customer interface — D-02 helper signature target):
```typescript
export interface Customer {
  id: string;
  name?: string;
  email?: string;
  company?: string;
  address?: string;
  notes?: string;
  createdAt: Date;
  lastUsedAt?: Date;
}
```

From src/components/CustomerEditModal.tsx (prop signature — TEST-01 render target):
```typescript
interface CustomerEditModalProps {
  isOpen: boolean;
  initialCustomer?: Customer;
  onSave: (customer: Customer) => Promise<void>;
  onClose: () => void;
}
```

D-02 helper signature (NEW — to add in backfill.ts):
```typescript
export function reconcileCustomerEmailLowercase(customers: Customer[]): Customer[];
// Returns ONLY the patched rows (rows whose email needs lowercasing).
// Empty array when nothing needs patching. Pure — no Dexie, no IO.
```

From src/components/RecordSaleModal.test.tsx lines 20-30 (spy declaration pattern to mirror):
```typescript
const addCustomerSpy = vi.fn<(c: Customer) => Promise<void>>().mockResolvedValue();
const onSaveSpy = vi.fn<(c: Customer) => Promise<void>>().mockResolvedValue();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reconcileCustomerEmailLowercase pure helper + Vitest cases</name>
  <files>src/db/backfill.ts, src/db/backfill.test.ts</files>
  <read_first>
    - src/db/backfill.ts (lines 1-50 for module-level comment about no-Dexie-imports; lines 391-432 for `reconcileCopiesSoldFromSales` shape — this is the analog per 23-PATTERNS.md; lines 309-389 for `backfillCustomersFromSales` to confirm Customer-shaped reconcile precedent)
    - src/db/backfill.test.ts (lines 1-110 for fixture-factory patterns + existing Customer-related test structure)
    - .planning/phases/23-test-coverage-hardening/23-CONTEXT.md §D-02 (helper signature contract + idempotency rules)
    - .planning/phases/23-test-coverage-hardening/23-PATTERNS.md §reconcile-helper-shape (rules: no Dexie imports, pure, returns ONLY patched rows, idempotent at row level, spread-copy never mutate)
  </read_first>
  <behavior>
    - Test 1: `reconcileCustomerEmailLowercase([{ id: 'c1', email: 'John@Example.com', name: 'John', createdAt: new Date() }])` returns array of length 1 with `out[0].email === 'john@example.com'`
    - Test 2: Idempotent — `reconcileCustomerEmailLowercase([{ id: 'c1', email: 'john@example.com', name: 'John', createdAt: new Date() }])` returns `[]`
    - Test 3: Skip undefined — `reconcileCustomerEmailLowercase([{ id: 'c1', email: undefined, name: 'John', createdAt: new Date() }])` returns `[]`
    - Test 4: Mixed array — input of [mixed-case, lowercase, undefined] returns array of length 1 containing only the patched (mixed-case-now-lowercased) row
    - Test 5: Does NOT mutate input — `const input = [{ ..., email: 'JOHN@EXAMPLE.COM' }]; reconcileCustomerEmailLowercase(input); expect(input[0].email).toBe('JOHN@EXAMPLE.COM')` (input still uppercase — output is a new spread-copy)
  </behavior>
  <action>
    Add `export function reconcileCustomerEmailLowercase(customers: Customer[]): Customer[]` to `src/db/backfill.ts`. Place it AFTER `reconcileCopiesSoldFromSales` (around line 433, after the existing closing brace) and BEFORE `reconcileFixedCostsAtSave`. Implementation: iterate over `customers`, skip rows where `!c.email`, skip rows where `c.email.toLowerCase() === c.email` (idempotent guard), push `{ ...c, email: c.email.toLowerCase() }` into the output. Return the output array. Match the docstring style of `reconcileCopiesSoldFromSales` (purpose, pure-helper rationale, idempotency note, examples). NO `import 'dexie'` lines. NO React imports.

    Then add a `describe('reconcileCustomerEmailLowercase (Phase 23 D-02)', () => { ... })` block to `src/db/backfill.test.ts` covering the 5 behavior tests above. Import the helper via `import { ..., reconcileCustomerEmailLowercase } from './backfill';` — add it to the existing named-import block at line 2. Reuse a `makeCustomer({ overrides })` factory pattern consistent with the existing `makeMinimalJob` / `makeMinimalSale` factories already in the file. Place the new describe block AFTER the existing `reconcileCopiesSoldFromSales` describe block (search for `reconcileCopiesSoldFromSales` in the file to find the location) and BEFORE `reconcileFixedCostsAtSave` describe. Implements decision D-02.
  </action>
  <verify>
    <automated>npm test -- src/db/backfill.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function reconcileCustomerEmailLowercase' src/db/backfill.ts` returns 1
    - `grep -c "import .*reconcileCustomerEmailLowercase" src/db/backfill.test.ts` returns 1
    - `grep "from 'dexie'" src/db/backfill.ts` returns 0 lines (pure-helper module stays jsdom-safe)
    - `npm test -- src/db/backfill.test.ts` exits 0
    - `grep -c "reconcileCustomerEmailLowercase" src/db/backfill.test.ts` returns at least 6 (1 import + 5 test references)
    - `npx tsc -b` exits 0 (no new type errors)
  </acceptance_criteria>
  <done>Pure helper exists, 5 Vitest cases pass, backfill module still has zero Dexie imports.</done>
</task>

<task type="auto">
  <name>Task 2: Wire customerEmailLowercaseRan reconcile into useCustomers + flip modal to lowercase</name>
  <files>src/hooks/useDatabase.ts, src/components/CustomerEditModal.tsx</files>
  <read_first>
    - src/hooks/useDatabase.ts (lines 1-30 for existing module-flag declarations; lines 460-498 for `copiesSoldReconcileRan` useEffect — THIS IS THE WR-01 ANALOG to mirror; lines 518-567 for `fixedCostsReconcileRan` useEffect — secondary analog; lines 760-870 for `useCustomers()` body where the new useEffect lives)
    - src/components/CustomerEditModal.tsx (lines 60-92 — the entire handleSubmit; line 79 is the one-line change)
    - src/db/backfill.ts (re-confirm the new `reconcileCustomerEmailLowercase` export from Task 1)
    - .planning/phases/23-test-coverage-hardening/23-CONTEXT.md §D-01 + §D-02 (one-line modal change + WR-01 set-flag-AFTER-await rule)
    - .planning/phases/23-test-coverage-hardening/23-PATTERNS.md §reconcile-useeffect-wiring (cancellation token, dep array `[customers === undefined]`, set flag only after final await)
  </read_first>
  <action>
    Two sub-edits in one atomic task.

    **Sub-edit A — useDatabase.ts wiring (D-02):**
    1. Update the named-import on line 6 from `import { backfillCustomersFromSales, reconcileCopiesSoldFromSales, normalizeTagsOnJob, reconcileFixedCostsAtSave } from '../db/backfill';` to add `reconcileCustomerEmailLowercase` to the import list.
    2. Add a new module-level flag declaration `let customerEmailLowercaseRan = false;` immediately after `let fixedCostsReconcileRan = false;` (line 27). Include a comment matching the style of the existing flag-comment block (lines 21-26): note that this is the Phase 23 D-02 flag, mirrors the existing 4 reconciles, idempotent at row level so subsequent loads pay zero cost.
    3. Inside `useCustomers()` (starts around line 758), add a new `useEffect` AFTER the existing `saleCustomerBackfillRan` useEffect (currently lines 773-797) and BEFORE the `customersFrozen` useMemo (line 814). The new useEffect MUST mirror the `copiesSoldReconcileRan` pattern at lines 466-498 (NOT the `saleCustomerBackfillRan` pattern at lines 773-797, which sets the flag BEFORE the async — that's pre-WR-01). Specifically: guard `if (customerEmailLowercaseRan) return; if (customers === undefined) return;` → declare `let cancelled = false;` → run an async IIFE inside try/catch → inside the try, call `const patches = reconcileCustomerEmailLowercase(customers);` → guard `if (cancelled) return;` → `if (patches.length > 0) await db.customers.bulkPut(patches);` → set `customerEmailLowercaseRan = true;` ONLY AFTER the `await` (WR-01 hardening). On catch: `console.error('customerEmailLowercase reconcile failed:', err);` and do NOT set the flag. Cleanup: `return () => { cancelled = true; };` Dep array: `[customers === undefined]` with the `// eslint-disable-next-line react-hooks/exhaustive-deps` comment line above it (mirrors the other reconciles).

    **Sub-edit B — CustomerEditModal.tsx one-line change (D-01):**
    Change line 79 from `email: email.trim() || undefined,` to `email: email.trim().toLowerCase() || undefined,`. Do NOT touch any other line in the modal. Per D-03, do NOT touch Sale.customer anywhere — this is scoped strictly to `db.customers` writes via the modal.

    These two edits are atomic by design: shipping the modal change without the reconcile leaves legacy mixed-case rows in user IndexedDB; shipping the reconcile without the modal change is meaningless because the modal would just re-introduce mixed-case rows.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'email: email.trim().toLowerCase()' src/components/CustomerEditModal.tsx` returns 1
    - `grep -c 'email: email.trim() || undefined' src/components/CustomerEditModal.tsx` returns 0 (old line removed)
    - `grep -c 'customerEmailLowercaseRan' src/hooks/useDatabase.ts` returns at least 4 (declaration + guard + assignment after await + reset comment, OR 3 if reset is not commented)
    - `grep 'reconcileCustomerEmailLowercase' src/hooks/useDatabase.ts` returns at least 2 hits (import line + call inside useEffect)
    - `grep -A2 'customerEmailLowercaseRan = true' src/hooks/useDatabase.ts` shows the assignment AFTER an `await db.customers.bulkPut` line (WR-01 lock — flag set AFTER the await, not before)
    - `grep -c 'db.customers.bulkPut' src/hooks/useDatabase.ts` returns at least 1 (the new reconcile call)
    - `npx tsc -b` exits 0
    - `npm test` exits 0 (all existing tests still pass; the new modal-test file will be added in Task 3 — at this point existing JobsManager/RecordSale/etc tests must remain green)
    - `grep -c 'Sale.customer' src/hooks/useDatabase.ts` returns the same count as before this task (Sales NOT touched per D-03)
  </acceptance_criteria>
  <done>Modal lowercases email on save; reconcile useEffect wired with WR-01 hardening; tsc and tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author CustomerEditModal.test.tsx — locks D-01 + 6 baseline behaviors</name>
  <files>src/components/CustomerEditModal.test.tsx</files>
  <read_first>
    - src/components/CustomerEditModal.tsx (full file, lines 1-200ish — the executor must see the actual JSX/labels/placeholders to write selectors)
    - src/components/RecordSaleModal.test.tsx (lines 1-100 for imports + spy + vi.mock + dynamic-import scaffold; lines 155-240 for beforeEach/afterEach DOM harness + renderModal + typeIntoInput + findButton helpers)
    - .planning/phases/23-test-coverage-hardening/23-PATTERNS.md §CustomerEditModal.test.tsx pattern assignments (full)
    - .planning/phases/23-test-coverage-hardening/23-CONTEXT.md §D-07 (real Modal primitive, no stub) + §D-08 (raw createRoot + act, no @testing-library/react)
    - .planning/phases/22-jobsmanager-decomposition-perf/22-CONTEXT.md §D-21 (carries forward the test-convention lock)
    - .planning/phases/19-modal-primitive-a11y-migration/19-CONTEXT.md §D-21 (original Modal primitive contract — modal renders into portal, mount/unmount lifecycle)
  </read_first>
  <behavior>
    The test file MUST cover the seven scenarios below (one `it()` block each, all inside one top-level `describe('CustomerEditModal', () => { ... })` — sub-describes allowed for grouping):

    1. **D-01 lowercase lock (THE PRIMARY NEW ASSERTION)**: Render with `initialCustomer=undefined` (Add mode) → type `John@Example.com` into the email Input → type `John` into the name Input (to satisfy validation) → click "Save customer" → assert `onSaveSpy.mock.calls.length === 1` AND `onSaveSpy.mock.calls[0][0].email === 'john@example.com'`. This is the lock for the post-D-01 contract.
    2. **Add hydration**: Render with `initialCustomer=undefined` and `isOpen=true` → assert name/email/company/address/notes inputs are all empty strings (`.value === ''`).
    3. **Edit hydration**: Render with `initialCustomer={ id: 'c1', name: 'Alice', email: 'alice@x.com', company: 'Acme', createdAt: new Date() }` → assert the name input's `.value === 'Alice'`, email's `.value === 'alice@x.com'`, company's `.value === 'Acme'`.
    4. **Name-OR-Email validation**: Render Add mode → leave both name and email empty → click Save → assert `onSaveSpy.mock.calls.length === 0` AND an error message containing the substring `at least one of Name or Email` appears in the DOM (look for the existing copy from CustomerEditModal.tsx lines 65 or 99).
    5. **Submit-disable during save**: Render Add mode → type name → install a slow-resolving `onSaveSpy` (returns a Promise that resolves after a `await act(async () => { resolve(); })` step) → click Save → assert the Save button's `disabled` attribute is truthy mid-flight.
    6. **Escape close**: Render Add mode → dispatch a `keydown` Escape event on `document.body` (or on the modal container) → assert `onCloseSpy.mock.calls.length === 1`. This exercises the real `<Modal>` primitive's Escape handling (D-07 — do NOT stub Modal).
    7. **Error recovery**: Render Add mode → type name → install `onSaveSpy` that throws/rejects → click Save → assert (a) the error message text `Could not save customer` appears in the DOM (from CustomerEditModal.tsx line 87), AND (b) `isSaving` is back to false (Save button no longer disabled).
  </behavior>
  <action>
    Create `src/components/CustomerEditModal.test.tsx` from scratch, modeling the scaffold byte-for-byte from `src/components/RecordSaleModal.test.tsx` lines 1-240 with these specific adaptations:

    - **Imports**: `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';` + `import { createRoot, type Root } from 'react-dom/client';` + `import { act } from 'react';` + `import type { Customer } from '../types';`. NO `import` from `@testing-library/react` — locked by D-08.
    - **Spy declarations at module scope (BEFORE any vi.mock)**: `const onSaveSpy = vi.fn<(c: Customer) => Promise<void>>().mockResolvedValue();` and `const onCloseSpy = vi.fn<() => void>();`. Per 23-PATTERNS.md §CustomerEditModal.test.tsx the modal does NOT need a `vi.mock('../hooks/useDatabase', ...)` block because the modal receives `onSave` via prop — confirm by reading CustomerEditModal.tsx that no `useDatabase` import exists at the modal level. If absent, omit the vi.mock entirely (cleaner test).
    - **Dynamic import AFTER mocks**: `const { CustomerEditModal } = await import('./CustomerEditModal');` at top level (after spies/mocks, before describes). Top-level `await` is supported because vitest treats test files as ESM modules.
    - **DOM harness**: copy the `let container / let root` + `beforeEach (mockClear + createRoot)` + `afterEach (unmount + remove)` block from RecordSaleModal.test.tsx lines 162-196 verbatim, adapting spy clears to `onSaveSpy.mockClear(); onCloseSpy.mockClear();`. RESET `onSaveSpy.mockResolvedValue()` in beforeEach so reject-then-resolve cases don't bleed across tests.
    - **Helpers**: copy `typeIntoInput` (lines 223-230) and `findButton(text)` (lines 232-236) verbatim. These work for the modal's Save / Cancel buttons.
    - **renderModal helper**: build a small `renderModal({ initialCustomer? })` helper that produces `props = { isOpen: true, initialCustomer, onSave: onSaveSpy, onClose: onCloseSpy }` and runs `await act(async () => { root!.render(<CustomerEditModal {...props} />); });`. Real `<Modal>` mounts on first render — D-07 explicitly forbids stubbing Modal.
    - **Selectors**: For the name/email/etc inputs, use `document.querySelector` keyed on the labels' visible text + the `for`/`id` pairing — or look up by placeholder if the modal uses placeholders (read CustomerEditModal.tsx lines 95-200 to confirm exact labels/placeholders). Per project memory (descriptive-placeholder-vs-info-icon), the modal likely uses `<label htmlFor>` with `useId()` so query by label `textContent` then walk to the associated `<input id={...}>`.
    - **For the Escape test (#6)**: dispatch via `document.body.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape', bubbles: true }));` inside `act(...)`. Confirm Modal's Escape handler attaches to document — read 23-PATTERNS.md §D-07 and confirm by reading `src/components/ui/Modal.tsx` if needed.

    Test count target: 7 `it()` blocks total. Total file length target: 200–350 lines (RecordSaleModal.test.tsx is ~600 lines for 3 modes; this file covers fewer behaviors so shorter is fine).

    Implements TEST-01 + locks D-01.
  </action>
  <verify>
    <automated>npm test -- src/components/CustomerEditModal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/CustomerEditModal.test.tsx` exists
    - `grep -c "import { describe" src/components/CustomerEditModal.test.tsx` returns 1
    - `grep -c "@testing-library/react" src/components/CustomerEditModal.test.tsx` returns 0 (D-08 lock)
    - `grep -c "createRoot" src/components/CustomerEditModal.test.tsx` returns at least 1
    - `grep -c "from 'react'" src/components/CustomerEditModal.test.tsx` returns at least 1 (for `act`)
    - `grep -c "vi.fn<(c: Customer)" src/components/CustomerEditModal.test.tsx` returns at least 1 (typed onSaveSpy)
    - `grep -c "await import('./CustomerEditModal')" src/components/CustomerEditModal.test.tsx` returns 1
    - `grep -c "john@example.com" src/components/CustomerEditModal.test.tsx` returns at least 1 (D-01 lowercase assertion)
    - `grep -c "John@Example.com" src/components/CustomerEditModal.test.tsx` returns at least 1 (input value for D-01 test)
    - `grep -cE "^  it\\(" src/components/CustomerEditModal.test.tsx` returns at least 7 (covers the 7 mandated behaviors)
    - `npm test -- src/components/CustomerEditModal.test.tsx` exits 0
    - `npm test` (full suite) exits 0
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>CustomerEditModal.test.tsx exists; 7 it() blocks pass; D-01 lowercase contract is locked by test; full vitest suite green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → IndexedDB (CustomerEditModal) | User-controlled string input crosses into persistent storage |
| IndexedDB reconcile → IndexedDB (useCustomers useEffect) | Module-flag-gated migration writes back to the same store it reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | reconcileCustomerEmailLowercase | accept | Pure helper, no IO; idempotent at row level; spread-copy never mutates input. Unit-tested in Task 1. |
| T-23-02 | Information Disclosure | Sale.customer.email historical snapshots | mitigate | D-03 lock: reconcile scoped strictly to `db.customers`; Sale.customer is by-value per CL-05 and is NOT touched. Acceptance criterion in Task 2 greps for `Sale.customer` count parity. |
| T-23-03 | Denial of Service | useCustomers reconcile useEffect | mitigate | WR-01 pattern: flag set ONLY after await completes; on catch, flag stays false so next mount retries; helper is idempotent so retries are safe; cancellation token prevents writes after unmount. |
| T-23-04 | Repudiation | Email canonicalization on save | accept | Toggle is locked by test (Task 3 test #1); audit trail preserved on Sale snapshots (D-03). |

Existing mitigations preserved: T-15-03 DUP-02 lock (untouched by this plan), CL-05 Sale.customer by-value lock (explicitly honored by D-03).
</threat_model>

<verification>
Run after Task 3:
- `npm run build` → exit 0
- `npm test` → all tests pass, no skipped; the 5 new backfill cases + 7 new CustomerEditModal cases all count
- `npm test -- --coverage` → coverage report contains non-zero coverage line for `src/components/CustomerEditModal.test.tsx`
- `npx tsc -b` → exit 0 (no new type errors)
- `grep -c 'Sale.customer' src/hooks/useDatabase.ts` returns the SAME count as on the parent commit (no Sales touched)
- Manual sanity check: open IndexedDB devtools after `npm run dev`, confirm db.customers entries with mixed-case email get lowercased on next reload (UAT optional, not gated)
</verification>

<success_criteria>
- CustomerEditModal.tsx canonicalizes email to lowercase on save (D-01)
- reconcileCustomerEmailLowercase pure helper exists in backfill.ts with 5 passing Vitest cases (D-02)
- useCustomers() wires the reconcile behind customerEmailLowercaseRan with WR-01 hardening (D-02)
- Sale.customer.email is byte-identical pre/post this plan (D-03 / CL-05)
- src/components/CustomerEditModal.test.tsx exists with 7+ it() blocks; one locks the D-01 lowercase contract (TEST-01)
- All gates green: `npm test`, `npx tsc -b`, `npm run build`
</success_criteria>

<output>
Create `.planning/phases/23-test-coverage-hardening/23-01-SUMMARY.md` when done.
</output>
