---
phase: 23-test-coverage-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/JobsManager.test.tsx
  - src/utils/duplicateJob.test.ts
autonomous: true
requirements: [TEST-05, TEST-06]
tags: [test, hygiene, typing, duplicate-job, dup-02]

must_haves:
  truths:
    - "JobsManager.test.tsx line 65 dbJobsPutSpy is retyped from any to PrintJob (per TEST-05)"
    - "JobsManager.test.tsx PrintJob is imported (already imported on line 4 — confirm and add only if absent)"
    - "The `// eslint-disable-next-line @typescript-eslint/no-explicit-any` comment on line 64 is REMOVED — no longer needed since the type is no longer any (per TEST-05)"
    - "duplicateJob.test.ts DUP-02 D-15 single 6-assertion it() block is split into 6 named it() blocks (per TEST-06 + D-09)"
    - "Every expect() expression that was originally in the DUP-02 D-15 block remains byte-identical (per D-09 assertion lock)"
    - "The lock comment on lines 29-33 of duplicateJob.test.ts is REWRITTEN to the D-10 verbatim text"
    - "git diff src/utils/duplicateJob.test.ts shows ZERO changes to the 6 expect() lines themselves — only added it() wrappers + repeated fixture+duplicateJob() lines (per TEST-06 lock proof)"
  artifacts:
    - path: "src/components/JobsManager.test.tsx"
      provides: "Retyped dbJobsPutSpy (no more `any`); typo-detection restored on spy assertions"
      contains: "vi.fn<(job: PrintJob) => Promise<void>>()"
    - path: "src/utils/duplicateJob.test.ts"
      provides: "DUP-02 D-15 contract split into 6 named it() blocks; rewritten lock comment per D-10"
      contains: "6 named it() blocks within the DUP-02 D-15 locked contract describe block"
  key_links:
    - from: "src/components/JobsManager.test.tsx"
      to: "PrintJob type"
      via: "named import on line 4 (already present)"
      pattern: "import.*PrintJob.*from '../types'"
    - from: "src/utils/duplicateJob.test.ts"
      to: "DUP-02 6-assertion contract"
      via: "6 named it() blocks each containing one of the original 6 expect() lines"
      pattern: "it\\("
---

<objective>
Two atomic hygiene fixes folded into one plan because they live in test files already being touched and share zero file dependencies with Plans 23-01, 23-02, 23-03:

1. **TEST-05 — JobsManager.test.tsx `dbJobsPutSpy` retype**: The current spy declaration at line 65 reads `const dbJobsPutSpy = vi.fn<(job: any) => Promise<void>>().mockResolvedValue(undefined);` with an `eslint-disable-next-line @typescript-eslint/no-explicit-any` comment on line 64. The `any` typing silently disables typo-detection on `dbJobsPutSpy.mock.calls[0][0]` accesses elsewhere in the test file. Retype to `vi.fn<(job: PrintJob) => Promise<void>>()` (matches `RecordSaleModal.test.tsx:30`), and DELETE the now-obsolete eslint-disable comment on line 64.

2. **TEST-06 — duplicateJob.test.ts DUP-02 D-15 split**: The current describe block at lines 35-56 contains ONE `it()` block with SIX `expect()` assertions packed into a single test (PII reset, taxRate reset, copiesSold reset, id rotation, createdAt advance, tags preserve). Per D-09, the assertion expressions are locked but the test-block shape is NOT — split into 6 separately-named `it()` blocks inside the same `describe('duplicateJob (DUP-02 D-15 locked contract)', ...)` parent. Each new `it()` block contains the `const dup = duplicateJob(jobWithCustomerAndTaxRate);` setup + EXACTLY ONE of the six original `expect()` lines (BYTE-IDENTICAL — no character changes to the assertion expressions, including spacing inside the parens, capitalization, and the trailing `// TAGS-F3 lock` comment on the tags assertion). Per D-10, also rewrite the lock comment at lines 29-33 to the EXACT verbatim text specified in 23-CONTEXT.md §D-10.

Purpose: Closes TEST-05 + TEST-06. Both are LOW-severity hygiene tasks but they unblock CI signal quality: TEST-05 restores type-checking on a spy that any future test could pass garbage to without notice; TEST-06 means a future CI failure on (say) the tags-preserve assertion names the specific property in the test output instead of hiding it inside a packed 6-assert block.

Per D-09 / D-10: the split is intentionally a SHAPE refactor — assertion expressions are locked. The threat-model mitigation T-15-03 (referenced in the original lock comment) is preserved by the rewritten lock comment's explicit re-reference.

Output: Two test files modified; full `npm test` + `tsc -b` green; the TEST-06 lock proof greps confirm zero changes to `expect()` lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-test-coverage-hardening/23-CONTEXT.md
@.planning/phases/23-test-coverage-hardening/23-PATTERNS.md
@.planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md

<interfaces>
<!-- TEST-05 source pattern — RecordSaleModal.test.tsx:30 (already lives in the codebase) -->
```typescript
const dbJobsPutSpy = vi.fn<(j: PrintJob) => Promise<void>>().mockResolvedValue();
```

<!-- TEST-06 D-10 lock comment verbatim text — MUST be copied byte-for-byte from 23-CONTEXT.md D-10 -->
```
// ---------------------------------------------------------------------------
// D-15 LOCKED test contract — CONTEXT.md lines 149-159 reproduced verbatim.
// Do NOT modify the assertion expressions below — they are the unit-test
// acceptance criteria for DUP-02 and are referenced by threat-model
// mitigation T-15-03. Shape refactoring (e.g., one it() per assertion,
// rename describe()) is permitted as long as every expect() line stays
// byte-identical and every threat-model mitigation remains locked.
// ---------------------------------------------------------------------------
```

<!-- The 6 LOCKED expect() lines from duplicateJob.test.ts lines 49-54 (BYTE-IDENTICAL across the split) -->
```typescript
expect(dup.customer).toBeUndefined();
expect(dup.taxRate).toBeUndefined();
expect(dup.copiesSold).toBe(0);
expect(dup.id).not.toBe(jobWithCustomerAndTaxRate.id);
expect(dup.createdAt.getTime()).toBeGreaterThan(jobWithCustomerAndTaxRate.createdAt.getTime());
expect(dup.tags).toEqual(jobWithCustomerAndTaxRate.tags);  // TAGS-F3 lock
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retype dbJobsPutSpy in JobsManager.test.tsx — TEST-05</name>
  <files>src/components/JobsManager.test.tsx</files>
  <read_first>
    - src/components/JobsManager.test.tsx (lines 1-70 — confirms PrintJob is imported on line 4 already; lines 60-67 are the eslint-disable comment + the spy declaration to edit)
    - src/components/RecordSaleModal.test.tsx (line 30 — the canonical typed pattern: `const dbJobsPutSpy = vi.fn<(j: PrintJob) => Promise<void>>().mockResolvedValue();`)
    - .planning/phases/23-test-coverage-hardening/23-CONTEXT.md §Test-file location §TEST-05 + §D-08
    - .planning/phases/23-test-coverage-hardening/23-PATTERNS.md §JobsManager.test.tsx pattern (lines 400-416 of PATTERNS.md)
  </read_first>
  <action>
    Make two surgical edits to `src/components/JobsManager.test.tsx`:

    1. **Line 64**: DELETE the `// eslint-disable-next-line @typescript-eslint/no-explicit-any` comment line entirely. Since the next line will no longer use `any`, the disable becomes dead code.
    2. **Line 65**: Replace `const dbJobsPutSpy = vi.fn<(job: any) => Promise<void>>().mockResolvedValue(undefined);` with `const dbJobsPutSpy = vi.fn<(job: PrintJob) => Promise<void>>().mockResolvedValue();`. Note two atomic changes inside this line: (a) `any` → `PrintJob`, (b) `.mockResolvedValue(undefined)` → `.mockResolvedValue()` to match RecordSaleModal.test.tsx:30 (mockResolvedValue() with no arg works because the Promise<void> type means the resolved value is `undefined` by default). The parameter name `job` stays — only the type changes.

    Confirm `PrintJob` is already imported on line 4 (it is, per the read_first — `import type { Quote, PrintJob, Sale, PrinterInstance, PrinterConfig, Material } from '../types';`). If somehow the file no longer imports PrintJob (defensive check), add it to the existing line-4 import.

    No other lines in JobsManager.test.tsx change. The spy is referenced elsewhere via `dbJobsPutSpy.mock.calls[...]`; the new tighter type now provides typo-detection on those access paths.

    Implements TEST-05.
  </action>
  <verify>
    <automated>npx tsc -b &amp;&amp; npm test -- src/components/JobsManager.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vi.fn<(job: PrintJob) => Promise<void>>" src/components/JobsManager.test.tsx` returns 1
    - `grep -c "vi.fn<(job: any) => Promise<void>>" src/components/JobsManager.test.tsx` returns 0 (old `any` typing removed)
    - `grep -c "eslint-disable-next-line @typescript-eslint/no-explicit-any" src/components/JobsManager.test.tsx` returns 0 (the eslint-disable line is removed — no other any-typed declaration in this file should need it after this change; if the grep returns more than 0, investigate whether another any-typing exists)
    - `grep -c "PrintJob" src/components/JobsManager.test.tsx` returns at least 2 (one in line-4 import, at least one in the new spy declaration)
    - `npx tsc -b` exits 0 (proves the new PrintJob-typed spy compiles + downstream `.mock.calls[0][0]` accesses are still type-valid)
    - `npm test -- src/components/JobsManager.test.tsx` exits 0
  </acceptance_criteria>
  <done>TEST-05 closed. dbJobsPutSpy is PrintJob-typed; no any-typing in the spy; obsolete eslint-disable removed; tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Split DUP-02 D-15 packed contract into 6 named it() blocks + rewrite lock comment — TEST-06</name>
  <files>src/utils/duplicateJob.test.ts</files>
  <read_first>
    - src/utils/duplicateJob.test.ts (lines 1-56 — entire DUP-02 D-15 block: lock comment at 29-33, describe at 35, fixture at 36-45, single it() at 47-55)
    - .planning/phases/23-test-coverage-hardening/23-CONTEXT.md §D-09 (split is allowed — assertion expressions are locked, test-block shape is not) + §D-10 (verbatim rewrite text — copy byte-for-byte)
    - .planning/phases/23-test-coverage-hardening/23-PATTERNS.md §duplicateJob.test.ts pattern (lines 419-450 of PATTERNS.md — the target shape after split + the verbatim lock comment)
    - .planning/phases/15-tags-search-quick-duplicate/15-CONTEXT.md (background context for the original DUP-02 lock — confirms T-15-03 threat-model mitigation is the security anchor the rewritten lock comment must continue to reference)
  </read_first>
  <action>
    Surgical refactor on `src/utils/duplicateJob.test.ts` lines 29-56. Steps:

    **Step 1 — Rewrite the lock comment (lines 29-33).**
    Replace the existing 5-line comment block:
    ```
    // ---------------------------------------------------------------------------
    // D-15 LOCKED test contract — CONTEXT.md lines 149-159 reproduced verbatim.
    // Do NOT modify these assertions. They are the unit-test acceptance criteria
    // for DUP-02 and are referenced by the threat-model mitigation T-15-03.
    // ---------------------------------------------------------------------------
    ```
    with the D-10 verbatim text from 23-CONTEXT.md (7 lines, longer than the original):
    ```
    // ---------------------------------------------------------------------------
    // D-15 LOCKED test contract — CONTEXT.md lines 149-159 reproduced verbatim.
    // Do NOT modify the assertion expressions below — they are the unit-test
    // acceptance criteria for DUP-02 and are referenced by threat-model
    // mitigation T-15-03. Shape refactoring (e.g., one it() per assertion,
    // rename describe()) is permitted as long as every expect() line stays
    // byte-identical and every threat-model mitigation remains locked.
    // ---------------------------------------------------------------------------
    ```
    This text is specified verbatim in 23-CONTEXT.md §D-10 — copy byte-for-byte. The hyphen rules at top/bottom should match the existing 75-character hyphen rules in the surrounding code (count via `grep '^// -' src/utils/duplicateJob.test.ts | head -1` to confirm if uncertain).

    **Step 2 — Split the single it() block at lines 47-55 into 6 named it() blocks inside the same describe.**
    The current packed block is:
    ```typescript
    it('resets PII, tax, copiesSold, id; preserves tags (TAGS-F3)', () => {
      const dup = duplicateJob(jobWithCustomerAndTaxRate);
      expect(dup.customer).toBeUndefined();
      expect(dup.taxRate).toBeUndefined();
      expect(dup.copiesSold).toBe(0);
      expect(dup.id).not.toBe(jobWithCustomerAndTaxRate.id);
      expect(dup.createdAt.getTime()).toBeGreaterThan(jobWithCustomerAndTaxRate.createdAt.getTime());
      expect(dup.tags).toEqual(jobWithCustomerAndTaxRate.tags);  // TAGS-F3 lock
    });
    ```

    Replace with 6 named it() blocks (still inside the same describe('duplicateJob (DUP-02 D-15 locked contract)', () => { ... }) parent and after the same `jobWithCustomerAndTaxRate` fixture declaration at lines 36-45). The fixture stays at the describe-scope (don't re-declare inside each it()). Each new it() block:
    1. Has a name describing the specific lock (suggested names below — executor may polish copy as long as the assertion expression stays byte-identical):
       - `it('resets PII (customer) on the duplicate', ...)`
       - `it('resets taxRate on the duplicate', ...)`
       - `it('resets copiesSold to 0 on the duplicate', ...)`
       - `it('rotates id — duplicate id differs from source id', ...)`
       - `it('advances createdAt — duplicate is newer than source', ...)`
       - `it('preserves tags (TAGS-F3 lock)', ...)`
    2. Body: `const dup = duplicateJob(jobWithCustomerAndTaxRate);` followed by EXACTLY ONE of the 6 original expect() lines.
    3. The `expect()` line is BYTE-IDENTICAL to its original — including the trailing `// TAGS-F3 lock` comment on the tags assertion (do NOT remove or relocate this trailing comment).

    Per D-09, the 6 expect() expressions are LOCKED. Per D-10, the it() name strings are NOT locked — they're new shape and CAN be polished. The `duplicateJob()` call IS allowed to be repeated 6 times (one per it()) since the original packed block called it once; this is permitted shape refactoring per D-10.

    Implements TEST-06.
  </action>
  <verify>
    <automated>npm test -- src/utils/duplicateJob.test.ts &amp;&amp; npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - The 6 expect() expressions are byte-identical to the originals. Lock proof:
      - `grep -c 'expect(dup.customer).toBeUndefined();' src/utils/duplicateJob.test.ts` returns at least 1
      - `grep -c 'expect(dup.taxRate).toBeUndefined();' src/utils/duplicateJob.test.ts` returns at least 1
      - `grep -c 'expect(dup.copiesSold).toBe(0);' src/utils/duplicateJob.test.ts` returns at least 1
      - `grep -c 'expect(dup.id).not.toBe(jobWithCustomerAndTaxRate.id);' src/utils/duplicateJob.test.ts` returns at least 1
      - `grep -c 'expect(dup.createdAt.getTime()).toBeGreaterThan(jobWithCustomerAndTaxRate.createdAt.getTime());' src/utils/duplicateJob.test.ts` returns at least 1
      - `grep -c 'expect(dup.tags).toEqual(jobWithCustomerAndTaxRate.tags);  // TAGS-F3 lock' src/utils/duplicateJob.test.ts` returns at least 1 (with the trailing comment preserved)
    - The describe block now contains exactly 6 it() blocks (not 1). Verify by extracting the DUP-02 D-15 describe body and counting `it(` occurrences inside it:
      - `sed -n '/describe.*DUP-02 D-15 locked contract/,/^});/p' src/utils/duplicateJob.test.ts | grep -cE '^  it\\(' returns 6` (6 named it blocks)
    - Lock comment is rewritten per D-10:
      - `grep -c "Shape refactoring" src/utils/duplicateJob.test.ts` returns 1 (the new D-10 text contains "Shape refactoring")
      - `grep -c "byte-identical" src/utils/duplicateJob.test.ts` returns 1 (the new D-10 text contains "byte-identical")
      - `grep -c "T-15-03" src/utils/duplicateJob.test.ts` returns at least 1 (threat-model reference preserved)
    - `npm test -- src/utils/duplicateJob.test.ts` exits 0 (all 6 new it() blocks pass + the other 2 describe blocks at lines 64+ untouched)
    - `npm test` (full suite) exits 0
    - `npx tsc -b` exits 0
  </acceptance_criteria>
  <done>TEST-06 closed. DUP-02 D-15 contract is split into 6 named it() blocks; lock comment rewritten per D-10; assertion expressions byte-identical; T-15-03 reference preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only refactors existing test files. No new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-10 | Tampering | duplicateJob.test.ts DUP-02 lock | mitigate | T-15-03 reference is preserved in the rewritten lock comment (D-10 verbatim text contains "T-15-03"). The 6 expect() expressions are byte-identical post-split (acceptance criteria grep them literally). |
| T-23-11 | Repudiation | TEST-06 lock proof | mitigate | Acceptance criteria include exact `grep -c '...expect_line...'` checks for each of the 6 assertions — automated proof that the lock was not violated by the split. |

No new attack surface — this plan only modifies test files. Existing mitigations (T-15-03 DUP-02 lock, CL-05 by-value lock) are preserved by construction.
</threat_model>

<verification>
After both tasks:
- `npm run build` → exit 0 (production code untouched — only tests modified)
- `npm test` → all tests pass, no skipped; the DUP-02 D-15 describe now runs 6 separate it() blocks
- `npx tsc -b` → exit 0 (the new tighter PrintJob typing on dbJobsPutSpy compiles)
- `git diff src/utils/duplicateJob.test.ts | grep -E '^[-+] *expect\\(' | grep -v '^-' | grep -v '^+' | wc -l` — alternative lock proof: among added lines (`^+`) the expect() lines should match removed lines (`^-`) byte-for-byte (the lines move shape but text is identical). A simpler proof: the 6 expect() greps in acceptance_criteria.
- Manual sanity check: run `npm test -- src/utils/duplicateJob.test.ts --reporter=verbose` and confirm test names reflect the new split (6 it names appear under the DUP-02 D-15 describe).
</verification>

<success_criteria>
- src/components/JobsManager.test.tsx line 65 retyped to PrintJob; obsolete eslint-disable comment removed (TEST-05)
- src/utils/duplicateJob.test.ts DUP-02 D-15 describe now contains 6 named it() blocks (TEST-06)
- Every original expect() expression is byte-identical post-split (D-09 lock)
- Lock comment rewritten per D-10 verbatim text including T-15-03 reference (D-10)
- All gates green: `npm test`, `npx tsc -b`, `npm run build`
- Production code unchanged — only test files modified
</success_criteria>

<output>
Create `.planning/phases/23-test-coverage-hardening/23-04-SUMMARY.md` when done.
</output>
