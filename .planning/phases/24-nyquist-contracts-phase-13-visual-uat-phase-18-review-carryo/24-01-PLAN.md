---
phase: 24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md
autonomous: true
requirements: [NYQ-01]

must_haves:
  truths:
    - "13-VALIDATION.md frontmatter has status: passed, nyquist_compliant: true, wave_0_complete: true (ROADMAP success criterion 1)"
    - "13-VALIDATION.md retains its existing 20-row Per-Task Verification Map with each row now marked against shipped evidence (no Wave 0 work needed — Phase 13 is closed, npm test 92/92 green)"
    - "A `## Validation Audit 2026-05-25` section is appended documenting the audit trail (D-01 trigger, no MISSING gaps, evidence pointers to costCalc.test.ts + taxResolution.test.ts)"
  artifacts:
    - path: ".planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md"
      provides: "Nyquist validation contract for Phase 13 — State A audit-update from existing draft"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "13-VALIDATION.md frontmatter"
      to: "shipped tests in costCalc.test.ts + taxResolution.test.ts"
      via: "Per-Task Map rows referencing each test by name"
      pattern: "automated_command"
---

<objective>
NYQ-01: Author the Nyquist validation contract for Phase 13 (tax-model-ui-sweep) by running `/gsd:validate-phase 13`. Phase 13 is in **State A** — a draft `13-VALIDATION.md` already exists with `status: draft, nyquist_compliant: false, wave_0_complete: false` and a 20-row Per-Task Verification Map populated against the shipped tests. Phase 13 is closed (npm test reports 6 files / 92 tests passing per `13-VERIFICATION.md`), so the workflow's gap analysis returns **zero MISSING tests** and proceeds directly to flag-flipping + audit-trail append.

Purpose: Close TECH-DEBT D1 (Phase 13 missing Nyquist contract) — the first of four Nyquist closures Phase 24 ships.

Output: `13-VALIDATION.md` flipped from draft to passed with a `## Validation Audit 2026-05-25` section appended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/workflows/validate-phase.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-CONTEXT.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-PATTERNS.md
@.planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md
@.planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run `/gsd:validate-phase 13` and verify produced VALIDATION.md flag flips</name>
  <files>.planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/workflows/validate-phase.md (the 8-step workflow this task invokes — read in full)
    - .planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md (current State A draft — frontmatter has `status: draft, nyquist_compliant: false, wave_0_complete: false`; 20-row Per-Task Map already populated)
    - .planning/phases/13-tax-model-ui-sweep/13-VERIFICATION.md (confirms tests green: "6 files, 92 tests passed | PASS" — this is the evidence that gap analysis returns 0 MISSING)
    - src/utils/costCalc.test.ts (proves cost-calculator tests exist for COVERED markings)
    - src/utils/taxResolution.test.ts (proves tax-resolution tests exist for COVERED markings)
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md §1 (State A workflow shape — explains expected behavior for Phase 13)
  </read_first>
  <action>
    Invoke the validate-phase workflow against Phase 13: run `/gsd:validate-phase 13` (or equivalent direct workflow invocation per `$HOME/.claude/get-shit-done/workflows/validate-phase.md`). The workflow detects State A from the existing `13-VALIDATION.md`, parses the Test Infrastructure + Per-Task Map sections, runs gap analysis against the shipped tests (per RESEARCH §1 the result is zero MISSING), proceeds to step 6 to update the file. The workflow MUST: (a) flip `status: draft` → `status: passed`, (b) flip `nyquist_compliant: false` → `true`, (c) flip `wave_0_complete: false` → `true`, (d) append a `## Validation Audit 2026-05-25` section documenting the audit trail (Phase 24 NYQ-01 trigger; tests-green baseline; no MISSING gaps; pointer to shipped costCalc.test.ts + taxResolution.test.ts evidence). Do NOT bypass the workflow — the gsd-nyquist-auditor subagent runs at step 5 and is the canonical authority. If the workflow surfaces unexpected gaps (the research predicts none), surface the gap list to the user via the workflow's step-4 AskUserQuestion gate; do NOT silently fill or skip. Commit the updated file using `gsd-sdk query commit "docs(phase-24): 13-VALIDATION.md — Nyquist compliance (NYQ-01)" --files ".planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md"` (no `--no-verify` per project CLAUDE.md).
  </action>
  <verify>
    <automated>grep -cE "^status: passed$" .planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md | grep -q '^1$' && grep -cE "^nyquist_compliant: true$" .planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md | grep -q '^1$' && grep -cE "^wave_0_complete: true$" .planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md | grep -q '^1$' && grep -q "## Validation Audit 2026-05-25" .planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md` frontmatter has `status: passed` (was `draft`)
    - Same file frontmatter has `nyquist_compliant: true` (was `false`)
    - Same file frontmatter has `wave_0_complete: true` (was `false`)
    - Same file body contains a new section header `## Validation Audit 2026-05-25` appended after the existing content
    - The 20-row Per-Task Verification Map is preserved (workflow updates rows in place, does not delete them)
    - `npm test` exits 0 (regression baseline — pre-existing 92 tests still green)
    - Commit recorded with message `docs(phase-24): 13-VALIDATION.md — Nyquist compliance (NYQ-01)` and pre-commit hooks ran (no `--no-verify`)
  </acceptance_criteria>
  <done>
    13-VALIDATION.md is the canonical Nyquist contract for Phase 13: status passed, nyquist_compliant true, wave_0_complete true, with a dated audit-trail section. NYQ-01 closes TECH-DEBT D1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Plan modifies a single `.planning/**` markdown file via an in-repo workflow. No untrusted input crosses any boundary. No package installs. No runtime code changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Tampering | `13-VALIDATION.md` frontmatter flag-flip | accept | Doc-only edit committed via `gsd-sdk query commit` with pre-commit hooks active; git history records the change. No runtime impact. |
| T-24-01-02 | Information Disclosure | Validation contract content | accept | Frontmatter + audit section describe internal validation state; no secrets, no PII, no third-party data exposed. |
</threat_model>

<verification>
- `npm test` exits 0 (92 tests still pass)
- `git log -1 --format=%s .planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md` returns the NYQ-01 commit message
- File frontmatter all three flags flipped correctly
</verification>

<success_criteria>
ROADMAP success criterion #1 satisfied: `13-VALIDATION.md` has `status: passed, nyquist_compliant: true, wave_0_complete: true`.
</success_criteria>

<output>
Create `.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-01-SUMMARY.md` when done — summarizing the audit outcome (zero MISSING gaps confirmed; flags flipped; commit hash).
</output>
