---
phase: 24
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15.1-customer-library/15.1-VALIDATION.md
autonomous: true
requirements: [NYQ-03]

must_haves:
  truths:
    - "15.1-VALIDATION.md exists and is nyquist_compliant: true (ROADMAP success criterion 3)"
    - "Per-Task Map has at least one row per declared requirement ID for Phase 15.1 (CL-01..05 — derived from REQUIREMENTS.md + the 5 plan SUMMARYs in the phase dir)"
    - "Wave 0 Requirements section either empty or notes that frameworks/tests already shipped — Phase 15.1 is closed (15.1-VERIFICATION.md: status passed)"
  artifacts:
    - path: ".planning/phases/15.1-customer-library/15.1-VALIDATION.md"
      provides: "Nyquist validation contract for Phase 15.1 — State B reconstruction from 5 plan SUMMARYs"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "15.1-VALIDATION.md Per-Task Map"
      to: ".planning/phases/15.1-customer-library/15.1-NN-SUMMARY.md (× 5)"
      via: "task IDs + automated_command pointers extracted from each SUMMARY"
      pattern: "Per-Task Verification Map"
---

<objective>
NYQ-03: Author the Nyquist validation contract for Phase 15.1 (customer-library) by running `/gsd:validate-phase 15.1`. Phase 15.1 is in **State B** — no `15.1-VALIDATION.md` exists. The workflow reconstructs the contract from the 5 plan SUMMARYs in the phase directory, using the canonical template. Phase 15.1 is closed, so reconstructed rows mark COVERED against shipped tests with no Wave 0 work needed.

Purpose: Close TECH-DEBT D3 (Phase 15.1 missing Nyquist contract).

Output: A new `15.1-VALIDATION.md` with `nyquist_compliant: true` and a populated Per-Task Map covering CL-01..05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/workflows/validate-phase.md
@$HOME/.claude/get-shit-done/templates/VALIDATION.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-CONTEXT.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-PATTERNS.md
@.planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run `/gsd:validate-phase 15.1` and verify produced VALIDATION.md State B reconstruction</name>
  <files>.planning/phases/15.1-customer-library/15.1-VALIDATION.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/workflows/validate-phase.md (the 8-step workflow this task invokes)
    - $HOME/.claude/get-shit-done/templates/VALIDATION.md (the canonical State B output template)
    - .planning/phases/15.1-customer-library/ (the full phase directory — all 5 SUMMARYs are the reconstruction input)
    - .planning/phases/15.1-customer-library/15.1-VERIFICATION.md (confirms status: passed — informs that reconstructed rows mark COVERED)
    - .planning/phases/13-tax-model-ui-sweep/13-VALIDATION.md (reference shape for Test Infrastructure table + Per-Task Map row format)
    - .planning/REQUIREMENTS.md (CL-01..05 requirement definitions — sets the row count for the Per-Task Map)
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md §1 (State B workflow shape)
  </read_first>
  <action>
    Invoke `/gsd:validate-phase 15.1`. The workflow detects State B (no `15.1-VALIDATION.md` present), spawns the `gsd-nyquist-auditor` subagent at step 5 to scan the 5 plan SUMMARYs in `.planning/phases/15.1-customer-library/` and reconstruct: (a) Test Infrastructure table (project Vitest 4.1.4 + 8-gate build chain), (b) Per-Task Verification Map with ≥1 row per declared Phase 15.1 requirement (CL-01 sort-order + undefined-first, CL-02 dedup-mode, CL-03..05 — read REQUIREMENTS.md "## Test coverage (TEST)" + Phase 15.1 SUMMARY content), (c) Wave 0 Requirements section reading "None — Phase 15.1 tests already shipped." Workflow writes the new file at step 6 using the template. Do NOT bypass. If gap analysis surfaces MISSING tests (research predicts none — note that TEST-01..03 in REQUIREMENTS.md flag the Customer-UI test gaps but those are Phase 23 scope, NOT Phase 15.1's existing shipped-functionality validation), surface via step-4 AskUserQuestion. Commit with `gsd-sdk query commit "docs(phase-24): 15.1-VALIDATION.md — Nyquist compliance (NYQ-03)" --files ".planning/phases/15.1-customer-library/15.1-VALIDATION.md"` (no `--no-verify`).
  </action>
  <verify>
    <automated>test -f .planning/phases/15.1-customer-library/15.1-VALIDATION.md && grep -cE "^nyquist_compliant: true$" .planning/phases/15.1-customer-library/15.1-VALIDATION.md | grep -q '^1$' && grep -q "Per-Task Verification Map\|Per-Task Map" .planning/phases/15.1-customer-library/15.1-VALIDATION.md && grep -q "CL-" .planning/phases/15.1-customer-library/15.1-VALIDATION.md && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/15.1-customer-library/15.1-VALIDATION.md` exists (file created)
    - Frontmatter has `nyquist_compliant: true`
    - Frontmatter has `status:` set to a non-draft value (workflow sets `passed` since Phase 15.1 is closed)
    - Body contains a Per-Task Verification Map with at least one row per declared Phase 15.1 requirement (CL-01..05; count ≥ 5)
    - Body contains a Wave 0 Requirements section
    - `npm test` exits 0 (regression baseline)
    - Commit recorded with NYQ-03 message; pre-commit hooks ran
  </acceptance_criteria>
  <done>
    15.1-VALIDATION.md is the canonical Nyquist contract for Phase 15.1. NYQ-03 closes TECH-DEBT D3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Plan creates a single `.planning/**` markdown file via an in-repo workflow. No untrusted input, no package installs, no runtime code changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03-01 | Tampering | `15.1-VALIDATION.md` reconstructed contract | accept | Workflow-generated file from canonical template + audited SUMMARYs; committed with hooks active. |
| T-24-03-02 | Repudiation | Reconstructed Per-Task Map accuracy | mitigate | Workflow's step-4 AskUserQuestion gate surfaces MISSING gaps before commit. |
</threat_model>

<verification>
- `npm test` exits 0
- `git log -1 --format=%s .planning/phases/15.1-customer-library/15.1-VALIDATION.md` returns the NYQ-03 commit message
</verification>

<success_criteria>
ROADMAP success criterion #3 satisfied: `15.1-VALIDATION.md` exists and is `nyquist_compliant: true`.
</success_criteria>

<output>
Create `.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-03-SUMMARY.md` when done.
</output>
