---
phase: 24
plan: 06
type: execute
wave: 3
depends_on: [24-05]
files_modified:
  - src/pdf/generateQuotePdf.ts
  - src/pdf/generateQuotePdf.test.ts
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
  - package.json
  - package-lock.json
autonomous: false
requirements: []

must_haves:
  truths:
    - "WR-01: `generateQuotePdf.test.ts` forbidden-path test collapsed to a single `generateQuotePdf()` invocation with a combined regex (ROADMAP success criterion 6); sticky-mock fragility eliminated"
    - "WR-02: `@tauri-apps/api` version skew resolved via strategy (b) — Rust `tauri` crate bumped to 2.11.x + `@tauri-apps/api` re-pinned to `^2.11.0`; only ONE copy of `@tauri-apps/api` ships in the built bundle (ROADMAP success criterion 7)"
    - "D-05 implemented: strategy (b) bump of the Rust `tauri` crate to 2.11.x + re-pin `@tauri-apps/api` to `^2.11.0`; strategies (a) plugin-downgrade and (c) dual-copy acceptance are NOT applied on the happy path (strategy (a) rejected outright per D-05a; strategy (c) reserved as conditional escape hatch only)"
    - "D-06 implemented: conditional UAT — RESEARCH §2 verbatim Tauri 2.11.x changelog evidence shows zero `fs:*`/dialog/local-origin capability changes, so the planner determined NO routine post-bump UAT-A re-run. Task 3 `checkpoint:human-verify` is `gate=conditional` and fires ONLY if Task 2 verification exits non-zero (rollback / accept-dual-copy decision)"
    - "WR-03: `generateQuotePdf.ts:337` forbidden-path substring check tightened from `includes('forbidden path')` to `startsWith('forbidden path:')`; false-positive matches on unrelated errors eliminated (ROADMAP success criterion 8)"
    - "All 8 build-chain gates remain green post-bump: `npx tsc -b` exit 0, `npm test` exit 0, `npm run build` exit 0, `npm run tauri build` exit 0"
  artifacts:
    - path: "src/pdf/generateQuotePdf.ts"
      provides: "Tightened forbidden-path error matcher anchored to the colon-space format per plugins-workspace/fs/src/error.rs#PathForbidden"
      contains: "startsWith('forbidden path:')"
    - path: "src/pdf/generateQuotePdf.test.ts"
      provides: "Collapsed forbidden-path test using a single invocation + combined regex matching both the savePath echo and the actionable message text"
      contains: "Cannot save to"
    - path: "src-tauri/Cargo.toml"
      provides: "Tauri crate bumped to 2.11"
      contains: "tauri = \"2.11\""
    - path: "package.json"
      provides: "JS-side @tauri-apps/api re-pinned to ^2.11.0 aligned with plugin-fs 2.5.1 + plugin-dialog 2.7.1 peerDeps"
      contains: "@tauri-apps/api"
  key_links:
    - from: "src/pdf/generateQuotePdf.ts:337 startsWith('forbidden path:')"
      to: "plugins-workspace/v2/plugins/fs/src/error.rs#PathForbidden #[error(\"forbidden path: {0}\")]"
      via: "anchored-prefix match against the Rust crate's literal error format"
      pattern: "forbidden path:"
    - from: "package.json @tauri-apps/api ^2.11.0"
      to: "node_modules/@tauri-apps/plugin-fs and plugin-dialog peerDeps"
      via: "npm install dedup at top-level (no nested /node_modules/@tauri-apps/api copies)"
      pattern: "dedup"

# Note: this plan addresses Phase 18 code review warnings WR-01, WR-02, WR-03 — these are
# not formal REQUIREMENTS.md NYQ-IDs but are documented in
# .planning/phases/18-tauri-fs-scope-fix/18-REVIEW.md and folded into Phase 24 via ROADMAP
# (success criteria 6, 7, 8). Traceability is preserved through commit messages and
# `must_haves.truths` above; `requirements: []` because the NYQ-XX ID space is reserved for
# the 5 Nyquist requirements covered by plans 24-01..24-05.
---

<objective>
Bundled Phase 18 code review carryover (WR-01/02/03) per CONTEXT.md D-01 and D-02. Three causally-related Phase 18 review findings ship in one plan with atomic commits per task:

- **WR-01:** Collapse the sticky-mock-fragile forbidden-path test in `generateQuotePdf.test.ts:519` to a single `generateQuotePdf()` invocation with a combined regex (matches the sibling test pattern at lines 527-535).
- **WR-02:** Resolve `@tauri-apps/api` dual-copy via D-05 strategy (b): bump Rust `tauri` crate to 2.11.x + re-pin `@tauri-apps/api` to `^2.11.0`. Plugin versions (`plugin-fs` 2.5.1 + `plugin-dialog` 2.7.1) stay; their peerDep `^2.11.0` aligns at top-level post-bump.
- **WR-03:** Tighten `generateQuotePdf.ts:337` forbidden-path substring match from `includes('forbidden path')` to `startsWith('forbidden path:')` anchored to the Rust crate's literal error format.

Per RESEARCH §2, the Tauri 2.11.x changelog has **zero** changes to `fs:*` permissions, `fs:scope` semantics, dialog plugin save behavior, or local-origin capability ACL resolution. The decisive D-06 call is: **no routine UAT-A re-run**. Task 3 is a CONDITIONAL `checkpoint:human-verify` that fires ONLY if Task 2's verification (`cargo check && npm run tauri build`) fails — per CONTEXT.md `<specifics>` the executor MUST surface the failure to the user, NOT silently switch strategies.

Purpose: Close Phase 18 review carryover so PDF error mapping + Tauri version pinning carry no fragility into v1.4+.

Output: Two atomic commits on the happy path (Task 1 + Task 2); a third commit only if Task 3's conditional checkpoint resolves with `accept-dual-copy` and a docs entry is added.

Depends on: 24-05 — wave boundary for serialization clarity only (no file conflict; the WR plan touches code/dep files, NYQ-05 touches `.planning/**` markdown). Per CONTEXT.md D-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-CONTEXT.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md
@.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-PATTERNS.md
@.planning/phases/18-tauri-fs-scope-fix/18-REVIEW.md
@.planning/phases/18-tauri-fs-scope-fix/18-01-PLAN.md
@.planning/phases/18-tauri-fs-scope-fix/18-01-SUMMARY.md
@src/pdf/generateQuotePdf.ts
@src/pdf/generateQuotePdf.test.ts
@src-tauri/Cargo.toml
@package.json

<interfaces>
<!-- Exact replacement targets the executor needs. Source: RESEARCH §5 verbatim, confirmed 2026-05-25. -->

WR-03 — `src/pdf/generateQuotePdf.ts:337` (single-line delta + 2 new comment lines):

Current state (lines 335-341):
  ```
  // Tauri fs plugin scope-denial error format: "forbidden path: {pathbuf}"
  // Source: plugins-workspace/v2/plugins/fs/src/error.rs — PathForbidden variant.
  const msg = err instanceof Error ? err.message : String(err);
  if (msg.toLowerCase().includes('forbidden path')) {
  ```

Replacement (adds 2 comment lines + flips `includes` to `startsWith` with anchored colon):
  ```
  // Tauri fs plugin scope-denial error format: "forbidden path: {pathbuf}"
  // Source: plugins-workspace/v2/plugins/fs/src/error.rs — PathForbidden variant.
  // Anchored to the trailing colon to exclude false-positive matches on errors
  // that contain the substring elsewhere (e.g., "operation not permitted on forbidden path component").
  const msg = err instanceof Error ? err.message : String(err);
  if (msg.toLowerCase().startsWith('forbidden path:')) {
  ```

Ground-truth (plugins-workspace/v2/plugins/fs/src/error.rs): `#[error("forbidden path: {0}")] PathForbidden(PathBuf)` — start-anchored with literal colon-space.

WR-01 — `src/pdf/generateQuotePdf.test.ts:510-524` (collapse double-invocation to single + combined regex):

Current state (lines 510-524):
  ```
  it('rewrites "forbidden path" error to actionable message including the savePath', async () => {
    const { save } = await import('@tauri-apps/plugin-dialog');
    const { writeFile } = await import('@tauri-apps/plugin-fs');
    const fakePath = '/Users/x/Desktop/foo.pdf';
    vi.mocked(save).mockResolvedValue(fakePath);
    vi.mocked(writeFile).mockRejectedValue(
      new Error(`forbidden path: ${fakePath}`),
    );

    await expect(generateQuotePdf(makeQuote())).rejects.toThrow(
      /this location is restricted/,
    );
    await expect(generateQuotePdf(makeQuote())).rejects.toThrow(
      /\/Users\/x\/Desktop\/foo\.pdf/,
    );
  });
  ```

Replacement (single invocation, combined regex):
  ```
  it('rewrites "forbidden path" error to actionable message including the savePath', async () => {
    const { save } = await import('@tauri-apps/plugin-dialog');
    const { writeFile } = await import('@tauri-apps/plugin-fs');
    const fakePath = '/Users/x/Desktop/foo.pdf';
    vi.mocked(save).mockResolvedValue(fakePath);
    vi.mocked(writeFile).mockRejectedValue(
      new Error(`forbidden path: ${fakePath}`),
    );

    await expect(generateQuotePdf(makeQuote())).rejects.toThrow(
      /Cannot save to "\/Users\/x\/Desktop\/foo\.pdf" — this location is restricted/,
    );
  });
  ```

Note: the em-dash (`—`, U+2014) MUST be preserved verbatim from `generateQuotePdf.ts:339`. The combined regex anchors the savePath position right after `Cannot save to "` rather than allowing it anywhere in the message.

Sibling-test parity reference — `src/pdf/generateQuotePdf.test.ts:527-535` (the analog that already uses single-invocation pattern; do NOT modify):
  ```
  it('passes through unrelated writeFile errors unchanged', async () => {
    const { save } = await import('@tauri-apps/plugin-dialog');
    const { writeFile } = await import('@tauri-apps/plugin-fs');
    const fakePath = '/Users/x/Desktop/foo.pdf';
    vi.mocked(save).mockResolvedValue(fakePath);
    vi.mocked(writeFile).mockRejectedValue(new Error('disk full'));

    await expect(generateQuotePdf(makeQuote())).rejects.toThrow(/disk full/);
  });
  ```

WR-02 — Dependency bumps:

`src-tauri/Cargo.toml:12` current line:
  `tauri = { version = "2", features = [] }`

`src-tauri/Cargo.toml:12` replacement:
  `tauri = { version = "2.11", features = [] }`

`package.json:39` current line:
  `    "@tauri-apps/api": "^2.10.1",`

`package.json:39` replacement:
  `    "@tauri-apps/api": "^2.11.0",`

`@tauri-apps/plugin-fs` 2.5.1 and `@tauri-apps/plugin-dialog` 2.7.1 stay (their `^2.11.0` peerDep matches the new top-level pin).

Expected post-bump state per RESEARCH §2:
- `find node_modules/@tauri-apps -name package.json | wc -l` returns 6 (was 8)
- `find node_modules/@tauri-apps -path '*/node_modules/*/api/package.json' | wc -l` returns 0 (was 2)
- `Cargo.lock` regen produces a multi-line diff (≥50 lines, similar to Phase 18's lockfile refresh)
- `package-lock.json` regen produces a deduped tree
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-01 + WR-03 combined TS edits (forbidden-path tightening + test collapse)</name>
  <files>src/pdf/generateQuotePdf.ts, src/pdf/generateQuotePdf.test.ts</files>
  <read_first>
    - .planning/phases/18-tauri-fs-scope-fix/18-REVIEW.md (the source of truth for WR-01 + WR-03 finding text)
    - src/pdf/generateQuotePdf.ts (current state — confirm lines 335-341 match the WR-03 replacement target before editing)
    - src/pdf/generateQuotePdf.test.ts (read lines 1-60 for mock scaffold + lines 499-545 for the entire `writeFile error mapping` describe block including the sibling-analog test at 527-535)
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md §5 (WR-01 + WR-03 surgical edit details + ground-truth confirmation against plugins-workspace error.rs)
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-PATTERNS.md (pattern map for both edit targets — confirms exact-self analog + sibling-test parity)
  </read_first>
  <action>
    Apply two surgical edits in one task, then verify and commit as a single atomic commit.

    WR-03 edit in `src/pdf/generateQuotePdf.ts` around line 337:
    1. Add 2 new comment lines after the existing "Source: plugins-workspace/v2/plugins/fs/src/error.rs — PathForbidden variant." comment and before the `const msg = ...` line, reading:
       - `    // Anchored to the trailing colon to exclude false-positive matches on errors`
       - `    // that contain the substring elsewhere (e.g., "operation not permitted on forbidden path component").`
    2. Change the `if` condition on the subsequent line from `if (msg.toLowerCase().includes('forbidden path')) {` to `if (msg.toLowerCase().startsWith('forbidden path:')) {`.

    WR-01 edit in `src/pdf/generateQuotePdf.test.ts` around line 519:
    Replace the two-`toThrow` assertion block with a single `toThrow` combined-regex form. The test name string at line 510 stays unchanged; only the two trailing `await expect(...).rejects.toThrow(...)` calls collapse into one, matching the regex `/Cannot save to "\/Users\/x\/Desktop\/foo\.pdf" — this location is restricted/`. The em-dash character `—` (U+2014) MUST be preserved verbatim — copy it from `generateQuotePdf.ts:339` if needed. Sibling tests at lines 527 (disk full) and 537 (no-Tauri pass-through) MUST NOT be modified.

    Verify both edits in a single sequence: `grep -c "startsWith('forbidden path:')" src/pdf/generateQuotePdf.ts` must return 1; `grep -c "includes('forbidden path')" src/pdf/generateQuotePdf.ts` must return 0; the regex form `/Cannot save to "/` must appear in the test file. Run `npx vitest run src/pdf/generateQuotePdf.test.ts` (28 tests must pass); run `npx tsc -b` (must exit 0 per global CLAUDE.md `tsc -b` rule); run `npm run build` (the 8-gate chain must stay green).

    Commit atomically with: `gsd-sdk query commit "fix(pdf): tighten forbidden-path match + collapse double-invocation test (WR-01 + WR-03)" --files "src/pdf/generateQuotePdf.ts src/pdf/generateQuotePdf.test.ts"` — no `--no-verify`.
  </action>
  <verify>
    <automated>grep -c "startsWith('forbidden path:')" src/pdf/generateQuotePdf.ts | grep -q '^1$' && grep -c "includes('forbidden path')" src/pdf/generateQuotePdf.ts | grep -q '^0$' && grep -q 'Cannot save to' src/pdf/generateQuotePdf.test.ts && npx vitest run src/pdf/generateQuotePdf.test.ts && npx tsc -b && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "startsWith('forbidden path:')" src/pdf/generateQuotePdf.ts` returns 1 (WR-03 anchored prefix landed)
    - `grep -c "includes('forbidden path')" src/pdf/generateQuotePdf.ts` returns 0 (loose substring match removed)
    - The 2 new comment lines documenting the anchoring rationale are present above the `const msg =` line in generateQuotePdf.ts
    - In `src/pdf/generateQuotePdf.test.ts` the forbidden-path test (around line 510-525) has exactly ONE `rejects.toThrow` call (was 2)
    - The combined regex `/Cannot save to "\/Users\/x\/Desktop\/foo\.pdf" — this location is restricted/` appears in the test file with the em-dash `—` (U+2014) intact
    - Sibling tests at `generateQuotePdf.test.ts:527-535` (disk full) and the no-Tauri pass-through test are unchanged
    - `npx vitest run src/pdf/generateQuotePdf.test.ts` exits 0 (all 28 tests in the file still pass)
    - `npx tsc -b` exits 0
    - `npm run build` exits 0 (the 8-gate chain stays green)
    - Commit recorded with `fix(pdf): tighten forbidden-path match + collapse double-invocation test (WR-01 + WR-03)` message; pre-commit hooks ran
  </acceptance_criteria>
  <done>
    WR-01 (test collapse) and WR-03 (substring tightening) shipped in one atomic commit. Sticky-mock fragility and false-positive `includes` match both eliminated. Phase 18 review carryover items 1 and 3 closed.
  </done>
</task>

<task type="auto">
  <name>Task 2: WR-02 Tauri 2.11.x dep bump (Rust crate + JS @tauri-apps/api re-pin)</name>
  <files>src-tauri/Cargo.toml, src-tauri/Cargo.lock, package.json, package-lock.json</files>
  <read_first>
    - .planning/phases/18-tauri-fs-scope-fix/18-REVIEW.md (WR-02 finding text and three strategy options — confirms D-05 strategy (b) is in scope)
    - .planning/phases/18-tauri-fs-scope-fix/18-01-SUMMARY.md (context on the current 2.10.1 pin — Phase 18 set this as a Rule-1 bug-fix deviation; this task corrects to ^2.11.0 alignment)
    - src-tauri/Cargo.toml (read the full file — confirm `tauri = { version = "2", features = [] }` line is at the expected position)
    - package.json (read the full file — confirm `"@tauri-apps/api": "^2.10.1"` pin location)
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md §2 (Tauri 2.11.x changelog findings — explicit D-06 SKIP-routine-UAT call backed by verbatim changelog quotes; zero `fs:*`/dialog/capability semantic changes)
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md §3 (the `checkpoint:human-verify` template — relevant ONLY for Task 3 fallback if this task fails)
  </read_first>
  <action>
    Apply WR-02 strategy (b) per CONTEXT.md D-05: bump the Rust `tauri` crate to 2.11.x AND re-pin `@tauri-apps/api` to `^2.11.0`.

    1. Edit `src-tauri/Cargo.toml`: change the `tauri` dependency line from `tauri = { version = "2", features = [] }` to `tauri = { version = "2.11", features = [] }`. Do NOT modify any other lines.
    2. Edit `package.json`: change the `"@tauri-apps/api"` pin from `"^2.10.1"` to `"^2.11.0"`. Do NOT modify other dependency lines. Plugin versions stay (`@tauri-apps/plugin-fs: ^2.5.1`, `@tauri-apps/plugin-dialog: ^2.7.1` — their peerDeps are `^2.11.0` per RESEARCH §2 confirmed via `npm view`).
    3. Run `npm install` from repo root. This refreshes `package-lock.json` and deduplicates `@tauri-apps/api` at the top level (no nested `/node_modules/@tauri-apps/{plugin-fs,plugin-dialog}/node_modules/@tauri-apps/api` copies should remain).
    4. Run `cd src-tauri && cargo check && cd ..` to refresh `Cargo.lock` against the new crate version. Expect a multi-line `Cargo.lock` diff (≥50 lines, similar to Phase 18's lockfile refresh — this is normal, not a deviation).
    5. Run the full verification chain (per RESEARCH §5 + ROADMAP success criterion 7):
       - `npx tsc -b` (must exit 0 — per global CLAUDE.md, use `tsc -b` not `--noEmit`)
       - `npm test` (must exit 0 — regression baseline)
       - `npm run build` (the 8-gate chain — must exit 0)
       - `npm run tauri build` (must exit 0 — Rust + bundle build with new crate)
       - `find node_modules/@tauri-apps -path '*/node_modules/*/api/package.json' | wc -l` (must return 0 — confirms dedup; per ROADMAP success criterion 7 wording)
       - `find node_modules/@tauri-apps -name package.json | wc -l` (must return 6 — was 8 pre-bump)

    If ALL verification commands exit 0 AND the dedup find commands return the expected values, commit atomically: `gsd-sdk query commit "chore(deps): bump tauri crate to 2.11.x + @tauri-apps/api ^2.11.0 (WR-02)" --files "src-tauri/Cargo.toml src-tauri/Cargo.lock package.json package-lock.json"` — no `--no-verify`.

    If ANY verification command exits non-zero, do NOT silently roll back, do NOT silently switch strategies, do NOT commit. Surface the failure to the orchestrator so Task 3 (the conditional `checkpoint:human-verify` fallback) fires. Per CONTEXT.md `<specifics>`: the executor MUST require an explicit user checkpoint to choose between rolling back the bump entirely OR accepting the dual-copy as a documented escape hatch. Strategy (a) plugin-downgrade is explicitly REJECTED per D-05a and must not be considered.

    Routine post-bump UAT-A re-run is NOT required per CONTEXT.md D-06 + RESEARCH §2 (changelog evidence: zero `fs:*`/`dialog:`/capability-ACL local-origin semantic changes in the entire 2.11.x line; the 2.11.1 remote-origin ACL fix is orthogonal to 3DCoster's local-origin desktop app). Trust the test suite + `npm run tauri build` exit code.
  </action>
  <verify>
    <automated>grep -q 'tauri = { version = "2.11"' src-tauri/Cargo.toml && grep -q '"@tauri-apps/api": "\^2.11.0"' package.json && (cd src-tauri && cargo check) && npx tsc -b && npm test && npm run build && npm run tauri build && [ "$(find node_modules/@tauri-apps -path '*/node_modules/*/api/package.json' | wc -l | tr -d ' ')" = "0" ] && [ "$(find node_modules/@tauri-apps -name package.json | wc -l | tr -d ' ')" = "6" ]</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/Cargo.toml` contains `tauri = { version = "2.11"` (was `version = "2"`)
    - `package.json` contains `"@tauri-apps/api": "^2.11.0"` (was `"^2.10.1"`)
    - `cd src-tauri && cargo check` exits 0
    - `npm run tauri build` exits 0
    - `npx tsc -b` exits 0
    - `npm test` exits 0 (regression baseline — all existing tests still pass)
    - `npm run build` exits 0 (the 8-gate chain stays green)
    - `find node_modules/@tauri-apps -path '*/node_modules/*/api/package.json' | wc -l` returns `0` (no nested api copies — per ROADMAP success criterion 7)
    - `find node_modules/@tauri-apps -name package.json | wc -l` returns `6` (was 8 pre-bump per RESEARCH §2)
    - `Cargo.lock` and `package-lock.json` regenerated (multi-line diffs expected; not a deviation)
    - Commit recorded with `chore(deps): bump tauri crate to 2.11.x + @tauri-apps/api ^2.11.0 (WR-02)` message; pre-commit hooks ran
  </acceptance_criteria>
  <done>
    WR-02 shipped: Tauri crate at 2.11, @tauri-apps/api at ^2.11.0, single top-level api copy in node_modules, full build chain green. Phase 18 review carryover item 2 closed; ROADMAP success criterion 7 satisfied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="conditional">
  <name>Task 3: CONDITIONAL fallback — fires only if Task 2 verification fails (rollback vs accept-dual-copy)</name>
  <files>(no file modifications — decision-only checkpoint)</files>
  <read_first>
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-RESEARCH.md §3 (the `checkpoint:human-verify` template + the WR-02 fallback path semantics — explains why this is conditional and what `rollback` vs `accept-dual-copy` mean)
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-CONTEXT.md `<specifics>` (the locked rule: "the planner should require an explicit user checkpoint to choose between rolling back the bump entirely or accepting the dual-copy as a documented escape hatch")
    - .planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-CONTEXT.md `<deferred>` (strategy (c) reserved as documented escape hatch only — not a routine fallback)
    - .planning/phases/18-tauri-fs-scope-fix/18-01-PLAN.md (lines 244-315 — Phase 18's `checkpoint:human-verify` Task 3 pattern, the byte-perfect template this task reuses)
    - src-tauri/Cargo.toml (current state — may have been edited by the failed Task 2)
    - src-tauri/Cargo.lock (current state)
    - package.json (current state)
    - package-lock.json (current state)
  </read_first>
  <what-built>
    THIS TASK IS CONDITIONAL — it fires ONLY IF Task 2's verification chain exited non-zero (any of `cargo check`, `tsc -b`, `npm test`, `npm run build`, `npm run tauri build`, or the dedup `find` checks failed). On the happy path (Task 2 verify all green), this task is SKIPPED and the plan completes after Task 2's commit.

    On the failure path: Task 2's bump from `tauri = "2"` → `tauri = "2.11"` + `@tauri-apps/api: ^2.11.0` has failed verification. The plan now needs an explicit user decision per CONTEXT.md `<specifics>`: ROLLBACK (revert all WR-02 edits to the pre-task state — return to the Phase 18 dual-copy baseline) OR ACCEPT-DUAL-COPY (revert ONLY the Rust crate bump, keep the `@tauri-apps/api` pin at 2.10.1, add a docs entry documenting the dual-copy as the accepted escape hatch per CONTEXT.md `<deferred>` reservation). The executor MUST NOT silently switch strategies and MUST NOT try the rejected strategy (a) plugin-downgrade.

    This checkpoint surfaces the failure output (paste the failed command output into the resume-signal context) and waits for the developer's explicit choice.
  </what-built>
  <action>
    The developer (user) performs the following — Claude does NOT execute these steps.

    Pre-condition: Task 2's verification chain exited non-zero. Confirm by running the failing command(s) and reading the error output before deciding.

    Choose ONE of the two paths below (do NOT mix; do NOT try strategy (a) plugin-downgrade — explicitly rejected per D-05a):

    1. ROLLBACK path (return to Phase 18 baseline with known dual-copy + WR-02 tech debt):
       - Run `git checkout -- src-tauri/Cargo.toml src-tauri/Cargo.lock package.json package-lock.json`. This restores the pre-WR-02 state on those 4 files.
       - Run `npm install` to restore the pre-bump `node_modules` state.
       - Run `npm run build && npm run tauri build` to confirm the project is back to a known-good state.
       - The plan's WR-02 portion is abandoned. WR-01 and WR-03 (Task 1) stay shipped.
       - No new commit needed (rollback restores HEAD).

    2. ACCEPT-DUAL-COPY path (keep `@tauri-apps/api` re-pin, revert Rust crate bump, document the dual-copy):
       - Run `git checkout -- src-tauri/Cargo.toml src-tauri/Cargo.lock` (revert ONLY the Rust crate bump).
       - Keep `package.json` `@tauri-apps/api: ^2.11.0` (Phase 18's pin moves forward to 2.11.0 even though the Rust crate stays at 2.10.x — this is the "documented dual-copy" escape hatch).
       - Add a new docs entry to `.planning/v1.3-TECH-DEBT.md` (create the file if it does not exist) noting the dual-copy as accepted with a back-reference to Phase 18 WR-02 + Phase 24 24-06-PLAN. The entry MUST explicitly state this is a deferred acceptance, NOT a silent strategy switch.
       - Run `npm install && npm run build && npm run tauri build` to confirm green build.
       - Commit with `gsd-sdk query commit "chore(deps): accept @tauri-apps/api dual-copy as documented escape hatch (WR-02 fallback)" --files "package.json package-lock.json .planning/v1.3-TECH-DEBT.md"` (no `--no-verify`).
  </action>
  <verify>
    <human-check>Developer types either `rollback` or `accept-dual-copy` in the resume-signal block AND has performed the corresponding git revert / docs edit AND has confirmed `npm run build` + `npm run tauri build` both exit 0 on the resulting tree.</human-check>
  </verify>
  <acceptance_criteria>
    If `rollback`:
      - `git status` shows clean working tree on `src-tauri/Cargo.toml`, `src-tauri/Cargo.lock`, `package.json`, `package-lock.json` (or shows uncommitted changes are reverted to the pre-Task-2 HEAD on those files)
      - `npm run build` exits 0 and `npm run tauri build` exits 0 (project back to known-good state)
      - Task 1 (WR-01 + WR-03) commit remains in history; WR-02 commit was never created

    If `accept-dual-copy`:
      - `src-tauri/Cargo.toml` contains `tauri = { version = "2"` (reverted to pre-Task-2 state)
      - `package.json` contains `"@tauri-apps/api": "^2.11.0"` (the new pin stays)
      - `.planning/v1.3-TECH-DEBT.md` exists and contains a new entry referencing WR-02 as accepted dual-copy with a back-pointer to Phase 18 and Phase 24
      - `npm run build` exits 0 and `npm run tauri build` exits 0
      - Commit recorded with `chore(deps): accept @tauri-apps/api dual-copy as documented escape hatch (WR-02 fallback)` message; pre-commit hooks ran
  </acceptance_criteria>
  <done>
    The Task 2 failure has been surfaced to the user and an explicit decision recorded. The WR plan terminates cleanly with either WR-01/WR-03 shipped + WR-02 abandoned (rollback) OR WR-01/WR-03/partial-WR-02 shipped + dual-copy documented (accept-dual-copy). No silent strategy switching occurred.
  </done>
  <resume-signal>
    Type `rollback` if you reverted all WR-02 edits and the project is back to the Phase 18 baseline, OR type `accept-dual-copy` if you kept the `@tauri-apps/api ^2.11.0` pin, reverted the Rust crate bump, and committed the documented escape-hatch entry. If neither path is acceptable (e.g., the failure mode is something neither option addresses), describe the situation and stop — the plan needs a different recovery strategy. Do NOT type `approved` or any other token; the orchestrator routes on the literal `rollback`/`accept-dual-copy` strings.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supply-chain → build | `npm install` and `cargo check` fetch packages from npm + crates.io. WR-02 bumps `tauri` Rust crate from 2.10.x → 2.11.x and `@tauri-apps/api` from 2.10.1 → 2.11.0. Both are pre-existing project dependencies under the same publishers, with prior Phase 18 supply-chain audit clearance. |
| Rust → JS error string | `generateQuotePdf.ts` consumes error messages from the Rust-side `tauri-plugin-fs` PathForbidden variant. WR-03 tightens the match anchor so unrelated error strings cannot satisfy the rewrite branch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06-01 | Tampering | `@tauri-apps/api` 2.10.1 → 2.11.0 npm install | accept | Same publisher (`tauri-apps`); 2.11.0 published 2026-04-30 via the official tauri-apps GitHub Actions release pipeline; ~10M weekly downloads; Phase 18 supply-chain audit cleared. No new dependency surface. |
| T-24-06-02 | Tampering | `tauri` Rust crate 2.10.2 → 2.11.2 cargo build | accept | Same publisher on crates.io; tagged release `tauri-v2.11.2` (2026-05-16) matches changelog. Per RESEARCH §2, zero `fs:*`/dialog/capability-ACL semantic changes from 2.10.x. |
| T-24-06-03 | Information Disclosure | `generateQuotePdf.ts` error string handling | mitigate | WR-03 tightens the substring matcher anchor (`startsWith('forbidden path:')`) so the rewrite path with `savePath` interpolation is no longer reachable from unrelated errors that incidentally contain `forbidden path` — eliminates a category of false-positive path disclosure in the rewritten error message. |
| T-24-06-04 | Denial of Service | Failed bump → broken build state | mitigate | Task 3 conditional `checkpoint:human-verify` provides explicit rollback or accept-dual-copy paths. The executor cannot silently leave the project in a broken state; the developer must attest to a green `npm run build` + `npm run tauri build` post-fallback. |
| T-24-06-05 | Elevation of Privilege | Tauri 2.11.1 remote-origin ACL hardening (vulnerability fix) | accept (mitigation comes for free with the bump) | The 2.11.1 changelog fixes a remote-origin IPC ACL bypass (#15266) and a `.localhost` suffix handling bug. 3DCoster is local-origin-only so the practical risk is low, but the bump pulls in the fix at no cost. |
| T-24-06-SC | Tampering | npm/cargo installs | accept (Phase 18 verified) | All 4 packages (`@tauri-apps/api`, `@tauri-apps/plugin-fs`, `@tauri-apps/plugin-dialog`, `tauri` crate) were cleared by Phase 18's package legitimacy audit (RESEARCH §2 lines 263-273); no `[ASSUMED]`/`[SUS]` flags. No blocking-human-verify checkpoint needed for this routine version bump within the same supplier. |
</threat_model>

<verification>
- Task 1 verification chain green: vitest + tsc + build all exit 0; grep counts confirm both edits landed
- Task 2 verification chain green: tsc + test + build + tauri build all exit 0; dedup find commands return expected counts (0 nested api copies; 6 total package.json files under node_modules/@tauri-apps)
- Task 3 fires ONLY if Task 2 exited non-zero; resolves with `rollback` or `accept-dual-copy` resume-signal — never silently switches strategies
- `git log --oneline -- src/pdf/generateQuotePdf.ts src/pdf/generateQuotePdf.test.ts src-tauri/Cargo.toml src-tauri/Cargo.lock package.json package-lock.json | head -5` shows 1-2 new commits (Task 1 always; Task 2 on happy path; Task 3 ACCEPT-DUAL-COPY adds a third)
</verification>

<success_criteria>
ROADMAP success criteria 6, 7, 8 satisfied:
- WR-01 (criterion 6): test collapse landed; sticky-mock fragility eliminated
- WR-02 (criterion 7): single `@tauri-apps/api` copy in built bundle confirmed via `find node_modules/@tauri-apps -path '*/node_modules/*/api/package.json' | wc -l` returning 0
- WR-03 (criterion 8): `startsWith('forbidden path:')` anchored matcher landed
</success_criteria>

<output>
Create `.planning/phases/24-nyquist-contracts-phase-13-visual-uat-phase-18-review-carryo/24-06-SUMMARY.md` when done — recording: (a) the WR-01 + WR-03 edits and Task 1 commit hash, (b) the WR-02 bump outcome (happy path commit hash OR Task 3 fallback resolution + reasoning), (c) any deviations from the recommended task order, (d) final state of `node_modules/@tauri-apps` (`find` command outputs).
</output>
