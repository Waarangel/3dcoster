---
phase: 26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - DOC-04
tags:
  - requirements
  - traceability
  - documentation

must_haves:
  truths:
    - "REQUIREMENTS.md DATA-07 detail row reads `- [x] **DATA-07**:` (was `- [ ]`)"
    - "REQUIREMENTS.md PERF-08 detail row reads `- [x] **PERF-08**:` (was `- [ ]`)"
    - "Traceability table row for DATA-07 reads `Complete` in Status column (was `Not started`)"
    - "Traceability table row for PERF-08 reads `Complete` in Status column (was `Not started`)"
    - "No other rows in REQUIREMENTS.md are modified (D-08 scope lock)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "v1.3 requirements registry with DATA-07 + PERF-08 marked complete"
      contains: "- [x] **DATA-07**"
  key_links:
    - from: ".planning/REQUIREMENTS.md DATA-07 row"
      to: ".planning/phases/22.1-break-even-formula-reconciliation/22.1-SUMMARY.md"
      via: "Phase 22.1 implementation evidence"
      pattern: "- \\[x\\] \\*\\*DATA-07\\*\\*"
    - from: ".planning/REQUIREMENTS.md PERF-08 row"
      to: ".planning/phases/22.1-break-even-formula-reconciliation/22.1-SUMMARY.md"
      via: "Phase 22.1 implementation evidence"
      pattern: "- \\[x\\] \\*\\*PERF-08\\*\\*"
---

<objective>
Sync REQUIREMENTS.md to reflect that DATA-07 + PERF-08 shipped successfully in Phase 22.1. Per CONTEXT.md D-07 and D-08: flip checkbox `[ ] → [x]` on the detail rows AND flip Status column `Not started → Complete` on the traceability table rows. Touch ONLY these 2 rows; no other audit.

Purpose: Close the "REQUIREMENTS.md traceability table stale" tech_debt item from `.planning/v1.3-MILESTONE-AUDIT.md`. The base requirements file is a planning artifact that was never updated post-execution; this plan reconciles it before `/gsd:complete-milestone v1.3` archives the file.

Output: 1 file modified, exactly 4 lines changed (2 checkbox flips + 2 traceability-table status flips).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m/26-CONTEXT.md
@.planning/v1.3-MILESTONE-AUDIT.md
@./CLAUDE.md

<!--
Per CONTEXT.md D-07, D-08:
- D-07: DATA-07 + PERF-08 detail rows flip `- [ ]` → `- [x]`. Traceability table flips Status column `Not started` → `Complete`. Cite Phase 22.1 SUMMARY.md in commit message.
- D-08: ONLY DATA-07 + PERF-08. Do not audit other rows. (REQUIREMENTS.md is archived by /gsd:complete-milestone v1.3 — any other lag is captured in archive snapshot.)

Exact current state in REQUIREMENTS.md (read into context — Task action references exact line numbers):

Line 42 (detail row): `- [ ] **DATA-07**: \`reconcileFixedCostsAtSave\` pure helper in \`src/db/backfill.ts\` is idempotent on the job level...` → flip `[ ]` to `[x]`

Line 80 (detail row): `- [ ] **PERF-08**: Formula correctness — the JobsManager break-even pill (\`computeBreakEvenInfo\` in \`src/components/JobsManager.tsx\`) computes \`Math.ceil((modelCost + fixedCostsAtSave.depreciation + fixedCostsAtSave.nozzleWear) / effectiveProfitPerUnit)\`...` → flip `[ ]` to `[x]`

Line 159 (traceability table row): `| DATA-07 | Phase 22.1 | MEDIUM | Not started |` → change Status column to `Complete`

Line 185 (traceability table row): `| PERF-08 | Phase 22.1 | HIGH (correctness) | Not started |` → change Status column to `Complete`

Phase 22.1 SUMMARY paths for commit-message citation:
- .planning/phases/22.1-break-even-formula-reconciliation/22.1-01-SUMMARY.md (DATA-07 reconcileFixedCostsAtSave helper)
- .planning/phases/22.1-break-even-formula-reconciliation/22.1-02-SUMMARY.md (PERF-08 formula widening + round-trip tests)
- .planning/phases/22.1-break-even-formula-reconciliation/22.1-04-SUMMARY.md (manual UAT APPROVED by user)
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip DATA-07 + PERF-08 checkboxes and traceability rows in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m/26-CONTEXT.md (D-07 + D-08)
    - .planning/REQUIREMENTS.md (read lines 1-200 to confirm current state of lines 42, 80, 159, 185 matches the snapshot in this plan's context comment)
    - .planning/v1.3-MILESTONE-AUDIT.md (the tech_debt entry that mandates this flip — see lines 30-31 for the audit's exact description of the gap)
  </read_first>
  <action>
    Use Edit tool (not Write — file has 200+ lines, surgical edits only per D-08). Make EXACTLY 4 edits:

    Edit 1 — DATA-07 detail row:
    - Find: `- [ ] **DATA-07**:`
    - Replace with: `- [x] **DATA-07**:`
    - (Only the leading checkbox changes; the body text after `**DATA-07**:` is preserved verbatim)

    Edit 2 — PERF-08 detail row:
    - Find: `- [ ] **PERF-08**:`
    - Replace with: `- [x] **PERF-08**:`
    - (Only the leading checkbox changes)

    Edit 3 — DATA-07 traceability table row:
    - Find: `| DATA-07 | Phase 22.1 | MEDIUM | Not started |`
    - Replace with: `| DATA-07 | Phase 22.1 | MEDIUM | Complete |`

    Edit 4 — PERF-08 traceability table row:
    - Find: `| PERF-08 | Phase 22.1 | HIGH (correctness) | Not started |`
    - Replace with: `| PERF-08 | Phase 22.1 | HIGH (correctness) | Complete |`

    DO NOT touch any other row. DO NOT audit DOC-03 or DOC-04 (which are this milestone's own requirements — they flip when Phase 26 itself ships, not in this plan). DO NOT update the milestone header count (`Coverage: 51/51 — 100%` line in ROADMAP.md is out of scope here; that's REQUIREMENTS.md vs ROADMAP.md — different file).

    DO NOT add/remove blank lines, comments, or any other content. Exactly 4 line replacements.

    Commit message MUST cite Phase 22.1 evidence per D-07 (recommended: `docs(26-03): flip DATA-07 + PERF-08 to complete in REQUIREMENTS.md (Phase 22.1 SUMMARY)`).
  </action>
  <verify>
    <automated>
      grep -E "^- \[x\] \*\*DATA-07\*\*:" .planning/REQUIREMENTS.md \
        && grep -E "^- \[x\] \*\*PERF-08\*\*:" .planning/REQUIREMENTS.md \
        && grep -E "^\| DATA-07 \| Phase 22\.1 \| MEDIUM \| Complete \|$" .planning/REQUIREMENTS.md \
        && grep -E "^\| PERF-08 \| Phase 22\.1 \| HIGH \(correctness\) \| Complete \|$" .planning/REQUIREMENTS.md \
        && test $(grep -cE "^- \[ \] \*\*(DATA-07|PERF-08)\*\*:" .planning/REQUIREMENTS.md) -eq 0 \
        && test $(grep -cE "^\| (DATA-07|PERF-08) \| Phase 22\.1 \|.*\| Not started \|$" .planning/REQUIREMENTS.md) -eq 0 \
        && test $(git diff --numstat .planning/REQUIREMENTS.md | awk '{print $1+$2}') -le 8
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*DATA-07\*\*:"` returns 1
    - `grep -c "^- \[x\] \*\*PERF-08\*\*:"` returns 1
    - `grep -c "^| DATA-07 | Phase 22\.1 | MEDIUM | Complete |$"` returns 1
    - `grep -c "^| PERF-08 | Phase 22\.1 | HIGH (correctness) | Complete |$"` returns 1
    - `grep -c "^- \[ \] \*\*(DATA-07|PERF-08)\*\*:"` returns 0 (old unchecked rows gone)
    - `grep -c "^| (DATA-07|PERF-08) | Phase 22\.1 |.*| Not started |$"` returns 0 (old traceability rows gone)
    - `git diff --numstat .planning/REQUIREMENTS.md` shows exactly 4 added + 4 removed lines (4 line replacements — total numstat ≤ 8)
    - `git diff -U0 .planning/REQUIREMENTS.md` shows changes ONLY on lines containing DATA-07 or PERF-08 (no other rows modified per D-08)
    - No other detail row checkbox state changed (sanity: `grep -c "^- \[x\] \*\*"` count == pre-edit count + 2)
    - `npm test` exit code 0 (doc-only change — must not affect tests)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md has DATA-07 + PERF-08 marked as complete in both the detail-row checkboxes and the traceability table Status column. No other rows touched. 4 lines changed. Commit ready citing Phase 22.1 SUMMARY evidence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Planning artifact only — no runtime code, no external surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-ID | Information Disclosure | Stale REQUIREMENTS.md misrepresents v1.3 completion state | mitigate | This plan IS the mitigation — flipping checkboxes + traceability per Phase 22.1 verified evidence |
| T-26-03-T | Tampering | Scope-creep editing other rows beyond DATA-07/PERF-08 | mitigate | D-08 lock + acceptance_criteria forbids `git diff` showing changes on non-DATA-07/PERF-08 lines |
</threat_model>

<verification>
After task completes:
1. All 4 grep assertions in `<verify><automated>` pass
2. `git diff --stat .planning/REQUIREMENTS.md` shows exactly 1 file, ≤ 8 line changes (4 replacements = 4 added + 4 removed)
3. `git diff -U0 .planning/REQUIREMENTS.md` shows no changes on rows not containing DATA-07 or PERF-08
4. v1.3-MILESTONE-AUDIT.md re-run would no longer list "REQUIREMENTS.md traceability table stale" under tech_debt
5. `npm test` exit 0
</verification>

<success_criteria>
- 1 file modified (REQUIREMENTS.md)
- Exactly 4 line replacements (2 checkbox flips + 2 traceability-table status flips)
- No scope creep onto other rows (D-08 honored)
- Commit message cites Phase 22.1 SUMMARY per D-07
- `npm test` baseline unchanged
</success_criteria>

<output>
Create `.planning/phases/26-v1-3-cleanup-flip-validation-md-statuses-sync-requirements-m/26-03-SUMMARY.md` when done. Summary must show the 4 before/after line diffs, confirm only DATA-07 and PERF-08 rows changed, and confirm `npm test` exit 0.
</output>
