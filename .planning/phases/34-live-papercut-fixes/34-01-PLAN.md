---
phase: 34-live-papercut-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PwaUpdatePrompt.tsx
  - src/components/PwaUpdatePrompt.test.tsx
  - src/components/ScrollToTop.tsx
  - src/main.tsx
  - src/components/ScrollToTop.test.tsx
autonomous: true
requirements: [FIX-01, FIX-02]
must_haves:
  truths:
    - "Clicking the web PWA 'Reload' button reloads onto the new version even on an uncontrolled page (fresh first load / hard refresh), not just a warm-controlled page."
    - "Navigating between routes via footer/nav links lands the user at the top of the destination page."
    - "In-page #hash anchors still jump to their target and are left untouched by ScrollToTop."
  artifacts:
    - path: "src/components/PwaUpdatePrompt.tsx"
      provides: "handleReload that handles both waiting-worker and uncontrolled-page reload paths (verified, not rebuilt)"
      contains: "handleReload"
    - path: "src/components/ScrollToTop.tsx"
      provides: "Router-mounted component that scrolls to top on pathname change, leaving #hash alone"
      contains: "useLocation"
    - path: "src/main.tsx"
      provides: "ScrollToTop mounted inside BrowserRouter"
      contains: "ScrollToTop"
  key_links:
    - from: "src/main.tsx"
      to: "src/components/ScrollToTop.tsx"
      via: "component mounted inside <BrowserRouter>"
      pattern: "<ScrollToTop"
    - from: "src/components/PwaUpdatePrompt.tsx"
      to: "navigator.serviceWorker"
      via: "getRegistration + controllerchange reload"
      pattern: "controllerchange"
---

<objective>
Lock in the two web-shell papercut fixes: the PWA "Reload" update button (FIX-01, already built on this branch) and route-change scroll-to-top (FIX-02, a clean cherry-pick from the redesign branch).

Purpose: Updates must reload reliably even on uncontrolled pages, and SPA navigation must land at the top of the next page instead of inheriting the previous scroll offset. Both are live, user-facing papercuts in the web build.

Output: A verified/regression-tested PwaUpdatePrompt, plus ScrollToTop.tsx mounted in the router with a focused behavior test. No new features, no version bumps, no badges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Existing PwaUpdatePrompt contract (already built + tested on this branch). -->
From src/components/PwaUpdatePrompt.tsx:
- export function PwaUpdatePrompt(): JSX.Element | null
- Uses useRegisterSW() from 'virtual:pwa-register/react' (aliased to src/test/stubs/pwaRegister.ts in vitest).
- handleReload(): controlled path adds a one-time 'controllerchange' listener that calls window.location.reload(); if registration.waiting exists -> updateServiceWorker(true) + 2s setTimeout safety net; else reload() immediately.

From src/main.tsx:
- Root() renders <BrowserRouter><Suspense><Routes>...</Routes></Suspense>{!__IS_TAURI__ && <PwaUpdatePrompt />}</BrowserRouter>
- ScrollToTop must mount INSIDE <BrowserRouter> (it depends on router context via useLocation), as a sibling of <Suspense>, before <Routes> or right after it.

Cherry-pick source (NOT on this branch yet):
- commit 4da205f "fix(nav): scroll to top on page navigation (ScrollToTop)" on branch test/design-skills-experiment.
- Adds src/components/ScrollToTop.tsx (25 lines) + 2 lines in src/main.tsx.
- Behavior: instant (not smooth) window.scrollTo on pathname change; leaves #hash anchors alone.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify FIX-01 PWA Reload regression — no rebuild</name>
  <read_first>
    - src/components/PwaUpdatePrompt.tsx (the implementation already on this branch)
    - src/components/PwaUpdatePrompt.test.tsx (the existing regression test)
    - src/test/stubs/pwaRegister.ts (the virtual:pwa-register/react stub used by the test)
    - vitest.config.ts (confirm the `virtual:pwa-register/react` alias points at the stub)
  </read_first>
  <action>
    This is a VERIFY/REGRESSION task — do NOT re-implement PwaUpdatePrompt. Read PwaUpdatePrompt.tsx and confirm `handleReload` covers BOTH branches: (a) waiting worker present → `updateServiceWorker(true)` + reload on a one-time `controllerchange` listener + a 2s `setTimeout` safety net, and (b) nothing waiting (uncontrolled / already-activated page) → `window.location.reload()` immediately. Confirm the existing test asserts the Reload click reaches `window.location.reload()` in both shapes. Run the test to prove it is green. If — and only if — a branch is missing or a test assertion is absent, add the missing assertion to PwaUpdatePrompt.test.tsx; otherwise change nothing in either file. Document in the SUMMARY that FIX-01 was verified (not rebuilt) and which test cases cover the uncontrolled-page path.
  </action>
  <verify>
    <automated>npm test -- PwaUpdatePrompt</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- PwaUpdatePrompt` passes.
    - PwaUpdatePrompt.tsx still contains the uncontrolled-page path (reload immediately when `registration.waiting` is falsy) and the controlled-page path (controllerchange + 2s safety net) — confirmed by reading the file.
    - No functional rewrite of handleReload occurred (git diff on PwaUpdatePrompt.tsx is empty unless a genuinely missing test assertion required a test-file-only edit).
  </acceptance_criteria>
  <done>FIX-01 confirmed present and regression-locked; PwaUpdatePrompt test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ScrollToTop and mount it in the router (FIX-02)</name>
  <files>src/components/ScrollToTop.tsx, src/main.tsx, src/components/ScrollToTop.test.tsx</files>
  <read_first>
    - src/main.tsx (current Root() — see where BrowserRouter/Suspense/Routes are and where PwaUpdatePrompt sits)
    - commit 4da205f via `git show 4da205f` (the exact ScrollToTop.tsx + main.tsx diff to port)
    - src/components/AnnouncementBanner.test.tsx (an existing test in this repo to mirror test setup/imports)
  </read_first>
  <behavior>
    - Test 1: On a `pathname` change (e.g. "/" → "/features"), ScrollToTop calls window.scrollTo to the top (x=0, y=0) using instant behavior.
    - Test 2: A change that only alters the `#hash` (same pathname, e.g. "/faq" → "/faq#shipping") does NOT trigger a scroll-to-top — hash anchors are left alone.
    - Test 3: ScrollToTop renders null (no DOM output).
  </behavior>
  <action>
    Bring FIX-02 onto this branch by `git cherry-pick 4da205f`. If the cherry-pick applies cleanly, accept it. If it conflicts on `src/main.tsx` (likely, since main.tsx has diverged), abort the cherry-pick and port the 27-line diff manually: create `src/components/ScrollToTop.tsx` as a router-aware component that reads `useLocation()` from react-router-dom and, in a `useEffect` keyed on `pathname` (NOT on the full location / hash), calls `window.scrollTo({ top: 0, left: 0, behavior: 'instant' })`; gate so a hash-only change does not scroll (effect depends on `pathname` only). It renders `null`. Then mount `<ScrollToTop />` inside `<BrowserRouter>` in `src/main.tsx` (sibling of `<Suspense>`, e.g. immediately after the opening `<BrowserRouter>` tag) so it has router context. Add `src/components/ScrollToTop.test.tsx` covering the three behaviors above (render the component inside a MemoryRouter, change the route, assert window.scrollTo was/wasn't called; spy on window.scrollTo). Do not animate (instant, per React Router guidance — smooth fights the per-page reveal animations). No badge, no features.ts entry (this is a fix, not a feature).
  </action>
  <verify>
    <automated>npm test -- ScrollToTop</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ScrollToTop.tsx exists, imports useLocation from react-router-dom, scrolls to top instantly on pathname change, and is keyed on pathname only (hash-only changes do not scroll).
    - src/main.tsx mounts `<ScrollToTop />` inside `<BrowserRouter>`.
    - `npm test -- ScrollToTop` passes with the three behavior assertions (pathname change scrolls, hash-only change does not, renders null).
    - `tsc -b` reports no type errors introduced by these files.
  </acceptance_criteria>
  <done>Route navigation lands at the top of the destination page; #hash anchors untouched; behavior is test-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page → service worker | The SW skip-waiting + reload flow (FIX-01) is the only privileged action; it is existing, browser-verified, and unit-tested. |
| router → window.scrollTo | ScrollToTop reads only the router pathname and calls a browser scroll API. No user input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | PwaUpdatePrompt SW skip-waiting reload | accept | Existing + verified flow; this plan only regression-tests it, introduces no new SW messaging surface. |
| T-34-02 | Denial of Service | ScrollToTop effect | accept | Effect keyed on pathname only; no loop, no listener leak (React cleans up effect). Renders null. |
</threat_model>

<verification>
- `npm test -- PwaUpdatePrompt` green (FIX-01 regression locked).
- `npm test -- ScrollToTop` green (FIX-02 behavior locked).
- `tsc -b` clean for the touched files.
- Read-back: PwaUpdatePrompt.tsx unchanged functionally; ScrollToTop mounted inside BrowserRouter in main.tsx.
</verification>

<success_criteria>
- FIX-01: The "A new version is available · Reload" button reloads onto the new version even on a hard-refreshed / uncontrolled page (no controlling SW), not just a warm-controlled page — verified, with a green regression test.
- FIX-02: Navigating between routes via footer/nav links lands the user at the top of the destination page; in-page `#hash` anchors still jump to their target (left untouched) — with a green behavior test.
</success_criteria>

<output>
Create `.planning/phases/34-live-papercut-fixes/34-01-SUMMARY.md` when done.
</output>
