---
phase: 34-live-papercut-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ResetAssetsModal.tsx
  - src/components/ResetAssetsModal.test.tsx
  - src/components/AssetLibrary.tsx
  - src/App.tsx
  - src/components/CostCalculator.tsx
  - src/components/CostCalculator.test.tsx
autonomous: true
requirements: [FIX-03, FIX-04]
must_haves:
  truths:
    - "The Asset 'Reset all' action opens the app's styled confirm modal showing the item counts about to be deleted, instead of a native window.confirm()."
    - "The reset modal requires an explicit affirmative click to confirm — it does not destroy data on a default/Enter-through."
    - "Confirming an edit-job jump scrolls the calculator to the 'Editing…' banner so the user can see which job they are editing."
    - "The edit-job scroll respects prefers-reduced-motion."
  artifacts:
    - path: "src/components/ResetAssetsModal.tsx"
      provides: "Styled confirm modal for Reset all (built on the shared Modal primitive), shows counts, danger confirm button"
      contains: "Modal"
    - path: "src/components/AssetLibrary.tsx"
      provides: "Reset flow that opens ResetAssetsModal instead of window.confirm()"
      contains: "ResetAssetsModal"
    - path: "src/components/CostCalculator.tsx"
      provides: "Editing banner with a ref that is scrolled into view when editingJob becomes set"
      contains: "scrollIntoView"
  key_links:
    - from: "src/components/AssetLibrary.tsx"
      to: "src/components/ResetAssetsModal.tsx"
      via: "modal opened from handleReset, confirm calls onResetPrinters/onResetMaterials"
      pattern: "ResetAssetsModal"
    - from: "src/components/CostCalculator.tsx"
      to: "the Editing banner element"
      via: "ref.current.scrollIntoView in a useEffect keyed on editingJob"
      pattern: "scrollIntoView"
---

<objective>
Close the two calculator-app papercuts: replace the native `window.confirm()` on the destructive Asset "Reset all" with the app's styled confirm modal showing item counts (FIX-03, audit 3.2), and scroll the calculator to the "Editing…" banner when the user jumps in to edit a job (FIX-04, audit 3.6).

Purpose: The most destructive action in the asset library currently uses an unbranded, easy-to-Enter-through `window.confirm()`; it should be a deliberate, counts-showing modal. And editing a job lands at the top of a long form with no visual confirmation of which job is being edited — scrolling to the banner fixes that.

Output: A new ResetAssetsModal built on the shared Modal primitive, AssetLibrary wired to it, and a reduced-motion-aware scroll-to-banner on edit. No new features, no version bumps, no badges. Surgical edits only — do not refactor AssetLibrary, App, or CostCalculator beyond the exact lines these fixes require.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@./.claude/CLAUDE.md

<interfaces>
<!-- The analog confirm modal to mirror (built on the shared Modal primitive). -->
From src/components/DeclineQuoteModal.tsx:
- Pattern: a `quote: Quote | null` prop drives open/close (isOpen = quote !== null); footer has a secondary Cancel button and a danger confirm button; built with `<Modal isOpen onClose title size>` from './ui'.

From src/components/ui/Modal.tsx (via src/components/ui index — `export { Modal } from './Modal'`):
- Modal provides role="dialog", aria-modal, and focus trap (via src/components/ui/dialogA11y.ts). Use it; do not hand-roll a dialog.

From src/components/AssetLibrary.tsx (existing reset flow to replace):
- Props include `onResetMaterials: () => void` and `onResetPrinters: () => void`.
- State: `const [resetError, setResetError] = useState<string | null>(null)`, `filterCategory: AssetCategory | 'all'`.
- `handleReset` (~line 826) currently calls `window.confirm(...)` then `await onResetPrinters()` (when filterCategory === 'printer') or `await onResetMaterials()`.
- Counts source: `assets` prop is the full list. Printers = assets.filter(a => a.category === 'printer'); materials = assets.filter(a => a.category !== 'printer') (the same split used by `filteredAssets`/`displayAssets`). The Reset button label (~line 969) already keys off filterCategory.
- An existing `resetError` is rendered with `role="alert"` (~line 986) — keep that error surface.

From src/App.tsx (FIX-04 wiring):
- `const [editingJob, setEditingJob] = useState<PrintJob | null>(null)` (line 45).
- `handleEditJob(job)` (~line 157) sets editingJob and switches activeTab to 'calculator'. This IS the "edit-job jump" — there is no intermediate confirm dialog; landing on the calculator with editingJob set is the confirmed jump.
- `<CostCalculator ... editingJob={editingJob} onCancelEdit={handleCancelEdit} ... />` (~line 321).

From src/components/CostCalculator.tsx:
- Props: `editingJob: PrintJob | null` (line 32), destructured in the component signature (~line 75).
- The "Editing Banner" JSX is at ~line 829–849: `{editingJob && (<div className="bg-blue-600/20 ...">Editing: {editingJob.name}...</div>)}`.

From src/index.css:
- Global reduced-motion handling exists via `@media (prefers-reduced-motion: reduce)` (CSS-only). For a JS scrollIntoView, gate the `behavior` with `window.matchMedia('(prefers-reduced-motion: reduce)').matches` → use 'auto'/'instant' when reduced, 'smooth' otherwise.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ResetAssetsModal + wire AssetLibrary reset to it (FIX-03)</name>
  <files>src/components/ResetAssetsModal.tsx, src/components/ResetAssetsModal.test.tsx, src/components/AssetLibrary.tsx</files>
  <read_first>
    - src/components/DeclineQuoteModal.tsx (the confirm-modal analog to mirror)
    - src/components/ui/Modal.tsx (the shared Modal primitive — props, focus trap)
    - src/components/AssetLibrary.tsx lines ~480–540 (props + reset state) and ~820–990 (handleReset, Reset button, resetError surface)
    - src/components/DeclineQuoteModal.test.tsx (test pattern for a Modal-based confirm)
    - ./.claude/CLAUDE.md (icon-button + no-raw-html conventions: use shared Button, never raw <button>)
  </read_first>
  <behavior>
    - Test 1: When open in 'printer' mode with N printers, the modal renders dialog chrome and copy stating it will replace the N custom printers with the default printer list (count N is shown).
    - Test 2: When open in 'materials' mode with M materials, the copy states it will replace the M materials with the default list (count M is shown).
    - Test 3: Clicking the danger confirm button calls onConfirm exactly once; clicking Cancel calls onClose and does NOT call onConfirm.
    - Test 4: The destructive action requires the explicit confirm click — rendering the open modal does not call onConfirm, and there is no auto-confirm on mount/Enter-through (confirm is wired only to the danger button's onClick).
  </behavior>
  <action>
    Create `src/components/ResetAssetsModal.tsx` mirroring DeclineQuoteModal: a confirm-style modal built on the shared `Modal` from './ui'. Props: an open/mode driver (e.g. `mode: 'printer' | 'material' | null` where null = closed, OR an `isOpen` + `mode` pair — match the DeclineQuoteModal `value | null` style), a `count: number` (printers or materials about to be replaced), `onConfirm: () => Promise<void> | void`, and `onClose: () => void`. Title and body copy must state the counts about to be deleted/replaced: printer mode → "This will replace your {count} custom printers with the default printer list." (singular/plural correctly), material mode → "This will replace your {count} materials with the default list." Footer: a secondary "Cancel" Button (onClose) and a `variant="danger"` confirm Button (e.g. "Reset printers" / "Reset materials") whose onClick is the ONLY path to onConfirm — no auto-confirm, no default-button Enter-through that destroys data. Surface a confirm error inside the modal like DeclineQuoteModal does. Use shared `Button` (never raw `<button>`); the icon-button/no-raw-html lint must stay green.

    Then in `src/components/AssetLibrary.tsx`, replace the two `window.confirm()` calls in `handleReset` (~lines 830 and 834) with modal-driven flow: add state for which reset is pending (e.g. `resetMode: 'printer' | 'material' | null`), have the Reset button open the modal in the mode matching `filterCategory` (printer tab → 'printer', otherwise → 'material'), compute the count from the `assets` prop (printers = `assets.filter(a => a.category === 'printer').length`, materials = `assets.filter(a => a.category !== 'printer').length`), and on modal confirm run the existing `await onResetPrinters()` / `await onResetMaterials()` inside the existing try/catch that sets `resetError`. Keep the existing `resetError` `role="alert"` surface. Do NOT touch any other AssetLibrary logic. No badge, no features.ts entry.
  </action>
  <verify>
    <automated>npm test -- ResetAssetsModal && node scripts/lint-no-raw-html.mjs</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ResetAssetsModal.tsx exists, built on the shared Modal (role="dialog" via the primitive), shows the item count in its copy, and confirm is reachable only via the danger button's onClick (no auto/Enter-through destroy).
    - AssetLibrary `handleReset` no longer calls `window.confirm` — `grep -n "window.confirm" src/components/AssetLibrary.tsx` returns nothing.
    - The Reset button opens ResetAssetsModal in the mode matching filterCategory; confirm calls onResetPrinters/onResetMaterials and preserves the resetError role="alert" surface.
    - `npm test -- ResetAssetsModal` passes all four behaviors; `node scripts/lint-no-raw-html.mjs` passes (no raw form elements introduced).
  </acceptance_criteria>
  <done>Reset all opens a styled, counts-showing confirm modal requiring an explicit affirmative click; window.confirm() removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scroll calculator to the Editing banner on edit-job jump (FIX-04)</name>
  <files>src/components/CostCalculator.tsx, src/components/CostCalculator.test.tsx</files>
  <read_first>
    - src/App.tsx ~lines 41–60 + 156–164 + 321–340 (editingJob state, handleEditJob, CostCalculator props)
    - src/components/CostCalculator.tsx ~line 75 (signature), ~line 319 (the editingJob useEffect deps), ~line 827–849 (the Editing Banner JSX)
    - src/index.css lines ~20–30 (the reduced-motion media query — for the matchMedia gating pattern)
    - src/components/CostCalculator.test.tsx (existing test setup to extend)
  </read_first>
  <behavior>
    - Test 1: When editingJob transitions from null to a job, an effect calls scrollIntoView on the banner ref (mock the ref element's scrollIntoView and assert it is called once).
    - Test 2: When prefers-reduced-motion is set (matchMedia mocked to matches=true), scrollIntoView is called with non-smooth behavior ('auto'); when not set, it uses 'smooth'.
    - Test 3: When editingJob is and stays null, scrollIntoView is not called (no spurious scroll on initial mount without an edit).
  </behavior>
  <action>
    In `src/components/CostCalculator.tsx`, add a `useRef<HTMLDivElement>(null)` for the Editing banner and attach it to the banner `<div>` at ~line 831 (the `bg-blue-600/20` editing-banner container). Add a `useEffect` keyed on `editingJob` (or on `editingJob?.id`) that, when `editingJob` is truthy and the ref is mounted, calls `bannerRef.current.scrollIntoView({ block: 'start', behavior })` where `behavior` is `'auto'` when `window.matchMedia('(prefers-reduced-motion: reduce)').matches` is true and `'smooth'` otherwise. This is the FIX-04 "confirming an edit-job jump" behavior — the jump is App.handleEditJob setting editingJob + switching to the calculator tab; the scroll fires when editingJob becomes set. Do not change handleEditJob in App.tsx (the existing wiring already passes editingJob down). Guard against null ref. Surgical edit only — do not refactor the long editingJob hydration effect at ~line 270. Add tests to CostCalculator.test.tsx covering the three behaviors (spy on Element.prototype.scrollIntoView or the ref node; mock matchMedia for the reduced-motion case). No badge, no features.ts entry.
  </action>
  <verify>
    <automated>npm test -- CostCalculator</automated>
  </verify>
  <acceptance_criteria>
    - CostCalculator.tsx attaches a ref to the Editing banner div and calls `scrollIntoView` in a useEffect keyed on editingJob when editingJob is truthy.
    - The scroll behavior is non-smooth ('auto') when prefers-reduced-motion matches, 'smooth' otherwise — gated via window.matchMedia.
    - No scroll fires when editingJob is null.
    - `npm test -- CostCalculator` passes the three new behaviors plus the existing suite; `tsc -b` clean for the file.
  </acceptance_criteria>
  <done>Jumping in to edit a job scrolls the calculator to the "Editing…" banner, respecting reduced-motion; test-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → destructive reset | "Reset all" replaces the user's custom printers/materials with defaults — irreversible data loss if mis-triggered. The confirm gate is the only protection. |
| editingJob → scroll | FIX-04 reads only editingJob and a media query, calls a browser scroll API. No user input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Tampering / data loss | ResetAssetsModal confirm flow | mitigate | Confirm is reachable ONLY via the danger button's onClick — no auto-confirm on open, no default/Enter-through that destroys data (asserted by Test 4). The modal shows the exact count about to be replaced so the user sees the blast radius before confirming. This strengthens the prior window.confirm() guard. |
| T-34-04 | Denial of Service | scrollIntoView effect | accept | Effect keyed on editingJob; null-guarded ref; no loop or listener leak. |
</threat_model>

<verification>
- `npm test -- ResetAssetsModal` and `npm test -- CostCalculator` green.
- `node scripts/lint-no-raw-html.mjs` green (no raw form elements introduced).
- `grep -n "window.confirm" src/components/AssetLibrary.tsx` returns nothing.
- `tsc -b` clean for touched files.
- Read-back: ResetAssetsModal built on shared Modal; CostCalculator banner has a ref + reduced-motion-gated scroll.
</verification>

<success_criteria>
- FIX-03: The Asset "Reset all" action opens the app's styled confirm modal showing the item counts about to be deleted, instead of a native window.confirm(); confirm requires an explicit affirmative click.
- FIX-04: Confirming an edit-job jump scrolls the calculator to the "Editing…" banner so the user can see which job they are editing, respecting prefers-reduced-motion.
</success_criteria>

<output>
Create `.planning/phases/34-live-papercut-fixes/34-02-SUMMARY.md` when done.
</output>
