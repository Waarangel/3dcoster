---
phase: 35-accessibility-tier-4
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AssetLibrary.tsx
  - src/components/AssetLibrary.test.tsx
autonomous: true
requirements: [A11Y-11, A11Y-12, A11Y-15]
must_haves:
  truths:
    - "When the asset form fails validation, AT immediately announces the error via role=alert"
    - "The failing required input is marked aria-invalid and points at the error text via aria-describedby"
    - "Every Asset-row edit/delete icon button announces a descriptive name"
    - "The category filter buttons are grouped under a labelled role=group; the sort ▲/▼ glyph is hidden from AT"
  artifacts:
    - path: "src/components/AssetLibrary.tsx"
      provides: "Form-error ARIA association + labelled category group + aria-hidden SortIndicator"
      contains: "role=\"alert\""
    - path: "src/components/AssetLibrary.test.tsx"
      provides: "Vitest assertions for form-error association + group label"
      contains: "aria-describedby"
  key_links:
    - from: "Name input (id={nameId})"
      to: "formError container id"
      via: "aria-describedby when formError set"
      pattern: "aria-describedby"
    - from: "formError container"
      to: "assistive technology"
      via: "role=alert announced on insert"
      pattern: "role=\"alert\""
---

<objective>
Close A11Y-11 (WCAG **Critical** — 3.3.1 / 1.3.1), the Asset-row portion of A11Y-12 (4.1.2), and the AssetLibrary AA cleanups in A11Y-15 (category filter `role="group"` + SortIndicator `aria-hidden`) — all on `AssetLibrary.tsx`.

Purpose: When the add/edit asset form fails validation, AT users hear nothing actionable — the inline `formError` div (lines 1084-1088) has no `role="alert"`, no `id`, and the failing inputs are not marked `aria-invalid`/`aria-describedby`. This is the phase's second WCAG-Critical item.
Output: `AssetLibrary.tsx` with form-error association + labelled category group + hidden sort glyph; new `AssetLibrary.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-accessibility-tier-4/35-RESEARCH.md
@.planning/phases/35-accessibility-tier-4/35-CONTEXT.md

<interfaces>
<!-- Confirmed by code read: -->
<!--   AssetLibrary already imports useId (line 1) and { EditButton, DeleteButton } from './ui' (line 13). -->
<!--   Existing useId() calls at lines 499-514 (nameId, unitId, packageCostId, ...). Add formErrorId the same way. -->
<!--   formError state: line 528. formError render: lines 1084-1088 (currently NO role/id). -->
<!--   Name input: lines 1092-1099 (id={nameId}, required). unit input id={unitId} (~1229); packageCost id={packageCostId} (~1240). -->
<!--   Asset-row EditButton/DeleteButton already correct at lines 337-338, 396-397, 464-465 (label={asset.name}) — VERIFY, do not regress. -->
<!--   SortIndicator: lines 475-478 (returns a <span> with ▲/▼). Column headers already use aria-sort (correct). -->
<!--   Category filter buttons with aria-pressed: in the filter row near line ~1027 (per audit/RESEARCH §15e). -->
<!--   NOTE: line 913 role="alert" is the loadError container — separate from formError; do not confuse them. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Associate asset-form validation errors with the failing inputs (A11Y-11, WCAG Critical)</name>
  <files>src/components/AssetLibrary.tsx, src/components/AssetLibrary.test.tsx</files>
  <read_first>
    - src/components/AssetLibrary.tsx (lines 495-530 useId block + formError state; lines 1082-1100 the form + formError render + Name input; the validation block that calls setFormError — search for `setFormError(` to see which fields each message covers, ~lines 685-720 region noted in RESEARCH)
    - .planning/phases/35-accessibility-tier-4/35-RESEARCH.md "A11Y-11 — Form Error Association" + "Code Examples › Form Error" + Pitfalls 1 & 2
    - src/components/ui/InfoTooltip.test.tsx (test convention)
  </read_first>
  <behavior>
    - Test: when the form is submitted with an empty required Name, a container with `role="alert"` renders containing the error text and carries a non-empty `id`.
    - Test: the Name input then has `aria-invalid="true"` and `aria-describedby` equal to the error container's `id`.
    - Test: when there is no formError, the Name input has neither `aria-invalid="true"` nor an `aria-describedby` pointing at a (now absent) error id.
  </behavior>
  <action>
    Add `const formErrorId = useId();` alongside the existing useId() calls (~line 499-514). On the formError container (lines 1084-1088), add `id={formErrorId}` and `role="alert"` (keep the existing className). Keep it conditionally rendered (`{formError && (...)}`) so insertion triggers the AT announcement — do NOT switch to an always-present empty alert (RESEARCH Pitfall 1). On the required Name `<Input id={nameId}>` (line 1092), add `aria-invalid={formError ? 'true' : undefined}` and `aria-describedby={formError ? formErrorId : undefined}`. Apply the same two attributes to the other required inputs that participate in the validation that sets `formError` (per the validation block: for the material form, the unit input `id={unitId}` and packageCost input `id={packageCostId}`; for the printer form, only Name is required). Always pair `aria-invalid` with `aria-describedby` and clear both (set to `undefined`) when `formError` is null (RESEARCH Pitfall 2). Confirm `Input` forwards `aria-invalid`/`aria-describedby` to the underlying `<input>` (it spreads `...props`); if it does not, pass them through.
  </action>
  <verify>
    <automated>npx vitest run src/components/AssetLibrary.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AssetLibrary form error container has `role="alert"` and a non-empty `id` when `formError` is set.
    - The Name input has `aria-invalid="true"` and `aria-describedby` pointing at the error container's id when `formError` is set.
    - When no error is present, the input exposes no `aria-invalid="true"` and no dangling `aria-describedby`.
    - `npx vitest run src/components/AssetLibrary.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Asset-form validation errors are programmatically associated with their failing inputs and announced via role=alert; vitest passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Asset-row icon-button labels + category-filter group + SortIndicator aria-hidden (A11Y-12 Asset rows, A11Y-15 cleanups)</name>
  <files>src/components/AssetLibrary.tsx, src/components/AssetLibrary.test.tsx</files>
  <read_first>
    - src/components/AssetLibrary.tsx (lines 337-338, 396-397, 464-465 Asset-row EditButton/DeleteButton; lines 475-478 SortIndicator; the category filter row near ~1027 with `aria-pressed` buttons)
    - .planning/phases/35-accessibility-tier-4/35-RESEARCH.md §15e (category group) and §15f (SortIndicator aria-hidden)
    - .claude/CLAUDE.md "Action buttons & icons" (icon-button + aria-label rules)
  </read_first>
  <behavior>
    - Test: Asset-row edit/delete controls expose `aria-label` of the form `Edit {name}` / `Delete {name}`.
    - Test: the category filter buttons are wrapped in an element with `role="group"` and an `aria-label` (e.g. "Filter by category").
    - Test: the SortIndicator `<span>` (▲/▼) has `aria-hidden="true"`.
  </behavior>
  <action>
    A11Y-12 Asset rows: verify the three Asset-row uses of `EditButton`/`DeleteButton` (lines 337-338, 396-397, 464-465) already pass `label={asset.name}` — they do per code read, so confirm via test and do not regress. A11Y-15 category group: wrap the category filter buttons (the `aria-pressed` "All" + per-category buttons in the filter row) in a `<div role="group" aria-label="Filter by category" className="flex gap-2 flex-wrap">`, keeping the search input OUTSIDE the group (RESEARCH §15e). A11Y-15 SortIndicator: add `aria-hidden="true"` to the `<span>` returned by `SortIndicator` (lines 475-478) so AT does not double-announce sort direction (the column headers' existing `aria-sort` is the authoritative mechanism — leave it untouched).
  </action>
  <verify>
    <automated>npx vitest run src/components/AssetLibrary.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Asset-row edit/delete buttons announce `Edit {name}` / `Delete {name}`.
    - The category filter buttons live inside a `role="group"` with a non-empty `aria-label`; the search input is not inside that group.
    - The SortIndicator span has `aria-hidden="true"`.
    - `npx vitest run src/components/AssetLibrary.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Asset-row icon buttons are labelled, the category filter is a labelled group, and the sort glyph is hidden from AT; vitest passes.</done>
</task>

</tasks>

<threat_model>
No new attack surface — ARIA-only changes to existing client-side form/list UI. No new auth, network, input-parsing, or data-handling. STRIDE register N/A for this plan.
</threat_model>

<verification>
- `npx vitest run src/components/AssetLibrary.test.tsx` green.
- `npm test` (full suite) green.
- Manual (phase smoke test): trigger an AssetLibrary form error under VoiceOver and confirm the error is announced and the field is identified.
</verification>

<success_criteria>
- A11Y-11: error container `role="alert"`; failing required inputs `aria-invalid="true"` + `aria-describedby` → error id; both cleared when no error.
- A11Y-12 (Asset rows): every edit/delete icon button has a descriptive `aria-label`.
- A11Y-15 (AssetLibrary cleanups): category filter is a labelled `role="group"`; SortIndicator glyph is `aria-hidden`.
</success_criteria>

<output>
Create `.planning/phases/35-accessibility-tier-4/35-02-SUMMARY.md` when done.
</output>
