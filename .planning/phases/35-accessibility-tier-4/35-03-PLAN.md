---
phase: 35-accessibility-tier-4
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FilamentSelector.tsx
  - src/components/FilamentSelector.test.tsx
autonomous: true
requirements: [A11Y-13]
must_haves:
  truths:
    - "The FilamentSelector trigger announces 'Filament' as its field label to AT"
    - "Each open brand submenu has its own accessible name (the brand)"
  artifacts:
    - path: "src/components/FilamentSelector.tsx"
      provides: "Trigger label association + submenu aria-label"
      contains: "aria-labelledby"
    - path: "src/components/FilamentSelector.test.tsx"
      provides: "Vitest assertions for trigger accessible name + submenu label"
      contains: "aria-label"
  key_links:
    - from: "Filament <label>"
      to: "trigger <Button>"
      via: "id + aria-labelledby"
      pattern: "aria-labelledby"
---

<objective>
Close A11Y-13 (WCAG 4.1.2) on `FilamentSelector.tsx`. Per RESEARCH (HIGH confidence), the component already has real focus-moving keyboard navigation, `role="menu"` with `aria-label="Filaments"` on the main menu, `aria-haspopup`/`aria-expanded` on the trigger and brand items. Only **two** gaps remain: (1) the trigger Button is not associated with its `<label>Filament</label>`, and (2) the brand submenu `role="menu"` div has no accessible name. No live region and no `aria-activedescendant` are needed — focus already moves to each item.

Purpose: Without the label association, AT announces the selected filament name but never says "Filament" (the field's purpose). Without a submenu name, the open brand submenu is an unnamed menu.
Output: `FilamentSelector.tsx` with the two label additions; new `FilamentSelector.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-accessibility-tier-4/35-RESEARCH.md
@.planning/phases/35-accessibility-tier-4/35-CONTEXT.md

<interfaces>
<!-- Confirmed by code read: -->
<!--   line 297: <label className="block text-xs text-slate-400 mb-1">Filament</label>  (no htmlFor / no id) -->
<!--   line 298-311: trigger <Button ... aria-haspopup="menu" aria-expanded={isOpen}>  (no aria-labelledby) -->
<!--   line 348: <div role="menu" aria-label="Filaments">  (main menu — already named, leave as-is) -->
<!--   line 381-382: brand items already have aria-haspopup="menu" + aria-expanded -->
<!--   line 410: <div role="menu" ...>  (brand submenu — NO aria-label; this is the gap) -->
<!--   The submenu is rendered per-brand; the brand identifier in scope (e.g. `brand`) is the right aria-label value. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Associate the FilamentSelector trigger with its label and name the brand submenu (A11Y-13)</name>
  <files>src/components/FilamentSelector.tsx, src/components/FilamentSelector.test.tsx</files>
  <read_first>
    - src/components/FilamentSelector.tsx (lines 290-430 — the `<label>` at 297, the trigger `<Button>` at 298, the main menu `role="menu"` at 348, the brand submenu `role="menu"` at 410; confirm the variable name holding the brand in the submenu render scope)
    - .planning/phases/35-accessibility-tier-4/35-RESEARCH.md "A11Y-13 — FilamentSelector Accessible Name + Submenu Announcement" (exact changes) + Pitfall 5 (use static `aria-label`, not `aria-labelledby`, on the menu container)
    - src/components/ui/InfoTooltip.test.tsx (test convention)
  </read_first>
  <behavior>
    - Test: the `<label>` rendered above the trigger has a non-empty `id`, and the trigger Button's `aria-labelledby` equals that id.
    - Test: when a brand submenu is open, its `role="menu"` div has a non-empty `aria-label` equal to the brand name.
    - Test: the main menu `role="menu"` still has `aria-label="Filaments"` (regression guard — unchanged).
  </behavior>
  <action>
    On the `<label>` at line 297, add a stable `id="filament-trigger-label"` (keep its existing classes/text). On the trigger `<Button>` at line 298, add `aria-labelledby="filament-trigger-label"` (keep existing `aria-haspopup`/`aria-expanded`). On the brand submenu `<div role="menu">` at line 410, add `aria-label={brand}` using whatever variable holds the current brand string in that render scope (so each open submenu announces e.g. "Bambu"). Do NOT add a live region or `aria-activedescendant` — the existing arrow-key handlers already call `.focus()` on each item, so AT announces items on focus (RESEARCH §A11Y-13). Leave the main menu `aria-label="Filaments"` (line 348) unchanged. Prefer static `aria-label` over `aria-labelledby` on the menu containers (RESEARCH Pitfall 5).
  </action>
  <verify>
    <automated>npx vitest run src/components/FilamentSelector.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The Filament `<label>` has `id="filament-trigger-label"` and the trigger Button has `aria-labelledby="filament-trigger-label"`.
    - An open brand submenu's `role="menu"` div has `aria-label` equal to the brand name.
    - The main menu `role="menu"` retains `aria-label="Filaments"`.
    - `npx vitest run src/components/FilamentSelector.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The trigger announces its "Filament" label and every open brand submenu has an accessible name; vitest passes.</done>
</task>

</tasks>

<threat_model>
No new attack surface — ARIA label additions to an existing client-side menu. No new auth, network, input-parsing, or data-handling. STRIDE register N/A for this plan.
</threat_model>

<verification>
- `npx vitest run src/components/FilamentSelector.test.tsx` green.
- `npm test` (full suite) green.
- Manual (phase smoke test): open the FilamentSelector and a brand submenu under VoiceOver; confirm "Filament" is announced for the field and the submenu announces the brand.
</verification>

<success_criteria>
- A11Y-13: FilamentSelector trigger has an accessible name via its associated label; each brand submenu has an accessible name. No live region / no aria-activedescendant added (existing focus-based nav already announces options).
</success_criteria>

<output>
Create `.planning/phases/35-accessibility-tier-4/35-03-SUMMARY.md` when done.
</output>
