---
phase: 35-accessibility-tier-4
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/App.a11y.test.tsx
  - src/components/ui/InfoTooltip.tsx
  - src/components/ui/InfoTooltip.test.tsx
autonomous: true
requirements: [A11Y-15]
must_haves:
  truths:
    - "The main app tabpanel can receive programmatic focus (tabIndex={-1})"
    - "The mobile 'Back to site' icon link has an accessible name regardless of screen width"
    - "InfoTooltip announces a concise label (not the full sentence) and dismisses on Escape"
  artifacts:
    - path: "src/App.tsx"
      provides: "main tabpanel tabIndex + labelled back-to-site link"
      contains: "tabIndex={-1}"
    - path: "src/components/ui/InfoTooltip.tsx"
      provides: "concise aria-label + Escape dismiss"
      contains: "Escape"
    - path: "src/App.a11y.test.tsx"
      provides: "Vitest assertions for tabpanel tabIndex + back-link label"
      contains: "Back to site"
  key_links:
    - from: "InfoTooltip button"
      to: "Escape key handler"
      via: "onKeyDown closing the open tooltip"
      pattern: "Escape"
---

<objective>
Close the remaining A11Y-15 AA cleanups (WCAG 4.1.2 / 2.4.3 supporting structure) across `App.tsx` and the shared `InfoTooltip.tsx`:
1. Main `<main role="tabpanel">` gets `tabIndex={-1}` so it can receive programmatic focus.
2. The mobile "Back to site" icon-only `<Link>` gets `aria-label="Back to site"` (and the icon SVG + CSS-hidden text span get `aria-hidden`) so it has an accessible name at all widths.
3. `InfoTooltip` changes its verbose `aria-label={text}` to a concise `aria-label="More information"` and gains Escape-to-dismiss. The existing `InfoTooltip.test.tsx` is updated for the new label.

Purpose: small, targeted structural ARIA fixes the audit flagged; the InfoTooltip change follows the canonical tooltip-button pattern (short label on the trigger, full text in the `role="tooltip"` via `aria-describedby`).
Output: `App.tsx` + `InfoTooltip.tsx` updated; new `App.a11y.test.tsx`; updated `InfoTooltip.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-accessibility-tier-4/35-RESEARCH.md
@.planning/phases/35-accessibility-tier-4/35-CONTEXT.md

<interfaces>
<!-- Confirmed by code read: -->
<!--   App.tsx <main>: lines 314-318 — role="tabpanel" id=... aria-labelledby={`tab-${activeTab}`}, NO tabIndex. -->
<!--   App.tsx back-to-site <Link>: lines 202-210 — to="/", icon <svg> (no aria-hidden), <span className="hidden sm:inline">Back to site</span>, NO aria-label on the Link. -->
<!--   InfoTooltip.tsx: src/components/ui/InfoTooltip.tsx — button line 22-37 has aria-label={text} (verbose) and onFocus/onBlur but NO onKeyDown. tooltip span role="tooltip" carries the full text. -->
<!--   InfoTooltip.test.tsx EXISTS (src/components/ui/InfoTooltip.test.tsx) — tests unique tooltip ids; it does not currently assert the button aria-label, but VERIFY and update any assertion that reads the button's aria-label as the full text. -->
<!--   App.tsx main tablist (line 277) arrow-key nav is OUT OF SCOPE (A11Y-10 covers SettingsModal only; RESEARCH Open Q3). Only add tabIndex={-1} to the main panel. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Main tabpanel tabIndex + labelled mobile "Back to site" link (A11Y-15)</name>
  <files>src/App.tsx, src/App.a11y.test.tsx</files>
  <read_first>
    - src/App.tsx (lines 200-211 the back-to-site `<Link>`; lines 314-318 the `<main role="tabpanel">`)
    - .planning/phases/35-accessibility-tier-4/35-RESEARCH.md §15a (main tabpanel) + §15d (back-to-site link)
    - src/components/ui/InfoTooltip.test.tsx (test convention — raw `createRoot` + `act`; for App, render inside a `MemoryRouter` since it uses react-router `<Link>`)
  </read_first>
  <behavior>
    - Test: the `<main role="tabpanel">` element has `tabIndex="-1"`.
    - Test: the back-to-site `<Link>` (the anchor) has `aria-label="Back to site"`.
    - Test: the link's icon SVG has `aria-hidden="true"` (no competing accessible name).
  </behavior>
  <action>
    On `<main>` (lines 314-318), add `tabIndex={-1}` (keep `role="tabpanel"`, `id`, `aria-labelledby`). This lets a future tab handler call `.focus()` on the panel; for the main tabs no arrow-key nav is added (out of scope — A11Y-10 is SettingsModal only). On the back-to-site `<Link>` (lines 202-210), add `aria-label="Back to site"`; add `aria-hidden="true"` to the icon `<svg>` (line 206) and to the `<span className="hidden sm:inline">Back to site</span>` (line 209) so the link's `aria-label` is the single accessible name regardless of viewport (RESEARCH §15d). Create `src/App.a11y.test.tsx` following the raw `createRoot`+`act` convention, wrapping `<App />` in a router context (`MemoryRouter` from react-router-dom) so the `<Link>` renders. If rendering full `<App />` pulls heavy DB providers, scope the test to query the rendered DOM for the `[role="tabpanel"]` and the back-to-site anchor; if full-App render is impractical in jsdom, assert against the smallest renderable subtree that includes these elements and document the choice in the test header comment.
  </action>
  <verify>
    <automated>npx vitest run src/App.a11y.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The `<main role="tabpanel">` has `tabIndex="-1"`.
    - The back-to-site link has `aria-label="Back to site"` and its icon SVG has `aria-hidden="true"`.
    - `npx vitest run src/App.a11y.test.tsx` exits 0.
  </acceptance_criteria>
  <done>The main tabpanel is programmatically focusable and the mobile back-to-site link has an accessible name at all widths; vitest passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: InfoTooltip concise label + Escape dismiss, and update its existing test (A11Y-15)</name>
  <files>src/components/ui/InfoTooltip.tsx, src/components/ui/InfoTooltip.test.tsx</files>
  <read_first>
    - src/components/ui/InfoTooltip.tsx (the full file — button at lines 22-37 with `aria-label={text}`, onFocus/onBlur; tooltip span `role="tooltip"` at 38-46 carries `text`)
    - src/components/ui/InfoTooltip.test.tsx (the full existing test — confirm which assertions read the button vs the tooltip span so the label change does not break unrelated id-uniqueness tests)
    - .planning/phases/35-accessibility-tier-4/35-RESEARCH.md §15c (InfoTooltip)
  </read_first>
  <behavior>
    - Test (updated/added): the trigger button's `aria-label` is "More information" (NOT the full `text` prop).
    - Test (added): when the tooltip is open, an Escape keydown on the button closes it (the `role="tooltip"` span is removed).
    - Test (regression): the full `text` is still present as the tooltip content (`role="tooltip"` span) when open, and `aria-describedby` links the button to it; the existing unique-id behavior is unchanged.
  </behavior>
  <action>
    In `InfoTooltip.tsx`, change the button's `aria-label={text}` to `aria-label="More information"` (the canonical concise tooltip-trigger label; the full `text` remains the visible/`aria-describedby`-linked tooltip content). Add `onKeyDown={(e) => { if (e.key === 'Escape' && open) { e.stopPropagation(); setOpen(false); } }}` to the button so an open tooltip dismisses on Escape without bubbling to any parent (e.g. a modal). Keep `onFocus`/`onBlur`/`onClick`, the `aria-describedby={open ? tooltipId : undefined}`, the `role="tooltip"` span, and the `useId()` id. Then update `InfoTooltip.test.tsx`: add/adjust assertions for the new `aria-label="More information"` and add an Escape-dismiss test; preserve the existing unique-id tests (they query `[role="tooltip"]`, not the button label, so they should still pass).
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/InfoTooltip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The InfoTooltip trigger button has `aria-label="More information"` (not the full sentence).
    - Pressing Escape while the tooltip is open removes the `role="tooltip"` span.
    - The full `text` still renders as the tooltip content when open; the unique-id tests still pass.
    - `npx vitest run src/components/ui/InfoTooltip.test.tsx` exits 0.
  </acceptance_criteria>
  <done>InfoTooltip announces a concise label and is Escape-dismissable; its existing test is updated and green.</done>
</task>

</tasks>

<threat_model>
No new attack surface — ARIA attribute + keyboard-handler changes to existing client-side UI (a nav link and a shared tooltip primitive). No new auth, network, input-parsing, or data-handling. STRIDE register N/A for this plan.
</threat_model>

<verification>
- `npx vitest run src/App.a11y.test.tsx` and `npx vitest run src/components/ui/InfoTooltip.test.tsx` green.
- `npm test` (full suite) green — InfoTooltip is shared; confirm no other test asserts the old verbose `aria-label`.
- Manual (phase smoke test): keyboard-focus an InfoTooltip, press Escape to dismiss; confirm SR announces "More information, button".
</verification>

<success_criteria>
- A11Y-15 (remaining): main tabpanel `tabIndex={-1}`; mobile "Back to site" link labelled; InfoTooltip concise `aria-label` + Escape-dismiss with its test updated.
</success_criteria>

<output>
Create `.planning/phases/35-accessibility-tier-4/35-05-SUMMARY.md` when done.
</output>
