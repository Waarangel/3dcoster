---
phase: 37-code-health-stretch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useDatabase.ts
  - src/hooks/useDatabase.test.ts
  - src/db/database.ts
  - src/db/database.test.ts
autonomous: true
requirements: [HYG-12]
must_haves:
  truths:
    - "useAssets init batches the two independent migration COUNT reads (printerCount + packagingCount) into a single Promise.all with one post-batch cancelled check"
    - "The conditional bulkPut writes after the batched reads remain sequential and individually cancelled-checked (reads batched, writes NOT)"
    - "database.ts v8 migration narrows JSON.parse to Partial<UserProfile> + typeof check before assigning currency (matches the v9 pattern), instead of an unvalidated `as UserProfile`"
    - "useDatabase.ts createQuote narrows JSON.parse to Partial<UserProfile> + typeof check before reading nextQuoteNumber, instead of an unvalidated `as UserProfile`"
    - "Asset seeding/migration and quote-number allocation behave identically to before (behavior-preserving)"
  artifacts:
    - path: "src/hooks/useDatabase.ts"
      provides: "Promise.all batch of printerCount+packagingCount in useAssets init; Partial<UserProfile>+typeof narrowing in createQuote (C3)"
      contains: "Promise.all"
    - path: "src/db/database.ts"
      provides: "v8 migration currency read narrowed via Partial<UserProfile>+typeof (C1)"
      contains: "Partial<UserProfile>"
    - path: "src/hooks/useDatabase.test.ts"
      provides: "Source-contract assertions for the Promise.all batch and the C3 narrowing"
      contains: "Promise.all"
    - path: "src/db/database.test.ts"
      provides: "Source-contract assertion for the C1 v8-migration narrowing"
      contains: "Partial<UserProfile>"
  key_links:
    - from: "useAssets init else-branch reads A+B"
      to: "single Promise.all + one cancelled guard"
      via: "const [printerCount, packagingCount] = await Promise.all([...])"
      pattern: "Promise\\.all\\(\\["
    - from: "createQuote tx settings read (C3)"
      to: "validated narrowing"
      via: "JSON.parse(...) as Partial<UserProfile> + typeof parsed.nextQuoteNumber === 'number'"
      pattern: "as Partial<UserProfile>"
---

<objective>
Close the two HYG-12 cleanups that live in the database layer — batching independent `useAssets` init reads with `Promise.all` (HYG-12.2) and tightening the two real unsafe `as UserProfile` casts to validated narrowing (HYG-12.3 C1 + C3). Both files (`useDatabase.ts`, `database.ts`) are touched, and C3 + HYG-12.2 BOTH live in `useDatabase.ts`, so they are planned together (cannot parallel-worktree the same file).

**HYG-12.2 batch-scope decision (LOCKED): batch A+B only.** RESEARCH §HYG-12.2 + Open Question 1 recommend batching `printerCount` (line 84) + `packagingCount` (line 92) — the two independent COUNT reads in the `else` branch — into one `Promise.all`, collapsing their two `if (cancelled) return` guards into a single post-batch check. Reads C/D/E (the Bambu/Ender-3/PLA-Pure migration probes at lines 102, 120, 138) are LEFT SEQUENTIAL: they are interleaved with conditional `bulkPut` writes inside `!flagRan` guards, and the `!flagRan ? db.get(...) : Promise.resolve(null)` short-circuit needed to batch them adds MEDIUM-risk complexity that exceeds the value for a droppable STRETCH phase. A+B-only still satisfies the audit ("batched `useAssets` init reads (`Promise.all`)"). DO NOT batch the writes — only the two reads.

**HYG-12.3 scope (LOCKED): tighten C1 + C3 only.** C1 = `database.ts:133` (v8 migration `(JSON.parse(...) as UserProfile).currency`). C3 = `useDatabase.ts:1211` (createQuote `(JSON.parse(...) as UserProfile).nextQuoteNumber`). Both become `JSON.parse(...) as Partial<UserProfile>` + a `typeof` check, mirroring the v9 validated-narrowing pattern already at `database.ts:179-181`. The other 7 casts (C2, C4–C9) are build-safe or validation-guarded — RESEARCH says LEAVE UNCHANGED; backfill.ts signature changes are OUT OF SCOPE. Optionally add a one-line annotation comment on C4/C5/C6 only if trivially safe, but do not force-narrow them.

Purpose: Make startup IDB reads consistent (one Promise.all where reads are independent) and make the two stored-JSON casts defensive (a corrupt/partial settings row can no longer flow `undefined`/wrong-shape into `currency` or `nextQuoteNumber`). Hard constraint: ZERO behavioral change for valid data; the only observable difference is strictly-safer fallback on corrupt rows (which already fell back elsewhere). Preserve the `cancelled` guard semantics, the try/catch error behavior, and the `'USD'` / `1` defaults.
Output: `useDatabase.ts` (batched A+B + C3 narrowing) and `database.ts` (C1 narrowing), plus source-contract test assertions in their `.test` siblings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-code-health-stretch/37-RESEARCH.md

<interfaces>
<!-- All confirmed by direct code read. -->

<!-- src/hooks/useDatabase.ts — useAssets init() (lines 70-157): -->
<!--   line 71: let cancelled = false; -->
<!--   line 75-76: const count = await db.materials.count(); if (cancelled) return;   <- gates the else branch; LEAVE sequential. -->
<!--   line 84-85: const printerCount = await db.materials.where('category').equals('printer').count(); if (cancelled) return;   <- read A -->
<!--   line 87-89: if (printerCount === 0) { await db.materials.bulkPut(defaultPrinterAssets); }   <- write after A -->
<!--   line 92-93: const packagingCount = await db.materials.where('category').equals('packaging').count(); if (cancelled) return;   <- read B -->
<!--   line 95-98: if (packagingCount === 0) { ...filter...; await db.materials.bulkPut(packagingDefaults); }   <- write after B -->
<!--   line 102, 120, 138: reads C/D/E (Bambu catalog / Ender-3 v3 / PLA Pure probes) — LEAVE SEQUENTIAL per locked decision. -->
<!--   line 146-149: catch (error) { console.error('Error initializing assets:', error); if (!cancelled) setError('Could not load your materials library — check available storage.'); }   <- error behavior MUST be preserved. -->
<!-- BATCH TARGET: move reads A (84) + B (92) into one Promise.all; place ONE `if (cancelled) return;` immediately after; KEEP the two `if (printerCount === 0)` / `if (packagingCount === 0)` bulkPut writes sequential and AFTER the cancelled check.
     Result shape: const [printerCount, packagingCount] = await Promise.all([
       db.materials.where('category').equals('printer').count(),
       db.materials.where('category').equals('packaging').count(),
     ]); if (cancelled) return; -->

<!-- src/hooks/useDatabase.ts — createQuote tx (lines ~1203-1216): -->
<!--   line 1207: const settingsRow = await tx.table('settings').get('userProfile'); -->
<!--   line 1208: let nextNum = 1; -->
<!--   line 1209-1216: if (settingsRow) { try { nextNum = (JSON.parse(settingsRow.value) as UserProfile).nextQuoteNumber ?? 1; } catch { /* corrupt -> nextNum = 1 */ } }   <- C3: narrow to Partial<UserProfile> + typeof check. -->
<!--   UserProfile type imported at line 4 of useDatabase.ts. -->

<!-- src/db/database.ts — v8 upgrade callback (lines 123-144): -->
<!--   line 129: const settingsRow = await tx.table('settings').get('userProfile'); -->
<!--   line 130: let currency = 'USD'; -->
<!--   line 131-139: if (settingsRow) { try { currency = (JSON.parse(settingsRow.value) as UserProfile).currency; } catch (err) { console.error('[v8 migration] skipped — settings unreadable:', err); } }   <- C1: narrow to Partial<UserProfile> + typeof check before assigning. -->
<!--   THE CANONICAL v9 PATTERN to mirror is in the SAME file at lines 178-185:
         const parsed = JSON.parse(settingsRow.value) as Partial<UserProfile>;
         if (typeof parsed.currency !== 'string' || parsed.currency.length === 0) return;
         userCurrency = parsed.currency; -->
<!--   UserProfile type is imported/in-scope in database.ts (used by the v9 stanza already). -->

<!-- Test harnesses (confirmed by code read): -->
<!--   src/hooks/useDatabase.test.ts: vitest + vi mocks; createQuote DATA-02 tests at line 211+ replicate the tx body; type imports at line 11. Add a SOURCE-CONTRACT block (readFileSync the source) for the Promise.all batch + C3 narrowing — the init() path is not exercised by an existing test (RESEARCH §Validation: source-scan only). -->
<!--   src/db/database.test.ts: vitest; isUserProfile validator tests at line 187+. Add a source-contract assertion that the v8 currency read uses `Partial<UserProfile>` + a typeof check (C1). -->
<!-- Use readFileSync source-contract assertions for the batching + narrowing (no runtime harness exercises useAssets init). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Batch useAssets init reads A+B with Promise.all (HYG-12.2) + narrow the C3 createQuote cast (HYG-12.3)</name>
  <files>src/hooks/useDatabase.ts, src/hooks/useDatabase.test.ts</files>
  <read_first>
    - src/hooks/useDatabase.ts (lines 70-157 the full useAssets init; specifically 84-98 reads A+B + their bulkPut writes + cancelled guards; 1203-1216 the createQuote tx settings read = C3)
    - .planning/phases/37-code-health-stretch/37-RESEARCH.md §HYG-12.2 (batch A+B; Pitfall 2 — consolidate the cancelled check, do NOT batch writes) and §HYG-12.3 C3 (the Partial<UserProfile> + typeof narrowing)
    - src/db/database.ts lines 178-185 (the v9 validated-narrowing pattern this C3 change mirrors)
    - src/hooks/useDatabase.test.ts (the createQuote DATA-02 block + the type imports; the readFileSync idiom if present, else add it like CostCalculator.test.tsx does)
  </read_first>
  <behavior>
    - Source-contract: useDatabase.ts contains a `Promise.all([` that batches `.equals('printer').count()` and `.equals('packaging').count()` (the two reads now run concurrently).
    - Source-contract: between read A and read B there is no longer a standalone `const packagingCount = await db.materials...count();` followed by its own `if (cancelled) return;` — the two reads are destructured from one `Promise.all` with a SINGLE following `if (cancelled) return;`.
    - Source-contract: the `if (printerCount === 0)` and `if (packagingCount === 0)` bulkPut blocks remain (writes not batched) and sit AFTER the single post-batch cancelled check.
    - Source-contract: the `catch` block still logs `'Error initializing assets:'` and calls `setError(...)` (error behavior preserved).
    - Source-contract (C3): createQuote parses settings as `Partial<UserProfile>` and guards `nextQuoteNumber` with a `typeof ... === 'number'` check (no `as UserProfile`), falling back to `1`.
  </behavior>
  <action>
    HYG-12.2 (batch A+B): Replace the sequential reads at lines 84-93 so that `printerCount` and `packagingCount` are read via a single `Promise.all`:
    `const [printerCount, packagingCount] = await Promise.all([ db.materials.where('category').equals('printer').count(), db.materials.where('category').equals('packaging').count() ]); if (cancelled) return;`
    Remove the now-redundant intermediate `if (cancelled) return;` that sat between A and B (RESEARCH Pitfall 2 — it becomes unreachable). KEEP the two conditional `bulkPut` writes (`if (printerCount === 0)` / `if (packagingCount === 0)`) exactly as they are, sequential, AFTER the single cancelled check. Do NOT touch reads C/D/E or any write (locked A+B-only decision). Add a short comment noting reads A+B are independent and batched; writes stay sequential and cancel-checked.

    HYG-12.3 C3 (createQuote, lines 1209-1216): replace `nextNum = (JSON.parse(settingsRow.value) as UserProfile).nextQuoteNumber ?? 1;` with the validated-narrowing form mirroring the v9 pattern:
    `const parsedProfile = JSON.parse(settingsRow.value) as Partial<UserProfile>; nextNum = typeof parsedProfile.nextQuoteNumber === 'number' ? parsedProfile.nextQuoteNumber : 1;`
    Keep it inside the existing `try { ... } catch { /* corrupt -> nextNum stays 1 */ }` so corrupt JSON still falls through to `1` (unchanged). The valid-data path is identical: a stored `nextQuoteNumber` number flows through as before.

    Add source-contract assertions to `src/hooks/useDatabase.test.ts` (new `describe('HYG-12.2 / HYG-12.3 batching + narrowing', ...)`) using `readFileSync` of `useDatabase.ts`. Assert each `behavior` item with `expect(SRC).toContain(...)` / `.not.toContain(...)` (e.g. `not.toContain('as UserProfile).nextQuoteNumber')`).
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useDatabase.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `useAssets` init reads `printerCount` + `packagingCount` via one `Promise.all`, with a single post-batch `if (cancelled) return;`; both bulkPut writes remain sequential and cancel-checked after it.
    - Reads C/D/E and all writes are unchanged; the catch block's log + setError are unchanged.
    - createQuote parses settings as `Partial<UserProfile>` with a `typeof parsedProfile.nextQuoteNumber === 'number'` guard and `1` fallback; no `as UserProfile` remains at that site.
    - New source-contract tests pass; `npx vitest run src/hooks/useDatabase.test.ts` exits 0.
  </acceptance_criteria>
  <done>The two independent useAssets init reads are batched via Promise.all with consolidated cancel handling; createQuote's nextQuoteNumber read is validated-narrowed; valid-data behavior is identical; source-contract tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Narrow the C1 v8-migration currency cast in database.ts (HYG-12.3)</name>
  <files>src/db/database.ts, src/db/database.test.ts</files>
  <read_first>
    - src/db/database.ts (lines 123-144 the v8 upgrade callback with the C1 cast at 133; AND lines 178-185 the v9 validated-narrowing pattern to mirror)
    - .planning/phases/37-code-health-stretch/37-RESEARCH.md §HYG-12.3 C1 (the exact before/after) and Pitfall 3 (the v8 migration already ran for existing users; the change only makes the fallback explicit — safe to ship)
    - src/db/database.test.ts (lines 187+ isUserProfile block + the vitest/readFileSync conventions)
  </read_first>
  <behavior>
    - Source-contract: the v8 migration no longer contains `(JSON.parse(settingsRow.value) as UserProfile).currency`.
    - Source-contract: the v8 migration parses settings as `Partial<UserProfile>` and only assigns `currency` when `typeof parsed.currency === 'string' && parsed.currency.length > 0` (otherwise the pre-set `'USD'` default stands).
    - Source-contract: the existing `try/catch` with the `'[v8 migration] skipped — settings unreadable:'` log is preserved.
  </behavior>
  <action>
    In the v8 upgrade callback (lines 131-139), replace `currency = (JSON.parse(settingsRow.value) as UserProfile).currency;` with the validated-narrowing form mirroring the v9 stanza (lines 178-181):
    `const parsedV8 = JSON.parse(settingsRow.value) as Partial<UserProfile>; if (typeof parsedV8.currency === 'string' && parsedV8.currency.length > 0) { currency = parsedV8.currency; }`
    Leave the `let currency = 'USD';` default (line 130) and the surrounding `try { ... } catch (err) { console.error('[v8 migration] skipped — settings unreadable:', err); }` exactly as-is — a corrupt or partial row now cleanly leaves `currency` at `'USD'` instead of assigning `undefined`. This is strictly-safer, behavior-identical for valid rows (RESEARCH Pitfall 3).

    Do NOT change C2 (line 179, already `Partial<UserProfile>`), nor any `as Currency` cast (C4–C9). backfill.ts is out of scope.

    Add a source-contract assertion to `src/db/database.test.ts` (new `describe('HYG-12.3 C1 v8 migration currency narrowing', ...)`) via `readFileSync` of `database.ts`: assert it contains `as Partial<UserProfile>` near the v8 path and `typeof parsedV8.currency === 'string'`, and `.not.toContain('as UserProfile).currency')`.
  </action>
  <verify>
    <automated>npx vitest run src/db/database.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The v8 migration uses `Partial<UserProfile>` + `typeof parsedV8.currency === 'string' && length > 0` before assigning `currency`; no `as UserProfile).currency` remains.
    - The `'USD'` default and the v8 try/catch log are unchanged.
    - C2 and all `as Currency` casts are untouched.
    - New source-contract test passes; `npx vitest run src/db/database.test.ts` exits 0.
  </acceptance_criteria>
  <done>The v8-migration currency read is validated-narrowed to match the v9 pattern; valid-row behavior is identical and corrupt rows safely keep the 'USD' default; source-contract test green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + typecheck regression gate (behavioral + type equivalence)</name>
  <files>src/hooks/useDatabase.ts, src/db/database.ts</files>
  <read_first>
    - .planning/phases/37-code-health-stretch/37-RESEARCH.md §Validation Architecture (phase gate = full suite green + tsc -b clean) and §Common Pitfalls Pitfall 4 (`tsc -b`, NOT `tsc --noEmit`)
  </read_first>
  <action>
    Run the full vitest suite plus a strict TypeScript build to prove no behavioral or type regression from the Promise.all batch + the two cast narrowings. Both must be clean before this plan is done. If a pre-existing database/useDatabase test fails, confirm whether it asserts on a string that legitimately moved (update the assertion to the new correct source) versus a real behavior break (fix the code, never weaken the test). Use `tsc -b` per CLAUDE.md so `noUnusedLocals`/`noUnusedParameters` are enforced as Vercel does — in particular confirm `parsedProfile` / `parsedV8` are used and no stale local is left behind.
  </action>
  <verify>
    <automated>npx vitest run && tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0 (entire suite).
    - `tsc -b` exits 0 with no new errors.
  </acceptance_criteria>
  <done>Whole test suite green and the project type-checks clean under `tsc -b` — the batching + narrowing changes are proven behavior- and type-preserving.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored settings JSON → app state | `tx.table('settings').get('userProfile')` returns attacker-uncontrolled but potentially corrupt/partial-shape JSON parsed at the v8 migration and createQuote sites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | v8 migration `currency` read (database.ts:133) | mitigate | C1: narrow to `Partial<UserProfile>` + `typeof currency === 'string' && length > 0` before assigning; corrupt/partial row keeps the `'USD'` default instead of propagating `undefined` |
| T-37-02 | Tampering | createQuote `nextQuoteNumber` read (useDatabase.ts:1211) | mitigate | C3: narrow to `Partial<UserProfile>` + `typeof nextQuoteNumber === 'number'`; non-number falls back to `1`; still wrapped in the existing try/catch |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH §Package Legitimacy Audit: N/A); no new dependency attack surface introduced |

No new externally-reachable surface, network, or auth boundary is added. The Promise.all batching (HYG-12.2) is a read-ordering change only (no new input parsing). ASVS V5 (Input Validation) is the relevant domain and is satisfied by the two narrowing changes above.
</threat_model>

<verification>
- `npx vitest run src/hooks/useDatabase.test.ts` green (HYG-12.2 batch + C3 narrowing source-contracts).
- `npx vitest run src/db/database.test.ts` green (C1 narrowing source-contract).
- `npx vitest run` (full suite) green.
- `tsc -b` clean.
- Source-contract: one `Promise.all` batching printer+packaging counts with a single cancelled check; no `as UserProfile).currency` / `as UserProfile).nextQuoteNumber` remain; reads C/D/E and all writes unchanged.
- Manual (phase smoke test): open the app fresh (asset seeding still runs), then create a quote — quote number allocates exactly as before with no console errors.
</verification>

<success_criteria>
- HYG-12.2: `useAssets` init batches the two independent COUNT reads (printer + packaging) into one `Promise.all` with a consolidated post-batch `cancelled` check; writes remain sequential and cancel-checked (A+B-only scope, locked).
- HYG-12.3: the two real unsafe `as UserProfile` casts (C1 v8 migration, C3 createQuote) are replaced with `Partial<UserProfile>` + `typeof` validated narrowing matching the v9 pattern; the other 7 casts are untouched (out of scope per RESEARCH).
- Zero behavioral change for valid data (strictly-safer fallback on corrupt rows), proven by the full suite staying green and `tsc -b` clean.
</success_criteria>

<output>
Create `.planning/phases/37-code-health-stretch/37-02-SUMMARY.md` when done.
</output>
