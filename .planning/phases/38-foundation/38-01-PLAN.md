---
phase: 38-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CostCalculator.equivalence.test.tsx
autonomous: true
requirements: [FOUND-01]
status: EXECUTED 2026-08-27 (commit bc6c90c) — drafted retroactively to document the gate
must_haves:
  truths:
    - "A mount-based test file drives the REAL CostCalculator through DOM events and asserts exact rendered pricing values"
    - "The consecutive same-field-edit interaction (the v1.9 PERF-11 revert trigger) is a locked scenario"
    - "The file imports no post-split module, so it runs unmodified before and after the split"
    - "All 12 scenarios pass against the pre-split component; a deliberately-stale assertion fails (negative control verified)"
  artifacts:
    - path: "src/components/CostCalculator.equivalence.test.tsx"
      provides: "12 golden scenarios: baseline margin-derivation, multi-filament+labor+failure clamp, price/margin/profit branches, consecutive same-field edits, trueCost rebase, Etsy USD + EUR FX fee, marketplace round-trip, trueCost<=0 guard, editingJob rebase, dropoff shipping"
      contains: "THE PERF-11 CASE"
---

<objective>
Land the FOUND-01 correctness gate BEFORE any refactor commit: an output-equivalence
harness that locks the CostCalculator's observable pricing behavior as frozen golden
values. Every subsequent Phase 38 commit must keep this file green; a golden-value
change is a regression to fix, never a value to re-capture.
</objective>

<context>
@.planning/phases/38-foundation/38-RESEARCH.md  (harness mechanics + scenario table)

Key mechanics (verified in execution):
- Raw `createRoot` + `act` (project convention; no @testing-library).
- Wrapped in `ToastProvider` (useToast throws outside one).
- `vi.mock('../hooks/useStockEvents')` — the component's only Dexie edge.
- Complex form state established via the component's own sessionStorage restore
  path (`costCalculatorForm` seed), typed edits via native value setter + `input` event.
- Fixtures chosen for hand-checkable numbers (baseline trueCost = $2.20).
- matchMedia/scrollIntoView stubbed only in the editingJob scenario.
</context>

<verification>
- `npx vitest run src/components/CostCalculator.equivalence.test.tsx` → 12 passed. ✓
- Full suite 831 passed, `tsc -b` clean, lint 0 errors. ✓
- Negative control: mutating S4's expected margin makes the run fail. ✓ (reverted)
</verification>
