---
phase: 38-foundation
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - src/hooks/useCostForm.ts
  - src/components/CostCalculator.tsx
autonomous: true
requirements: [FOUND-01]
status: DRAFT (pre-authored 2026-08-27 from the research spike — review at /gsd:plan-phase time)
must_haves:
  truths:
    - "All non-pricing form state (~21 useState fields), the filament/material row helpers, clearForm, and the sessionStorage persistence effect live in useCostForm"
    - "The persistence effect still writes ONE JSON blob under 'costCalculatorForm' with the exact same keys (stored sessions from before the split restore identically)"
    - "Pricing state (margin/profit/price/lastEdited) does NOT move in this plan — it stays in the component until 38-03 replaces it with the reducer"
    - "The equivalence harness passes with zero golden changes"
  artifacts:
    - path: "src/hooks/useCostForm.ts"
      provides: "FORM_STORAGE_KEY, getStoredValue, nextRowKey/FilamentRow/MaterialRow, all non-pricing useStates with lazy sessionStorage initializers, row add/remove/update helpers, printer auto-select effect, clearForm, persistence effect"
      contains: "FORM_STORAGE_KEY"
    - path: "src/components/CostCalculator.tsx"
      provides: "Consumes useCostForm; behavior unchanged"
      contains: "useCostForm"
  key_links:
    - from: "CostCalculator"
      to: "useCostForm"
      via: "const form = useCostForm({ printerInstances, userCurrency, defaultProfitMargin, editingJob })"
      pattern: "useCostForm\\("
---

<objective>
Extract `useCostForm` — the first seam of the FOUND-01 split. Pure state relocation,
zero behavioral change, gated by the 38-01 harness.

Scope per 38-RESEARCH source map: lines 1–75 (helpers/types), 161–246 (non-pricing
useStates + row helpers), 249–284 (persistence effect), 341–351 (printer auto-select),
613–639 (clearForm), 854–874 (row mutators). Pricing fields stay put for 38-03.
</objective>

<context>
@.planning/phases/38-foundation/38-RESEARCH.md

Constraints:
- The persistence effect reads pricing values too. Until 38-03, pass them INTO the
  hook (persistence input arg) or keep persistence keyed off a merged object the
  orchestrator assembles — either way the stored JSON shape must stay byte-compatible.
  Recommended: `useCostForm` accepts `{ pricingSnapshot }` and owns the effect,
  keeping `editingJob`-guard semantics identical.
- The editingJob population effect does NOT move (it writes pricing state; stays in
  the component with its documented eslint-disable).
- `rowKeySeq` stays module-scope in the new file; seeded uids are re-keyed on
  restore exactly as today.
- clearForm resets margin to `defaultProfitMargin` — until 38-03 that setter is
  still component-owned; clearForm takes a callback or the orchestrator composes
  `clearForm` from hook-clear + pricing-clear. Keep observable behavior identical.
</context>

<verification>
- Equivalence harness: 12/12, zero golden edits.
- Full Vitest suite green; `tsc -b` clean; `npm run lint` 0 errors.
- Manual: restore-from-sessionStorage round-trip (fill form, reload dev server, fields persist).
</verification>
