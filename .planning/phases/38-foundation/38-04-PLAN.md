---
phase: 38-foundation
plan: 04
type: execute
wave: 4
depends_on: [38-03]
files_modified:
  - src/components/calculator/JobDetailsSection.tsx
  - src/components/calculator/PostProcessingSection.tsx
  - src/components/calculator/ShippingSection.tsx
  - src/components/calculator/PricingSection.tsx
  - src/components/calculator/CostSummarySection.tsx
  - src/components/CostCalculator.tsx
  - src/components/CostCalculator.test.tsx
autonomous: true
requirements: [FOUND-01]
status: DRAFT (pre-authored 2026-08-27 from the research spike — review at /gsd:plan-phase time)
must_haves:
  truths:
    - "CostCalculator.tsx is a ~150 LOC orchestrator: editingJob banner + population effect, handleSaveJob/finishAfterSave, and section composition — no business logic, no cost math"
    - "The five section components are purely presentational: grouped props in (form/derived/pricing objects), callbacks out, zero useState of their own"
    - "Every useId/label↔input association, aria attribute, and allow-raw-html exception moves intact (lint:no-raw-html stays green)"
    - "The equivalence harness passes with zero golden changes"
    - "The stale source-contract tests in CostCalculator.test.tsx are re-pointed at the files that now contain their contracts (they grep source text)"
  artifacts:
    - path: "src/components/calculator/PricingSection.tsx"
      provides: "Marketplace picker + three interlinked fields + tax-rate override + summary tiles + author-min warning; onChange handlers dispatch usePricingInterlink actions"
      contains: "Selling Price"
    - path: "src/components/CostCalculator.tsx"
      provides: "~150 LOC orchestrator"
      contains: "JobDetailsSection"
  key_links:
    - from: "orchestrator hooks (useCostForm/useCostDerivedValues/usePricingInterlink)"
      to: "section components"
      via: "grouped props (form=, derived=, pricing=) — NOT 30 scalars, NOT context"
      pattern: "derived="
---

<objective>
Final FOUND-01 cut: move JSX lines ~876–1814 into five section components under
src/components/calculator/ and shrink CostCalculator.tsx to the orchestrator.

Section boundaries (per 38-RESEARCH source map):
1. JobDetailsSection — G-code import, name/printer, model row, filament rows, print params (consumes stockByAssetId).
2. PostProcessingSection — materials-used rows.
3. ShippingSection — method/distance/override, packaging rows, totals.
4. PricingSection — marketplace + interlinked fields + tax + summary tiles.
5. CostSummarySection — breakdown table, fee note, fixed costs, break-even, Etsy CollapsibleSection, Save/Clear.
Split Etsy into a sixth component only if CostSummarySection exceeds ~300 LOC.
</objective>

<context>
@.planning/phases/38-foundation/38-RESEARCH.md

Constraints:
- Pure JSX relocation — no markup, class, id-wiring, or copy changes. The
  equivalence harness locates inputs by label text and asserts rendered strings;
  it is the arbiter.
- useId calls move to the section that renders the field (ids are opaque; only
  the label↔input pairing matters).
- `useStockEvents` moves into JobDetailsSection (its only consumer) — keeps the
  harness's single Dexie mock point valid.
- CostCalculator.test.tsx source-contract tests read CostCalculator.tsx text
  (scroll-to-banner, D-21 tax notes). Re-point each readFileSync at the file that
  now carries the contract; do not weaken assertions.
- New files fall under the 38-03 scoped exhaustive-deps error override
  (src/components/calculator/**) — write clean effects or none at all.
</context>

<verification>
- Equivalence harness 12/12, zero golden edits.
- `wc -l src/components/CostCalculator.tsx` ≈ 150 (hard cap 200).
- Full suite + tsc + lint (incl. lint:no-raw-html) green; bundle gate (300 KB gz) green.
- Human UAT (phase close): PITFALLS C-03 checklist — rapid two-field typing,
  same-field consecutive typing, marketplace toggle, edit-job flow, G-code import,
  clear form, save/update round-trip.
</verification>
