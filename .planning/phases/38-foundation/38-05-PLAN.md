---
phase: 38-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/App.tab-url.test.tsx
autonomous: true
requirements: [FOUND-03]
status: DRAFT (pre-authored 2026-08-27 from the research spike — review at /gsd:plan-phase time)
must_haves:
  truths:
    - "The active tab derives from the URL: /app?tab=jobs renders the Jobs tab; all six Tab values work; unknown/missing tab renders calculator WITHOUT a redirect loop"
    - "Tab switches push history entries, so browser Back/Forward move between tabs instead of leaving /app"
    - "Bare /app and the PWA start_url '/' keep working unchanged (no tab param required)"
    - "Desktop (__IS_TAURI__ routes App at '/') behaves identically — the mechanism is search-param-only, no route changes"
    - "Tab switching does NOT scroll to top (ScrollToTop keys on [pathname, hash] — verified at research time — and must stay that way)"
    - "The roving-tabindex arrow-key contract (A11Y C-01) is unchanged: arrow keys move tab + focus exactly as before"
  artifacts:
    - path: "src/App.tsx"
      provides: "useSearchParams-backed activeTab + setActiveTab wrapper; Tab type guard; all 6 setActiveTab call sites migrated"
      contains: "useSearchParams"
    - path: "src/App.tab-url.test.tsx"
      provides: "Mount tests under MemoryRouter: param→tab render, tab click→param, unknown param fallback, back/forward simulation, no-param default"
      contains: "MemoryRouter"
  key_links:
    - from: "tab buttons + arrow-key handler + onViewJobs + edit-job jump"
      to: "URL search params"
      via: "setActiveTab(t) → setSearchParams({ tab: t }) (push); initial normalize uses { replace: true }"
      pattern: "setSearchParams"
---

<objective>
FOUND-03: move the active app tab from `useState<Tab>` (App.tsx:42) into the URL
(`/app?tab=calculator|jobs|materials|customers|reports|settings`), so browser
Back/Forward navigate tabs instead of exiting to the marketing site, and tab
links are bookmarkable/shareable.

Independent of plans 02–04 (different file) — runs in wave 1 alongside 38-01.
</objective>

<context>
@.planning/phases/38-foundation/38-RESEARCH.md  (§FOUND-03)

Verified at research time:
- App already renders inside BrowserRouter (main.tsx); `/` (Tauri) and `/app`
  (web) both mount App — search-param mechanism covers both.
- 6 `setActiveTab` call sites: tab buttons, arrow-key roving handler, onViewJobs
  (calculator toast + save flow), edit-job jump (~App.tsx:158).
- ScrollToTop effect deps are `[pathname, hash]` — search-param navigation does
  not scroll. Add a regression comment there so nobody "fixes" it to full location.
- PWA start_url is '/' (vite.config.ts:35) — untouched.

Design:
- `const tab = isTab(raw) ? raw : 'calculator'` — never redirect on bad input.
- Guard no-op sets (`if (t === activeTab) return`) to avoid history spam from
  the roving-tabindex handler.
- Tests use MemoryRouter with initialEntries + navigate(-1) to simulate Back
  (project convention mount tooling: createRoot + act).
</context>

<verification>
- New App.tab-url tests green; existing App.a11y tests green (tablist contract intact).
- Full suite + tsc + lint green.
- Human UAT: Back/Forward through 3 tabs on web; PWA install opens calculator;
  desktop build tab nav unchanged; hard-refresh on /app?tab=reports lands on Reports.
</verification>
