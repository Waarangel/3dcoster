# v1.9 Hardening — Release-Diff Code Review

**Reviewed:** 2026-06-26 (pre-release gate; required before any `v1.9.0` tag)
**Diff:** `git diff v1.8.0..HEAD -- src/` (Phases 34–37: FIX-01..04, A11Y-10..15, PERF-09..11, HYG-11/12)
**Method:** 4 parallel reviewers — React/hooks (perf equivalence), accessibility (WCAG 2.2), general quality/conventions, security/safety.
**Scope of source change:** 27 files, ~600 lines production source (bulk is new tests).

## Verdict

Initial: **BLOCK** — 1 HIGH/CRITICAL regression. **All findings now fixed** on `v1.9-hardening`; full suite 759 passed/0 fail, `tsc -b` 0, lint 0 errors. Ready to re-confirm.

## Findings & resolutions

| # | Sev | Finding | Resolution |
|---|-----|---------|------------|
| 1 | HIGH/CRIT | **PERF-11 dep-trim desynced pricing fields** on consecutive same-field edits (re-editing price/margin/profit twice left `lastEdited` + `trueCost` unchanged → effect never re-fired → profit/margin went stale vs the entered value). Core pricing-math regression; PERF-11's source-string tests couldn't catch it. | **Reverted PERF-11** (restored full deps `[trueCost, lastEdited, profitMarginPercent, targetProfit, sellingPrice]`). Tests flipped to lock correct deps + guard re-trim. PERF-11 **deferred to v2.0** (the benign double-render belongs with the CostCalculator God-component split, audit 6.1). |
| 2 | MED | **SettingsModal arrow keys focused the panel**, not the active tab — a second arrow press did nothing, defeating the A11Y-10 roving-tabindex contract. (Was a LOCKED Phase-35 decision; conflicted with the requirement.) | Arrow handler now keeps focus on the newly-selected **tab**; panel still reachable via Tab. Source-contract test added. |
| 3 | LOW | ResetAssetsModal printer copy said "N **custom** printers" but reset **preserves** custom printers and the count was all printers. | Reworded: "restore the default printer list. Your custom printers are kept." Material count unchanged (it's exact). Test updated. |
| 4 | LOW | Redundant double error surface on reset failure (parent banner + modal). | Removed the `resetError` banner/state in AssetLibrary; ResetAssetsModal is the single error owner. |
| 5 | LOW | `createQuote` empty `catch {}` swallowed a JSON parse failure (silent quote-number reset). | Added a `console.error` matching the v8/v9/v10 migration handlers. |
| 6 | LOW (post-ship) | `npm audit` = 19 vulns, **all devDependencies** (Vite/rollup/babel) — zero runtime exposure. | Run `npm audit fix` after v1.9 ships. Not a gate. |

## Verified genuinely correct (no action)

PERF-09 (useQuotes lift — 1 subscription not 160, Reopen wiring intact), PERF-10 (materialsById Map, identical output), HYG-11/12 (immutability + Promise.all + cast narrowing — semantics preserved), all of Phase 34 (PWA reload, ScrollToTop, ResetAssetsModal can't auto-fire), A11Y-11..15 (all ARIA relationships resolve; the v1.4.4-class `aria-controls` regression is specifically absent). Security: no XSS, no `dangerouslySetInnerHTML`, no secrets, `isSafeHttpUrl` guard intact, JSON-parse narrowing safe.

## Net requirement impact

- v1.9 delivers **14 of 15** requirements. **PERF-11 deferred to v2.0** (reverted as a regression; the issue it targeted is benign).
- Still pending before tag: UAT (incl. runtime-only items from Phase 35/36 verifications), then version bumps + CHANGELOG + explicit per-tag approval.
