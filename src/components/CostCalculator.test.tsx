import { describe, it } from 'vitest';

// ---------------------------------------------------------------------------
// Wave 0 scaffold — Phase 16 CostCalculator PDF button disabled-state
// @testing-library/react is NOT installed in this project (package.json has 0 matches).
// Using it.todo() scaffold pattern — plan 04 (UI wiring) will activate these tests
// once the Generate PDF button is wired into CostCalculator.tsx.
// ---------------------------------------------------------------------------

describe('CostCalculator — Generate PDF button', () => {
  it.todo('Generate PDF button is disabled when sellingPrice <= 0');
  it.todo('Generate PDF button is enabled when editing a saved job with sellingPrice > 0');
});
